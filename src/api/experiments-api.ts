/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { MlCreateExperimentRequest } from '../models';
// @ts-ignore
import type { MlExperiment } from '../models';
// @ts-ignore
import type { MlExperimentRun } from '../models';
// @ts-ignore
import type { MlGetRunMetrics200Response } from '../models';
// @ts-ignore
import type { MlListExperimentRuns200Response } from '../models';
// @ts-ignore
import type { MlListExperiments200Response } from '../models';
// @ts-ignore
import type { MlStartExperimentRunRequest } from '../models';
/**
 * ExperimentsApi - axios parameter creator
 * @export
 */
export const ExperimentsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns {data, total} ordered by project then id. Scoped to the org resolved from the validated principal — a distinct org is a distinct physical store, so no query here can reach another tenant\'s rows — and further narrowed to the caller\'s project scope when the credential carries one. A principal with NO project scope sees the org\'s experiments across all of its projects, which is the answer a reader most often expects to be filtered and is not.  Requires a validated principal; refuses without one rather than answering an empty list.
         * @summary Every experiment in the caller\'s org, with its variants, status and decision.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Experiments: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/experiments`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads the registry row only — the definition and the decision, never live measurements. Assignment lives in the flags plane and outcomes in analytics; this is the value that names both.  Scoped to the caller\'s org and project from the validated principal, so another tenant\'s experiment of the same id is simply not found. An id that is not a legal slug is answered the same way, without a store read — the shape check and the existence check are one answer, so neither leaks the other.
         * @summary One experiment\'s definition and lifecycle: variants, weights, control arm, status and winner.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ExperimentsById: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudGetV1ExperimentsById', 'id', id)
            const localVarPath = `/v1/experiments/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Evaluates the experiment\'s assignment flag for the `subject` in the query and answers {experiment, subject, variant, on, payload}. The bucketing is a deterministic hash of the subject, so the same subject gets the same arm on every call for as long as the flag definition is unchanged — and this is a pure READ: it records nothing. In particular it does NOT record an exposure. The caller\'s SDK must emit the experiment\'s exposure event itself, or the analysis has an empty denominator and every arm measures zero.  `subject` is required. `props` may carry a JSON object of person properties for targeting; a `props` value that is not valid JSON is dropped silently rather than refused, so a malformed one changes the bucketing without saying so.  An empty `variant` with `on` false is not an error — it means the flag returned nothing for this subject, so the subject is not enrolled. A flags engine that is unavailable refuses rather than defaulting to an arm. Requires a validated principal, and the experiment must exist in the caller\'s org and project.
         * @summary The variant one subject is bucketed into, and the payload that variant carries.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ExperimentsByIdAssign: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudGetV1ExperimentsByIdAssign', 'id', id)
            const localVarPath = `/v1/experiments/{id}/assign`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers {\"ok\":true,\"subsystem\":\"experiments\"} unconditionally. It proves exactly one thing — that this binary registered the experiments routes and is dispatching them — and deliberately no more: it reads no principal, opens no per-org registry, and touches neither the flags engine nor the analytics plane, so a 200 here says nothing about whether a given tenant\'s store will open or whether an analysis can run. It is the only route on this surface that needs no org.  The static path is registered ahead of the /:id read, so it always wins the first-match scan. `health` is a legal experiment id, which means an experiment created under that id can never be fetched by id — pick another.
         * @summary Whether the experiments subsystem is mounted and serving in this process.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ExperimentsHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/experiments/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Registers the experiment AND writes its multivariate assignment flag, in that order, so the arms start bucketing subjects the moment this returns 201 — the flag is created active at 100% rollout, with each variant weighted as declared. There is no separate start call; creating IS starting.  The body names the experiment (`id`, a slug that is claimed once), what it measures (`metricEvent`, required; `exposureEvent` defaults to the SDK\'s `$feature_flag_called` marker), the unit it assigns (`subjectKind`: user, org, session or audience — user by default), and at least two `variants`. A variant carries an opaque `payload` this primitive never interprets: a feature config, an ad-creative id, a subject line, a model id. Weights that are all zero become an even split; otherwise they must sum to 100. At most one variant may be flagged `control`; with none, the first arm is the baseline. `flagKey` defaults to `exp_<id>`.  Requires a validated principal, and refuses without one. The org and project are taken from that principal and the creator is stamped from the credential — none of the three is a body field, so an experiment cannot be filed against another tenant. An id already used in this project is a conflict, never a silent overwrite: re-creating would stomp the assignment flag of a run in progress.  It fails closed on the flag write. An experiment whose assignment flag does not exist would assign nobody, so if that write fails nothing is registered.
         * @summary Create a controlled experiment and put its assignment flag live.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Experiments: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/experiments`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads per-subject outcomes from the analytics plane over a window, folds them into per-variant samples, and returns each arm\'s exposed count, conversions, rate, lift versus control, two-proportion z, two-tailed p-value and whether it clears alpha. Arms with no data still appear with zero exposed, so the read is complete over the experiment\'s declared arms; the control arm sorts first. The pooled-variance estimator is used and the p-value is exact; a degenerate comparison (an empty arm, no variance) answers z 0 and p 1 — not significant, never an error.  The window is `start`/`end` in RFC3339 if given, otherwise the last `days` (1 to 365, 30 by default) up to now. `alpha` overrides the 0.05 two-tailed threshold when it lies strictly between 0 and 1; anything else leaves the default in place.  Only EXPOSED subjects are counted, and each is joined to its arm by re-evaluating the assignment flag AT ANALYSIS TIME — not from what was in force during the window. That is the one rule to get right: analyzing an experiment after its winner has been promoted re-buckets every subject into the promoted arm, collapsing the control to zero exposed and making the result meaningless. Read the analysis before deciding. A subject the flag cannot place is dropped rather than allowed to poison the fold.  `winner` in the response is ADVISORY — the significant, control-beating arm with the highest rate, or empty when inconclusive. It promotes nothing; the decision is a separate, explicit act.  Every plane read is scoped to the caller\'s org. Per-variant samples are also written to the research evidence plane as immutable `ab` rows, best-effort: the analysis is still returned if that write fails, because the samples are recomputable, and the failure is logged rather than swallowed.
         * @summary Per-variant conversion, lift and statistical significance against the control arm.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ExperimentsByIdAnalyze: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudPostV1ExperimentsByIdAnalyze', 'id', id)
            const localVarPath = `/v1/experiments/{id}/analyze`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Rewrites the assignment flag so the named `winner` serves 100% of the rollout and every other arm 0%, preserving the flag\'s targeting groups and payloads, then stamps the experiment decided with the winner, the deciding credential and the time. This is a production behaviour change that takes effect immediately for every subject the flag evaluates.  Requires an ORG ADMIN of the caller\'s own org — a stricter gate than the rest of this surface, matching the flags write plane, because promoting is a flag write. The admin check runs AFTER the experiment is found, so a caller from another tenant is answered not-found rather than forbidden and learns nothing about what exists.  `winner` is required and must name one of the experiment\'s own variants. An experiment whose assignment flag has gone missing is a conflict rather than a silent no-op — there is nothing to promote.  Deciding is NOT terminal. A second call re-promotes a different variant and re-stamps the row; the status stays decided and the previous winner is overwritten with no record that it was ever chosen. Nothing here reverts the flag to its original weights either, so an experiment cannot be un-decided through this route — restoring a split means writing the flag definition back through the flags plane.
         * @summary Promote one variant to the whole rollout and record who decided.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ExperimentsByIdDecide: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudPostV1ExperimentsByIdDecide', 'id', id)
            const localVarPath = `/v1/experiments/{id}/decide`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary Create an experiment
         * @param {MlCreateExperimentRequest} mlCreateExperimentRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlCreateExperiment: async (mlCreateExperimentRequest: MlCreateExperimentRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'mlCreateExperimentRequest' is not null or undefined
            assertParamExists('mlCreateExperiment', 'mlCreateExperimentRequest', mlCreateExperimentRequest)
            const localVarPath = `/v1/ml/experiments`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(mlCreateExperimentRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary Get run metrics
         * @param {string} experimentId 
         * @param {string} runId 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlGetRunMetrics: async (experimentId: string, runId: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'experimentId' is not null or undefined
            assertParamExists('mlGetRunMetrics', 'experimentId', experimentId)
            // verify required parameter 'runId' is not null or undefined
            assertParamExists('mlGetRunMetrics', 'runId', runId)
            const localVarPath = `/v1/ml/experiments/{experiment_id}/runs/{run_id}/metrics`
                .replace(`{${"experiment_id"}}`, encodeURIComponent(String(experimentId)))
                .replace(`{${"run_id"}}`, encodeURIComponent(String(runId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary List experiment runs
         * @param {string} experimentId 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlListExperimentRuns: async (experimentId: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'experimentId' is not null or undefined
            assertParamExists('mlListExperimentRuns', 'experimentId', experimentId)
            const localVarPath = `/v1/ml/experiments/{experiment_id}/runs`
                .replace(`{${"experiment_id"}}`, encodeURIComponent(String(experimentId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary List experiments
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlListExperiments: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/ml/experiments`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Log metrics for a running experiment.
         * @summary Log metrics
         * @param {string} experimentId 
         * @param {string} runId 
         * @param {{ [key: string]: number; }} requestBody 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlLogMetrics: async (experimentId: string, runId: string, requestBody: { [key: string]: number; }, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'experimentId' is not null or undefined
            assertParamExists('mlLogMetrics', 'experimentId', experimentId)
            // verify required parameter 'runId' is not null or undefined
            assertParamExists('mlLogMetrics', 'runId', runId)
            // verify required parameter 'requestBody' is not null or undefined
            assertParamExists('mlLogMetrics', 'requestBody', requestBody)
            const localVarPath = `/v1/ml/experiments/{experiment_id}/runs/{run_id}/metrics`
                .replace(`{${"experiment_id"}}`, encodeURIComponent(String(experimentId)))
                .replace(`{${"run_id"}}`, encodeURIComponent(String(runId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(requestBody, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary Start an experiment run
         * @param {string} experimentId 
         * @param {MlStartExperimentRunRequest} mlStartExperimentRunRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlStartExperimentRun: async (experimentId: string, mlStartExperimentRunRequest: MlStartExperimentRunRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'experimentId' is not null or undefined
            assertParamExists('mlStartExperimentRun', 'experimentId', experimentId)
            // verify required parameter 'mlStartExperimentRunRequest' is not null or undefined
            assertParamExists('mlStartExperimentRun', 'mlStartExperimentRunRequest', mlStartExperimentRunRequest)
            const localVarPath = `/v1/ml/experiments/{experiment_id}/runs`
                .replace(`{${"experiment_id"}}`, encodeURIComponent(String(experimentId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(mlStartExperimentRunRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * ExperimentsApi - functional programming interface
 * @export
 */
export const ExperimentsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = ExperimentsApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns {data, total} ordered by project then id. Scoped to the org resolved from the validated principal — a distinct org is a distinct physical store, so no query here can reach another tenant\'s rows — and further narrowed to the caller\'s project scope when the credential carries one. A principal with NO project scope sees the org\'s experiments across all of its projects, which is the answer a reader most often expects to be filtered and is not.  Requires a validated principal; refuses without one rather than answering an empty list.
         * @summary Every experiment in the caller\'s org, with its variants, status and decision.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Experiments(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Experiments(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.cloudGetV1Experiments']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads the registry row only — the definition and the decision, never live measurements. Assignment lives in the flags plane and outcomes in analytics; this is the value that names both.  Scoped to the caller\'s org and project from the validated principal, so another tenant\'s experiment of the same id is simply not found. An id that is not a legal slug is answered the same way, without a store read — the shape check and the existence check are one answer, so neither leaks the other.
         * @summary One experiment\'s definition and lifecycle: variants, weights, control arm, status and winner.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ExperimentsById(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ExperimentsById(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.cloudGetV1ExperimentsById']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Evaluates the experiment\'s assignment flag for the `subject` in the query and answers {experiment, subject, variant, on, payload}. The bucketing is a deterministic hash of the subject, so the same subject gets the same arm on every call for as long as the flag definition is unchanged — and this is a pure READ: it records nothing. In particular it does NOT record an exposure. The caller\'s SDK must emit the experiment\'s exposure event itself, or the analysis has an empty denominator and every arm measures zero.  `subject` is required. `props` may carry a JSON object of person properties for targeting; a `props` value that is not valid JSON is dropped silently rather than refused, so a malformed one changes the bucketing without saying so.  An empty `variant` with `on` false is not an error — it means the flag returned nothing for this subject, so the subject is not enrolled. A flags engine that is unavailable refuses rather than defaulting to an arm. Requires a validated principal, and the experiment must exist in the caller\'s org and project.
         * @summary The variant one subject is bucketed into, and the payload that variant carries.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ExperimentsByIdAssign(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ExperimentsByIdAssign(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.cloudGetV1ExperimentsByIdAssign']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers {\"ok\":true,\"subsystem\":\"experiments\"} unconditionally. It proves exactly one thing — that this binary registered the experiments routes and is dispatching them — and deliberately no more: it reads no principal, opens no per-org registry, and touches neither the flags engine nor the analytics plane, so a 200 here says nothing about whether a given tenant\'s store will open or whether an analysis can run. It is the only route on this surface that needs no org.  The static path is registered ahead of the /:id read, so it always wins the first-match scan. `health` is a legal experiment id, which means an experiment created under that id can never be fetched by id — pick another.
         * @summary Whether the experiments subsystem is mounted and serving in this process.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ExperimentsHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ExperimentsHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.cloudGetV1ExperimentsHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Registers the experiment AND writes its multivariate assignment flag, in that order, so the arms start bucketing subjects the moment this returns 201 — the flag is created active at 100% rollout, with each variant weighted as declared. There is no separate start call; creating IS starting.  The body names the experiment (`id`, a slug that is claimed once), what it measures (`metricEvent`, required; `exposureEvent` defaults to the SDK\'s `$feature_flag_called` marker), the unit it assigns (`subjectKind`: user, org, session or audience — user by default), and at least two `variants`. A variant carries an opaque `payload` this primitive never interprets: a feature config, an ad-creative id, a subject line, a model id. Weights that are all zero become an even split; otherwise they must sum to 100. At most one variant may be flagged `control`; with none, the first arm is the baseline. `flagKey` defaults to `exp_<id>`.  Requires a validated principal, and refuses without one. The org and project are taken from that principal and the creator is stamped from the credential — none of the three is a body field, so an experiment cannot be filed against another tenant. An id already used in this project is a conflict, never a silent overwrite: re-creating would stomp the assignment flag of a run in progress.  It fails closed on the flag write. An experiment whose assignment flag does not exist would assign nobody, so if that write fails nothing is registered.
         * @summary Create a controlled experiment and put its assignment flag live.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1Experiments(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1Experiments(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.cloudPostV1Experiments']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads per-subject outcomes from the analytics plane over a window, folds them into per-variant samples, and returns each arm\'s exposed count, conversions, rate, lift versus control, two-proportion z, two-tailed p-value and whether it clears alpha. Arms with no data still appear with zero exposed, so the read is complete over the experiment\'s declared arms; the control arm sorts first. The pooled-variance estimator is used and the p-value is exact; a degenerate comparison (an empty arm, no variance) answers z 0 and p 1 — not significant, never an error.  The window is `start`/`end` in RFC3339 if given, otherwise the last `days` (1 to 365, 30 by default) up to now. `alpha` overrides the 0.05 two-tailed threshold when it lies strictly between 0 and 1; anything else leaves the default in place.  Only EXPOSED subjects are counted, and each is joined to its arm by re-evaluating the assignment flag AT ANALYSIS TIME — not from what was in force during the window. That is the one rule to get right: analyzing an experiment after its winner has been promoted re-buckets every subject into the promoted arm, collapsing the control to zero exposed and making the result meaningless. Read the analysis before deciding. A subject the flag cannot place is dropped rather than allowed to poison the fold.  `winner` in the response is ADVISORY — the significant, control-beating arm with the highest rate, or empty when inconclusive. It promotes nothing; the decision is a separate, explicit act.  Every plane read is scoped to the caller\'s org. Per-variant samples are also written to the research evidence plane as immutable `ab` rows, best-effort: the analysis is still returned if that write fails, because the samples are recomputable, and the failure is logged rather than swallowed.
         * @summary Per-variant conversion, lift and statistical significance against the control arm.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1ExperimentsByIdAnalyze(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1ExperimentsByIdAnalyze(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.cloudPostV1ExperimentsByIdAnalyze']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Rewrites the assignment flag so the named `winner` serves 100% of the rollout and every other arm 0%, preserving the flag\'s targeting groups and payloads, then stamps the experiment decided with the winner, the deciding credential and the time. This is a production behaviour change that takes effect immediately for every subject the flag evaluates.  Requires an ORG ADMIN of the caller\'s own org — a stricter gate than the rest of this surface, matching the flags write plane, because promoting is a flag write. The admin check runs AFTER the experiment is found, so a caller from another tenant is answered not-found rather than forbidden and learns nothing about what exists.  `winner` is required and must name one of the experiment\'s own variants. An experiment whose assignment flag has gone missing is a conflict rather than a silent no-op — there is nothing to promote.  Deciding is NOT terminal. A second call re-promotes a different variant and re-stamps the row; the status stays decided and the previous winner is overwritten with no record that it was ever chosen. Nothing here reverts the flag to its original weights either, so an experiment cannot be un-decided through this route — restoring a split means writing the flag definition back through the flags plane.
         * @summary Promote one variant to the whole rollout and record who decided.
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1ExperimentsByIdDecide(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1ExperimentsByIdDecide(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.cloudPostV1ExperimentsByIdDecide']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary Create an experiment
         * @param {MlCreateExperimentRequest} mlCreateExperimentRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlCreateExperiment(mlCreateExperimentRequest: MlCreateExperimentRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlExperiment>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlCreateExperiment(mlCreateExperimentRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.mlCreateExperiment']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary Get run metrics
         * @param {string} experimentId 
         * @param {string} runId 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlGetRunMetrics(experimentId: string, runId: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlGetRunMetrics200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlGetRunMetrics(experimentId, runId, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.mlGetRunMetrics']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary List experiment runs
         * @param {string} experimentId 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlListExperimentRuns(experimentId: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlListExperimentRuns200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlListExperimentRuns(experimentId, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.mlListExperimentRuns']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary List experiments
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlListExperiments(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlListExperiments200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlListExperiments(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.mlListExperiments']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Log metrics for a running experiment.
         * @summary Log metrics
         * @param {string} experimentId 
         * @param {string} runId 
         * @param {{ [key: string]: number; }} requestBody 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlLogMetrics(experimentId: string, runId: string, requestBody: { [key: string]: number; }, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlLogMetrics(experimentId, runId, requestBody, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.mlLogMetrics']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary Start an experiment run
         * @param {string} experimentId 
         * @param {MlStartExperimentRunRequest} mlStartExperimentRunRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlStartExperimentRun(experimentId: string, mlStartExperimentRunRequest: MlStartExperimentRunRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlExperimentRun>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlStartExperimentRun(experimentId, mlStartExperimentRunRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ExperimentsApi.mlStartExperimentRun']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * ExperimentsApi - factory interface
 * @export
 */
export const ExperimentsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = ExperimentsApiFp(configuration)
    return {
        /**
         * Returns {data, total} ordered by project then id. Scoped to the org resolved from the validated principal — a distinct org is a distinct physical store, so no query here can reach another tenant\'s rows — and further narrowed to the caller\'s project scope when the credential carries one. A principal with NO project scope sees the org\'s experiments across all of its projects, which is the answer a reader most often expects to be filtered and is not.  Requires a validated principal; refuses without one rather than answering an empty list.
         * @summary Every experiment in the caller\'s org, with its variants, status and decision.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Experiments(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1Experiments(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads the registry row only — the definition and the decision, never live measurements. Assignment lives in the flags plane and outcomes in analytics; this is the value that names both.  Scoped to the caller\'s org and project from the validated principal, so another tenant\'s experiment of the same id is simply not found. An id that is not a legal slug is answered the same way, without a store read — the shape check and the existence check are one answer, so neither leaks the other.
         * @summary One experiment\'s definition and lifecycle: variants, weights, control arm, status and winner.
         * @param {ExperimentsApiCloudGetV1ExperimentsByIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ExperimentsById(requestParameters: ExperimentsApiCloudGetV1ExperimentsByIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1ExperimentsById(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Evaluates the experiment\'s assignment flag for the `subject` in the query and answers {experiment, subject, variant, on, payload}. The bucketing is a deterministic hash of the subject, so the same subject gets the same arm on every call for as long as the flag definition is unchanged — and this is a pure READ: it records nothing. In particular it does NOT record an exposure. The caller\'s SDK must emit the experiment\'s exposure event itself, or the analysis has an empty denominator and every arm measures zero.  `subject` is required. `props` may carry a JSON object of person properties for targeting; a `props` value that is not valid JSON is dropped silently rather than refused, so a malformed one changes the bucketing without saying so.  An empty `variant` with `on` false is not an error — it means the flag returned nothing for this subject, so the subject is not enrolled. A flags engine that is unavailable refuses rather than defaulting to an arm. Requires a validated principal, and the experiment must exist in the caller\'s org and project.
         * @summary The variant one subject is bucketed into, and the payload that variant carries.
         * @param {ExperimentsApiCloudGetV1ExperimentsByIdAssignRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ExperimentsByIdAssign(requestParameters: ExperimentsApiCloudGetV1ExperimentsByIdAssignRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1ExperimentsByIdAssign(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers {\"ok\":true,\"subsystem\":\"experiments\"} unconditionally. It proves exactly one thing — that this binary registered the experiments routes and is dispatching them — and deliberately no more: it reads no principal, opens no per-org registry, and touches neither the flags engine nor the analytics plane, so a 200 here says nothing about whether a given tenant\'s store will open or whether an analysis can run. It is the only route on this surface that needs no org.  The static path is registered ahead of the /:id read, so it always wins the first-match scan. `health` is a legal experiment id, which means an experiment created under that id can never be fetched by id — pick another.
         * @summary Whether the experiments subsystem is mounted and serving in this process.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ExperimentsHealth(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1ExperimentsHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Registers the experiment AND writes its multivariate assignment flag, in that order, so the arms start bucketing subjects the moment this returns 201 — the flag is created active at 100% rollout, with each variant weighted as declared. There is no separate start call; creating IS starting.  The body names the experiment (`id`, a slug that is claimed once), what it measures (`metricEvent`, required; `exposureEvent` defaults to the SDK\'s `$feature_flag_called` marker), the unit it assigns (`subjectKind`: user, org, session or audience — user by default), and at least two `variants`. A variant carries an opaque `payload` this primitive never interprets: a feature config, an ad-creative id, a subject line, a model id. Weights that are all zero become an even split; otherwise they must sum to 100. At most one variant may be flagged `control`; with none, the first arm is the baseline. `flagKey` defaults to `exp_<id>`.  Requires a validated principal, and refuses without one. The org and project are taken from that principal and the creator is stamped from the credential — none of the three is a body field, so an experiment cannot be filed against another tenant. An id already used in this project is a conflict, never a silent overwrite: re-creating would stomp the assignment flag of a run in progress.  It fails closed on the flag write. An experiment whose assignment flag does not exist would assign nobody, so if that write fails nothing is registered.
         * @summary Create a controlled experiment and put its assignment flag live.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Experiments(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1Experiments(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads per-subject outcomes from the analytics plane over a window, folds them into per-variant samples, and returns each arm\'s exposed count, conversions, rate, lift versus control, two-proportion z, two-tailed p-value and whether it clears alpha. Arms with no data still appear with zero exposed, so the read is complete over the experiment\'s declared arms; the control arm sorts first. The pooled-variance estimator is used and the p-value is exact; a degenerate comparison (an empty arm, no variance) answers z 0 and p 1 — not significant, never an error.  The window is `start`/`end` in RFC3339 if given, otherwise the last `days` (1 to 365, 30 by default) up to now. `alpha` overrides the 0.05 two-tailed threshold when it lies strictly between 0 and 1; anything else leaves the default in place.  Only EXPOSED subjects are counted, and each is joined to its arm by re-evaluating the assignment flag AT ANALYSIS TIME — not from what was in force during the window. That is the one rule to get right: analyzing an experiment after its winner has been promoted re-buckets every subject into the promoted arm, collapsing the control to zero exposed and making the result meaningless. Read the analysis before deciding. A subject the flag cannot place is dropped rather than allowed to poison the fold.  `winner` in the response is ADVISORY — the significant, control-beating arm with the highest rate, or empty when inconclusive. It promotes nothing; the decision is a separate, explicit act.  Every plane read is scoped to the caller\'s org. Per-variant samples are also written to the research evidence plane as immutable `ab` rows, best-effort: the analysis is still returned if that write fails, because the samples are recomputable, and the failure is logged rather than swallowed.
         * @summary Per-variant conversion, lift and statistical significance against the control arm.
         * @param {ExperimentsApiCloudPostV1ExperimentsByIdAnalyzeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ExperimentsByIdAnalyze(requestParameters: ExperimentsApiCloudPostV1ExperimentsByIdAnalyzeRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1ExperimentsByIdAnalyze(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Rewrites the assignment flag so the named `winner` serves 100% of the rollout and every other arm 0%, preserving the flag\'s targeting groups and payloads, then stamps the experiment decided with the winner, the deciding credential and the time. This is a production behaviour change that takes effect immediately for every subject the flag evaluates.  Requires an ORG ADMIN of the caller\'s own org — a stricter gate than the rest of this surface, matching the flags write plane, because promoting is a flag write. The admin check runs AFTER the experiment is found, so a caller from another tenant is answered not-found rather than forbidden and learns nothing about what exists.  `winner` is required and must name one of the experiment\'s own variants. An experiment whose assignment flag has gone missing is a conflict rather than a silent no-op — there is nothing to promote.  Deciding is NOT terminal. A second call re-promotes a different variant and re-stamps the row; the status stays decided and the previous winner is overwritten with no record that it was ever chosen. Nothing here reverts the flag to its original weights either, so an experiment cannot be un-decided through this route — restoring a split means writing the flag definition back through the flags plane.
         * @summary Promote one variant to the whole rollout and record who decided.
         * @param {ExperimentsApiCloudPostV1ExperimentsByIdDecideRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ExperimentsByIdDecide(requestParameters: ExperimentsApiCloudPostV1ExperimentsByIdDecideRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1ExperimentsByIdDecide(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary Create an experiment
         * @param {ExperimentsApiMlCreateExperimentRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlCreateExperiment(requestParameters: ExperimentsApiMlCreateExperimentRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlExperiment> {
            return localVarFp.mlCreateExperiment(requestParameters.mlCreateExperimentRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary Get run metrics
         * @param {ExperimentsApiMlGetRunMetricsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlGetRunMetrics(requestParameters: ExperimentsApiMlGetRunMetricsRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlGetRunMetrics200Response> {
            return localVarFp.mlGetRunMetrics(requestParameters.experimentId, requestParameters.runId, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary List experiment runs
         * @param {ExperimentsApiMlListExperimentRunsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlListExperimentRuns(requestParameters: ExperimentsApiMlListExperimentRunsRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlListExperimentRuns200Response> {
            return localVarFp.mlListExperimentRuns(requestParameters.experimentId, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary List experiments
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlListExperiments(options?: RawAxiosRequestConfig): AxiosPromise<MlListExperiments200Response> {
            return localVarFp.mlListExperiments(options).then((request) => request(axios, basePath));
        },
        /**
         * Log metrics for a running experiment.
         * @summary Log metrics
         * @param {ExperimentsApiMlLogMetricsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlLogMetrics(requestParameters: ExperimentsApiMlLogMetricsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.mlLogMetrics(requestParameters.experimentId, requestParameters.runId, requestParameters.requestBody, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary Start an experiment run
         * @param {ExperimentsApiMlStartExperimentRunRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlStartExperimentRun(requestParameters: ExperimentsApiMlStartExperimentRunRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlExperimentRun> {
            return localVarFp.mlStartExperimentRun(requestParameters.experimentId, requestParameters.mlStartExperimentRunRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudGetV1ExperimentsById operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiCloudGetV1ExperimentsByIdRequest
 */
export interface ExperimentsApiCloudGetV1ExperimentsByIdRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiCloudGetV1ExperimentsById
     */
    readonly id: string
}

/**
 * Request parameters for cloudGetV1ExperimentsByIdAssign operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiCloudGetV1ExperimentsByIdAssignRequest
 */
export interface ExperimentsApiCloudGetV1ExperimentsByIdAssignRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiCloudGetV1ExperimentsByIdAssign
     */
    readonly id: string
}

/**
 * Request parameters for cloudPostV1ExperimentsByIdAnalyze operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiCloudPostV1ExperimentsByIdAnalyzeRequest
 */
export interface ExperimentsApiCloudPostV1ExperimentsByIdAnalyzeRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiCloudPostV1ExperimentsByIdAnalyze
     */
    readonly id: string
}

/**
 * Request parameters for cloudPostV1ExperimentsByIdDecide operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiCloudPostV1ExperimentsByIdDecideRequest
 */
export interface ExperimentsApiCloudPostV1ExperimentsByIdDecideRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiCloudPostV1ExperimentsByIdDecide
     */
    readonly id: string
}

/**
 * Request parameters for mlCreateExperiment operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiMlCreateExperimentRequest
 */
export interface ExperimentsApiMlCreateExperimentRequest {
    /**
     * 
     * @type {MlCreateExperimentRequest}
     * @memberof ExperimentsApiMlCreateExperiment
     */
    readonly mlCreateExperimentRequest: MlCreateExperimentRequest
}

/**
 * Request parameters for mlGetRunMetrics operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiMlGetRunMetricsRequest
 */
export interface ExperimentsApiMlGetRunMetricsRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiMlGetRunMetrics
     */
    readonly experimentId: string

    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiMlGetRunMetrics
     */
    readonly runId: string
}

/**
 * Request parameters for mlListExperimentRuns operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiMlListExperimentRunsRequest
 */
export interface ExperimentsApiMlListExperimentRunsRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiMlListExperimentRuns
     */
    readonly experimentId: string
}

/**
 * Request parameters for mlLogMetrics operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiMlLogMetricsRequest
 */
export interface ExperimentsApiMlLogMetricsRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiMlLogMetrics
     */
    readonly experimentId: string

    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiMlLogMetrics
     */
    readonly runId: string

    /**
     * 
     * @type {{ [key: string]: number; }}
     * @memberof ExperimentsApiMlLogMetrics
     */
    readonly requestBody: { [key: string]: number; }
}

/**
 * Request parameters for mlStartExperimentRun operation in ExperimentsApi.
 * @export
 * @interface ExperimentsApiMlStartExperimentRunRequest
 */
export interface ExperimentsApiMlStartExperimentRunRequest {
    /**
     * 
     * @type {string}
     * @memberof ExperimentsApiMlStartExperimentRun
     */
    readonly experimentId: string

    /**
     * 
     * @type {MlStartExperimentRunRequest}
     * @memberof ExperimentsApiMlStartExperimentRun
     */
    readonly mlStartExperimentRunRequest: MlStartExperimentRunRequest
}

/**
 * ExperimentsApi - object-oriented interface
 * @export
 * @class ExperimentsApi
 * @extends {BaseAPI}
 */
export class ExperimentsApi extends BaseAPI {
    /**
     * Returns {data, total} ordered by project then id. Scoped to the org resolved from the validated principal — a distinct org is a distinct physical store, so no query here can reach another tenant\'s rows — and further narrowed to the caller\'s project scope when the credential carries one. A principal with NO project scope sees the org\'s experiments across all of its projects, which is the answer a reader most often expects to be filtered and is not.  Requires a validated principal; refuses without one rather than answering an empty list.
     * @summary Every experiment in the caller\'s org, with its variants, status and decision.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public cloudGetV1Experiments(options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).cloudGetV1Experiments(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads the registry row only — the definition and the decision, never live measurements. Assignment lives in the flags plane and outcomes in analytics; this is the value that names both.  Scoped to the caller\'s org and project from the validated principal, so another tenant\'s experiment of the same id is simply not found. An id that is not a legal slug is answered the same way, without a store read — the shape check and the existence check are one answer, so neither leaks the other.
     * @summary One experiment\'s definition and lifecycle: variants, weights, control arm, status and winner.
     * @param {ExperimentsApiCloudGetV1ExperimentsByIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public cloudGetV1ExperimentsById(requestParameters: ExperimentsApiCloudGetV1ExperimentsByIdRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).cloudGetV1ExperimentsById(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Evaluates the experiment\'s assignment flag for the `subject` in the query and answers {experiment, subject, variant, on, payload}. The bucketing is a deterministic hash of the subject, so the same subject gets the same arm on every call for as long as the flag definition is unchanged — and this is a pure READ: it records nothing. In particular it does NOT record an exposure. The caller\'s SDK must emit the experiment\'s exposure event itself, or the analysis has an empty denominator and every arm measures zero.  `subject` is required. `props` may carry a JSON object of person properties for targeting; a `props` value that is not valid JSON is dropped silently rather than refused, so a malformed one changes the bucketing without saying so.  An empty `variant` with `on` false is not an error — it means the flag returned nothing for this subject, so the subject is not enrolled. A flags engine that is unavailable refuses rather than defaulting to an arm. Requires a validated principal, and the experiment must exist in the caller\'s org and project.
     * @summary The variant one subject is bucketed into, and the payload that variant carries.
     * @param {ExperimentsApiCloudGetV1ExperimentsByIdAssignRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public cloudGetV1ExperimentsByIdAssign(requestParameters: ExperimentsApiCloudGetV1ExperimentsByIdAssignRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).cloudGetV1ExperimentsByIdAssign(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers {\"ok\":true,\"subsystem\":\"experiments\"} unconditionally. It proves exactly one thing — that this binary registered the experiments routes and is dispatching them — and deliberately no more: it reads no principal, opens no per-org registry, and touches neither the flags engine nor the analytics plane, so a 200 here says nothing about whether a given tenant\'s store will open or whether an analysis can run. It is the only route on this surface that needs no org.  The static path is registered ahead of the /:id read, so it always wins the first-match scan. `health` is a legal experiment id, which means an experiment created under that id can never be fetched by id — pick another.
     * @summary Whether the experiments subsystem is mounted and serving in this process.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public cloudGetV1ExperimentsHealth(options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).cloudGetV1ExperimentsHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Registers the experiment AND writes its multivariate assignment flag, in that order, so the arms start bucketing subjects the moment this returns 201 — the flag is created active at 100% rollout, with each variant weighted as declared. There is no separate start call; creating IS starting.  The body names the experiment (`id`, a slug that is claimed once), what it measures (`metricEvent`, required; `exposureEvent` defaults to the SDK\'s `$feature_flag_called` marker), the unit it assigns (`subjectKind`: user, org, session or audience — user by default), and at least two `variants`. A variant carries an opaque `payload` this primitive never interprets: a feature config, an ad-creative id, a subject line, a model id. Weights that are all zero become an even split; otherwise they must sum to 100. At most one variant may be flagged `control`; with none, the first arm is the baseline. `flagKey` defaults to `exp_<id>`.  Requires a validated principal, and refuses without one. The org and project are taken from that principal and the creator is stamped from the credential — none of the three is a body field, so an experiment cannot be filed against another tenant. An id already used in this project is a conflict, never a silent overwrite: re-creating would stomp the assignment flag of a run in progress.  It fails closed on the flag write. An experiment whose assignment flag does not exist would assign nobody, so if that write fails nothing is registered.
     * @summary Create a controlled experiment and put its assignment flag live.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public cloudPostV1Experiments(options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).cloudPostV1Experiments(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads per-subject outcomes from the analytics plane over a window, folds them into per-variant samples, and returns each arm\'s exposed count, conversions, rate, lift versus control, two-proportion z, two-tailed p-value and whether it clears alpha. Arms with no data still appear with zero exposed, so the read is complete over the experiment\'s declared arms; the control arm sorts first. The pooled-variance estimator is used and the p-value is exact; a degenerate comparison (an empty arm, no variance) answers z 0 and p 1 — not significant, never an error.  The window is `start`/`end` in RFC3339 if given, otherwise the last `days` (1 to 365, 30 by default) up to now. `alpha` overrides the 0.05 two-tailed threshold when it lies strictly between 0 and 1; anything else leaves the default in place.  Only EXPOSED subjects are counted, and each is joined to its arm by re-evaluating the assignment flag AT ANALYSIS TIME — not from what was in force during the window. That is the one rule to get right: analyzing an experiment after its winner has been promoted re-buckets every subject into the promoted arm, collapsing the control to zero exposed and making the result meaningless. Read the analysis before deciding. A subject the flag cannot place is dropped rather than allowed to poison the fold.  `winner` in the response is ADVISORY — the significant, control-beating arm with the highest rate, or empty when inconclusive. It promotes nothing; the decision is a separate, explicit act.  Every plane read is scoped to the caller\'s org. Per-variant samples are also written to the research evidence plane as immutable `ab` rows, best-effort: the analysis is still returned if that write fails, because the samples are recomputable, and the failure is logged rather than swallowed.
     * @summary Per-variant conversion, lift and statistical significance against the control arm.
     * @param {ExperimentsApiCloudPostV1ExperimentsByIdAnalyzeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public cloudPostV1ExperimentsByIdAnalyze(requestParameters: ExperimentsApiCloudPostV1ExperimentsByIdAnalyzeRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).cloudPostV1ExperimentsByIdAnalyze(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Rewrites the assignment flag so the named `winner` serves 100% of the rollout and every other arm 0%, preserving the flag\'s targeting groups and payloads, then stamps the experiment decided with the winner, the deciding credential and the time. This is a production behaviour change that takes effect immediately for every subject the flag evaluates.  Requires an ORG ADMIN of the caller\'s own org — a stricter gate than the rest of this surface, matching the flags write plane, because promoting is a flag write. The admin check runs AFTER the experiment is found, so a caller from another tenant is answered not-found rather than forbidden and learns nothing about what exists.  `winner` is required and must name one of the experiment\'s own variants. An experiment whose assignment flag has gone missing is a conflict rather than a silent no-op — there is nothing to promote.  Deciding is NOT terminal. A second call re-promotes a different variant and re-stamps the row; the status stays decided and the previous winner is overwritten with no record that it was ever chosen. Nothing here reverts the flag to its original weights either, so an experiment cannot be un-decided through this route — restoring a split means writing the flag definition back through the flags plane.
     * @summary Promote one variant to the whole rollout and record who decided.
     * @param {ExperimentsApiCloudPostV1ExperimentsByIdDecideRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public cloudPostV1ExperimentsByIdDecide(requestParameters: ExperimentsApiCloudPostV1ExperimentsByIdDecideRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).cloudPostV1ExperimentsByIdDecide(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary Create an experiment
     * @param {ExperimentsApiMlCreateExperimentRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public mlCreateExperiment(requestParameters: ExperimentsApiMlCreateExperimentRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).mlCreateExperiment(requestParameters.mlCreateExperimentRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary Get run metrics
     * @param {ExperimentsApiMlGetRunMetricsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public mlGetRunMetrics(requestParameters: ExperimentsApiMlGetRunMetricsRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).mlGetRunMetrics(requestParameters.experimentId, requestParameters.runId, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary List experiment runs
     * @param {ExperimentsApiMlListExperimentRunsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public mlListExperimentRuns(requestParameters: ExperimentsApiMlListExperimentRunsRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).mlListExperimentRuns(requestParameters.experimentId, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary List experiments
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public mlListExperiments(options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).mlListExperiments(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Log metrics for a running experiment.
     * @summary Log metrics
     * @param {ExperimentsApiMlLogMetricsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public mlLogMetrics(requestParameters: ExperimentsApiMlLogMetricsRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).mlLogMetrics(requestParameters.experimentId, requestParameters.runId, requestParameters.requestBody, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary Start an experiment run
     * @param {ExperimentsApiMlStartExperimentRunRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ExperimentsApi
     */
    public mlStartExperimentRun(requestParameters: ExperimentsApiMlStartExperimentRunRequest, options?: RawAxiosRequestConfig) {
        return ExperimentsApiFp(this.configuration).mlStartExperimentRun(requestParameters.experimentId, requestParameters.mlStartExperimentRunRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

