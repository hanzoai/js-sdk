/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { ChallengeView } from '../models';
// @ts-ignore
import type { SlotView } from '../models';
// @ts-ignore
import type { ValidatorClaim } from '../models';
// @ts-ignore
import type { ValidatorList } from '../models';
/**
 * ValidatorApi - axios parameter creator
 * @export
 */
export const ValidatorApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
         * @summary Returns the validator slots the caller\'s org has claimed.
         * @param {string} [limit] Limit is how many slots to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 200; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getValidator: async (limit?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/validator`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
         * @summary Returns one claimed validator slot, scoped to the caller\'s org.
         * @param {string} tokenId TokenID is the slot\&#39;s GenesisNFT token id, from the path, as a decimal string. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getValidatorByTokenid: async (tokenId: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'tokenId' is not null or undefined
            assertParamExists('getValidatorByTokenid', 'tokenId', tokenId)
            const localVarPath = `/v1/validator/{tokenId}`
                .replace(`{${"tokenId"}}`, encodeURIComponent(String(tokenId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validator verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validator before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
         * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
         * @param {string} [tokenId] TokenID is the Validator-tier GenesisNFT token id, as a decimal string in the &#x60;?tokenId&#x3D;&#x60; query. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getValidatorChallenge: async (tokenId?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/validator/challenge`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (tokenId !== undefined) {
                localVarQueryParameter['tokenId'] = tokenId;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
         * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
         * @param {ValidatorClaim} validatorClaim 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postValidator: async (validatorClaim: ValidatorClaim, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'validatorClaim' is not null or undefined
            assertParamExists('postValidator', 'validatorClaim', validatorClaim)
            const localVarPath = `/v1/validator`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(validatorClaim, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * ValidatorApi - functional programming interface
 * @export
 */
export const ValidatorApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = ValidatorApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
         * @summary Returns the validator slots the caller\'s org has claimed.
         * @param {string} [limit] Limit is how many slots to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 200; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getValidator(limit?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ValidatorList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getValidator(limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorApi.getValidator']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
         * @summary Returns one claimed validator slot, scoped to the caller\'s org.
         * @param {string} tokenId TokenID is the slot\&#39;s GenesisNFT token id, from the path, as a decimal string. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getValidatorByTokenid(tokenId: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SlotView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getValidatorByTokenid(tokenId, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorApi.getValidatorByTokenid']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validator verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validator before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
         * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
         * @param {string} [tokenId] TokenID is the Validator-tier GenesisNFT token id, as a decimal string in the &#x60;?tokenId&#x3D;&#x60; query. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getValidatorChallenge(tokenId?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ChallengeView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getValidatorChallenge(tokenId, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorApi.getValidatorChallenge']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
         * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
         * @param {ValidatorClaim} validatorClaim 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postValidator(validatorClaim: ValidatorClaim, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SlotView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postValidator(validatorClaim, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorApi.postValidator']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * ValidatorApi - factory interface
 * @export
 */
export const ValidatorApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = ValidatorApiFp(configuration)
    return {
        /**
         * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
         * @summary Returns the validator slots the caller\'s org has claimed.
         * @param {ValidatorApiGetValidatorRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getValidator(requestParameters: ValidatorApiGetValidatorRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<ValidatorList> {
            return localVarFp.getValidator(requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
         * @summary Returns one claimed validator slot, scoped to the caller\'s org.
         * @param {ValidatorApiGetValidatorByTokenidRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getValidatorByTokenid(requestParameters: ValidatorApiGetValidatorByTokenidRequest, options?: RawAxiosRequestConfig): AxiosPromise<SlotView> {
            return localVarFp.getValidatorByTokenid(requestParameters.tokenId, options).then((request) => request(axios, basePath));
        },
        /**
         * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validator verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validator before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
         * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
         * @param {ValidatorApiGetValidatorChallengeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getValidatorChallenge(requestParameters: ValidatorApiGetValidatorChallengeRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<ChallengeView> {
            return localVarFp.getValidatorChallenge(requestParameters.tokenId, options).then((request) => request(axios, basePath));
        },
        /**
         * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
         * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
         * @param {ValidatorApiPostValidatorRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postValidator(requestParameters: ValidatorApiPostValidatorRequest, options?: RawAxiosRequestConfig): AxiosPromise<SlotView> {
            return localVarFp.postValidator(requestParameters.validatorClaim, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for getValidator operation in ValidatorApi.
 * @export
 * @interface ValidatorApiGetValidatorRequest
 */
export interface ValidatorApiGetValidatorRequest {
    /**
     * Limit is how many slots to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 200; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
     * @type {string}
     * @memberof ValidatorApiGetValidator
     */
    readonly limit?: string
}

/**
 * Request parameters for getValidatorByTokenid operation in ValidatorApi.
 * @export
 * @interface ValidatorApiGetValidatorByTokenidRequest
 */
export interface ValidatorApiGetValidatorByTokenidRequest {
    /**
     * TokenID is the slot\&#39;s GenesisNFT token id, from the path, as a decimal string. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
     * @type {string}
     * @memberof ValidatorApiGetValidatorByTokenid
     */
    readonly tokenId: string
}

/**
 * Request parameters for getValidatorChallenge operation in ValidatorApi.
 * @export
 * @interface ValidatorApiGetValidatorChallengeRequest
 */
export interface ValidatorApiGetValidatorChallengeRequest {
    /**
     * TokenID is the Validator-tier GenesisNFT token id, as a decimal string in the &#x60;?tokenId&#x3D;&#x60; query. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
     * @type {string}
     * @memberof ValidatorApiGetValidatorChallenge
     */
    readonly tokenId?: string
}

/**
 * Request parameters for postValidator operation in ValidatorApi.
 * @export
 * @interface ValidatorApiPostValidatorRequest
 */
export interface ValidatorApiPostValidatorRequest {
    /**
     * 
     * @type {ValidatorClaim}
     * @memberof ValidatorApiPostValidator
     */
    readonly validatorClaim: ValidatorClaim
}

/**
 * ValidatorApi - object-oriented interface
 * @export
 * @class ValidatorApi
 * @extends {BaseAPI}
 */
export class ValidatorApi extends BaseAPI {
    /**
     * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
     * @summary Returns the validator slots the caller\'s org has claimed.
     * @param {ValidatorApiGetValidatorRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorApi
     */
    public getValidator(requestParameters: ValidatorApiGetValidatorRequest = {}, options?: RawAxiosRequestConfig) {
        return ValidatorApiFp(this.configuration).getValidator(requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
     * @summary Returns one claimed validator slot, scoped to the caller\'s org.
     * @param {ValidatorApiGetValidatorByTokenidRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorApi
     */
    public getValidatorByTokenid(requestParameters: ValidatorApiGetValidatorByTokenidRequest, options?: RawAxiosRequestConfig) {
        return ValidatorApiFp(this.configuration).getValidatorByTokenid(requestParameters.tokenId, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validator verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validator before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
     * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
     * @param {ValidatorApiGetValidatorChallengeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorApi
     */
    public getValidatorChallenge(requestParameters: ValidatorApiGetValidatorChallengeRequest = {}, options?: RawAxiosRequestConfig) {
        return ValidatorApiFp(this.configuration).getValidatorChallenge(requestParameters.tokenId, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
     * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
     * @param {ValidatorApiPostValidatorRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorApi
     */
    public postValidator(requestParameters: ValidatorApiPostValidatorRequest, options?: RawAxiosRequestConfig) {
        return ValidatorApiFp(this.configuration).postValidator(requestParameters.validatorClaim, options).then((request) => request(this.axios, this.basePath));
    }
}

