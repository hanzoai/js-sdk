/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
/**
 * MeetApi - axios parameter creator
 * @export
 */
export const MeetApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Reports whether this deployment holds the LiveKit key pair it needs. `ready:true` with 200 when tokens can be minted; the SAME body with `ready:false`, `status:\"degraded\"` and 503 when they cannot, so a probe and a dashboard both read the degraded state instead of someone grepping a boot log.  It takes no credential and is reachable on every public host, so it withholds both the reason and the signing key\'s name on purpose: `ready` is the whole dashboard fact, and the reason — which names the key file and the Secret — is written to the boot log where an operator already is.
         * @summary Whether the office can mint join tokens
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1MeetHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/meet/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers with a LiveKit join token for exactly the room named in the body. The body is the RAW token as text/plain — one opaque string, not JSON and not wrapped in an envelope, which is what the office client reads.  The caller presents its workspace session as a Bearer. Every clause is a refusal: the session must verify, its SIGNED workspace claim must equal the room\'s leading name segment — rooms are named `<workspace>_<room>_<id>`, and that prefix is the only thing binding a room to a tenant — and the session must carry a privileged workspace role, so a guest is refused rather than seated.  The participant identity is the SESSION\'S, never the body\'s. `_id` is accepted for compatibility with the published client bundle and deliberately ignored: LiveKit treats the identity as unique and ejects a duplicate, so honouring a caller-chosen one would let anyone in a workspace kick out a colleague and impersonate them. `participantName` is a display name only.  An unconfigured deployment answers 503 under its own name rather than 404, and the refusal states only that the office is unconfigured — the reason names key material and stays in the boot log.
         * @summary Mint a join token for one video room
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1MeetGettoken: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/meet/getToken`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * MeetApi - functional programming interface
 * @export
 */
export const MeetApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = MeetApiAxiosParamCreator(configuration)
    return {
        /**
         * Reports whether this deployment holds the LiveKit key pair it needs. `ready:true` with 200 when tokens can be minted; the SAME body with `ready:false`, `status:\"degraded\"` and 503 when they cannot, so a probe and a dashboard both read the degraded state instead of someone grepping a boot log.  It takes no credential and is reachable on every public host, so it withholds both the reason and the signing key\'s name on purpose: `ready` is the whole dashboard fact, and the reason — which names the key file and the Secret — is written to the boot log where an operator already is.
         * @summary Whether the office can mint join tokens
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1MeetHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1MeetHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MeetApi.cloudGetV1MeetHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers with a LiveKit join token for exactly the room named in the body. The body is the RAW token as text/plain — one opaque string, not JSON and not wrapped in an envelope, which is what the office client reads.  The caller presents its workspace session as a Bearer. Every clause is a refusal: the session must verify, its SIGNED workspace claim must equal the room\'s leading name segment — rooms are named `<workspace>_<room>_<id>`, and that prefix is the only thing binding a room to a tenant — and the session must carry a privileged workspace role, so a guest is refused rather than seated.  The participant identity is the SESSION\'S, never the body\'s. `_id` is accepted for compatibility with the published client bundle and deliberately ignored: LiveKit treats the identity as unique and ejects a duplicate, so honouring a caller-chosen one would let anyone in a workspace kick out a colleague and impersonate them. `participantName` is a display name only.  An unconfigured deployment answers 503 under its own name rather than 404, and the refusal states only that the office is unconfigured — the reason names key material and stays in the boot log.
         * @summary Mint a join token for one video room
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1MeetGettoken(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1MeetGettoken(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MeetApi.cloudPostV1MeetGettoken']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * MeetApi - factory interface
 * @export
 */
export const MeetApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = MeetApiFp(configuration)
    return {
        /**
         * Reports whether this deployment holds the LiveKit key pair it needs. `ready:true` with 200 when tokens can be minted; the SAME body with `ready:false`, `status:\"degraded\"` and 503 when they cannot, so a probe and a dashboard both read the degraded state instead of someone grepping a boot log.  It takes no credential and is reachable on every public host, so it withholds both the reason and the signing key\'s name on purpose: `ready` is the whole dashboard fact, and the reason — which names the key file and the Secret — is written to the boot log where an operator already is.
         * @summary Whether the office can mint join tokens
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1MeetHealth(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1MeetHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers with a LiveKit join token for exactly the room named in the body. The body is the RAW token as text/plain — one opaque string, not JSON and not wrapped in an envelope, which is what the office client reads.  The caller presents its workspace session as a Bearer. Every clause is a refusal: the session must verify, its SIGNED workspace claim must equal the room\'s leading name segment — rooms are named `<workspace>_<room>_<id>`, and that prefix is the only thing binding a room to a tenant — and the session must carry a privileged workspace role, so a guest is refused rather than seated.  The participant identity is the SESSION\'S, never the body\'s. `_id` is accepted for compatibility with the published client bundle and deliberately ignored: LiveKit treats the identity as unique and ejects a duplicate, so honouring a caller-chosen one would let anyone in a workspace kick out a colleague and impersonate them. `participantName` is a display name only.  An unconfigured deployment answers 503 under its own name rather than 404, and the refusal states only that the office is unconfigured — the reason names key material and stays in the boot log.
         * @summary Mint a join token for one video room
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1MeetGettoken(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1MeetGettoken(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * MeetApi - object-oriented interface
 * @export
 * @class MeetApi
 * @extends {BaseAPI}
 */
export class MeetApi extends BaseAPI {
    /**
     * Reports whether this deployment holds the LiveKit key pair it needs. `ready:true` with 200 when tokens can be minted; the SAME body with `ready:false`, `status:\"degraded\"` and 503 when they cannot, so a probe and a dashboard both read the degraded state instead of someone grepping a boot log.  It takes no credential and is reachable on every public host, so it withholds both the reason and the signing key\'s name on purpose: `ready` is the whole dashboard fact, and the reason — which names the key file and the Secret — is written to the boot log where an operator already is.
     * @summary Whether the office can mint join tokens
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MeetApi
     */
    public cloudGetV1MeetHealth(options?: RawAxiosRequestConfig) {
        return MeetApiFp(this.configuration).cloudGetV1MeetHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers with a LiveKit join token for exactly the room named in the body. The body is the RAW token as text/plain — one opaque string, not JSON and not wrapped in an envelope, which is what the office client reads.  The caller presents its workspace session as a Bearer. Every clause is a refusal: the session must verify, its SIGNED workspace claim must equal the room\'s leading name segment — rooms are named `<workspace>_<room>_<id>`, and that prefix is the only thing binding a room to a tenant — and the session must carry a privileged workspace role, so a guest is refused rather than seated.  The participant identity is the SESSION\'S, never the body\'s. `_id` is accepted for compatibility with the published client bundle and deliberately ignored: LiveKit treats the identity as unique and ejects a duplicate, so honouring a caller-chosen one would let anyone in a workspace kick out a colleague and impersonate them. `participantName` is a display name only.  An unconfigured deployment answers 503 under its own name rather than 404, and the refusal states only that the office is unconfigured — the reason names key material and stays in the boot log.
     * @summary Mint a join token for one video room
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MeetApi
     */
    public cloudPostV1MeetGettoken(options?: RawAxiosRequestConfig) {
        return MeetApiFp(this.configuration).cloudPostV1MeetGettoken(options).then((request) => request(this.axios, this.basePath));
    }
}

