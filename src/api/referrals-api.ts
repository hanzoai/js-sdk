/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudClaimRequest } from '../models';
// @ts-ignore
import type { CloudClaimView } from '../models';
// @ts-ignore
import type { CloudMyReferrals } from '../models';
/**
 * ReferralsApi - axios parameter creator
 * @export
 */
export const ReferralsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the caller\'s referral code, share link and the referrals they have made.  The code is a stable, deterministic function of the org, so the link in this response is the same one every time. Each row carries the referee, its status and the credit this org earned from it; creditsEarnedCents is their sum.  The read is self-updating: before listing, it runs the qualify check over this org\'s still-pending referees, so a referee who has since made metered spend is credited by the act of the referrer loading their page. That check is best-effort and bounded — a commerce hiccup leaves the referral pending for the next check rather than failing the page — and the grant is latched at-most-once, so this path and the admin sweep can never double-pay.
         * @summary Returns the caller\'s referral code, share link and the referrals they have made.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Referrals: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/referrals`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Records that the caller\'s org signed up through a referral code.  The REFEREE is the validated caller, never a client field, and the referrer is resolved from the code — so a caller can only ever attach THEMSELVES to someone else\'s code. Referring yourself is 400 and an unknown code is 404.  It is idempotent and first-touch: an org can be referred once, ever. A repeat call returns the referral already on file with created=false and 200, where the first call answers 201.  Recording a referral grants nothing. Both bonuses are granted later, when the referee actually makes metered spend — see GET /v1/referrals and POST /v1/admin/referrals/sweep.
         * @summary Records that the caller\'s org signed up through a referral code.
         * @param {CloudClaimRequest} cloudClaimRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ReferralsClaim: async (cloudClaimRequest: CloudClaimRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudClaimRequest' is not null or undefined
            assertParamExists('cloudPostV1ReferralsClaim', 'cloudClaimRequest', cloudClaimRequest)
            const localVarPath = `/v1/referrals/claim`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudClaimRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * ReferralsApi - functional programming interface
 * @export
 */
export const ReferralsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = ReferralsApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the caller\'s referral code, share link and the referrals they have made.  The code is a stable, deterministic function of the org, so the link in this response is the same one every time. Each row carries the referee, its status and the credit this org earned from it; creditsEarnedCents is their sum.  The read is self-updating: before listing, it runs the qualify check over this org\'s still-pending referees, so a referee who has since made metered spend is credited by the act of the referrer loading their page. That check is best-effort and bounded — a commerce hiccup leaves the referral pending for the next check rather than failing the page — and the grant is latched at-most-once, so this path and the admin sweep can never double-pay.
         * @summary Returns the caller\'s referral code, share link and the referrals they have made.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Referrals(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMyReferrals>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Referrals(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ReferralsApi.cloudGetV1Referrals']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Records that the caller\'s org signed up through a referral code.  The REFEREE is the validated caller, never a client field, and the referrer is resolved from the code — so a caller can only ever attach THEMSELVES to someone else\'s code. Referring yourself is 400 and an unknown code is 404.  It is idempotent and first-touch: an org can be referred once, ever. A repeat call returns the referral already on file with created=false and 200, where the first call answers 201.  Recording a referral grants nothing. Both bonuses are granted later, when the referee actually makes metered spend — see GET /v1/referrals and POST /v1/admin/referrals/sweep.
         * @summary Records that the caller\'s org signed up through a referral code.
         * @param {CloudClaimRequest} cloudClaimRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1ReferralsClaim(cloudClaimRequest: CloudClaimRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudClaimView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1ReferralsClaim(cloudClaimRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ReferralsApi.cloudPostV1ReferralsClaim']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * ReferralsApi - factory interface
 * @export
 */
export const ReferralsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = ReferralsApiFp(configuration)
    return {
        /**
         * Returns the caller\'s referral code, share link and the referrals they have made.  The code is a stable, deterministic function of the org, so the link in this response is the same one every time. Each row carries the referee, its status and the credit this org earned from it; creditsEarnedCents is their sum.  The read is self-updating: before listing, it runs the qualify check over this org\'s still-pending referees, so a referee who has since made metered spend is credited by the act of the referrer loading their page. That check is best-effort and bounded — a commerce hiccup leaves the referral pending for the next check rather than failing the page — and the grant is latched at-most-once, so this path and the admin sweep can never double-pay.
         * @summary Returns the caller\'s referral code, share link and the referrals they have made.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Referrals(options?: RawAxiosRequestConfig): AxiosPromise<CloudMyReferrals> {
            return localVarFp.cloudGetV1Referrals(options).then((request) => request(axios, basePath));
        },
        /**
         * Records that the caller\'s org signed up through a referral code.  The REFEREE is the validated caller, never a client field, and the referrer is resolved from the code — so a caller can only ever attach THEMSELVES to someone else\'s code. Referring yourself is 400 and an unknown code is 404.  It is idempotent and first-touch: an org can be referred once, ever. A repeat call returns the referral already on file with created=false and 200, where the first call answers 201.  Recording a referral grants nothing. Both bonuses are granted later, when the referee actually makes metered spend — see GET /v1/referrals and POST /v1/admin/referrals/sweep.
         * @summary Records that the caller\'s org signed up through a referral code.
         * @param {ReferralsApiCloudPostV1ReferralsClaimRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ReferralsClaim(requestParameters: ReferralsApiCloudPostV1ReferralsClaimRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudClaimView> {
            return localVarFp.cloudPostV1ReferralsClaim(requestParameters.cloudClaimRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudPostV1ReferralsClaim operation in ReferralsApi.
 * @export
 * @interface ReferralsApiCloudPostV1ReferralsClaimRequest
 */
export interface ReferralsApiCloudPostV1ReferralsClaimRequest {
    /**
     * 
     * @type {CloudClaimRequest}
     * @memberof ReferralsApiCloudPostV1ReferralsClaim
     */
    readonly cloudClaimRequest: CloudClaimRequest
}

/**
 * ReferralsApi - object-oriented interface
 * @export
 * @class ReferralsApi
 * @extends {BaseAPI}
 */
export class ReferralsApi extends BaseAPI {
    /**
     * Returns the caller\'s referral code, share link and the referrals they have made.  The code is a stable, deterministic function of the org, so the link in this response is the same one every time. Each row carries the referee, its status and the credit this org earned from it; creditsEarnedCents is their sum.  The read is self-updating: before listing, it runs the qualify check over this org\'s still-pending referees, so a referee who has since made metered spend is credited by the act of the referrer loading their page. That check is best-effort and bounded — a commerce hiccup leaves the referral pending for the next check rather than failing the page — and the grant is latched at-most-once, so this path and the admin sweep can never double-pay.
     * @summary Returns the caller\'s referral code, share link and the referrals they have made.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ReferralsApi
     */
    public cloudGetV1Referrals(options?: RawAxiosRequestConfig) {
        return ReferralsApiFp(this.configuration).cloudGetV1Referrals(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Records that the caller\'s org signed up through a referral code.  The REFEREE is the validated caller, never a client field, and the referrer is resolved from the code — so a caller can only ever attach THEMSELVES to someone else\'s code. Referring yourself is 400 and an unknown code is 404.  It is idempotent and first-touch: an org can be referred once, ever. A repeat call returns the referral already on file with created=false and 200, where the first call answers 201.  Recording a referral grants nothing. Both bonuses are granted later, when the referee actually makes metered spend — see GET /v1/referrals and POST /v1/admin/referrals/sweep.
     * @summary Records that the caller\'s org signed up through a referral code.
     * @param {ReferralsApiCloudPostV1ReferralsClaimRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ReferralsApi
     */
    public cloudPostV1ReferralsClaim(requestParameters: ReferralsApiCloudPostV1ReferralsClaimRequest, options?: RawAxiosRequestConfig) {
        return ReferralsApiFp(this.configuration).cloudPostV1ReferralsClaim(requestParameters.cloudClaimRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

