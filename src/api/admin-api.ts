/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { AccessOut } from '../models';
// @ts-ignore
import type { AccrualsOut } from '../models';
// @ts-ignore
import type { ActionOut } from '../models';
// @ts-ignore
import type { AdminBonusesEnvelope } from '../models';
// @ts-ignore
import type { AdminBook } from '../models';
// @ts-ignore
import type { AdminCatalogOut } from '../models';
// @ts-ignore
import type { AdminEnablementBoard } from '../models';
// @ts-ignore
import type { AdminEnablementItem } from '../models';
// @ts-ignore
import type { AdminReportOut } from '../models';
// @ts-ignore
import type { AffiliateOut } from '../models';
// @ts-ignore
import type { AimetricsOut } from '../models';
// @ts-ignore
import type { AnalyticsOut } from '../models';
// @ts-ignore
import type { AnchorOut } from '../models';
// @ts-ignore
import type { Approval } from '../models';
// @ts-ignore
import type { ApproveRequest } from '../models';
// @ts-ignore
import type { AuthorResult } from '../models';
// @ts-ignore
import type { AuthorSweepResult } from '../models';
// @ts-ignore
import type { BackfillIn } from '../models';
// @ts-ignore
import type { BackfillOut } from '../models';
// @ts-ignore
import type { BasesOut } from '../models';
// @ts-ignore
import type { BasisResult } from '../models';
// @ts-ignore
import type { CapIn } from '../models';
// @ts-ignore
import type { ComputeOut } from '../models';
// @ts-ignore
import type { CordonIn } from '../models';
// @ts-ignore
import type { CustomerDetailOut } from '../models';
// @ts-ignore
import type { CustomersOut } from '../models';
// @ts-ignore
import type { DirectoryOut } from '../models';
// @ts-ignore
import type { Disbursal } from '../models';
// @ts-ignore
import type { DropletIn } from '../models';
// @ts-ignore
import type { FinanceOut } from '../models';
// @ts-ignore
import type { FlagsOut } from '../models';
// @ts-ignore
import type { GrantIn } from '../models';
// @ts-ignore
import type { GrantOut } from '../models';
// @ts-ignore
import type { GrantsOut } from '../models';
// @ts-ignore
import type { IamRowsOut } from '../models';
// @ts-ignore
import type { InvoicesOut } from '../models';
// @ts-ignore
import type { ListOut } from '../models';
// @ts-ignore
import type { MeOut } from '../models';
// @ts-ignore
import type { MetricsOut } from '../models';
// @ts-ignore
import type { MoneyOut } from '../models';
// @ts-ignore
import type { MutationOut } from '../models';
// @ts-ignore
import type { NameIn } from '../models';
// @ts-ignore
import type { O11yOut } from '../models';
// @ts-ignore
import type { OrgsOut } from '../models';
// @ts-ignore
import type { Overlay } from '../models';
// @ts-ignore
import type { OverviewOut } from '../models';
// @ts-ignore
import type { PayoutOut } from '../models';
// @ts-ignore
import type { PayoutRequest } from '../models';
// @ts-ignore
import type { PayoutResult } from '../models';
// @ts-ignore
import type { PolicyOut } from '../models';
// @ts-ignore
import type { PolicyRequest } from '../models';
// @ts-ignore
import type { ProductsOut } from '../models';
// @ts-ignore
import type { PromoIn } from '../models';
// @ts-ignore
import type { ProviderPatchIn } from '../models';
// @ts-ignore
import type { ProvidersCreditOut } from '../models';
// @ts-ignore
import type { RateSet } from '../models';
// @ts-ignore
import type { RawOut } from '../models';
// @ts-ignore
import type { ReadOut } from '../models';
// @ts-ignore
import type { RecordsOut } from '../models';
// @ts-ignore
import type { ReferralsOut } from '../models';
// @ts-ignore
import type { ReloadIn } from '../models';
// @ts-ignore
import type { RevenueOut } from '../models';
// @ts-ignore
import type { ScaleIn } from '../models';
// @ts-ignore
import type { SeedOut } from '../models';
// @ts-ignore
import type { SeedRequest } from '../models';
// @ts-ignore
import type { ServiceInput } from '../models';
// @ts-ignore
import type { ServiceModeIn } from '../models';
// @ts-ignore
import type { ServiceOut } from '../models';
// @ts-ignore
import type { ServicesOut } from '../models';
// @ts-ignore
import type { SetEnablementBody } from '../models';
// @ts-ignore
import type { SetFlagIn } from '../models';
// @ts-ignore
import type { SignerOut } from '../models';
// @ts-ignore
import type { SubscriptionsOut } from '../models';
// @ts-ignore
import type { SubsystemsOut } from '../models';
// @ts-ignore
import type { SweepEnvelope } from '../models';
// @ts-ignore
import type { SweepOut } from '../models';
// @ts-ignore
import type { SweepRequest } from '../models';
// @ts-ignore
import type { SyncOut } from '../models';
// @ts-ignore
import type { UsageFundingOut } from '../models';
// @ts-ignore
import type { UsageOut } from '../models';
// @ts-ignore
import type { UsersOut } from '../models';
// @ts-ignore
import type { VerifyOut } from '../models';
// @ts-ignore
import type { VolumeIn } from '../models';
// @ts-ignore
import type { VolumeSnapshotOut } from '../models';
// @ts-ignore
import type { VolumesOut } from '../models';
// @ts-ignore
import type { WaitlistBoostRequest } from '../models';
/**
 * AdminApi - axios parameter creator
 * @export
 */
export const AdminApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).  Every signal degrades INDEPENDENTLY — a table that is absent or errors contributes its zero value and the read still succeeds. Generation latency is a SEPARATE query from generations and cost on purpose: a duration/attribute mismatch there must not zero the two numbers that did read.
         * @summary Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAIMetrics: async (range?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/aimetrics`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.  The window is the caller\'s, not the fleet\'s: a SuperAdmin gets every org, a white-label admin only their own subtree (core.ScopedOrgs, the one scope predicate).  sources[] carries each upstream\'s freshness so a partial read is VISIBLE rather than silently low: a ledger that answered for only some orgs marks commerce-ledger degraded instead of publishing an undercount as healthy.
         * @summary Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAnalytics: async (range?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/analytics`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications. These are the platform\'s OIDC clients — the console reads clientId off each row.
         * @summary Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications.
         * @param {string} [owner] Owner is the org whose rows to read. Defaults to the admin org, which owns the platform\&#39;s roles and applications.
         * @param {string} [p] Page is the 1-based page number. Forwarded only when set — IAM applies its own default otherwise.
         * @param {string} [pageSize] PageSize is rows per page. Forwarded only when set.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminApplications: async (owner?: string, p?: string, pageSize?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/applications`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (owner !== undefined) {
                localVarQueryParameter['owner'] = owner;
            }

            if (p !== undefined) {
                localVarQueryParameter['p'] = p;
            }

            if (pageSize !== undefined) {
                localVarQueryParameter['pageSize'] = pageSize;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.  When cloud has no local store configured it falls back to forwarding IAM\'s own get-records trail verbatim — a DIFFERENT trail, federated so the endpoint never regresses to an empty list. Those rows carry no integrity of ours, so the field is null there.
         * @summary Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.
         * @param {string} [org] Org restricts the trail to one tenant.
         * @param {string} [sub] Sub restricts it to one actor (the validated subject that made the request).
         * @param {string} [action] Action restricts it to one action name, e.g. \&quot;admin.waitlist.grant\&quot;.
         * @param {string} [resource] Resource restricts it to one resource kind, e.g. \&quot;credit-grant\&quot;.
         * @param {string} [resourceId] ResourceID restricts it to one resource instance.
         * @param {string} [result] Result restricts it to \&quot;success\&quot; or \&quot;error\&quot;.
         * @param {string} [since] Since is the inclusive lower time bound, RFC3339. An unparseable value is ignored rather than refused — one malformed filter must not hide the trail.
         * @param {string} [until] Until is the upper time bound, RFC3339, with the same tolerance.
         * @param {string} [pageSize] PageSize is rows per page, default 100.
         * @param {string} [p] Page is the 1-based page number, driving the offset.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAudit: async (org?: string, sub?: string, action?: string, resource?: string, resourceId?: string, result?: string, since?: string, until?: string, pageSize?: string, p?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/audit`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }

            if (sub !== undefined) {
                localVarQueryParameter['sub'] = sub;
            }

            if (action !== undefined) {
                localVarQueryParameter['action'] = action;
            }

            if (resource !== undefined) {
                localVarQueryParameter['resource'] = resource;
            }

            if (resourceId !== undefined) {
                localVarQueryParameter['resourceId'] = resourceId;
            }

            if (result !== undefined) {
                localVarQueryParameter['result'] = result;
            }

            if (since !== undefined) {
                localVarQueryParameter['since'] = since;
            }

            if (until !== undefined) {
                localVarQueryParameter['until'] = until;
            }

            if (pageSize !== undefined) {
                localVarQueryParameter['pageSize'] = pageSize;
            }

            if (p !== undefined) {
                localVarQueryParameter['p'] = p;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.  The trail is a FAMILY of chains, one per process, so the answer is a set and not a boolean: `intact`, `broken` and `unread` count the three verdicts and sum to the number of chains. A chain that could not be READ is reported `unread` and is never a pass — an unreadable chain and a verified one must not render the same, which is the whole reason this is not one flag.  An unconfigured store is an honest failure here rather than a fabricated pass.
         * @summary Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAuditVerify: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/audit/verify`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.  The scope is enforced TWICE: the upstream is asked for the caller\'s org, AND every row it returns is re-checked against the resolved scope. An upstream that ignored the filter therefore degrades to empty, never to a cross-tenant leak.  The Base engine is being embedded into cloud; until it lands this proxies BASE_ADMIN_URL and, when that is unset, answers 200 with an empty list and msg saying so — the honest not-yet state, never fabricated instances.
         * @summary Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminBases: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/bases`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.  These are the SAME rows the customer edits in their own console — a platform override and a customer budget are one model, not two.
         * @summary Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.
         * @param {string} [org] Org is the tenant to act on. Required for a SuperAdmin — they must name their target; ignored for a white-label admin, who always acts on their own org.
         * @param {string} [id] ID is the cap to edit or remove, from the path. Unused by the list and create ops.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCaps: async (org?: string, id?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/caps`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }

            if (id !== undefined) {
                localVarQueryParameter['id'] = id;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event. The console folds these into its org → app → project tree.  A machine counts as ACTIVE when its LATEST lifecycle event is not a terminal one (stop/destroy/terminate/delete/off/shutdown/expire and their past tenses) — the same fold the console applies, done in the warehouse so the count is over every machine and not just the page.  Honest-empty when the warehouse is not connected or hanzo.compute_usage is not provisioned yet: an empty list, never a fabricated fleet.
         * @summary Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event.
         * @param {string} [kind] Kind narrows to one workload class (bot | machine | cluster | nodepool | container | function | …). An OPEN spectrum matched as a plain string, lowercased to the warehouse\&#39;s convention; empty means every kind.
         * @param {string} [org] Org narrows to one tenant. Empty means every tenant — this board is cross-tenant by nature.
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as 30d.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCompute: async (kind?: string, org?: string, range?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/compute`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (kind !== undefined) {
                localVarQueryParameter['kind'] = kind;
            }

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.  It is the ONE infra change that does not go through the run discipline, because there is no destructive verdict to check: cordoning is reversible and evicting respects the cluster\'s own PodDisruptionBudgets. It reads the cached board for the same reason. The outcome is audited either way, and the result reports how many pods were evicted.
         * @summary Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.
         * @param {string} id ID is the node\&#39;s droplet id, from the path.
         * @param {CordonIn} cordonIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCordonNode: async (id: string, cordonIn: CordonIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminCordonNode', 'id', id)
            // verify required parameter 'cordonIn' is not null or undefined
            assertParamExists('adminCordonNode', 'cordonIn', cordonIn)
            const localVarPath = `/v1/admin/infra/nodes/{id}/cordon`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cordonIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows. The body is commerce\'s spend-alert contract, forwarded byte-for-byte.
         * @summary Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows.
         * @param {CapIn} capIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCreateCap: async (capIn: CapIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'capIn' is not null or undefined
            assertParamExists('adminCreateCap', 'capIn', capIn)
            const localVarPath = `/v1/admin/caps`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(capIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers GET /v1/admin/customers/:org.
         * @summary Answers GET /v1/admin/customers/:org.
         * @param {string} org Org is the tenant slug from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCustomer: async (org: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('adminCustomer', 'org', org)
            const localVarPath = `/v1/admin/customers/{org}`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.  Each row costs one IAM read plus the org\'s money reads, fanned out under a fixed concurrency ceiling so a large fleet cannot stampede the upstreams. Every read is best-effort per row: an upstream miss degrades THAT field to its honest zero rather than failing the fleet.
         * @summary Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCustomers: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/customers`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes one cap by id, lifting the ceiling entirely.
         * @summary Removes one cap by id, lifting the ceiling entirely.
         * @param {string} id ID is the cap to edit or remove, from the path. Unused by the list and create ops.
         * @param {string} [org] Org is the tenant to act on. Required for a SuperAdmin — they must name their target; ignored for a white-label admin, who always acts on their own org.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteCap: async (id: string, org?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminDeleteCap', 'id', id)
            const localVarPath = `/v1/admin/caps/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Destroys a droplet the board has just proven is NOT a DOKS node. There is no snapshot-first undo for a droplet the way there is for a volume: the local disk goes with it.
         * @summary Destroys a droplet the board has just proven is NOT a DOKS node.
         * @param {string} id ID is the DO droplet id, from the path. Numeric.
         * @param {string} [size] Size is the target DigitalOcean size slug on resize, e.g. \&quot;s-4vcpu-8gb\&quot;.
         * @param {boolean} [disk] Disk requests a PERMANENT resize that grows the disk. DO can never resize such a droplet down again, so it defaults false — a CPU/RAM-only change, reversible.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteDroplet: async (id: string, size?: string, disk?: boolean, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminDeleteDroplet', 'id', id)
            const localVarPath = `/v1/admin/infra/droplets/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (size !== undefined) {
                localVarQueryParameter['size'] = size;
            }

            if (disk !== undefined) {
                localVarQueryParameter['disk'] = disk;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
         * @summary Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
         * @param {string} id ID is the DO load balancer id, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteLoadBalancer: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminDeleteLoadBalancer', 'id', id)
            const localVarPath = `/v1/admin/infra/loadbalancers/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Destroys a volume the board has just proven no PersistentVolume in any cluster references. Irreversible, so it snapshots first unless explicitly waived — the snapshot IS the undo.
         * @summary Destroys a volume the board has just proven no PersistentVolume in any cluster references.
         * @param {string} id ID is the DO volume id, from the path.
         * @param {string} [snapshot] Snapshot is the snapshot-first switch on DELETE. Anything other than the literal \&quot;false\&quot; snapshots before destroying — the snapshot IS the undo, so waiving it is deliberate and explicit.
         * @param {string} [name] Name is the snapshot name on the snapshot action. Blank gets a deterministic \&quot;&lt;volume&gt;-predelete-&lt;unix&gt;\&quot; so the undo is findable in the DO console.
         * @param {number} [sizeGiB] SizeGiB is the target size on the resize action. A volume only ever grows — ExpandTo is the verdict that refuses a shrink, so this is not validated here.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteVolume: async (id: string, snapshot?: string, name?: string, sizeGiB?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminDeleteVolume', 'id', id)
            const localVarPath = `/v1/admin/infra/volumes/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (snapshot !== undefined) {
                localVarQueryParameter['snapshot'] = snapshot;
            }

            if (name !== undefined) {
                localVarQueryParameter['name'] = name;
            }

            if (sizeGiB !== undefined) {
                localVarQueryParameter['sizeGiB'] = sizeGiB;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Stops the plugin. Its routes STAY REGISTERED and answer 503 — not 404.  That is zip\'s choice and this keeps it. Removing the routes would mutate the route table, and re-adding them on enable would grow it without bound across repeated cycles, which is the invariant that makes reloads flat in memory. It is also the better answer: 404 says \"no such API\" and a client may cache it and stop retrying, while 503 says \"this API exists and is down right now\", which is true and retryable. Which of the two 503s this is — deliberate stop or crash — is what the status\'s disabled flag reports.
         * @summary Stops the plugin.
         * @param {string} name Name is the app, from the path.
         * @param {NameIn} nameIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDisablePlugin: async (name: string, nameIn: NameIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('adminDisablePlugin', 'name', name)
            // verify required parameter 'nameIn' is not null or undefined
            assertParamExists('adminDisablePlugin', 'nameIn', nameIn)
            const localVarPath = `/v1/admin/plugins/{name}/disable`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(nameIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag. Named for what an operator means by it.
         * @summary Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag.
         * @param {string} name Name is the app, from the path.
         * @param {NameIn} nameIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminEnablePlugin: async (name: string, nameIn: NameIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('adminEnablePlugin', 'name', name)
            // verify required parameter 'nameIn' is not null or undefined
            assertParamExists('adminEnablePlugin', 'nameIn', nameIn)
            const localVarPath = `/v1/admin/plugins/{name}/enable`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(nameIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers GET /v1/admin/finance. It reads the multi-vendor COGS from commerce /v1/costs, the DO promo-credit/burn-down treasury view, and the fleet commerce revenue, then hands them to ComputeFinance. SuperAdmin only.
         * @summary Answers GET /v1/admin/finance.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminFinance: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/finance`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.  It is IDEMPOTENT: the deposit uses the fixed ref \"backfill:<org>\", so re-running it credits the wallet at most once. Safe to retry.  The pre-migration balance is read from the CO-RESIDENT commerce ledger, not over HTTP: the admin HTTP client dials an unroutable in-process address and would read $0, and a phantom zero would silently carry nothing while reporting success. When commerce is not co-resident this fails rather than migrating nothing.
         * @summary Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.
         * @param {BackfillIn} backfillIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminFinanceBackfill: async (backfillIn: BackfillIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'backfillIn' is not null or undefined
            assertParamExists('adminFinanceBackfill', 'backfillIn', backfillIn)
            const localVarPath = `/v1/admin/finance/backfill`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(backfillIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
         * @summary Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminFlags: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/flags`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.  The credit lands on the account account.Payer resolves, NOT necessarily the org: name a member of a pooled org and the pool is credited. The receipt echoes the subject so the caller can see which.
         * @summary Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.
         * @param {string} org Org is the tenant to credit. Required.
         * @param {GrantIn} grantIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminGrantCredit: async (org: string, grantIn: GrantIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('adminGrantCredit', 'org', org)
            // verify required parameter 'grantIn' is not null or undefined
            assertParamExists('adminGrantCredit', 'grantIn', grantIn)
            const localVarPath = `/v1/admin/customers/{org}/credit`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(grantIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.  It is a PROJECTION of the tamper-evident audit trail, not a second store: every grant is written there as action \"admin.customer.credit\", so this view cannot drift from what actually happened, and FAILED grants appear too.  A deployment with no local audit store has no history to project, and says so with an empty list and a msg rather than an error.
         * @summary Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.
         * @param {string} [org] Org filters by the ACTOR\&#39;s org (the staff org that issued the grant), which is rarely what a reader wants — the target org is a row field, not a filter.
         * @param {string} [result] Result filters by outcome: \&quot;success\&quot; or \&quot;error\&quot;. Empty returns both, which is the point of this view — a refused grant is as interesting as a granted one.
         * @param {string} [limit] Limit caps the rows returned. Default 200.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminGrants: async (org?: string, result?: string, limit?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/grants`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }

            if (result !== undefined) {
                localVarQueryParameter['result'] = result;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.  It is cached for up to a minute because one read is a fan-out over the DO API plus a full pod/PV listing per cluster. Staleness is never load-bearing: every MUTATION re-scans from scratch and ignores this cache.  Only an unusable DO account is a hard failure. A partial read still produces a board, with the failing source named in sources[] — except for clusters and volumes, which the safety verdict depends on; without those the analysis degrades rather than classifying anything it cannot prove.
         * @summary Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.
         * @param {string} [refresh] Refresh, when present, forces a full re-scan instead of serving the cached snapshot. Every MUTATION re-scans regardless — this is only for the reader.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminInfra: async (refresh?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/infra`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (refresh !== undefined) {
                localVarQueryParameter['refresh'] = refresh;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers GET /v1/admin/invoices.   GET /v1/admin/invoices?org=&status=&limit=
         * @summary Answers GET /v1/admin/invoices.
         * @param {string} [status] Status filters on the invoice\&#39;s LATEST lifecycle status (paid, open, void, …), matched case-insensitively.
         * @param {string} [org] Org filters to one tenant, matched exactly.
         * @param {string} [limit] Limit caps the rows returned. total still reports the full match count.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminInvoices: async (status?: string, org?: string, limit?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/invoices`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (status !== undefined) {
                localVarQueryParameter['status'] = status;
            }

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Issues a credit grant to any org from the operator Grants view, with the target named in the body. It funnels through the SAME core.ApplyGrant that POST /v1/admin/customers/:org/credit uses, so there is exactly ONE credit-write path and one audit trail behind both.
         * @summary Issues a credit grant to any org from the operator Grants view, with the target named in the body.
         * @param {GrantIn} grantIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminIssueGrant: async (grantIn: GrantIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'grantIn' is not null or undefined
            assertParamExists('adminIssueGrant', 'grantIn', grantIn)
            const localVarPath = `/v1/admin/grants`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(grantIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is. The fields come from the sanitized identity headers the gate just read, so they are authoritative and never client-forgeable; nothing is looked up.
         * @summary Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminMe: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/me`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out). SuperAdmin only.   GET /v1/admin/metrics?window=30d&limit=20
         * @summary Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out).
         * @param {string} [window] Window is the movement window the new/churned MRR and the recent feed are measured over. Anything unrecognised falls back to the board default.
         * @param {string} [limit] Limit caps the top-customers table.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminMetrics: async (window?: string, limit?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/metrics`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (window !== undefined) {
                localVarQueryParameter['window'] = window;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * moneyBoardHandler answers GET /v1/admin/money.
         * @summary moneyBoardHandler answers GET /v1/admin/money.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminMoney: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/money`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.  Every signal degrades INDEPENDENTLY. A table that is absent or errors contributes its zero value and the read still succeeds, so the board renders exactly what the warehouse holds rather than failing whole because one of four sources is missing. Same when the warehouse is not connected at all: the zero board, never a fabricated fleet.
         * @summary Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminO11y: async (range?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/o11y`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.  The rows are the caller\'s tenant window, not the fleet: a SuperAdmin gets every org, a white-label admin only their own subtree. A per-org read that fails degrades THAT row to an honest zero — this panel carries no sources[] channel to report freshness on, so the alternative would be a fleet total that silently reads healthy.
         * @summary Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminOrgs: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/orgs`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.  It ALWAYS answers 200 — a tile board that fails as a whole because one upstream is down is useless. Instead every upstream reports itself in sources[]: ok, degraded, or not-configured. A commerce read that failed for ANY org marks that source degraded, because the spend/credits totals are then an undercount and must not read healthy.  The AI tiles — 30-day spend and tokens — come from the AI ledger (ledger.go), the plane that owns \"what was served\". They used to come from the money plane with the token counter hardcoded to zero, so the board read $0.00 and 0 tokens over a month in which the fleet served fifteen thousand requests. Credits still come from commerce, which owns the wallet.
         * @summary Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminOverview: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/overview`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.  Reading this from deployment config would answer what was INTENDED. Only the process knows what is TRUE, and during a rolling upgrade the two disagree on purpose.
         * @summary Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.
         * @param {string} [scope] Scope \&quot;host\&quot; answers for THIS host only. Default \&quot;fleet\&quot; fans out to every live peer. A peer answers a host-scoped read, which is what stops the fan-out recursing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminPlugins: async (scope?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/plugins`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (scope !== undefined) {
                localVarQueryParameter['scope'] = scope;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict. Optionally narrowed by kind, tier or env, each an exact match.  The rows are the SAME observation /v1/platform/fleet renders — read through the in-process platform seam, not a second k8s client — so the two boards can never disagree about what the fleet is. A PaaS plane that is not co-resident yields an honestly empty registry, never a fabricated row.
         * @summary Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict.
         * @param {string} [kind] Kind matches the operator App CR\&#39;s declared spec.role (sql|kv|generic|ingress).
         * @param {string} [tier] Tier matches the derived infra grouping (cloud|data|edge|daemon|paas|app).
         * @param {string} [env] Env matches the lifecycle namespace (main|test|dev).
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminProducts: async (kind?: string, tier?: string, env?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/products`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (kind !== undefined) {
                localVarQueryParameter['kind'] = kind;
            }

            if (tier !== undefined) {
                localVarQueryParameter['tier'] = tier;
            }

            if (env !== undefined) {
                localVarQueryParameter['env'] = env;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads the current platform plan promo — the singleton discount offer, e.g. the 50%-off launch promo. Commerce stores it in the reserved platform namespace, so the org sent with the read is the admin org and the service token is what passes commerce\'s own platform-admin gate.
         * @summary Reads the current platform plan promo — the singleton discount offer, e.g.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminPromo: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/promos`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger. SuperAdmin-guarded (see Routes).
         * @summary Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminProvidersCredit: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/providers/credit`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Restores access for every member of the org, undoing a suspend. It reports the same per-user breakdown.
         * @summary Restores access for every member of the org, undoing a suspend.
         * @param {string} org Org is the tenant slug from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminReactivateCustomer: async (org: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('adminReactivateCustomer', 'org', org)
            const localVarPath = `/v1/admin/customers/{org}/reactivate`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Swaps a plugin for another build without dropping a request. The replacement is started and proven to be LISTENING before any traffic moves to it, so a bad build leaves the old one serving and returns an error rather than a hole; the old process then drains before it is killed.  With a version or url+sum it pins; naming a digest this host has run before is the rollback, and costs no network because the digest IS the cache key. With neither it restarts what is already loaded.  Fleet scope applies it to one host at a time and STOPS at the first failure, so a build that cannot come up reaches exactly one host.
         * @summary Swaps a plugin for another build without dropping a request.
         * @param {string} name Name is the app, from the path. It must be one the manifest declares.
         * @param {ReloadIn} reloadIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminReloadPlugin: async (name: string, reloadIn: ReloadIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('adminReloadPlugin', 'name', name)
            // verify required parameter 'reloadIn' is not null or undefined
            assertParamExists('adminReloadPlugin', 'reloadIn', reloadIn)
            const localVarPath = `/v1/admin/plugins/{name}/reload`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(reloadIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Changes a droplet\'s plan. Same refusal as delete and for the same reason: a DOKS node\'s size is the node pool\'s to declare.  disk=true is a PERMANENT resize — the disk grows and DO can never resize the droplet DOWN again. disk=false (the default) changes CPU/RAM only and is reversible. DO requires the droplet to be powered off and applies the change asynchronously, so the response carries the action to poll, not a completed change.
         * @summary Changes a droplet\'s plan.
         * @param {string} id ID is the DO droplet id, from the path. Numeric.
         * @param {DropletIn} dropletIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminResizeDroplet: async (id: string, dropletIn: DropletIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminResizeDroplet', 'id', id)
            // verify required parameter 'dropletIn' is not null or undefined
            assertParamExists('adminResizeDroplet', 'dropletIn', dropletIn)
            const localVarPath = `/v1/admin/infra/droplets/{id}/resize`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(dropletIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Grows a volume. GROW ONLY — see Volume.ExpandTo for why the other direction is a data migration this board deliberately refuses to run.  The MECHANISM follows the volume\'s owner, because there is exactly one way to grow each kind completely. A volume a PVC claims is grown by patching the claim: the CSI driver then resizes the DigitalOcean device AND grows the filesystem on it, leaving claim, PV, device and filesystem all agreeing. Calling DigitalOcean directly for that volume would grow the device while the PV kept declaring the old capacity and the filesystem never grew at all. One operation, one correct mechanism per owner — not two ways to do it.
         * @summary Grows a volume.
         * @param {string} id ID is the DO volume id, from the path.
         * @param {VolumeIn} volumeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminResizeVolume: async (id: string, volumeIn: VolumeIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminResizeVolume', 'id', id)
            // verify required parameter 'volumeIn' is not null or undefined
            assertParamExists('adminResizeVolume', 'volumeIn', volumeIn)
            const localVarPath = `/v1/admin/infra/volumes/{id}/resize`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(volumeIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.  ORTHOGONAL to /v1/admin/finance, which is the COGS/margin view of what WE pay vendors. This is the customer side: what each customer holds, spends and subscribes to.  arpu divides realized spend by PAYING customers, not by all of them — a fleet of free signups must not deflate the number. A customer counts as paying when it has spend or MRR.  An org whose money did not read degrades to honest zeros and marks the commerce source degraded in sources[], so a partial fleet read is visible instead of quietly low.
         * @summary Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminRevenue: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/revenue`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
         * @summary Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
         * @param {string} [owner] Owner is the org whose rows to read. Defaults to the admin org, which owns the platform\&#39;s roles and applications.
         * @param {string} [p] Page is the 1-based page number. Forwarded only when set — IAM applies its own default otherwise.
         * @param {string} [pageSize] PageSize is rows per page. Forwarded only when set.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminRoles: async (owner?: string, p?: string, pageSize?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/roles`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (owner !== undefined) {
                localVarQueryParameter['owner'] = owner;
            }

            if (p !== undefined) {
                localVarQueryParameter['p'] = p;
            }

            if (pageSize !== undefined) {
                localVarQueryParameter['pageSize'] = pageSize;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.  The response states what the board could NOT prove: DOKS picks which nodes a shrink removes, so no particular pod is shown to survive one. See NodePool.ScaleTo.
         * @summary Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.
         * @param {string} id ID is the DOKS cluster id, from the path.
         * @param {string} pool Pool is the node pool, from the path. Its DO id or its name — both are unique within a cluster, and an operator reads the name off the board.
         * @param {ScaleIn} scaleIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminScaleNodePool: async (id: string, pool: string, scaleIn: ScaleIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminScaleNodePool', 'id', id)
            // verify required parameter 'pool' is not null or undefined
            assertParamExists('adminScaleNodePool', 'pool', pool)
            // verify required parameter 'scaleIn' is not null or undefined
            assertParamExists('adminScaleNodePool', 'scaleIn', scaleIn)
            const localVarPath = `/v1/admin/infra/clusters/{id}/nodepools/{pool}/scale`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)))
                .replace(`{${"pool"}}`, encodeURIComponent(String(pool)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(scaleIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine. This is the \"remove the waitlist one service at a time\" view.
         * @summary Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminServices: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/services`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands. The flip is hot: this pod applies it immediately and peers converge within one evaluation TTL (15s by default), with no redeploy.  The body reaches the flag engine BYTE-FOR-BYTE — it is the engine\'s definition format, not this layer\'s, so a field the engine understands and admin does not must still arrive intact. setFlagIn names the two fields that matter for documentation; it is not a filter.  The write is recorded in the store\'s activity log against the caller\'s email.
         * @summary Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands.
         * @param {string} key Key is the switch to write, taken from the path (e.g. \&quot;waitlist.chat\&quot;).
         * @param {SetFlagIn} setFlagIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSetFlag: async (key: string, setFlagIn: SetFlagIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('adminSetFlag', 'key', key)
            // verify required parameter 'setFlagIn' is not null or undefined
            assertParamExists('adminSetFlag', 'setFlagIn', setFlagIn)
            const localVarPath = `/v1/admin/flags/{key}`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(setFlagIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Upserts the platform plan promo — the ONE place the offer is configured.  The body is commerce\'s own promo contract and is forwarded BYTE-FOR-BYTE, so no field commerce accepts is dropped in transit. promoIn names its documented fields.
         * @summary Upserts the platform plan promo — the ONE place the offer is configured.
         * @param {PromoIn} promoIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSetPromo: async (promoIn: PromoIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'promoIn' is not null or undefined
            assertParamExists('adminSetPromo', 'promoIn', promoIn)
            const localVarPath = `/v1/admin/promos`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(promoIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Flips ONE service\'s waitlist switch — the launch lever. Hot: it takes effect on this pod immediately and on peers within one evaluation TTL, with no redeploy. An unknown service is a 404, not a silent create; onboarding goes through upsertService.
         * @summary Flips ONE service\'s waitlist switch — the launch lever.
         * @param {string} service Service is the slug to flip, taken from the path.
         * @param {ServiceModeIn} serviceModeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSetServiceMode: async (service: string, serviceModeIn: ServiceModeIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'service' is not null or undefined
            assertParamExists('adminSetServiceMode', 'service', service)
            // verify required parameter 'serviceModeIn' is not null or undefined
            assertParamExists('adminSetServiceMode', 'serviceModeIn', serviceModeIn)
            const localVarPath = `/v1/admin/services/{service}/mode`
                .replace(`{${"service"}}`, encodeURIComponent(String(service)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(serviceModeIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.  It re-scans the board first (never the cache) so the volume it snapshots is one that exists right now, and audits the outcome either way.
         * @summary Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.
         * @param {string} id ID is the DO volume id, from the path.
         * @param {VolumeIn} volumeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSnapshotVolume: async (id: string, volumeIn: VolumeIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminSnapshotVolume', 'id', id)
            // verify required parameter 'volumeIn' is not null or undefined
            assertParamExists('adminSnapshotVolume', 'volumeIn', volumeIn)
            const localVarPath = `/v1/admin/infra/volumes/{id}/snapshot`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(volumeIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers GET /v1/admin/subscriptions.   GET /v1/admin/subscriptions?org=&status=&limit=
         * @summary Answers GET /v1/admin/subscriptions.
         * @param {string} [status] Status filters on the subscription\&#39;s LATEST lifecycle status (active, trialing, canceled, …), matched case-insensitively.
         * @param {string} [org] Org filters to one tenant, matched exactly.
         * @param {string} [limit] Limit caps the rows returned. total still reports the full match count.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSubscriptions: async (status?: string, org?: string, limit?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/subscriptions`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (status !== undefined) {
                localVarQueryParameter['status'] = status;
            }

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * subsystems answers GET /v1/admin/subsystems. ?range=24h|7d|30d bounds the telemetry window (default 30d) — the same enum, and the same helpers, as the o11y board.
         * @summary subsystems answers GET /v1/admin/subsystems.
         * @param {string} [range] Range bounds the telemetry window: 24h, 7d or 30d. Anything else, including empty, resolves to the default through the same o11yRange the o11y board uses.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSubsystems: async (range?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/subsystems`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token. Fully reversible with ReactivateCustomer.  The result names every user updated and every user that was NOT — a partial failure leaves the org in a mixed state and says so instead of reporting a clean success.
         * @summary Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token.
         * @param {string} org Org is the tenant slug from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSuspendCustomer: async (org: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('adminSuspendCustomer', 'org', org)
            const localVarPath = `/v1/admin/customers/{org}/suspend`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the operator\'s \"Sync now\" button. There is nothing to kick: admin aggregates LIVE on every read, so the button is just a re-read. It acknowledges honestly with started:true rather than pretending a batch job was queued.
         * @summary Answers the operator\'s \"Sync now\" button.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSync: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/sync`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Edits one cap by id — raise or lower the ceiling, flip enforcement. The body is commerce\'s spend-alert patch contract, forwarded byte-for-byte.
         * @summary Edits one cap by id — raise or lower the ceiling, flip enforcement.
         * @param {string} id ID is the cap to edit or remove, from the path. Unused by the list and create ops.
         * @param {CapIn} capIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUpdateCap: async (id: string, capIn: CapIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('adminUpdateCap', 'id', id)
            // verify required parameter 'capIn' is not null or undefined
            assertParamExists('adminUpdateCap', 'capIn', capIn)
            const localVarPath = `/v1/admin/caps/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(capIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy. Re-registering an existing service PRESERVES its live switch — editing the hosts of a service that is already open must not silently close it again.
         * @summary Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy.
         * @param {ServiceInput} serviceInput 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUpsertService: async (serviceInput: ServiceInput, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'serviceInput' is not null or undefined
            assertParamExists('adminUpsertService', 'serviceInput', serviceInput)
            const localVarPath = `/v1/admin/services`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(serviceInput, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.  It reads the AI ledger (ledger.go), which is the plane that owns this question. It used to ask the commerce billing API instead, once per org, and answer with a hardcoded empty series, zero tokens and zero requests, on the reasoning that a trend and a split were \"not derivable from the commerce billing API\". They are not — but the question was never commerce\'s. hanzo.cloud_usage carries a row per served request, so all three fall out of the same window the totals do.
         * @summary Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.
         * @param {string} [org] Org reads ONE tenant\&#39;s trailing-30-day total instead of the fleet sum. Honoured for a SuperAdmin only — a white-label admin always reads their own org.  The window is the one core.OrgMoney returns, and it is what the operator board beside this already labelled (\&quot;Daily, last 30 days\&quot;). The wire used to say month-to-date while that UI said 30 days; they agree now. This comment is REGENERATED into plugin/admin/openapi.json and openapi.yaml as the ?org parameter description, so a stale word here ships as a contradiction inside one spec file — which is the drift this whole change set exists to remove.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUsage: async (org?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/usage`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).  The class is resolved at the PROVIDER level from the credit ledger, not per call — the per-call split, and the `byo` class, arrive when the metering write stamps a funding column on cloud_usage and this can GROUP BY it directly. Until then a provider with remaining grant reports all of its usage as credit, which is right in aggregate and approximate at the boundary where a grant runs out mid-window.  An unparseable window falls back to the last 30 days rather than refusing: this is a dashboard read, and a typo in a date must not blank the board.
         * @summary Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).
         * @param {string} [from] From is the inclusive start of the window. Unparseable or absent, together with To, falls back to the last 30 days.
         * @param {string} [to] To is the exclusive end of the window.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUsageFunding: async (from?: string, to?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/usage/funding`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (from !== undefined) {
                localVarQueryParameter['from'] = from;
            }

            if (to !== undefined) {
                localVarQueryParameter['to'] = to;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the user directory across the caller\'s tenant window, one page at a time. total is IAM\'s REAL total, so the console can page through it.  A SuperAdmin may aim the read at one tenant with org; a white-label admin cannot — for them the owner is hard-pinned to their own org and org is ignored, which is what keeps the directory from becoming a cross-tenant read.
         * @summary Lists the user directory across the caller\'s tenant window, one page at a time.
         * @param {string} [org] Org narrows the directory to ONE tenant. Honoured for a SuperAdmin only — a white-label admin is pinned to their own org and this is ignored.
         * @param {string} [q] Query is a free-text filter, matched by IAM as a \&quot;contains\&quot; over the user name.
         * @param {string} [p] Page is the 1-based page number. Defaults to \&quot;1\&quot;; IAM returns zero rows AND a zero total when it is unset, so this layer never leaves it empty.
         * @param {string} [pageSize] PageSize is rows per page. Defaults to \&quot;200\&quot;, the shared admin page size.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUsers: async (org?: string, q?: string, p?: string, pageSize?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/users`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (org !== undefined) {
                localVarQueryParameter['org'] = org;
            }

            if (q !== undefined) {
                localVarQueryParameter['q'] = q;
            }

            if (p !== undefined) {
                localVarQueryParameter['p'] = p;
            }

            if (pageSize !== undefined) {
                localVarQueryParameter['pageSize'] = pageSize;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.  A volume\'s usedGiB and pct are null, always: DO exposes capacity and attachment but no fill, so the console renders \"—\" rather than a number nobody measured. The datastore card is the one real fill here, and it is the number to scale on.  The two sources degrade independently — a DO outage still returns the datastore fill, and a disconnected datastore still returns the DO fleet.
         * @summary Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminVolumes: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/volumes`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.  The engine\'s payload is forwarded VERBATIM as data; the console normalizes it. When the engine is not configured on this deployment the read still succeeds, with an empty object and a msg saying so, so the panel shows an honest not-wired state instead of an error the operator would chase.
         * @summary Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.
         * @param {string} [waitlist] Waitlist is the waitlist slug to read (e.g. \&quot;chat\&quot;). The engine decides what an empty slug means.
         * @param {string} [page] Page is the 1-based page number.
         * @param {string} [pageSize] PageSize is entries per page.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminWaitlist: async (waitlist?: string, page?: string, pageSize?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/waitlist`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (waitlist !== undefined) {
                localVarQueryParameter['waitlist'] = waitlist;
            }

            if (page !== undefined) {
                localVarQueryParameter['page'] = page;
            }

            if (pageSize !== undefined) {
                localVarQueryParameter['pageSize'] = pageSize;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Grants a user waitlist points, moving them up toward the access cutoff. This is the access lever: the cutoff itself does not move, the person does.  It funnels through the engine\'s verified grant seam (POST /v1/waitlist/award with source=\"grant\" — the ONE path that honours an explicit points amount) and writes a tamper-evident audit row either way, so a FAILED grant is recorded too. The reason field goes only to that row.
         * @summary Grants a user waitlist points, moving them up toward the access cutoff.
         * @param {WaitlistBoostRequest} waitlistBoostRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminWaitlistBoost: async (waitlistBoostRequest: WaitlistBoostRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'waitlistBoostRequest' is not null or undefined
            assertParamExists('adminWaitlistBoost', 'waitlistBoostRequest', waitlistBoostRequest)
            const localVarPath = `/v1/admin/waitlist/boost`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(waitlistBoostRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.  PLATFORM SUDO ONLY, and a non-admin is refused outright. This is the cross-tenant view and it names orgs — exactly what the partner-facing leaderboard refuses to do. There is deliberately no org-scoped variant of this read; a partner sees its own standing through its own dashboard. Bounded per request.
         * @summary Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.
         * @param {number} [limit] Limit caps the rows returned. Absent or non-positive means the default of 500; anything above 1000 is clamped to 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminAffiliates: async (limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/affiliates`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.  It is a Hanzo platform operation: a caller who is not a SuperAdmin gets 403. It exposes the owning org of each author, which no tenant-facing read ever does.
         * @summary Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.
         * @param {number} [limit] Limit bounds the page. 0 or less means the default of 500; anything above 1000 is clamped to 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminAuthors: async (limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/authors`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.  The data object carries: id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger (every row with its spend, the share applied then, the platform\'s matching half, whether it satisfies the formula and the attribution edges that explain it), reconciliation (does the ledger foot to the balance) and window (what slice was actually returned) — plus period when one was requested.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.
         * @param {string} id ID is the author record\&#39;s handle, from the path.
         * @param {string} [period] Period is the UTC accrual month, YYYY-MM. Empty means every period; any other shape is refused with 400.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminAuthorsByIdBasis: async (id: string, period?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getAdminAuthorsByIdBasis', 'id', id)
            const localVarPath = `/v1/admin/authors/{id}/basis`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (period !== undefined) {
                localVarQueryParameter['period'] = period;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console. Nothing is hidden: this is the admin\'s view of what exists and what is currently off, in beta or generally available. SuperAdmin only; every other caller is refused.
         * @summary Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminCatalog: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/catalog`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta. An item nobody has touched is absent, because an untouched item is generally available; the console composes the candidate list from the live catalog. SuperAdmin only; every other caller is refused.
         * @summary Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminEnablement: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/enablement`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.  Read the liability figure carefully — it is commission accrued and NOT yet paid, so it is money owed, not money spent, and the per-level split says how much of it comes from direct referrals versus the second and third levels.  PLATFORM SUDO ONLY, cross-tenant, and it names orgs. It reads the SAME single attribution spine the accrual itself walks, so the board and the ledger cannot disagree. Amounts are integer cents.
         * @summary Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminReferrals: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/referrals`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns every referral edge in the directory with a fleet summary.  SuperAdmin only, fail-closed. This is the ATTRIBUTION directory — who referred whom and whether that referee became a customer. It carries no amounts because this package issues none. The cross-tenant referral ANALYTICS board (top referrers, conversion) is a different surface, GET /v1/admin/referrals, owned by the affiliates subsystem over the shared attribution spine.
         * @summary Returns every referral edge in the directory with a fleet summary.
         * @param {string} [limit] Limit is how many referrals to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 500; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminReferralsBonuses: async (limit?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/referrals/bonuses`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root. ?limit= bounds the journal page.
         * @summary Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root.
         * @param {number} [limit] Limit caps the journal entries returned. Out of range or unparseable takes the default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminTreasury: async (limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/treasury`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sets one model\'s availability overlay — and the price overrides applied on top of the catalog — then answers the new effective overlay, so a console needs no second read. The model id is the whole remaining path, so a slashed id like `acme/some-model-1` addresses intact.  SuperAdmin only; every other caller is 403, decided before the body is read. The overlay is PLATFORM-WIDE — this is the catalog every org prices against, not a per-org setting — and `betaOrgs` is what narrows a beta to named orgs.  Only the fields the patch names change; an entry with no overlay yet starts from the catalog default, which is enabled. `state` is the coherent tri-state setter (`off`|`beta`|`ga`) and the low-level `enabled`/`beta` flags are applied AFTER it, so they win where both are sent; anything else in `state` is 400. A field sent as an explicit `null` arrives indistinguishable from an absent one, so null does not clear anything.  The rule worth reading twice: a disabled entry that still carries beta orgs IS a beta — `{\"enabled\":false,\"betaOrgs\":[\"acme\"]}` leaves acme seeing the model. Only an explicit `off` (or `beta:false`) with an empty list is the absolute kill switch that a user\'s own beta opt-in can never re-open.  `overrides` is an RFC 7386 merge patch, stored and echoed back verbatim; it must be a JSON object or null — an array or a scalar is refused — and is bounded in size and nesting depth. An uninitialised overlay store answers 503.
         * @summary Turn one model off, into beta for named orgs, or generally available
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchAdminCatalogModelsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('patchAdminCatalogModelsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/admin/catalog/models/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sets one provider\'s availability overlay.  The overlay decides whether a provider is off, in beta for named orgs, or generally available, and carries the price overrides applied on top of the catalog. Only the fields the patch names change; every other field keeps the value it had, and an absent overlay starts from the catalog default (enabled). Answers the new effective overlay, so a console needs no second read.  SuperAdmin only.
         * @summary Sets one provider\'s availability overlay.
         * @param {string} name Name is the provider the overlay belongs to, from the URL.
         * @param {ProviderPatchIn} providerPatchIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchAdminCatalogProvidersByName: async (name: string, providerPatchIn: ProviderPatchIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('patchAdminCatalogProvidersByName', 'name', name)
            // verify required parameter 'providerPatchIn' is not null or undefined
            assertParamExists('patchAdminCatalogProvidersByName', 'providerPatchIn', providerPatchIn)
            const localVarPath = `/v1/admin/catalog/providers/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(providerPatchIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.  The code is taken from the body if one is given, else the vanity code the applicant requested, else a slug derived for them. Codes are ONE global namespace, so a taken code is a 409 and nothing is approved. The minted code is also mirrored as a link row so click tracking is uniform across every code the affiliate holds; that mirror is best-effort and its failure never fails the approval.  Approval is what makes an affiliate eligible: before it, attribution against its code does not resolve and no sweep accrues to it. PLATFORM SUDO ONLY. Audited.
         * @summary Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.
         * @param {string} id ID is the affiliate to approve, from the path.
         * @param {Approval} approval 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdApprove: async (id: string, approval: Approval, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postAdminAffiliatesByIdApprove', 'id', id)
            // verify required parameter 'approval' is not null or undefined
            assertParamExists('postAdminAffiliatesByIdApprove', 'approval', approval)
            const localVarPath = `/v1/admin/affiliates/{id}/approve`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(approval, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.  The amount is reserved atomically against the affiliate\'s PENDING commission — accrued minus paid — so a payout can never exceed what is owed. The METHOD decides whether money actually moves: `credits` issues a commerce grant into the affiliate ORG\'s own wallet, tagged so the ledger can tell an affiliate payout apart from an admin or referral grant; every other method — wire, paypal and the rest — is RECORD-ONLY: the payout row and the balances move, the cash is disbursed out of band.  The amount is integer cents and must be positive. PLATFORM SUDO ONLY. Audited.
         * @summary Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.
         * @param {string} id ID is the affiliate to pay, from the path.
         * @param {Disbursal} disbursal 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdPayout: async (id: string, disbursal: Disbursal, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postAdminAffiliatesByIdPayout', 'id', id)
            // verify required parameter 'disbursal' is not null or undefined
            assertParamExists('postAdminAffiliatesByIdPayout', 'disbursal', disbursal)
            const localVarPath = `/v1/admin/affiliates/{id}/payout`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(disbursal, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.  The rate is CAPPED so that the direct rate plus the platform-wide second- and third-level rates can never exceed the whole margin — the structural guarantee that everything paid on one source event stays inside the margin actually earned. The cap is resolved from the rates in force at the moment of the call and quoted in the refusal, because those switches move; a hardcoded bound would start lying the moment somebody edits the schedule.  Only the direct level is per-affiliate. The second and third levels are platform switches and are not settable here. The change applies to FUTURE accruals — commission already latched for a period is not recomputed. PLATFORM SUDO ONLY. Audited.
         * @summary Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.
         * @param {string} id ID is the affiliate whose direct rate moves, from the path.
         * @param {RateSet} rateSet 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdRate: async (id: string, rateSet: RateSet, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postAdminAffiliatesByIdRate', 'id', id)
            // verify required parameter 'rateSet' is not null or undefined
            assertParamExists('postAdminAffiliatesByIdRate', 'rateSet', rateSet)
            const localVarPath = `/v1/admin/affiliates/{id}/rate`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(rateSet, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.  It CLAWS NOTHING BACK. Commission already accrued stays accrued and stays payable, and existing attribution edges are left standing — suspension ends earning, it does not unwind history. PLATFORM SUDO ONLY. Audited.
         * @summary Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.
         * @param {string} id ID is the affiliate\&#39;s server-minted handle, \&quot;aff_\&quot;-prefixed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdSuspend: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postAdminAffiliatesByIdSuspend', 'id', id)
            const localVarPath = `/v1/admin/affiliates/{id}/suspend`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.  This is the cron path, and it is LATCHED at most once per affiliate, source org and period — so re-running it inside the same period accrues nothing further. Safe to retry, and safe to run by hand beside the schedule.  Commission is a rate of Hanzo\'s MARGIN on that spend, never of the customer\'s gross bill, so every level\'s share summed over one source event stays within the margin actually earned and the customer\'s charge is untouched. Nothing accrues past the third upline level, and only an APPROVED affiliate accrues at all.  The same spend read drives the OSS author royalty — one read, both programs — so the answer reports royalties accrued alongside. PLATFORM SUDO ONLY. Bounded per run; a source whose spend cannot be read is skipped and picked up next time, never half-accrued.
         * @summary Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesSweep: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/affiliates/sweep`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Admits one author to EARNING, optionally on a negotiated royalty share. Until this runs, a connected author accrues nothing however many verified repositories they have.  A share override applies from here forward only — existing ledger rows keep the share that was applied when they were written, because a rate change must never rewrite what was already owed.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Admits one author to EARNING, optionally on a negotiated royalty share.
         * @param {string} id ID is the author to approve, from the path.
         * @param {ApproveRequest} approveRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsByIdApprove: async (id: string, approveRequest: ApproveRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postAdminAuthorsByIdApprove', 'id', id)
            // verify required parameter 'approveRequest' is not null or undefined
            assertParamExists('postAdminAuthorsByIdApprove', 'approveRequest', approveRequest)
            const localVarPath = `/v1/admin/authors/{id}/approve`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(approveRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Records a payout of accrued royalty and settles it.  The amount is RESERVED against the author\'s pending royalty atomically before anything is paid, so a payout can never exceed what is owed even under concurrent calls. An external author\'s payout is then BACKED against the platform reserve fund — a second, independent guard — and refused with 402 if the reserve cannot cover it, with the reservation voided. A \"credits\" method issues the actual wallet grant after both guards; a cash method is record-only. A first-party (treasury) author\'s royalty is realized into Hanzo\'s own reserve instead of an external wallet, and every payout row discloses which of the three it was.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Records a payout of accrued royalty and settles it.
         * @param {string} id ID is the author to pay, from the path.
         * @param {PayoutRequest} payoutRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsByIdPayout: async (id: string, payoutRequest: PayoutRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postAdminAuthorsByIdPayout', 'id', id)
            // verify required parameter 'payoutRequest' is not null or undefined
            assertParamExists('postAdminAuthorsByIdPayout', 'payoutRequest', payoutRequest)
            const localVarPath = `/v1/admin/authors/{id}/payout`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(payoutRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Stops one author earning. Their record, verified claims and ledger are untouched — suspension halts future accrual, it does not erase what was already owed, and it does not delete the evidence behind it.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Stops one author earning.
         * @param {string} id ID is the author record\&#39;s handle, \&quot;aut_\&quot;-prefixed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsByIdSuspend: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postAdminAuthorsByIdSuspend', 'id', id)
            const localVarPath = `/v1/admin/authors/{id}/suspend`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.  It is an OVERRIDE, not the mechanism: a background scheduler runs the same sweep on its own, and every author\'s dashboard read sweeps their own accruals lazily. This is the manual trigger for an operator who needs the numbers now. It is idempotent — the per-period latch means running it twice accrues nothing the second time.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsSweep: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/authors/sweep`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Qualify-checks every pending referral and advances the ones that now qualify.  SuperAdmin only, fail-closed. This is the cron path, and the ONLY path that advances a referral: a referee QUALIFIES once they have made metered spend — the honest signal that they actually used the product rather than merely signing up.  Qualifying moves NO money. It records that an attribution became a real customer; what is owed for that is an affiliate payable in commerce, settled by wire or to a connected wallet. One pass is bounded, so a large backlog drains over several runs instead of wedging one request, and the latch makes the transition at-most-once under a concurrent sweep.  It reads nothing from the caller — the counters it returns are the whole result.
         * @summary Qualify-checks every pending referral and advances the ones that now qualify.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminReferralsSweep: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/referrals/sweep`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status. When the chain path is wired it signs and submits the anchor transaction and records it; when it is not, it returns the root that WOULD be committed plus the exact remaining wiring step and records nothing false. A submit that fails still answers 200 with the anchor\'s own status set to \"error\" — the attempt is the product. SuperAdmin only.
         * @summary Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasuryAnchor: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/treasury/anchor`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy. 0–10000; the change is audited. SuperAdmin only.
         * @summary Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy.
         * @param {PolicyRequest} policyRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasuryPolicy: async (policyRequest: PolicyRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'policyRequest' is not null or undefined
            assertParamExists('postAdminTreasuryPolicy', 'policyRequest', policyRequest)
            const localVarPath = `/v1/admin/treasury/policy`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(policyRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote. A repeat of the same ref is at-most-once and reports created=false. SuperAdmin only.
         * @summary Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote.
         * @param {SeedRequest} seedRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasurySeed: async (seedRequest: SeedRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'seedRequest' is not null or undefined
            assertParamExists('postAdminTreasurySeed', 'seedRequest', seedRequest)
            const localVarPath = `/v1/admin/treasury/seed`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(seedRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved. It is idempotent per period: a re-run of a period already swept accrues nothing and reports created=false. SuperAdmin only.
         * @summary Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved.
         * @param {SweepRequest} sweepRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasurySweep: async (sweepRequest: SweepRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'sweepRequest' is not null or undefined
            assertParamExists('postAdminTreasurySweep', 'sweepRequest', sweepRequest)
            const localVarPath = `/v1/admin/treasury/sweep`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(sweepRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta. It is generic over kind, so the same call manages models, providers and product features through the one registry. `off` is an absolute kill switch: a self-service opt-in can never re-open it. SuperAdmin only; every other caller is refused.
         * @summary Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta.
         * @param {SetEnablementBody} setEnablementBody 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putAdminEnablement: async (setEnablementBody: SetEnablementBody, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'setEnablementBody' is not null or undefined
            assertParamExists('putAdminEnablement', 'setEnablementBody', setEnablementBody)
            const localVarPath = `/v1/admin/enablement`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(setEnablementBody, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas. It provisions-or-resolves the caller org\'s treasury wallet on the deployed MPC ring and installs it, so every later anchor commits the ledger root SIGNED BY THE QUORUM WALLET instead of a lone KMS key. Idempotent — a repeat resolves the same wallet, which is why the address is a PUT. SuperAdmin only.
         * @summary Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putAdminTreasuryAnchorSigner: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/admin/treasury/anchor/signer`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * AdminApi - functional programming interface
 * @export
 */
export const AdminApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = AdminApiAxiosParamCreator(configuration)
    return {
        /**
         * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).  Every signal degrades INDEPENDENTLY — a table that is absent or errors contributes its zero value and the read still succeeds. Generation latency is a SEPARATE query from generations and cost on purpose: a duration/attribute mismatch there must not zero the two numbers that did read.
         * @summary Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminAIMetrics(range?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AimetricsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminAIMetrics(range, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminAIMetrics']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.  The window is the caller\'s, not the fleet\'s: a SuperAdmin gets every org, a white-label admin only their own subtree (core.ScopedOrgs, the one scope predicate).  sources[] carries each upstream\'s freshness so a partial read is VISIBLE rather than silently low: a ledger that answered for only some orgs marks commerce-ledger degraded instead of publishing an undercount as healthy.
         * @summary Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminAnalytics(range?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AnalyticsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminAnalytics(range, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminAnalytics']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications. These are the platform\'s OIDC clients — the console reads clientId off each row.
         * @summary Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications.
         * @param {string} [owner] Owner is the org whose rows to read. Defaults to the admin org, which owns the platform\&#39;s roles and applications.
         * @param {string} [p] Page is the 1-based page number. Forwarded only when set — IAM applies its own default otherwise.
         * @param {string} [pageSize] PageSize is rows per page. Forwarded only when set.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminApplications(owner?: string, p?: string, pageSize?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<IamRowsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminApplications(owner, p, pageSize, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminApplications']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.  When cloud has no local store configured it falls back to forwarding IAM\'s own get-records trail verbatim — a DIFFERENT trail, federated so the endpoint never regresses to an empty list. Those rows carry no integrity of ours, so the field is null there.
         * @summary Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.
         * @param {string} [org] Org restricts the trail to one tenant.
         * @param {string} [sub] Sub restricts it to one actor (the validated subject that made the request).
         * @param {string} [action] Action restricts it to one action name, e.g. \&quot;admin.waitlist.grant\&quot;.
         * @param {string} [resource] Resource restricts it to one resource kind, e.g. \&quot;credit-grant\&quot;.
         * @param {string} [resourceId] ResourceID restricts it to one resource instance.
         * @param {string} [result] Result restricts it to \&quot;success\&quot; or \&quot;error\&quot;.
         * @param {string} [since] Since is the inclusive lower time bound, RFC3339. An unparseable value is ignored rather than refused — one malformed filter must not hide the trail.
         * @param {string} [until] Until is the upper time bound, RFC3339, with the same tolerance.
         * @param {string} [pageSize] PageSize is rows per page, default 100.
         * @param {string} [p] Page is the 1-based page number, driving the offset.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminAudit(org?: string, sub?: string, action?: string, resource?: string, resourceId?: string, result?: string, since?: string, until?: string, pageSize?: string, p?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RecordsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminAudit(org, sub, action, resource, resourceId, result, since, until, pageSize, p, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminAudit']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.  The trail is a FAMILY of chains, one per process, so the answer is a set and not a boolean: `intact`, `broken` and `unread` count the three verdicts and sum to the number of chains. A chain that could not be READ is reported `unread` and is never a pass — an unreadable chain and a verified one must not render the same, which is the whole reason this is not one flag.  An unconfigured store is an honest failure here rather than a fabricated pass.
         * @summary Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminAuditVerify(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<VerifyOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminAuditVerify(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminAuditVerify']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.  The scope is enforced TWICE: the upstream is asked for the caller\'s org, AND every row it returns is re-checked against the resolved scope. An upstream that ignored the filter therefore degrades to empty, never to a cross-tenant leak.  The Base engine is being embedded into cloud; until it lands this proxies BASE_ADMIN_URL and, when that is unset, answers 200 with an empty list and msg saying so — the honest not-yet state, never fabricated instances.
         * @summary Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminBases(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<BasesOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminBases(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminBases']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.  These are the SAME rows the customer edits in their own console — a platform override and a customer budget are one model, not two.
         * @summary Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.
         * @param {string} [org] Org is the tenant to act on. Required for a SuperAdmin — they must name their target; ignored for a white-label admin, who always acts on their own org.
         * @param {string} [id] ID is the cap to edit or remove, from the path. Unused by the list and create ops.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminCaps(org?: string, id?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminCaps(org, id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminCaps']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event. The console folds these into its org → app → project tree.  A machine counts as ACTIVE when its LATEST lifecycle event is not a terminal one (stop/destroy/terminate/delete/off/shutdown/expire and their past tenses) — the same fold the console applies, done in the warehouse so the count is over every machine and not just the page.  Honest-empty when the warehouse is not connected or hanzo.compute_usage is not provisioned yet: an empty list, never a fabricated fleet.
         * @summary Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event.
         * @param {string} [kind] Kind narrows to one workload class (bot | machine | cluster | nodepool | container | function | …). An OPEN spectrum matched as a plain string, lowercased to the warehouse\&#39;s convention; empty means every kind.
         * @param {string} [org] Org narrows to one tenant. Empty means every tenant — this board is cross-tenant by nature.
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as 30d.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminCompute(kind?: string, org?: string, range?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ComputeOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminCompute(kind, org, range, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminCompute']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.  It is the ONE infra change that does not go through the run discipline, because there is no destructive verdict to check: cordoning is reversible and evicting respects the cluster\'s own PodDisruptionBudgets. It reads the cached board for the same reason. The outcome is audited either way, and the result reports how many pods were evicted.
         * @summary Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.
         * @param {string} id ID is the node\&#39;s droplet id, from the path.
         * @param {CordonIn} cordonIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminCordonNode(id: string, cordonIn: CordonIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MutationOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminCordonNode(id, cordonIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminCordonNode']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows. The body is commerce\'s spend-alert contract, forwarded byte-for-byte.
         * @summary Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows.
         * @param {CapIn} capIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminCreateCap(capIn: CapIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminCreateCap(capIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminCreateCap']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers GET /v1/admin/customers/:org.
         * @summary Answers GET /v1/admin/customers/:org.
         * @param {string} org Org is the tenant slug from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminCustomer(org: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CustomerDetailOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminCustomer(org, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminCustomer']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.  Each row costs one IAM read plus the org\'s money reads, fanned out under a fixed concurrency ceiling so a large fleet cannot stampede the upstreams. Every read is best-effort per row: an upstream miss degrades THAT field to its honest zero rather than failing the fleet.
         * @summary Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminCustomers(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CustomersOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminCustomers(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminCustomers']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes one cap by id, lifting the ceiling entirely.
         * @summary Removes one cap by id, lifting the ceiling entirely.
         * @param {string} id ID is the cap to edit or remove, from the path. Unused by the list and create ops.
         * @param {string} [org] Org is the tenant to act on. Required for a SuperAdmin — they must name their target; ignored for a white-label admin, who always acts on their own org.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminDeleteCap(id: string, org?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminDeleteCap(id, org, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminDeleteCap']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Destroys a droplet the board has just proven is NOT a DOKS node. There is no snapshot-first undo for a droplet the way there is for a volume: the local disk goes with it.
         * @summary Destroys a droplet the board has just proven is NOT a DOKS node.
         * @param {string} id ID is the DO droplet id, from the path. Numeric.
         * @param {string} [size] Size is the target DigitalOcean size slug on resize, e.g. \&quot;s-4vcpu-8gb\&quot;.
         * @param {boolean} [disk] Disk requests a PERMANENT resize that grows the disk. DO can never resize such a droplet down again, so it defaults false — a CPU/RAM-only change, reversible.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminDeleteDroplet(id: string, size?: string, disk?: boolean, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MutationOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminDeleteDroplet(id, size, disk, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminDeleteDroplet']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
         * @summary Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
         * @param {string} id ID is the DO load balancer id, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminDeleteLoadBalancer(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MutationOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminDeleteLoadBalancer(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminDeleteLoadBalancer']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Destroys a volume the board has just proven no PersistentVolume in any cluster references. Irreversible, so it snapshots first unless explicitly waived — the snapshot IS the undo.
         * @summary Destroys a volume the board has just proven no PersistentVolume in any cluster references.
         * @param {string} id ID is the DO volume id, from the path.
         * @param {string} [snapshot] Snapshot is the snapshot-first switch on DELETE. Anything other than the literal \&quot;false\&quot; snapshots before destroying — the snapshot IS the undo, so waiving it is deliberate and explicit.
         * @param {string} [name] Name is the snapshot name on the snapshot action. Blank gets a deterministic \&quot;&lt;volume&gt;-predelete-&lt;unix&gt;\&quot; so the undo is findable in the DO console.
         * @param {number} [sizeGiB] SizeGiB is the target size on the resize action. A volume only ever grows — ExpandTo is the verdict that refuses a shrink, so this is not validated here.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminDeleteVolume(id: string, snapshot?: string, name?: string, sizeGiB?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MutationOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminDeleteVolume(id, snapshot, name, sizeGiB, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminDeleteVolume']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Stops the plugin. Its routes STAY REGISTERED and answer 503 — not 404.  That is zip\'s choice and this keeps it. Removing the routes would mutate the route table, and re-adding them on enable would grow it without bound across repeated cycles, which is the invariant that makes reloads flat in memory. It is also the better answer: 404 says \"no such API\" and a client may cache it and stop retrying, while 503 says \"this API exists and is down right now\", which is true and retryable. Which of the two 503s this is — deliberate stop or crash — is what the status\'s disabled flag reports.
         * @summary Stops the plugin.
         * @param {string} name Name is the app, from the path.
         * @param {NameIn} nameIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminDisablePlugin(name: string, nameIn: NameIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ActionOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminDisablePlugin(name, nameIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminDisablePlugin']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag. Named for what an operator means by it.
         * @summary Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag.
         * @param {string} name Name is the app, from the path.
         * @param {NameIn} nameIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminEnablePlugin(name: string, nameIn: NameIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ActionOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminEnablePlugin(name, nameIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminEnablePlugin']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers GET /v1/admin/finance. It reads the multi-vendor COGS from commerce /v1/costs, the DO promo-credit/burn-down treasury view, and the fleet commerce revenue, then hands them to ComputeFinance. SuperAdmin only.
         * @summary Answers GET /v1/admin/finance.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminFinance(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<FinanceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminFinance(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminFinance']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.  It is IDEMPOTENT: the deposit uses the fixed ref \"backfill:<org>\", so re-running it credits the wallet at most once. Safe to retry.  The pre-migration balance is read from the CO-RESIDENT commerce ledger, not over HTTP: the admin HTTP client dials an unroutable in-process address and would read $0, and a phantom zero would silently carry nothing while reporting success. When commerce is not co-resident this fails rather than migrating nothing.
         * @summary Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.
         * @param {BackfillIn} backfillIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminFinanceBackfill(backfillIn: BackfillIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<BackfillOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminFinanceBackfill(backfillIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminFinanceBackfill']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
         * @summary Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminFlags(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<FlagsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminFlags(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminFlags']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.  The credit lands on the account account.Payer resolves, NOT necessarily the org: name a member of a pooled org and the pool is credited. The receipt echoes the subject so the caller can see which.
         * @summary Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.
         * @param {string} org Org is the tenant to credit. Required.
         * @param {GrantIn} grantIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminGrantCredit(org: string, grantIn: GrantIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<GrantOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminGrantCredit(org, grantIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminGrantCredit']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.  It is a PROJECTION of the tamper-evident audit trail, not a second store: every grant is written there as action \"admin.customer.credit\", so this view cannot drift from what actually happened, and FAILED grants appear too.  A deployment with no local audit store has no history to project, and says so with an empty list and a msg rather than an error.
         * @summary Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.
         * @param {string} [org] Org filters by the ACTOR\&#39;s org (the staff org that issued the grant), which is rarely what a reader wants — the target org is a row field, not a filter.
         * @param {string} [result] Result filters by outcome: \&quot;success\&quot; or \&quot;error\&quot;. Empty returns both, which is the point of this view — a refused grant is as interesting as a granted one.
         * @param {string} [limit] Limit caps the rows returned. Default 200.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminGrants(org?: string, result?: string, limit?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<GrantsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminGrants(org, result, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminGrants']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.  It is cached for up to a minute because one read is a fan-out over the DO API plus a full pod/PV listing per cluster. Staleness is never load-bearing: every MUTATION re-scans from scratch and ignores this cache.  Only an unusable DO account is a hard failure. A partial read still produces a board, with the failing source named in sources[] — except for clusters and volumes, which the safety verdict depends on; without those the analysis degrades rather than classifying anything it cannot prove.
         * @summary Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.
         * @param {string} [refresh] Refresh, when present, forces a full re-scan instead of serving the cached snapshot. Every MUTATION re-scans regardless — this is only for the reader.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminInfra(refresh?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ReadOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminInfra(refresh, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminInfra']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers GET /v1/admin/invoices.   GET /v1/admin/invoices?org=&status=&limit=
         * @summary Answers GET /v1/admin/invoices.
         * @param {string} [status] Status filters on the invoice\&#39;s LATEST lifecycle status (paid, open, void, …), matched case-insensitively.
         * @param {string} [org] Org filters to one tenant, matched exactly.
         * @param {string} [limit] Limit caps the rows returned. total still reports the full match count.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminInvoices(status?: string, org?: string, limit?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<InvoicesOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminInvoices(status, org, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminInvoices']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Issues a credit grant to any org from the operator Grants view, with the target named in the body. It funnels through the SAME core.ApplyGrant that POST /v1/admin/customers/:org/credit uses, so there is exactly ONE credit-write path and one audit trail behind both.
         * @summary Issues a credit grant to any org from the operator Grants view, with the target named in the body.
         * @param {GrantIn} grantIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminIssueGrant(grantIn: GrantIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<GrantOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminIssueGrant(grantIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminIssueGrant']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is. The fields come from the sanitized identity headers the gate just read, so they are authoritative and never client-forgeable; nothing is looked up.
         * @summary Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminMe(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MeOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminMe(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminMe']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out). SuperAdmin only.   GET /v1/admin/metrics?window=30d&limit=20
         * @summary Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out).
         * @param {string} [window] Window is the movement window the new/churned MRR and the recent feed are measured over. Anything unrecognised falls back to the board default.
         * @param {string} [limit] Limit caps the top-customers table.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminMetrics(window?: string, limit?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MetricsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminMetrics(window, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminMetrics']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * moneyBoardHandler answers GET /v1/admin/money.
         * @summary moneyBoardHandler answers GET /v1/admin/money.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminMoney(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MoneyOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminMoney(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminMoney']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.  Every signal degrades INDEPENDENTLY. A table that is absent or errors contributes its zero value and the read still succeeds, so the board renders exactly what the warehouse holds rather than failing whole because one of four sources is missing. Same when the warehouse is not connected at all: the zero board, never a fabricated fleet.
         * @summary Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.
         * @param {string} [range] Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminO11y(range?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<O11yOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminO11y(range, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminO11y']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.  The rows are the caller\'s tenant window, not the fleet: a SuperAdmin gets every org, a white-label admin only their own subtree. A per-org read that fails degrades THAT row to an honest zero — this panel carries no sources[] channel to report freshness on, so the alternative would be a fleet total that silently reads healthy.
         * @summary Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminOrgs(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<OrgsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminOrgs(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminOrgs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.  It ALWAYS answers 200 — a tile board that fails as a whole because one upstream is down is useless. Instead every upstream reports itself in sources[]: ok, degraded, or not-configured. A commerce read that failed for ANY org marks that source degraded, because the spend/credits totals are then an undercount and must not read healthy.  The AI tiles — 30-day spend and tokens — come from the AI ledger (ledger.go), the plane that owns \"what was served\". They used to come from the money plane with the token counter hardcoded to zero, so the board read $0.00 and 0 tokens over a month in which the fleet served fifteen thousand requests. Credits still come from commerce, which owns the wallet.
         * @summary Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminOverview(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<OverviewOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminOverview(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminOverview']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.  Reading this from deployment config would answer what was INTENDED. Only the process knows what is TRUE, and during a rolling upgrade the two disagree on purpose.
         * @summary Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.
         * @param {string} [scope] Scope \&quot;host\&quot; answers for THIS host only. Default \&quot;fleet\&quot; fans out to every live peer. A peer answers a host-scoped read, which is what stops the fan-out recursing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminPlugins(scope?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ListOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminPlugins(scope, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminPlugins']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict. Optionally narrowed by kind, tier or env, each an exact match.  The rows are the SAME observation /v1/platform/fleet renders — read through the in-process platform seam, not a second k8s client — so the two boards can never disagree about what the fleet is. A PaaS plane that is not co-resident yields an honestly empty registry, never a fabricated row.
         * @summary Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict.
         * @param {string} [kind] Kind matches the operator App CR\&#39;s declared spec.role (sql|kv|generic|ingress).
         * @param {string} [tier] Tier matches the derived infra grouping (cloud|data|edge|daemon|paas|app).
         * @param {string} [env] Env matches the lifecycle namespace (main|test|dev).
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminProducts(kind?: string, tier?: string, env?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ProductsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminProducts(kind, tier, env, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminProducts']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads the current platform plan promo — the singleton discount offer, e.g. the 50%-off launch promo. Commerce stores it in the reserved platform namespace, so the org sent with the read is the admin org and the service token is what passes commerce\'s own platform-admin gate.
         * @summary Reads the current platform plan promo — the singleton discount offer, e.g.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminPromo(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminPromo(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminPromo']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger. SuperAdmin-guarded (see Routes).
         * @summary Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminProvidersCredit(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ProvidersCreditOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminProvidersCredit(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminProvidersCredit']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Restores access for every member of the org, undoing a suspend. It reports the same per-user breakdown.
         * @summary Restores access for every member of the org, undoing a suspend.
         * @param {string} org Org is the tenant slug from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminReactivateCustomer(org: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AccessOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminReactivateCustomer(org, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminReactivateCustomer']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Swaps a plugin for another build without dropping a request. The replacement is started and proven to be LISTENING before any traffic moves to it, so a bad build leaves the old one serving and returns an error rather than a hole; the old process then drains before it is killed.  With a version or url+sum it pins; naming a digest this host has run before is the rollback, and costs no network because the digest IS the cache key. With neither it restarts what is already loaded.  Fleet scope applies it to one host at a time and STOPS at the first failure, so a build that cannot come up reaches exactly one host.
         * @summary Swaps a plugin for another build without dropping a request.
         * @param {string} name Name is the app, from the path. It must be one the manifest declares.
         * @param {ReloadIn} reloadIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminReloadPlugin(name: string, reloadIn: ReloadIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ActionOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminReloadPlugin(name, reloadIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminReloadPlugin']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Changes a droplet\'s plan. Same refusal as delete and for the same reason: a DOKS node\'s size is the node pool\'s to declare.  disk=true is a PERMANENT resize — the disk grows and DO can never resize the droplet DOWN again. disk=false (the default) changes CPU/RAM only and is reversible. DO requires the droplet to be powered off and applies the change asynchronously, so the response carries the action to poll, not a completed change.
         * @summary Changes a droplet\'s plan.
         * @param {string} id ID is the DO droplet id, from the path. Numeric.
         * @param {DropletIn} dropletIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminResizeDroplet(id: string, dropletIn: DropletIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MutationOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminResizeDroplet(id, dropletIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminResizeDroplet']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Grows a volume. GROW ONLY — see Volume.ExpandTo for why the other direction is a data migration this board deliberately refuses to run.  The MECHANISM follows the volume\'s owner, because there is exactly one way to grow each kind completely. A volume a PVC claims is grown by patching the claim: the CSI driver then resizes the DigitalOcean device AND grows the filesystem on it, leaving claim, PV, device and filesystem all agreeing. Calling DigitalOcean directly for that volume would grow the device while the PV kept declaring the old capacity and the filesystem never grew at all. One operation, one correct mechanism per owner — not two ways to do it.
         * @summary Grows a volume.
         * @param {string} id ID is the DO volume id, from the path.
         * @param {VolumeIn} volumeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminResizeVolume(id: string, volumeIn: VolumeIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MutationOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminResizeVolume(id, volumeIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminResizeVolume']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.  ORTHOGONAL to /v1/admin/finance, which is the COGS/margin view of what WE pay vendors. This is the customer side: what each customer holds, spends and subscribes to.  arpu divides realized spend by PAYING customers, not by all of them — a fleet of free signups must not deflate the number. A customer counts as paying when it has spend or MRR.  An org whose money did not read degrades to honest zeros and marks the commerce source degraded in sources[], so a partial fleet read is visible instead of quietly low.
         * @summary Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminRevenue(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RevenueOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminRevenue(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminRevenue']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
         * @summary Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
         * @param {string} [owner] Owner is the org whose rows to read. Defaults to the admin org, which owns the platform\&#39;s roles and applications.
         * @param {string} [p] Page is the 1-based page number. Forwarded only when set — IAM applies its own default otherwise.
         * @param {string} [pageSize] PageSize is rows per page. Forwarded only when set.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminRoles(owner?: string, p?: string, pageSize?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<IamRowsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminRoles(owner, p, pageSize, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminRoles']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.  The response states what the board could NOT prove: DOKS picks which nodes a shrink removes, so no particular pod is shown to survive one. See NodePool.ScaleTo.
         * @summary Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.
         * @param {string} id ID is the DOKS cluster id, from the path.
         * @param {string} pool Pool is the node pool, from the path. Its DO id or its name — both are unique within a cluster, and an operator reads the name off the board.
         * @param {ScaleIn} scaleIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminScaleNodePool(id: string, pool: string, scaleIn: ScaleIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MutationOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminScaleNodePool(id, pool, scaleIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminScaleNodePool']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine. This is the \"remove the waitlist one service at a time\" view.
         * @summary Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminServices(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ServicesOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminServices(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminServices']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands. The flip is hot: this pod applies it immediately and peers converge within one evaluation TTL (15s by default), with no redeploy.  The body reaches the flag engine BYTE-FOR-BYTE — it is the engine\'s definition format, not this layer\'s, so a field the engine understands and admin does not must still arrive intact. setFlagIn names the two fields that matter for documentation; it is not a filter.  The write is recorded in the store\'s activity log against the caller\'s email.
         * @summary Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands.
         * @param {string} key Key is the switch to write, taken from the path (e.g. \&quot;waitlist.chat\&quot;).
         * @param {SetFlagIn} setFlagIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSetFlag(key: string, setFlagIn: SetFlagIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<FlagsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSetFlag(key, setFlagIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSetFlag']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Upserts the platform plan promo — the ONE place the offer is configured.  The body is commerce\'s own promo contract and is forwarded BYTE-FOR-BYTE, so no field commerce accepts is dropped in transit. promoIn names its documented fields.
         * @summary Upserts the platform plan promo — the ONE place the offer is configured.
         * @param {PromoIn} promoIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSetPromo(promoIn: PromoIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSetPromo(promoIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSetPromo']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Flips ONE service\'s waitlist switch — the launch lever. Hot: it takes effect on this pod immediately and on peers within one evaluation TTL, with no redeploy. An unknown service is a 404, not a silent create; onboarding goes through upsertService.
         * @summary Flips ONE service\'s waitlist switch — the launch lever.
         * @param {string} service Service is the slug to flip, taken from the path.
         * @param {ServiceModeIn} serviceModeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSetServiceMode(service: string, serviceModeIn: ServiceModeIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ServiceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSetServiceMode(service, serviceModeIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSetServiceMode']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.  It re-scans the board first (never the cache) so the volume it snapshots is one that exists right now, and audits the outcome either way.
         * @summary Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.
         * @param {string} id ID is the DO volume id, from the path.
         * @param {VolumeIn} volumeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSnapshotVolume(id: string, volumeIn: VolumeIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<VolumeSnapshotOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSnapshotVolume(id, volumeIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSnapshotVolume']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers GET /v1/admin/subscriptions.   GET /v1/admin/subscriptions?org=&status=&limit=
         * @summary Answers GET /v1/admin/subscriptions.
         * @param {string} [status] Status filters on the subscription\&#39;s LATEST lifecycle status (active, trialing, canceled, …), matched case-insensitively.
         * @param {string} [org] Org filters to one tenant, matched exactly.
         * @param {string} [limit] Limit caps the rows returned. total still reports the full match count.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSubscriptions(status?: string, org?: string, limit?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SubscriptionsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSubscriptions(status, org, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSubscriptions']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * subsystems answers GET /v1/admin/subsystems. ?range=24h|7d|30d bounds the telemetry window (default 30d) — the same enum, and the same helpers, as the o11y board.
         * @summary subsystems answers GET /v1/admin/subsystems.
         * @param {string} [range] Range bounds the telemetry window: 24h, 7d or 30d. Anything else, including empty, resolves to the default through the same o11yRange the o11y board uses.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSubsystems(range?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SubsystemsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSubsystems(range, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSubsystems']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token. Fully reversible with ReactivateCustomer.  The result names every user updated and every user that was NOT — a partial failure leaves the org in a mixed state and says so instead of reporting a clean success.
         * @summary Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token.
         * @param {string} org Org is the tenant slug from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSuspendCustomer(org: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AccessOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSuspendCustomer(org, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSuspendCustomer']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the operator\'s \"Sync now\" button. There is nothing to kick: admin aggregates LIVE on every read, so the button is just a re-read. It acknowledges honestly with started:true rather than pretending a batch job was queued.
         * @summary Answers the operator\'s \"Sync now\" button.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminSync(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SyncOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminSync(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminSync']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Edits one cap by id — raise or lower the ceiling, flip enforcement. The body is commerce\'s spend-alert patch contract, forwarded byte-for-byte.
         * @summary Edits one cap by id — raise or lower the ceiling, flip enforcement.
         * @param {string} id ID is the cap to edit or remove, from the path. Unused by the list and create ops.
         * @param {CapIn} capIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminUpdateCap(id: string, capIn: CapIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminUpdateCap(id, capIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminUpdateCap']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy. Re-registering an existing service PRESERVES its live switch — editing the hosts of a service that is already open must not silently close it again.
         * @summary Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy.
         * @param {ServiceInput} serviceInput 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminUpsertService(serviceInput: ServiceInput, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ServiceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminUpsertService(serviceInput, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminUpsertService']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.  It reads the AI ledger (ledger.go), which is the plane that owns this question. It used to ask the commerce billing API instead, once per org, and answer with a hardcoded empty series, zero tokens and zero requests, on the reasoning that a trend and a split were \"not derivable from the commerce billing API\". They are not — but the question was never commerce\'s. hanzo.cloud_usage carries a row per served request, so all three fall out of the same window the totals do.
         * @summary Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.
         * @param {string} [org] Org reads ONE tenant\&#39;s trailing-30-day total instead of the fleet sum. Honoured for a SuperAdmin only — a white-label admin always reads their own org.  The window is the one core.OrgMoney returns, and it is what the operator board beside this already labelled (\&quot;Daily, last 30 days\&quot;). The wire used to say month-to-date while that UI said 30 days; they agree now. This comment is REGENERATED into plugin/admin/openapi.json and openapi.yaml as the ?org parameter description, so a stale word here ships as a contradiction inside one spec file — which is the drift this whole change set exists to remove.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminUsage(org?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<UsageOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminUsage(org, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminUsage']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).  The class is resolved at the PROVIDER level from the credit ledger, not per call — the per-call split, and the `byo` class, arrive when the metering write stamps a funding column on cloud_usage and this can GROUP BY it directly. Until then a provider with remaining grant reports all of its usage as credit, which is right in aggregate and approximate at the boundary where a grant runs out mid-window.  An unparseable window falls back to the last 30 days rather than refusing: this is a dashboard read, and a typo in a date must not blank the board.
         * @summary Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).
         * @param {string} [from] From is the inclusive start of the window. Unparseable or absent, together with To, falls back to the last 30 days.
         * @param {string} [to] To is the exclusive end of the window.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminUsageFunding(from?: string, to?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<UsageFundingOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminUsageFunding(from, to, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminUsageFunding']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the user directory across the caller\'s tenant window, one page at a time. total is IAM\'s REAL total, so the console can page through it.  A SuperAdmin may aim the read at one tenant with org; a white-label admin cannot — for them the owner is hard-pinned to their own org and org is ignored, which is what keeps the directory from becoming a cross-tenant read.
         * @summary Lists the user directory across the caller\'s tenant window, one page at a time.
         * @param {string} [org] Org narrows the directory to ONE tenant. Honoured for a SuperAdmin only — a white-label admin is pinned to their own org and this is ignored.
         * @param {string} [q] Query is a free-text filter, matched by IAM as a \&quot;contains\&quot; over the user name.
         * @param {string} [p] Page is the 1-based page number. Defaults to \&quot;1\&quot;; IAM returns zero rows AND a zero total when it is unset, so this layer never leaves it empty.
         * @param {string} [pageSize] PageSize is rows per page. Defaults to \&quot;200\&quot;, the shared admin page size.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminUsers(org?: string, q?: string, p?: string, pageSize?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<UsersOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminUsers(org, q, p, pageSize, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminUsers']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.  A volume\'s usedGiB and pct are null, always: DO exposes capacity and attachment but no fill, so the console renders \"—\" rather than a number nobody measured. The datastore card is the one real fill here, and it is the number to scale on.  The two sources degrade independently — a DO outage still returns the datastore fill, and a disconnected datastore still returns the DO fleet.
         * @summary Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminVolumes(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<VolumesOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminVolumes(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminVolumes']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.  The engine\'s payload is forwarded VERBATIM as data; the console normalizes it. When the engine is not configured on this deployment the read still succeeds, with an empty object and a msg saying so, so the panel shows an honest not-wired state instead of an error the operator would chase.
         * @summary Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.
         * @param {string} [waitlist] Waitlist is the waitlist slug to read (e.g. \&quot;chat\&quot;). The engine decides what an empty slug means.
         * @param {string} [page] Page is the 1-based page number.
         * @param {string} [pageSize] PageSize is entries per page.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminWaitlist(waitlist?: string, page?: string, pageSize?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminWaitlist(waitlist, page, pageSize, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminWaitlist']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Grants a user waitlist points, moving them up toward the access cutoff. This is the access lever: the cutoff itself does not move, the person does.  It funnels through the engine\'s verified grant seam (POST /v1/waitlist/award with source=\"grant\" — the ONE path that honours an explicit points amount) and writes a tamper-evident audit row either way, so a FAILED grant is recorded too. The reason field goes only to that row.
         * @summary Grants a user waitlist points, moving them up toward the access cutoff.
         * @param {WaitlistBoostRequest} waitlistBoostRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async adminWaitlistBoost(waitlistBoostRequest: WaitlistBoostRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RawOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.adminWaitlistBoost(waitlistBoostRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.adminWaitlistBoost']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.  PLATFORM SUDO ONLY, and a non-admin is refused outright. This is the cross-tenant view and it names orgs — exactly what the partner-facing leaderboard refuses to do. There is deliberately no org-scoped variant of this read; a partner sees its own standing through its own dashboard. Bounded per request.
         * @summary Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.
         * @param {number} [limit] Limit caps the rows returned. Absent or non-positive means the default of 500; anything above 1000 is clamped to 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminAffiliates(limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<DirectoryOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminAffiliates(limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminAffiliates']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.  It is a Hanzo platform operation: a caller who is not a SuperAdmin gets 403. It exposes the owning org of each author, which no tenant-facing read ever does.
         * @summary Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.
         * @param {number} [limit] Limit bounds the page. 0 or less means the default of 500; anything above 1000 is clamped to 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminAuthors(limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AdminBook>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminAuthors(limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminAuthors']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.  The data object carries: id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger (every row with its spend, the share applied then, the platform\'s matching half, whether it satisfies the formula and the attribution edges that explain it), reconciliation (does the ledger foot to the balance) and window (what slice was actually returned) — plus period when one was requested.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.
         * @param {string} id ID is the author record\&#39;s handle, from the path.
         * @param {string} [period] Period is the UTC accrual month, YYYY-MM. Empty means every period; any other shape is refused with 400.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminAuthorsByIdBasis(id: string, period?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<BasisResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminAuthorsByIdBasis(id, period, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminAuthorsByIdBasis']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console. Nothing is hidden: this is the admin\'s view of what exists and what is currently off, in beta or generally available. SuperAdmin only; every other caller is refused.
         * @summary Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminCatalog(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AdminCatalogOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminCatalog(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminCatalog']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta. An item nobody has touched is absent, because an untouched item is generally available; the console composes the candidate list from the live catalog. SuperAdmin only; every other caller is refused.
         * @summary Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminEnablement(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AdminEnablementBoard>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminEnablement(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminEnablement']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.  Read the liability figure carefully — it is commission accrued and NOT yet paid, so it is money owed, not money spent, and the per-level split says how much of it comes from direct referrals versus the second and third levels.  PLATFORM SUDO ONLY, cross-tenant, and it names orgs. It reads the SAME single attribution spine the accrual itself walks, so the board and the ledger cannot disagree. Amounts are integer cents.
         * @summary Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminReferrals(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ReferralsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminReferrals(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminReferrals']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns every referral edge in the directory with a fleet summary.  SuperAdmin only, fail-closed. This is the ATTRIBUTION directory — who referred whom and whether that referee became a customer. It carries no amounts because this package issues none. The cross-tenant referral ANALYTICS board (top referrers, conversion) is a different surface, GET /v1/admin/referrals, owned by the affiliates subsystem over the shared attribution spine.
         * @summary Returns every referral edge in the directory with a fleet summary.
         * @param {string} [limit] Limit is how many referrals to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 500; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminReferralsBonuses(limit?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AdminBonusesEnvelope>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminReferralsBonuses(limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminReferralsBonuses']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root. ?limit= bounds the journal page.
         * @summary Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root.
         * @param {number} [limit] Limit caps the journal entries returned. Out of range or unparseable takes the default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAdminTreasury(limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AdminReportOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAdminTreasury(limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.getAdminTreasury']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sets one model\'s availability overlay — and the price overrides applied on top of the catalog — then answers the new effective overlay, so a console needs no second read. The model id is the whole remaining path, so a slashed id like `acme/some-model-1` addresses intact.  SuperAdmin only; every other caller is 403, decided before the body is read. The overlay is PLATFORM-WIDE — this is the catalog every org prices against, not a per-org setting — and `betaOrgs` is what narrows a beta to named orgs.  Only the fields the patch names change; an entry with no overlay yet starts from the catalog default, which is enabled. `state` is the coherent tri-state setter (`off`|`beta`|`ga`) and the low-level `enabled`/`beta` flags are applied AFTER it, so they win where both are sent; anything else in `state` is 400. A field sent as an explicit `null` arrives indistinguishable from an absent one, so null does not clear anything.  The rule worth reading twice: a disabled entry that still carries beta orgs IS a beta — `{\"enabled\":false,\"betaOrgs\":[\"acme\"]}` leaves acme seeing the model. Only an explicit `off` (or `beta:false`) with an empty list is the absolute kill switch that a user\'s own beta opt-in can never re-open.  `overrides` is an RFC 7386 merge patch, stored and echoed back verbatim; it must be a JSON object or null — an array or a scalar is refused — and is bounded in size and nesting depth. An uninitialised overlay store answers 503.
         * @summary Turn one model off, into beta for named orgs, or generally available
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchAdminCatalogModelsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchAdminCatalogModelsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.patchAdminCatalogModelsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sets one provider\'s availability overlay.  The overlay decides whether a provider is off, in beta for named orgs, or generally available, and carries the price overrides applied on top of the catalog. Only the fields the patch names change; every other field keeps the value it had, and an absent overlay starts from the catalog default (enabled). Answers the new effective overlay, so a console needs no second read.  SuperAdmin only.
         * @summary Sets one provider\'s availability overlay.
         * @param {string} name Name is the provider the overlay belongs to, from the URL.
         * @param {ProviderPatchIn} providerPatchIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchAdminCatalogProvidersByName(name: string, providerPatchIn: ProviderPatchIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Overlay>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchAdminCatalogProvidersByName(name, providerPatchIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.patchAdminCatalogProvidersByName']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.  The code is taken from the body if one is given, else the vanity code the applicant requested, else a slug derived for them. Codes are ONE global namespace, so a taken code is a 409 and nothing is approved. The minted code is also mirrored as a link row so click tracking is uniform across every code the affiliate holds; that mirror is best-effort and its failure never fails the approval.  Approval is what makes an affiliate eligible: before it, attribution against its code does not resolve and no sweep accrues to it. PLATFORM SUDO ONLY. Audited.
         * @summary Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.
         * @param {string} id ID is the affiliate to approve, from the path.
         * @param {Approval} approval 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAffiliatesByIdApprove(id: string, approval: Approval, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AffiliateOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAffiliatesByIdApprove(id, approval, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAffiliatesByIdApprove']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.  The amount is reserved atomically against the affiliate\'s PENDING commission — accrued minus paid — so a payout can never exceed what is owed. The METHOD decides whether money actually moves: `credits` issues a commerce grant into the affiliate ORG\'s own wallet, tagged so the ledger can tell an affiliate payout apart from an admin or referral grant; every other method — wire, paypal and the rest — is RECORD-ONLY: the payout row and the balances move, the cash is disbursed out of band.  The amount is integer cents and must be positive. PLATFORM SUDO ONLY. Audited.
         * @summary Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.
         * @param {string} id ID is the affiliate to pay, from the path.
         * @param {Disbursal} disbursal 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAffiliatesByIdPayout(id: string, disbursal: Disbursal, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<PayoutOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAffiliatesByIdPayout(id, disbursal, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAffiliatesByIdPayout']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.  The rate is CAPPED so that the direct rate plus the platform-wide second- and third-level rates can never exceed the whole margin — the structural guarantee that everything paid on one source event stays inside the margin actually earned. The cap is resolved from the rates in force at the moment of the call and quoted in the refusal, because those switches move; a hardcoded bound would start lying the moment somebody edits the schedule.  Only the direct level is per-affiliate. The second and third levels are platform switches and are not settable here. The change applies to FUTURE accruals — commission already latched for a period is not recomputed. PLATFORM SUDO ONLY. Audited.
         * @summary Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.
         * @param {string} id ID is the affiliate whose direct rate moves, from the path.
         * @param {RateSet} rateSet 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAffiliatesByIdRate(id: string, rateSet: RateSet, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AffiliateOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAffiliatesByIdRate(id, rateSet, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAffiliatesByIdRate']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.  It CLAWS NOTHING BACK. Commission already accrued stays accrued and stays payable, and existing attribution edges are left standing — suspension ends earning, it does not unwind history. PLATFORM SUDO ONLY. Audited.
         * @summary Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.
         * @param {string} id ID is the affiliate\&#39;s server-minted handle, \&quot;aff_\&quot;-prefixed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAffiliatesByIdSuspend(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AffiliateOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAffiliatesByIdSuspend(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAffiliatesByIdSuspend']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.  This is the cron path, and it is LATCHED at most once per affiliate, source org and period — so re-running it inside the same period accrues nothing further. Safe to retry, and safe to run by hand beside the schedule.  Commission is a rate of Hanzo\'s MARGIN on that spend, never of the customer\'s gross bill, so every level\'s share summed over one source event stays within the margin actually earned and the customer\'s charge is untouched. Nothing accrues past the third upline level, and only an APPROVED affiliate accrues at all.  The same spend read drives the OSS author royalty — one read, both programs — so the answer reports royalties accrued alongside. PLATFORM SUDO ONLY. Bounded per run; a source whose spend cannot be read is skipped and picked up next time, never half-accrued.
         * @summary Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAffiliatesSweep(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AccrualsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAffiliatesSweep(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAffiliatesSweep']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Admits one author to EARNING, optionally on a negotiated royalty share. Until this runs, a connected author accrues nothing however many verified repositories they have.  A share override applies from here forward only — existing ledger rows keep the share that was applied when they were written, because a rate change must never rewrite what was already owed.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Admits one author to EARNING, optionally on a negotiated royalty share.
         * @param {string} id ID is the author to approve, from the path.
         * @param {ApproveRequest} approveRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAuthorsByIdApprove(id: string, approveRequest: ApproveRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AuthorResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAuthorsByIdApprove(id, approveRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAuthorsByIdApprove']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Records a payout of accrued royalty and settles it.  The amount is RESERVED against the author\'s pending royalty atomically before anything is paid, so a payout can never exceed what is owed even under concurrent calls. An external author\'s payout is then BACKED against the platform reserve fund — a second, independent guard — and refused with 402 if the reserve cannot cover it, with the reservation voided. A \"credits\" method issues the actual wallet grant after both guards; a cash method is record-only. A first-party (treasury) author\'s royalty is realized into Hanzo\'s own reserve instead of an external wallet, and every payout row discloses which of the three it was.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Records a payout of accrued royalty and settles it.
         * @param {string} id ID is the author to pay, from the path.
         * @param {PayoutRequest} payoutRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAuthorsByIdPayout(id: string, payoutRequest: PayoutRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<PayoutResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAuthorsByIdPayout(id, payoutRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAuthorsByIdPayout']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Stops one author earning. Their record, verified claims and ledger are untouched — suspension halts future accrual, it does not erase what was already owed, and it does not delete the evidence behind it.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Stops one author earning.
         * @param {string} id ID is the author record\&#39;s handle, \&quot;aut_\&quot;-prefixed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAuthorsByIdSuspend(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AuthorResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAuthorsByIdSuspend(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAuthorsByIdSuspend']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.  It is an OVERRIDE, not the mechanism: a background scheduler runs the same sweep on its own, and every author\'s dashboard read sweeps their own accruals lazily. This is the manual trigger for an operator who needs the numbers now. It is idempotent — the per-period latch means running it twice accrues nothing the second time.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminAuthorsSweep(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AuthorSweepResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminAuthorsSweep(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminAuthorsSweep']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Qualify-checks every pending referral and advances the ones that now qualify.  SuperAdmin only, fail-closed. This is the cron path, and the ONLY path that advances a referral: a referee QUALIFIES once they have made metered spend — the honest signal that they actually used the product rather than merely signing up.  Qualifying moves NO money. It records that an attribution became a real customer; what is owed for that is an affiliate payable in commerce, settled by wire or to a connected wallet. One pass is bounded, so a large backlog drains over several runs instead of wedging one request, and the latch makes the transition at-most-once under a concurrent sweep.  It reads nothing from the caller — the counters it returns are the whole result.
         * @summary Qualify-checks every pending referral and advances the ones that now qualify.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminReferralsSweep(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SweepEnvelope>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminReferralsSweep(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminReferralsSweep']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status. When the chain path is wired it signs and submits the anchor transaction and records it; when it is not, it returns the root that WOULD be committed plus the exact remaining wiring step and records nothing false. A submit that fails still answers 200 with the anchor\'s own status set to \"error\" — the attempt is the product. SuperAdmin only.
         * @summary Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminTreasuryAnchor(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AnchorOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminTreasuryAnchor(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminTreasuryAnchor']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy. 0–10000; the change is audited. SuperAdmin only.
         * @summary Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy.
         * @param {PolicyRequest} policyRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminTreasuryPolicy(policyRequest: PolicyRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<PolicyOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminTreasuryPolicy(policyRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminTreasuryPolicy']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote. A repeat of the same ref is at-most-once and reports created=false. SuperAdmin only.
         * @summary Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote.
         * @param {SeedRequest} seedRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminTreasurySeed(seedRequest: SeedRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SeedOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminTreasurySeed(seedRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminTreasurySeed']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved. It is idempotent per period: a re-run of a period already swept accrues nothing and reports created=false. SuperAdmin only.
         * @summary Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved.
         * @param {SweepRequest} sweepRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAdminTreasurySweep(sweepRequest: SweepRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SweepOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAdminTreasurySweep(sweepRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.postAdminTreasurySweep']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta. It is generic over kind, so the same call manages models, providers and product features through the one registry. `off` is an absolute kill switch: a self-service opt-in can never re-open it. SuperAdmin only; every other caller is refused.
         * @summary Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta.
         * @param {SetEnablementBody} setEnablementBody 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async putAdminEnablement(setEnablementBody: SetEnablementBody, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AdminEnablementItem>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.putAdminEnablement(setEnablementBody, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.putAdminEnablement']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas. It provisions-or-resolves the caller org\'s treasury wallet on the deployed MPC ring and installs it, so every later anchor commits the ledger root SIGNED BY THE QUORUM WALLET instead of a lone KMS key. Idempotent — a repeat resolves the same wallet, which is why the address is a PUT. SuperAdmin only.
         * @summary Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async putAdminTreasuryAnchorSigner(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SignerOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.putAdminTreasuryAnchorSigner(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AdminApi.putAdminTreasuryAnchorSigner']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * AdminApi - factory interface
 * @export
 */
export const AdminApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = AdminApiFp(configuration)
    return {
        /**
         * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).  Every signal degrades INDEPENDENTLY — a table that is absent or errors contributes its zero value and the read still succeeds. Generation latency is a SEPARATE query from generations and cost on purpose: a duration/attribute mismatch there must not zero the two numbers that did read.
         * @summary Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).
         * @param {AdminApiAdminAIMetricsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAIMetrics(requestParameters: AdminApiAdminAIMetricsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<AimetricsOut> {
            return localVarFp.adminAIMetrics(requestParameters.range, options).then((request) => request(axios, basePath));
        },
        /**
         * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.  The window is the caller\'s, not the fleet\'s: a SuperAdmin gets every org, a white-label admin only their own subtree (core.ScopedOrgs, the one scope predicate).  sources[] carries each upstream\'s freshness so a partial read is VISIBLE rather than silently low: a ledger that answered for only some orgs marks commerce-ledger degraded instead of publishing an undercount as healthy.
         * @summary Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.
         * @param {AdminApiAdminAnalyticsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAnalytics(requestParameters: AdminApiAdminAnalyticsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<AnalyticsOut> {
            return localVarFp.adminAnalytics(requestParameters.range, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications. These are the platform\'s OIDC clients — the console reads clientId off each row.
         * @summary Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications.
         * @param {AdminApiAdminApplicationsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminApplications(requestParameters: AdminApiAdminApplicationsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<IamRowsOut> {
            return localVarFp.adminApplications(requestParameters.owner, requestParameters.p, requestParameters.pageSize, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.  When cloud has no local store configured it falls back to forwarding IAM\'s own get-records trail verbatim — a DIFFERENT trail, federated so the endpoint never regresses to an empty list. Those rows carry no integrity of ours, so the field is null there.
         * @summary Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.
         * @param {AdminApiAdminAuditRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAudit(requestParameters: AdminApiAdminAuditRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<RecordsOut> {
            return localVarFp.adminAudit(requestParameters.org, requestParameters.sub, requestParameters.action, requestParameters.resource, requestParameters.resourceId, requestParameters.result, requestParameters.since, requestParameters.until, requestParameters.pageSize, requestParameters.p, options).then((request) => request(axios, basePath));
        },
        /**
         * Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.  The trail is a FAMILY of chains, one per process, so the answer is a set and not a boolean: `intact`, `broken` and `unread` count the three verdicts and sum to the number of chains. A chain that could not be READ is reported `unread` and is never a pass — an unreadable chain and a verified one must not render the same, which is the whole reason this is not one flag.  An unconfigured store is an honest failure here rather than a fabricated pass.
         * @summary Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminAuditVerify(options?: RawAxiosRequestConfig): AxiosPromise<VerifyOut> {
            return localVarFp.adminAuditVerify(options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.  The scope is enforced TWICE: the upstream is asked for the caller\'s org, AND every row it returns is re-checked against the resolved scope. An upstream that ignored the filter therefore degrades to empty, never to a cross-tenant leak.  The Base engine is being embedded into cloud; until it lands this proxies BASE_ADMIN_URL and, when that is unset, answers 200 with an empty list and msg saying so — the honest not-yet state, never fabricated instances.
         * @summary Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminBases(options?: RawAxiosRequestConfig): AxiosPromise<BasesOut> {
            return localVarFp.adminBases(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.  These are the SAME rows the customer edits in their own console — a platform override and a customer budget are one model, not two.
         * @summary Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.
         * @param {AdminApiAdminCapsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCaps(requestParameters: AdminApiAdminCapsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminCaps(requestParameters.org, requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event. The console folds these into its org → app → project tree.  A machine counts as ACTIVE when its LATEST lifecycle event is not a terminal one (stop/destroy/terminate/delete/off/shutdown/expire and their past tenses) — the same fold the console applies, done in the warehouse so the count is over every machine and not just the page.  Honest-empty when the warehouse is not connected or hanzo.compute_usage is not provisioned yet: an empty list, never a fabricated fleet.
         * @summary Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event.
         * @param {AdminApiAdminComputeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCompute(requestParameters: AdminApiAdminComputeRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<ComputeOut> {
            return localVarFp.adminCompute(requestParameters.kind, requestParameters.org, requestParameters.range, options).then((request) => request(axios, basePath));
        },
        /**
         * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.  It is the ONE infra change that does not go through the run discipline, because there is no destructive verdict to check: cordoning is reversible and evicting respects the cluster\'s own PodDisruptionBudgets. It reads the cached board for the same reason. The outcome is audited either way, and the result reports how many pods were evicted.
         * @summary Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.
         * @param {AdminApiAdminCordonNodeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCordonNode(requestParameters: AdminApiAdminCordonNodeRequest, options?: RawAxiosRequestConfig): AxiosPromise<MutationOut> {
            return localVarFp.adminCordonNode(requestParameters.id, requestParameters.cordonIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows. The body is commerce\'s spend-alert contract, forwarded byte-for-byte.
         * @summary Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows.
         * @param {AdminApiAdminCreateCapRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCreateCap(requestParameters: AdminApiAdminCreateCapRequest, options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminCreateCap(requestParameters.capIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers GET /v1/admin/customers/:org.
         * @summary Answers GET /v1/admin/customers/:org.
         * @param {AdminApiAdminCustomerRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCustomer(requestParameters: AdminApiAdminCustomerRequest, options?: RawAxiosRequestConfig): AxiosPromise<CustomerDetailOut> {
            return localVarFp.adminCustomer(requestParameters.org, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.  Each row costs one IAM read plus the org\'s money reads, fanned out under a fixed concurrency ceiling so a large fleet cannot stampede the upstreams. Every read is best-effort per row: an upstream miss degrades THAT field to its honest zero rather than failing the fleet.
         * @summary Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminCustomers(options?: RawAxiosRequestConfig): AxiosPromise<CustomersOut> {
            return localVarFp.adminCustomers(options).then((request) => request(axios, basePath));
        },
        /**
         * Removes one cap by id, lifting the ceiling entirely.
         * @summary Removes one cap by id, lifting the ceiling entirely.
         * @param {AdminApiAdminDeleteCapRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteCap(requestParameters: AdminApiAdminDeleteCapRequest, options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminDeleteCap(requestParameters.id, requestParameters.org, options).then((request) => request(axios, basePath));
        },
        /**
         * Destroys a droplet the board has just proven is NOT a DOKS node. There is no snapshot-first undo for a droplet the way there is for a volume: the local disk goes with it.
         * @summary Destroys a droplet the board has just proven is NOT a DOKS node.
         * @param {AdminApiAdminDeleteDropletRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteDroplet(requestParameters: AdminApiAdminDeleteDropletRequest, options?: RawAxiosRequestConfig): AxiosPromise<MutationOut> {
            return localVarFp.adminDeleteDroplet(requestParameters.id, requestParameters.size, requestParameters.disk, options).then((request) => request(axios, basePath));
        },
        /**
         * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
         * @summary Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
         * @param {AdminApiAdminDeleteLoadBalancerRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteLoadBalancer(requestParameters: AdminApiAdminDeleteLoadBalancerRequest, options?: RawAxiosRequestConfig): AxiosPromise<MutationOut> {
            return localVarFp.adminDeleteLoadBalancer(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Destroys a volume the board has just proven no PersistentVolume in any cluster references. Irreversible, so it snapshots first unless explicitly waived — the snapshot IS the undo.
         * @summary Destroys a volume the board has just proven no PersistentVolume in any cluster references.
         * @param {AdminApiAdminDeleteVolumeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDeleteVolume(requestParameters: AdminApiAdminDeleteVolumeRequest, options?: RawAxiosRequestConfig): AxiosPromise<MutationOut> {
            return localVarFp.adminDeleteVolume(requestParameters.id, requestParameters.snapshot, requestParameters.name, requestParameters.sizeGiB, options).then((request) => request(axios, basePath));
        },
        /**
         * Stops the plugin. Its routes STAY REGISTERED and answer 503 — not 404.  That is zip\'s choice and this keeps it. Removing the routes would mutate the route table, and re-adding them on enable would grow it without bound across repeated cycles, which is the invariant that makes reloads flat in memory. It is also the better answer: 404 says \"no such API\" and a client may cache it and stop retrying, while 503 says \"this API exists and is down right now\", which is true and retryable. Which of the two 503s this is — deliberate stop or crash — is what the status\'s disabled flag reports.
         * @summary Stops the plugin.
         * @param {AdminApiAdminDisablePluginRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminDisablePlugin(requestParameters: AdminApiAdminDisablePluginRequest, options?: RawAxiosRequestConfig): AxiosPromise<ActionOut> {
            return localVarFp.adminDisablePlugin(requestParameters.name, requestParameters.nameIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag. Named for what an operator means by it.
         * @summary Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag.
         * @param {AdminApiAdminEnablePluginRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminEnablePlugin(requestParameters: AdminApiAdminEnablePluginRequest, options?: RawAxiosRequestConfig): AxiosPromise<ActionOut> {
            return localVarFp.adminEnablePlugin(requestParameters.name, requestParameters.nameIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers GET /v1/admin/finance. It reads the multi-vendor COGS from commerce /v1/costs, the DO promo-credit/burn-down treasury view, and the fleet commerce revenue, then hands them to ComputeFinance. SuperAdmin only.
         * @summary Answers GET /v1/admin/finance.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminFinance(options?: RawAxiosRequestConfig): AxiosPromise<FinanceOut> {
            return localVarFp.adminFinance(options).then((request) => request(axios, basePath));
        },
        /**
         * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.  It is IDEMPOTENT: the deposit uses the fixed ref \"backfill:<org>\", so re-running it credits the wallet at most once. Safe to retry.  The pre-migration balance is read from the CO-RESIDENT commerce ledger, not over HTTP: the admin HTTP client dials an unroutable in-process address and would read $0, and a phantom zero would silently carry nothing while reporting success. When commerce is not co-resident this fails rather than migrating nothing.
         * @summary Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.
         * @param {AdminApiAdminFinanceBackfillRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminFinanceBackfill(requestParameters: AdminApiAdminFinanceBackfillRequest, options?: RawAxiosRequestConfig): AxiosPromise<BackfillOut> {
            return localVarFp.adminFinanceBackfill(requestParameters.backfillIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
         * @summary Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminFlags(options?: RawAxiosRequestConfig): AxiosPromise<FlagsOut> {
            return localVarFp.adminFlags(options).then((request) => request(axios, basePath));
        },
        /**
         * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.  The credit lands on the account account.Payer resolves, NOT necessarily the org: name a member of a pooled org and the pool is credited. The receipt echoes the subject so the caller can see which.
         * @summary Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.
         * @param {AdminApiAdminGrantCreditRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminGrantCredit(requestParameters: AdminApiAdminGrantCreditRequest, options?: RawAxiosRequestConfig): AxiosPromise<GrantOut> {
            return localVarFp.adminGrantCredit(requestParameters.org, requestParameters.grantIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.  It is a PROJECTION of the tamper-evident audit trail, not a second store: every grant is written there as action \"admin.customer.credit\", so this view cannot drift from what actually happened, and FAILED grants appear too.  A deployment with no local audit store has no history to project, and says so with an empty list and a msg rather than an error.
         * @summary Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.
         * @param {AdminApiAdminGrantsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminGrants(requestParameters: AdminApiAdminGrantsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<GrantsOut> {
            return localVarFp.adminGrants(requestParameters.org, requestParameters.result, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.  It is cached for up to a minute because one read is a fan-out over the DO API plus a full pod/PV listing per cluster. Staleness is never load-bearing: every MUTATION re-scans from scratch and ignores this cache.  Only an unusable DO account is a hard failure. A partial read still produces a board, with the failing source named in sources[] — except for clusters and volumes, which the safety verdict depends on; without those the analysis degrades rather than classifying anything it cannot prove.
         * @summary Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.
         * @param {AdminApiAdminInfraRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminInfra(requestParameters: AdminApiAdminInfraRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<ReadOut> {
            return localVarFp.adminInfra(requestParameters.refresh, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers GET /v1/admin/invoices.   GET /v1/admin/invoices?org=&status=&limit=
         * @summary Answers GET /v1/admin/invoices.
         * @param {AdminApiAdminInvoicesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminInvoices(requestParameters: AdminApiAdminInvoicesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<InvoicesOut> {
            return localVarFp.adminInvoices(requestParameters.status, requestParameters.org, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Issues a credit grant to any org from the operator Grants view, with the target named in the body. It funnels through the SAME core.ApplyGrant that POST /v1/admin/customers/:org/credit uses, so there is exactly ONE credit-write path and one audit trail behind both.
         * @summary Issues a credit grant to any org from the operator Grants view, with the target named in the body.
         * @param {AdminApiAdminIssueGrantRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminIssueGrant(requestParameters: AdminApiAdminIssueGrantRequest, options?: RawAxiosRequestConfig): AxiosPromise<GrantOut> {
            return localVarFp.adminIssueGrant(requestParameters.grantIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is. The fields come from the sanitized identity headers the gate just read, so they are authoritative and never client-forgeable; nothing is looked up.
         * @summary Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminMe(options?: RawAxiosRequestConfig): AxiosPromise<MeOut> {
            return localVarFp.adminMe(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out). SuperAdmin only.   GET /v1/admin/metrics?window=30d&limit=20
         * @summary Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out).
         * @param {AdminApiAdminMetricsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminMetrics(requestParameters: AdminApiAdminMetricsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<MetricsOut> {
            return localVarFp.adminMetrics(requestParameters.window, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * moneyBoardHandler answers GET /v1/admin/money.
         * @summary moneyBoardHandler answers GET /v1/admin/money.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminMoney(options?: RawAxiosRequestConfig): AxiosPromise<MoneyOut> {
            return localVarFp.adminMoney(options).then((request) => request(axios, basePath));
        },
        /**
         * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.  Every signal degrades INDEPENDENTLY. A table that is absent or errors contributes its zero value and the read still succeeds, so the board renders exactly what the warehouse holds rather than failing whole because one of four sources is missing. Same when the warehouse is not connected at all: the zero board, never a fabricated fleet.
         * @summary Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.
         * @param {AdminApiAdminO11yRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminO11y(requestParameters: AdminApiAdminO11yRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<O11yOut> {
            return localVarFp.adminO11y(requestParameters.range, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.  The rows are the caller\'s tenant window, not the fleet: a SuperAdmin gets every org, a white-label admin only their own subtree. A per-org read that fails degrades THAT row to an honest zero — this panel carries no sources[] channel to report freshness on, so the alternative would be a fleet total that silently reads healthy.
         * @summary Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminOrgs(options?: RawAxiosRequestConfig): AxiosPromise<OrgsOut> {
            return localVarFp.adminOrgs(options).then((request) => request(axios, basePath));
        },
        /**
         * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.  It ALWAYS answers 200 — a tile board that fails as a whole because one upstream is down is useless. Instead every upstream reports itself in sources[]: ok, degraded, or not-configured. A commerce read that failed for ANY org marks that source degraded, because the spend/credits totals are then an undercount and must not read healthy.  The AI tiles — 30-day spend and tokens — come from the AI ledger (ledger.go), the plane that owns \"what was served\". They used to come from the money plane with the token counter hardcoded to zero, so the board read $0.00 and 0 tokens over a month in which the fleet served fifteen thousand requests. Credits still come from commerce, which owns the wallet.
         * @summary Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminOverview(options?: RawAxiosRequestConfig): AxiosPromise<OverviewOut> {
            return localVarFp.adminOverview(options).then((request) => request(axios, basePath));
        },
        /**
         * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.  Reading this from deployment config would answer what was INTENDED. Only the process knows what is TRUE, and during a rolling upgrade the two disagree on purpose.
         * @summary Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.
         * @param {AdminApiAdminPluginsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminPlugins(requestParameters: AdminApiAdminPluginsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<ListOut> {
            return localVarFp.adminPlugins(requestParameters.scope, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict. Optionally narrowed by kind, tier or env, each an exact match.  The rows are the SAME observation /v1/platform/fleet renders — read through the in-process platform seam, not a second k8s client — so the two boards can never disagree about what the fleet is. A PaaS plane that is not co-resident yields an honestly empty registry, never a fabricated row.
         * @summary Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict.
         * @param {AdminApiAdminProductsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminProducts(requestParameters: AdminApiAdminProductsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<ProductsOut> {
            return localVarFp.adminProducts(requestParameters.kind, requestParameters.tier, requestParameters.env, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads the current platform plan promo — the singleton discount offer, e.g. the 50%-off launch promo. Commerce stores it in the reserved platform namespace, so the org sent with the read is the admin org and the service token is what passes commerce\'s own platform-admin gate.
         * @summary Reads the current platform plan promo — the singleton discount offer, e.g.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminPromo(options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminPromo(options).then((request) => request(axios, basePath));
        },
        /**
         * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger. SuperAdmin-guarded (see Routes).
         * @summary Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminProvidersCredit(options?: RawAxiosRequestConfig): AxiosPromise<ProvidersCreditOut> {
            return localVarFp.adminProvidersCredit(options).then((request) => request(axios, basePath));
        },
        /**
         * Restores access for every member of the org, undoing a suspend. It reports the same per-user breakdown.
         * @summary Restores access for every member of the org, undoing a suspend.
         * @param {AdminApiAdminReactivateCustomerRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminReactivateCustomer(requestParameters: AdminApiAdminReactivateCustomerRequest, options?: RawAxiosRequestConfig): AxiosPromise<AccessOut> {
            return localVarFp.adminReactivateCustomer(requestParameters.org, options).then((request) => request(axios, basePath));
        },
        /**
         * Swaps a plugin for another build without dropping a request. The replacement is started and proven to be LISTENING before any traffic moves to it, so a bad build leaves the old one serving and returns an error rather than a hole; the old process then drains before it is killed.  With a version or url+sum it pins; naming a digest this host has run before is the rollback, and costs no network because the digest IS the cache key. With neither it restarts what is already loaded.  Fleet scope applies it to one host at a time and STOPS at the first failure, so a build that cannot come up reaches exactly one host.
         * @summary Swaps a plugin for another build without dropping a request.
         * @param {AdminApiAdminReloadPluginRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminReloadPlugin(requestParameters: AdminApiAdminReloadPluginRequest, options?: RawAxiosRequestConfig): AxiosPromise<ActionOut> {
            return localVarFp.adminReloadPlugin(requestParameters.name, requestParameters.reloadIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Changes a droplet\'s plan. Same refusal as delete and for the same reason: a DOKS node\'s size is the node pool\'s to declare.  disk=true is a PERMANENT resize — the disk grows and DO can never resize the droplet DOWN again. disk=false (the default) changes CPU/RAM only and is reversible. DO requires the droplet to be powered off and applies the change asynchronously, so the response carries the action to poll, not a completed change.
         * @summary Changes a droplet\'s plan.
         * @param {AdminApiAdminResizeDropletRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminResizeDroplet(requestParameters: AdminApiAdminResizeDropletRequest, options?: RawAxiosRequestConfig): AxiosPromise<MutationOut> {
            return localVarFp.adminResizeDroplet(requestParameters.id, requestParameters.dropletIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Grows a volume. GROW ONLY — see Volume.ExpandTo for why the other direction is a data migration this board deliberately refuses to run.  The MECHANISM follows the volume\'s owner, because there is exactly one way to grow each kind completely. A volume a PVC claims is grown by patching the claim: the CSI driver then resizes the DigitalOcean device AND grows the filesystem on it, leaving claim, PV, device and filesystem all agreeing. Calling DigitalOcean directly for that volume would grow the device while the PV kept declaring the old capacity and the filesystem never grew at all. One operation, one correct mechanism per owner — not two ways to do it.
         * @summary Grows a volume.
         * @param {AdminApiAdminResizeVolumeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminResizeVolume(requestParameters: AdminApiAdminResizeVolumeRequest, options?: RawAxiosRequestConfig): AxiosPromise<MutationOut> {
            return localVarFp.adminResizeVolume(requestParameters.id, requestParameters.volumeIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.  ORTHOGONAL to /v1/admin/finance, which is the COGS/margin view of what WE pay vendors. This is the customer side: what each customer holds, spends and subscribes to.  arpu divides realized spend by PAYING customers, not by all of them — a fleet of free signups must not deflate the number. A customer counts as paying when it has spend or MRR.  An org whose money did not read degrades to honest zeros and marks the commerce source degraded in sources[], so a partial fleet read is visible instead of quietly low.
         * @summary Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminRevenue(options?: RawAxiosRequestConfig): AxiosPromise<RevenueOut> {
            return localVarFp.adminRevenue(options).then((request) => request(axios, basePath));
        },
        /**
         * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
         * @summary Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
         * @param {AdminApiAdminRolesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminRoles(requestParameters: AdminApiAdminRolesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<IamRowsOut> {
            return localVarFp.adminRoles(requestParameters.owner, requestParameters.p, requestParameters.pageSize, options).then((request) => request(axios, basePath));
        },
        /**
         * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.  The response states what the board could NOT prove: DOKS picks which nodes a shrink removes, so no particular pod is shown to survive one. See NodePool.ScaleTo.
         * @summary Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.
         * @param {AdminApiAdminScaleNodePoolRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminScaleNodePool(requestParameters: AdminApiAdminScaleNodePoolRequest, options?: RawAxiosRequestConfig): AxiosPromise<MutationOut> {
            return localVarFp.adminScaleNodePool(requestParameters.id, requestParameters.pool, requestParameters.scaleIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine. This is the \"remove the waitlist one service at a time\" view.
         * @summary Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminServices(options?: RawAxiosRequestConfig): AxiosPromise<ServicesOut> {
            return localVarFp.adminServices(options).then((request) => request(axios, basePath));
        },
        /**
         * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands. The flip is hot: this pod applies it immediately and peers converge within one evaluation TTL (15s by default), with no redeploy.  The body reaches the flag engine BYTE-FOR-BYTE — it is the engine\'s definition format, not this layer\'s, so a field the engine understands and admin does not must still arrive intact. setFlagIn names the two fields that matter for documentation; it is not a filter.  The write is recorded in the store\'s activity log against the caller\'s email.
         * @summary Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands.
         * @param {AdminApiAdminSetFlagRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSetFlag(requestParameters: AdminApiAdminSetFlagRequest, options?: RawAxiosRequestConfig): AxiosPromise<FlagsOut> {
            return localVarFp.adminSetFlag(requestParameters.key, requestParameters.setFlagIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Upserts the platform plan promo — the ONE place the offer is configured.  The body is commerce\'s own promo contract and is forwarded BYTE-FOR-BYTE, so no field commerce accepts is dropped in transit. promoIn names its documented fields.
         * @summary Upserts the platform plan promo — the ONE place the offer is configured.
         * @param {AdminApiAdminSetPromoRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSetPromo(requestParameters: AdminApiAdminSetPromoRequest, options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminSetPromo(requestParameters.promoIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Flips ONE service\'s waitlist switch — the launch lever. Hot: it takes effect on this pod immediately and on peers within one evaluation TTL, with no redeploy. An unknown service is a 404, not a silent create; onboarding goes through upsertService.
         * @summary Flips ONE service\'s waitlist switch — the launch lever.
         * @param {AdminApiAdminSetServiceModeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSetServiceMode(requestParameters: AdminApiAdminSetServiceModeRequest, options?: RawAxiosRequestConfig): AxiosPromise<ServiceOut> {
            return localVarFp.adminSetServiceMode(requestParameters.service, requestParameters.serviceModeIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.  It re-scans the board first (never the cache) so the volume it snapshots is one that exists right now, and audits the outcome either way.
         * @summary Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.
         * @param {AdminApiAdminSnapshotVolumeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSnapshotVolume(requestParameters: AdminApiAdminSnapshotVolumeRequest, options?: RawAxiosRequestConfig): AxiosPromise<VolumeSnapshotOut> {
            return localVarFp.adminSnapshotVolume(requestParameters.id, requestParameters.volumeIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers GET /v1/admin/subscriptions.   GET /v1/admin/subscriptions?org=&status=&limit=
         * @summary Answers GET /v1/admin/subscriptions.
         * @param {AdminApiAdminSubscriptionsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSubscriptions(requestParameters: AdminApiAdminSubscriptionsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<SubscriptionsOut> {
            return localVarFp.adminSubscriptions(requestParameters.status, requestParameters.org, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * subsystems answers GET /v1/admin/subsystems. ?range=24h|7d|30d bounds the telemetry window (default 30d) — the same enum, and the same helpers, as the o11y board.
         * @summary subsystems answers GET /v1/admin/subsystems.
         * @param {AdminApiAdminSubsystemsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSubsystems(requestParameters: AdminApiAdminSubsystemsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<SubsystemsOut> {
            return localVarFp.adminSubsystems(requestParameters.range, options).then((request) => request(axios, basePath));
        },
        /**
         * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token. Fully reversible with ReactivateCustomer.  The result names every user updated and every user that was NOT — a partial failure leaves the org in a mixed state and says so instead of reporting a clean success.
         * @summary Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token.
         * @param {AdminApiAdminSuspendCustomerRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSuspendCustomer(requestParameters: AdminApiAdminSuspendCustomerRequest, options?: RawAxiosRequestConfig): AxiosPromise<AccessOut> {
            return localVarFp.adminSuspendCustomer(requestParameters.org, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the operator\'s \"Sync now\" button. There is nothing to kick: admin aggregates LIVE on every read, so the button is just a re-read. It acknowledges honestly with started:true rather than pretending a batch job was queued.
         * @summary Answers the operator\'s \"Sync now\" button.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminSync(options?: RawAxiosRequestConfig): AxiosPromise<SyncOut> {
            return localVarFp.adminSync(options).then((request) => request(axios, basePath));
        },
        /**
         * Edits one cap by id — raise or lower the ceiling, flip enforcement. The body is commerce\'s spend-alert patch contract, forwarded byte-for-byte.
         * @summary Edits one cap by id — raise or lower the ceiling, flip enforcement.
         * @param {AdminApiAdminUpdateCapRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUpdateCap(requestParameters: AdminApiAdminUpdateCapRequest, options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminUpdateCap(requestParameters.id, requestParameters.capIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy. Re-registering an existing service PRESERVES its live switch — editing the hosts of a service that is already open must not silently close it again.
         * @summary Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy.
         * @param {AdminApiAdminUpsertServiceRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUpsertService(requestParameters: AdminApiAdminUpsertServiceRequest, options?: RawAxiosRequestConfig): AxiosPromise<ServiceOut> {
            return localVarFp.adminUpsertService(requestParameters.serviceInput, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.  It reads the AI ledger (ledger.go), which is the plane that owns this question. It used to ask the commerce billing API instead, once per org, and answer with a hardcoded empty series, zero tokens and zero requests, on the reasoning that a trend and a split were \"not derivable from the commerce billing API\". They are not — but the question was never commerce\'s. hanzo.cloud_usage carries a row per served request, so all three fall out of the same window the totals do.
         * @summary Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.
         * @param {AdminApiAdminUsageRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUsage(requestParameters: AdminApiAdminUsageRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<UsageOut> {
            return localVarFp.adminUsage(requestParameters.org, options).then((request) => request(axios, basePath));
        },
        /**
         * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).  The class is resolved at the PROVIDER level from the credit ledger, not per call — the per-call split, and the `byo` class, arrive when the metering write stamps a funding column on cloud_usage and this can GROUP BY it directly. Until then a provider with remaining grant reports all of its usage as credit, which is right in aggregate and approximate at the boundary where a grant runs out mid-window.  An unparseable window falls back to the last 30 days rather than refusing: this is a dashboard read, and a typo in a date must not blank the board.
         * @summary Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).
         * @param {AdminApiAdminUsageFundingRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUsageFunding(requestParameters: AdminApiAdminUsageFundingRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<UsageFundingOut> {
            return localVarFp.adminUsageFunding(requestParameters.from, requestParameters.to, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the user directory across the caller\'s tenant window, one page at a time. total is IAM\'s REAL total, so the console can page through it.  A SuperAdmin may aim the read at one tenant with org; a white-label admin cannot — for them the owner is hard-pinned to their own org and org is ignored, which is what keeps the directory from becoming a cross-tenant read.
         * @summary Lists the user directory across the caller\'s tenant window, one page at a time.
         * @param {AdminApiAdminUsersRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminUsers(requestParameters: AdminApiAdminUsersRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<UsersOut> {
            return localVarFp.adminUsers(requestParameters.org, requestParameters.q, requestParameters.p, requestParameters.pageSize, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.  A volume\'s usedGiB and pct are null, always: DO exposes capacity and attachment but no fill, so the console renders \"—\" rather than a number nobody measured. The datastore card is the one real fill here, and it is the number to scale on.  The two sources degrade independently — a DO outage still returns the datastore fill, and a disconnected datastore still returns the DO fleet.
         * @summary Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminVolumes(options?: RawAxiosRequestConfig): AxiosPromise<VolumesOut> {
            return localVarFp.adminVolumes(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.  The engine\'s payload is forwarded VERBATIM as data; the console normalizes it. When the engine is not configured on this deployment the read still succeeds, with an empty object and a msg saying so, so the panel shows an honest not-wired state instead of an error the operator would chase.
         * @summary Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.
         * @param {AdminApiAdminWaitlistRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminWaitlist(requestParameters: AdminApiAdminWaitlistRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminWaitlist(requestParameters.waitlist, requestParameters.page, requestParameters.pageSize, options).then((request) => request(axios, basePath));
        },
        /**
         * Grants a user waitlist points, moving them up toward the access cutoff. This is the access lever: the cutoff itself does not move, the person does.  It funnels through the engine\'s verified grant seam (POST /v1/waitlist/award with source=\"grant\" — the ONE path that honours an explicit points amount) and writes a tamper-evident audit row either way, so a FAILED grant is recorded too. The reason field goes only to that row.
         * @summary Grants a user waitlist points, moving them up toward the access cutoff.
         * @param {AdminApiAdminWaitlistBoostRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        adminWaitlistBoost(requestParameters: AdminApiAdminWaitlistBoostRequest, options?: RawAxiosRequestConfig): AxiosPromise<RawOut> {
            return localVarFp.adminWaitlistBoost(requestParameters.waitlistBoostRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.  PLATFORM SUDO ONLY, and a non-admin is refused outright. This is the cross-tenant view and it names orgs — exactly what the partner-facing leaderboard refuses to do. There is deliberately no org-scoped variant of this read; a partner sees its own standing through its own dashboard. Bounded per request.
         * @summary Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.
         * @param {AdminApiGetAdminAffiliatesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminAffiliates(requestParameters: AdminApiGetAdminAffiliatesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<DirectoryOut> {
            return localVarFp.getAdminAffiliates(requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.  It is a Hanzo platform operation: a caller who is not a SuperAdmin gets 403. It exposes the owning org of each author, which no tenant-facing read ever does.
         * @summary Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.
         * @param {AdminApiGetAdminAuthorsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminAuthors(requestParameters: AdminApiGetAdminAuthorsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<AdminBook> {
            return localVarFp.getAdminAuthors(requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.  The data object carries: id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger (every row with its spend, the share applied then, the platform\'s matching half, whether it satisfies the formula and the attribution edges that explain it), reconciliation (does the ledger foot to the balance) and window (what slice was actually returned) — plus period when one was requested.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.
         * @param {AdminApiGetAdminAuthorsByIdBasisRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminAuthorsByIdBasis(requestParameters: AdminApiGetAdminAuthorsByIdBasisRequest, options?: RawAxiosRequestConfig): AxiosPromise<BasisResult> {
            return localVarFp.getAdminAuthorsByIdBasis(requestParameters.id, requestParameters.period, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console. Nothing is hidden: this is the admin\'s view of what exists and what is currently off, in beta or generally available. SuperAdmin only; every other caller is refused.
         * @summary Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminCatalog(options?: RawAxiosRequestConfig): AxiosPromise<AdminCatalogOut> {
            return localVarFp.getAdminCatalog(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta. An item nobody has touched is absent, because an untouched item is generally available; the console composes the candidate list from the live catalog. SuperAdmin only; every other caller is refused.
         * @summary Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminEnablement(options?: RawAxiosRequestConfig): AxiosPromise<AdminEnablementBoard> {
            return localVarFp.getAdminEnablement(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.  Read the liability figure carefully — it is commission accrued and NOT yet paid, so it is money owed, not money spent, and the per-level split says how much of it comes from direct referrals versus the second and third levels.  PLATFORM SUDO ONLY, cross-tenant, and it names orgs. It reads the SAME single attribution spine the accrual itself walks, so the board and the ledger cannot disagree. Amounts are integer cents.
         * @summary Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminReferrals(options?: RawAxiosRequestConfig): AxiosPromise<ReferralsOut> {
            return localVarFp.getAdminReferrals(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns every referral edge in the directory with a fleet summary.  SuperAdmin only, fail-closed. This is the ATTRIBUTION directory — who referred whom and whether that referee became a customer. It carries no amounts because this package issues none. The cross-tenant referral ANALYTICS board (top referrers, conversion) is a different surface, GET /v1/admin/referrals, owned by the affiliates subsystem over the shared attribution spine.
         * @summary Returns every referral edge in the directory with a fleet summary.
         * @param {AdminApiGetAdminReferralsBonusesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminReferralsBonuses(requestParameters: AdminApiGetAdminReferralsBonusesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<AdminBonusesEnvelope> {
            return localVarFp.getAdminReferralsBonuses(requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root. ?limit= bounds the journal page.
         * @summary Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root.
         * @param {AdminApiGetAdminTreasuryRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAdminTreasury(requestParameters: AdminApiGetAdminTreasuryRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<AdminReportOut> {
            return localVarFp.getAdminTreasury(requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Sets one model\'s availability overlay — and the price overrides applied on top of the catalog — then answers the new effective overlay, so a console needs no second read. The model id is the whole remaining path, so a slashed id like `acme/some-model-1` addresses intact.  SuperAdmin only; every other caller is 403, decided before the body is read. The overlay is PLATFORM-WIDE — this is the catalog every org prices against, not a per-org setting — and `betaOrgs` is what narrows a beta to named orgs.  Only the fields the patch names change; an entry with no overlay yet starts from the catalog default, which is enabled. `state` is the coherent tri-state setter (`off`|`beta`|`ga`) and the low-level `enabled`/`beta` flags are applied AFTER it, so they win where both are sent; anything else in `state` is 400. A field sent as an explicit `null` arrives indistinguishable from an absent one, so null does not clear anything.  The rule worth reading twice: a disabled entry that still carries beta orgs IS a beta — `{\"enabled\":false,\"betaOrgs\":[\"acme\"]}` leaves acme seeing the model. Only an explicit `off` (or `beta:false`) with an empty list is the absolute kill switch that a user\'s own beta opt-in can never re-open.  `overrides` is an RFC 7386 merge patch, stored and echoed back verbatim; it must be a JSON object or null — an array or a scalar is refused — and is bounded in size and nesting depth. An uninitialised overlay store answers 503.
         * @summary Turn one model off, into beta for named orgs, or generally available
         * @param {AdminApiPatchAdminCatalogModelsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchAdminCatalogModelsByWildcard1(requestParameters: AdminApiPatchAdminCatalogModelsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.patchAdminCatalogModelsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Sets one provider\'s availability overlay.  The overlay decides whether a provider is off, in beta for named orgs, or generally available, and carries the price overrides applied on top of the catalog. Only the fields the patch names change; every other field keeps the value it had, and an absent overlay starts from the catalog default (enabled). Answers the new effective overlay, so a console needs no second read.  SuperAdmin only.
         * @summary Sets one provider\'s availability overlay.
         * @param {AdminApiPatchAdminCatalogProvidersByNameRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchAdminCatalogProvidersByName(requestParameters: AdminApiPatchAdminCatalogProvidersByNameRequest, options?: RawAxiosRequestConfig): AxiosPromise<Overlay> {
            return localVarFp.patchAdminCatalogProvidersByName(requestParameters.name, requestParameters.providerPatchIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.  The code is taken from the body if one is given, else the vanity code the applicant requested, else a slug derived for them. Codes are ONE global namespace, so a taken code is a 409 and nothing is approved. The minted code is also mirrored as a link row so click tracking is uniform across every code the affiliate holds; that mirror is best-effort and its failure never fails the approval.  Approval is what makes an affiliate eligible: before it, attribution against its code does not resolve and no sweep accrues to it. PLATFORM SUDO ONLY. Audited.
         * @summary Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.
         * @param {AdminApiPostAdminAffiliatesByIdApproveRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdApprove(requestParameters: AdminApiPostAdminAffiliatesByIdApproveRequest, options?: RawAxiosRequestConfig): AxiosPromise<AffiliateOut> {
            return localVarFp.postAdminAffiliatesByIdApprove(requestParameters.id, requestParameters.approval, options).then((request) => request(axios, basePath));
        },
        /**
         * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.  The amount is reserved atomically against the affiliate\'s PENDING commission — accrued minus paid — so a payout can never exceed what is owed. The METHOD decides whether money actually moves: `credits` issues a commerce grant into the affiliate ORG\'s own wallet, tagged so the ledger can tell an affiliate payout apart from an admin or referral grant; every other method — wire, paypal and the rest — is RECORD-ONLY: the payout row and the balances move, the cash is disbursed out of band.  The amount is integer cents and must be positive. PLATFORM SUDO ONLY. Audited.
         * @summary Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.
         * @param {AdminApiPostAdminAffiliatesByIdPayoutRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdPayout(requestParameters: AdminApiPostAdminAffiliatesByIdPayoutRequest, options?: RawAxiosRequestConfig): AxiosPromise<PayoutOut> {
            return localVarFp.postAdminAffiliatesByIdPayout(requestParameters.id, requestParameters.disbursal, options).then((request) => request(axios, basePath));
        },
        /**
         * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.  The rate is CAPPED so that the direct rate plus the platform-wide second- and third-level rates can never exceed the whole margin — the structural guarantee that everything paid on one source event stays inside the margin actually earned. The cap is resolved from the rates in force at the moment of the call and quoted in the refusal, because those switches move; a hardcoded bound would start lying the moment somebody edits the schedule.  Only the direct level is per-affiliate. The second and third levels are platform switches and are not settable here. The change applies to FUTURE accruals — commission already latched for a period is not recomputed. PLATFORM SUDO ONLY. Audited.
         * @summary Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.
         * @param {AdminApiPostAdminAffiliatesByIdRateRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdRate(requestParameters: AdminApiPostAdminAffiliatesByIdRateRequest, options?: RawAxiosRequestConfig): AxiosPromise<AffiliateOut> {
            return localVarFp.postAdminAffiliatesByIdRate(requestParameters.id, requestParameters.rateSet, options).then((request) => request(axios, basePath));
        },
        /**
         * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.  It CLAWS NOTHING BACK. Commission already accrued stays accrued and stays payable, and existing attribution edges are left standing — suspension ends earning, it does not unwind history. PLATFORM SUDO ONLY. Audited.
         * @summary Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.
         * @param {AdminApiPostAdminAffiliatesByIdSuspendRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesByIdSuspend(requestParameters: AdminApiPostAdminAffiliatesByIdSuspendRequest, options?: RawAxiosRequestConfig): AxiosPromise<AffiliateOut> {
            return localVarFp.postAdminAffiliatesByIdSuspend(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.  This is the cron path, and it is LATCHED at most once per affiliate, source org and period — so re-running it inside the same period accrues nothing further. Safe to retry, and safe to run by hand beside the schedule.  Commission is a rate of Hanzo\'s MARGIN on that spend, never of the customer\'s gross bill, so every level\'s share summed over one source event stays within the margin actually earned and the customer\'s charge is untouched. Nothing accrues past the third upline level, and only an APPROVED affiliate accrues at all.  The same spend read drives the OSS author royalty — one read, both programs — so the answer reports royalties accrued alongside. PLATFORM SUDO ONLY. Bounded per run; a source whose spend cannot be read is skipped and picked up next time, never half-accrued.
         * @summary Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAffiliatesSweep(options?: RawAxiosRequestConfig): AxiosPromise<AccrualsOut> {
            return localVarFp.postAdminAffiliatesSweep(options).then((request) => request(axios, basePath));
        },
        /**
         * Admits one author to EARNING, optionally on a negotiated royalty share. Until this runs, a connected author accrues nothing however many verified repositories they have.  A share override applies from here forward only — existing ledger rows keep the share that was applied when they were written, because a rate change must never rewrite what was already owed.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Admits one author to EARNING, optionally on a negotiated royalty share.
         * @param {AdminApiPostAdminAuthorsByIdApproveRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsByIdApprove(requestParameters: AdminApiPostAdminAuthorsByIdApproveRequest, options?: RawAxiosRequestConfig): AxiosPromise<AuthorResult> {
            return localVarFp.postAdminAuthorsByIdApprove(requestParameters.id, requestParameters.approveRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Records a payout of accrued royalty and settles it.  The amount is RESERVED against the author\'s pending royalty atomically before anything is paid, so a payout can never exceed what is owed even under concurrent calls. An external author\'s payout is then BACKED against the platform reserve fund — a second, independent guard — and refused with 402 if the reserve cannot cover it, with the reservation voided. A \"credits\" method issues the actual wallet grant after both guards; a cash method is record-only. A first-party (treasury) author\'s royalty is realized into Hanzo\'s own reserve instead of an external wallet, and every payout row discloses which of the three it was.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Records a payout of accrued royalty and settles it.
         * @param {AdminApiPostAdminAuthorsByIdPayoutRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsByIdPayout(requestParameters: AdminApiPostAdminAuthorsByIdPayoutRequest, options?: RawAxiosRequestConfig): AxiosPromise<PayoutResult> {
            return localVarFp.postAdminAuthorsByIdPayout(requestParameters.id, requestParameters.payoutRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Stops one author earning. Their record, verified claims and ledger are untouched — suspension halts future accrual, it does not erase what was already owed, and it does not delete the evidence behind it.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Stops one author earning.
         * @param {AdminApiPostAdminAuthorsByIdSuspendRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsByIdSuspend(requestParameters: AdminApiPostAdminAuthorsByIdSuspendRequest, options?: RawAxiosRequestConfig): AxiosPromise<AuthorResult> {
            return localVarFp.postAdminAuthorsByIdSuspend(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.  It is an OVERRIDE, not the mechanism: a background scheduler runs the same sweep on its own, and every author\'s dashboard read sweeps their own accruals lazily. This is the manual trigger for an operator who needs the numbers now. It is idempotent — the per-period latch means running it twice accrues nothing the second time.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
         * @summary Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminAuthorsSweep(options?: RawAxiosRequestConfig): AxiosPromise<AuthorSweepResult> {
            return localVarFp.postAdminAuthorsSweep(options).then((request) => request(axios, basePath));
        },
        /**
         * Qualify-checks every pending referral and advances the ones that now qualify.  SuperAdmin only, fail-closed. This is the cron path, and the ONLY path that advances a referral: a referee QUALIFIES once they have made metered spend — the honest signal that they actually used the product rather than merely signing up.  Qualifying moves NO money. It records that an attribution became a real customer; what is owed for that is an affiliate payable in commerce, settled by wire or to a connected wallet. One pass is bounded, so a large backlog drains over several runs instead of wedging one request, and the latch makes the transition at-most-once under a concurrent sweep.  It reads nothing from the caller — the counters it returns are the whole result.
         * @summary Qualify-checks every pending referral and advances the ones that now qualify.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminReferralsSweep(options?: RawAxiosRequestConfig): AxiosPromise<SweepEnvelope> {
            return localVarFp.postAdminReferralsSweep(options).then((request) => request(axios, basePath));
        },
        /**
         * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status. When the chain path is wired it signs and submits the anchor transaction and records it; when it is not, it returns the root that WOULD be committed plus the exact remaining wiring step and records nothing false. A submit that fails still answers 200 with the anchor\'s own status set to \"error\" — the attempt is the product. SuperAdmin only.
         * @summary Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasuryAnchor(options?: RawAxiosRequestConfig): AxiosPromise<AnchorOut> {
            return localVarFp.postAdminTreasuryAnchor(options).then((request) => request(axios, basePath));
        },
        /**
         * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy. 0–10000; the change is audited. SuperAdmin only.
         * @summary Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy.
         * @param {AdminApiPostAdminTreasuryPolicyRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasuryPolicy(requestParameters: AdminApiPostAdminTreasuryPolicyRequest, options?: RawAxiosRequestConfig): AxiosPromise<PolicyOut> {
            return localVarFp.postAdminTreasuryPolicy(requestParameters.policyRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote. A repeat of the same ref is at-most-once and reports created=false. SuperAdmin only.
         * @summary Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote.
         * @param {AdminApiPostAdminTreasurySeedRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasurySeed(requestParameters: AdminApiPostAdminTreasurySeedRequest, options?: RawAxiosRequestConfig): AxiosPromise<SeedOut> {
            return localVarFp.postAdminTreasurySeed(requestParameters.seedRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved. It is idempotent per period: a re-run of a period already swept accrues nothing and reports created=false. SuperAdmin only.
         * @summary Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved.
         * @param {AdminApiPostAdminTreasurySweepRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAdminTreasurySweep(requestParameters: AdminApiPostAdminTreasurySweepRequest, options?: RawAxiosRequestConfig): AxiosPromise<SweepOut> {
            return localVarFp.postAdminTreasurySweep(requestParameters.sweepRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta. It is generic over kind, so the same call manages models, providers and product features through the one registry. `off` is an absolute kill switch: a self-service opt-in can never re-open it. SuperAdmin only; every other caller is refused.
         * @summary Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta.
         * @param {AdminApiPutAdminEnablementRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putAdminEnablement(requestParameters: AdminApiPutAdminEnablementRequest, options?: RawAxiosRequestConfig): AxiosPromise<AdminEnablementItem> {
            return localVarFp.putAdminEnablement(requestParameters.setEnablementBody, options).then((request) => request(axios, basePath));
        },
        /**
         * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas. It provisions-or-resolves the caller org\'s treasury wallet on the deployed MPC ring and installs it, so every later anchor commits the ledger root SIGNED BY THE QUORUM WALLET instead of a lone KMS key. Idempotent — a repeat resolves the same wallet, which is why the address is a PUT. SuperAdmin only.
         * @summary Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putAdminTreasuryAnchorSigner(options?: RawAxiosRequestConfig): AxiosPromise<SignerOut> {
            return localVarFp.putAdminTreasuryAnchorSigner(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for adminAIMetrics operation in AdminApi.
 * @export
 * @interface AdminApiAdminAIMetricsRequest
 */
export interface AdminApiAdminAIMetricsRequest {
    /**
     * Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
     * @type {string}
     * @memberof AdminApiAdminAIMetrics
     */
    readonly range?: string
}

/**
 * Request parameters for adminAnalytics operation in AdminApi.
 * @export
 * @interface AdminApiAdminAnalyticsRequest
 */
export interface AdminApiAdminAnalyticsRequest {
    /**
     * Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
     * @type {string}
     * @memberof AdminApiAdminAnalytics
     */
    readonly range?: string
}

/**
 * Request parameters for adminApplications operation in AdminApi.
 * @export
 * @interface AdminApiAdminApplicationsRequest
 */
export interface AdminApiAdminApplicationsRequest {
    /**
     * Owner is the org whose rows to read. Defaults to the admin org, which owns the platform\&#39;s roles and applications.
     * @type {string}
     * @memberof AdminApiAdminApplications
     */
    readonly owner?: string

    /**
     * Page is the 1-based page number. Forwarded only when set — IAM applies its own default otherwise.
     * @type {string}
     * @memberof AdminApiAdminApplications
     */
    readonly p?: string

    /**
     * PageSize is rows per page. Forwarded only when set.
     * @type {string}
     * @memberof AdminApiAdminApplications
     */
    readonly pageSize?: string
}

/**
 * Request parameters for adminAudit operation in AdminApi.
 * @export
 * @interface AdminApiAdminAuditRequest
 */
export interface AdminApiAdminAuditRequest {
    /**
     * Org restricts the trail to one tenant.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly org?: string

    /**
     * Sub restricts it to one actor (the validated subject that made the request).
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly sub?: string

    /**
     * Action restricts it to one action name, e.g. \&quot;admin.waitlist.grant\&quot;.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly action?: string

    /**
     * Resource restricts it to one resource kind, e.g. \&quot;credit-grant\&quot;.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly resource?: string

    /**
     * ResourceID restricts it to one resource instance.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly resourceId?: string

    /**
     * Result restricts it to \&quot;success\&quot; or \&quot;error\&quot;.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly result?: string

    /**
     * Since is the inclusive lower time bound, RFC3339. An unparseable value is ignored rather than refused — one malformed filter must not hide the trail.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly since?: string

    /**
     * Until is the upper time bound, RFC3339, with the same tolerance.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly until?: string

    /**
     * PageSize is rows per page, default 100.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly pageSize?: string

    /**
     * Page is the 1-based page number, driving the offset.
     * @type {string}
     * @memberof AdminApiAdminAudit
     */
    readonly p?: string
}

/**
 * Request parameters for adminCaps operation in AdminApi.
 * @export
 * @interface AdminApiAdminCapsRequest
 */
export interface AdminApiAdminCapsRequest {
    /**
     * Org is the tenant to act on. Required for a SuperAdmin — they must name their target; ignored for a white-label admin, who always acts on their own org.
     * @type {string}
     * @memberof AdminApiAdminCaps
     */
    readonly org?: string

    /**
     * ID is the cap to edit or remove, from the path. Unused by the list and create ops.
     * @type {string}
     * @memberof AdminApiAdminCaps
     */
    readonly id?: string
}

/**
 * Request parameters for adminCompute operation in AdminApi.
 * @export
 * @interface AdminApiAdminComputeRequest
 */
export interface AdminApiAdminComputeRequest {
    /**
     * Kind narrows to one workload class (bot | machine | cluster | nodepool | container | function | …). An OPEN spectrum matched as a plain string, lowercased to the warehouse\&#39;s convention; empty means every kind.
     * @type {string}
     * @memberof AdminApiAdminCompute
     */
    readonly kind?: string

    /**
     * Org narrows to one tenant. Empty means every tenant — this board is cross-tenant by nature.
     * @type {string}
     * @memberof AdminApiAdminCompute
     */
    readonly org?: string

    /**
     * Range is the lower time bound: 24h, 7d or 30d. Anything else reads as 30d.
     * @type {string}
     * @memberof AdminApiAdminCompute
     */
    readonly range?: string
}

/**
 * Request parameters for adminCordonNode operation in AdminApi.
 * @export
 * @interface AdminApiAdminCordonNodeRequest
 */
export interface AdminApiAdminCordonNodeRequest {
    /**
     * ID is the node\&#39;s droplet id, from the path.
     * @type {string}
     * @memberof AdminApiAdminCordonNode
     */
    readonly id: string

    /**
     * 
     * @type {CordonIn}
     * @memberof AdminApiAdminCordonNode
     */
    readonly cordonIn: CordonIn
}

/**
 * Request parameters for adminCreateCap operation in AdminApi.
 * @export
 * @interface AdminApiAdminCreateCapRequest
 */
export interface AdminApiAdminCreateCapRequest {
    /**
     * 
     * @type {CapIn}
     * @memberof AdminApiAdminCreateCap
     */
    readonly capIn: CapIn
}

/**
 * Request parameters for adminCustomer operation in AdminApi.
 * @export
 * @interface AdminApiAdminCustomerRequest
 */
export interface AdminApiAdminCustomerRequest {
    /**
     * Org is the tenant slug from the path.
     * @type {string}
     * @memberof AdminApiAdminCustomer
     */
    readonly org: string
}

/**
 * Request parameters for adminDeleteCap operation in AdminApi.
 * @export
 * @interface AdminApiAdminDeleteCapRequest
 */
export interface AdminApiAdminDeleteCapRequest {
    /**
     * ID is the cap to edit or remove, from the path. Unused by the list and create ops.
     * @type {string}
     * @memberof AdminApiAdminDeleteCap
     */
    readonly id: string

    /**
     * Org is the tenant to act on. Required for a SuperAdmin — they must name their target; ignored for a white-label admin, who always acts on their own org.
     * @type {string}
     * @memberof AdminApiAdminDeleteCap
     */
    readonly org?: string
}

/**
 * Request parameters for adminDeleteDroplet operation in AdminApi.
 * @export
 * @interface AdminApiAdminDeleteDropletRequest
 */
export interface AdminApiAdminDeleteDropletRequest {
    /**
     * ID is the DO droplet id, from the path. Numeric.
     * @type {string}
     * @memberof AdminApiAdminDeleteDroplet
     */
    readonly id: string

    /**
     * Size is the target DigitalOcean size slug on resize, e.g. \&quot;s-4vcpu-8gb\&quot;.
     * @type {string}
     * @memberof AdminApiAdminDeleteDroplet
     */
    readonly size?: string

    /**
     * Disk requests a PERMANENT resize that grows the disk. DO can never resize such a droplet down again, so it defaults false — a CPU/RAM-only change, reversible.
     * @type {boolean}
     * @memberof AdminApiAdminDeleteDroplet
     */
    readonly disk?: boolean
}

/**
 * Request parameters for adminDeleteLoadBalancer operation in AdminApi.
 * @export
 * @interface AdminApiAdminDeleteLoadBalancerRequest
 */
export interface AdminApiAdminDeleteLoadBalancerRequest {
    /**
     * ID is the DO load balancer id, from the path.
     * @type {string}
     * @memberof AdminApiAdminDeleteLoadBalancer
     */
    readonly id: string
}

/**
 * Request parameters for adminDeleteVolume operation in AdminApi.
 * @export
 * @interface AdminApiAdminDeleteVolumeRequest
 */
export interface AdminApiAdminDeleteVolumeRequest {
    /**
     * ID is the DO volume id, from the path.
     * @type {string}
     * @memberof AdminApiAdminDeleteVolume
     */
    readonly id: string

    /**
     * Snapshot is the snapshot-first switch on DELETE. Anything other than the literal \&quot;false\&quot; snapshots before destroying — the snapshot IS the undo, so waiving it is deliberate and explicit.
     * @type {string}
     * @memberof AdminApiAdminDeleteVolume
     */
    readonly snapshot?: string

    /**
     * Name is the snapshot name on the snapshot action. Blank gets a deterministic \&quot;&lt;volume&gt;-predelete-&lt;unix&gt;\&quot; so the undo is findable in the DO console.
     * @type {string}
     * @memberof AdminApiAdminDeleteVolume
     */
    readonly name?: string

    /**
     * SizeGiB is the target size on the resize action. A volume only ever grows — ExpandTo is the verdict that refuses a shrink, so this is not validated here.
     * @type {number}
     * @memberof AdminApiAdminDeleteVolume
     */
    readonly sizeGiB?: number
}

/**
 * Request parameters for adminDisablePlugin operation in AdminApi.
 * @export
 * @interface AdminApiAdminDisablePluginRequest
 */
export interface AdminApiAdminDisablePluginRequest {
    /**
     * Name is the app, from the path.
     * @type {string}
     * @memberof AdminApiAdminDisablePlugin
     */
    readonly name: string

    /**
     * 
     * @type {NameIn}
     * @memberof AdminApiAdminDisablePlugin
     */
    readonly nameIn: NameIn
}

/**
 * Request parameters for adminEnablePlugin operation in AdminApi.
 * @export
 * @interface AdminApiAdminEnablePluginRequest
 */
export interface AdminApiAdminEnablePluginRequest {
    /**
     * Name is the app, from the path.
     * @type {string}
     * @memberof AdminApiAdminEnablePlugin
     */
    readonly name: string

    /**
     * 
     * @type {NameIn}
     * @memberof AdminApiAdminEnablePlugin
     */
    readonly nameIn: NameIn
}

/**
 * Request parameters for adminFinanceBackfill operation in AdminApi.
 * @export
 * @interface AdminApiAdminFinanceBackfillRequest
 */
export interface AdminApiAdminFinanceBackfillRequest {
    /**
     * 
     * @type {BackfillIn}
     * @memberof AdminApiAdminFinanceBackfill
     */
    readonly backfillIn: BackfillIn
}

/**
 * Request parameters for adminGrantCredit operation in AdminApi.
 * @export
 * @interface AdminApiAdminGrantCreditRequest
 */
export interface AdminApiAdminGrantCreditRequest {
    /**
     * Org is the tenant to credit. Required.
     * @type {string}
     * @memberof AdminApiAdminGrantCredit
     */
    readonly org: string

    /**
     * 
     * @type {GrantIn}
     * @memberof AdminApiAdminGrantCredit
     */
    readonly grantIn: GrantIn
}

/**
 * Request parameters for adminGrants operation in AdminApi.
 * @export
 * @interface AdminApiAdminGrantsRequest
 */
export interface AdminApiAdminGrantsRequest {
    /**
     * Org filters by the ACTOR\&#39;s org (the staff org that issued the grant), which is rarely what a reader wants — the target org is a row field, not a filter.
     * @type {string}
     * @memberof AdminApiAdminGrants
     */
    readonly org?: string

    /**
     * Result filters by outcome: \&quot;success\&quot; or \&quot;error\&quot;. Empty returns both, which is the point of this view — a refused grant is as interesting as a granted one.
     * @type {string}
     * @memberof AdminApiAdminGrants
     */
    readonly result?: string

    /**
     * Limit caps the rows returned. Default 200.
     * @type {string}
     * @memberof AdminApiAdminGrants
     */
    readonly limit?: string
}

/**
 * Request parameters for adminInfra operation in AdminApi.
 * @export
 * @interface AdminApiAdminInfraRequest
 */
export interface AdminApiAdminInfraRequest {
    /**
     * Refresh, when present, forces a full re-scan instead of serving the cached snapshot. Every MUTATION re-scans regardless — this is only for the reader.
     * @type {string}
     * @memberof AdminApiAdminInfra
     */
    readonly refresh?: string
}

/**
 * Request parameters for adminInvoices operation in AdminApi.
 * @export
 * @interface AdminApiAdminInvoicesRequest
 */
export interface AdminApiAdminInvoicesRequest {
    /**
     * Status filters on the invoice\&#39;s LATEST lifecycle status (paid, open, void, …), matched case-insensitively.
     * @type {string}
     * @memberof AdminApiAdminInvoices
     */
    readonly status?: string

    /**
     * Org filters to one tenant, matched exactly.
     * @type {string}
     * @memberof AdminApiAdminInvoices
     */
    readonly org?: string

    /**
     * Limit caps the rows returned. total still reports the full match count.
     * @type {string}
     * @memberof AdminApiAdminInvoices
     */
    readonly limit?: string
}

/**
 * Request parameters for adminIssueGrant operation in AdminApi.
 * @export
 * @interface AdminApiAdminIssueGrantRequest
 */
export interface AdminApiAdminIssueGrantRequest {
    /**
     * 
     * @type {GrantIn}
     * @memberof AdminApiAdminIssueGrant
     */
    readonly grantIn: GrantIn
}

/**
 * Request parameters for adminMetrics operation in AdminApi.
 * @export
 * @interface AdminApiAdminMetricsRequest
 */
export interface AdminApiAdminMetricsRequest {
    /**
     * Window is the movement window the new/churned MRR and the recent feed are measured over. Anything unrecognised falls back to the board default.
     * @type {string}
     * @memberof AdminApiAdminMetrics
     */
    readonly window?: string

    /**
     * Limit caps the top-customers table.
     * @type {string}
     * @memberof AdminApiAdminMetrics
     */
    readonly limit?: string
}

/**
 * Request parameters for adminO11y operation in AdminApi.
 * @export
 * @interface AdminApiAdminO11yRequest
 */
export interface AdminApiAdminO11yRequest {
    /**
     * Range is the lower time bound: 24h, 7d or 30d. Anything else reads as the board\&#39;s own default.
     * @type {string}
     * @memberof AdminApiAdminO11y
     */
    readonly range?: string
}

/**
 * Request parameters for adminPlugins operation in AdminApi.
 * @export
 * @interface AdminApiAdminPluginsRequest
 */
export interface AdminApiAdminPluginsRequest {
    /**
     * Scope \&quot;host\&quot; answers for THIS host only. Default \&quot;fleet\&quot; fans out to every live peer. A peer answers a host-scoped read, which is what stops the fan-out recursing.
     * @type {string}
     * @memberof AdminApiAdminPlugins
     */
    readonly scope?: string
}

/**
 * Request parameters for adminProducts operation in AdminApi.
 * @export
 * @interface AdminApiAdminProductsRequest
 */
export interface AdminApiAdminProductsRequest {
    /**
     * Kind matches the operator App CR\&#39;s declared spec.role (sql|kv|generic|ingress).
     * @type {string}
     * @memberof AdminApiAdminProducts
     */
    readonly kind?: string

    /**
     * Tier matches the derived infra grouping (cloud|data|edge|daemon|paas|app).
     * @type {string}
     * @memberof AdminApiAdminProducts
     */
    readonly tier?: string

    /**
     * Env matches the lifecycle namespace (main|test|dev).
     * @type {string}
     * @memberof AdminApiAdminProducts
     */
    readonly env?: string
}

/**
 * Request parameters for adminReactivateCustomer operation in AdminApi.
 * @export
 * @interface AdminApiAdminReactivateCustomerRequest
 */
export interface AdminApiAdminReactivateCustomerRequest {
    /**
     * Org is the tenant slug from the path.
     * @type {string}
     * @memberof AdminApiAdminReactivateCustomer
     */
    readonly org: string
}

/**
 * Request parameters for adminReloadPlugin operation in AdminApi.
 * @export
 * @interface AdminApiAdminReloadPluginRequest
 */
export interface AdminApiAdminReloadPluginRequest {
    /**
     * Name is the app, from the path. It must be one the manifest declares.
     * @type {string}
     * @memberof AdminApiAdminReloadPlugin
     */
    readonly name: string

    /**
     * 
     * @type {ReloadIn}
     * @memberof AdminApiAdminReloadPlugin
     */
    readonly reloadIn: ReloadIn
}

/**
 * Request parameters for adminResizeDroplet operation in AdminApi.
 * @export
 * @interface AdminApiAdminResizeDropletRequest
 */
export interface AdminApiAdminResizeDropletRequest {
    /**
     * ID is the DO droplet id, from the path. Numeric.
     * @type {string}
     * @memberof AdminApiAdminResizeDroplet
     */
    readonly id: string

    /**
     * 
     * @type {DropletIn}
     * @memberof AdminApiAdminResizeDroplet
     */
    readonly dropletIn: DropletIn
}

/**
 * Request parameters for adminResizeVolume operation in AdminApi.
 * @export
 * @interface AdminApiAdminResizeVolumeRequest
 */
export interface AdminApiAdminResizeVolumeRequest {
    /**
     * ID is the DO volume id, from the path.
     * @type {string}
     * @memberof AdminApiAdminResizeVolume
     */
    readonly id: string

    /**
     * 
     * @type {VolumeIn}
     * @memberof AdminApiAdminResizeVolume
     */
    readonly volumeIn: VolumeIn
}

/**
 * Request parameters for adminRoles operation in AdminApi.
 * @export
 * @interface AdminApiAdminRolesRequest
 */
export interface AdminApiAdminRolesRequest {
    /**
     * Owner is the org whose rows to read. Defaults to the admin org, which owns the platform\&#39;s roles and applications.
     * @type {string}
     * @memberof AdminApiAdminRoles
     */
    readonly owner?: string

    /**
     * Page is the 1-based page number. Forwarded only when set — IAM applies its own default otherwise.
     * @type {string}
     * @memberof AdminApiAdminRoles
     */
    readonly p?: string

    /**
     * PageSize is rows per page. Forwarded only when set.
     * @type {string}
     * @memberof AdminApiAdminRoles
     */
    readonly pageSize?: string
}

/**
 * Request parameters for adminScaleNodePool operation in AdminApi.
 * @export
 * @interface AdminApiAdminScaleNodePoolRequest
 */
export interface AdminApiAdminScaleNodePoolRequest {
    /**
     * ID is the DOKS cluster id, from the path.
     * @type {string}
     * @memberof AdminApiAdminScaleNodePool
     */
    readonly id: string

    /**
     * Pool is the node pool, from the path. Its DO id or its name — both are unique within a cluster, and an operator reads the name off the board.
     * @type {string}
     * @memberof AdminApiAdminScaleNodePool
     */
    readonly pool: string

    /**
     * 
     * @type {ScaleIn}
     * @memberof AdminApiAdminScaleNodePool
     */
    readonly scaleIn: ScaleIn
}

/**
 * Request parameters for adminSetFlag operation in AdminApi.
 * @export
 * @interface AdminApiAdminSetFlagRequest
 */
export interface AdminApiAdminSetFlagRequest {
    /**
     * Key is the switch to write, taken from the path (e.g. \&quot;waitlist.chat\&quot;).
     * @type {string}
     * @memberof AdminApiAdminSetFlag
     */
    readonly key: string

    /**
     * 
     * @type {SetFlagIn}
     * @memberof AdminApiAdminSetFlag
     */
    readonly setFlagIn: SetFlagIn
}

/**
 * Request parameters for adminSetPromo operation in AdminApi.
 * @export
 * @interface AdminApiAdminSetPromoRequest
 */
export interface AdminApiAdminSetPromoRequest {
    /**
     * 
     * @type {PromoIn}
     * @memberof AdminApiAdminSetPromo
     */
    readonly promoIn: PromoIn
}

/**
 * Request parameters for adminSetServiceMode operation in AdminApi.
 * @export
 * @interface AdminApiAdminSetServiceModeRequest
 */
export interface AdminApiAdminSetServiceModeRequest {
    /**
     * Service is the slug to flip, taken from the path.
     * @type {string}
     * @memberof AdminApiAdminSetServiceMode
     */
    readonly service: string

    /**
     * 
     * @type {ServiceModeIn}
     * @memberof AdminApiAdminSetServiceMode
     */
    readonly serviceModeIn: ServiceModeIn
}

/**
 * Request parameters for adminSnapshotVolume operation in AdminApi.
 * @export
 * @interface AdminApiAdminSnapshotVolumeRequest
 */
export interface AdminApiAdminSnapshotVolumeRequest {
    /**
     * ID is the DO volume id, from the path.
     * @type {string}
     * @memberof AdminApiAdminSnapshotVolume
     */
    readonly id: string

    /**
     * 
     * @type {VolumeIn}
     * @memberof AdminApiAdminSnapshotVolume
     */
    readonly volumeIn: VolumeIn
}

/**
 * Request parameters for adminSubscriptions operation in AdminApi.
 * @export
 * @interface AdminApiAdminSubscriptionsRequest
 */
export interface AdminApiAdminSubscriptionsRequest {
    /**
     * Status filters on the subscription\&#39;s LATEST lifecycle status (active, trialing, canceled, …), matched case-insensitively.
     * @type {string}
     * @memberof AdminApiAdminSubscriptions
     */
    readonly status?: string

    /**
     * Org filters to one tenant, matched exactly.
     * @type {string}
     * @memberof AdminApiAdminSubscriptions
     */
    readonly org?: string

    /**
     * Limit caps the rows returned. total still reports the full match count.
     * @type {string}
     * @memberof AdminApiAdminSubscriptions
     */
    readonly limit?: string
}

/**
 * Request parameters for adminSubsystems operation in AdminApi.
 * @export
 * @interface AdminApiAdminSubsystemsRequest
 */
export interface AdminApiAdminSubsystemsRequest {
    /**
     * Range bounds the telemetry window: 24h, 7d or 30d. Anything else, including empty, resolves to the default through the same o11yRange the o11y board uses.
     * @type {string}
     * @memberof AdminApiAdminSubsystems
     */
    readonly range?: string
}

/**
 * Request parameters for adminSuspendCustomer operation in AdminApi.
 * @export
 * @interface AdminApiAdminSuspendCustomerRequest
 */
export interface AdminApiAdminSuspendCustomerRequest {
    /**
     * Org is the tenant slug from the path.
     * @type {string}
     * @memberof AdminApiAdminSuspendCustomer
     */
    readonly org: string
}

/**
 * Request parameters for adminUpdateCap operation in AdminApi.
 * @export
 * @interface AdminApiAdminUpdateCapRequest
 */
export interface AdminApiAdminUpdateCapRequest {
    /**
     * ID is the cap to edit or remove, from the path. Unused by the list and create ops.
     * @type {string}
     * @memberof AdminApiAdminUpdateCap
     */
    readonly id: string

    /**
     * 
     * @type {CapIn}
     * @memberof AdminApiAdminUpdateCap
     */
    readonly capIn: CapIn
}

/**
 * Request parameters for adminUpsertService operation in AdminApi.
 * @export
 * @interface AdminApiAdminUpsertServiceRequest
 */
export interface AdminApiAdminUpsertServiceRequest {
    /**
     * 
     * @type {ServiceInput}
     * @memberof AdminApiAdminUpsertService
     */
    readonly serviceInput: ServiceInput
}

/**
 * Request parameters for adminUsage operation in AdminApi.
 * @export
 * @interface AdminApiAdminUsageRequest
 */
export interface AdminApiAdminUsageRequest {
    /**
     * Org reads ONE tenant\&#39;s trailing-30-day total instead of the fleet sum. Honoured for a SuperAdmin only — a white-label admin always reads their own org.  The window is the one core.OrgMoney returns, and it is what the operator board beside this already labelled (\&quot;Daily, last 30 days\&quot;). The wire used to say month-to-date while that UI said 30 days; they agree now. This comment is REGENERATED into plugin/admin/openapi.json and openapi.yaml as the ?org parameter description, so a stale word here ships as a contradiction inside one spec file — which is the drift this whole change set exists to remove.
     * @type {string}
     * @memberof AdminApiAdminUsage
     */
    readonly org?: string
}

/**
 * Request parameters for adminUsageFunding operation in AdminApi.
 * @export
 * @interface AdminApiAdminUsageFundingRequest
 */
export interface AdminApiAdminUsageFundingRequest {
    /**
     * From is the inclusive start of the window. Unparseable or absent, together with To, falls back to the last 30 days.
     * @type {string}
     * @memberof AdminApiAdminUsageFunding
     */
    readonly from?: string

    /**
     * To is the exclusive end of the window.
     * @type {string}
     * @memberof AdminApiAdminUsageFunding
     */
    readonly to?: string
}

/**
 * Request parameters for adminUsers operation in AdminApi.
 * @export
 * @interface AdminApiAdminUsersRequest
 */
export interface AdminApiAdminUsersRequest {
    /**
     * Org narrows the directory to ONE tenant. Honoured for a SuperAdmin only — a white-label admin is pinned to their own org and this is ignored.
     * @type {string}
     * @memberof AdminApiAdminUsers
     */
    readonly org?: string

    /**
     * Query is a free-text filter, matched by IAM as a \&quot;contains\&quot; over the user name.
     * @type {string}
     * @memberof AdminApiAdminUsers
     */
    readonly q?: string

    /**
     * Page is the 1-based page number. Defaults to \&quot;1\&quot;; IAM returns zero rows AND a zero total when it is unset, so this layer never leaves it empty.
     * @type {string}
     * @memberof AdminApiAdminUsers
     */
    readonly p?: string

    /**
     * PageSize is rows per page. Defaults to \&quot;200\&quot;, the shared admin page size.
     * @type {string}
     * @memberof AdminApiAdminUsers
     */
    readonly pageSize?: string
}

/**
 * Request parameters for adminWaitlist operation in AdminApi.
 * @export
 * @interface AdminApiAdminWaitlistRequest
 */
export interface AdminApiAdminWaitlistRequest {
    /**
     * Waitlist is the waitlist slug to read (e.g. \&quot;chat\&quot;). The engine decides what an empty slug means.
     * @type {string}
     * @memberof AdminApiAdminWaitlist
     */
    readonly waitlist?: string

    /**
     * Page is the 1-based page number.
     * @type {string}
     * @memberof AdminApiAdminWaitlist
     */
    readonly page?: string

    /**
     * PageSize is entries per page.
     * @type {string}
     * @memberof AdminApiAdminWaitlist
     */
    readonly pageSize?: string
}

/**
 * Request parameters for adminWaitlistBoost operation in AdminApi.
 * @export
 * @interface AdminApiAdminWaitlistBoostRequest
 */
export interface AdminApiAdminWaitlistBoostRequest {
    /**
     * 
     * @type {WaitlistBoostRequest}
     * @memberof AdminApiAdminWaitlistBoost
     */
    readonly waitlistBoostRequest: WaitlistBoostRequest
}

/**
 * Request parameters for getAdminAffiliates operation in AdminApi.
 * @export
 * @interface AdminApiGetAdminAffiliatesRequest
 */
export interface AdminApiGetAdminAffiliatesRequest {
    /**
     * Limit caps the rows returned. Absent or non-positive means the default of 500; anything above 1000 is clamped to 1000.
     * @type {number}
     * @memberof AdminApiGetAdminAffiliates
     */
    readonly limit?: number
}

/**
 * Request parameters for getAdminAuthors operation in AdminApi.
 * @export
 * @interface AdminApiGetAdminAuthorsRequest
 */
export interface AdminApiGetAdminAuthorsRequest {
    /**
     * Limit bounds the page. 0 or less means the default of 500; anything above 1000 is clamped to 1000.
     * @type {number}
     * @memberof AdminApiGetAdminAuthors
     */
    readonly limit?: number
}

/**
 * Request parameters for getAdminAuthorsByIdBasis operation in AdminApi.
 * @export
 * @interface AdminApiGetAdminAuthorsByIdBasisRequest
 */
export interface AdminApiGetAdminAuthorsByIdBasisRequest {
    /**
     * ID is the author record\&#39;s handle, from the path.
     * @type {string}
     * @memberof AdminApiGetAdminAuthorsByIdBasis
     */
    readonly id: string

    /**
     * Period is the UTC accrual month, YYYY-MM. Empty means every period; any other shape is refused with 400.
     * @type {string}
     * @memberof AdminApiGetAdminAuthorsByIdBasis
     */
    readonly period?: string
}

/**
 * Request parameters for getAdminReferralsBonuses operation in AdminApi.
 * @export
 * @interface AdminApiGetAdminReferralsBonusesRequest
 */
export interface AdminApiGetAdminReferralsBonusesRequest {
    /**
     * Limit is how many referrals to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 500; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
     * @type {string}
     * @memberof AdminApiGetAdminReferralsBonuses
     */
    readonly limit?: string
}

/**
 * Request parameters for getAdminTreasury operation in AdminApi.
 * @export
 * @interface AdminApiGetAdminTreasuryRequest
 */
export interface AdminApiGetAdminTreasuryRequest {
    /**
     * Limit caps the journal entries returned. Out of range or unparseable takes the default.
     * @type {number}
     * @memberof AdminApiGetAdminTreasury
     */
    readonly limit?: number
}

/**
 * Request parameters for patchAdminCatalogModelsByWildcard1 operation in AdminApi.
 * @export
 * @interface AdminApiPatchAdminCatalogModelsByWildcard1Request
 */
export interface AdminApiPatchAdminCatalogModelsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof AdminApiPatchAdminCatalogModelsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for patchAdminCatalogProvidersByName operation in AdminApi.
 * @export
 * @interface AdminApiPatchAdminCatalogProvidersByNameRequest
 */
export interface AdminApiPatchAdminCatalogProvidersByNameRequest {
    /**
     * Name is the provider the overlay belongs to, from the URL.
     * @type {string}
     * @memberof AdminApiPatchAdminCatalogProvidersByName
     */
    readonly name: string

    /**
     * 
     * @type {ProviderPatchIn}
     * @memberof AdminApiPatchAdminCatalogProvidersByName
     */
    readonly providerPatchIn: ProviderPatchIn
}

/**
 * Request parameters for postAdminAffiliatesByIdApprove operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminAffiliatesByIdApproveRequest
 */
export interface AdminApiPostAdminAffiliatesByIdApproveRequest {
    /**
     * ID is the affiliate to approve, from the path.
     * @type {string}
     * @memberof AdminApiPostAdminAffiliatesByIdApprove
     */
    readonly id: string

    /**
     * 
     * @type {Approval}
     * @memberof AdminApiPostAdminAffiliatesByIdApprove
     */
    readonly approval: Approval
}

/**
 * Request parameters for postAdminAffiliatesByIdPayout operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminAffiliatesByIdPayoutRequest
 */
export interface AdminApiPostAdminAffiliatesByIdPayoutRequest {
    /**
     * ID is the affiliate to pay, from the path.
     * @type {string}
     * @memberof AdminApiPostAdminAffiliatesByIdPayout
     */
    readonly id: string

    /**
     * 
     * @type {Disbursal}
     * @memberof AdminApiPostAdminAffiliatesByIdPayout
     */
    readonly disbursal: Disbursal
}

/**
 * Request parameters for postAdminAffiliatesByIdRate operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminAffiliatesByIdRateRequest
 */
export interface AdminApiPostAdminAffiliatesByIdRateRequest {
    /**
     * ID is the affiliate whose direct rate moves, from the path.
     * @type {string}
     * @memberof AdminApiPostAdminAffiliatesByIdRate
     */
    readonly id: string

    /**
     * 
     * @type {RateSet}
     * @memberof AdminApiPostAdminAffiliatesByIdRate
     */
    readonly rateSet: RateSet
}

/**
 * Request parameters for postAdminAffiliatesByIdSuspend operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminAffiliatesByIdSuspendRequest
 */
export interface AdminApiPostAdminAffiliatesByIdSuspendRequest {
    /**
     * ID is the affiliate\&#39;s server-minted handle, \&quot;aff_\&quot;-prefixed.
     * @type {string}
     * @memberof AdminApiPostAdminAffiliatesByIdSuspend
     */
    readonly id: string
}

/**
 * Request parameters for postAdminAuthorsByIdApprove operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminAuthorsByIdApproveRequest
 */
export interface AdminApiPostAdminAuthorsByIdApproveRequest {
    /**
     * ID is the author to approve, from the path.
     * @type {string}
     * @memberof AdminApiPostAdminAuthorsByIdApprove
     */
    readonly id: string

    /**
     * 
     * @type {ApproveRequest}
     * @memberof AdminApiPostAdminAuthorsByIdApprove
     */
    readonly approveRequest: ApproveRequest
}

/**
 * Request parameters for postAdminAuthorsByIdPayout operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminAuthorsByIdPayoutRequest
 */
export interface AdminApiPostAdminAuthorsByIdPayoutRequest {
    /**
     * ID is the author to pay, from the path.
     * @type {string}
     * @memberof AdminApiPostAdminAuthorsByIdPayout
     */
    readonly id: string

    /**
     * 
     * @type {PayoutRequest}
     * @memberof AdminApiPostAdminAuthorsByIdPayout
     */
    readonly payoutRequest: PayoutRequest
}

/**
 * Request parameters for postAdminAuthorsByIdSuspend operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminAuthorsByIdSuspendRequest
 */
export interface AdminApiPostAdminAuthorsByIdSuspendRequest {
    /**
     * ID is the author record\&#39;s handle, \&quot;aut_\&quot;-prefixed.
     * @type {string}
     * @memberof AdminApiPostAdminAuthorsByIdSuspend
     */
    readonly id: string
}

/**
 * Request parameters for postAdminTreasuryPolicy operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminTreasuryPolicyRequest
 */
export interface AdminApiPostAdminTreasuryPolicyRequest {
    /**
     * 
     * @type {PolicyRequest}
     * @memberof AdminApiPostAdminTreasuryPolicy
     */
    readonly policyRequest: PolicyRequest
}

/**
 * Request parameters for postAdminTreasurySeed operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminTreasurySeedRequest
 */
export interface AdminApiPostAdminTreasurySeedRequest {
    /**
     * 
     * @type {SeedRequest}
     * @memberof AdminApiPostAdminTreasurySeed
     */
    readonly seedRequest: SeedRequest
}

/**
 * Request parameters for postAdminTreasurySweep operation in AdminApi.
 * @export
 * @interface AdminApiPostAdminTreasurySweepRequest
 */
export interface AdminApiPostAdminTreasurySweepRequest {
    /**
     * 
     * @type {SweepRequest}
     * @memberof AdminApiPostAdminTreasurySweep
     */
    readonly sweepRequest: SweepRequest
}

/**
 * Request parameters for putAdminEnablement operation in AdminApi.
 * @export
 * @interface AdminApiPutAdminEnablementRequest
 */
export interface AdminApiPutAdminEnablementRequest {
    /**
     * 
     * @type {SetEnablementBody}
     * @memberof AdminApiPutAdminEnablement
     */
    readonly setEnablementBody: SetEnablementBody
}

/**
 * AdminApi - object-oriented interface
 * @export
 * @class AdminApi
 * @extends {BaseAPI}
 */
export class AdminApi extends BaseAPI {
    /**
     * Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).  Every signal degrades INDEPENDENTLY — a table that is absent or errors contributes its zero value and the read still succeeds. Generation latency is a SEPARATE query from generations and cost on purpose: a duration/attribute mismatch there must not zero the two numbers that did read.
     * @summary Is the fleet AI board: LLM generations over gen_ai spans (count, cost, avg/p95 latency, per-model), per-model usage from the live cloud_usage ledger, and the eval plane (traces, scores, score names, runs, and the average-score trend).
     * @param {AdminApiAdminAIMetricsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminAIMetrics(requestParameters: AdminApiAdminAIMetricsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminAIMetrics(requestParameters.range, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.  The window is the caller\'s, not the fleet\'s: a SuperAdmin gets every org, a white-label admin only their own subtree (core.ScopedOrgs, the one scope predicate).  sources[] carries each upstream\'s freshness so a partial read is VISIBLE rather than silently low: a ledger that answered for only some orgs marks commerce-ledger degraded instead of publishing an undercount as healthy.
     * @summary Is the SaaS product-analytics board over the caller\'s tenant window: active customers, new and churned, retention, MRR, ARPU, the usage trend and the top customers by spend — every number folded from the commerce ledger, not sampled.
     * @param {AdminApiAdminAnalyticsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminAnalytics(requestParameters: AdminApiAdminAnalyticsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminAnalytics(requestParameters.range, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications. These are the platform\'s OIDC clients — the console reads clientId off each row.
     * @summary Lists IAM applications for one owner org, forwarded VERBATIM from IAM\'s get-applications.
     * @param {AdminApiAdminApplicationsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminApplications(requestParameters: AdminApiAdminApplicationsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminApplications(requestParameters.owner, requestParameters.p, requestParameters.pageSize, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.  When cloud has no local store configured it falls back to forwarding IAM\'s own get-records trail verbatim — a DIFFERENT trail, federated so the endpoint never regresses to an empty list. Those rows carry no integrity of ours, so the field is null there.
     * @summary Reads one chain of cloud\'s tamper-evident audit trail, newest first, with that chain\'s live integrity attached so a listing can be badged as verified.
     * @param {AdminApiAdminAuditRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminAudit(requestParameters: AdminApiAdminAuditRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminAudit(requestParameters.org, requestParameters.sub, requestParameters.action, requestParameters.resource, requestParameters.resourceId, requestParameters.result, requestParameters.since, requestParameters.until, requestParameters.pageSize, requestParameters.p, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.  The trail is a FAMILY of chains, one per process, so the answer is a set and not a boolean: `intact`, `broken` and `unread` count the three verdicts and sum to the number of chains. A chain that could not be READ is reported `unread` and is never a pass — an unreadable chain and a verified one must not render the same, which is the whole reason this is not one flag.  An unconfigured store is an honest failure here rather than a fabricated pass.
     * @summary Walks EVERY hash chain this deployment keeps and reports each one: which chains were checked, how many records each holds, the head hash to pin externally against tail-truncation, and — when a chain is broken — the seq of the first bad record and why.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminAuditVerify(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminAuditVerify(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.  The scope is enforced TWICE: the upstream is asked for the caller\'s org, AND every row it returns is re-checked against the resolved scope. An upstream that ignored the filter therefore degrades to empty, never to a cross-tenant leak.  The Base engine is being embedded into cloud; until it lands this proxies BASE_ADMIN_URL and, when that is unset, answers 200 with an empty list and msg saying so — the honest not-yet state, never fabricated instances.
     * @summary Lists the tenant Base instances in the caller\'s window — a SuperAdmin sees every tenant\'s, anyone else only their own subtree\'s.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminBases(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminBases(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.  These are the SAME rows the customer edits in their own console — a platform override and a customer budget are one model, not two.
     * @summary Reads one org\'s usage caps: its spend alerts plus the derived period spend, over/warn state and reset time.
     * @param {AdminApiAdminCapsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminCaps(requestParameters: AdminApiAdminCapsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminCaps(requestParameters.org, requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event. The console folds these into its org → app → project tree.  A machine counts as ACTIVE when its LATEST lifecycle event is not a terminal one (stop/destroy/terminate/delete/off/shutdown/expire and their past tenses) — the same fold the console applies, done in the warehouse so the count is over every machine and not just the page.  Honest-empty when the warehouse is not connected or hanzo.compute_usage is not provisioned yet: an empty list, never a fabricated fleet.
     * @summary Rolls the fleet\'s compute usage up to one row per (org, app, project, kind): how many distinct machines ran in the window, how many are still active, what they billed, and when each group last emitted an event.
     * @param {AdminApiAdminComputeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminCompute(requestParameters: AdminApiAdminComputeRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminCompute(requestParameters.kind, requestParameters.org, requestParameters.range, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.  It is the ONE infra change that does not go through the run discipline, because there is no destructive verdict to check: cordoning is reversible and evicting respects the cluster\'s own PodDisruptionBudgets. It reads the cached board for the same reason. The outcome is audited either way, and the result reports how many pods were evicted.
     * @summary Marks one cluster node unschedulable — or schedulable again — and can drain the pods already on it.
     * @param {AdminApiAdminCordonNodeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminCordonNode(requestParameters: AdminApiAdminCordonNodeRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminCordonNode(requestParameters.id, requestParameters.cordonIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows. The body is commerce\'s spend-alert contract, forwarded byte-for-byte.
     * @summary Sets a usage cap on one org — a platform override of a customer budget, written to the customer\'s own spend-alert rows.
     * @param {AdminApiAdminCreateCapRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminCreateCap(requestParameters: AdminApiAdminCreateCapRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminCreateCap(requestParameters.capIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers GET /v1/admin/customers/:org.
     * @summary Answers GET /v1/admin/customers/:org.
     * @param {AdminApiAdminCustomerRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminCustomer(requestParameters: AdminApiAdminCustomerRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminCustomer(requestParameters.org, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.  Each row costs one IAM read plus the org\'s money reads, fanned out under a fixed concurrency ceiling so a large fleet cannot stampede the upstreams. Every read is best-effort per row: an upstream miss degrades THAT field to its honest zero rather than failing the fleet.
     * @summary Lists every customer org at a glance, sorted by slug: owner email, plan, suspend status, member count, balance, month-to-date spend and MRR.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminCustomers(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminCustomers(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes one cap by id, lifting the ceiling entirely.
     * @summary Removes one cap by id, lifting the ceiling entirely.
     * @param {AdminApiAdminDeleteCapRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminDeleteCap(requestParameters: AdminApiAdminDeleteCapRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminDeleteCap(requestParameters.id, requestParameters.org, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Destroys a droplet the board has just proven is NOT a DOKS node. There is no snapshot-first undo for a droplet the way there is for a volume: the local disk goes with it.
     * @summary Destroys a droplet the board has just proven is NOT a DOKS node.
     * @param {AdminApiAdminDeleteDropletRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminDeleteDroplet(requestParameters: AdminApiAdminDeleteDropletRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminDeleteDroplet(requestParameters.id, requestParameters.size, requestParameters.disk, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
     * @summary Destroys a load balancer the board has just proven no live type=LoadBalancer Service in any cluster targets.
     * @param {AdminApiAdminDeleteLoadBalancerRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminDeleteLoadBalancer(requestParameters: AdminApiAdminDeleteLoadBalancerRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminDeleteLoadBalancer(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Destroys a volume the board has just proven no PersistentVolume in any cluster references. Irreversible, so it snapshots first unless explicitly waived — the snapshot IS the undo.
     * @summary Destroys a volume the board has just proven no PersistentVolume in any cluster references.
     * @param {AdminApiAdminDeleteVolumeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminDeleteVolume(requestParameters: AdminApiAdminDeleteVolumeRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminDeleteVolume(requestParameters.id, requestParameters.snapshot, requestParameters.name, requestParameters.sizeGiB, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Stops the plugin. Its routes STAY REGISTERED and answer 503 — not 404.  That is zip\'s choice and this keeps it. Removing the routes would mutate the route table, and re-adding them on enable would grow it without bound across repeated cycles, which is the invariant that makes reloads flat in memory. It is also the better answer: 404 says \"no such API\" and a client may cache it and stop retrying, while 503 says \"this API exists and is down right now\", which is true and retryable. Which of the two 503s this is — deliberate stop or crash — is what the status\'s disabled flag reports.
     * @summary Stops the plugin.
     * @param {AdminApiAdminDisablePluginRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminDisablePlugin(requestParameters: AdminApiAdminDisablePluginRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminDisablePlugin(requestParameters.name, requestParameters.nameIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag. Named for what an operator means by it.
     * @summary Brings a stopped or disabled plugin back on the artifact it already has: the zero Plugin names no new artifact, so Reload reuses the loaded spec and clears the disabled flag.
     * @param {AdminApiAdminEnablePluginRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminEnablePlugin(requestParameters: AdminApiAdminEnablePluginRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminEnablePlugin(requestParameters.name, requestParameters.nameIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers GET /v1/admin/finance. It reads the multi-vendor COGS from commerce /v1/costs, the DO promo-credit/burn-down treasury view, and the fleet commerce revenue, then hands them to ComputeFinance. SuperAdmin only.
     * @summary Answers GET /v1/admin/finance.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminFinance(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminFinance(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.  It is IDEMPOTENT: the deposit uses the fixed ref \"backfill:<org>\", so re-running it credits the wallet at most once. Safe to retry.  The pre-migration balance is read from the CO-RESIDENT commerce ledger, not over HTTP: the admin HTTP client dials an unroutable in-process address and would read $0, and a phantom zero would silently carry nothing while reporting success. When commerce is not co-resident this fails rather than migrating nothing.
     * @summary Carries ONE org\'s current commerce prepaid balance into the native finance wallet — the one-time cutover between the two ledgers.
     * @param {AdminApiAdminFinanceBackfillRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminFinanceBackfill(requestParameters: AdminApiAdminFinanceBackfillRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminFinanceBackfill(requestParameters.backfillIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
     * @summary Reads the platform control-plane board: every runtime launch/release switch (waitlist, public signup, subsystem activation, gateway limits, network ids) with its LIVE value and where that value came from — a stored definition or the compiled-in default.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminFlags(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminFlags(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.  The credit lands on the account account.Payer resolves, NOT necessarily the org: name a member of a pooled org and the pool is credited. The receipt echoes the subject so the caller can see which.
     * @summary Issues a staff credit grant to the org named in the path — a comp, refund or promo — through the ONE credit-write path core.ApplyGrant, which validates the amount against the per-grant cap, checks the org exists, moves the money and records the tamper-evident audit row.
     * @param {AdminApiAdminGrantCreditRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminGrantCredit(requestParameters: AdminApiAdminGrantCreditRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminGrantCredit(requestParameters.org, requestParameters.grantIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.  It is a PROJECTION of the tamper-evident audit trail, not a second store: every grant is written there as action \"admin.customer.credit\", so this view cannot drift from what actually happened, and FAILED grants appear too.  A deployment with no local audit store has no history to project, and says so with an empty list and a msg rather than an error.
     * @summary Reads the credit-grant ledger across ALL orgs, newest first — who granted what to whom, when, and from which money bucket.
     * @param {AdminApiAdminGrantsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminGrants(requestParameters: AdminApiAdminGrantsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminGrants(requestParameters.org, requestParameters.result, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.  It is cached for up to a minute because one read is a fan-out over the DO API plus a full pod/PV listing per cluster. Staleness is never load-bearing: every MUTATION re-scans from scratch and ignores this cache.  Only an unusable DO account is a hard failure. A partial read still produces a board, with the failing source named in sources[] — except for clusters and volumes, which the safety verdict depends on; without those the analysis degrades rather than classifying anything it cannot prove.
     * @summary Serves the whole DigitalOcean infrastructure board: droplets, volumes, DOKS clusters and load balancers, each cross-referenced against every cluster\'s live Kubernetes state so the board can say what is safe to destroy and what is not.
     * @param {AdminApiAdminInfraRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminInfra(requestParameters: AdminApiAdminInfraRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminInfra(requestParameters.refresh, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers GET /v1/admin/invoices.   GET /v1/admin/invoices?org=&status=&limit=
     * @summary Answers GET /v1/admin/invoices.
     * @param {AdminApiAdminInvoicesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminInvoices(requestParameters: AdminApiAdminInvoicesRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminInvoices(requestParameters.status, requestParameters.org, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Issues a credit grant to any org from the operator Grants view, with the target named in the body. It funnels through the SAME core.ApplyGrant that POST /v1/admin/customers/:org/credit uses, so there is exactly ONE credit-write path and one audit trail behind both.
     * @summary Issues a credit grant to any org from the operator Grants view, with the target named in the body.
     * @param {AdminApiAdminIssueGrantRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminIssueGrant(requestParameters: AdminApiAdminIssueGrantRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminIssueGrant(requestParameters.grantIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is. The fields come from the sanitized identity headers the gate just read, so they are authoritative and never client-forgeable; nothing is looked up.
     * @summary Answers with the validated operator identity — who the console is signed in as, which tier they are, and how wide their tenant window is.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminMe(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminMe(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out). SuperAdmin only.   GET /v1/admin/metrics?window=30d&limit=20
     * @summary Answers GET /v1/admin/metrics by aggregating commerce.events directly (fleet-wide, no per-org fan-out).
     * @param {AdminApiAdminMetricsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminMetrics(requestParameters: AdminApiAdminMetricsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminMetrics(requestParameters.window, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * moneyBoardHandler answers GET /v1/admin/money.
     * @summary moneyBoardHandler answers GET /v1/admin/money.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminMoney(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminMoney(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.  Every signal degrades INDEPENDENTLY. A table that is absent or errors contributes its zero value and the read still succeeds, so the board renders exactly what the warehouse holds rather than failing whole because one of four sources is missing. Same when the warehouse is not connected at all: the zero board, never a fabricated fleet.
     * @summary Is the fleet-wide observability board: LLM usage (requests, tokens, cost, errors, top orgs, top models), trace RED metrics (count, p50/p95/p99 latency in ms, error rate, top services), fleet log volume, and the O11yAI generation rollup — all aggregated across EVERY tenant, with no org filter applied.
     * @param {AdminApiAdminO11yRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminO11y(requestParameters: AdminApiAdminO11yRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminO11y(requestParameters.range, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.  The rows are the caller\'s tenant window, not the fleet: a SuperAdmin gets every org, a white-label admin only their own subtree. A per-org read that fails degrades THAT row to an honest zero — this panel carries no sources[] channel to report freshness on, so the alternative would be a fleet total that silently reads healthy.
     * @summary Lists the tenant directory one row per org, sorted by slug: member count and the org\'s month-to-date spend and credit balance, read live from IAM and commerce.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminOrgs(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminOrgs(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.  It ALWAYS answers 200 — a tile board that fails as a whole because one upstream is down is useless. Instead every upstream reports itself in sources[]: ok, degraded, or not-configured. A commerce read that failed for ANY org marks that source degraded, because the spend/credits totals are then an undercount and must not read healthy.  The AI tiles — 30-day spend and tokens — come from the AI ledger (ledger.go), the plane that owns \"what was served\". They used to come from the money plane with the token counter hardcoded to zero, so the board read $0.00 and 0 tokens over a month in which the fleet served fifteen thousand requests. Credits still come from commerce, which owns the wallet.
     * @summary Is the Platform Overview tiles: how many orgs and users are in the caller\'s tenant window, the fleet workload counts, and month-to-date spend and credits.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminOverview(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminOverview(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.  Reading this from deployment config would answer what was INTENDED. Only the process knows what is TRUE, and during a rolling upgrade the two disagree on purpose.
     * @summary Reports what each host is actually running: every loaded plugin with its version, pid, uptime, reload and restart counts, and its measured CPU, RSS, thread and fd cost — read from the kernel, which is only answerable at all because a plugin is a process.
     * @param {AdminApiAdminPluginsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminPlugins(requestParameters: AdminApiAdminPluginsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminPlugins(requestParameters.scope, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict. Optionally narrowed by kind, tier or env, each an exact match.  The rows are the SAME observation /v1/platform/fleet renders — read through the in-process platform seam, not a second k8s client — so the two boards can never disagree about what the fleet is. A PaaS plane that is not co-resident yields an honestly empty registry, never a fabricated row.
     * @summary Lists the fleet workload registry: every operator App CR across the platform namespaces with its declared vs running image tag, reconciled health/phase and drift verdict.
     * @param {AdminApiAdminProductsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminProducts(requestParameters: AdminApiAdminProductsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminProducts(requestParameters.kind, requestParameters.tier, requestParameters.env, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads the current platform plan promo — the singleton discount offer, e.g. the 50%-off launch promo. Commerce stores it in the reserved platform namespace, so the org sent with the read is the admin org and the service token is what passes commerce\'s own platform-admin gate.
     * @summary Reads the current platform plan promo — the singleton discount offer, e.g.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminPromo(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminPromo(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger. SuperAdmin-guarded (see Routes).
     * @summary Serves GET /v1/admin/providers/credit — the per-provider upstream credit ledger.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminProvidersCredit(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminProvidersCredit(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Restores access for every member of the org, undoing a suspend. It reports the same per-user breakdown.
     * @summary Restores access for every member of the org, undoing a suspend.
     * @param {AdminApiAdminReactivateCustomerRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminReactivateCustomer(requestParameters: AdminApiAdminReactivateCustomerRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminReactivateCustomer(requestParameters.org, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Swaps a plugin for another build without dropping a request. The replacement is started and proven to be LISTENING before any traffic moves to it, so a bad build leaves the old one serving and returns an error rather than a hole; the old process then drains before it is killed.  With a version or url+sum it pins; naming a digest this host has run before is the rollback, and costs no network because the digest IS the cache key. With neither it restarts what is already loaded.  Fleet scope applies it to one host at a time and STOPS at the first failure, so a build that cannot come up reaches exactly one host.
     * @summary Swaps a plugin for another build without dropping a request.
     * @param {AdminApiAdminReloadPluginRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminReloadPlugin(requestParameters: AdminApiAdminReloadPluginRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminReloadPlugin(requestParameters.name, requestParameters.reloadIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Changes a droplet\'s plan. Same refusal as delete and for the same reason: a DOKS node\'s size is the node pool\'s to declare.  disk=true is a PERMANENT resize — the disk grows and DO can never resize the droplet DOWN again. disk=false (the default) changes CPU/RAM only and is reversible. DO requires the droplet to be powered off and applies the change asynchronously, so the response carries the action to poll, not a completed change.
     * @summary Changes a droplet\'s plan.
     * @param {AdminApiAdminResizeDropletRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminResizeDroplet(requestParameters: AdminApiAdminResizeDropletRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminResizeDroplet(requestParameters.id, requestParameters.dropletIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Grows a volume. GROW ONLY — see Volume.ExpandTo for why the other direction is a data migration this board deliberately refuses to run.  The MECHANISM follows the volume\'s owner, because there is exactly one way to grow each kind completely. A volume a PVC claims is grown by patching the claim: the CSI driver then resizes the DigitalOcean device AND grows the filesystem on it, leaving claim, PV, device and filesystem all agreeing. Calling DigitalOcean directly for that volume would grow the device while the PV kept declaring the old capacity and the filesystem never grew at all. One operation, one correct mechanism per owner — not two ways to do it.
     * @summary Grows a volume.
     * @param {AdminApiAdminResizeVolumeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminResizeVolume(requestParameters: AdminApiAdminResizeVolumeRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminResizeVolume(requestParameters.id, requestParameters.volumeIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.  ORTHOGONAL to /v1/admin/finance, which is the COGS/margin view of what WE pay vendors. This is the customer side: what each customer holds, spends and subscribes to.  arpu divides realized spend by PAYING customers, not by all of them — a fleet of free signups must not deflate the number. A customer counts as paying when it has spend or MRR.  An org whose money did not read degrades to honest zeros and marks the commerce source degraded in sources[], so a partial fleet read is visible instead of quietly low.
     * @summary Is the fleet money board: total prepaid balances held, total realized spend, MRR, ARPU, a per-customer table sorted highest-revenue first, and a real 30-day spend trend from the usage ledger.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminRevenue(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminRevenue(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
     * @summary Lists IAM roles for one owner org, forwarded VERBATIM from IAM\'s get-roles.
     * @param {AdminApiAdminRolesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminRoles(requestParameters: AdminApiAdminRolesRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminRoles(requestParameters.owner, requestParameters.p, requestParameters.pageSize, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.  The response states what the board could NOT prove: DOKS picks which nodes a shrink removes, so no particular pod is shown to survive one. See NodePool.ScaleTo.
     * @summary Sets a node pool\'s node count — the ONE correct way to change how many nodes a DOKS cluster has.
     * @param {AdminApiAdminScaleNodePoolRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminScaleNodePool(requestParameters: AdminApiAdminScaleNodePoolRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminScaleNodePool(requestParameters.id, requestParameters.pool, requestParameters.scaleIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine. This is the \"remove the waitlist one service at a time\" view.
     * @summary Reads the launch board: every hosted service in the registry with its LIVE waitlist mode, evaluated through the flag engine.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminServices(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminServices(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands. The flip is hot: this pod applies it immediately and peers converge within one evaluation TTL (15s by default), with no redeploy.  The body reaches the flag engine BYTE-FOR-BYTE — it is the engine\'s definition format, not this layer\'s, so a field the engine understands and admin does not must still arrive intact. setFlagIn names the two fields that matter for documentation; it is not a filter.  The write is recorded in the store\'s activity log against the caller\'s email.
     * @summary Stores or overwrites ONE platform switch\'s definition and answers with the whole board as it now stands.
     * @param {AdminApiAdminSetFlagRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSetFlag(requestParameters: AdminApiAdminSetFlagRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSetFlag(requestParameters.key, requestParameters.setFlagIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Upserts the platform plan promo — the ONE place the offer is configured.  The body is commerce\'s own promo contract and is forwarded BYTE-FOR-BYTE, so no field commerce accepts is dropped in transit. promoIn names its documented fields.
     * @summary Upserts the platform plan promo — the ONE place the offer is configured.
     * @param {AdminApiAdminSetPromoRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSetPromo(requestParameters: AdminApiAdminSetPromoRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSetPromo(requestParameters.promoIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Flips ONE service\'s waitlist switch — the launch lever. Hot: it takes effect on this pod immediately and on peers within one evaluation TTL, with no redeploy. An unknown service is a 404, not a silent create; onboarding goes through upsertService.
     * @summary Flips ONE service\'s waitlist switch — the launch lever.
     * @param {AdminApiAdminSetServiceModeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSetServiceMode(requestParameters: AdminApiAdminSetServiceModeRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSetServiceMode(requestParameters.service, requestParameters.serviceModeIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.  It re-scans the board first (never the cache) so the volume it snapshots is one that exists right now, and audits the outcome either way.
     * @summary Takes a point-in-time snapshot of one volume — the undo a delete relies on, available on its own so an operator can take one before any risky change.
     * @param {AdminApiAdminSnapshotVolumeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSnapshotVolume(requestParameters: AdminApiAdminSnapshotVolumeRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSnapshotVolume(requestParameters.id, requestParameters.volumeIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers GET /v1/admin/subscriptions.   GET /v1/admin/subscriptions?org=&status=&limit=
     * @summary Answers GET /v1/admin/subscriptions.
     * @param {AdminApiAdminSubscriptionsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSubscriptions(requestParameters: AdminApiAdminSubscriptionsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSubscriptions(requestParameters.status, requestParameters.org, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * subsystems answers GET /v1/admin/subsystems. ?range=24h|7d|30d bounds the telemetry window (default 30d) — the same enum, and the same helpers, as the o11y board.
     * @summary subsystems answers GET /v1/admin/subsystems.
     * @param {AdminApiAdminSubsystemsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSubsystems(requestParameters: AdminApiAdminSubsystemsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSubsystems(requestParameters.range, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token. Fully reversible with ReactivateCustomer.  The result names every user updated and every user that was NOT — a partial failure leaves the org in a mixed state and says so instead of reporting a clean success.
     * @summary Cuts off every member of the org: IAM refuses a forbidden user at login AND at token issuance, so a suspended customer can neither sign in nor mint a fresh token.
     * @param {AdminApiAdminSuspendCustomerRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSuspendCustomer(requestParameters: AdminApiAdminSuspendCustomerRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSuspendCustomer(requestParameters.org, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the operator\'s \"Sync now\" button. There is nothing to kick: admin aggregates LIVE on every read, so the button is just a re-read. It acknowledges honestly with started:true rather than pretending a batch job was queued.
     * @summary Answers the operator\'s \"Sync now\" button.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminSync(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminSync(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Edits one cap by id — raise or lower the ceiling, flip enforcement. The body is commerce\'s spend-alert patch contract, forwarded byte-for-byte.
     * @summary Edits one cap by id — raise or lower the ceiling, flip enforcement.
     * @param {AdminApiAdminUpdateCapRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminUpdateCap(requestParameters: AdminApiAdminUpdateCapRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminUpdateCap(requestParameters.id, requestParameters.capIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy. Re-registering an existing service PRESERVES its live switch — editing the hosts of a service that is already open must not silently close it again.
     * @summary Onboards a hosted service, or edits one, so a new host comes under the launch gate WITHOUT a redeploy.
     * @param {AdminApiAdminUpsertServiceRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminUpsertService(requestParameters: AdminApiAdminUpsertServiceRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminUpsertService(requestParameters.serviceInput, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.  It reads the AI ledger (ledger.go), which is the plane that owns this question. It used to ask the commerce billing API instead, once per org, and answer with a hardcoded empty series, zero tokens and zero requests, on the reasoning that a trend and a split were \"not derivable from the commerce billing API\". They are not — but the question was never commerce\'s. hanzo.cloud_usage carries a row per served request, so all three fall out of the same window the totals do.
     * @summary Returns the trailing 30 days of AI usage: one org\'s when org names one, else the whole fleet\'s — the spend, the tokens and the requests, the daily curve behind them, and the split by model.
     * @param {AdminApiAdminUsageRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminUsage(requestParameters: AdminApiAdminUsageRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminUsage(requestParameters.org, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).  The class is resolved at the PROVIDER level from the credit ledger, not per call — the per-call split, and the `byo` class, arrive when the metering write stamps a funding column on cloud_usage and this can GROUP BY it directly. Until then a provider with remaining grant reports all of its usage as credit, which is right in aggregate and approximate at the boundary where a grant runs out mid-window.  An unparseable window falls back to the last 30 days rather than refusing: this is a dashboard read, and a typo in a date must not blank the board.
     * @summary Splits our upstream AI usage by how it was FUNDED: one row per (provider, model) over the window, tagged credit (provider grant still remaining), paid (grant exhausted) or paid_only (no grant at all).
     * @param {AdminApiAdminUsageFundingRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminUsageFunding(requestParameters: AdminApiAdminUsageFundingRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminUsageFunding(requestParameters.from, requestParameters.to, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the user directory across the caller\'s tenant window, one page at a time. total is IAM\'s REAL total, so the console can page through it.  A SuperAdmin may aim the read at one tenant with org; a white-label admin cannot — for them the owner is hard-pinned to their own org and org is ignored, which is what keeps the directory from becoming a cross-tenant read.
     * @summary Lists the user directory across the caller\'s tenant window, one page at a time.
     * @param {AdminApiAdminUsersRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminUsers(requestParameters: AdminApiAdminUsersRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminUsers(requestParameters.org, requestParameters.q, requestParameters.p, requestParameters.pageSize, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.  A volume\'s usedGiB and pct are null, always: DO exposes capacity and attachment but no fill, so the console renders \"—\" rather than a number nobody measured. The datastore card is the one real fill here, and it is the number to scale on.  The two sources degrade independently — a DO outage still returns the datastore fill, and a disconnected datastore still returns the DO fleet.
     * @summary Returns the realtime block-storage board: the DigitalOcean volume fleet (count, capacity, monthly list cost, per-volume region and attachment) plus the analytics datastore\'s OWN fill, read from its system.disks.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminVolumes(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminVolumes(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.  The engine\'s payload is forwarded VERBATIM as data; the console normalizes it. When the engine is not configured on this deployment the read still succeeds, with an empty object and a msg saying so, so the panel shows an honest not-wired state instead of an error the operator would chase.
     * @summary Reads one waitlist\'s leaderboard from the Hanzo waitlist engine — position, points and referral standing per entry — proxied server-authed with the engine secret, never a client credential.
     * @param {AdminApiAdminWaitlistRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminWaitlist(requestParameters: AdminApiAdminWaitlistRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminWaitlist(requestParameters.waitlist, requestParameters.page, requestParameters.pageSize, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Grants a user waitlist points, moving them up toward the access cutoff. This is the access lever: the cutoff itself does not move, the person does.  It funnels through the engine\'s verified grant seam (POST /v1/waitlist/award with source=\"grant\" — the ONE path that honours an explicit points amount) and writes a tamper-evident audit row either way, so a FAILED grant is recorded too. The reason field goes only to that row.
     * @summary Grants a user waitlist points, moving them up toward the access cutoff.
     * @param {AdminApiAdminWaitlistBoostRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public adminWaitlistBoost(requestParameters: AdminApiAdminWaitlistBoostRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).adminWaitlistBoost(requestParameters.waitlistBoostRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.  PLATFORM SUDO ONLY, and a non-admin is refused outright. This is the cross-tenant view and it names orgs — exactly what the partner-facing leaderboard refuses to do. There is deliberately no org-scoped variant of this read; a partner sees its own standing through its own dashboard. Bounded per request.
     * @summary Lists every affiliate across the fleet with its ORG exposed, plus a fleet summary of lifetime accrued, still-pending and paid commission in integer cents.
     * @param {AdminApiGetAdminAffiliatesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminAffiliates(requestParameters: AdminApiGetAdminAffiliatesRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminAffiliates(requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.  It is a Hanzo platform operation: a caller who is not a SuperAdmin gets 403. It exposes the owning org of each author, which no tenant-facing read ever does.
     * @summary Returns the platform\'s whole author program — every org\'s author record, not the caller\'s — with each one\'s repository and deploy counts and a fleet roll-up of the money accrued, pending and paid.
     * @param {AdminApiGetAdminAuthorsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminAuthors(requestParameters: AdminApiGetAdminAuthorsRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminAuthors(requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.  The data object carries: id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger (every row with its spend, the share applied then, the platform\'s matching half, whether it satisfies the formula and the attribution edges that explain it), reconciliation (does the ledger foot to the balance) and window (what slice was actually returned) — plus period when one was requested.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * @summary Returns the audit trail behind ONE author\'s royalty — the same payload the author reads at /v1/authors/basis, from the same builder, so support sees exactly what the author sees rather than a parallel view free to drift.
     * @param {AdminApiGetAdminAuthorsByIdBasisRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminAuthorsByIdBasis(requestParameters: AdminApiGetAdminAuthorsByIdBasisRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminAuthorsByIdBasis(requestParameters.id, requestParameters.period, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console. Nothing is hidden: this is the admin\'s view of what exists and what is currently off, in beta or generally available. SuperAdmin only; every other caller is refused.
     * @summary Returns the full model and provider catalog annotated with each entry\'s enablement state, for the operator console.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminCatalog(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminCatalog(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta. An item nobody has touched is absent, because an untouched item is generally available; the console composes the candidate list from the live catalog. SuperAdmin only; every other caller is refused.
     * @summary Returns every item an operator has set an enablement state on — its global state (off, beta or ga) and the orgs granted its beta.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminEnablement(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminEnablement(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.  Read the liability figure carefully — it is commission accrued and NOT yet paid, so it is money owed, not money spent, and the per-level split says how much of it comes from direct referrals versus the second and third levels.  PLATFORM SUDO ONLY, cross-tenant, and it names orgs. It reads the SAME single attribution spine the accrual itself walks, so the board and the ledger cannot disagree. Amounts are integer cents.
     * @summary Answers the referral board: the top referrers by lifetime commission, the funnel conversion rate (referred orgs that have actually produced commission, over all referred orgs), and the accrual LIABILITY the platform owes, broken out by upline level.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminReferrals(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminReferrals(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns every referral edge in the directory with a fleet summary.  SuperAdmin only, fail-closed. This is the ATTRIBUTION directory — who referred whom and whether that referee became a customer. It carries no amounts because this package issues none. The cross-tenant referral ANALYTICS board (top referrers, conversion) is a different surface, GET /v1/admin/referrals, owned by the affiliates subsystem over the shared attribution spine.
     * @summary Returns every referral edge in the directory with a fleet summary.
     * @param {AdminApiGetAdminReferralsBonusesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminReferralsBonuses(requestParameters: AdminApiGetAdminReferralsBonusesRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminReferralsBonuses(requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root. ?limit= bounds the journal page.
     * @summary Returns the whole treasury board for a SuperAdmin: the reserve fund report, the recent double-entry journal, and the Hanzo L1 anchor status of the ledger root.
     * @param {AdminApiGetAdminTreasuryRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public getAdminTreasury(requestParameters: AdminApiGetAdminTreasuryRequest = {}, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).getAdminTreasury(requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sets one model\'s availability overlay — and the price overrides applied on top of the catalog — then answers the new effective overlay, so a console needs no second read. The model id is the whole remaining path, so a slashed id like `acme/some-model-1` addresses intact.  SuperAdmin only; every other caller is 403, decided before the body is read. The overlay is PLATFORM-WIDE — this is the catalog every org prices against, not a per-org setting — and `betaOrgs` is what narrows a beta to named orgs.  Only the fields the patch names change; an entry with no overlay yet starts from the catalog default, which is enabled. `state` is the coherent tri-state setter (`off`|`beta`|`ga`) and the low-level `enabled`/`beta` flags are applied AFTER it, so they win where both are sent; anything else in `state` is 400. A field sent as an explicit `null` arrives indistinguishable from an absent one, so null does not clear anything.  The rule worth reading twice: a disabled entry that still carries beta orgs IS a beta — `{\"enabled\":false,\"betaOrgs\":[\"acme\"]}` leaves acme seeing the model. Only an explicit `off` (or `beta:false`) with an empty list is the absolute kill switch that a user\'s own beta opt-in can never re-open.  `overrides` is an RFC 7386 merge patch, stored and echoed back verbatim; it must be a JSON object or null — an array or a scalar is refused — and is bounded in size and nesting depth. An uninitialised overlay store answers 503.
     * @summary Turn one model off, into beta for named orgs, or generally available
     * @param {AdminApiPatchAdminCatalogModelsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public patchAdminCatalogModelsByWildcard1(requestParameters: AdminApiPatchAdminCatalogModelsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).patchAdminCatalogModelsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sets one provider\'s availability overlay.  The overlay decides whether a provider is off, in beta for named orgs, or generally available, and carries the price overrides applied on top of the catalog. Only the fields the patch names change; every other field keeps the value it had, and an absent overlay starts from the catalog default (enabled). Answers the new effective overlay, so a console needs no second read.  SuperAdmin only.
     * @summary Sets one provider\'s availability overlay.
     * @param {AdminApiPatchAdminCatalogProvidersByNameRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public patchAdminCatalogProvidersByName(requestParameters: AdminApiPatchAdminCatalogProvidersByNameRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).patchAdminCatalogProvidersByName(requestParameters.name, requestParameters.providerPatchIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.  The code is taken from the body if one is given, else the vanity code the applicant requested, else a slug derived for them. Codes are ONE global namespace, so a taken code is a 409 and nothing is approved. The minted code is also mirrored as a link row so click tracking is uniform across every code the affiliate holds; that mirror is best-effort and its failure never fails the approval.  Approval is what makes an affiliate eligible: before it, attribution against its code does not resolve and no sweep accrues to it. PLATFORM SUDO ONLY. Audited.
     * @summary Approves an affiliate and MINTS its referral code — the moment the partner has a working share link and starts accruing.
     * @param {AdminApiPostAdminAffiliatesByIdApproveRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAffiliatesByIdApprove(requestParameters: AdminApiPostAdminAffiliatesByIdApproveRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAffiliatesByIdApprove(requestParameters.id, requestParameters.approval, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.  The amount is reserved atomically against the affiliate\'s PENDING commission — accrued minus paid — so a payout can never exceed what is owed. The METHOD decides whether money actually moves: `credits` issues a commerce grant into the affiliate ORG\'s own wallet, tagged so the ledger can tell an affiliate payout apart from an admin or referral grant; every other method — wire, paypal and the rest — is RECORD-ONLY: the payout row and the balances move, the cash is disbursed out of band.  The amount is integer cents and must be positive. PLATFORM SUDO ONLY. Audited.
     * @summary Pays out accrued commission and answers the payout row with the affiliate\'s updated balances.
     * @param {AdminApiPostAdminAffiliatesByIdPayoutRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAffiliatesByIdPayout(requestParameters: AdminApiPostAdminAffiliatesByIdPayoutRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAffiliatesByIdPayout(requestParameters.id, requestParameters.disbursal, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.  The rate is CAPPED so that the direct rate plus the platform-wide second- and third-level rates can never exceed the whole margin — the structural guarantee that everything paid on one source event stays inside the margin actually earned. The cap is resolved from the rates in force at the moment of the call and quoted in the refusal, because those switches move; a hardcoded bound would start lying the moment somebody edits the schedule.  Only the direct level is per-affiliate. The second and third levels are platform switches and are not settable here. The change applies to FUTURE accruals — commission already latched for a period is not recomputed. PLATFORM SUDO ONLY. Audited.
     * @summary Sets one affiliate\'s DIRECT commission rate, in basis points of Hanzo\'s margin.
     * @param {AdminApiPostAdminAffiliatesByIdRateRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAffiliatesByIdRate(requestParameters: AdminApiPostAdminAffiliatesByIdRateRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAffiliatesByIdRate(requestParameters.id, requestParameters.rateSet, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.  It CLAWS NOTHING BACK. Commission already accrued stays accrued and stays payable, and existing attribution edges are left standing — suspension ends earning, it does not unwind history. PLATFORM SUDO ONLY. Audited.
     * @summary Suspends an affiliate: it stops accruing on the next sweep, and its code stops resolving for new attributions.
     * @param {AdminApiPostAdminAffiliatesByIdSuspendRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAffiliatesByIdSuspend(requestParameters: AdminApiPostAdminAffiliatesByIdSuspendRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAffiliatesByIdSuspend(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.  This is the cron path, and it is LATCHED at most once per affiliate, source org and period — so re-running it inside the same period accrues nothing further. Safe to retry, and safe to run by hand beside the schedule.  Commission is a rate of Hanzo\'s MARGIN on that spend, never of the customer\'s gross bill, so every level\'s share summed over one source event stays within the margin actually earned and the customer\'s charge is untouched. Nothing accrues past the third upline level, and only an APPROVED affiliate accrues at all.  The same spend read drives the OSS author royalty — one read, both programs — so the answer reports royalties accrued alongside. PLATFORM SUDO ONLY. Bounded per run; a source whose spend cannot be read is skipped and picked up next time, never half-accrued.
     * @summary Runs the accrual: for each referred org it reads that org\'s metered spend for the current period and accrues commission to every affiliate up its referral chain, then answers how many sources were swept and how many NEW accruals landed.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAffiliatesSweep(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAffiliatesSweep(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Admits one author to EARNING, optionally on a negotiated royalty share. Until this runs, a connected author accrues nothing however many verified repositories they have.  A share override applies from here forward only — existing ledger rows keep the share that was applied when they were written, because a rate change must never rewrite what was already owed.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * @summary Admits one author to EARNING, optionally on a negotiated royalty share.
     * @param {AdminApiPostAdminAuthorsByIdApproveRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAuthorsByIdApprove(requestParameters: AdminApiPostAdminAuthorsByIdApproveRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAuthorsByIdApprove(requestParameters.id, requestParameters.approveRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Records a payout of accrued royalty and settles it.  The amount is RESERVED against the author\'s pending royalty atomically before anything is paid, so a payout can never exceed what is owed even under concurrent calls. An external author\'s payout is then BACKED against the platform reserve fund — a second, independent guard — and refused with 402 if the reserve cannot cover it, with the reservation voided. A \"credits\" method issues the actual wallet grant after both guards; a cash method is record-only. A first-party (treasury) author\'s royalty is realized into Hanzo\'s own reserve instead of an external wallet, and every payout row discloses which of the three it was.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * @summary Records a payout of accrued royalty and settles it.
     * @param {AdminApiPostAdminAuthorsByIdPayoutRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAuthorsByIdPayout(requestParameters: AdminApiPostAdminAuthorsByIdPayoutRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAuthorsByIdPayout(requestParameters.id, requestParameters.payoutRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Stops one author earning. Their record, verified claims and ledger are untouched — suspension halts future accrual, it does not erase what was already owed, and it does not delete the evidence behind it.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * @summary Stops one author earning.
     * @param {AdminApiPostAdminAuthorsByIdSuspendRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAuthorsByIdSuspend(requestParameters: AdminApiPostAdminAuthorsByIdSuspendRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAuthorsByIdSuspend(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.  It is an OVERRIDE, not the mechanism: a background scheduler runs the same sweep on its own, and every author\'s dashboard read sweeps their own accruals lazily. This is the manual trigger for an operator who needs the numbers now. It is idempotent — the per-period latch means running it twice accrues nothing the second time.  A Hanzo platform operation: a caller who is not a SuperAdmin gets 403.
     * @summary Runs the accrual sweep across every approved author: for each of their deploying orgs it computes this period\'s royalty from that org\'s metered spend and latches it at most once per period.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminAuthorsSweep(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminAuthorsSweep(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Qualify-checks every pending referral and advances the ones that now qualify.  SuperAdmin only, fail-closed. This is the cron path, and the ONLY path that advances a referral: a referee QUALIFIES once they have made metered spend — the honest signal that they actually used the product rather than merely signing up.  Qualifying moves NO money. It records that an attribution became a real customer; what is owed for that is an affiliate payable in commerce, settled by wire or to a connected wallet. One pass is bounded, so a large backlog drains over several runs instead of wedging one request, and the latch makes the transition at-most-once under a concurrent sweep.  It reads nothing from the caller — the counters it returns are the whole result.
     * @summary Qualify-checks every pending referral and advances the ones that now qualify.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminReferralsSweep(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminReferralsSweep(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status. When the chain path is wired it signs and submits the anchor transaction and records it; when it is not, it returns the root that WOULD be committed plus the exact remaining wiring step and records nothing false. A submit that fails still answers 200 with the anchor\'s own status set to \"error\" — the attempt is the product. SuperAdmin only.
     * @summary Commits the current ledger root to Hanzo L1, making the books tamper-evident on chain, and returns the anchoring status.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminTreasuryAnchor(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminTreasuryAnchor(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy. 0–10000; the change is audited. SuperAdmin only.
     * @summary Sets the revenue-share basis points a sweep accrues into the reserve fund and returns the stored policy.
     * @param {AdminApiPostAdminTreasuryPolicyRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminTreasuryPolicy(requestParameters: AdminApiPostAdminTreasuryPolicyRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminTreasuryPolicy(requestParameters.policyRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote. A repeat of the same ref is at-most-once and reports created=false. SuperAdmin only.
     * @summary Injects bootstrap capital into the reserve fund so backed payouts can begin before the first revenue-share sweep, and returns the journal entry it wrote.
     * @param {AdminApiPostAdminTreasurySeedRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminTreasurySeed(requestParameters: AdminApiPostAdminTreasurySeedRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminTreasurySeed(requestParameters.seedRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved. It is idempotent per period: a re-run of a period already swept accrues nothing and reports created=false. SuperAdmin only.
     * @summary Posts the revenue-share accrual for one period — revenue into the reserve fund, at the current policy\'s basis points — and returns what it moved.
     * @param {AdminApiPostAdminTreasurySweepRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public postAdminTreasurySweep(requestParameters: AdminApiPostAdminTreasurySweepRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).postAdminTreasurySweep(requestParameters.sweepRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta. It is generic over kind, so the same call manages models, providers and product features through the one registry. `off` is an absolute kill switch: a self-service opt-in can never re-open it. SuperAdmin only; every other caller is refused.
     * @summary Sets one item\'s global enablement state — off, beta or ga — and optionally replaces the list of orgs granted its beta.
     * @param {AdminApiPutAdminEnablementRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public putAdminEnablement(requestParameters: AdminApiPutAdminEnablementRequest, options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).putAdminEnablement(requestParameters.setEnablementBody, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas. It provisions-or-resolves the caller org\'s treasury wallet on the deployed MPC ring and installs it, so every later anchor commits the ledger root SIGNED BY THE QUORUM WALLET instead of a lone KMS key. Idempotent — a repeat resolves the same wallet, which is why the address is a PUT. SuperAdmin only.
     * @summary Installs the reserve\'s threshold MPC wallet as the signer for on-chain anchors, and returns its EVM address so an operator can fund it for gas.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AdminApi
     */
    public putAdminTreasuryAnchorSigner(options?: RawAxiosRequestConfig) {
        return AdminApiFp(this.configuration).putAdminTreasuryAnchorSigner(options).then((request) => request(this.axios, this.basePath));
    }
}

