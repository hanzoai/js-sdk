/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudChallengeView } from '../models';
// @ts-ignore
import type { CloudSlotView } from '../models';
// @ts-ignore
import type { CloudValidatorClaim } from '../models';
// @ts-ignore
import type { CloudValidatorList } from '../models';
/**
 * ValidatorsApi - axios parameter creator
 * @export
 */
export const ValidatorsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
         * @summary Returns the validator slots the caller\'s org has claimed.
         * @param {string} [limit] Limit is how many slots to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 200; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Validators: async (limit?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/validators`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validators verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validators before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
         * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
         * @param {string} [tokenId] TokenID is the Validator-tier GenesisNFT token id, as a decimal string in the &#x60;?tokenId&#x3D;&#x60; query. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ValidatorsChallenge: async (tokenId?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/validators/challenge`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (tokenId !== undefined) {
                localVarQueryParameter['tokenId'] = tokenId;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
         * @summary Returns one claimed validator slot, scoped to the caller\'s org.
         * @param {string} tokenId TokenID is the slot\&#39;s GenesisNFT token id, from the path, as a decimal string. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ValidatorsTokenId: async (tokenId: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'tokenId' is not null or undefined
            assertParamExists('cloudGetV1ValidatorsTokenId', 'tokenId', tokenId)
            const localVarPath = `/v1/validators/{tokenId}`
                .replace(`{${"tokenId"}}`, encodeURIComponent(String(tokenId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
         * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
         * @param {CloudValidatorClaim} cloudValidatorClaim 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Validators: async (cloudValidatorClaim: CloudValidatorClaim, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudValidatorClaim' is not null or undefined
            assertParamExists('cloudPostV1Validators', 'cloudValidatorClaim', cloudValidatorClaim)
            const localVarPath = `/v1/validators`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudValidatorClaim, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * ValidatorsApi - functional programming interface
 * @export
 */
export const ValidatorsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = ValidatorsApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
         * @summary Returns the validator slots the caller\'s org has claimed.
         * @param {string} [limit] Limit is how many slots to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 200; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Validators(limit?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudValidatorList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Validators(limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorsApi.cloudGetV1Validators']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validators verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validators before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
         * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
         * @param {string} [tokenId] TokenID is the Validator-tier GenesisNFT token id, as a decimal string in the &#x60;?tokenId&#x3D;&#x60; query. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ValidatorsChallenge(tokenId?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudChallengeView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ValidatorsChallenge(tokenId, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorsApi.cloudGetV1ValidatorsChallenge']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
         * @summary Returns one claimed validator slot, scoped to the caller\'s org.
         * @param {string} tokenId TokenID is the slot\&#39;s GenesisNFT token id, from the path, as a decimal string. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ValidatorsTokenId(tokenId: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudSlotView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ValidatorsTokenId(tokenId, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorsApi.cloudGetV1ValidatorsTokenId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
         * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
         * @param {CloudValidatorClaim} cloudValidatorClaim 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1Validators(cloudValidatorClaim: CloudValidatorClaim, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudSlotView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1Validators(cloudValidatorClaim, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ValidatorsApi.cloudPostV1Validators']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * ValidatorsApi - factory interface
 * @export
 */
export const ValidatorsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = ValidatorsApiFp(configuration)
    return {
        /**
         * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
         * @summary Returns the validator slots the caller\'s org has claimed.
         * @param {ValidatorsApiCloudGetV1ValidatorsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Validators(requestParameters: ValidatorsApiCloudGetV1ValidatorsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudValidatorList> {
            return localVarFp.cloudGetV1Validators(requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validators verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validators before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
         * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
         * @param {ValidatorsApiCloudGetV1ValidatorsChallengeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ValidatorsChallenge(requestParameters: ValidatorsApiCloudGetV1ValidatorsChallengeRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudChallengeView> {
            return localVarFp.cloudGetV1ValidatorsChallenge(requestParameters.tokenId, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
         * @summary Returns one claimed validator slot, scoped to the caller\'s org.
         * @param {ValidatorsApiCloudGetV1ValidatorsTokenIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ValidatorsTokenId(requestParameters: ValidatorsApiCloudGetV1ValidatorsTokenIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudSlotView> {
            return localVarFp.cloudGetV1ValidatorsTokenId(requestParameters.tokenId, options).then((request) => request(axios, basePath));
        },
        /**
         * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
         * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
         * @param {ValidatorsApiCloudPostV1ValidatorsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Validators(requestParameters: ValidatorsApiCloudPostV1ValidatorsRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudSlotView> {
            return localVarFp.cloudPostV1Validators(requestParameters.cloudValidatorClaim, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudGetV1Validators operation in ValidatorsApi.
 * @export
 * @interface ValidatorsApiCloudGetV1ValidatorsRequest
 */
export interface ValidatorsApiCloudGetV1ValidatorsRequest {
    /**
     * Limit is how many slots to return, as a decimal string in the &#x60;?limit&#x3D;&#x60; query. Absent, unparseable or non-positive means 200; over 1000 is clamped to 1000. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
     * @type {string}
     * @memberof ValidatorsApiCloudGetV1Validators
     */
    readonly limit?: string
}

/**
 * Request parameters for cloudGetV1ValidatorsChallenge operation in ValidatorsApi.
 * @export
 * @interface ValidatorsApiCloudGetV1ValidatorsChallengeRequest
 */
export interface ValidatorsApiCloudGetV1ValidatorsChallengeRequest {
    /**
     * TokenID is the Validator-tier GenesisNFT token id, as a decimal string in the &#x60;?tokenId&#x3D;&#x60; query. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
     * @type {string}
     * @memberof ValidatorsApiCloudGetV1ValidatorsChallenge
     */
    readonly tokenId?: string
}

/**
 * Request parameters for cloudGetV1ValidatorsTokenId operation in ValidatorsApi.
 * @export
 * @interface ValidatorsApiCloudGetV1ValidatorsTokenIdRequest
 */
export interface ValidatorsApiCloudGetV1ValidatorsTokenIdRequest {
    /**
     * TokenID is the slot\&#39;s GenesisNFT token id, from the path, as a decimal string. A value that is not a positive integer is 400. It is a string rather than a number because the parse that has always served this route trims surrounding whitespace, and one parse rule is better than two.
     * @type {string}
     * @memberof ValidatorsApiCloudGetV1ValidatorsTokenId
     */
    readonly tokenId: string
}

/**
 * Request parameters for cloudPostV1Validators operation in ValidatorsApi.
 * @export
 * @interface ValidatorsApiCloudPostV1ValidatorsRequest
 */
export interface ValidatorsApiCloudPostV1ValidatorsRequest {
    /**
     * 
     * @type {CloudValidatorClaim}
     * @memberof ValidatorsApiCloudPostV1Validators
     */
    readonly cloudValidatorClaim: CloudValidatorClaim
}

/**
 * ValidatorsApi - object-oriented interface
 * @export
 * @class ValidatorsApi
 * @extends {BaseAPI}
 */
export class ValidatorsApi extends BaseAPI {
    /**
     * Returns the validator slots the caller\'s org has claimed.  One entry per claimed slot with its node identity, its live-ish node status and the owner-gated registration queued for it, if any. Slots are org-scoped by the validated identity, so a caller can only ever see their own — a slot claimed by another org is not merely hidden from this list, it is unreachable through the whole surface.
     * @summary Returns the validator slots the caller\'s org has claimed.
     * @param {ValidatorsApiCloudGetV1ValidatorsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorsApi
     */
    public cloudGetV1Validators(requestParameters: ValidatorsApiCloudGetV1ValidatorsRequest = {}, options?: RawAxiosRequestConfig) {
        return ValidatorsApiFp(this.configuration).cloudGetV1Validators(requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.  The nonce is bound to (validated org, slot) and stored server-side, so a signature obtained for one org or one slot can never be replayed for another, and the message POST /v1/validators verifies is rebuilt from those same server facts rather than trusted from the caller. Redeem it with POST /v1/validators before it expires; it can be redeemed once.  A tokenId outside the Validator tier is refused here rather than after signing.
     * @summary Issues the single-use nonce and the exact message a wallet must sign to claim a validator slot.
     * @param {ValidatorsApiCloudGetV1ValidatorsChallengeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorsApi
     */
    public cloudGetV1ValidatorsChallenge(requestParameters: ValidatorsApiCloudGetV1ValidatorsChallengeRequest = {}, options?: RawAxiosRequestConfig) {
        return ValidatorsApiFp(this.configuration).cloudGetV1ValidatorsChallenge(requestParameters.tokenId, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one claimed validator slot, scoped to the caller\'s org.  A slot another org holds, and a slot nobody holds, are both 404 — never a different status, so this route cannot be used to probe which slots are taken.
     * @summary Returns one claimed validator slot, scoped to the caller\'s org.
     * @param {ValidatorsApiCloudGetV1ValidatorsTokenIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorsApi
     */
    public cloudGetV1ValidatorsTokenId(requestParameters: ValidatorsApiCloudGetV1ValidatorsTokenIdRequest, options?: RawAxiosRequestConfig) {
        return ValidatorsApiFp(this.configuration).cloudGetV1ValidatorsTokenId(requestParameters.tokenId, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.  The pipeline, all server-enforced: burn the single-use challenge (so a replayed or forged nonce dies before any chain read), recover the signer from the message this server rebuilds, require that wallet to hold Validator-tier GenesisNFT #tokenId on Ethereum mainnet, generate a fresh luxd staking identity and seal it into KMS, write a LuxNetwork CR for a NEW node, and ENQUEUE an owner-gated registration. The registration is never auto-submitted to any P-Chain — the owner co-signs it out of band — and the stake weight is set at co-sign time, never derived from the NFT.  It fails CLOSED at every gate: a bad signature, a non-owner, a non-tier slot or an unavailable KMS all leave no claim persisted and no key material exposed. Re-claiming a slot this org already holds re-applies the node CR and returns 200 with the existing identity (keys and NodeID are stable); a slot held by another org is 409. A cluster-less deployment still claims the slot, seals the keys and queues the registration, reporting the node as \"node_pending\".
     * @summary Claims a validator slot and provisions its node, after proving the caller\'s wallet owns the slot\'s NFT.
     * @param {ValidatorsApiCloudPostV1ValidatorsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ValidatorsApi
     */
    public cloudPostV1Validators(requestParameters: ValidatorsApiCloudPostV1ValidatorsRequest, options?: RawAxiosRequestConfig) {
        return ValidatorsApiFp(this.configuration).cloudPostV1Validators(requestParameters.cloudValidatorClaim, options).then((request) => request(this.axios, this.basePath));
    }
}

