/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { WorldWorldLimits200Response } from '../models';
/**
 * PlanApi - axios parameter creator
 * @export
 */
export const PlanApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the Hanzo World enforcement limits for a plan, resolved from the @hanzo/plans catalog (the single source of truth) via the world.* entitlement vocabulary. Agents and the dashboard read this to self-config against the live catalog instead of hardcoding tier numbers. Defaults to world-free; degrades to the fail-closed Free floor on a catalog outage. 
         * @summary Resolved World plan limits (contract echo)
         * @param {string} [plan] Plan id (world-free | world-pro | world-team | world-enterprise). Defaults to world-free.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        worldWorldLimits: async (plan?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/world/limits`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (plan !== undefined) {
                localVarQueryParameter['plan'] = plan;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * PlanApi - functional programming interface
 * @export
 */
export const PlanApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = PlanApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the Hanzo World enforcement limits for a plan, resolved from the @hanzo/plans catalog (the single source of truth) via the world.* entitlement vocabulary. Agents and the dashboard read this to self-config against the live catalog instead of hardcoding tier numbers. Defaults to world-free; degrades to the fail-closed Free floor on a catalog outage. 
         * @summary Resolved World plan limits (contract echo)
         * @param {string} [plan] Plan id (world-free | world-pro | world-team | world-enterprise). Defaults to world-free.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async worldWorldLimits(plan?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<WorldWorldLimits200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.worldWorldLimits(plan, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['PlanApi.worldWorldLimits']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * PlanApi - factory interface
 * @export
 */
export const PlanApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = PlanApiFp(configuration)
    return {
        /**
         * Returns the Hanzo World enforcement limits for a plan, resolved from the @hanzo/plans catalog (the single source of truth) via the world.* entitlement vocabulary. Agents and the dashboard read this to self-config against the live catalog instead of hardcoding tier numbers. Defaults to world-free; degrades to the fail-closed Free floor on a catalog outage. 
         * @summary Resolved World plan limits (contract echo)
         * @param {PlanApiWorldWorldLimitsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        worldWorldLimits(requestParameters: PlanApiWorldWorldLimitsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<WorldWorldLimits200Response> {
            return localVarFp.worldWorldLimits(requestParameters.plan, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for worldWorldLimits operation in PlanApi.
 * @export
 * @interface PlanApiWorldWorldLimitsRequest
 */
export interface PlanApiWorldWorldLimitsRequest {
    /**
     * Plan id (world-free | world-pro | world-team | world-enterprise). Defaults to world-free.
     * @type {string}
     * @memberof PlanApiWorldWorldLimits
     */
    readonly plan?: string
}

/**
 * PlanApi - object-oriented interface
 * @export
 * @class PlanApi
 * @extends {BaseAPI}
 */
export class PlanApi extends BaseAPI {
    /**
     * Returns the Hanzo World enforcement limits for a plan, resolved from the @hanzo/plans catalog (the single source of truth) via the world.* entitlement vocabulary. Agents and the dashboard read this to self-config against the live catalog instead of hardcoding tier numbers. Defaults to world-free; degrades to the fail-closed Free floor on a catalog outage. 
     * @summary Resolved World plan limits (contract echo)
     * @param {PlanApiWorldWorldLimitsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof PlanApi
     */
    public worldWorldLimits(requestParameters: PlanApiWorldWorldLimitsRequest = {}, options?: RawAxiosRequestConfig) {
        return PlanApiFp(this.configuration).worldWorldLimits(requestParameters.plan, options).then((request) => request(this.axios, this.basePath));
    }
}

