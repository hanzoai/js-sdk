/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { NotifyHealth } from '../models';
/**
 * NotifyApi - axios parameter creator
 * @export
 */
export const NotifyApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Reports that the notify send surface is mounted.  It is a pure liveness probe: it answers 200 whenever this subsystem is mounted and checks nothing downstream, so an \"ok\" here says the routes are reachable, not that any provider credential is configured. The body is notifyd\'s verbatim, so probes and clients that keyed on the standalone service keep working unchanged.
         * @summary Reports that the notify send surface is mounted.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1NotifyHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/notify/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Delivers a message to each address in `to` over the channel the body names — sms or email — using the CALLER ORG\'S own provider credential, read from KMS at orgs/<org>/notify/<service>/<key> and never from the environment. The org is the validated principal\'s, never a client-supplied header, so a caller can only ever send as their own tenant; an unauthenticated caller gets 401. Naming no provider picks the one whose credentials are actually configured (Twilio, then Plivo for SMS; Twilio Email, then SMTP for email) and fails closed when none is. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional message by email or SMS through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1NotifySend: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/notify/send`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to email, OVERRIDING whatever the body names — so a body that says sms still goes out as mail. The provider is the org\'s own email credential from KMS (Twilio Email, then SMTP), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Subject is carried on the email channel only. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional email through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1NotifySendEmail: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/notify/send/email`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to sms, OVERRIDING whatever the body names — so a body that says email still goes out as a text message. The provider is the org\'s own SMS credential from KMS (Twilio, then Plivo), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional SMS through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1NotifySendSms: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/notify/send/sms`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * NotifyApi - functional programming interface
 * @export
 */
export const NotifyApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = NotifyApiAxiosParamCreator(configuration)
    return {
        /**
         * Reports that the notify send surface is mounted.  It is a pure liveness probe: it answers 200 whenever this subsystem is mounted and checks nothing downstream, so an \"ok\" here says the routes are reachable, not that any provider credential is configured. The body is notifyd\'s verbatim, so probes and clients that keyed on the standalone service keep working unchanged.
         * @summary Reports that the notify send surface is mounted.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1NotifyHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<NotifyHealth>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1NotifyHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NotifyApi.getV1NotifyHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Delivers a message to each address in `to` over the channel the body names — sms or email — using the CALLER ORG\'S own provider credential, read from KMS at orgs/<org>/notify/<service>/<key> and never from the environment. The org is the validated principal\'s, never a client-supplied header, so a caller can only ever send as their own tenant; an unauthenticated caller gets 401. Naming no provider picks the one whose credentials are actually configured (Twilio, then Plivo for SMS; Twilio Email, then SMTP for email) and fails closed when none is. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional message by email or SMS through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1NotifySend(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1NotifySend(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NotifyApi.postV1NotifySend']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to email, OVERRIDING whatever the body names — so a body that says sms still goes out as mail. The provider is the org\'s own email credential from KMS (Twilio Email, then SMTP), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Subject is carried on the email channel only. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional email through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1NotifySendEmail(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1NotifySendEmail(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NotifyApi.postV1NotifySendEmail']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to sms, OVERRIDING whatever the body names — so a body that says email still goes out as a text message. The provider is the org\'s own SMS credential from KMS (Twilio, then Plivo), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional SMS through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1NotifySendSms(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1NotifySendSms(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NotifyApi.postV1NotifySendSms']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * NotifyApi - factory interface
 * @export
 */
export const NotifyApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = NotifyApiFp(configuration)
    return {
        /**
         * Reports that the notify send surface is mounted.  It is a pure liveness probe: it answers 200 whenever this subsystem is mounted and checks nothing downstream, so an \"ok\" here says the routes are reachable, not that any provider credential is configured. The body is notifyd\'s verbatim, so probes and clients that keyed on the standalone service keep working unchanged.
         * @summary Reports that the notify send surface is mounted.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1NotifyHealth(options?: RawAxiosRequestConfig): AxiosPromise<NotifyHealth> {
            return localVarFp.getV1NotifyHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Delivers a message to each address in `to` over the channel the body names — sms or email — using the CALLER ORG\'S own provider credential, read from KMS at orgs/<org>/notify/<service>/<key> and never from the environment. The org is the validated principal\'s, never a client-supplied header, so a caller can only ever send as their own tenant; an unauthenticated caller gets 401. Naming no provider picks the one whose credentials are actually configured (Twilio, then Plivo for SMS; Twilio Email, then SMTP for email) and fails closed when none is. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional message by email or SMS through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1NotifySend(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1NotifySend(options).then((request) => request(axios, basePath));
        },
        /**
         * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to email, OVERRIDING whatever the body names — so a body that says sms still goes out as mail. The provider is the org\'s own email credential from KMS (Twilio Email, then SMTP), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Subject is carried on the email channel only. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional email through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1NotifySendEmail(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1NotifySendEmail(options).then((request) => request(axios, basePath));
        },
        /**
         * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to sms, OVERRIDING whatever the body names — so a body that says email still goes out as a text message. The provider is the org\'s own SMS credential from KMS (Twilio, then Plivo), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
         * @summary Send one transactional SMS through your org\'s own provider credential
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1NotifySendSms(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1NotifySendSms(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * NotifyApi - object-oriented interface
 * @export
 * @class NotifyApi
 * @extends {BaseAPI}
 */
export class NotifyApi extends BaseAPI {
    /**
     * Reports that the notify send surface is mounted.  It is a pure liveness probe: it answers 200 whenever this subsystem is mounted and checks nothing downstream, so an \"ok\" here says the routes are reachable, not that any provider credential is configured. The body is notifyd\'s verbatim, so probes and clients that keyed on the standalone service keep working unchanged.
     * @summary Reports that the notify send surface is mounted.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NotifyApi
     */
    public getV1NotifyHealth(options?: RawAxiosRequestConfig) {
        return NotifyApiFp(this.configuration).getV1NotifyHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Delivers a message to each address in `to` over the channel the body names — sms or email — using the CALLER ORG\'S own provider credential, read from KMS at orgs/<org>/notify/<service>/<key> and never from the environment. The org is the validated principal\'s, never a client-supplied header, so a caller can only ever send as their own tenant; an unauthenticated caller gets 401. Naming no provider picks the one whose credentials are actually configured (Twilio, then Plivo for SMS; Twilio Email, then SMTP for email) and fails closed when none is. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
     * @summary Send one transactional message by email or SMS through your org\'s own provider credential
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NotifyApi
     */
    public postV1NotifySend(options?: RawAxiosRequestConfig) {
        return NotifyApiFp(this.configuration).postV1NotifySend(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to email, OVERRIDING whatever the body names — so a body that says sms still goes out as mail. The provider is the org\'s own email credential from KMS (Twilio Email, then SMTP), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Subject is carried on the email channel only. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
     * @summary Send one transactional email through your org\'s own provider credential
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NotifyApi
     */
    public postV1NotifySendEmail(options?: RawAxiosRequestConfig) {
        return NotifyApiFp(this.configuration).postV1NotifySendEmail(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The channel-pinned form of the generic send: identical in every respect except that the channel is fixed to sms, OVERRIDING whatever the body names — so a body that says email still goes out as a text message. The provider is the org\'s own SMS credential from KMS (Twilio, then Plivo), resolved for the validated principal\'s org and never from a client-supplied header; an unauthenticated caller gets 401. Delivery is synchronous and per recipient: one recipient answers the bare {messageId,status} SendResponse, several answer the {items:[…]} envelope. A terminal provider failure is a 200 whose status is failed with the reason in error, never a transport error. ?sync=true is REQUIRED — an async dispatch answers 503, because the queue plane that would run it is owned elsewhere and is not folded in here. The message body wins verbatim when present; otherwise template_id (or the event name) selects a built-in template rendered against template_vars.
     * @summary Send one transactional SMS through your org\'s own provider credential
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NotifyApi
     */
    public postV1NotifySendSms(options?: RawAxiosRequestConfig) {
        return NotifyApiFp(this.configuration).postV1NotifySendSms(options).then((request) => request(this.axios, this.basePath));
    }
}

