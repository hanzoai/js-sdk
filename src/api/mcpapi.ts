/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { AutomationsMcpRequest } from '../models';
// @ts-ignore
import type { AutomationsMcpResponse } from '../models';
// @ts-ignore
import type { CloudAdminAdminCreatePromo400Response } from '../models';
// @ts-ignore
import type { CloudCreateServerReq } from '../models';
// @ts-ignore
import type { CloudMCPServer } from '../models';
// @ts-ignore
import type { CloudMcpServerList } from '../models';
// @ts-ignore
import type { McpRequest } from '../models';
// @ts-ignore
import type { McpResponse } from '../models';
/**
 * MCPApi - axios parameter creator
 * @export
 */
export const MCPApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * HIP-0300 JSON-RPC 2.0. Methods: `initialize`, `ping`, `tools/list`, `tools/call`. Every connector action is a tool named `<connector>_<action>`. JSON-RPC errors are returned with HTTP 200 and an `error` member. 
         * @summary JSON-RPC 2.0 tool surface over connector actions
         * @param {AutomationsMcpRequest} automationsMcpRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        automationsMcp: async (automationsMcpRequest: AutomationsMcpRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'automationsMcpRequest' is not null or undefined
            assertParamExists('automationsMcp', 'automationsMcpRequest', automationsMcpRequest)
            const localVarPath = `/v1/automations/mcp`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(automationsMcpRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry. Scoped to the caller\'s org, so an id belonging to another tenant is a 404 and not a delete. Answers 204 with no body; a server this org does not have is 404.
         * @summary DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry.
         * @param {string} id ID is the server to deregister, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1McpServersId: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudDeleteV1McpServersId', 'id', id)
            const localVarPath = `/v1/mcp/servers/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * ListServers lists the external MCP servers the caller\'s org has registered. Each record carries the URL and the name of the header its credential is injected into; the credential VALUE lives only in KMS and is never returned, so hasSecret is the whole of what this surface says about it.
         * @summary ListServers lists the external MCP servers the caller\'s org has registered.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1McpServers: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/mcp/servers`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door. It is the ONE way an org gains a server, whether it typed the URL in or enabled a catalog listing: both write the SAME record, and `source` says which it was. A second registration path would be a second place for a server to exist, and then a second place to forget to check the credential.  The credential VALUE is sealed in KMS under a per-org ref; the row keeps only the URL, the header name to inject it into, and a has-secret flag — so a secret with no KMS configured is refused 503 rather than stored in the clear. The URL is SSRF-validated here and re-checked by the dialer at connect time, which is the DNS-rebinding defense.  Enabling a listing the org already enabled REVISES that server rather than adding a near-duplicate beside it, so a retried enable is the same one server. Answers 201 with the stored record.
         * @summary CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door.
         * @param {CloudCreateServerReq} cloudCreateServerReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1McpServers: async (cloudCreateServerReq: CloudCreateServerReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudCreateServerReq' is not null or undefined
            assertParamExists('cloudPostV1McpServers', 'cloudCreateServerReq', cloudCreateServerReq)
            const localVarPath = `/v1/mcp/servers`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudCreateServerReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * One request, one response, three methods. `initialize` reports the protocol version and server capabilities; `tools/list` returns every tool this credential can reach; `tools/call` runs one by name.  Read `error` before `result`, and `result.isError` before `result.content`: both failure channels live inside a 200.  `id` is declared a string. The wire accepts a number too — JSON-RPC 2.0 allows either and this door echoes back the type it was given — but a scalar `oneOf` generates an empty carrier class in every typed language, so the document declares the one form all of them can send.
         * @summary JSON-RPC call
         * @param {McpRequest} mcpRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mcpRpc: async (mcpRequest: McpRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'mcpRequest' is not null or undefined
            assertParamExists('mcpRpc', 'mcpRequest', mcpRequest)
            const localVarPath = `/v1/mcp`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(mcpRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * MCPApi - functional programming interface
 * @export
 */
export const MCPApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = MCPApiAxiosParamCreator(configuration)
    return {
        /**
         * HIP-0300 JSON-RPC 2.0. Methods: `initialize`, `ping`, `tools/list`, `tools/call`. Every connector action is a tool named `<connector>_<action>`. JSON-RPC errors are returned with HTTP 200 and an `error` member. 
         * @summary JSON-RPC 2.0 tool surface over connector actions
         * @param {AutomationsMcpRequest} automationsMcpRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async automationsMcp(automationsMcpRequest: AutomationsMcpRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AutomationsMcpResponse>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.automationsMcp(automationsMcpRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MCPApi.automationsMcp']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry. Scoped to the caller\'s org, so an id belonging to another tenant is a 404 and not a delete. Answers 204 with no body; a server this org does not have is 404.
         * @summary DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry.
         * @param {string} id ID is the server to deregister, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1McpServersId(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1McpServersId(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MCPApi.cloudDeleteV1McpServersId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * ListServers lists the external MCP servers the caller\'s org has registered. Each record carries the URL and the name of the header its credential is injected into; the credential VALUE lives only in KMS and is never returned, so hasSecret is the whole of what this surface says about it.
         * @summary ListServers lists the external MCP servers the caller\'s org has registered.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1McpServers(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMcpServerList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1McpServers(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MCPApi.cloudGetV1McpServers']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door. It is the ONE way an org gains a server, whether it typed the URL in or enabled a catalog listing: both write the SAME record, and `source` says which it was. A second registration path would be a second place for a server to exist, and then a second place to forget to check the credential.  The credential VALUE is sealed in KMS under a per-org ref; the row keeps only the URL, the header name to inject it into, and a has-secret flag — so a secret with no KMS configured is refused 503 rather than stored in the clear. The URL is SSRF-validated here and re-checked by the dialer at connect time, which is the DNS-rebinding defense.  Enabling a listing the org already enabled REVISES that server rather than adding a near-duplicate beside it, so a retried enable is the same one server. Answers 201 with the stored record.
         * @summary CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door.
         * @param {CloudCreateServerReq} cloudCreateServerReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1McpServers(cloudCreateServerReq: CloudCreateServerReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMCPServer>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1McpServers(cloudCreateServerReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MCPApi.cloudPostV1McpServers']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * One request, one response, three methods. `initialize` reports the protocol version and server capabilities; `tools/list` returns every tool this credential can reach; `tools/call` runs one by name.  Read `error` before `result`, and `result.isError` before `result.content`: both failure channels live inside a 200.  `id` is declared a string. The wire accepts a number too — JSON-RPC 2.0 allows either and this door echoes back the type it was given — but a scalar `oneOf` generates an empty carrier class in every typed language, so the document declares the one form all of them can send.
         * @summary JSON-RPC call
         * @param {McpRequest} mcpRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mcpRpc(mcpRequest: McpRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<McpResponse>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mcpRpc(mcpRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MCPApi.mcpRpc']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * MCPApi - factory interface
 * @export
 */
export const MCPApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = MCPApiFp(configuration)
    return {
        /**
         * HIP-0300 JSON-RPC 2.0. Methods: `initialize`, `ping`, `tools/list`, `tools/call`. Every connector action is a tool named `<connector>_<action>`. JSON-RPC errors are returned with HTTP 200 and an `error` member. 
         * @summary JSON-RPC 2.0 tool surface over connector actions
         * @param {MCPApiAutomationsMcpRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        automationsMcp(requestParameters: MCPApiAutomationsMcpRequest, options?: RawAxiosRequestConfig): AxiosPromise<AutomationsMcpResponse> {
            return localVarFp.automationsMcp(requestParameters.automationsMcpRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry. Scoped to the caller\'s org, so an id belonging to another tenant is a 404 and not a delete. Answers 204 with no body; a server this org does not have is 404.
         * @summary DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry.
         * @param {MCPApiCloudDeleteV1McpServersIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1McpServersId(requestParameters: MCPApiCloudDeleteV1McpServersIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudDeleteV1McpServersId(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * ListServers lists the external MCP servers the caller\'s org has registered. Each record carries the URL and the name of the header its credential is injected into; the credential VALUE lives only in KMS and is never returned, so hasSecret is the whole of what this surface says about it.
         * @summary ListServers lists the external MCP servers the caller\'s org has registered.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1McpServers(options?: RawAxiosRequestConfig): AxiosPromise<CloudMcpServerList> {
            return localVarFp.cloudGetV1McpServers(options).then((request) => request(axios, basePath));
        },
        /**
         * CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door. It is the ONE way an org gains a server, whether it typed the URL in or enabled a catalog listing: both write the SAME record, and `source` says which it was. A second registration path would be a second place for a server to exist, and then a second place to forget to check the credential.  The credential VALUE is sealed in KMS under a per-org ref; the row keeps only the URL, the header name to inject it into, and a has-secret flag — so a secret with no KMS configured is refused 503 rather than stored in the clear. The URL is SSRF-validated here and re-checked by the dialer at connect time, which is the DNS-rebinding defense.  Enabling a listing the org already enabled REVISES that server rather than adding a near-duplicate beside it, so a retried enable is the same one server. Answers 201 with the stored record.
         * @summary CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door.
         * @param {MCPApiCloudPostV1McpServersRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1McpServers(requestParameters: MCPApiCloudPostV1McpServersRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudMCPServer> {
            return localVarFp.cloudPostV1McpServers(requestParameters.cloudCreateServerReq, options).then((request) => request(axios, basePath));
        },
        /**
         * One request, one response, three methods. `initialize` reports the protocol version and server capabilities; `tools/list` returns every tool this credential can reach; `tools/call` runs one by name.  Read `error` before `result`, and `result.isError` before `result.content`: both failure channels live inside a 200.  `id` is declared a string. The wire accepts a number too — JSON-RPC 2.0 allows either and this door echoes back the type it was given — but a scalar `oneOf` generates an empty carrier class in every typed language, so the document declares the one form all of them can send.
         * @summary JSON-RPC call
         * @param {MCPApiMcpRpcRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mcpRpc(requestParameters: MCPApiMcpRpcRequest, options?: RawAxiosRequestConfig): AxiosPromise<McpResponse> {
            return localVarFp.mcpRpc(requestParameters.mcpRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for automationsMcp operation in MCPApi.
 * @export
 * @interface MCPApiAutomationsMcpRequest
 */
export interface MCPApiAutomationsMcpRequest {
    /**
     * 
     * @type {AutomationsMcpRequest}
     * @memberof MCPApiAutomationsMcp
     */
    readonly automationsMcpRequest: AutomationsMcpRequest
}

/**
 * Request parameters for cloudDeleteV1McpServersId operation in MCPApi.
 * @export
 * @interface MCPApiCloudDeleteV1McpServersIdRequest
 */
export interface MCPApiCloudDeleteV1McpServersIdRequest {
    /**
     * ID is the server to deregister, from the path.
     * @type {string}
     * @memberof MCPApiCloudDeleteV1McpServersId
     */
    readonly id: string
}

/**
 * Request parameters for cloudPostV1McpServers operation in MCPApi.
 * @export
 * @interface MCPApiCloudPostV1McpServersRequest
 */
export interface MCPApiCloudPostV1McpServersRequest {
    /**
     * 
     * @type {CloudCreateServerReq}
     * @memberof MCPApiCloudPostV1McpServers
     */
    readonly cloudCreateServerReq: CloudCreateServerReq
}

/**
 * Request parameters for mcpRpc operation in MCPApi.
 * @export
 * @interface MCPApiMcpRpcRequest
 */
export interface MCPApiMcpRpcRequest {
    /**
     * 
     * @type {McpRequest}
     * @memberof MCPApiMcpRpc
     */
    readonly mcpRequest: McpRequest
}

/**
 * MCPApi - object-oriented interface
 * @export
 * @class MCPApi
 * @extends {BaseAPI}
 */
export class MCPApi extends BaseAPI {
    /**
     * HIP-0300 JSON-RPC 2.0. Methods: `initialize`, `ping`, `tools/list`, `tools/call`. Every connector action is a tool named `<connector>_<action>`. JSON-RPC errors are returned with HTTP 200 and an `error` member. 
     * @summary JSON-RPC 2.0 tool surface over connector actions
     * @param {MCPApiAutomationsMcpRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MCPApi
     */
    public automationsMcp(requestParameters: MCPApiAutomationsMcpRequest, options?: RawAxiosRequestConfig) {
        return MCPApiFp(this.configuration).automationsMcp(requestParameters.automationsMcpRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry. Scoped to the caller\'s org, so an id belonging to another tenant is a 404 and not a delete. Answers 204 with no body; a server this org does not have is 404.
     * @summary DeleteServer deregisters one of the caller org\'s external MCP servers, so its tools leave the registry.
     * @param {MCPApiCloudDeleteV1McpServersIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MCPApi
     */
    public cloudDeleteV1McpServersId(requestParameters: MCPApiCloudDeleteV1McpServersIdRequest, options?: RawAxiosRequestConfig) {
        return MCPApiFp(this.configuration).cloudDeleteV1McpServersId(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * ListServers lists the external MCP servers the caller\'s org has registered. Each record carries the URL and the name of the header its credential is injected into; the credential VALUE lives only in KMS and is never returned, so hasSecret is the whole of what this surface says about it.
     * @summary ListServers lists the external MCP servers the caller\'s org has registered.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MCPApi
     */
    public cloudGetV1McpServers(options?: RawAxiosRequestConfig) {
        return MCPApiFp(this.configuration).cloudGetV1McpServers(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door. It is the ONE way an org gains a server, whether it typed the URL in or enabled a catalog listing: both write the SAME record, and `source` says which it was. A second registration path would be a second place for a server to exist, and then a second place to forget to check the credential.  The credential VALUE is sealed in KMS under a per-org ref; the row keeps only the URL, the header name to inject it into, and a has-secret flag — so a secret with no KMS configured is refused 503 rather than stored in the clear. The URL is SSRF-validated here and re-checked by the dialer at connect time, which is the DNS-rebinding defense.  Enabling a listing the org already enabled REVISES that server rather than adding a near-duplicate beside it, so a retried enable is the same one server. Answers 201 with the stored record.
     * @summary CreateServer gives the caller\'s org one more external MCP server, so its tools join the org\'s tool plane and the fleet\'s MCP door.
     * @param {MCPApiCloudPostV1McpServersRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MCPApi
     */
    public cloudPostV1McpServers(requestParameters: MCPApiCloudPostV1McpServersRequest, options?: RawAxiosRequestConfig) {
        return MCPApiFp(this.configuration).cloudPostV1McpServers(requestParameters.cloudCreateServerReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * One request, one response, three methods. `initialize` reports the protocol version and server capabilities; `tools/list` returns every tool this credential can reach; `tools/call` runs one by name.  Read `error` before `result`, and `result.isError` before `result.content`: both failure channels live inside a 200.  `id` is declared a string. The wire accepts a number too — JSON-RPC 2.0 allows either and this door echoes back the type it was given — but a scalar `oneOf` generates an empty carrier class in every typed language, so the document declares the one form all of them can send.
     * @summary JSON-RPC call
     * @param {MCPApiMcpRpcRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MCPApi
     */
    public mcpRpc(requestParameters: MCPApiMcpRpcRequest, options?: RawAxiosRequestConfig) {
        return MCPApiFp(this.configuration).mcpRpc(requestParameters.mcpRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

