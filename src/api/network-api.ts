/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { MeshServiceList } from '../models';
// @ts-ignore
import type { NetworkList } from '../models';
// @ts-ignore
import type { NetworkView } from '../models';
// @ts-ignore
import type { RouterList } from '../models';
/**
 * NetworkApi - axios parameter creator
 * @export
 */
export const NetworkApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the caller\'s org overlay network on the Zero Trust fabric.  The org has at most ONE overlay, projected from the edge-routers tagged with its \"org-<org>\" role attribute: nodes is the real router count and status is \"connected\" once at least one router has dialed home, \"provisioning\" while none has. An org with no routers gets an empty list, never a fabricated network.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list so the console\'s Networks page renders a clean empty state instead of an error.
         * @summary Returns the caller\'s org overlay network on the Zero Trust fabric.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetwork: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/network`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one overlay network by id, scoped to the caller\'s org.  The org has exactly one overlay network and its id is derived from the org, so any other id — another tenant\'s, or one that does not exist — is 404 rather than a peek across the tenant boundary. An org whose network exists but has no edge-routers is 404 too, for the same reason the list is empty: there is no overlay until something is on it.
         * @summary Returns one overlay network by id, scoped to the caller\'s org.
         * @param {string} id ID is the network id from the path. The URL is the addressing authority, so it binds from there whatever else the request carries.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetworkById: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getNetworkById', 'id', id)
            const localVarPath = `/v1/network/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the Zero Trust routers the caller\'s org owns.  One row per real ZT edge-router tagged with the org\'s \"org-<org>\" role attribute, carrying the controller\'s own health signal: \"online\" when connected, \"disabled\" when administratively disabled, \"offline\" otherwise. region is filled only from a \"region-<slug>\" role attribute and omitted when the router carries none, so the column renders \"—\" rather than a guess.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list.
         * @summary Returns the Zero Trust routers the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetworkRouters: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/network/routers`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and router reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
         * @summary Returns the Zero Trust edge services the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetworkServices: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/network/services`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * NetworkApi - functional programming interface
 * @export
 */
export const NetworkApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = NetworkApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the caller\'s org overlay network on the Zero Trust fabric.  The org has at most ONE overlay, projected from the edge-routers tagged with its \"org-<org>\" role attribute: nodes is the real router count and status is \"connected\" once at least one router has dialed home, \"provisioning\" while none has. An org with no routers gets an empty list, never a fabricated network.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list so the console\'s Networks page renders a clean empty state instead of an error.
         * @summary Returns the caller\'s org overlay network on the Zero Trust fabric.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getNetwork(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<NetworkList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getNetwork(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NetworkApi.getNetwork']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one overlay network by id, scoped to the caller\'s org.  The org has exactly one overlay network and its id is derived from the org, so any other id — another tenant\'s, or one that does not exist — is 404 rather than a peek across the tenant boundary. An org whose network exists but has no edge-routers is 404 too, for the same reason the list is empty: there is no overlay until something is on it.
         * @summary Returns one overlay network by id, scoped to the caller\'s org.
         * @param {string} id ID is the network id from the path. The URL is the addressing authority, so it binds from there whatever else the request carries.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getNetworkById(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<NetworkView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getNetworkById(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NetworkApi.getNetworkById']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the Zero Trust routers the caller\'s org owns.  One row per real ZT edge-router tagged with the org\'s \"org-<org>\" role attribute, carrying the controller\'s own health signal: \"online\" when connected, \"disabled\" when administratively disabled, \"offline\" otherwise. region is filled only from a \"region-<slug>\" role attribute and omitted when the router carries none, so the column renders \"—\" rather than a guess.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list.
         * @summary Returns the Zero Trust routers the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getNetworkRouters(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RouterList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getNetworkRouters(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NetworkApi.getNetworkRouters']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and router reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
         * @summary Returns the Zero Trust edge services the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getNetworkServices(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MeshServiceList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getNetworkServices(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['NetworkApi.getNetworkServices']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * NetworkApi - factory interface
 * @export
 */
export const NetworkApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = NetworkApiFp(configuration)
    return {
        /**
         * Returns the caller\'s org overlay network on the Zero Trust fabric.  The org has at most ONE overlay, projected from the edge-routers tagged with its \"org-<org>\" role attribute: nodes is the real router count and status is \"connected\" once at least one router has dialed home, \"provisioning\" while none has. An org with no routers gets an empty list, never a fabricated network.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list so the console\'s Networks page renders a clean empty state instead of an error.
         * @summary Returns the caller\'s org overlay network on the Zero Trust fabric.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetwork(options?: RawAxiosRequestConfig): AxiosPromise<NetworkList> {
            return localVarFp.getNetwork(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one overlay network by id, scoped to the caller\'s org.  The org has exactly one overlay network and its id is derived from the org, so any other id — another tenant\'s, or one that does not exist — is 404 rather than a peek across the tenant boundary. An org whose network exists but has no edge-routers is 404 too, for the same reason the list is empty: there is no overlay until something is on it.
         * @summary Returns one overlay network by id, scoped to the caller\'s org.
         * @param {NetworkApiGetNetworkByIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetworkById(requestParameters: NetworkApiGetNetworkByIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<NetworkView> {
            return localVarFp.getNetworkById(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the Zero Trust routers the caller\'s org owns.  One row per real ZT edge-router tagged with the org\'s \"org-<org>\" role attribute, carrying the controller\'s own health signal: \"online\" when connected, \"disabled\" when administratively disabled, \"offline\" otherwise. region is filled only from a \"region-<slug>\" role attribute and omitted when the router carries none, so the column renders \"—\" rather than a guess.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list.
         * @summary Returns the Zero Trust routers the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetworkRouters(options?: RawAxiosRequestConfig): AxiosPromise<RouterList> {
            return localVarFp.getNetworkRouters(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and router reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
         * @summary Returns the Zero Trust edge services the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getNetworkServices(options?: RawAxiosRequestConfig): AxiosPromise<MeshServiceList> {
            return localVarFp.getNetworkServices(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for getNetworkById operation in NetworkApi.
 * @export
 * @interface NetworkApiGetNetworkByIdRequest
 */
export interface NetworkApiGetNetworkByIdRequest {
    /**
     * ID is the network id from the path. The URL is the addressing authority, so it binds from there whatever else the request carries.
     * @type {string}
     * @memberof NetworkApiGetNetworkById
     */
    readonly id: string
}

/**
 * NetworkApi - object-oriented interface
 * @export
 * @class NetworkApi
 * @extends {BaseAPI}
 */
export class NetworkApi extends BaseAPI {
    /**
     * Returns the caller\'s org overlay network on the Zero Trust fabric.  The org has at most ONE overlay, projected from the edge-routers tagged with its \"org-<org>\" role attribute: nodes is the real router count and status is \"connected\" once at least one router has dialed home, \"provisioning\" while none has. An org with no routers gets an empty list, never a fabricated network.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list so the console\'s Networks page renders a clean empty state instead of an error.
     * @summary Returns the caller\'s org overlay network on the Zero Trust fabric.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NetworkApi
     */
    public getNetwork(options?: RawAxiosRequestConfig) {
        return NetworkApiFp(this.configuration).getNetwork(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one overlay network by id, scoped to the caller\'s org.  The org has exactly one overlay network and its id is derived from the org, so any other id — another tenant\'s, or one that does not exist — is 404 rather than a peek across the tenant boundary. An org whose network exists but has no edge-routers is 404 too, for the same reason the list is empty: there is no overlay until something is on it.
     * @summary Returns one overlay network by id, scoped to the caller\'s org.
     * @param {NetworkApiGetNetworkByIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NetworkApi
     */
    public getNetworkById(requestParameters: NetworkApiGetNetworkByIdRequest, options?: RawAxiosRequestConfig) {
        return NetworkApiFp(this.configuration).getNetworkById(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the Zero Trust routers the caller\'s org owns.  One row per real ZT edge-router tagged with the org\'s \"org-<org>\" role attribute, carrying the controller\'s own health signal: \"online\" when connected, \"disabled\" when administratively disabled, \"offline\" otherwise. region is filled only from a \"region-<slug>\" role attribute and omitted when the router carries none, so the column renders \"—\" rather than a guess.  The read degrades rather than erroring: a deployment with no ZT credential, and a controller that cannot be reached, both answer 200 with an empty list.
     * @summary Returns the Zero Trust routers the caller\'s org owns.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NetworkApi
     */
    public getNetworkRouters(options?: RawAxiosRequestConfig) {
        return NetworkApiFp(this.configuration).getNetworkRouters(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and router reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
     * @summary Returns the Zero Trust edge services the caller\'s org owns.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof NetworkApi
     */
    public getNetworkServices(options?: RawAxiosRequestConfig) {
        return NetworkApiFp(this.configuration).getNetworkServices(options).then((request) => request(this.axios, this.basePath));
    }
}

