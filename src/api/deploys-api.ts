/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { AuthorsDeployRecorded } from '../models';
// @ts-ignore
import type { AuthorsError } from '../models';
// @ts-ignore
import type { AuthorsRecordDeploy200Response } from '../models';
// @ts-ignore
import type { AuthorsRecordDeployRequest } from '../models';
/**
 * DeploysApi - axios parameter creator
 * @export
 */
export const DeploysApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Records a deploy-attribution edge for the caller (the DEPLOYING org). When `repoUrl` matches a verified author repo, the edge is recorded (idempotent per repo+project+org) and becomes eligible for royalty. A deploy of a repo that is not a verified author repo is NOT an error — it returns `recorded: false` so the deploy path can fire this unconditionally. A self-deploy is recorded (provenance) but excluded from accrual. 
         * @summary Record a deploy
         * @param {AuthorsRecordDeployRequest} authorsRecordDeployRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        authorsRecordDeploy: async (authorsRecordDeployRequest: AuthorsRecordDeployRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'authorsRecordDeployRequest' is not null or undefined
            assertParamExists('authorsRecordDeploy', 'authorsRecordDeployRequest', authorsRecordDeployRequest)
            const localVarPath = `/v1/authors/deploys/record`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(authorsRecordDeployRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * DeploysApi - functional programming interface
 * @export
 */
export const DeploysApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = DeploysApiAxiosParamCreator(configuration)
    return {
        /**
         * Records a deploy-attribution edge for the caller (the DEPLOYING org). When `repoUrl` matches a verified author repo, the edge is recorded (idempotent per repo+project+org) and becomes eligible for royalty. A deploy of a repo that is not a verified author repo is NOT an error — it returns `recorded: false` so the deploy path can fire this unconditionally. A self-deploy is recorded (provenance) but excluded from accrual. 
         * @summary Record a deploy
         * @param {AuthorsRecordDeployRequest} authorsRecordDeployRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async authorsRecordDeploy(authorsRecordDeployRequest: AuthorsRecordDeployRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AuthorsRecordDeploy200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.authorsRecordDeploy(authorsRecordDeployRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DeploysApi.authorsRecordDeploy']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * DeploysApi - factory interface
 * @export
 */
export const DeploysApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = DeploysApiFp(configuration)
    return {
        /**
         * Records a deploy-attribution edge for the caller (the DEPLOYING org). When `repoUrl` matches a verified author repo, the edge is recorded (idempotent per repo+project+org) and becomes eligible for royalty. A deploy of a repo that is not a verified author repo is NOT an error — it returns `recorded: false` so the deploy path can fire this unconditionally. A self-deploy is recorded (provenance) but excluded from accrual. 
         * @summary Record a deploy
         * @param {DeploysApiAuthorsRecordDeployRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        authorsRecordDeploy(requestParameters: DeploysApiAuthorsRecordDeployRequest, options?: RawAxiosRequestConfig): AxiosPromise<AuthorsRecordDeploy200Response> {
            return localVarFp.authorsRecordDeploy(requestParameters.authorsRecordDeployRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for authorsRecordDeploy operation in DeploysApi.
 * @export
 * @interface DeploysApiAuthorsRecordDeployRequest
 */
export interface DeploysApiAuthorsRecordDeployRequest {
    /**
     * 
     * @type {AuthorsRecordDeployRequest}
     * @memberof DeploysApiAuthorsRecordDeploy
     */
    readonly authorsRecordDeployRequest: AuthorsRecordDeployRequest
}

/**
 * DeploysApi - object-oriented interface
 * @export
 * @class DeploysApi
 * @extends {BaseAPI}
 */
export class DeploysApi extends BaseAPI {
    /**
     * Records a deploy-attribution edge for the caller (the DEPLOYING org). When `repoUrl` matches a verified author repo, the edge is recorded (idempotent per repo+project+org) and becomes eligible for royalty. A deploy of a repo that is not a verified author repo is NOT an error — it returns `recorded: false` so the deploy path can fire this unconditionally. A self-deploy is recorded (provenance) but excluded from accrual. 
     * @summary Record a deploy
     * @param {DeploysApiAuthorsRecordDeployRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DeploysApi
     */
    public authorsRecordDeploy(requestParameters: DeploysApiAuthorsRecordDeployRequest, options?: RawAxiosRequestConfig) {
        return DeploysApiFp(this.configuration).authorsRecordDeploy(requestParameters.authorsRecordDeployRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

