/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { DriveIn } from '../models';
// @ts-ignore
import type { DriveItem } from '../models';
// @ts-ignore
import type { DriveList } from '../models';
// @ts-ignore
import type { FileList } from '../models';
// @ts-ignore
import type { SpaceHealth } from '../models';
// @ts-ignore
import type { SpaceIn } from '../models';
// @ts-ignore
import type { SpaceItem } from '../models';
// @ts-ignore
import type { SpaceList } from '../models';
/**
 * SpaceApi - axios parameter creator
 * @export
 */
export const SpaceApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Removes an EMPTY drive and answers 204.  A drive holding files is 409 rather than a cascade: deleting an org\'s files behind a single drive call is not a thing this surface will do silently. A drive or space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the drive is gone.
         * @summary Removes an EMPTY drive and answers 204.
         * @param {string} space Space is the space\&#39;s name, from the path.
         * @param {string} drive Drive is the drive\&#39;s name, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteSpaceBySpaceDrivesByDrive: async (space: string, drive: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'space' is not null or undefined
            assertParamExists('deleteSpaceBySpaceDrivesByDrive', 'space', space)
            // verify required parameter 'drive' is not null or undefined
            assertParamExists('deleteSpaceBySpaceDrivesByDrive', 'drive', drive)
            const localVarPath = `/v1/space/{space}/drives/{drive}`
                .replace(`{${"space"}}`, encodeURIComponent(String(space)))
                .replace(`{${"drive"}}`, encodeURIComponent(String(drive)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists a space\'s drives.  Listing the drives IS listing the space\'s root folder, because a drive is the first segment of a key and nothing else — so the two can never disagree the way a drives table and the keys under it would. A space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
         * @summary Lists a space\'s drives.
         * @param {string} space Space is the space\&#39;s name, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceBySpaceDrives: async (space: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'space' is not null or undefined
            assertParamExists('getSpaceBySpaceDrives', 'space', space)
            const localVarPath = `/v1/space/{space}/drives`
                .replace(`{${"space"}}`, encodeURIComponent(String(space)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists one folder level of a drive.  Folder-style by default: sub-folders come back as folder entries, which is the file-manager view. `?recursive=true` lists every file flat under the folder instead. Names are RELATIVE to `?folder=`, and the listing is bounded so a huge drive cannot exhaust memory — Total is what came back, not what the drive holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
         * @summary Lists one folder level of a drive.
         * @param {string} space Space is the space to list in, from the path.
         * @param {string} drive Drive is the drive to list, from the path.
         * @param {string} [folder] 
         * @param {string} [recursive] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceBySpaceDrivesByDriveFiles: async (space: string, drive: string, folder?: string, recursive?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'space' is not null or undefined
            assertParamExists('getSpaceBySpaceDrivesByDriveFiles', 'space', space)
            // verify required parameter 'drive' is not null or undefined
            assertParamExists('getSpaceBySpaceDrivesByDriveFiles', 'drive', drive)
            const localVarPath = `/v1/space/{space}/drives/{drive}/files`
                .replace(`{${"space"}}`, encodeURIComponent(String(space)))
                .replace(`{${"drive"}}`, encodeURIComponent(String(drive)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (folder !== undefined) {
                localVarQueryParameter['folder'] = folder;
            }

            if (recursive !== undefined) {
                localVarQueryParameter['recursive'] = recursive;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Health reports whether this deployment can serve spaces, drives and files.  It is a REAL probe rather than a constant: 200 when object-store credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no space and bills nothing.
         * @summary Health reports whether this deployment can serve spaces, drives and files.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/space/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the caller org\'s own spaces.  Only the caller\'s: every space is physically named under a per-org prefix and the listing strips that prefix, so another org\'s spaces are not in the answer at all. Another org\'s space is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
         * @summary Lists the caller org\'s own spaces.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceSpaces: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/space/spaces`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Makes a new drive in a space and answers 201 with it.  A drive is a PREFIX and not a bucket, so making one writes a zero-byte marker at \"<name>/\" — which is what makes an empty drive visible to a listing that has no other key to find. A name already taken in the space is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the drive exists.
         * @summary Makes a new drive in a space and answers 201 with it.
         * @param {string} space Space is the space to create the drive in, from the path. It carries NO &#x60;url:\&quot;-\&quot;&#x60;, unlike the field below it, and the difference is the whole reason both tags are written out: zip\&#39;s binder skips a field tagged \&quot;-\&quot; for EVERY URL source, path params included, so a path-borne value that carried it would arrive empty and the create would refuse a perfectly good address.
         * @param {DriveIn} driveIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSpaceBySpaceDrives: async (space: string, driveIn: DriveIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'space' is not null or undefined
            assertParamExists('postSpaceBySpaceDrives', 'space', space)
            // verify required parameter 'driveIn' is not null or undefined
            assertParamExists('postSpaceBySpaceDrives', 'driveIn', driveIn)
            const localVarPath = `/v1/space/{space}/drives`
                .replace(`{${"space"}}`, encodeURIComponent(String(space)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(driveIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Makes a new space for the caller\'s org and answers 201 with it.  The one bucket a space\'s files live in is derived from the caller\'s VALIDATED org, so an org can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the space exists.
         * @summary Makes a new space for the caller\'s org and answers 201 with it.
         * @param {SpaceIn} spaceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSpaceSpaces: async (spaceIn: SpaceIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'spaceIn' is not null or undefined
            assertParamExists('postSpaceSpaces', 'spaceIn', spaceIn)
            const localVarPath = `/v1/space/spaces`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(spaceIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * SpaceApi - functional programming interface
 * @export
 */
export const SpaceApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = SpaceApiAxiosParamCreator(configuration)
    return {
        /**
         * Removes an EMPTY drive and answers 204.  A drive holding files is 409 rather than a cascade: deleting an org\'s files behind a single drive call is not a thing this surface will do silently. A drive or space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the drive is gone.
         * @summary Removes an EMPTY drive and answers 204.
         * @param {string} space Space is the space\&#39;s name, from the path.
         * @param {string} drive Drive is the drive\&#39;s name, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteSpaceBySpaceDrivesByDrive(space: string, drive: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteSpaceBySpaceDrivesByDrive(space, drive, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SpaceApi.deleteSpaceBySpaceDrivesByDrive']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists a space\'s drives.  Listing the drives IS listing the space\'s root folder, because a drive is the first segment of a key and nothing else — so the two can never disagree the way a drives table and the keys under it would. A space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
         * @summary Lists a space\'s drives.
         * @param {string} space Space is the space\&#39;s name, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSpaceBySpaceDrives(space: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<DriveList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSpaceBySpaceDrives(space, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SpaceApi.getSpaceBySpaceDrives']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists one folder level of a drive.  Folder-style by default: sub-folders come back as folder entries, which is the file-manager view. `?recursive=true` lists every file flat under the folder instead. Names are RELATIVE to `?folder=`, and the listing is bounded so a huge drive cannot exhaust memory — Total is what came back, not what the drive holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
         * @summary Lists one folder level of a drive.
         * @param {string} space Space is the space to list in, from the path.
         * @param {string} drive Drive is the drive to list, from the path.
         * @param {string} [folder] 
         * @param {string} [recursive] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSpaceBySpaceDrivesByDriveFiles(space: string, drive: string, folder?: string, recursive?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<FileList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSpaceBySpaceDrivesByDriveFiles(space, drive, folder, recursive, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SpaceApi.getSpaceBySpaceDrivesByDriveFiles']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Health reports whether this deployment can serve spaces, drives and files.  It is a REAL probe rather than a constant: 200 when object-store credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no space and bills nothing.
         * @summary Health reports whether this deployment can serve spaces, drives and files.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSpaceHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SpaceHealth>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSpaceHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SpaceApi.getSpaceHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the caller org\'s own spaces.  Only the caller\'s: every space is physically named under a per-org prefix and the listing strips that prefix, so another org\'s spaces are not in the answer at all. Another org\'s space is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
         * @summary Lists the caller org\'s own spaces.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSpaceSpaces(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SpaceList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSpaceSpaces(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SpaceApi.getSpaceSpaces']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Makes a new drive in a space and answers 201 with it.  A drive is a PREFIX and not a bucket, so making one writes a zero-byte marker at \"<name>/\" — which is what makes an empty drive visible to a listing that has no other key to find. A name already taken in the space is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the drive exists.
         * @summary Makes a new drive in a space and answers 201 with it.
         * @param {string} space Space is the space to create the drive in, from the path. It carries NO &#x60;url:\&quot;-\&quot;&#x60;, unlike the field below it, and the difference is the whole reason both tags are written out: zip\&#39;s binder skips a field tagged \&quot;-\&quot; for EVERY URL source, path params included, so a path-borne value that carried it would arrive empty and the create would refuse a perfectly good address.
         * @param {DriveIn} driveIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postSpaceBySpaceDrives(space: string, driveIn: DriveIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<DriveItem>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postSpaceBySpaceDrives(space, driveIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SpaceApi.postSpaceBySpaceDrives']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Makes a new space for the caller\'s org and answers 201 with it.  The one bucket a space\'s files live in is derived from the caller\'s VALIDATED org, so an org can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the space exists.
         * @summary Makes a new space for the caller\'s org and answers 201 with it.
         * @param {SpaceIn} spaceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postSpaceSpaces(spaceIn: SpaceIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SpaceItem>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postSpaceSpaces(spaceIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SpaceApi.postSpaceSpaces']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * SpaceApi - factory interface
 * @export
 */
export const SpaceApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = SpaceApiFp(configuration)
    return {
        /**
         * Removes an EMPTY drive and answers 204.  A drive holding files is 409 rather than a cascade: deleting an org\'s files behind a single drive call is not a thing this surface will do silently. A drive or space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the drive is gone.
         * @summary Removes an EMPTY drive and answers 204.
         * @param {SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteSpaceBySpaceDrivesByDrive(requestParameters: SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteSpaceBySpaceDrivesByDrive(requestParameters.space, requestParameters.drive, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists a space\'s drives.  Listing the drives IS listing the space\'s root folder, because a drive is the first segment of a key and nothing else — so the two can never disagree the way a drives table and the keys under it would. A space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
         * @summary Lists a space\'s drives.
         * @param {SpaceApiGetSpaceBySpaceDrivesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceBySpaceDrives(requestParameters: SpaceApiGetSpaceBySpaceDrivesRequest, options?: RawAxiosRequestConfig): AxiosPromise<DriveList> {
            return localVarFp.getSpaceBySpaceDrives(requestParameters.space, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists one folder level of a drive.  Folder-style by default: sub-folders come back as folder entries, which is the file-manager view. `?recursive=true` lists every file flat under the folder instead. Names are RELATIVE to `?folder=`, and the listing is bounded so a huge drive cannot exhaust memory — Total is what came back, not what the drive holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
         * @summary Lists one folder level of a drive.
         * @param {SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceBySpaceDrivesByDriveFiles(requestParameters: SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest, options?: RawAxiosRequestConfig): AxiosPromise<FileList> {
            return localVarFp.getSpaceBySpaceDrivesByDriveFiles(requestParameters.space, requestParameters.drive, requestParameters.folder, requestParameters.recursive, options).then((request) => request(axios, basePath));
        },
        /**
         * Health reports whether this deployment can serve spaces, drives and files.  It is a REAL probe rather than a constant: 200 when object-store credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no space and bills nothing.
         * @summary Health reports whether this deployment can serve spaces, drives and files.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceHealth(options?: RawAxiosRequestConfig): AxiosPromise<SpaceHealth> {
            return localVarFp.getSpaceHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the caller org\'s own spaces.  Only the caller\'s: every space is physically named under a per-org prefix and the listing strips that prefix, so another org\'s spaces are not in the answer at all. Another org\'s space is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
         * @summary Lists the caller org\'s own spaces.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSpaceSpaces(options?: RawAxiosRequestConfig): AxiosPromise<SpaceList> {
            return localVarFp.getSpaceSpaces(options).then((request) => request(axios, basePath));
        },
        /**
         * Makes a new drive in a space and answers 201 with it.  A drive is a PREFIX and not a bucket, so making one writes a zero-byte marker at \"<name>/\" — which is what makes an empty drive visible to a listing that has no other key to find. A name already taken in the space is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the drive exists.
         * @summary Makes a new drive in a space and answers 201 with it.
         * @param {SpaceApiPostSpaceBySpaceDrivesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSpaceBySpaceDrives(requestParameters: SpaceApiPostSpaceBySpaceDrivesRequest, options?: RawAxiosRequestConfig): AxiosPromise<DriveItem> {
            return localVarFp.postSpaceBySpaceDrives(requestParameters.space, requestParameters.driveIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Makes a new space for the caller\'s org and answers 201 with it.  The one bucket a space\'s files live in is derived from the caller\'s VALIDATED org, so an org can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the space exists.
         * @summary Makes a new space for the caller\'s org and answers 201 with it.
         * @param {SpaceApiPostSpaceSpacesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSpaceSpaces(requestParameters: SpaceApiPostSpaceSpacesRequest, options?: RawAxiosRequestConfig): AxiosPromise<SpaceItem> {
            return localVarFp.postSpaceSpaces(requestParameters.spaceIn, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteSpaceBySpaceDrivesByDrive operation in SpaceApi.
 * @export
 * @interface SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest
 */
export interface SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest {
    /**
     * Space is the space\&#39;s name, from the path.
     * @type {string}
     * @memberof SpaceApiDeleteSpaceBySpaceDrivesByDrive
     */
    readonly space: string

    /**
     * Drive is the drive\&#39;s name, from the path.
     * @type {string}
     * @memberof SpaceApiDeleteSpaceBySpaceDrivesByDrive
     */
    readonly drive: string
}

/**
 * Request parameters for getSpaceBySpaceDrives operation in SpaceApi.
 * @export
 * @interface SpaceApiGetSpaceBySpaceDrivesRequest
 */
export interface SpaceApiGetSpaceBySpaceDrivesRequest {
    /**
     * Space is the space\&#39;s name, from the path.
     * @type {string}
     * @memberof SpaceApiGetSpaceBySpaceDrives
     */
    readonly space: string
}

/**
 * Request parameters for getSpaceBySpaceDrivesByDriveFiles operation in SpaceApi.
 * @export
 * @interface SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest
 */
export interface SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest {
    /**
     * Space is the space to list in, from the path.
     * @type {string}
     * @memberof SpaceApiGetSpaceBySpaceDrivesByDriveFiles
     */
    readonly space: string

    /**
     * Drive is the drive to list, from the path.
     * @type {string}
     * @memberof SpaceApiGetSpaceBySpaceDrivesByDriveFiles
     */
    readonly drive: string

    /**
     * 
     * @type {string}
     * @memberof SpaceApiGetSpaceBySpaceDrivesByDriveFiles
     */
    readonly folder?: string

    /**
     * 
     * @type {string}
     * @memberof SpaceApiGetSpaceBySpaceDrivesByDriveFiles
     */
    readonly recursive?: string
}

/**
 * Request parameters for postSpaceBySpaceDrives operation in SpaceApi.
 * @export
 * @interface SpaceApiPostSpaceBySpaceDrivesRequest
 */
export interface SpaceApiPostSpaceBySpaceDrivesRequest {
    /**
     * Space is the space to create the drive in, from the path. It carries NO &#x60;url:\&quot;-\&quot;&#x60;, unlike the field below it, and the difference is the whole reason both tags are written out: zip\&#39;s binder skips a field tagged \&quot;-\&quot; for EVERY URL source, path params included, so a path-borne value that carried it would arrive empty and the create would refuse a perfectly good address.
     * @type {string}
     * @memberof SpaceApiPostSpaceBySpaceDrives
     */
    readonly space: string

    /**
     * 
     * @type {DriveIn}
     * @memberof SpaceApiPostSpaceBySpaceDrives
     */
    readonly driveIn: DriveIn
}

/**
 * Request parameters for postSpaceSpaces operation in SpaceApi.
 * @export
 * @interface SpaceApiPostSpaceSpacesRequest
 */
export interface SpaceApiPostSpaceSpacesRequest {
    /**
     * 
     * @type {SpaceIn}
     * @memberof SpaceApiPostSpaceSpaces
     */
    readonly spaceIn: SpaceIn
}

/**
 * SpaceApi - object-oriented interface
 * @export
 * @class SpaceApi
 * @extends {BaseAPI}
 */
export class SpaceApi extends BaseAPI {
    /**
     * Removes an EMPTY drive and answers 204.  A drive holding files is 409 rather than a cascade: deleting an org\'s files behind a single drive call is not a thing this surface will do silently. A drive or space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing deleted, and the debit lands only once the drive is gone.
     * @summary Removes an EMPTY drive and answers 204.
     * @param {SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SpaceApi
     */
    public deleteSpaceBySpaceDrivesByDrive(requestParameters: SpaceApiDeleteSpaceBySpaceDrivesByDriveRequest, options?: RawAxiosRequestConfig) {
        return SpaceApiFp(this.configuration).deleteSpaceBySpaceDrivesByDrive(requestParameters.space, requestParameters.drive, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists a space\'s drives.  Listing the drives IS listing the space\'s root folder, because a drive is the first segment of a key and nothing else — so the two can never disagree the way a drives table and the keys under it would. A space the caller\'s org does not own is the same 404 an unknown name gives.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * @summary Lists a space\'s drives.
     * @param {SpaceApiGetSpaceBySpaceDrivesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SpaceApi
     */
    public getSpaceBySpaceDrives(requestParameters: SpaceApiGetSpaceBySpaceDrivesRequest, options?: RawAxiosRequestConfig) {
        return SpaceApiFp(this.configuration).getSpaceBySpaceDrives(requestParameters.space, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists one folder level of a drive.  Folder-style by default: sub-folders come back as folder entries, which is the file-manager view. `?recursive=true` lists every file flat under the folder instead. Names are RELATIVE to `?folder=`, and the listing is bounded so a huge drive cannot exhaust memory — Total is what came back, not what the drive holds.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing read, and the debit lands only once the listing has succeeded.
     * @summary Lists one folder level of a drive.
     * @param {SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SpaceApi
     */
    public getSpaceBySpaceDrivesByDriveFiles(requestParameters: SpaceApiGetSpaceBySpaceDrivesByDriveFilesRequest, options?: RawAxiosRequestConfig) {
        return SpaceApiFp(this.configuration).getSpaceBySpaceDrivesByDriveFiles(requestParameters.space, requestParameters.drive, requestParameters.folder, requestParameters.recursive, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Health reports whether this deployment can serve spaces, drives and files.  It is a REAL probe rather than a constant: 200 when object-store credentials are present, so the store is reachable in principle, and 503 with the reason when they are not. It is deliberately NOT gated — liveness has to be probe-able without a token — so it is the one operation here that names no space and bills nothing.
     * @summary Health reports whether this deployment can serve spaces, drives and files.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SpaceApi
     */
    public getSpaceHealth(options?: RawAxiosRequestConfig) {
        return SpaceApiFp(this.configuration).getSpaceHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the caller org\'s own spaces.  Only the caller\'s: every space is physically named under a per-org prefix and the listing strips that prefix, so another org\'s spaces are not in the answer at all. Another org\'s space is not refused but INVISIBLE, so this cannot be used to learn that a name is taken elsewhere.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit lands only once the work has succeeded.
     * @summary Lists the caller org\'s own spaces.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SpaceApi
     */
    public getSpaceSpaces(options?: RawAxiosRequestConfig) {
        return SpaceApiFp(this.configuration).getSpaceSpaces(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Makes a new drive in a space and answers 201 with it.  A drive is a PREFIX and not a bucket, so making one writes a zero-byte marker at \"<name>/\" — which is what makes an empty drive visible to a listing that has no other key to find. A name already taken in the space is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the drive exists.
     * @summary Makes a new drive in a space and answers 201 with it.
     * @param {SpaceApiPostSpaceBySpaceDrivesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SpaceApi
     */
    public postSpaceBySpaceDrives(requestParameters: SpaceApiPostSpaceBySpaceDrivesRequest, options?: RawAxiosRequestConfig) {
        return SpaceApiFp(this.configuration).postSpaceBySpaceDrives(requestParameters.space, requestParameters.driveIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Makes a new space for the caller\'s org and answers 201 with it.  The one bucket a space\'s files live in is derived from the caller\'s VALIDATED org, so an org can only ever create inside its own namespace and no request field can redirect that. A name already taken in the org is 409.  Billed per call: the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing created, and the debit lands only once the space exists.
     * @summary Makes a new space for the caller\'s org and answers 201 with it.
     * @param {SpaceApiPostSpaceSpacesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SpaceApi
     */
    public postSpaceSpaces(requestParameters: SpaceApiPostSpaceSpacesRequest, options?: RawAxiosRequestConfig) {
        return SpaceApiFp(this.configuration).postSpaceSpaces(requestParameters.spaceIn, options).then((request) => request(this.axios, this.basePath));
    }
}

