/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
/**
 * CollectionsApi - axios parameter creator
 * @export
 */
export const CollectionsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('deleteV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('getV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('patchV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('postV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('putV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * CollectionsApi - functional programming interface
 * @export
 */
export const CollectionsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = CollectionsApiAxiosParamCreator(configuration)
    return {
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.deleteV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.deleteV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.getV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.getV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.patchV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.patchV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.postV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.postV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async putV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.putV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.putV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async putV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.putV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.putV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * CollectionsApi - factory interface
 * @export
 */
export const CollectionsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = CollectionsApiFp(configuration)
    return {
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiDeleteV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1CollectionsByWildcard1(requestParameters: CollectionsApiDeleteV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiGetV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1CollectionsByWildcard1(requestParameters: CollectionsApiGetV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.patchV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiPatchV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1CollectionsByWildcard1(requestParameters: CollectionsApiPatchV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.patchV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiPostV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1CollectionsByWildcard1(requestParameters: CollectionsApiPostV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.putV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiPutV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putV1CollectionsByWildcard1(requestParameters: CollectionsApiPutV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.putV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiDeleteV1CollectionsByWildcard1Request
 */
export interface CollectionsApiDeleteV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiDeleteV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for getV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiGetV1CollectionsByWildcard1Request
 */
export interface CollectionsApiGetV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiGetV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for patchV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiPatchV1CollectionsByWildcard1Request
 */
export interface CollectionsApiPatchV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiPatchV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for postV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiPostV1CollectionsByWildcard1Request
 */
export interface CollectionsApiPostV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiPostV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for putV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiPutV1CollectionsByWildcard1Request
 */
export interface CollectionsApiPutV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiPutV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * CollectionsApi - object-oriented interface
 * @export
 * @class CollectionsApi
 * @extends {BaseAPI}
 */
export class CollectionsApi extends BaseAPI {
    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public deleteV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).deleteV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiDeleteV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public deleteV1CollectionsByWildcard1(requestParameters: CollectionsApiDeleteV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).deleteV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public getV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).getV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiGetV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public getV1CollectionsByWildcard1(requestParameters: CollectionsApiGetV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).getV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public patchV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).patchV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiPatchV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public patchV1CollectionsByWildcard1(requestParameters: CollectionsApiPatchV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).patchV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public postV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).postV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiPostV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public postV1CollectionsByWildcard1(requestParameters: CollectionsApiPostV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).postV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public putV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).putV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiPutV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public putV1CollectionsByWildcard1(requestParameters: CollectionsApiPutV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).putV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }
}

