/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { VectorCreateCollection200Response } from '../models';
// @ts-ignore
import type { VectorCreateCollectionRequest } from '../models';
// @ts-ignore
import type { VectorGetCollection200Response } from '../models';
/**
 * CollectionsApi - axios parameter creator
 * @export
 */
export const CollectionsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudDeleteV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudGetV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'OPTIONS', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudOptionsV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'OPTIONS', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudPatchV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudPostV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1Collections: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/collections`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1CollectionsByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudPutV1CollectionsByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/collections/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary Create collection
         * @param {string} collectionName 
         * @param {VectorCreateCollectionRequest} vectorCreateCollectionRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        vectorCreateCollection: async (collectionName: string, vectorCreateCollectionRequest: VectorCreateCollectionRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'collectionName' is not null or undefined
            assertParamExists('vectorCreateCollection', 'collectionName', collectionName)
            // verify required parameter 'vectorCreateCollectionRequest' is not null or undefined
            assertParamExists('vectorCreateCollection', 'vectorCreateCollectionRequest', vectorCreateCollectionRequest)
            const localVarPath = `/v1/vector/collections/{collection_name}`
                .replace(`{${"collection_name"}}`, encodeURIComponent(String(collectionName)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(vectorCreateCollectionRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary Delete collection
         * @param {string} collectionName 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        vectorDeleteCollection: async (collectionName: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'collectionName' is not null or undefined
            assertParamExists('vectorDeleteCollection', 'collectionName', collectionName)
            const localVarPath = `/v1/vector/collections/{collection_name}`
                .replace(`{${"collection_name"}}`, encodeURIComponent(String(collectionName)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary Get collection info
         * @param {string} collectionName 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        vectorGetCollection: async (collectionName: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'collectionName' is not null or undefined
            assertParamExists('vectorGetCollection', 'collectionName', collectionName)
            const localVarPath = `/v1/vector/collections/{collection_name}`
                .replace(`{${"collection_name"}}`, encodeURIComponent(String(collectionName)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * CollectionsApi - functional programming interface
 * @export
 */
export const CollectionsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = CollectionsApiAxiosParamCreator(configuration)
    return {
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudDeleteV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudDeleteV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudGetV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudGetV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudOptionsV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudOptionsV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudOptionsV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudOptionsV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudOptionsV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudOptionsV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPatchV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPatchV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudPatchV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPatchV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPatchV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudPatchV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudPostV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudPostV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1Collections(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1Collections(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudPutV1Collections']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1CollectionsByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1CollectionsByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.cloudPutV1CollectionsByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary Create collection
         * @param {string} collectionName 
         * @param {VectorCreateCollectionRequest} vectorCreateCollectionRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async vectorCreateCollection(collectionName: string, vectorCreateCollectionRequest: VectorCreateCollectionRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<VectorCreateCollection200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.vectorCreateCollection(collectionName, vectorCreateCollectionRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.vectorCreateCollection']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary Delete collection
         * @param {string} collectionName 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async vectorDeleteCollection(collectionName: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<VectorCreateCollection200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.vectorDeleteCollection(collectionName, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.vectorDeleteCollection']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary Get collection info
         * @param {string} collectionName 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async vectorGetCollection(collectionName: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<VectorGetCollection200Response>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.vectorGetCollection(collectionName, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['CollectionsApi.vectorGetCollection']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * CollectionsApi - factory interface
 * @export
 */
export const CollectionsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = CollectionsApiFp(configuration)
    return {
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudDeleteV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiCloudDeleteV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudDeleteV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudDeleteV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiCloudGetV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudGetV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudOptionsV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiCloudOptionsV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudOptionsV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudOptionsV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPatchV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiCloudPatchV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudPatchV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPatchV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiCloudPostV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudPostV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary The org\'s Base content types
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1Collections(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPutV1Collections(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
         * @summary One Base content type, and its records
         * @param {CollectionsApiCloudPutV1CollectionsByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudPutV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPutV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary Create collection
         * @param {CollectionsApiVectorCreateCollectionRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        vectorCreateCollection(requestParameters: CollectionsApiVectorCreateCollectionRequest, options?: RawAxiosRequestConfig): AxiosPromise<VectorCreateCollection200Response> {
            return localVarFp.vectorCreateCollection(requestParameters.collectionName, requestParameters.vectorCreateCollectionRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary Delete collection
         * @param {CollectionsApiVectorDeleteCollectionRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        vectorDeleteCollection(requestParameters: CollectionsApiVectorDeleteCollectionRequest, options?: RawAxiosRequestConfig): AxiosPromise<VectorCreateCollection200Response> {
            return localVarFp.vectorDeleteCollection(requestParameters.collectionName, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary Get collection info
         * @param {CollectionsApiVectorGetCollectionRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        vectorGetCollection(requestParameters: CollectionsApiVectorGetCollectionRequest, options?: RawAxiosRequestConfig): AxiosPromise<VectorGetCollection200Response> {
            return localVarFp.vectorGetCollection(requestParameters.collectionName, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudDeleteV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiCloudDeleteV1CollectionsByWildcard1Request
 */
export interface CollectionsApiCloudDeleteV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiCloudDeleteV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudGetV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiCloudGetV1CollectionsByWildcard1Request
 */
export interface CollectionsApiCloudGetV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiCloudGetV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudOptionsV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiCloudOptionsV1CollectionsByWildcard1Request
 */
export interface CollectionsApiCloudOptionsV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiCloudOptionsV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudPatchV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiCloudPatchV1CollectionsByWildcard1Request
 */
export interface CollectionsApiCloudPatchV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiCloudPatchV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudPostV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiCloudPostV1CollectionsByWildcard1Request
 */
export interface CollectionsApiCloudPostV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiCloudPostV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudPutV1CollectionsByWildcard1 operation in CollectionsApi.
 * @export
 * @interface CollectionsApiCloudPutV1CollectionsByWildcard1Request
 */
export interface CollectionsApiCloudPutV1CollectionsByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiCloudPutV1CollectionsByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for vectorCreateCollection operation in CollectionsApi.
 * @export
 * @interface CollectionsApiVectorCreateCollectionRequest
 */
export interface CollectionsApiVectorCreateCollectionRequest {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiVectorCreateCollection
     */
    readonly collectionName: string

    /**
     * 
     * @type {VectorCreateCollectionRequest}
     * @memberof CollectionsApiVectorCreateCollection
     */
    readonly vectorCreateCollectionRequest: VectorCreateCollectionRequest
}

/**
 * Request parameters for vectorDeleteCollection operation in CollectionsApi.
 * @export
 * @interface CollectionsApiVectorDeleteCollectionRequest
 */
export interface CollectionsApiVectorDeleteCollectionRequest {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiVectorDeleteCollection
     */
    readonly collectionName: string
}

/**
 * Request parameters for vectorGetCollection operation in CollectionsApi.
 * @export
 * @interface CollectionsApiVectorGetCollectionRequest
 */
export interface CollectionsApiVectorGetCollectionRequest {
    /**
     * 
     * @type {string}
     * @memberof CollectionsApiVectorGetCollection
     */
    readonly collectionName: string
}

/**
 * CollectionsApi - object-oriented interface
 * @export
 * @class CollectionsApi
 * @extends {BaseAPI}
 */
export class CollectionsApi extends BaseAPI {
    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudDeleteV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudDeleteV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiCloudDeleteV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudDeleteV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudDeleteV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudDeleteV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudGetV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudGetV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiCloudGetV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudGetV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudGetV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudGetV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudOptionsV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudOptionsV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiCloudOptionsV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudOptionsV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudOptionsV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudOptionsV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudPatchV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudPatchV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiCloudPatchV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudPatchV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudPatchV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudPatchV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudPostV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudPostV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiCloudPostV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudPostV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudPostV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudPostV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the content types in the org\'s managed Base, and creates one. This is what the console\'s Bases manager reads to render the schema.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary The org\'s Base content types
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudPutV1Collections(options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudPutV1Collections(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads and writes below the collections root: `meta/scaffolds` is the field-template palette a new content type is built from, `<name>` is one content type (view, update, delete), `<name>/records` is that type\'s rows (list, create) and `<name>/records/<id>` is one row (get, update, delete). This is the data plane behind the console\'s Records browser.  Any other shape below /v1/collections is refused with 404 before it is forwarded, so the wildcard admits exactly those five addresses and nothing more.  The path is forwarded to the managed Base unchanged and its answer comes back verbatim, so the schema, the records and every refusal are the managed Base\'s own.  AUTH is one credential, forwarded and never minted: cloud validates the caller\'s hanzo.id bearer and passes THAT SAME token on, because the managed Base scopes each row by the token\'s own subject. A caller with no validated principal is refused here, before the request leaves the process, and the org header that rides along is the one cloud validated — a client-forged org was stripped upstream.  This is a COLLECTIONS proxy, not a Base tunnel: only the collections data plane is admitted, and everything else the managed Base mounts — settings, backups, logs — is 404 here whatever the caller\'s rights on that deployment are.  One registration owns this address for every method, so which methods answer is the managed Base\'s decision, not this edge\'s.
     * @summary One Base content type, and its records
     * @param {CollectionsApiCloudPutV1CollectionsByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public cloudPutV1CollectionsByWildcard1(requestParameters: CollectionsApiCloudPutV1CollectionsByWildcard1Request, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).cloudPutV1CollectionsByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary Create collection
     * @param {CollectionsApiVectorCreateCollectionRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public vectorCreateCollection(requestParameters: CollectionsApiVectorCreateCollectionRequest, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).vectorCreateCollection(requestParameters.collectionName, requestParameters.vectorCreateCollectionRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary Delete collection
     * @param {CollectionsApiVectorDeleteCollectionRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public vectorDeleteCollection(requestParameters: CollectionsApiVectorDeleteCollectionRequest, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).vectorDeleteCollection(requestParameters.collectionName, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary Get collection info
     * @param {CollectionsApiVectorGetCollectionRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof CollectionsApi
     */
    public vectorGetCollection(requestParameters: CollectionsApiVectorGetCollectionRequest, options?: RawAxiosRequestConfig) {
        return CollectionsApiFp(this.configuration).vectorGetCollection(requestParameters.collectionName, options).then((request) => request(this.axios, this.basePath));
    }
}

