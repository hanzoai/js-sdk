/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { ProjectsBuildSiteRequest } from '../models';
// @ts-ignore
import type { ProjectsDeploySiteRequest } from '../models';
// @ts-ignore
import type { ProjectsError } from '../models';
// @ts-ignore
import type { ProjectsSite } from '../models';
// @ts-ignore
import type { ProjectsSiteDeployResult } from '../models';
/**
 * SitesApi - axios parameter creator
 * @export
 */
export const SitesApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Generate a self-contained, mobile-responsive static site from a natural-language `brief` (one inference call), then deploy it through the one publish core. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. The brief is capped at 8 KiB. Responds `200` with the published-site result. 
         * @summary Build a site from a brief and deploy it
         * @param {ProjectsBuildSiteRequest} projectsBuildSiteRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        projectsBuildSite: async (projectsBuildSiteRequest: ProjectsBuildSiteRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'projectsBuildSiteRequest' is not null or undefined
            assertParamExists('projectsBuildSite', 'projectsBuildSiteRequest', projectsBuildSiteRequest)
            const localVarPath = `/v1/sites`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(projectsBuildSiteRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Deploy a caller-supplied file manifest (the same `{path, content}` shape the brief build emits) through the one publish core. Every file passes the same guards as the brief build: index.html required at the root; absolute and traversal paths rejected; a mobile viewport tag injected into HTML when absent. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. Responds `200` with the published-site result. 
         * @summary Deploy a raw file manifest
         * @param {ProjectsDeploySiteRequest} projectsDeploySiteRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        projectsDeploySite: async (projectsDeploySiteRequest: ProjectsDeploySiteRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'projectsDeploySiteRequest' is not null or undefined
            assertParamExists('projectsDeploySite', 'projectsDeploySiteRequest', projectsDeploySiteRequest)
            const localVarPath = `/v1/sites/deploy`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(projectsDeploySiteRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * List the org\'s `live` projects as sites at their canonical live URL `https://<slug>.<apex>`. Reads the SAME org-scoped projects store as `/v1/projects`; a project that is not `live` is omitted. 
         * @summary List the org\'s live sites
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        projectsListSites: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/sites`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * SitesApi - functional programming interface
 * @export
 */
export const SitesApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = SitesApiAxiosParamCreator(configuration)
    return {
        /**
         * Generate a self-contained, mobile-responsive static site from a natural-language `brief` (one inference call), then deploy it through the one publish core. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. The brief is capped at 8 KiB. Responds `200` with the published-site result. 
         * @summary Build a site from a brief and deploy it
         * @param {ProjectsBuildSiteRequest} projectsBuildSiteRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async projectsBuildSite(projectsBuildSiteRequest: ProjectsBuildSiteRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ProjectsSiteDeployResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.projectsBuildSite(projectsBuildSiteRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SitesApi.projectsBuildSite']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Deploy a caller-supplied file manifest (the same `{path, content}` shape the brief build emits) through the one publish core. Every file passes the same guards as the brief build: index.html required at the root; absolute and traversal paths rejected; a mobile viewport tag injected into HTML when absent. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. Responds `200` with the published-site result. 
         * @summary Deploy a raw file manifest
         * @param {ProjectsDeploySiteRequest} projectsDeploySiteRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async projectsDeploySite(projectsDeploySiteRequest: ProjectsDeploySiteRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ProjectsSiteDeployResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.projectsDeploySite(projectsDeploySiteRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SitesApi.projectsDeploySite']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * List the org\'s `live` projects as sites at their canonical live URL `https://<slug>.<apex>`. Reads the SAME org-scoped projects store as `/v1/projects`; a project that is not `live` is omitted. 
         * @summary List the org\'s live sites
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async projectsListSites(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Array<ProjectsSite>>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.projectsListSites(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SitesApi.projectsListSites']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * SitesApi - factory interface
 * @export
 */
export const SitesApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = SitesApiFp(configuration)
    return {
        /**
         * Generate a self-contained, mobile-responsive static site from a natural-language `brief` (one inference call), then deploy it through the one publish core. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. The brief is capped at 8 KiB. Responds `200` with the published-site result. 
         * @summary Build a site from a brief and deploy it
         * @param {SitesApiProjectsBuildSiteRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        projectsBuildSite(requestParameters: SitesApiProjectsBuildSiteRequest, options?: RawAxiosRequestConfig): AxiosPromise<ProjectsSiteDeployResult> {
            return localVarFp.projectsBuildSite(requestParameters.projectsBuildSiteRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Deploy a caller-supplied file manifest (the same `{path, content}` shape the brief build emits) through the one publish core. Every file passes the same guards as the brief build: index.html required at the root; absolute and traversal paths rejected; a mobile viewport tag injected into HTML when absent. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. Responds `200` with the published-site result. 
         * @summary Deploy a raw file manifest
         * @param {SitesApiProjectsDeploySiteRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        projectsDeploySite(requestParameters: SitesApiProjectsDeploySiteRequest, options?: RawAxiosRequestConfig): AxiosPromise<ProjectsSiteDeployResult> {
            return localVarFp.projectsDeploySite(requestParameters.projectsDeploySiteRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * List the org\'s `live` projects as sites at their canonical live URL `https://<slug>.<apex>`. Reads the SAME org-scoped projects store as `/v1/projects`; a project that is not `live` is omitted. 
         * @summary List the org\'s live sites
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        projectsListSites(options?: RawAxiosRequestConfig): AxiosPromise<Array<ProjectsSite>> {
            return localVarFp.projectsListSites(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for projectsBuildSite operation in SitesApi.
 * @export
 * @interface SitesApiProjectsBuildSiteRequest
 */
export interface SitesApiProjectsBuildSiteRequest {
    /**
     * 
     * @type {ProjectsBuildSiteRequest}
     * @memberof SitesApiProjectsBuildSite
     */
    readonly projectsBuildSiteRequest: ProjectsBuildSiteRequest
}

/**
 * Request parameters for projectsDeploySite operation in SitesApi.
 * @export
 * @interface SitesApiProjectsDeploySiteRequest
 */
export interface SitesApiProjectsDeploySiteRequest {
    /**
     * 
     * @type {ProjectsDeploySiteRequest}
     * @memberof SitesApiProjectsDeploySite
     */
    readonly projectsDeploySiteRequest: ProjectsDeploySiteRequest
}

/**
 * SitesApi - object-oriented interface
 * @export
 * @class SitesApi
 * @extends {BaseAPI}
 */
export class SitesApi extends BaseAPI {
    /**
     * Generate a self-contained, mobile-responsive static site from a natural-language `brief` (one inference call), then deploy it through the one publish core. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. The brief is capped at 8 KiB. Responds `200` with the published-site result. 
     * @summary Build a site from a brief and deploy it
     * @param {SitesApiProjectsBuildSiteRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SitesApi
     */
    public projectsBuildSite(requestParameters: SitesApiProjectsBuildSiteRequest, options?: RawAxiosRequestConfig) {
        return SitesApiFp(this.configuration).projectsBuildSite(requestParameters.projectsBuildSiteRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Deploy a caller-supplied file manifest (the same `{path, content}` shape the brief build emits) through the one publish core. Every file passes the same guards as the brief build: index.html required at the root; absolute and traversal paths rejected; a mobile viewport tag injected into HTML when absent. Writes into the SAME org-scoped projects store as `/v1/projects`: it ensures a `project` (framework `static`) for the resolved slug and records a `deployment`. `/v1/sites` is the brief-build/raw-manifest entrypoint; `/v1/projects/{slug}/deploy` is the artifact/git entrypoint — one store, one publish core, not a second copy of project state. Responds `200` with the published-site result. 
     * @summary Deploy a raw file manifest
     * @param {SitesApiProjectsDeploySiteRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SitesApi
     */
    public projectsDeploySite(requestParameters: SitesApiProjectsDeploySiteRequest, options?: RawAxiosRequestConfig) {
        return SitesApiFp(this.configuration).projectsDeploySite(requestParameters.projectsDeploySiteRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * List the org\'s `live` projects as sites at their canonical live URL `https://<slug>.<apex>`. Reads the SAME org-scoped projects store as `/v1/projects`; a project that is not `live` is omitted. 
     * @summary List the org\'s live sites
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SitesApi
     */
    public projectsListSites(options?: RawAxiosRequestConfig) {
        return SitesApiFp(this.configuration).projectsListSites(options).then((request) => request(this.axios, this.basePath));
    }
}

