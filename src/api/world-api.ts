/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudLimitsView } from '../models';
// @ts-ignore
import type { CloudNewsResponse } from '../models';
// @ts-ignore
import type { CloudPipelineReq } from '../models';
// @ts-ignore
import type { CloudPipelineView } from '../models';
/**
 * WorldApi - axios parameter creator
 * @export
 */
export const WorldApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.  An empty or unknown plan resolves world-free, and a catalog failure serves that same free floor rather than erroring — so this always answers 200, and it can only ever under-grant. It reports the contract; it does not enforce it.
         * @summary Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.
         * @param {string} [plan] Plan is a World plan id from the live @hanzo/plans catalog, e.g. world-pro. Empty means world-free, and so does an id the catalog does not know — this never fails on an unknown plan.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldLimits: async (plan?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/world/limits`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (plan !== undefined) {
                localVarQueryParameter['plan'] = plan;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.  A project with no stored pipeline gets a sensible default set of world feeds rather than an empty answer. A source that fails or times out is SKIPPED: the feed degrades to honest partial results and never 5xxs because one outlet was down. Reading also publishes the result to the /v1/world/stream subscribers of the same (org, project), so a dashboard\'s own refresh updates every open tab.
         * @summary Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldNews: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/world/news`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered. A project that has never written one is answered with the built-in world feeds and `default: true`, so a fresh project sees the same feed /v1/world/news would actually serve rather than an empty configuration.
         * @summary Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldPipeline: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/world/pipeline`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Holds the connection open as text/event-stream and pushes a `news` event — the same {items:[…]} body GET /v1/world/news answers — each time the caller\'s (org, project) feed refreshes, with a `: ping` heartbeat comment every 25s. Delivery is best-effort: a slow consumer is dropped on buffer overrun and reconnects, re-fetching GET /v1/world/news, which stays the source of truth. Requires a validated principal; 403 without one.
         * @summary Live news refreshes for the caller\'s org and project, as Server-Sent Events.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldStream: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/world/stream`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Replaces the caller project\'s news pipeline and returns what was stored. It is a WHOLE replacement, not a patch: a field the request leaves out is stored empty, so sending only feeds clears the filters.  Every feed URL is validated HERE, at the write boundary — http(s) only, and the host must be on the server\'s allowlist — so a stored pipeline can never name a host the fetcher would later refuse, and the allowlist is one decision in one place rather than a check at each fetch.
         * @summary Replaces the caller project\'s news pipeline and returns what was stored.
         * @param {CloudPipelineReq} cloudPipelineReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1WorldPipeline: async (cloudPipelineReq: CloudPipelineReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudPipelineReq' is not null or undefined
            assertParamExists('cloudPutV1WorldPipeline', 'cloudPipelineReq', cloudPipelineReq)
            const localVarPath = `/v1/world/pipeline`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudPipelineReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * WorldApi - functional programming interface
 * @export
 */
export const WorldApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = WorldApiAxiosParamCreator(configuration)
    return {
        /**
         * Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.  An empty or unknown plan resolves world-free, and a catalog failure serves that same free floor rather than erroring — so this always answers 200, and it can only ever under-grant. It reports the contract; it does not enforce it.
         * @summary Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.
         * @param {string} [plan] Plan is a World plan id from the live @hanzo/plans catalog, e.g. world-pro. Empty means world-free, and so does an id the catalog does not know — this never fails on an unknown plan.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1WorldLimits(plan?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudLimitsView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1WorldLimits(plan, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['WorldApi.cloudGetV1WorldLimits']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.  A project with no stored pipeline gets a sensible default set of world feeds rather than an empty answer. A source that fails or times out is SKIPPED: the feed degrades to honest partial results and never 5xxs because one outlet was down. Reading also publishes the result to the /v1/world/stream subscribers of the same (org, project), so a dashboard\'s own refresh updates every open tab.
         * @summary Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1WorldNews(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudNewsResponse>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1WorldNews(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['WorldApi.cloudGetV1WorldNews']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered. A project that has never written one is answered with the built-in world feeds and `default: true`, so a fresh project sees the same feed /v1/world/news would actually serve rather than an empty configuration.
         * @summary Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1WorldPipeline(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudPipelineView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1WorldPipeline(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['WorldApi.cloudGetV1WorldPipeline']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Holds the connection open as text/event-stream and pushes a `news` event — the same {items:[…]} body GET /v1/world/news answers — each time the caller\'s (org, project) feed refreshes, with a `: ping` heartbeat comment every 25s. Delivery is best-effort: a slow consumer is dropped on buffer overrun and reconnects, re-fetching GET /v1/world/news, which stays the source of truth. Requires a validated principal; 403 without one.
         * @summary Live news refreshes for the caller\'s org and project, as Server-Sent Events.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1WorldStream(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1WorldStream(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['WorldApi.cloudGetV1WorldStream']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Replaces the caller project\'s news pipeline and returns what was stored. It is a WHOLE replacement, not a patch: a field the request leaves out is stored empty, so sending only feeds clears the filters.  Every feed URL is validated HERE, at the write boundary — http(s) only, and the host must be on the server\'s allowlist — so a stored pipeline can never name a host the fetcher would later refuse, and the allowlist is one decision in one place rather than a check at each fetch.
         * @summary Replaces the caller project\'s news pipeline and returns what was stored.
         * @param {CloudPipelineReq} cloudPipelineReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1WorldPipeline(cloudPipelineReq: CloudPipelineReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudPipelineView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1WorldPipeline(cloudPipelineReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['WorldApi.cloudPutV1WorldPipeline']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * WorldApi - factory interface
 * @export
 */
export const WorldApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = WorldApiFp(configuration)
    return {
        /**
         * Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.  An empty or unknown plan resolves world-free, and a catalog failure serves that same free floor rather than erroring — so this always answers 200, and it can only ever under-grant. It reports the contract; it does not enforce it.
         * @summary Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.
         * @param {WorldApiCloudGetV1WorldLimitsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldLimits(requestParameters: WorldApiCloudGetV1WorldLimitsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudLimitsView> {
            return localVarFp.cloudGetV1WorldLimits(requestParameters.plan, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.  A project with no stored pipeline gets a sensible default set of world feeds rather than an empty answer. A source that fails or times out is SKIPPED: the feed degrades to honest partial results and never 5xxs because one outlet was down. Reading also publishes the result to the /v1/world/stream subscribers of the same (org, project), so a dashboard\'s own refresh updates every open tab.
         * @summary Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldNews(options?: RawAxiosRequestConfig): AxiosPromise<CloudNewsResponse> {
            return localVarFp.cloudGetV1WorldNews(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered. A project that has never written one is answered with the built-in world feeds and `default: true`, so a fresh project sees the same feed /v1/world/news would actually serve rather than an empty configuration.
         * @summary Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldPipeline(options?: RawAxiosRequestConfig): AxiosPromise<CloudPipelineView> {
            return localVarFp.cloudGetV1WorldPipeline(options).then((request) => request(axios, basePath));
        },
        /**
         * Holds the connection open as text/event-stream and pushes a `news` event — the same {items:[…]} body GET /v1/world/news answers — each time the caller\'s (org, project) feed refreshes, with a `: ping` heartbeat comment every 25s. Delivery is best-effort: a slow consumer is dropped on buffer overrun and reconnects, re-fetching GET /v1/world/news, which stays the source of truth. Requires a validated principal; 403 without one.
         * @summary Live news refreshes for the caller\'s org and project, as Server-Sent Events.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1WorldStream(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1WorldStream(options).then((request) => request(axios, basePath));
        },
        /**
         * Replaces the caller project\'s news pipeline and returns what was stored. It is a WHOLE replacement, not a patch: a field the request leaves out is stored empty, so sending only feeds clears the filters.  Every feed URL is validated HERE, at the write boundary — http(s) only, and the host must be on the server\'s allowlist — so a stored pipeline can never name a host the fetcher would later refuse, and the allowlist is one decision in one place rather than a check at each fetch.
         * @summary Replaces the caller project\'s news pipeline and returns what was stored.
         * @param {WorldApiCloudPutV1WorldPipelineRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1WorldPipeline(requestParameters: WorldApiCloudPutV1WorldPipelineRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudPipelineView> {
            return localVarFp.cloudPutV1WorldPipeline(requestParameters.cloudPipelineReq, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudGetV1WorldLimits operation in WorldApi.
 * @export
 * @interface WorldApiCloudGetV1WorldLimitsRequest
 */
export interface WorldApiCloudGetV1WorldLimitsRequest {
    /**
     * Plan is a World plan id from the live @hanzo/plans catalog, e.g. world-pro. Empty means world-free, and so does an id the catalog does not know — this never fails on an unknown plan.
     * @type {string}
     * @memberof WorldApiCloudGetV1WorldLimits
     */
    readonly plan?: string
}

/**
 * Request parameters for cloudPutV1WorldPipeline operation in WorldApi.
 * @export
 * @interface WorldApiCloudPutV1WorldPipelineRequest
 */
export interface WorldApiCloudPutV1WorldPipelineRequest {
    /**
     * 
     * @type {CloudPipelineReq}
     * @memberof WorldApiCloudPutV1WorldPipeline
     */
    readonly cloudPipelineReq: CloudPipelineReq
}

/**
 * WorldApi - object-oriented interface
 * @export
 * @class WorldApi
 * @extends {BaseAPI}
 */
export class WorldApi extends BaseAPI {
    /**
     * Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.  An empty or unknown plan resolves world-free, and a catalog failure serves that same free floor rather than erroring — so this always answers 200, and it can only ever under-grant. It reports the contract; it does not enforce it.
     * @summary Echoes a World plan\'s rate limits, alert quota and model-API grant, read straight from the live @hanzo/plans catalog, so agents and dashboards configure themselves against the catalog instead of hardcoding tier numbers.
     * @param {WorldApiCloudGetV1WorldLimitsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WorldApi
     */
    public cloudGetV1WorldLimits(requestParameters: WorldApiCloudGetV1WorldLimitsRequest = {}, options?: RawAxiosRequestConfig) {
        return WorldApiFp(this.configuration).cloudGetV1WorldLimits(requestParameters.plan, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.  A project with no stored pipeline gets a sensible default set of world feeds rather than an empty answer. A source that fails or times out is SKIPPED: the feed degrades to honest partial results and never 5xxs because one outlet was down. Reading also publishes the result to the /v1/world/stream subscribers of the same (org, project), so a dashboard\'s own refresh updates every open tab.
     * @summary Returns the caller\'s merged world-news feed: every source their project\'s pipeline names — GDELT once per keyword, plus each allowlisted RSS or Atom feed — fetched concurrently, narrowed by the pipeline\'s keyword/region/source filters, deduplicated by link and sorted freshest first, capped at 50 items.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WorldApi
     */
    public cloudGetV1WorldNews(options?: RawAxiosRequestConfig) {
        return WorldApiFp(this.configuration).cloudGetV1WorldNews(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered. A project that has never written one is answered with the built-in world feeds and `default: true`, so a fresh project sees the same feed /v1/world/news would actually serve rather than an empty configuration.
     * @summary Returns the caller project\'s news pipeline: which feeds it reads and how the merged result is filtered.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WorldApi
     */
    public cloudGetV1WorldPipeline(options?: RawAxiosRequestConfig) {
        return WorldApiFp(this.configuration).cloudGetV1WorldPipeline(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Holds the connection open as text/event-stream and pushes a `news` event — the same {items:[…]} body GET /v1/world/news answers — each time the caller\'s (org, project) feed refreshes, with a `: ping` heartbeat comment every 25s. Delivery is best-effort: a slow consumer is dropped on buffer overrun and reconnects, re-fetching GET /v1/world/news, which stays the source of truth. Requires a validated principal; 403 without one.
     * @summary Live news refreshes for the caller\'s org and project, as Server-Sent Events.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WorldApi
     */
    public cloudGetV1WorldStream(options?: RawAxiosRequestConfig) {
        return WorldApiFp(this.configuration).cloudGetV1WorldStream(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Replaces the caller project\'s news pipeline and returns what was stored. It is a WHOLE replacement, not a patch: a field the request leaves out is stored empty, so sending only feeds clears the filters.  Every feed URL is validated HERE, at the write boundary — http(s) only, and the host must be on the server\'s allowlist — so a stored pipeline can never name a host the fetcher would later refuse, and the allowlist is one decision in one place rather than a check at each fetch.
     * @summary Replaces the caller project\'s news pipeline and returns what was stored.
     * @param {WorldApiCloudPutV1WorldPipelineRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WorldApi
     */
    public cloudPutV1WorldPipeline(requestParameters: WorldApiCloudPutV1WorldPipelineRequest, options?: RawAxiosRequestConfig) {
        return WorldApiFp(this.configuration).cloudPutV1WorldPipeline(requestParameters.cloudPipelineReq, options).then((request) => request(this.axios, this.basePath));
    }
}

