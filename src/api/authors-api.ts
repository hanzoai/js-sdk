/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudClaim } from '../models';
// @ts-ignore
import type { CloudConnectRequest } from '../models';
// @ts-ignore
import type { CloudDeployRecord } from '../models';
// @ts-ignore
import type { CloudDeployRequest } from '../models';
// @ts-ignore
import type { CloudEnrolment } from '../models';
// @ts-ignore
import type { CloudVerifyRequest } from '../models';
/**
 * AuthorsApi - axios parameter creator
 * @export
 */
export const AuthorsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.  It answers ONE OF TWO SHAPES from this address. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\", \"badgeBase\"} — an honest \"not enrolled\" rather than a 404, so the console can render the connect form. An enrolled org gets the dashboard: isAuthor, id, status, githubLogin, verified, verifyCode, verifyFile, verifySnippet, shareBps, badgeBase, repos, orgs, deploys, accruedCents, pendingCents, paidCents, payouts and ledger.  For an APPROVED author this read ALSO runs the accrual sweep opportunistically, so the dashboard is self-updating. That is why the royalty AUDIT lives at its own address: an audit must not move the money it is auditing.
         * @summary MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Authors: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/authors`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.  It answers ONE OF TWO SHAPES. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\"} — never a 404, which would answer \"is this org an author?\" for anyone who asked. An enrolled org gets the basis: isAuthor, id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger, reconciliation, window, and period when one was requested.  This read NEVER sweeps, and that is the point of it being a separate address from the dashboard: an audit must not move the money it is auditing, so calling it N times leaves the balances and the ledger byte-identical.
         * @summary MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.
         * @param {string} [period] Period is the UTC accrual month, YYYY-MM. Empty means every period; any other shape is refused with 400, because the period is echoed back and used as a SQL filter and is only ever accepted in the one form the accrual latch mints.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AuthorsBasis: async (period?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/authors/basis`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (period !== undefined) {
                localVarQueryParameter['period'] = period;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs. It is IDEMPOTENT: a second call returns the same enrolment rather than a conflict.  The forge login is taken from IAM\'s LINKED account for the provider when there is one — that is identity proof, not a claim — and only otherwise from the login in the body, which then has to be proven per repository. Connecting does not admit an org to earning: a platform reviewer approves that separately.  Answers 201 when it enrolled the org and 200 when it found an existing enrolment.
         * @summary ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs.
         * @param {CloudConnectRequest} cloudConnectRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1AuthorsConnect: async (cloudConnectRequest: CloudConnectRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudConnectRequest' is not null or undefined
            assertParamExists('cloudPostV1AuthorsConnect', 'cloudConnectRequest', cloudConnectRequest)
            const localVarPath = `/v1/authors/connect`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudConnectRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.  It is deliberately NOT an error for a deploy to attribute to nobody: a project built from no repository, or from one no author has verified, answers {\"recorded\": false, \"reason\"} so a deploy pipeline can fire this on every deploy without branching. Attribution resolves per-repository first, then owner-wide, so a repository with its own claim always earns for its own author.  A deploy of a Hanzo-maintained template attributes to the platform treasury, and a self-deploy (the author\'s own org deploying its own repository) is recorded for provenance but excluded from accrual. The edge is idempotent per repository+project+org.  Answers 201 when it recorded a new edge and 200 otherwise.
         * @summary RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.
         * @param {CloudDeployRequest} cloudDeployRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1AuthorsDeploysRecord: async (cloudDeployRequest: CloudDeployRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudDeployRequest' is not null or undefined
            assertParamExists('cloudPostV1AuthorsDeploysRecord', 'cloudDeployRequest', cloudDeployRequest)
            const localVarPath = `/v1/authors/deploys/record`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudDeployRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.  Ownership is proven the SAME two ways in both cases, tried in order: an IAM-linked forge token with admin or push permission, or a hanzo.json on the default branch carrying the author\'s verify code. Claiming an OWNER proves it against that owner\'s \".github\" control repository, and is exactly as strong as a per-repository claim — an owner the caller cannot prove is refused with 422, never assumed.  A per-repository claim wins over an owner-wide one, so a specifically-claimed repository always earns for its own author. A repository another author has already verified is a 409. The org must have connected first.  Answers 201 when it recorded a new claim and 200 when the claim already existed.
         * @summary VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.
         * @param {CloudVerifyRequest} cloudVerifyRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1AuthorsReposVerify: async (cloudVerifyRequest: CloudVerifyRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudVerifyRequest' is not null or undefined
            assertParamExists('cloudPostV1AuthorsReposVerify', 'cloudVerifyRequest', cloudVerifyRequest)
            const localVarPath = `/v1/authors/repos/verify`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudVerifyRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * AuthorsApi - functional programming interface
 * @export
 */
export const AuthorsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = AuthorsApiAxiosParamCreator(configuration)
    return {
        /**
         * MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.  It answers ONE OF TWO SHAPES from this address. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\", \"badgeBase\"} — an honest \"not enrolled\" rather than a 404, so the console can render the connect form. An enrolled org gets the dashboard: isAuthor, id, status, githubLogin, verified, verifyCode, verifyFile, verifySnippet, shareBps, badgeBase, repos, orgs, deploys, accruedCents, pendingCents, paidCents, payouts and ledger.  For an APPROVED author this read ALSO runs the accrual sweep opportunistically, so the dashboard is self-updating. That is why the royalty AUDIT lives at its own address: an audit must not move the money it is auditing.
         * @summary MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Authors(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<{ [key: string]: object; }>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Authors(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AuthorsApi.cloudGetV1Authors']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.  It answers ONE OF TWO SHAPES. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\"} — never a 404, which would answer \"is this org an author?\" for anyone who asked. An enrolled org gets the basis: isAuthor, id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger, reconciliation, window, and period when one was requested.  This read NEVER sweeps, and that is the point of it being a separate address from the dashboard: an audit must not move the money it is auditing, so calling it N times leaves the balances and the ledger byte-identical.
         * @summary MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.
         * @param {string} [period] Period is the UTC accrual month, YYYY-MM. Empty means every period; any other shape is refused with 400, because the period is echoed back and used as a SQL filter and is only ever accepted in the one form the accrual latch mints.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1AuthorsBasis(period?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<{ [key: string]: object; }>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1AuthorsBasis(period, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AuthorsApi.cloudGetV1AuthorsBasis']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs. It is IDEMPOTENT: a second call returns the same enrolment rather than a conflict.  The forge login is taken from IAM\'s LINKED account for the provider when there is one — that is identity proof, not a claim — and only otherwise from the login in the body, which then has to be proven per repository. Connecting does not admit an org to earning: a platform reviewer approves that separately.  Answers 201 when it enrolled the org and 200 when it found an existing enrolment.
         * @summary ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs.
         * @param {CloudConnectRequest} cloudConnectRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1AuthorsConnect(cloudConnectRequest: CloudConnectRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudEnrolment>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1AuthorsConnect(cloudConnectRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AuthorsApi.cloudPostV1AuthorsConnect']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.  It is deliberately NOT an error for a deploy to attribute to nobody: a project built from no repository, or from one no author has verified, answers {\"recorded\": false, \"reason\"} so a deploy pipeline can fire this on every deploy without branching. Attribution resolves per-repository first, then owner-wide, so a repository with its own claim always earns for its own author.  A deploy of a Hanzo-maintained template attributes to the platform treasury, and a self-deploy (the author\'s own org deploying its own repository) is recorded for provenance but excluded from accrual. The edge is idempotent per repository+project+org.  Answers 201 when it recorded a new edge and 200 otherwise.
         * @summary RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.
         * @param {CloudDeployRequest} cloudDeployRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1AuthorsDeploysRecord(cloudDeployRequest: CloudDeployRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudDeployRecord>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1AuthorsDeploysRecord(cloudDeployRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AuthorsApi.cloudPostV1AuthorsDeploysRecord']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.  Ownership is proven the SAME two ways in both cases, tried in order: an IAM-linked forge token with admin or push permission, or a hanzo.json on the default branch carrying the author\'s verify code. Claiming an OWNER proves it against that owner\'s \".github\" control repository, and is exactly as strong as a per-repository claim — an owner the caller cannot prove is refused with 422, never assumed.  A per-repository claim wins over an owner-wide one, so a specifically-claimed repository always earns for its own author. A repository another author has already verified is a 409. The org must have connected first.  Answers 201 when it recorded a new claim and 200 when the claim already existed.
         * @summary VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.
         * @param {CloudVerifyRequest} cloudVerifyRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1AuthorsReposVerify(cloudVerifyRequest: CloudVerifyRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudClaim>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1AuthorsReposVerify(cloudVerifyRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AuthorsApi.cloudPostV1AuthorsReposVerify']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * AuthorsApi - factory interface
 * @export
 */
export const AuthorsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = AuthorsApiFp(configuration)
    return {
        /**
         * MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.  It answers ONE OF TWO SHAPES from this address. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\", \"badgeBase\"} — an honest \"not enrolled\" rather than a 404, so the console can render the connect form. An enrolled org gets the dashboard: isAuthor, id, status, githubLogin, verified, verifyCode, verifyFile, verifySnippet, shareBps, badgeBase, repos, orgs, deploys, accruedCents, pendingCents, paidCents, payouts and ledger.  For an APPROVED author this read ALSO runs the accrual sweep opportunistically, so the dashboard is self-updating. That is why the royalty AUDIT lives at its own address: an audit must not move the money it is auditing.
         * @summary MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Authors(options?: RawAxiosRequestConfig): AxiosPromise<{ [key: string]: object; }> {
            return localVarFp.cloudGetV1Authors(options).then((request) => request(axios, basePath));
        },
        /**
         * MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.  It answers ONE OF TWO SHAPES. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\"} — never a 404, which would answer \"is this org an author?\" for anyone who asked. An enrolled org gets the basis: isAuthor, id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger, reconciliation, window, and period when one was requested.  This read NEVER sweeps, and that is the point of it being a separate address from the dashboard: an audit must not move the money it is auditing, so calling it N times leaves the balances and the ledger byte-identical.
         * @summary MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.
         * @param {AuthorsApiCloudGetV1AuthorsBasisRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AuthorsBasis(requestParameters: AuthorsApiCloudGetV1AuthorsBasisRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<{ [key: string]: object; }> {
            return localVarFp.cloudGetV1AuthorsBasis(requestParameters.period, options).then((request) => request(axios, basePath));
        },
        /**
         * ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs. It is IDEMPOTENT: a second call returns the same enrolment rather than a conflict.  The forge login is taken from IAM\'s LINKED account for the provider when there is one — that is identity proof, not a claim — and only otherwise from the login in the body, which then has to be proven per repository. Connecting does not admit an org to earning: a platform reviewer approves that separately.  Answers 201 when it enrolled the org and 200 when it found an existing enrolment.
         * @summary ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs.
         * @param {AuthorsApiCloudPostV1AuthorsConnectRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1AuthorsConnect(requestParameters: AuthorsApiCloudPostV1AuthorsConnectRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudEnrolment> {
            return localVarFp.cloudPostV1AuthorsConnect(requestParameters.cloudConnectRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.  It is deliberately NOT an error for a deploy to attribute to nobody: a project built from no repository, or from one no author has verified, answers {\"recorded\": false, \"reason\"} so a deploy pipeline can fire this on every deploy without branching. Attribution resolves per-repository first, then owner-wide, so a repository with its own claim always earns for its own author.  A deploy of a Hanzo-maintained template attributes to the platform treasury, and a self-deploy (the author\'s own org deploying its own repository) is recorded for provenance but excluded from accrual. The edge is idempotent per repository+project+org.  Answers 201 when it recorded a new edge and 200 otherwise.
         * @summary RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.
         * @param {AuthorsApiCloudPostV1AuthorsDeploysRecordRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1AuthorsDeploysRecord(requestParameters: AuthorsApiCloudPostV1AuthorsDeploysRecordRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudDeployRecord> {
            return localVarFp.cloudPostV1AuthorsDeploysRecord(requestParameters.cloudDeployRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.  Ownership is proven the SAME two ways in both cases, tried in order: an IAM-linked forge token with admin or push permission, or a hanzo.json on the default branch carrying the author\'s verify code. Claiming an OWNER proves it against that owner\'s \".github\" control repository, and is exactly as strong as a per-repository claim — an owner the caller cannot prove is refused with 422, never assumed.  A per-repository claim wins over an owner-wide one, so a specifically-claimed repository always earns for its own author. A repository another author has already verified is a 409. The org must have connected first.  Answers 201 when it recorded a new claim and 200 when the claim already existed.
         * @summary VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.
         * @param {AuthorsApiCloudPostV1AuthorsReposVerifyRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1AuthorsReposVerify(requestParameters: AuthorsApiCloudPostV1AuthorsReposVerifyRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudClaim> {
            return localVarFp.cloudPostV1AuthorsReposVerify(requestParameters.cloudVerifyRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudGetV1AuthorsBasis operation in AuthorsApi.
 * @export
 * @interface AuthorsApiCloudGetV1AuthorsBasisRequest
 */
export interface AuthorsApiCloudGetV1AuthorsBasisRequest {
    /**
     * Period is the UTC accrual month, YYYY-MM. Empty means every period; any other shape is refused with 400, because the period is echoed back and used as a SQL filter and is only ever accepted in the one form the accrual latch mints.
     * @type {string}
     * @memberof AuthorsApiCloudGetV1AuthorsBasis
     */
    readonly period?: string
}

/**
 * Request parameters for cloudPostV1AuthorsConnect operation in AuthorsApi.
 * @export
 * @interface AuthorsApiCloudPostV1AuthorsConnectRequest
 */
export interface AuthorsApiCloudPostV1AuthorsConnectRequest {
    /**
     * 
     * @type {CloudConnectRequest}
     * @memberof AuthorsApiCloudPostV1AuthorsConnect
     */
    readonly cloudConnectRequest: CloudConnectRequest
}

/**
 * Request parameters for cloudPostV1AuthorsDeploysRecord operation in AuthorsApi.
 * @export
 * @interface AuthorsApiCloudPostV1AuthorsDeploysRecordRequest
 */
export interface AuthorsApiCloudPostV1AuthorsDeploysRecordRequest {
    /**
     * 
     * @type {CloudDeployRequest}
     * @memberof AuthorsApiCloudPostV1AuthorsDeploysRecord
     */
    readonly cloudDeployRequest: CloudDeployRequest
}

/**
 * Request parameters for cloudPostV1AuthorsReposVerify operation in AuthorsApi.
 * @export
 * @interface AuthorsApiCloudPostV1AuthorsReposVerifyRequest
 */
export interface AuthorsApiCloudPostV1AuthorsReposVerifyRequest {
    /**
     * 
     * @type {CloudVerifyRequest}
     * @memberof AuthorsApiCloudPostV1AuthorsReposVerify
     */
    readonly cloudVerifyRequest: CloudVerifyRequest
}

/**
 * AuthorsApi - object-oriented interface
 * @export
 * @class AuthorsApi
 * @extends {BaseAPI}
 */
export class AuthorsApi extends BaseAPI {
    /**
     * MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.  It answers ONE OF TWO SHAPES from this address. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\", \"badgeBase\"} — an honest \"not enrolled\" rather than a 404, so the console can render the connect form. An enrolled org gets the dashboard: isAuthor, id, status, githubLogin, verified, verifyCode, verifyFile, verifySnippet, shareBps, badgeBase, repos, orgs, deploys, accruedCents, pendingCents, paidCents, payouts and ledger.  For an APPROVED author this read ALSO runs the accrual sweep opportunistically, so the dashboard is self-updating. That is why the royalty AUDIT lives at its own address: an audit must not move the money it is auditing.
     * @summary MyAuthorProgram returns the caller\'s author-program dashboard: enrolment status, linked forge login, verified repositories and owner-wide claims, recorded deploys, accrued / pending / paid royalty, and the payout history.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthorsApi
     */
    public cloudGetV1Authors(options?: RawAxiosRequestConfig) {
        return AuthorsApiFp(this.configuration).cloudGetV1Authors(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.  It answers ONE OF TWO SHAPES. An org that has never connected gets {\"isAuthor\": false, \"defaultShareBps\"} — never a 404, which would answer \"is this org an author?\" for anyone who asked. An enrolled org gets the basis: isAuthor, id, status, asOf, shareBps, platformShareBps, defaultShareBps, shareSource, settlesTo, method (the formula, the rate card and the sizing), ledger, reconciliation, window, and period when one was requested.  This read NEVER sweeps, and that is the point of it being a separate address from the dashboard: an audit must not move the money it is auditing, so calling it N times leaves the balances and the ledger byte-identical.
     * @summary MyRoyaltyBasis returns the AUDIT TRAIL behind the caller\'s own royalty: every ledger row with the spend it was computed from, the share applied at the time, the platform\'s matching half, whether each row satisfies the formula, and the attribution edges that already existed when the row was written.
     * @param {AuthorsApiCloudGetV1AuthorsBasisRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthorsApi
     */
    public cloudGetV1AuthorsBasis(requestParameters: AuthorsApiCloudGetV1AuthorsBasisRequest = {}, options?: RawAxiosRequestConfig) {
        return AuthorsApiFp(this.configuration).cloudGetV1AuthorsBasis(requestParameters.period, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs. It is IDEMPOTENT: a second call returns the same enrolment rather than a conflict.  The forge login is taken from IAM\'s LINKED account for the provider when there is one — that is identity proof, not a claim — and only otherwise from the login in the body, which then has to be proven per repository. Connecting does not admit an org to earning: a platform reviewer approves that separately.  Answers 201 when it enrolled the org and 200 when it found an existing enrolment.
     * @summary ConnectAuthor enrols the caller\'s org in the author program at status \"connected\" and returns its enrolment, including the verify code the file method needs.
     * @param {AuthorsApiCloudPostV1AuthorsConnectRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthorsApi
     */
    public cloudPostV1AuthorsConnect(requestParameters: AuthorsApiCloudPostV1AuthorsConnectRequest, options?: RawAxiosRequestConfig) {
        return AuthorsApiFp(this.configuration).cloudPostV1AuthorsConnect(requestParameters.cloudConnectRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.  It is deliberately NOT an error for a deploy to attribute to nobody: a project built from no repository, or from one no author has verified, answers {\"recorded\": false, \"reason\"} so a deploy pipeline can fire this on every deploy without branching. Attribution resolves per-repository first, then owner-wide, so a repository with its own claim always earns for its own author.  A deploy of a Hanzo-maintained template attributes to the platform treasury, and a self-deploy (the author\'s own org deploying its own repository) is recorded for provenance but excluded from accrual. The edge is idempotent per repository+project+org.  Answers 201 when it recorded a new edge and 200 otherwise.
     * @summary RecordAuthorDeploy records that the caller\'s org deployed a project built from a source repository, which is the edge that makes an author\'s work earn royalty.
     * @param {AuthorsApiCloudPostV1AuthorsDeploysRecordRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthorsApi
     */
    public cloudPostV1AuthorsDeploysRecord(requestParameters: AuthorsApiCloudPostV1AuthorsDeploysRecordRequest, options?: RawAxiosRequestConfig) {
        return AuthorsApiFp(this.configuration).cloudPostV1AuthorsDeploysRecord(requestParameters.cloudDeployRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.  Ownership is proven the SAME two ways in both cases, tried in order: an IAM-linked forge token with admin or push permission, or a hanzo.json on the default branch carrying the author\'s verify code. Claiming an OWNER proves it against that owner\'s \".github\" control repository, and is exactly as strong as a per-repository claim — an owner the caller cannot prove is refused with 422, never assumed.  A per-repository claim wins over an owner-wide one, so a specifically-claimed repository always earns for its own author. A repository another author has already verified is a 409. The org must have connected first.  Answers 201 when it recorded a new claim and 200 when the claim already existed.
     * @summary VerifyAuthorRepo proves that the caller owns a repository — or a whole OWNER — and records the claim, which is what makes deploys of that code earn royalty.
     * @param {AuthorsApiCloudPostV1AuthorsReposVerifyRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AuthorsApi
     */
    public cloudPostV1AuthorsReposVerify(requestParameters: AuthorsApiCloudPostV1AuthorsReposVerifyRequest, options?: RawAxiosRequestConfig) {
        return AuthorsApiFp(this.configuration).cloudPostV1AuthorsReposVerify(requestParameters.cloudVerifyRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

