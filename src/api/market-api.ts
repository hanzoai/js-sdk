/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { History } from '../models';
// @ts-ignore
import type { Pools } from '../models';
// @ts-ignore
import type { Roster } from '../models';
// @ts-ignore
import type { Survey } from '../models';
// @ts-ignore
import type { Tokens } from '../models';
/**
 * MarketApi - axios parameter creator
 * @export
 */
export const MarketApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.  One call. It reads the chain registry, then every chain\'s indexer for its figures and its most recent active day, all at once — where a client doing it itself makes one registry request and two more per chain.  THE ROW IS THE UNIT OF TRUTH. Each carries its own reach, so one indexer being unreachable costs one row its figures and leaves the rest answered. A chain with no market maker deployed — the registry names no factory for it — answers `read` with totals of nothing, which is a fact about that chain and is not the same as a chain nobody could ask.
         * @summary Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketChains: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/market/chains`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.  The two price fields on a pool are the ratio its own reserves stand at, as the indexer computed them. They are not a price ON either token and not a mark: nothing here derives one, ranks the pools, or names a route through them.  A chain with no market maker deployed answers `read` with no pools. That is the chain\'s real condition, and it is deliberately not the same answer as an indexer that could not be asked.
         * @summary Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.
         * @param {string} [chain] Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketPools: async (chain?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/market/pools`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (chain !== undefined) {
                localVarQueryParameter['chain'] = chain;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers which of the four settlement precompiles carry code on one chain.  An address with no code answers a call with empty data rather than an error, so \"this chain has no view precompile\" and \"this market was never opened\" reach a caller as the same silence — and only the second is a fact about a market. This says which it is, by asking the node for the code at each address.  It reads presence and nothing else. No market, no quote, no depth and no order is requested here, and `eth_getCode` is the only method this operation ever sends.
         * @summary Answers which of the four settlement precompiles carry code on one chain.
         * @param {string} [chain] Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketSurvey: async (chain?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/market/survey`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (chain !== undefined) {
                localVarQueryParameter['chain'] = chain;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.  Every figure is the indexer\'s own arithmetic, passed through as the decimal string it computed. Nothing here rounds one, converts one, or fills a gap: a day the indexer holds no figure for arrives with that field absent, which says \"not indexed\" where a zero would say \"worth nothing\".
         * @summary Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.
         * @param {string} [chain] 
         * @param {string} [at] At is the token\&#39;s contract address.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketToken: async (chain?: string, at?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/market/token`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (chain !== undefined) {
                localVarQueryParameter['chain'] = chain;
            }

            if (at !== undefined) {
                localVarQueryParameter['at'] = at;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.  This is what the indexer INGESTED, which is not the same as what exists on the chain: a token nothing has traded has no row here, and this is not a registry of what is permitted or listed.
         * @summary Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.
         * @param {string} [chain] Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketTokens: async (chain?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/market/tokens`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (chain !== undefined) {
                localVarQueryParameter['chain'] = chain;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * MarketApi - functional programming interface
 * @export
 */
export const MarketApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = MarketApiAxiosParamCreator(configuration)
    return {
        /**
         * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.  One call. It reads the chain registry, then every chain\'s indexer for its figures and its most recent active day, all at once — where a client doing it itself makes one registry request and two more per chain.  THE ROW IS THE UNIT OF TRUTH. Each carries its own reach, so one indexer being unreachable costs one row its figures and leaves the rest answered. A chain with no market maker deployed — the registry names no factory for it — answers `read` with totals of nothing, which is a fact about that chain and is not the same as a chain nobody could ask.
         * @summary Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getMarketChains(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Roster>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getMarketChains(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MarketApi.getMarketChains']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.  The two price fields on a pool are the ratio its own reserves stand at, as the indexer computed them. They are not a price ON either token and not a mark: nothing here derives one, ranks the pools, or names a route through them.  A chain with no market maker deployed answers `read` with no pools. That is the chain\'s real condition, and it is deliberately not the same answer as an indexer that could not be asked.
         * @summary Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.
         * @param {string} [chain] Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getMarketPools(chain?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Pools>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getMarketPools(chain, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MarketApi.getMarketPools']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers which of the four settlement precompiles carry code on one chain.  An address with no code answers a call with empty data rather than an error, so \"this chain has no view precompile\" and \"this market was never opened\" reach a caller as the same silence — and only the second is a fact about a market. This says which it is, by asking the node for the code at each address.  It reads presence and nothing else. No market, no quote, no depth and no order is requested here, and `eth_getCode` is the only method this operation ever sends.
         * @summary Answers which of the four settlement precompiles carry code on one chain.
         * @param {string} [chain] Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getMarketSurvey(chain?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Survey>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getMarketSurvey(chain, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MarketApi.getMarketSurvey']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.  Every figure is the indexer\'s own arithmetic, passed through as the decimal string it computed. Nothing here rounds one, converts one, or fills a gap: a day the indexer holds no figure for arrives with that field absent, which says \"not indexed\" where a zero would say \"worth nothing\".
         * @summary Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.
         * @param {string} [chain] 
         * @param {string} [at] At is the token\&#39;s contract address.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getMarketToken(chain?: string, at?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<History>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getMarketToken(chain, at, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MarketApi.getMarketToken']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.  This is what the indexer INGESTED, which is not the same as what exists on the chain: a token nothing has traded has no row here, and this is not a registry of what is permitted or listed.
         * @summary Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.
         * @param {string} [chain] Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getMarketTokens(chain?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Tokens>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getMarketTokens(chain, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MarketApi.getMarketTokens']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * MarketApi - factory interface
 * @export
 */
export const MarketApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = MarketApiFp(configuration)
    return {
        /**
         * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.  One call. It reads the chain registry, then every chain\'s indexer for its figures and its most recent active day, all at once — where a client doing it itself makes one registry request and two more per chain.  THE ROW IS THE UNIT OF TRUTH. Each carries its own reach, so one indexer being unreachable costs one row its figures and leaves the rest answered. A chain with no market maker deployed — the registry names no factory for it — answers `read` with totals of nothing, which is a fact about that chain and is not the same as a chain nobody could ask.
         * @summary Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketChains(options?: RawAxiosRequestConfig): AxiosPromise<Roster> {
            return localVarFp.getMarketChains(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.  The two price fields on a pool are the ratio its own reserves stand at, as the indexer computed them. They are not a price ON either token and not a mark: nothing here derives one, ranks the pools, or names a route through them.  A chain with no market maker deployed answers `read` with no pools. That is the chain\'s real condition, and it is deliberately not the same answer as an indexer that could not be asked.
         * @summary Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.
         * @param {MarketApiGetMarketPoolsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketPools(requestParameters: MarketApiGetMarketPoolsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<Pools> {
            return localVarFp.getMarketPools(requestParameters.chain, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers which of the four settlement precompiles carry code on one chain.  An address with no code answers a call with empty data rather than an error, so \"this chain has no view precompile\" and \"this market was never opened\" reach a caller as the same silence — and only the second is a fact about a market. This says which it is, by asking the node for the code at each address.  It reads presence and nothing else. No market, no quote, no depth and no order is requested here, and `eth_getCode` is the only method this operation ever sends.
         * @summary Answers which of the four settlement precompiles carry code on one chain.
         * @param {MarketApiGetMarketSurveyRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketSurvey(requestParameters: MarketApiGetMarketSurveyRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<Survey> {
            return localVarFp.getMarketSurvey(requestParameters.chain, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.  Every figure is the indexer\'s own arithmetic, passed through as the decimal string it computed. Nothing here rounds one, converts one, or fills a gap: a day the indexer holds no figure for arrives with that field absent, which says \"not indexed\" where a zero would say \"worth nothing\".
         * @summary Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.
         * @param {MarketApiGetMarketTokenRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketToken(requestParameters: MarketApiGetMarketTokenRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<History> {
            return localVarFp.getMarketToken(requestParameters.chain, requestParameters.at, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.  This is what the indexer INGESTED, which is not the same as what exists on the chain: a token nothing has traded has no row here, and this is not a registry of what is permitted or listed.
         * @summary Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.
         * @param {MarketApiGetMarketTokensRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getMarketTokens(requestParameters: MarketApiGetMarketTokensRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<Tokens> {
            return localVarFp.getMarketTokens(requestParameters.chain, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for getMarketPools operation in MarketApi.
 * @export
 * @interface MarketApiGetMarketPoolsRequest
 */
export interface MarketApiGetMarketPoolsRequest {
    /**
     * Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
     * @type {string}
     * @memberof MarketApiGetMarketPools
     */
    readonly chain?: string
}

/**
 * Request parameters for getMarketSurvey operation in MarketApi.
 * @export
 * @interface MarketApiGetMarketSurveyRequest
 */
export interface MarketApiGetMarketSurveyRequest {
    /**
     * Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
     * @type {string}
     * @memberof MarketApiGetMarketSurvey
     */
    readonly chain?: string
}

/**
 * Request parameters for getMarketToken operation in MarketApi.
 * @export
 * @interface MarketApiGetMarketTokenRequest
 */
export interface MarketApiGetMarketTokenRequest {
    /**
     * 
     * @type {string}
     * @memberof MarketApiGetMarketToken
     */
    readonly chain?: string

    /**
     * At is the token\&#39;s contract address.
     * @type {string}
     * @memberof MarketApiGetMarketToken
     */
    readonly at?: string
}

/**
 * Request parameters for getMarketTokens operation in MarketApi.
 * @export
 * @interface MarketApiGetMarketTokensRequest
 */
export interface MarketApiGetMarketTokensRequest {
    /**
     * Chain is the chain\&#39;s slug — &#x60;cchain&#x60;, &#x60;zoo&#x60; — as &#x60;chains&#x60; reports it. It is the indexer\&#39;s word for the chain and NOT the chain id: &#x60;96369&#x60;, &#x60;C&#x60; and &#x60;c-chain&#x60; all name nothing.
     * @type {string}
     * @memberof MarketApiGetMarketTokens
     */
    readonly chain?: string
}

/**
 * MarketApi - object-oriented interface
 * @export
 * @class MarketApi
 * @extends {BaseAPI}
 */
export class MarketApi extends BaseAPI {
    /**
     * Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.  One call. It reads the chain registry, then every chain\'s indexer for its figures and its most recent active day, all at once — where a client doing it itself makes one registry request and two more per chain.  THE ROW IS THE UNIT OF TRUTH. Each carries its own reach, so one indexer being unreachable costs one row its figures and leaves the rest answered. A chain with no market maker deployed — the registry names no factory for it — answers `read` with totals of nothing, which is a fact about that chain and is not the same as a chain nobody could ask.
     * @summary Answers every chain this deployment can read, what is deployed on each, and what its automated market maker amounts to.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MarketApi
     */
    public getMarketChains(options?: RawAxiosRequestConfig) {
        return MarketApiFp(this.configuration).getMarketChains(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.  The two price fields on a pool are the ratio its own reserves stand at, as the indexer computed them. They are not a price ON either token and not a mark: nothing here derives one, ranks the pools, or names a route through them.  A chain with no market maker deployed answers `read` with no pools. That is the chain\'s real condition, and it is deliberately not the same answer as an indexer that could not be asked.
     * @summary Answers the automated market makers on one chain: their two tokens, their fee tier, and what has moved through each.
     * @param {MarketApiGetMarketPoolsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MarketApi
     */
    public getMarketPools(requestParameters: MarketApiGetMarketPoolsRequest = {}, options?: RawAxiosRequestConfig) {
        return MarketApiFp(this.configuration).getMarketPools(requestParameters.chain, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers which of the four settlement precompiles carry code on one chain.  An address with no code answers a call with empty data rather than an error, so \"this chain has no view precompile\" and \"this market was never opened\" reach a caller as the same silence — and only the second is a fact about a market. This says which it is, by asking the node for the code at each address.  It reads presence and nothing else. No market, no quote, no depth and no order is requested here, and `eth_getCode` is the only method this operation ever sends.
     * @summary Answers which of the four settlement precompiles carry code on one chain.
     * @param {MarketApiGetMarketSurveyRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MarketApi
     */
    public getMarketSurvey(requestParameters: MarketApiGetMarketSurveyRequest = {}, options?: RawAxiosRequestConfig) {
        return MarketApiFp(this.configuration).getMarketSurvey(requestParameters.chain, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.  Every figure is the indexer\'s own arithmetic, passed through as the decimal string it computed. Nothing here rounds one, converts one, or fills a gap: a day the indexer holds no figure for arrives with that field absent, which says \"not indexed\" where a zero would say \"worth nothing\".
     * @summary Answers one token\'s daily history — open, high, low, close, price and volume per UTC day, oldest first.
     * @param {MarketApiGetMarketTokenRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MarketApi
     */
    public getMarketToken(requestParameters: MarketApiGetMarketTokenRequest = {}, options?: RawAxiosRequestConfig) {
        return MarketApiFp(this.configuration).getMarketToken(requestParameters.chain, requestParameters.at, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.  This is what the indexer INGESTED, which is not the same as what exists on the chain: a token nothing has traded has no row here, and this is not a registry of what is permitted or listed.
     * @summary Answers the tokens one chain\'s indexer has seen, with the decimals a caller needs to read any amount of one correctly.
     * @param {MarketApiGetMarketTokensRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MarketApi
     */
    public getMarketTokens(requestParameters: MarketApiGetMarketTokensRequest = {}, options?: RawAxiosRequestConfig) {
        return MarketApiFp(this.configuration).getMarketTokens(requestParameters.chain, options).then((request) => request(this.axios, this.basePath));
    }
}

