/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: 8.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { BlobJSON } from '../models';
// @ts-ignore
import type { CommitsJSON } from '../models';
// @ts-ignore
import type { CreateReq } from '../models';
// @ts-ignore
import type { FilesJSON } from '../models';
// @ts-ignore
import type { GcOut } from '../models';
// @ts-ignore
import type { KeyList } from '../models';
// @ts-ignore
import type { KeyView } from '../models';
// @ts-ignore
import type { MirrorList } from '../models';
// @ts-ignore
import type { MirrorReq } from '../models';
// @ts-ignore
import type { MirrorTargetReq } from '../models';
// @ts-ignore
import type { MirrorTargetView } from '../models';
// @ts-ignore
import type { PatchIn } from '../models';
// @ts-ignore
import type { PushReq } from '../models';
// @ts-ignore
import type { PushResp } from '../models';
// @ts-ignore
import type { ReadmeJSON } from '../models';
// @ts-ignore
import type { RefsJSON } from '../models';
// @ts-ignore
import type { RegisterKeyReq } from '../models';
// @ts-ignore
import type { RepoList } from '../models';
// @ts-ignore
import type { RepoView } from '../models';
// @ts-ignore
import type { SubscribeReq } from '../models';
// @ts-ignore
import type { SubscriptionList } from '../models';
// @ts-ignore
import type { SubscriptionView } from '../models';
// @ts-ignore
import type { TreeJSON } from '../models';
// @ts-ignore
import type { UsageView } from '../models';
// @ts-ignore
import type { ZapProcReq } from '../models';
/**
 * GitApi - axios parameter creator
 * @export
 */
export const GitApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found. Answers 204 with no body. Once removed the key no longer authenticates any SSH git access.
         * @summary Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found.
         * @param {string} id ID is the key\&#39;s identifier (\&quot;gitkey_…\&quot;), from the :id path segment.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitKeysId: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('deleteV1GitKeysId', 'id', id)
            const localVarPath = `/v1/git/keys/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes a repo\'s metadata and purges its storage. Answers 204 with no body. The metadata row is the source of truth for existence, so a storage purge that fails is logged and the delete still succeeds — and a second call is a 404, not a second delete.
         * @summary Removes a repo\'s metadata and purges its storage.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitReposName: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('deleteV1GitReposName', 'name', name)
            const localVarPath = `/v1/git/repos/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes one outbound mirror target; later pushes stop being forwarded to it. Answers 204 with no body. Nothing is done to the downstream remote itself — only this repo\'s intent to push there is dropped.
         * @summary Removes one outbound mirror target; later pushes stop being forwarded to it.
         * @param {string} name Name is the repo, from the :name path segment.
         * @param {string} id ID is the row to remove, from the :id path segment.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitReposNameMirrorsId: async (name: string, id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('deleteV1GitReposNameMirrorsId', 'name', name)
            // verify required parameter 'id' is not null or undefined
            assertParamExists('deleteV1GitReposNameMirrorsId', 'id', id)
            const localVarPath = `/v1/git/repos/{name}/mirrors/{id}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)))
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel. Answers 204 with no body. An id that is not this repo\'s subscription is not found.
         * @summary Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel.
         * @param {string} name Name is the repo, from the :name path segment.
         * @param {string} id ID is the row to remove, from the :id path segment.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitReposNameSubscriptionsId: async (name: string, id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('deleteV1GitReposNameSubscriptionsId', 'name', name)
            // verify required parameter 'id' is not null or undefined
            assertParamExists('deleteV1GitReposNameSubscriptionsId', 'id', id)
            const localVarPath = `/v1/git/repos/{name}/subscriptions/{id}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)))
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Browse your org\'s repositories
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        get: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByProjectByRepoInfoRefs: async (org: string, project: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getByOrgByProjectByRepoInfoRefs', 'org', org)
            // verify required parameter 'project' is not null or undefined
            assertParamExists('getByOrgByProjectByRepoInfoRefs', 'project', project)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getByOrgByProjectByRepoInfoRefs', 'repo', repo)
            const localVarPath = `/{org}/{project}/{repo}/info/refs`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"project"}}`, encodeURIComponent(String(project)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Open a repository\'s home page
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepo: async (org: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getByOrgByRepo', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getByOrgByRepo', 'repo', repo)
            const localVarPath = `/{org}/{repo}`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary View a file in a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoBlobByWildcard1: async (org: string, repo: string, wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getByOrgByRepoBlobByWildcard1', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getByOrgByRepoBlobByWildcard1', 'repo', repo)
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('getByOrgByRepoBlobByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/{org}/{repo}/blob/{wildcard1}`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)))
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Read a repository\'s commit log
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoCommits: async (org: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getByOrgByRepoCommits', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getByOrgByRepoCommits', 'repo', repo)
            const localVarPath = `/{org}/{repo}/commits`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoInfoRefs: async (org: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getByOrgByRepoInfoRefs', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getByOrgByRepoInfoRefs', 'repo', repo)
            const localVarPath = `/{org}/{repo}/info/refs`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Browse a directory inside a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoTreeByWildcard1: async (org: string, repo: string, wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getByOrgByRepoTreeByWildcard1', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getByOrgByRepoTreeByWildcard1', 'repo', repo)
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('getByOrgByRepoTreeByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/{org}/{repo}/tree/{wildcard1}`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)))
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Discover public repositories across every org
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getExplore: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/explore`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Browse your org\'s repositories
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGit: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/git`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Open a repository\'s home page
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepo: async (org: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getGitByOrgByRepo', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getGitByOrgByRepo', 'repo', repo)
            const localVarPath = `/git/{org}/{repo}`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary View a file in a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepoBlobByWildcard1: async (org: string, repo: string, wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getGitByOrgByRepoBlobByWildcard1', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getGitByOrgByRepoBlobByWildcard1', 'repo', repo)
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('getGitByOrgByRepoBlobByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/git/{org}/{repo}/blob/{wildcard1}`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)))
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Read a repository\'s commit log
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepoCommits: async (org: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getGitByOrgByRepoCommits', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getGitByOrgByRepoCommits', 'repo', repo)
            const localVarPath = `/git/{org}/{repo}/commits`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Browse a directory inside a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepoTreeByWildcard1: async (org: string, repo: string, wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getGitByOrgByRepoTreeByWildcard1', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getGitByOrgByRepoTreeByWildcard1', 'repo', repo)
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('getGitByOrgByRepoTreeByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/git/{org}/{repo}/tree/{wildcard1}`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)))
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Discover public repositories across every org
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitExplore: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/git/explore`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitByOrgByProjectByRepoInfoRefs: async (org: string, project: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getV1GitByOrgByProjectByRepoInfoRefs', 'org', org)
            // verify required parameter 'project' is not null or undefined
            assertParamExists('getV1GitByOrgByProjectByRepoInfoRefs', 'project', project)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getV1GitByOrgByProjectByRepoInfoRefs', 'repo', repo)
            const localVarPath = `/v1/git/{org}/{project}/{repo}/info/refs`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"project"}}`, encodeURIComponent(String(project)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitByOrgByRepoInfoRefs: async (org: string, repo: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('getV1GitByOrgByRepoInfoRefs', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('getV1GitByOrgByRepoInfoRefs', 'repo', repo)
            const localVarPath = `/v1/git/{org}/{repo}/info/refs`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`. Keys are org-scoped on read even though the fingerprint index is global, so one org never sees another\'s.
         * @summary Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitKeys: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/keys`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the repos in the caller\'s scope, most recently updated first. The scope is the request principal\'s — the gateway-minted org and its optional project — never anything off the wire, so a caller only ever sees its own. Rows carry no branches or HEAD; read one repo for those.
         * @summary Returns the repos in the caller\'s scope, most recently updated first.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitRepos: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/repos`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one repo with its live ref state: every branch name and the resolved HEAD commit. Both are read from the object store on each call, so an empty repo reports no branches and an empty head rather than failing. A repo outside the caller\'s scope is not found.
         * @summary Returns one repo with its live ref state: every branch name and the resolved HEAD commit.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposName: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposName', 'name', name)
            const localVarPath = `/v1/git/repos/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one file\'s bytes at one revision. Text comes back verbatim, binary comes back base64, and a file past the 1 MiB view cap comes back marked truncated with NO content — the client is expected to clone instead.
         * @summary Returns one file\'s bytes at one revision.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {string} [path] Path is repo-relative; empty is the tree root. Traversal is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameBlob: async (name: string, ref?: string, path?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameBlob', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/blob`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (ref !== undefined) {
                localVarQueryParameter['ref'] = ref;
            }

            if (path !== undefined) {
                localVarQueryParameter['path'] = path;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Walks a ref\'s history newest first, or one path\'s history when a path is given. There is no cursor: the page is the newest `limit` commits.
         * @summary Walks a ref\'s history newest first, or one path\'s history when a path is given.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is the branch, tag or commit to walk back from; empty means HEAD.
         * @param {string} [path] Path narrows the history to commits touching it; empty walks the whole ref.
         * @param {number} [limit] Limit caps the page. Anything not positive means 50; the cap is 100.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameCommits: async (name: string, ref?: string, path?: string, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameCommits', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/commits`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (ref !== undefined) {
                localVarQueryParameter['ref'] = ref;
            }

            if (path !== undefined) {
                localVarQueryParameter['path'] = path;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from. It is the read a delivery generator makes: one call answers \"what is the inventory at this commit, and what does it say\", where listing and then fetching would be a request per file.  Returning the resolved revision matters as much as the bytes. A generator that lists at `main` and then reads at `main` can straddle a push and assemble half its inventory from one commit and half from the next; resolving once makes the whole read consistent by construction.  A file past the read cap comes back Truncated with no content rather than being dropped. A caller building a desired set has to know the difference between \"this file is empty\" and \"this file was not read\" — silently omitting it is how a pruning reconcile deletes what the missing file declared.
         * @summary Returns every file a glob selects at one revision, WITH its bytes and the revision they came from.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {string} [glob] Glob selects files, matched segment by segment so &#x60;*&#x60; never crosses a &#x60;/&#x60;. &#x60;**&#x60; matches zero or more whole segments.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameFiles: async (name: string, ref?: string, glob?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameFiles', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/files`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (ref !== undefined) {
                localVarQueryParameter['ref'] = ref;
            }

            if (glob !== undefined) {
                localVarQueryParameter['glob'] = glob;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
         * @summary Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameMirrors: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameMirrors', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/mirrors`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it. A repo with no README is not found.
         * @summary Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameReadme: async (name: string, ref?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameReadme', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/readme`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (ref !== undefined) {
                localVarQueryParameter['ref'] = ref;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call. Unlike the other read ops it tolerates a repo with no commits: the ref sets come back empty and the default branch is still named.
         * @summary Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameRefs: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameRefs', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/refs`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
         * @summary Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameSubscriptions: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameSubscriptions', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/subscriptions`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the immediate children of one directory at one revision, directories before files. It does not recurse — walk down a level at a time.
         * @summary Lists the immediate children of one directory at one revision, directories before files.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {string} [path] Path is repo-relative; empty is the tree root. Traversal is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameTree: async (name: string, ref?: string, path?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1GitReposNameTree', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/tree`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (ref !== undefined) {
                localVarQueryParameter['ref'] = ref;
            }

            if (path !== undefined) {
                localVarQueryParameter['path'] = path;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on. It spans EVERY project sub-scope, unlike the repo list, so a billing consumer sees the whole tenant footprint in one call. Sizes are last-measured values (create, push, mirror and gc each re-measure), not a live walk of the disk.
         * @summary Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitUsage: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/usage`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Flips a repo\'s public bit, the one mutable repo setting today. Public grants ANONYMOUS fetch only; push and the whole control plane stay org-authed. Returns the updated repo.
         * @summary Flips a repo\'s public bit, the one mutable repo setting today.
         * @param {string} name Name is the repo to update, from the :name path segment.
         * @param {PatchIn} patchIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1GitReposName: async (name: string, patchIn: PatchIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('patchV1GitReposName', 'name', name)
            // verify required parameter 'patchIn' is not null or undefined
            assertParamExists('patchV1GitReposName', 'patchIn', patchIn)
            const localVarPath = `/v1/git/repos/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(patchIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByProjectByRepoGitReceivePack: async (org: string, project: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postByOrgByProjectByRepoGitReceivePack', 'org', org)
            // verify required parameter 'project' is not null or undefined
            assertParamExists('postByOrgByProjectByRepoGitReceivePack', 'project', project)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postByOrgByProjectByRepoGitReceivePack', 'repo', repo)
            const localVarPath = `/{org}/{project}/{repo}/git-receive-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"project"}}`, encodeURIComponent(String(project)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByProjectByRepoGitUploadPack: async (org: string, project: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postByOrgByProjectByRepoGitUploadPack', 'org', org)
            // verify required parameter 'project' is not null or undefined
            assertParamExists('postByOrgByProjectByRepoGitUploadPack', 'project', project)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postByOrgByProjectByRepoGitUploadPack', 'repo', repo)
            const localVarPath = `/{org}/{project}/{repo}/git-upload-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"project"}}`, encodeURIComponent(String(project)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByRepoGitReceivePack: async (org: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postByOrgByRepoGitReceivePack', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postByOrgByRepoGitReceivePack', 'repo', repo)
            const localVarPath = `/{org}/{repo}/git-receive-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByRepoGitUploadPack: async (org: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postByOrgByRepoGitUploadPack', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postByOrgByRepoGitUploadPack', 'repo', repo)
            const localVarPath = `/{org}/{repo}/git-upload-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByProjectByRepoGitReceivePack: async (org: string, project: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postV1GitByOrgByProjectByRepoGitReceivePack', 'org', org)
            // verify required parameter 'project' is not null or undefined
            assertParamExists('postV1GitByOrgByProjectByRepoGitReceivePack', 'project', project)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postV1GitByOrgByProjectByRepoGitReceivePack', 'repo', repo)
            const localVarPath = `/v1/git/{org}/{project}/{repo}/git-receive-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"project"}}`, encodeURIComponent(String(project)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByProjectByRepoGitUploadPack: async (org: string, project: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postV1GitByOrgByProjectByRepoGitUploadPack', 'org', org)
            // verify required parameter 'project' is not null or undefined
            assertParamExists('postV1GitByOrgByProjectByRepoGitUploadPack', 'project', project)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postV1GitByOrgByProjectByRepoGitUploadPack', 'repo', repo)
            const localVarPath = `/v1/git/{org}/{project}/{repo}/git-upload-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"project"}}`, encodeURIComponent(String(project)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByRepoGitReceivePack: async (org: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postV1GitByOrgByRepoGitReceivePack', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postV1GitByOrgByRepoGitReceivePack', 'repo', repo)
            const localVarPath = `/v1/git/{org}/{repo}/git-receive-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByRepoGitUploadPack: async (org: string, repo: string, body?: File, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'org' is not null or undefined
            assertParamExists('postV1GitByOrgByRepoGitUploadPack', 'org', org)
            // verify required parameter 'repo' is not null or undefined
            assertParamExists('postV1GitByOrgByRepoGitUploadPack', 'repo', repo)
            const localVarPath = `/v1/git/{org}/{repo}/git-upload-pack`
                .replace(`{${"org"}}`, encodeURIComponent(String(org)))
                .replace(`{${"repo"}}`, encodeURIComponent(String(repo)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/octet-stream';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(body, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org. The key line is parsed and canonicalized before storage, its SHA256 fingerprint becomes the auth lookup handle, and the full public key round-trips (it is public). Answers 201. Fingerprints are globally unique, so a key already registered — to this org or any other — is a 409: one key belongs to exactly one org.
         * @summary Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org.
         * @param {RegisterKeyReq} registerKeyReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitKeys: async (registerKeyReq: RegisterKeyReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'registerKeyReq' is not null or undefined
            assertParamExists('postV1GitKeys', 'registerKeyReq', registerKeyReq)
            const localVarPath = `/v1/git/keys`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(registerKeyReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs. Answers 201. The name must be unique within the scope — a repeat is a 409, never a silent overwrite of an existing repo. The org comes from the validated principal, so a repo is always born owned by the caller\'s own tenant.
         * @summary Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs.
         * @param {CreateReq} createReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitRepos: async (createReq: CreateReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'createReq' is not null or undefined
            assertParamExists('postV1GitRepos', 'createReq', createReq)
            const localVarPath = `/v1/git/repos`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(createReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph. Idempotent, and safe to interrupt — git swaps both artifacts atomically. It runs under one pack slot with the same memory bounds as a clone, so it can block behind heavy pack traffic rather than compete with it. Storage usage is re-measured afterwards, since a repack reclaims space.
         * @summary Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameGc: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('postV1GitReposNameGc', 'name', name)
            const localVarPath = `/v1/git/repos/{name}/gc`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Imports an external git repository into the caller\'s repo, provisioning it on first use. Fetch is FORCED and covers every ref, so a first call clones the source and a repeat call re-syncs it — the endpoint is idempotent by mirror semantics. Mirrored bytes are metered exactly like a push, and a push.landed event is emitted for the default branch so the code index picks the repo up.
         * @summary Imports an external git repository into the caller\'s repo, provisioning it on first use.
         * @param {string} name Name is the local repo to mirror into, from the :name path segment. It is CREATED on first use.
         * @param {MirrorReq} mirrorReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameMirror: async (name: string, mirrorReq: MirrorReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('postV1GitReposNameMirror', 'name', name)
            // verify required parameter 'mirrorReq' is not null or undefined
            assertParamExists('postV1GitReposNameMirror', 'mirrorReq', mirrorReq)
            const localVarPath = `/v1/git/repos/{name}/mirror`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(mirrorReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here. Answers 201. The URL must be https to a host on the mirror allowlist (github.com / gitlab.com): the same set the mirror credential may be sent to, so a target can never capture the shared token or point the push at an internal service. Any embedded userinfo is stripped — credentials ride env-only at push time and never enter the stored URL. One mirror per host per repo; a second is a 409.
         * @summary Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here.
         * @param {string} name Name is the repo whose advanced refs are pushed downstream, from the :name path segment.
         * @param {MirrorTargetReq} mirrorTargetReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameMirrors: async (name: string, mirrorTargetReq: MirrorTargetReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('postV1GitReposNameMirrors', 'name', name)
            // verify required parameter 'mirrorTargetReq' is not null or undefined
            assertParamExists('postV1GitReposNameMirrors', 'mirrorTargetReq', mirrorTargetReq)
            const localVarPath = `/v1/git/repos/{name}/mirrors`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(mirrorTargetReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push. The repo is CREATED on first push, the files are merged onto the branch tip (unlisted files survive), and the same push-to-deploy hook a real receive-pack fires is fired, so downstream this is indistinguishable from a `git push`.
         * @summary Lands a set of files as one commit without a git client — the hanzo.app builder\'s push.
         * @param {string} name Name is the repo to push into, from the :name path segment. It is CREATED on first push if it does not exist.
         * @param {PushReq} pushReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNamePush: async (name: string, pushReq: PushReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('postV1GitReposNamePush', 'name', name)
            // verify required parameter 'pushReq' is not null or undefined
            assertParamExists('postV1GitReposNamePush', 'pushReq', pushReq)
            const localVarPath = `/v1/git/repos/{name}/push`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(pushReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there. Answers 201. The same channel twice on one repo is a 409; a repo outside the caller\'s scope is a 404, exactly as reading it is.
         * @summary Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there.
         * @param {string} name Name is the repo to subscribe, from the :name path segment.
         * @param {SubscribeReq} subscribeReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameSubscriptions: async (name: string, subscribeReq: SubscribeReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('postV1GitReposNameSubscriptions', 'name', name)
            // verify required parameter 'subscribeReq' is not null or undefined
            assertParamExists('postV1GitReposNameSubscriptions', 'subscribeReq', subscribeReq)
            const localVarPath = `/v1/git/repos/{name}/subscriptions`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(subscribeReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * GONE (410). This was the canonical forge\'s push-to-deploy door, and it never dispatched a build in its life.  It handed each verified push to cloud.OnGitPush, a single-registrant seam whose only registrant lives in apps/platform. cloud runs each app as its own OS process, so in the git process that builder is nil forever — and this handler answered 204 either way. Delivered, signature valid, green on the forge\'s hook page, and nothing built.  Push-to-deploy now belongs to POST https://platform.hanzo.ai/v1/git-webhook, which owns the build system-of-record and dispatches BuildKit Jobs. git.hanzo.ai delivers there through ONE forge-wide system webhook covering every repository; a repo opts in by committing hanzo.yml, not by owning a hook of its own.  The route is kept, and answers 410 naming that address, precisely so a misdirected delivery says what is wrong. Deleting it would 404, and a 404 here reads as \'the API is switched off\' — the wrong conclusion this estate has already drawn twice.
         * @summary Retired — forge pushes build via platform.hanzo.ai
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitWebhook: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/webhook`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Creates a repository in the caller\'s org and project scope and answers with its record. `name` is required and `description` is optional; `project` narrows the scope within the org. A name already taken in that scope is a 409 envelope and an invalid name a 400.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Create a repository over the ZAP transport
         * @param {ZapProcReq} [zapProcReq] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapCreaterepo: async (zapProcReq?: ZapProcReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/zap/createRepo`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(zapProcReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Deletes the repository named by `name` and answers with the deleted name. A repository outside the caller\'s org and project scope is a 404 envelope, so a delete can never reach another tenant\'s repository.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Delete a repository over the ZAP transport
         * @param {ZapProcReq} [zapProcReq] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapDeleterepo: async (zapProcReq?: ZapProcReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/zap/deleteRepo`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(zapProcReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers a single repository\'s record, named by `name`. A repository outside the caller\'s org and project scope is a 404 envelope, the same answer one that does not exist gets.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Read one repository over the ZAP transport
         * @param {ZapProcReq} [zapProcReq] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapGetrepo: async (zapProcReq?: ZapProcReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/zap/getRepo`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(zapProcReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers every repository in the caller\'s org and project scope. It reads NO body — the scope is entirely the caller\'s identity — so a request with an empty object is correct.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary List your repositories over the ZAP transport
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapListrepos: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/zap/listRepos`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers every repository in the caller\'s org with its size in bytes, plus the org\'s total — what git storage is actually being used, and by which repository. It reads NO body, and it is scoped to the caller\'s own org, so it is that org\'s footprint and never the fleet\'s.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Report your org\'s git storage footprint over the ZAP transport
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapUsage: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/git/zap/usage`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * GitApi - functional programming interface
 * @export
 */
export const GitApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = GitApiAxiosParamCreator(configuration)
    return {
        /**
         * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found. Answers 204 with no body. Once removed the key no longer authenticates any SSH git access.
         * @summary Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found.
         * @param {string} id ID is the key\&#39;s identifier (\&quot;gitkey_…\&quot;), from the :id path segment.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1GitKeysId(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1GitKeysId(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.deleteV1GitKeysId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes a repo\'s metadata and purges its storage. Answers 204 with no body. The metadata row is the source of truth for existence, so a storage purge that fails is logged and the delete still succeeds — and a second call is a 404, not a second delete.
         * @summary Removes a repo\'s metadata and purges its storage.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1GitReposName(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1GitReposName(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.deleteV1GitReposName']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes one outbound mirror target; later pushes stop being forwarded to it. Answers 204 with no body. Nothing is done to the downstream remote itself — only this repo\'s intent to push there is dropped.
         * @summary Removes one outbound mirror target; later pushes stop being forwarded to it.
         * @param {string} name Name is the repo, from the :name path segment.
         * @param {string} id ID is the row to remove, from the :id path segment.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1GitReposNameMirrorsId(name: string, id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1GitReposNameMirrorsId(name, id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.deleteV1GitReposNameMirrorsId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel. Answers 204 with no body. An id that is not this repo\'s subscription is not found.
         * @summary Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel.
         * @param {string} name Name is the repo, from the :name path segment.
         * @param {string} id ID is the row to remove, from the :id path segment.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1GitReposNameSubscriptionsId(name: string, id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1GitReposNameSubscriptionsId(name, id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.deleteV1GitReposNameSubscriptionsId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Browse your org\'s repositories
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async get(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.get(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.get']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getByOrgByProjectByRepoInfoRefs(org: string, project: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getByOrgByProjectByRepoInfoRefs(org, project, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getByOrgByProjectByRepoInfoRefs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Open a repository\'s home page
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getByOrgByRepo(org: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getByOrgByRepo(org, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getByOrgByRepo']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary View a file in a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getByOrgByRepoBlobByWildcard1(org: string, repo: string, wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getByOrgByRepoBlobByWildcard1(org, repo, wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getByOrgByRepoBlobByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Read a repository\'s commit log
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getByOrgByRepoCommits(org: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getByOrgByRepoCommits(org, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getByOrgByRepoCommits']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getByOrgByRepoInfoRefs(org: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getByOrgByRepoInfoRefs(org, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getByOrgByRepoInfoRefs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Browse a directory inside a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getByOrgByRepoTreeByWildcard1(org: string, repo: string, wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getByOrgByRepoTreeByWildcard1(org, repo, wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getByOrgByRepoTreeByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Discover public repositories across every org
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getExplore(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getExplore(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getExplore']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Browse your org\'s repositories
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getGit(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getGit(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getGit']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Open a repository\'s home page
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getGitByOrgByRepo(org: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getGitByOrgByRepo(org, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getGitByOrgByRepo']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary View a file in a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getGitByOrgByRepoBlobByWildcard1(org: string, repo: string, wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getGitByOrgByRepoBlobByWildcard1(org, repo, wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getGitByOrgByRepoBlobByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Read a repository\'s commit log
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getGitByOrgByRepoCommits(org: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getGitByOrgByRepoCommits(org, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getGitByOrgByRepoCommits']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Browse a directory inside a repository
         * @param {string} org 
         * @param {string} repo 
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getGitByOrgByRepoTreeByWildcard1(org: string, repo: string, wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getGitByOrgByRepoTreeByWildcard1(org, repo, wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getGitByOrgByRepoTreeByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Discover public repositories across every org
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getGitExplore(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getGitExplore(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getGitExplore']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitByOrgByProjectByRepoInfoRefs(org: string, project: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitByOrgByProjectByRepoInfoRefs(org, project, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitByOrgByProjectByRepoInfoRefs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {string} org 
         * @param {string} repo 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitByOrgByRepoInfoRefs(org: string, repo: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitByOrgByRepoInfoRefs(org, repo, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitByOrgByRepoInfoRefs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`. Keys are org-scoped on read even though the fingerprint index is global, so one org never sees another\'s.
         * @summary Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitKeys(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<KeyList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitKeys(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitKeys']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the repos in the caller\'s scope, most recently updated first. The scope is the request principal\'s — the gateway-minted org and its optional project — never anything off the wire, so a caller only ever sees its own. Rows carry no branches or HEAD; read one repo for those.
         * @summary Returns the repos in the caller\'s scope, most recently updated first.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitRepos(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RepoList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitRepos(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitRepos']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one repo with its live ref state: every branch name and the resolved HEAD commit. Both are read from the object store on each call, so an empty repo reports no branches and an empty head rather than failing. A repo outside the caller\'s scope is not found.
         * @summary Returns one repo with its live ref state: every branch name and the resolved HEAD commit.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposName(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RepoView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposName(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposName']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one file\'s bytes at one revision. Text comes back verbatim, binary comes back base64, and a file past the 1 MiB view cap comes back marked truncated with NO content — the client is expected to clone instead.
         * @summary Returns one file\'s bytes at one revision.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {string} [path] Path is repo-relative; empty is the tree root. Traversal is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameBlob(name: string, ref?: string, path?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<BlobJSON>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameBlob(name, ref, path, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameBlob']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Walks a ref\'s history newest first, or one path\'s history when a path is given. There is no cursor: the page is the newest `limit` commits.
         * @summary Walks a ref\'s history newest first, or one path\'s history when a path is given.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is the branch, tag or commit to walk back from; empty means HEAD.
         * @param {string} [path] Path narrows the history to commits touching it; empty walks the whole ref.
         * @param {number} [limit] Limit caps the page. Anything not positive means 50; the cap is 100.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameCommits(name: string, ref?: string, path?: string, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CommitsJSON>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameCommits(name, ref, path, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameCommits']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from. It is the read a delivery generator makes: one call answers \"what is the inventory at this commit, and what does it say\", where listing and then fetching would be a request per file.  Returning the resolved revision matters as much as the bytes. A generator that lists at `main` and then reads at `main` can straddle a push and assemble half its inventory from one commit and half from the next; resolving once makes the whole read consistent by construction.  A file past the read cap comes back Truncated with no content rather than being dropped. A caller building a desired set has to know the difference between \"this file is empty\" and \"this file was not read\" — silently omitting it is how a pruning reconcile deletes what the missing file declared.
         * @summary Returns every file a glob selects at one revision, WITH its bytes and the revision they came from.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {string} [glob] Glob selects files, matched segment by segment so &#x60;*&#x60; never crosses a &#x60;/&#x60;. &#x60;**&#x60; matches zero or more whole segments.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameFiles(name: string, ref?: string, glob?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<FilesJSON>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameFiles(name, ref, glob, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameFiles']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
         * @summary Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameMirrors(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MirrorList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameMirrors(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameMirrors']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it. A repo with no README is not found.
         * @summary Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameReadme(name: string, ref?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ReadmeJSON>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameReadme(name, ref, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameReadme']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call. Unlike the other read ops it tolerates a repo with no commits: the ref sets come back empty and the default branch is still named.
         * @summary Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameRefs(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RefsJSON>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameRefs(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameRefs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
         * @summary Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameSubscriptions(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SubscriptionList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameSubscriptions(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameSubscriptions']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the immediate children of one directory at one revision, directories before files. It does not recurse — walk down a level at a time.
         * @summary Lists the immediate children of one directory at one revision, directories before files.
         * @param {string} name Name is the repo to read, from the :name path segment.
         * @param {string} [ref] Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
         * @param {string} [path] Path is repo-relative; empty is the tree root. Traversal is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitReposNameTree(name: string, ref?: string, path?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<TreeJSON>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitReposNameTree(name, ref, path, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitReposNameTree']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on. It spans EVERY project sub-scope, unlike the repo list, so a billing consumer sees the whole tenant footprint in one call. Sizes are last-measured values (create, push, mirror and gc each re-measure), not a live walk of the disk.
         * @summary Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1GitUsage(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<UsageView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1GitUsage(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.getV1GitUsage']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Flips a repo\'s public bit, the one mutable repo setting today. Public grants ANONYMOUS fetch only; push and the whole control plane stay org-authed. Returns the updated repo.
         * @summary Flips a repo\'s public bit, the one mutable repo setting today.
         * @param {string} name Name is the repo to update, from the :name path segment.
         * @param {PatchIn} patchIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchV1GitReposName(name: string, patchIn: PatchIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RepoView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchV1GitReposName(name, patchIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.patchV1GitReposName']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postByOrgByProjectByRepoGitReceivePack(org: string, project: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postByOrgByProjectByRepoGitReceivePack(org, project, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postByOrgByProjectByRepoGitReceivePack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postByOrgByProjectByRepoGitUploadPack(org: string, project: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postByOrgByProjectByRepoGitUploadPack(org, project, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postByOrgByProjectByRepoGitUploadPack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postByOrgByRepoGitReceivePack(org: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postByOrgByRepoGitReceivePack(org, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postByOrgByRepoGitReceivePack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postByOrgByRepoGitUploadPack(org: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postByOrgByRepoGitUploadPack(org, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postByOrgByRepoGitUploadPack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitByOrgByProjectByRepoGitReceivePack(org: string, project: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitByOrgByProjectByRepoGitReceivePack(org, project, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitByOrgByProjectByRepoGitReceivePack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} project 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitByOrgByProjectByRepoGitUploadPack(org: string, project: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitByOrgByProjectByRepoGitUploadPack(org, project, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitByOrgByProjectByRepoGitUploadPack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitByOrgByRepoGitReceivePack(org: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitByOrgByRepoGitReceivePack(org, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitByOrgByRepoGitReceivePack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {string} org 
         * @param {string} repo 
         * @param {File} [body] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitByOrgByRepoGitUploadPack(org: string, repo: string, body?: File, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitByOrgByRepoGitUploadPack(org, repo, body, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitByOrgByRepoGitUploadPack']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org. The key line is parsed and canonicalized before storage, its SHA256 fingerprint becomes the auth lookup handle, and the full public key round-trips (it is public). Answers 201. Fingerprints are globally unique, so a key already registered — to this org or any other — is a 409: one key belongs to exactly one org.
         * @summary Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org.
         * @param {RegisterKeyReq} registerKeyReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitKeys(registerKeyReq: RegisterKeyReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<KeyView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitKeys(registerKeyReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitKeys']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs. Answers 201. The name must be unique within the scope — a repeat is a 409, never a silent overwrite of an existing repo. The org comes from the validated principal, so a repo is always born owned by the caller\'s own tenant.
         * @summary Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs.
         * @param {CreateReq} createReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitRepos(createReq: CreateReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RepoView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitRepos(createReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitRepos']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph. Idempotent, and safe to interrupt — git swaps both artifacts atomically. It runs under one pack slot with the same memory bounds as a clone, so it can block behind heavy pack traffic rather than compete with it. Storage usage is re-measured afterwards, since a repack reclaims space.
         * @summary Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph.
         * @param {string} name Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitReposNameGc(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<GcOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitReposNameGc(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitReposNameGc']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Imports an external git repository into the caller\'s repo, provisioning it on first use. Fetch is FORCED and covers every ref, so a first call clones the source and a repeat call re-syncs it — the endpoint is idempotent by mirror semantics. Mirrored bytes are metered exactly like a push, and a push.landed event is emitted for the default branch so the code index picks the repo up.
         * @summary Imports an external git repository into the caller\'s repo, provisioning it on first use.
         * @param {string} name Name is the local repo to mirror into, from the :name path segment. It is CREATED on first use.
         * @param {MirrorReq} mirrorReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitReposNameMirror(name: string, mirrorReq: MirrorReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RepoView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitReposNameMirror(name, mirrorReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitReposNameMirror']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here. Answers 201. The URL must be https to a host on the mirror allowlist (github.com / gitlab.com): the same set the mirror credential may be sent to, so a target can never capture the shared token or point the push at an internal service. Any embedded userinfo is stripped — credentials ride env-only at push time and never enter the stored URL. One mirror per host per repo; a second is a 409.
         * @summary Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here.
         * @param {string} name Name is the repo whose advanced refs are pushed downstream, from the :name path segment.
         * @param {MirrorTargetReq} mirrorTargetReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitReposNameMirrors(name: string, mirrorTargetReq: MirrorTargetReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MirrorTargetView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitReposNameMirrors(name, mirrorTargetReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitReposNameMirrors']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push. The repo is CREATED on first push, the files are merged onto the branch tip (unlisted files survive), and the same push-to-deploy hook a real receive-pack fires is fired, so downstream this is indistinguishable from a `git push`.
         * @summary Lands a set of files as one commit without a git client — the hanzo.app builder\'s push.
         * @param {string} name Name is the repo to push into, from the :name path segment. It is CREATED on first push if it does not exist.
         * @param {PushReq} pushReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitReposNamePush(name: string, pushReq: PushReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<PushResp>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitReposNamePush(name, pushReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitReposNamePush']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there. Answers 201. The same channel twice on one repo is a 409; a repo outside the caller\'s scope is a 404, exactly as reading it is.
         * @summary Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there.
         * @param {string} name Name is the repo to subscribe, from the :name path segment.
         * @param {SubscribeReq} subscribeReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitReposNameSubscriptions(name: string, subscribeReq: SubscribeReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SubscriptionView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitReposNameSubscriptions(name, subscribeReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitReposNameSubscriptions']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * GONE (410). This was the canonical forge\'s push-to-deploy door, and it never dispatched a build in its life.  It handed each verified push to cloud.OnGitPush, a single-registrant seam whose only registrant lives in apps/platform. cloud runs each app as its own OS process, so in the git process that builder is nil forever — and this handler answered 204 either way. Delivered, signature valid, green on the forge\'s hook page, and nothing built.  Push-to-deploy now belongs to POST https://platform.hanzo.ai/v1/git-webhook, which owns the build system-of-record and dispatches BuildKit Jobs. git.hanzo.ai delivers there through ONE forge-wide system webhook covering every repository; a repo opts in by committing hanzo.yml, not by owning a hook of its own.  The route is kept, and answers 410 naming that address, precisely so a misdirected delivery says what is wrong. Deleting it would 404, and a 404 here reads as \'the API is switched off\' — the wrong conclusion this estate has already drawn twice.
         * @summary Retired — forge pushes build via platform.hanzo.ai
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitWebhook(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitWebhook(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitWebhook']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Creates a repository in the caller\'s org and project scope and answers with its record. `name` is required and `description` is optional; `project` narrows the scope within the org. A name already taken in that scope is a 409 envelope and an invalid name a 400.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Create a repository over the ZAP transport
         * @param {ZapProcReq} [zapProcReq] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitZapCreaterepo(zapProcReq?: ZapProcReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitZapCreaterepo(zapProcReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitZapCreaterepo']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Deletes the repository named by `name` and answers with the deleted name. A repository outside the caller\'s org and project scope is a 404 envelope, so a delete can never reach another tenant\'s repository.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Delete a repository over the ZAP transport
         * @param {ZapProcReq} [zapProcReq] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitZapDeleterepo(zapProcReq?: ZapProcReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitZapDeleterepo(zapProcReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitZapDeleterepo']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers a single repository\'s record, named by `name`. A repository outside the caller\'s org and project scope is a 404 envelope, the same answer one that does not exist gets.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Read one repository over the ZAP transport
         * @param {ZapProcReq} [zapProcReq] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitZapGetrepo(zapProcReq?: ZapProcReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitZapGetrepo(zapProcReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitZapGetrepo']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers every repository in the caller\'s org and project scope. It reads NO body — the scope is entirely the caller\'s identity — so a request with an empty object is correct.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary List your repositories over the ZAP transport
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitZapListrepos(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitZapListrepos(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitZapListrepos']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers every repository in the caller\'s org with its size in bytes, plus the org\'s total — what git storage is actually being used, and by which repository. It reads NO body, and it is scoped to the caller\'s own org, so it is that org\'s footprint and never the fleet\'s.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Report your org\'s git storage footprint over the ZAP transport
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1GitZapUsage(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1GitZapUsage(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GitApi.postV1GitZapUsage']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * GitApi - factory interface
 * @export
 */
export const GitApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = GitApiFp(configuration)
    return {
        /**
         * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found. Answers 204 with no body. Once removed the key no longer authenticates any SSH git access.
         * @summary Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found.
         * @param {GitApiDeleteV1GitKeysIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitKeysId(requestParameters: GitApiDeleteV1GitKeysIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteV1GitKeysId(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Removes a repo\'s metadata and purges its storage. Answers 204 with no body. The metadata row is the source of truth for existence, so a storage purge that fails is logged and the delete still succeeds — and a second call is a 404, not a second delete.
         * @summary Removes a repo\'s metadata and purges its storage.
         * @param {GitApiDeleteV1GitReposNameRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitReposName(requestParameters: GitApiDeleteV1GitReposNameRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteV1GitReposName(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Removes one outbound mirror target; later pushes stop being forwarded to it. Answers 204 with no body. Nothing is done to the downstream remote itself — only this repo\'s intent to push there is dropped.
         * @summary Removes one outbound mirror target; later pushes stop being forwarded to it.
         * @param {GitApiDeleteV1GitReposNameMirrorsIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitReposNameMirrorsId(requestParameters: GitApiDeleteV1GitReposNameMirrorsIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteV1GitReposNameMirrorsId(requestParameters.name, requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel. Answers 204 with no body. An id that is not this repo\'s subscription is not found.
         * @summary Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel.
         * @param {GitApiDeleteV1GitReposNameSubscriptionsIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1GitReposNameSubscriptionsId(requestParameters: GitApiDeleteV1GitReposNameSubscriptionsIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteV1GitReposNameSubscriptionsId(requestParameters.name, requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Browse your org\'s repositories
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        get(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.get(options).then((request) => request(axios, basePath));
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {GitApiGetByOrgByProjectByRepoInfoRefsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByProjectByRepoInfoRefs(requestParameters: GitApiGetByOrgByProjectByRepoInfoRefsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getByOrgByProjectByRepoInfoRefs(requestParameters.org, requestParameters.project, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Open a repository\'s home page
         * @param {GitApiGetByOrgByRepoRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepo(requestParameters: GitApiGetByOrgByRepoRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getByOrgByRepo(requestParameters.org, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary View a file in a repository
         * @param {GitApiGetByOrgByRepoBlobByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoBlobByWildcard1(requestParameters: GitApiGetByOrgByRepoBlobByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getByOrgByRepoBlobByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Read a repository\'s commit log
         * @param {GitApiGetByOrgByRepoCommitsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoCommits(requestParameters: GitApiGetByOrgByRepoCommitsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getByOrgByRepoCommits(requestParameters.org, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {GitApiGetByOrgByRepoInfoRefsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoInfoRefs(requestParameters: GitApiGetByOrgByRepoInfoRefsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getByOrgByRepoInfoRefs(requestParameters.org, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Browse a directory inside a repository
         * @param {GitApiGetByOrgByRepoTreeByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getByOrgByRepoTreeByWildcard1(requestParameters: GitApiGetByOrgByRepoTreeByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getByOrgByRepoTreeByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
         * @summary Discover public repositories across every org
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getExplore(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getExplore(options).then((request) => request(axios, basePath));
        },
        /**
         * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Browse your org\'s repositories
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGit(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getGit(options).then((request) => request(axios, basePath));
        },
        /**
         * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Open a repository\'s home page
         * @param {GitApiGetGitByOrgByRepoRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepo(requestParameters: GitApiGetGitByOrgByRepoRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getGitByOrgByRepo(requestParameters.org, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary View a file in a repository
         * @param {GitApiGetGitByOrgByRepoBlobByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepoBlobByWildcard1(requestParameters: GitApiGetGitByOrgByRepoBlobByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getGitByOrgByRepoBlobByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Read a repository\'s commit log
         * @param {GitApiGetGitByOrgByRepoCommitsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepoCommits(requestParameters: GitApiGetGitByOrgByRepoCommitsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getGitByOrgByRepoCommits(requestParameters.org, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Browse a directory inside a repository
         * @param {GitApiGetGitByOrgByRepoTreeByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitByOrgByRepoTreeByWildcard1(requestParameters: GitApiGetGitByOrgByRepoTreeByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getGitByOrgByRepoTreeByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
         * @summary Discover public repositories across every org
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getGitExplore(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getGitExplore(options).then((request) => request(axios, basePath));
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {GitApiGetV1GitByOrgByProjectByRepoInfoRefsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitByOrgByProjectByRepoInfoRefs(requestParameters: GitApiGetV1GitByOrgByProjectByRepoInfoRefsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getV1GitByOrgByProjectByRepoInfoRefs(requestParameters.org, requestParameters.project, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Advertise a repository\'s refs to a git client
         * @param {GitApiGetV1GitByOrgByRepoInfoRefsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitByOrgByRepoInfoRefs(requestParameters: GitApiGetV1GitByOrgByRepoInfoRefsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getV1GitByOrgByRepoInfoRefs(requestParameters.org, requestParameters.repo, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`. Keys are org-scoped on read even though the fingerprint index is global, so one org never sees another\'s.
         * @summary Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitKeys(options?: RawAxiosRequestConfig): AxiosPromise<KeyList> {
            return localVarFp.getV1GitKeys(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the repos in the caller\'s scope, most recently updated first. The scope is the request principal\'s — the gateway-minted org and its optional project — never anything off the wire, so a caller only ever sees its own. Rows carry no branches or HEAD; read one repo for those.
         * @summary Returns the repos in the caller\'s scope, most recently updated first.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitRepos(options?: RawAxiosRequestConfig): AxiosPromise<RepoList> {
            return localVarFp.getV1GitRepos(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one repo with its live ref state: every branch name and the resolved HEAD commit. Both are read from the object store on each call, so an empty repo reports no branches and an empty head rather than failing. A repo outside the caller\'s scope is not found.
         * @summary Returns one repo with its live ref state: every branch name and the resolved HEAD commit.
         * @param {GitApiGetV1GitReposNameRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposName(requestParameters: GitApiGetV1GitReposNameRequest, options?: RawAxiosRequestConfig): AxiosPromise<RepoView> {
            return localVarFp.getV1GitReposName(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one file\'s bytes at one revision. Text comes back verbatim, binary comes back base64, and a file past the 1 MiB view cap comes back marked truncated with NO content — the client is expected to clone instead.
         * @summary Returns one file\'s bytes at one revision.
         * @param {GitApiGetV1GitReposNameBlobRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameBlob(requestParameters: GitApiGetV1GitReposNameBlobRequest, options?: RawAxiosRequestConfig): AxiosPromise<BlobJSON> {
            return localVarFp.getV1GitReposNameBlob(requestParameters.name, requestParameters.ref, requestParameters.path, options).then((request) => request(axios, basePath));
        },
        /**
         * Walks a ref\'s history newest first, or one path\'s history when a path is given. There is no cursor: the page is the newest `limit` commits.
         * @summary Walks a ref\'s history newest first, or one path\'s history when a path is given.
         * @param {GitApiGetV1GitReposNameCommitsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameCommits(requestParameters: GitApiGetV1GitReposNameCommitsRequest, options?: RawAxiosRequestConfig): AxiosPromise<CommitsJSON> {
            return localVarFp.getV1GitReposNameCommits(requestParameters.name, requestParameters.ref, requestParameters.path, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from. It is the read a delivery generator makes: one call answers \"what is the inventory at this commit, and what does it say\", where listing and then fetching would be a request per file.  Returning the resolved revision matters as much as the bytes. A generator that lists at `main` and then reads at `main` can straddle a push and assemble half its inventory from one commit and half from the next; resolving once makes the whole read consistent by construction.  A file past the read cap comes back Truncated with no content rather than being dropped. A caller building a desired set has to know the difference between \"this file is empty\" and \"this file was not read\" — silently omitting it is how a pruning reconcile deletes what the missing file declared.
         * @summary Returns every file a glob selects at one revision, WITH its bytes and the revision they came from.
         * @param {GitApiGetV1GitReposNameFilesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameFiles(requestParameters: GitApiGetV1GitReposNameFilesRequest, options?: RawAxiosRequestConfig): AxiosPromise<FilesJSON> {
            return localVarFp.getV1GitReposNameFiles(requestParameters.name, requestParameters.ref, requestParameters.glob, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
         * @summary Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
         * @param {GitApiGetV1GitReposNameMirrorsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameMirrors(requestParameters: GitApiGetV1GitReposNameMirrorsRequest, options?: RawAxiosRequestConfig): AxiosPromise<MirrorList> {
            return localVarFp.getV1GitReposNameMirrors(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it. A repo with no README is not found.
         * @summary Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it.
         * @param {GitApiGetV1GitReposNameReadmeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameReadme(requestParameters: GitApiGetV1GitReposNameReadmeRequest, options?: RawAxiosRequestConfig): AxiosPromise<ReadmeJSON> {
            return localVarFp.getV1GitReposNameReadme(requestParameters.name, requestParameters.ref, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call. Unlike the other read ops it tolerates a repo with no commits: the ref sets come back empty and the default branch is still named.
         * @summary Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call.
         * @param {GitApiGetV1GitReposNameRefsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameRefs(requestParameters: GitApiGetV1GitReposNameRefsRequest, options?: RawAxiosRequestConfig): AxiosPromise<RefsJSON> {
            return localVarFp.getV1GitReposNameRefs(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
         * @summary Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
         * @param {GitApiGetV1GitReposNameSubscriptionsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameSubscriptions(requestParameters: GitApiGetV1GitReposNameSubscriptionsRequest, options?: RawAxiosRequestConfig): AxiosPromise<SubscriptionList> {
            return localVarFp.getV1GitReposNameSubscriptions(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the immediate children of one directory at one revision, directories before files. It does not recurse — walk down a level at a time.
         * @summary Lists the immediate children of one directory at one revision, directories before files.
         * @param {GitApiGetV1GitReposNameTreeRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitReposNameTree(requestParameters: GitApiGetV1GitReposNameTreeRequest, options?: RawAxiosRequestConfig): AxiosPromise<TreeJSON> {
            return localVarFp.getV1GitReposNameTree(requestParameters.name, requestParameters.ref, requestParameters.path, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on. It spans EVERY project sub-scope, unlike the repo list, so a billing consumer sees the whole tenant footprint in one call. Sizes are last-measured values (create, push, mirror and gc each re-measure), not a live walk of the disk.
         * @summary Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1GitUsage(options?: RawAxiosRequestConfig): AxiosPromise<UsageView> {
            return localVarFp.getV1GitUsage(options).then((request) => request(axios, basePath));
        },
        /**
         * Flips a repo\'s public bit, the one mutable repo setting today. Public grants ANONYMOUS fetch only; push and the whole control plane stay org-authed. Returns the updated repo.
         * @summary Flips a repo\'s public bit, the one mutable repo setting today.
         * @param {GitApiPatchV1GitReposNameRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1GitReposName(requestParameters: GitApiPatchV1GitReposNameRequest, options?: RawAxiosRequestConfig): AxiosPromise<RepoView> {
            return localVarFp.patchV1GitReposName(requestParameters.name, requestParameters.patchIn, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {GitApiPostByOrgByProjectByRepoGitReceivePackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByProjectByRepoGitReceivePack(requestParameters: GitApiPostByOrgByProjectByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postByOrgByProjectByRepoGitReceivePack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {GitApiPostByOrgByProjectByRepoGitUploadPackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByProjectByRepoGitUploadPack(requestParameters: GitApiPostByOrgByProjectByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postByOrgByProjectByRepoGitUploadPack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {GitApiPostByOrgByRepoGitReceivePackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByRepoGitReceivePack(requestParameters: GitApiPostByOrgByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postByOrgByRepoGitReceivePack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {GitApiPostByOrgByRepoGitUploadPackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postByOrgByRepoGitUploadPack(requestParameters: GitApiPostByOrgByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postByOrgByRepoGitUploadPack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {GitApiPostV1GitByOrgByProjectByRepoGitReceivePackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByProjectByRepoGitReceivePack(requestParameters: GitApiPostV1GitByOrgByProjectByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitByOrgByProjectByRepoGitReceivePack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {GitApiPostV1GitByOrgByProjectByRepoGitUploadPackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByProjectByRepoGitUploadPack(requestParameters: GitApiPostV1GitByOrgByProjectByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitByOrgByProjectByRepoGitUploadPack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Accept a push, and turn it into a build
         * @param {GitApiPostV1GitByOrgByRepoGitReceivePackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByRepoGitReceivePack(requestParameters: GitApiPostV1GitByOrgByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitByOrgByRepoGitReceivePack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
         * @summary Serve a clone or fetch
         * @param {GitApiPostV1GitByOrgByRepoGitUploadPackRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitByOrgByRepoGitUploadPack(requestParameters: GitApiPostV1GitByOrgByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitByOrgByRepoGitUploadPack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(axios, basePath));
        },
        /**
         * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org. The key line is parsed and canonicalized before storage, its SHA256 fingerprint becomes the auth lookup handle, and the full public key round-trips (it is public). Answers 201. Fingerprints are globally unique, so a key already registered — to this org or any other — is a 409: one key belongs to exactly one org.
         * @summary Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org.
         * @param {GitApiPostV1GitKeysRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitKeys(requestParameters: GitApiPostV1GitKeysRequest, options?: RawAxiosRequestConfig): AxiosPromise<KeyView> {
            return localVarFp.postV1GitKeys(requestParameters.registerKeyReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs. Answers 201. The name must be unique within the scope — a repeat is a 409, never a silent overwrite of an existing repo. The org comes from the validated principal, so a repo is always born owned by the caller\'s own tenant.
         * @summary Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs.
         * @param {GitApiPostV1GitReposRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitRepos(requestParameters: GitApiPostV1GitReposRequest, options?: RawAxiosRequestConfig): AxiosPromise<RepoView> {
            return localVarFp.postV1GitRepos(requestParameters.createReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph. Idempotent, and safe to interrupt — git swaps both artifacts atomically. It runs under one pack slot with the same memory bounds as a clone, so it can block behind heavy pack traffic rather than compete with it. Storage usage is re-measured afterwards, since a repack reclaims space.
         * @summary Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph.
         * @param {GitApiPostV1GitReposNameGcRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameGc(requestParameters: GitApiPostV1GitReposNameGcRequest, options?: RawAxiosRequestConfig): AxiosPromise<GcOut> {
            return localVarFp.postV1GitReposNameGc(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Imports an external git repository into the caller\'s repo, provisioning it on first use. Fetch is FORCED and covers every ref, so a first call clones the source and a repeat call re-syncs it — the endpoint is idempotent by mirror semantics. Mirrored bytes are metered exactly like a push, and a push.landed event is emitted for the default branch so the code index picks the repo up.
         * @summary Imports an external git repository into the caller\'s repo, provisioning it on first use.
         * @param {GitApiPostV1GitReposNameMirrorRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameMirror(requestParameters: GitApiPostV1GitReposNameMirrorRequest, options?: RawAxiosRequestConfig): AxiosPromise<RepoView> {
            return localVarFp.postV1GitReposNameMirror(requestParameters.name, requestParameters.mirrorReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here. Answers 201. The URL must be https to a host on the mirror allowlist (github.com / gitlab.com): the same set the mirror credential may be sent to, so a target can never capture the shared token or point the push at an internal service. Any embedded userinfo is stripped — credentials ride env-only at push time and never enter the stored URL. One mirror per host per repo; a second is a 409.
         * @summary Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here.
         * @param {GitApiPostV1GitReposNameMirrorsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameMirrors(requestParameters: GitApiPostV1GitReposNameMirrorsRequest, options?: RawAxiosRequestConfig): AxiosPromise<MirrorTargetView> {
            return localVarFp.postV1GitReposNameMirrors(requestParameters.name, requestParameters.mirrorTargetReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push. The repo is CREATED on first push, the files are merged onto the branch tip (unlisted files survive), and the same push-to-deploy hook a real receive-pack fires is fired, so downstream this is indistinguishable from a `git push`.
         * @summary Lands a set of files as one commit without a git client — the hanzo.app builder\'s push.
         * @param {GitApiPostV1GitReposNamePushRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNamePush(requestParameters: GitApiPostV1GitReposNamePushRequest, options?: RawAxiosRequestConfig): AxiosPromise<PushResp> {
            return localVarFp.postV1GitReposNamePush(requestParameters.name, requestParameters.pushReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there. Answers 201. The same channel twice on one repo is a 409; a repo outside the caller\'s scope is a 404, exactly as reading it is.
         * @summary Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there.
         * @param {GitApiPostV1GitReposNameSubscriptionsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitReposNameSubscriptions(requestParameters: GitApiPostV1GitReposNameSubscriptionsRequest, options?: RawAxiosRequestConfig): AxiosPromise<SubscriptionView> {
            return localVarFp.postV1GitReposNameSubscriptions(requestParameters.name, requestParameters.subscribeReq, options).then((request) => request(axios, basePath));
        },
        /**
         * GONE (410). This was the canonical forge\'s push-to-deploy door, and it never dispatched a build in its life.  It handed each verified push to cloud.OnGitPush, a single-registrant seam whose only registrant lives in apps/platform. cloud runs each app as its own OS process, so in the git process that builder is nil forever — and this handler answered 204 either way. Delivered, signature valid, green on the forge\'s hook page, and nothing built.  Push-to-deploy now belongs to POST https://platform.hanzo.ai/v1/git-webhook, which owns the build system-of-record and dispatches BuildKit Jobs. git.hanzo.ai delivers there through ONE forge-wide system webhook covering every repository; a repo opts in by committing hanzo.yml, not by owning a hook of its own.  The route is kept, and answers 410 naming that address, precisely so a misdirected delivery says what is wrong. Deleting it would 404, and a 404 here reads as \'the API is switched off\' — the wrong conclusion this estate has already drawn twice.
         * @summary Retired — forge pushes build via platform.hanzo.ai
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitWebhook(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitWebhook(options).then((request) => request(axios, basePath));
        },
        /**
         * Creates a repository in the caller\'s org and project scope and answers with its record. `name` is required and `description` is optional; `project` narrows the scope within the org. A name already taken in that scope is a 409 envelope and an invalid name a 400.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Create a repository over the ZAP transport
         * @param {GitApiPostV1GitZapCreaterepoRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapCreaterepo(requestParameters: GitApiPostV1GitZapCreaterepoRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitZapCreaterepo(requestParameters.zapProcReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Deletes the repository named by `name` and answers with the deleted name. A repository outside the caller\'s org and project scope is a 404 envelope, so a delete can never reach another tenant\'s repository.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Delete a repository over the ZAP transport
         * @param {GitApiPostV1GitZapDeleterepoRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapDeleterepo(requestParameters: GitApiPostV1GitZapDeleterepoRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitZapDeleterepo(requestParameters.zapProcReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers a single repository\'s record, named by `name`. A repository outside the caller\'s org and project scope is a 404 envelope, the same answer one that does not exist gets.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Read one repository over the ZAP transport
         * @param {GitApiPostV1GitZapGetrepoRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapGetrepo(requestParameters: GitApiPostV1GitZapGetrepoRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitZapGetrepo(requestParameters.zapProcReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers every repository in the caller\'s org and project scope. It reads NO body — the scope is entirely the caller\'s identity — so a request with an empty object is correct.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary List your repositories over the ZAP transport
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapListrepos(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitZapListrepos(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers every repository in the caller\'s org with its size in bytes, plus the org\'s total — what git storage is actually being used, and by which repository. It reads NO body, and it is scoped to the caller\'s own org, so it is that org\'s footprint and never the fleet\'s.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
         * @summary Report your org\'s git storage footprint over the ZAP transport
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1GitZapUsage(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1GitZapUsage(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteV1GitKeysId operation in GitApi.
 * @export
 * @interface GitApiDeleteV1GitKeysIdRequest
 */
export interface GitApiDeleteV1GitKeysIdRequest {
    /**
     * ID is the key\&#39;s identifier (\&quot;gitkey_…\&quot;), from the :id path segment.
     * @type {string}
     * @memberof GitApiDeleteV1GitKeysId
     */
    readonly id: string
}

/**
 * Request parameters for deleteV1GitReposName operation in GitApi.
 * @export
 * @interface GitApiDeleteV1GitReposNameRequest
 */
export interface GitApiDeleteV1GitReposNameRequest {
    /**
     * Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
     * @type {string}
     * @memberof GitApiDeleteV1GitReposName
     */
    readonly name: string
}

/**
 * Request parameters for deleteV1GitReposNameMirrorsId operation in GitApi.
 * @export
 * @interface GitApiDeleteV1GitReposNameMirrorsIdRequest
 */
export interface GitApiDeleteV1GitReposNameMirrorsIdRequest {
    /**
     * Name is the repo, from the :name path segment.
     * @type {string}
     * @memberof GitApiDeleteV1GitReposNameMirrorsId
     */
    readonly name: string

    /**
     * ID is the row to remove, from the :id path segment.
     * @type {string}
     * @memberof GitApiDeleteV1GitReposNameMirrorsId
     */
    readonly id: string
}

/**
 * Request parameters for deleteV1GitReposNameSubscriptionsId operation in GitApi.
 * @export
 * @interface GitApiDeleteV1GitReposNameSubscriptionsIdRequest
 */
export interface GitApiDeleteV1GitReposNameSubscriptionsIdRequest {
    /**
     * Name is the repo, from the :name path segment.
     * @type {string}
     * @memberof GitApiDeleteV1GitReposNameSubscriptionsId
     */
    readonly name: string

    /**
     * ID is the row to remove, from the :id path segment.
     * @type {string}
     * @memberof GitApiDeleteV1GitReposNameSubscriptionsId
     */
    readonly id: string
}

/**
 * Request parameters for getByOrgByProjectByRepoInfoRefs operation in GitApi.
 * @export
 * @interface GitApiGetByOrgByProjectByRepoInfoRefsRequest
 */
export interface GitApiGetByOrgByProjectByRepoInfoRefsRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByProjectByRepoInfoRefs
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByProjectByRepoInfoRefs
     */
    readonly project: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByProjectByRepoInfoRefs
     */
    readonly repo: string
}

/**
 * Request parameters for getByOrgByRepo operation in GitApi.
 * @export
 * @interface GitApiGetByOrgByRepoRequest
 */
export interface GitApiGetByOrgByRepoRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepo
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepo
     */
    readonly repo: string
}

/**
 * Request parameters for getByOrgByRepoBlobByWildcard1 operation in GitApi.
 * @export
 * @interface GitApiGetByOrgByRepoBlobByWildcard1Request
 */
export interface GitApiGetByOrgByRepoBlobByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoBlobByWildcard1
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoBlobByWildcard1
     */
    readonly repo: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoBlobByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for getByOrgByRepoCommits operation in GitApi.
 * @export
 * @interface GitApiGetByOrgByRepoCommitsRequest
 */
export interface GitApiGetByOrgByRepoCommitsRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoCommits
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoCommits
     */
    readonly repo: string
}

/**
 * Request parameters for getByOrgByRepoInfoRefs operation in GitApi.
 * @export
 * @interface GitApiGetByOrgByRepoInfoRefsRequest
 */
export interface GitApiGetByOrgByRepoInfoRefsRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoInfoRefs
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoInfoRefs
     */
    readonly repo: string
}

/**
 * Request parameters for getByOrgByRepoTreeByWildcard1 operation in GitApi.
 * @export
 * @interface GitApiGetByOrgByRepoTreeByWildcard1Request
 */
export interface GitApiGetByOrgByRepoTreeByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoTreeByWildcard1
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoTreeByWildcard1
     */
    readonly repo: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetByOrgByRepoTreeByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for getGitByOrgByRepo operation in GitApi.
 * @export
 * @interface GitApiGetGitByOrgByRepoRequest
 */
export interface GitApiGetGitByOrgByRepoRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepo
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepo
     */
    readonly repo: string
}

/**
 * Request parameters for getGitByOrgByRepoBlobByWildcard1 operation in GitApi.
 * @export
 * @interface GitApiGetGitByOrgByRepoBlobByWildcard1Request
 */
export interface GitApiGetGitByOrgByRepoBlobByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoBlobByWildcard1
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoBlobByWildcard1
     */
    readonly repo: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoBlobByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for getGitByOrgByRepoCommits operation in GitApi.
 * @export
 * @interface GitApiGetGitByOrgByRepoCommitsRequest
 */
export interface GitApiGetGitByOrgByRepoCommitsRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoCommits
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoCommits
     */
    readonly repo: string
}

/**
 * Request parameters for getGitByOrgByRepoTreeByWildcard1 operation in GitApi.
 * @export
 * @interface GitApiGetGitByOrgByRepoTreeByWildcard1Request
 */
export interface GitApiGetGitByOrgByRepoTreeByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoTreeByWildcard1
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoTreeByWildcard1
     */
    readonly repo: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetGitByOrgByRepoTreeByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for getV1GitByOrgByProjectByRepoInfoRefs operation in GitApi.
 * @export
 * @interface GitApiGetV1GitByOrgByProjectByRepoInfoRefsRequest
 */
export interface GitApiGetV1GitByOrgByProjectByRepoInfoRefsRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetV1GitByOrgByProjectByRepoInfoRefs
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetV1GitByOrgByProjectByRepoInfoRefs
     */
    readonly project: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetV1GitByOrgByProjectByRepoInfoRefs
     */
    readonly repo: string
}

/**
 * Request parameters for getV1GitByOrgByRepoInfoRefs operation in GitApi.
 * @export
 * @interface GitApiGetV1GitByOrgByRepoInfoRefsRequest
 */
export interface GitApiGetV1GitByOrgByRepoInfoRefsRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiGetV1GitByOrgByRepoInfoRefs
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiGetV1GitByOrgByRepoInfoRefs
     */
    readonly repo: string
}

/**
 * Request parameters for getV1GitReposName operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameRequest
 */
export interface GitApiGetV1GitReposNameRequest {
    /**
     * Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
     * @type {string}
     * @memberof GitApiGetV1GitReposName
     */
    readonly name: string
}

/**
 * Request parameters for getV1GitReposNameBlob operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameBlobRequest
 */
export interface GitApiGetV1GitReposNameBlobRequest {
    /**
     * Name is the repo to read, from the :name path segment.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameBlob
     */
    readonly name: string

    /**
     * Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameBlob
     */
    readonly ref?: string

    /**
     * Path is repo-relative; empty is the tree root. Traversal is stripped.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameBlob
     */
    readonly path?: string
}

/**
 * Request parameters for getV1GitReposNameCommits operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameCommitsRequest
 */
export interface GitApiGetV1GitReposNameCommitsRequest {
    /**
     * Name is the repo to read, from the :name path segment.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameCommits
     */
    readonly name: string

    /**
     * Ref is the branch, tag or commit to walk back from; empty means HEAD.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameCommits
     */
    readonly ref?: string

    /**
     * Path narrows the history to commits touching it; empty walks the whole ref.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameCommits
     */
    readonly path?: string

    /**
     * Limit caps the page. Anything not positive means 50; the cap is 100.
     * @type {number}
     * @memberof GitApiGetV1GitReposNameCommits
     */
    readonly limit?: number
}

/**
 * Request parameters for getV1GitReposNameFiles operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameFilesRequest
 */
export interface GitApiGetV1GitReposNameFilesRequest {
    /**
     * Name is the repo to read, from the :name path segment.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameFiles
     */
    readonly name: string

    /**
     * Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameFiles
     */
    readonly ref?: string

    /**
     * Glob selects files, matched segment by segment so &#x60;*&#x60; never crosses a &#x60;/&#x60;. &#x60;**&#x60; matches zero or more whole segments.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameFiles
     */
    readonly glob?: string
}

/**
 * Request parameters for getV1GitReposNameMirrors operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameMirrorsRequest
 */
export interface GitApiGetV1GitReposNameMirrorsRequest {
    /**
     * Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameMirrors
     */
    readonly name: string
}

/**
 * Request parameters for getV1GitReposNameReadme operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameReadmeRequest
 */
export interface GitApiGetV1GitReposNameReadmeRequest {
    /**
     * Name is the repo to read, from the :name path segment.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameReadme
     */
    readonly name: string

    /**
     * Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameReadme
     */
    readonly ref?: string
}

/**
 * Request parameters for getV1GitReposNameRefs operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameRefsRequest
 */
export interface GitApiGetV1GitReposNameRefsRequest {
    /**
     * Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameRefs
     */
    readonly name: string
}

/**
 * Request parameters for getV1GitReposNameSubscriptions operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameSubscriptionsRequest
 */
export interface GitApiGetV1GitReposNameSubscriptionsRequest {
    /**
     * Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameSubscriptions
     */
    readonly name: string
}

/**
 * Request parameters for getV1GitReposNameTree operation in GitApi.
 * @export
 * @interface GitApiGetV1GitReposNameTreeRequest
 */
export interface GitApiGetV1GitReposNameTreeRequest {
    /**
     * Name is the repo to read, from the :name path segment.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameTree
     */
    readonly name: string

    /**
     * Ref is a branch, tag or commit; empty means the repo\&#39;s HEAD.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameTree
     */
    readonly ref?: string

    /**
     * Path is repo-relative; empty is the tree root. Traversal is stripped.
     * @type {string}
     * @memberof GitApiGetV1GitReposNameTree
     */
    readonly path?: string
}

/**
 * Request parameters for patchV1GitReposName operation in GitApi.
 * @export
 * @interface GitApiPatchV1GitReposNameRequest
 */
export interface GitApiPatchV1GitReposNameRequest {
    /**
     * Name is the repo to update, from the :name path segment.
     * @type {string}
     * @memberof GitApiPatchV1GitReposName
     */
    readonly name: string

    /**
     * 
     * @type {PatchIn}
     * @memberof GitApiPatchV1GitReposName
     */
    readonly patchIn: PatchIn
}

/**
 * Request parameters for postByOrgByProjectByRepoGitReceivePack operation in GitApi.
 * @export
 * @interface GitApiPostByOrgByProjectByRepoGitReceivePackRequest
 */
export interface GitApiPostByOrgByProjectByRepoGitReceivePackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByProjectByRepoGitReceivePack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByProjectByRepoGitReceivePack
     */
    readonly project: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByProjectByRepoGitReceivePack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostByOrgByProjectByRepoGitReceivePack
     */
    readonly body?: File
}

/**
 * Request parameters for postByOrgByProjectByRepoGitUploadPack operation in GitApi.
 * @export
 * @interface GitApiPostByOrgByProjectByRepoGitUploadPackRequest
 */
export interface GitApiPostByOrgByProjectByRepoGitUploadPackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByProjectByRepoGitUploadPack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByProjectByRepoGitUploadPack
     */
    readonly project: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByProjectByRepoGitUploadPack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostByOrgByProjectByRepoGitUploadPack
     */
    readonly body?: File
}

/**
 * Request parameters for postByOrgByRepoGitReceivePack operation in GitApi.
 * @export
 * @interface GitApiPostByOrgByRepoGitReceivePackRequest
 */
export interface GitApiPostByOrgByRepoGitReceivePackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByRepoGitReceivePack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByRepoGitReceivePack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostByOrgByRepoGitReceivePack
     */
    readonly body?: File
}

/**
 * Request parameters for postByOrgByRepoGitUploadPack operation in GitApi.
 * @export
 * @interface GitApiPostByOrgByRepoGitUploadPackRequest
 */
export interface GitApiPostByOrgByRepoGitUploadPackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByRepoGitUploadPack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostByOrgByRepoGitUploadPack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostByOrgByRepoGitUploadPack
     */
    readonly body?: File
}

/**
 * Request parameters for postV1GitByOrgByProjectByRepoGitReceivePack operation in GitApi.
 * @export
 * @interface GitApiPostV1GitByOrgByProjectByRepoGitReceivePackRequest
 */
export interface GitApiPostV1GitByOrgByProjectByRepoGitReceivePackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitReceivePack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitReceivePack
     */
    readonly project: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitReceivePack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitReceivePack
     */
    readonly body?: File
}

/**
 * Request parameters for postV1GitByOrgByProjectByRepoGitUploadPack operation in GitApi.
 * @export
 * @interface GitApiPostV1GitByOrgByProjectByRepoGitUploadPackRequest
 */
export interface GitApiPostV1GitByOrgByProjectByRepoGitUploadPackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitUploadPack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitUploadPack
     */
    readonly project: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitUploadPack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostV1GitByOrgByProjectByRepoGitUploadPack
     */
    readonly body?: File
}

/**
 * Request parameters for postV1GitByOrgByRepoGitReceivePack operation in GitApi.
 * @export
 * @interface GitApiPostV1GitByOrgByRepoGitReceivePackRequest
 */
export interface GitApiPostV1GitByOrgByRepoGitReceivePackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByRepoGitReceivePack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByRepoGitReceivePack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostV1GitByOrgByRepoGitReceivePack
     */
    readonly body?: File
}

/**
 * Request parameters for postV1GitByOrgByRepoGitUploadPack operation in GitApi.
 * @export
 * @interface GitApiPostV1GitByOrgByRepoGitUploadPackRequest
 */
export interface GitApiPostV1GitByOrgByRepoGitUploadPackRequest {
    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByRepoGitUploadPack
     */
    readonly org: string

    /**
     * 
     * @type {string}
     * @memberof GitApiPostV1GitByOrgByRepoGitUploadPack
     */
    readonly repo: string

    /**
     * 
     * @type {File}
     * @memberof GitApiPostV1GitByOrgByRepoGitUploadPack
     */
    readonly body?: File
}

/**
 * Request parameters for postV1GitKeys operation in GitApi.
 * @export
 * @interface GitApiPostV1GitKeysRequest
 */
export interface GitApiPostV1GitKeysRequest {
    /**
     * 
     * @type {RegisterKeyReq}
     * @memberof GitApiPostV1GitKeys
     */
    readonly registerKeyReq: RegisterKeyReq
}

/**
 * Request parameters for postV1GitRepos operation in GitApi.
 * @export
 * @interface GitApiPostV1GitReposRequest
 */
export interface GitApiPostV1GitReposRequest {
    /**
     * 
     * @type {CreateReq}
     * @memberof GitApiPostV1GitRepos
     */
    readonly createReq: CreateReq
}

/**
 * Request parameters for postV1GitReposNameGc operation in GitApi.
 * @export
 * @interface GitApiPostV1GitReposNameGcRequest
 */
export interface GitApiPostV1GitReposNameGcRequest {
    /**
     * Name is the repo\&#39;s org-unique handle, from the :name path segment. A trailing \&quot;.git\&quot; is stripped.
     * @type {string}
     * @memberof GitApiPostV1GitReposNameGc
     */
    readonly name: string
}

/**
 * Request parameters for postV1GitReposNameMirror operation in GitApi.
 * @export
 * @interface GitApiPostV1GitReposNameMirrorRequest
 */
export interface GitApiPostV1GitReposNameMirrorRequest {
    /**
     * Name is the local repo to mirror into, from the :name path segment. It is CREATED on first use.
     * @type {string}
     * @memberof GitApiPostV1GitReposNameMirror
     */
    readonly name: string

    /**
     * 
     * @type {MirrorReq}
     * @memberof GitApiPostV1GitReposNameMirror
     */
    readonly mirrorReq: MirrorReq
}

/**
 * Request parameters for postV1GitReposNameMirrors operation in GitApi.
 * @export
 * @interface GitApiPostV1GitReposNameMirrorsRequest
 */
export interface GitApiPostV1GitReposNameMirrorsRequest {
    /**
     * Name is the repo whose advanced refs are pushed downstream, from the :name path segment.
     * @type {string}
     * @memberof GitApiPostV1GitReposNameMirrors
     */
    readonly name: string

    /**
     * 
     * @type {MirrorTargetReq}
     * @memberof GitApiPostV1GitReposNameMirrors
     */
    readonly mirrorTargetReq: MirrorTargetReq
}

/**
 * Request parameters for postV1GitReposNamePush operation in GitApi.
 * @export
 * @interface GitApiPostV1GitReposNamePushRequest
 */
export interface GitApiPostV1GitReposNamePushRequest {
    /**
     * Name is the repo to push into, from the :name path segment. It is CREATED on first push if it does not exist.
     * @type {string}
     * @memberof GitApiPostV1GitReposNamePush
     */
    readonly name: string

    /**
     * 
     * @type {PushReq}
     * @memberof GitApiPostV1GitReposNamePush
     */
    readonly pushReq: PushReq
}

/**
 * Request parameters for postV1GitReposNameSubscriptions operation in GitApi.
 * @export
 * @interface GitApiPostV1GitReposNameSubscriptionsRequest
 */
export interface GitApiPostV1GitReposNameSubscriptionsRequest {
    /**
     * Name is the repo to subscribe, from the :name path segment.
     * @type {string}
     * @memberof GitApiPostV1GitReposNameSubscriptions
     */
    readonly name: string

    /**
     * 
     * @type {SubscribeReq}
     * @memberof GitApiPostV1GitReposNameSubscriptions
     */
    readonly subscribeReq: SubscribeReq
}

/**
 * Request parameters for postV1GitZapCreaterepo operation in GitApi.
 * @export
 * @interface GitApiPostV1GitZapCreaterepoRequest
 */
export interface GitApiPostV1GitZapCreaterepoRequest {
    /**
     * 
     * @type {ZapProcReq}
     * @memberof GitApiPostV1GitZapCreaterepo
     */
    readonly zapProcReq?: ZapProcReq
}

/**
 * Request parameters for postV1GitZapDeleterepo operation in GitApi.
 * @export
 * @interface GitApiPostV1GitZapDeleterepoRequest
 */
export interface GitApiPostV1GitZapDeleterepoRequest {
    /**
     * 
     * @type {ZapProcReq}
     * @memberof GitApiPostV1GitZapDeleterepo
     */
    readonly zapProcReq?: ZapProcReq
}

/**
 * Request parameters for postV1GitZapGetrepo operation in GitApi.
 * @export
 * @interface GitApiPostV1GitZapGetrepoRequest
 */
export interface GitApiPostV1GitZapGetrepoRequest {
    /**
     * 
     * @type {ZapProcReq}
     * @memberof GitApiPostV1GitZapGetrepo
     */
    readonly zapProcReq?: ZapProcReq
}

/**
 * GitApi - object-oriented interface
 * @export
 * @class GitApi
 * @extends {BaseAPI}
 */
export class GitApi extends BaseAPI {
    /**
     * Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found. Answers 204 with no body. Once removed the key no longer authenticates any SSH git access.
     * @summary Removes a registered SSH key, scoped to the caller\'s org: an org can only delete its own, and a key id it does not own is not found.
     * @param {GitApiDeleteV1GitKeysIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public deleteV1GitKeysId(requestParameters: GitApiDeleteV1GitKeysIdRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).deleteV1GitKeysId(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes a repo\'s metadata and purges its storage. Answers 204 with no body. The metadata row is the source of truth for existence, so a storage purge that fails is logged and the delete still succeeds — and a second call is a 404, not a second delete.
     * @summary Removes a repo\'s metadata and purges its storage.
     * @param {GitApiDeleteV1GitReposNameRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public deleteV1GitReposName(requestParameters: GitApiDeleteV1GitReposNameRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).deleteV1GitReposName(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes one outbound mirror target; later pushes stop being forwarded to it. Answers 204 with no body. Nothing is done to the downstream remote itself — only this repo\'s intent to push there is dropped.
     * @summary Removes one outbound mirror target; later pushes stop being forwarded to it.
     * @param {GitApiDeleteV1GitReposNameMirrorsIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public deleteV1GitReposNameMirrorsId(requestParameters: GitApiDeleteV1GitReposNameMirrorsIdRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).deleteV1GitReposNameMirrorsId(requestParameters.name, requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel. Answers 204 with no body. An id that is not this repo\'s subscription is not found.
     * @summary Removes one Slack subscription from a repo; the notifier stops posting that repo\'s events to that channel.
     * @param {GitApiDeleteV1GitReposNameSubscriptionsIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public deleteV1GitReposNameSubscriptionsId(requestParameters: GitApiDeleteV1GitReposNameSubscriptionsIdRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).deleteV1GitReposNameSubscriptionsId(requestParameters.name, requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
     * @summary Browse your org\'s repositories
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public get(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).get(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Advertise a repository\'s refs to a git client
     * @param {GitApiGetByOrgByProjectByRepoInfoRefsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getByOrgByProjectByRepoInfoRefs(requestParameters: GitApiGetByOrgByProjectByRepoInfoRefsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getByOrgByProjectByRepoInfoRefs(requestParameters.org, requestParameters.project, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
     * @summary Open a repository\'s home page
     * @param {GitApiGetByOrgByRepoRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getByOrgByRepo(requestParameters: GitApiGetByOrgByRepoRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getByOrgByRepo(requestParameters.org, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
     * @summary View a file in a repository
     * @param {GitApiGetByOrgByRepoBlobByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getByOrgByRepoBlobByWildcard1(requestParameters: GitApiGetByOrgByRepoBlobByWildcard1Request, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getByOrgByRepoBlobByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
     * @summary Read a repository\'s commit log
     * @param {GitApiGetByOrgByRepoCommitsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getByOrgByRepoCommits(requestParameters: GitApiGetByOrgByRepoCommitsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getByOrgByRepoCommits(requestParameters.org, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Advertise a repository\'s refs to a git client
     * @param {GitApiGetByOrgByRepoInfoRefsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getByOrgByRepoInfoRefs(requestParameters: GitApiGetByOrgByRepoInfoRefsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getByOrgByRepoInfoRefs(requestParameters.org, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
     * @summary Browse a directory inside a repository
     * @param {GitApiGetByOrgByRepoTreeByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getByOrgByRepoTreeByWildcard1(requestParameters: GitApiGetByOrgByRepoTreeByWildcard1Request, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getByOrgByRepoTreeByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served only on the dedicated git host, where a browse URL matches the clone URL; on the API and console hosts it falls through to their own routes, so it can never shadow them.
     * @summary Discover public repositories across every org
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getExplore(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getExplore(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The repository list for the signed-in caller\'s org — each repo with its description, default branch, size and last update. SIGNED OUT it renders the public explore page instead of refusing, because most Hanzo repos are open source and the open face is the default one; signed in, the caller\'s own org shows its private repositories alongside its public ones. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
     * @summary Browse your org\'s repositories
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getGit(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getGit(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * A repository at a glance: its branches, the tree at the tip, its most recent commits, its README rendered, and the HTTPS and SSH clone URLs. `?ref=` selects a branch, tag or commit; the default branch is used when it is omitted. A repository with no commits yet renders its clone instructions rather than an error, which is what a caller who has just created one needs to see. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
     * @summary Open a repository\'s home page
     * @param {GitApiGetGitByOrgByRepoRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getGitByOrgByRepo(requestParameters: GitApiGetGitByOrgByRepoRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getGitByOrgByRepo(requestParameters.org, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * One file\'s contents at one revision, with its size and line count. A BINARY file is reported as binary rather than dumped into the page. The path after /blob/ is the file and `?ref=` selects the branch, tag or commit. An unknown ref or a path that is not a file in it is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
     * @summary View a file in a repository
     * @param {GitApiGetGitByOrgByRepoBlobByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getGitByOrgByRepoBlobByWildcard1(requestParameters: GitApiGetGitByOrgByRepoBlobByWildcard1Request, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getGitByOrgByRepoBlobByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The hundred most recent commits on one ref, each with its author, message and date. `?ref=` selects the branch, tag or commit, defaulting to the repository\'s default branch; an unknown one is 404. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
     * @summary Read a repository\'s commit log
     * @param {GitApiGetGitByOrgByRepoCommitsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getGitByOrgByRepoCommits(requestParameters: GitApiGetGitByOrgByRepoCommitsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getGitByOrgByRepoCommits(requestParameters.org, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The contents of one directory at one revision, with breadcrumbs back up and links onward into subdirectories and files. The path after /tree/ is the directory and `?ref=` selects the branch, tag or commit, defaulting to the repository\'s own default branch. An unknown ref is 404, as is a repository with no commits. A public repository is readable by anyone; a private one only by its own org. A repository that does not exist and one belonging to another org answer the SAME 404, so the page is never an existence oracle. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
     * @summary Browse a directory inside a repository
     * @param {GitApiGetGitByOrgByRepoTreeByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getGitByOrgByRepoTreeByWildcard1(requestParameters: GitApiGetGitByOrgByRepoTreeByWildcard1Request, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getGitByOrgByRepoTreeByWildcard1(requestParameters.org, requestParameters.repo, requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The open, unauthenticated face of the git host: every PUBLIC repository in the fleet, org-qualified, so a project can be found and cloned with no account at all — signing in is for private repos and for writes. Repositories live in per-org stores with no global index, so this unions each org\'s public rows and is bounded to a fixed number of stores per request, keeping discovery quick however many orgs exist. A fleet with no orgs yet is an empty page, not an error. This is a server-rendered browser page, not JSON — the console repo-browser reads the same repository through the JSON ops under /v1/git. Repository names, paths and file contents all render through auto-escaping templates rather than being concatenated into HTML. Served on every host, which is how the console embeds the git browser under /git.
     * @summary Discover public repositories across every org
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getGitExplore(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getGitExplore(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Advertise a repository\'s refs to a git client
     * @param {GitApiGetV1GitByOrgByProjectByRepoInfoRefsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitByOrgByProjectByRepoInfoRefs(requestParameters: GitApiGetV1GitByOrgByProjectByRepoInfoRefsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitByOrgByProjectByRepoInfoRefs(requestParameters.org, requestParameters.project, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The ref-advertisement phase of git\'s smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.  ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Advertise a repository\'s refs to a git client
     * @param {GitApiGetV1GitByOrgByRepoInfoRefsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitByOrgByRepoInfoRefs(requestParameters: GitApiGetV1GitByOrgByRepoInfoRefsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitByOrgByRepoInfoRefs(requestParameters.org, requestParameters.repo, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`. Keys are org-scoped on read even though the fingerprint index is global, so one org never sees another\'s.
     * @summary Returns the SSH public keys registered to the caller\'s org — the keys that authenticate `git clone git@<host>:<org>/<repo>.git`.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitKeys(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitKeys(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the repos in the caller\'s scope, most recently updated first. The scope is the request principal\'s — the gateway-minted org and its optional project — never anything off the wire, so a caller only ever sees its own. Rows carry no branches or HEAD; read one repo for those.
     * @summary Returns the repos in the caller\'s scope, most recently updated first.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitRepos(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitRepos(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one repo with its live ref state: every branch name and the resolved HEAD commit. Both are read from the object store on each call, so an empty repo reports no branches and an empty head rather than failing. A repo outside the caller\'s scope is not found.
     * @summary Returns one repo with its live ref state: every branch name and the resolved HEAD commit.
     * @param {GitApiGetV1GitReposNameRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposName(requestParameters: GitApiGetV1GitReposNameRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposName(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one file\'s bytes at one revision. Text comes back verbatim, binary comes back base64, and a file past the 1 MiB view cap comes back marked truncated with NO content — the client is expected to clone instead.
     * @summary Returns one file\'s bytes at one revision.
     * @param {GitApiGetV1GitReposNameBlobRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameBlob(requestParameters: GitApiGetV1GitReposNameBlobRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameBlob(requestParameters.name, requestParameters.ref, requestParameters.path, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Walks a ref\'s history newest first, or one path\'s history when a path is given. There is no cursor: the page is the newest `limit` commits.
     * @summary Walks a ref\'s history newest first, or one path\'s history when a path is given.
     * @param {GitApiGetV1GitReposNameCommitsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameCommits(requestParameters: GitApiGetV1GitReposNameCommitsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameCommits(requestParameters.name, requestParameters.ref, requestParameters.path, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns every file a glob selects at one revision, WITH its bytes and the revision they came from. It is the read a delivery generator makes: one call answers \"what is the inventory at this commit, and what does it say\", where listing and then fetching would be a request per file.  Returning the resolved revision matters as much as the bytes. A generator that lists at `main` and then reads at `main` can straddle a push and assemble half its inventory from one commit and half from the next; resolving once makes the whole read consistent by construction.  A file past the read cap comes back Truncated with no content rather than being dropped. A caller building a desired set has to know the difference between \"this file is empty\" and \"this file was not read\" — silently omitting it is how a pruning reconcile deletes what the missing file declared.
     * @summary Returns every file a glob selects at one revision, WITH its bytes and the revision they came from.
     * @param {GitApiGetV1GitReposNameFilesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameFiles(requestParameters: GitApiGetV1GitReposNameFilesRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameFiles(requestParameters.name, requestParameters.ref, requestParameters.glob, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
     * @summary Returns a repo\'s outbound mirror targets — the downstream remotes the mirror reactor pushes to whenever a push lands here.
     * @param {GitApiGetV1GitReposNameMirrorsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameMirrors(requestParameters: GitApiGetV1GitReposNameMirrorsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameMirrors(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it. A repo with no README is not found.
     * @summary Returns the README at the tree root as plain text — unrendered, so the caller decides how to present it.
     * @param {GitApiGetV1GitReposNameReadmeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameReadme(requestParameters: GitApiGetV1GitReposNameReadmeRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameReadme(requestParameters.name, requestParameters.ref, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call. Unlike the other read ops it tolerates a repo with no commits: the ref sets come back empty and the default branch is still named.
     * @summary Lists a repo\'s branches, tags and default branch — what a branch picker needs in one call.
     * @param {GitApiGetV1GitReposNameRefsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameRefs(requestParameters: GitApiGetV1GitReposNameRefsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameRefs(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
     * @summary Returns a repo\'s Slack subscriptions — which channels the lifecycle notifier posts this repo\'s push and deploy events to.
     * @param {GitApiGetV1GitReposNameSubscriptionsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameSubscriptions(requestParameters: GitApiGetV1GitReposNameSubscriptionsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameSubscriptions(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the immediate children of one directory at one revision, directories before files. It does not recurse — walk down a level at a time.
     * @summary Lists the immediate children of one directory at one revision, directories before files.
     * @param {GitApiGetV1GitReposNameTreeRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitReposNameTree(requestParameters: GitApiGetV1GitReposNameTreeRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitReposNameTree(requestParameters.name, requestParameters.ref, requestParameters.path, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on. It spans EVERY project sub-scope, unlike the repo list, so a billing consumer sees the whole tenant footprint in one call. Sizes are last-measured values (create, push, mirror and gc each re-measure), not a live walk of the disk.
     * @summary Returns per-repo and total storage bytes for the caller\'s org — the queryable, per-tenant number commerce and o11y meter on.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public getV1GitUsage(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).getV1GitUsage(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Flips a repo\'s public bit, the one mutable repo setting today. Public grants ANONYMOUS fetch only; push and the whole control plane stay org-authed. Returns the updated repo.
     * @summary Flips a repo\'s public bit, the one mutable repo setting today.
     * @param {GitApiPatchV1GitReposNameRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public patchV1GitReposName(requestParameters: GitApiPatchV1GitReposNameRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).patchV1GitReposName(requestParameters.name, requestParameters.patchIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Accept a push, and turn it into a build
     * @param {GitApiPostByOrgByProjectByRepoGitReceivePackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postByOrgByProjectByRepoGitReceivePack(requestParameters: GitApiPostByOrgByProjectByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postByOrgByProjectByRepoGitReceivePack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root with the PROJECT as a middle path segment — the canonical-URL form of the project-scoped remote, since a git client has no header to carry a project. Served only on the dedicated git host; elsewhere it falls through. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Serve a clone or fetch
     * @param {GitApiPostByOrgByProjectByRepoGitUploadPackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postByOrgByProjectByRepoGitUploadPack(requestParameters: GitApiPostByOrgByProjectByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postByOrgByProjectByRepoGitUploadPack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Accept a push, and turn it into a build
     * @param {GitApiPostByOrgByRepoGitReceivePackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postByOrgByRepoGitReceivePack(requestParameters: GitApiPostByOrgByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postByOrgByRepoGitReceivePack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed at the git host\'s root, so `git clone https://<git-host>/<org>/<repo>.git` works with the canonical URL and no prefix. Served ONLY on the dedicated git host; on the API and console hosts it falls through, so a bare /:org/:repo can never shadow another surface. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Serve a clone or fetch
     * @param {GitApiPostByOrgByRepoGitUploadPackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postByOrgByRepoGitUploadPack(requestParameters: GitApiPostByOrgByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postByOrgByRepoGitUploadPack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Accept a push, and turn it into a build
     * @param {GitApiPostV1GitByOrgByProjectByRepoGitReceivePackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitByOrgByProjectByRepoGitReceivePack(requestParameters: GitApiPostV1GitByOrgByProjectByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitByOrgByProjectByRepoGitReceivePack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Serve a clone or fetch
     * @param {GitApiPostV1GitByOrgByProjectByRepoGitUploadPackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitByOrgByProjectByRepoGitUploadPack(requestParameters: GitApiPostV1GitByOrgByProjectByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitByOrgByProjectByRepoGitUploadPack(requestParameters.org, requestParameters.project, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.  Once the pack is on disk the repository\'s storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.  A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Accept a push, and turn it into a build
     * @param {GitApiPostV1GitByOrgByRepoGitReceivePackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitByOrgByRepoGitReceivePack(requestParameters: GitApiPostV1GitByOrgByRepoGitReceivePackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitByOrgByRepoGitReceivePack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The pack-transfer phase of a clone or fetch: the request and the response are git\'s binary pack protocol, streamed straight through git itself — request body to git\'s stdin, git\'s stdout to the response — so a multi-gigabyte clone never lands in this process\'s memory.  A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git\'s own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
     * @summary Serve a clone or fetch
     * @param {GitApiPostV1GitByOrgByRepoGitUploadPackRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitByOrgByRepoGitUploadPack(requestParameters: GitApiPostV1GitByOrgByRepoGitUploadPackRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitByOrgByRepoGitUploadPack(requestParameters.org, requestParameters.repo, requestParameters.body, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org. The key line is parsed and canonicalized before storage, its SHA256 fingerprint becomes the auth lookup handle, and the full public key round-trips (it is public). Answers 201. Fingerprints are globally unique, so a key already registered — to this org or any other — is a 409: one key belongs to exactly one org.
     * @summary Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git` for the caller\'s org.
     * @param {GitApiPostV1GitKeysRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitKeys(requestParameters: GitApiPostV1GitKeysRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitKeys(requestParameters.registerKeyReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs. Answers 201. The name must be unique within the scope — a repeat is a 409, never a silent overwrite of an existing repo. The org comes from the validated principal, so a repo is always born owned by the caller\'s own tenant.
     * @summary Provisions an empty bare repository in the caller\'s scope and returns it with its clone URLs.
     * @param {GitApiPostV1GitReposRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitRepos(requestParameters: GitApiPostV1GitReposRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitRepos(requestParameters.createReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph. Idempotent, and safe to interrupt — git swaps both artifacts atomically. It runs under one pack slot with the same memory bounds as a clone, so it can block behind heavy pack traffic rather than compete with it. Storage usage is re-measured afterwards, since a repack reclaims space.
     * @summary Repacks a repo into one bitmapped pack and rewrites its commit-graph, so the next clone reuses the bitmap instead of walking the whole object graph.
     * @param {GitApiPostV1GitReposNameGcRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitReposNameGc(requestParameters: GitApiPostV1GitReposNameGcRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitReposNameGc(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Imports an external git repository into the caller\'s repo, provisioning it on first use. Fetch is FORCED and covers every ref, so a first call clones the source and a repeat call re-syncs it — the endpoint is idempotent by mirror semantics. Mirrored bytes are metered exactly like a push, and a push.landed event is emitted for the default branch so the code index picks the repo up.
     * @summary Imports an external git repository into the caller\'s repo, provisioning it on first use.
     * @param {GitApiPostV1GitReposNameMirrorRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitReposNameMirror(requestParameters: GitApiPostV1GitReposNameMirrorRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitReposNameMirror(requestParameters.name, requestParameters.mirrorReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here. Answers 201. The URL must be https to a host on the mirror allowlist (github.com / gitlab.com): the same set the mirror credential may be sent to, so a target can never capture the shared token or point the push at an internal service. Any embedded userinfo is stripped — credentials ride env-only at push time and never enter the stored URL. One mirror per host per repo; a second is a 409.
     * @summary Registers a downstream remote the repo\'s advanced refs are pushed to whenever a push lands here.
     * @param {GitApiPostV1GitReposNameMirrorsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitReposNameMirrors(requestParameters: GitApiPostV1GitReposNameMirrorsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitReposNameMirrors(requestParameters.name, requestParameters.mirrorTargetReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lands a set of files as one commit without a git client — the hanzo.app builder\'s push. The repo is CREATED on first push, the files are merged onto the branch tip (unlisted files survive), and the same push-to-deploy hook a real receive-pack fires is fired, so downstream this is indistinguishable from a `git push`.
     * @summary Lands a set of files as one commit without a git client — the hanzo.app builder\'s push.
     * @param {GitApiPostV1GitReposNamePushRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitReposNamePush(requestParameters: GitApiPostV1GitReposNamePushRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitReposNamePush(requestParameters.name, requestParameters.pushReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there. Answers 201. The same channel twice on one repo is a 409; a repo outside the caller\'s scope is a 404, exactly as reading it is.
     * @summary Binds a Slack channel to a repo, so the lifecycle notifier posts that repo\'s push and deploy events there.
     * @param {GitApiPostV1GitReposNameSubscriptionsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitReposNameSubscriptions(requestParameters: GitApiPostV1GitReposNameSubscriptionsRequest, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitReposNameSubscriptions(requestParameters.name, requestParameters.subscribeReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * GONE (410). This was the canonical forge\'s push-to-deploy door, and it never dispatched a build in its life.  It handed each verified push to cloud.OnGitPush, a single-registrant seam whose only registrant lives in apps/platform. cloud runs each app as its own OS process, so in the git process that builder is nil forever — and this handler answered 204 either way. Delivered, signature valid, green on the forge\'s hook page, and nothing built.  Push-to-deploy now belongs to POST https://platform.hanzo.ai/v1/git-webhook, which owns the build system-of-record and dispatches BuildKit Jobs. git.hanzo.ai delivers there through ONE forge-wide system webhook covering every repository; a repo opts in by committing hanzo.yml, not by owning a hook of its own.  The route is kept, and answers 410 naming that address, precisely so a misdirected delivery says what is wrong. Deleting it would 404, and a 404 here reads as \'the API is switched off\' — the wrong conclusion this estate has already drawn twice.
     * @summary Retired — forge pushes build via platform.hanzo.ai
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitWebhook(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitWebhook(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Creates a repository in the caller\'s org and project scope and answers with its record. `name` is required and `description` is optional; `project` narrows the scope within the org. A name already taken in that scope is a 409 envelope and an invalid name a 400.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * @summary Create a repository over the ZAP transport
     * @param {GitApiPostV1GitZapCreaterepoRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitZapCreaterepo(requestParameters: GitApiPostV1GitZapCreaterepoRequest = {}, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitZapCreaterepo(requestParameters.zapProcReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Deletes the repository named by `name` and answers with the deleted name. A repository outside the caller\'s org and project scope is a 404 envelope, so a delete can never reach another tenant\'s repository.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * @summary Delete a repository over the ZAP transport
     * @param {GitApiPostV1GitZapDeleterepoRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitZapDeleterepo(requestParameters: GitApiPostV1GitZapDeleterepoRequest = {}, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitZapDeleterepo(requestParameters.zapProcReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers a single repository\'s record, named by `name`. A repository outside the caller\'s org and project scope is a 404 envelope, the same answer one that does not exist gets.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * @summary Read one repository over the ZAP transport
     * @param {GitApiPostV1GitZapGetrepoRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitZapGetrepo(requestParameters: GitApiPostV1GitZapGetrepoRequest = {}, options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitZapGetrepo(requestParameters.zapProcReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers every repository in the caller\'s org and project scope. It reads NO body — the scope is entirely the caller\'s identity — so a request with an empty object is correct.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * @summary List your repositories over the ZAP transport
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitZapListrepos(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitZapListrepos(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers every repository in the caller\'s org with its size in bytes, plus the org\'s total — what git storage is actually being used, and by which repository. It reads NO body, and it is scoped to the caller\'s own org, so it is that org\'s footprint and never the fleet\'s.  A ZAP PROCEDURE, not a REST resource. It answers the bridge\'s {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller\'s scope. Without a validated org the answer is a 403 envelope.
     * @summary Report your org\'s git storage footprint over the ZAP transport
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GitApi
     */
    public postV1GitZapUsage(options?: RawAxiosRequestConfig) {
        return GitApiFp(this.configuration).postV1GitZapUsage(options).then((request) => request(this.axios, this.basePath));
    }
}

