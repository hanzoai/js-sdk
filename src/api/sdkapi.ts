/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { KmsError } from '../models';
// @ts-ignore
import type { KmsSdkEnvelope } from '../models';
/**
 * SDKApi - axios parameter creator
 * @export
 */
export const SDKApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * The SDK-facing native secrets plane. A single RPC endpoint: the body is a signed Envelope (ML-DSA-65 over the request), and the OPERATION is the signed `op` field — never the URL. Authorization is consensus-native: the caller\'s mnemonic-derived NodeID must be in the validator authority (reads: OpSecretGet 0x0040, OpSecretList 0x0042, OpVerify 0x0051) and, for writes (OpSecretPut 0x0041 — also the rotate op, OpSecretDelete 0x0043, OpSign 0x0050), additionally in the operator authority. Every envelope is verified for signature, wall-clock freshness (±5m), and replay (per-(NodeID,nonce) ledger) before dispatch. There is NO bearer token on this surface — the ML-DSA-65 envelope IS the credential. 
         * @summary Enveloped secret + threshold-key operation
         * @param {KmsSdkEnvelope} kmsSdkEnvelope 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        kmsSdkSecretsOp: async (kmsSdkEnvelope: KmsSdkEnvelope, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'kmsSdkEnvelope' is not null or undefined
            assertParamExists('kmsSdkSecretsOp', 'kmsSdkEnvelope', kmsSdkEnvelope)
            const localVarPath = `/v1/sdk/secrets`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(kmsSdkEnvelope, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * SDKApi - functional programming interface
 * @export
 */
export const SDKApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = SDKApiAxiosParamCreator(configuration)
    return {
        /**
         * The SDK-facing native secrets plane. A single RPC endpoint: the body is a signed Envelope (ML-DSA-65 over the request), and the OPERATION is the signed `op` field — never the URL. Authorization is consensus-native: the caller\'s mnemonic-derived NodeID must be in the validator authority (reads: OpSecretGet 0x0040, OpSecretList 0x0042, OpVerify 0x0051) and, for writes (OpSecretPut 0x0041 — also the rotate op, OpSecretDelete 0x0043, OpSign 0x0050), additionally in the operator authority. Every envelope is verified for signature, wall-clock freshness (±5m), and replay (per-(NodeID,nonce) ledger) before dispatch. There is NO bearer token on this surface — the ML-DSA-65 envelope IS the credential. 
         * @summary Enveloped secret + threshold-key operation
         * @param {KmsSdkEnvelope} kmsSdkEnvelope 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async kmsSdkSecretsOp(kmsSdkEnvelope: KmsSdkEnvelope, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<object>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.kmsSdkSecretsOp(kmsSdkEnvelope, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SDKApi.kmsSdkSecretsOp']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * SDKApi - factory interface
 * @export
 */
export const SDKApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = SDKApiFp(configuration)
    return {
        /**
         * The SDK-facing native secrets plane. A single RPC endpoint: the body is a signed Envelope (ML-DSA-65 over the request), and the OPERATION is the signed `op` field — never the URL. Authorization is consensus-native: the caller\'s mnemonic-derived NodeID must be in the validator authority (reads: OpSecretGet 0x0040, OpSecretList 0x0042, OpVerify 0x0051) and, for writes (OpSecretPut 0x0041 — also the rotate op, OpSecretDelete 0x0043, OpSign 0x0050), additionally in the operator authority. Every envelope is verified for signature, wall-clock freshness (±5m), and replay (per-(NodeID,nonce) ledger) before dispatch. There is NO bearer token on this surface — the ML-DSA-65 envelope IS the credential. 
         * @summary Enveloped secret + threshold-key operation
         * @param {SDKApiKmsSdkSecretsOpRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        kmsSdkSecretsOp(requestParameters: SDKApiKmsSdkSecretsOpRequest, options?: RawAxiosRequestConfig): AxiosPromise<object> {
            return localVarFp.kmsSdkSecretsOp(requestParameters.kmsSdkEnvelope, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for kmsSdkSecretsOp operation in SDKApi.
 * @export
 * @interface SDKApiKmsSdkSecretsOpRequest
 */
export interface SDKApiKmsSdkSecretsOpRequest {
    /**
     * 
     * @type {KmsSdkEnvelope}
     * @memberof SDKApiKmsSdkSecretsOp
     */
    readonly kmsSdkEnvelope: KmsSdkEnvelope
}

/**
 * SDKApi - object-oriented interface
 * @export
 * @class SDKApi
 * @extends {BaseAPI}
 */
export class SDKApi extends BaseAPI {
    /**
     * The SDK-facing native secrets plane. A single RPC endpoint: the body is a signed Envelope (ML-DSA-65 over the request), and the OPERATION is the signed `op` field — never the URL. Authorization is consensus-native: the caller\'s mnemonic-derived NodeID must be in the validator authority (reads: OpSecretGet 0x0040, OpSecretList 0x0042, OpVerify 0x0051) and, for writes (OpSecretPut 0x0041 — also the rotate op, OpSecretDelete 0x0043, OpSign 0x0050), additionally in the operator authority. Every envelope is verified for signature, wall-clock freshness (±5m), and replay (per-(NodeID,nonce) ledger) before dispatch. There is NO bearer token on this surface — the ML-DSA-65 envelope IS the credential. 
     * @summary Enveloped secret + threshold-key operation
     * @param {SDKApiKmsSdkSecretsOpRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SDKApi
     */
    public kmsSdkSecretsOp(requestParameters: SDKApiKmsSdkSecretsOpRequest, options?: RawAxiosRequestConfig) {
        return SDKApiFp(this.configuration).kmsSdkSecretsOp(requestParameters.kmsSdkEnvelope, options).then((request) => request(this.axios, this.basePath));
    }
}

