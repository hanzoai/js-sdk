/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
/**
 * DnsApi - axios parameter creator
 * @export
 */
export const DnsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Removes one of the calling org\'s zones, and the records in it, from the DNS control plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Delete a DNS zone
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteDnsZonesByZone: async (zone: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('deleteDnsZonesByZone', 'zone', zone)
            const localVarPath = `/v1/dns/zones/{zone}`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes one record from one zone. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Delete a DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteDnsZonesByZoneRecordsByRecord: async (zone: string, record: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('deleteDnsZonesByZoneRecordsByRecord', 'zone', zone)
            // verify required parameter 'record' is not null or undefined
            assertParamExists('deleteDnsZonesByZoneRecordsByRecord', 'record', record)
            const localVarPath = `/v1/dns/zones/{zone}/records/{record}`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)))
                .replace(`{${"record"}}`, encodeURIComponent(String(record)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reports whether the DNS control plane is answering. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Check the DNS control plane
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/dns/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists every DNS zone the calling org holds, authoritative and provider-backed alike. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary List your org\'s DNS zones
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZones: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/dns/zones`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads one of the calling org\'s zones by name. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Read one DNS zone
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZonesByZone: async (zone: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('getDnsZonesByZone', 'zone', zone)
            const localVarPath = `/v1/dns/zones/{zone}`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the records in one zone. A provider-backed zone is read from the provider, which is its source of truth. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary List a zone\'s DNS records
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZonesByZoneRecords: async (zone: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('getDnsZonesByZoneRecords', 'zone', zone)
            const localVarPath = `/v1/dns/zones/{zone}/records`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads one record of one zone by its id. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Read one DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZonesByZoneRecordsByRecord: async (zone: string, record: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('getDnsZonesByZoneRecordsByRecord', 'zone', zone)
            // verify required parameter 'record' is not null or undefined
            assertParamExists('getDnsZonesByZoneRecordsByRecord', 'record', record)
            const localVarPath = `/v1/dns/zones/{zone}/records/{record}`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)))
                .replace(`{${"record"}}`, encodeURIComponent(String(record)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Amends one record of one zone. Only the fields the body carries change; the rest keep the values they hold at the plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Amend a DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchDnsZonesByZoneRecordsByRecord: async (zone: string, record: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('patchDnsZonesByZoneRecordsByRecord', 'zone', zone)
            // verify required parameter 'record' is not null or undefined
            assertParamExists('patchDnsZonesByZoneRecordsByRecord', 'record', record)
            const localVarPath = `/v1/dns/zones/{zone}/records/{record}`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)))
                .replace(`{${"record"}}`, encodeURIComponent(String(record)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Replaces the calling org\'s zones and their records in bulk. The owning org is the caller\'s own validated claim, never the body, so a sync reaches nobody else\'s zones. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Push a set of zones and records in one call
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postDnsSync: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/dns/sync`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Creates a zone for the calling org — authoritative, or backed by a DNS provider the org has connected. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Create a DNS zone
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postDnsZones: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/dns/zones`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Creates a record in one zone. A provider-backed zone is written at the provider first, then mirrored locally so the resolver serves it. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Create a DNS record
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postDnsZonesByZoneRecords: async (zone: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('postDnsZonesByZoneRecords', 'zone', zone)
            const localVarPath = `/v1/dns/zones/{zone}/records`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Amends one record of one zone. Only the fields the body carries change; this is the same partial update PATCH performs, not a replacement of the whole record. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Amend a DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putDnsZonesByZoneRecordsByRecord: async (zone: string, record: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('putDnsZonesByZoneRecordsByRecord', 'zone', zone)
            // verify required parameter 'record' is not null or undefined
            assertParamExists('putDnsZonesByZoneRecordsByRecord', 'record', record)
            const localVarPath = `/v1/dns/zones/{zone}/records/{record}`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)))
                .replace(`{${"record"}}`, encodeURIComponent(String(record)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * DnsApi - functional programming interface
 * @export
 */
export const DnsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = DnsApiAxiosParamCreator(configuration)
    return {
        /**
         * Removes one of the calling org\'s zones, and the records in it, from the DNS control plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Delete a DNS zone
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteDnsZonesByZone(zone: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteDnsZonesByZone(zone, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.deleteDnsZonesByZone']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes one record from one zone. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Delete a DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteDnsZonesByZoneRecordsByRecord(zone: string, record: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteDnsZonesByZoneRecordsByRecord(zone, record, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.deleteDnsZonesByZoneRecordsByRecord']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reports whether the DNS control plane is answering. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Check the DNS control plane
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getDnsHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getDnsHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.getDnsHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists every DNS zone the calling org holds, authoritative and provider-backed alike. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary List your org\'s DNS zones
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getDnsZones(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getDnsZones(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.getDnsZones']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads one of the calling org\'s zones by name. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Read one DNS zone
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getDnsZonesByZone(zone: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getDnsZonesByZone(zone, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.getDnsZonesByZone']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the records in one zone. A provider-backed zone is read from the provider, which is its source of truth. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary List a zone\'s DNS records
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getDnsZonesByZoneRecords(zone: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getDnsZonesByZoneRecords(zone, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.getDnsZonesByZoneRecords']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads one record of one zone by its id. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Read one DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getDnsZonesByZoneRecordsByRecord(zone: string, record: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getDnsZonesByZoneRecordsByRecord(zone, record, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.getDnsZonesByZoneRecordsByRecord']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Amends one record of one zone. Only the fields the body carries change; the rest keep the values they hold at the plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Amend a DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchDnsZonesByZoneRecordsByRecord(zone: string, record: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchDnsZonesByZoneRecordsByRecord(zone, record, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.patchDnsZonesByZoneRecordsByRecord']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Replaces the calling org\'s zones and their records in bulk. The owning org is the caller\'s own validated claim, never the body, so a sync reaches nobody else\'s zones. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Push a set of zones and records in one call
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postDnsSync(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postDnsSync(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.postDnsSync']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Creates a zone for the calling org — authoritative, or backed by a DNS provider the org has connected. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Create a DNS zone
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postDnsZones(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postDnsZones(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.postDnsZones']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Creates a record in one zone. A provider-backed zone is written at the provider first, then mirrored locally so the resolver serves it. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Create a DNS record
         * @param {string} zone 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postDnsZonesByZoneRecords(zone: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postDnsZonesByZoneRecords(zone, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.postDnsZonesByZoneRecords']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Amends one record of one zone. Only the fields the body carries change; this is the same partial update PATCH performs, not a replacement of the whole record. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Amend a DNS record
         * @param {string} zone 
         * @param {string} record 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async putDnsZonesByZoneRecordsByRecord(zone: string, record: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.putDnsZonesByZoneRecordsByRecord(zone, record, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['DnsApi.putDnsZonesByZoneRecordsByRecord']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * DnsApi - factory interface
 * @export
 */
export const DnsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = DnsApiFp(configuration)
    return {
        /**
         * Removes one of the calling org\'s zones, and the records in it, from the DNS control plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Delete a DNS zone
         * @param {DnsApiDeleteDnsZonesByZoneRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteDnsZonesByZone(requestParameters: DnsApiDeleteDnsZonesByZoneRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteDnsZonesByZone(requestParameters.zone, options).then((request) => request(axios, basePath));
        },
        /**
         * Removes one record from one zone. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Delete a DNS record
         * @param {DnsApiDeleteDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiDeleteDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(axios, basePath));
        },
        /**
         * Reports whether the DNS control plane is answering. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Check the DNS control plane
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsHealth(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getDnsHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Lists every DNS zone the calling org holds, authoritative and provider-backed alike. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary List your org\'s DNS zones
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZones(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getDnsZones(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads one of the calling org\'s zones by name. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Read one DNS zone
         * @param {DnsApiGetDnsZonesByZoneRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZonesByZone(requestParameters: DnsApiGetDnsZonesByZoneRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getDnsZonesByZone(requestParameters.zone, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the records in one zone. A provider-backed zone is read from the provider, which is its source of truth. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary List a zone\'s DNS records
         * @param {DnsApiGetDnsZonesByZoneRecordsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZonesByZoneRecords(requestParameters: DnsApiGetDnsZonesByZoneRecordsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getDnsZonesByZoneRecords(requestParameters.zone, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads one record of one zone by its id. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Read one DNS record
         * @param {DnsApiGetDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiGetDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(axios, basePath));
        },
        /**
         * Amends one record of one zone. Only the fields the body carries change; the rest keep the values they hold at the plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Amend a DNS record
         * @param {DnsApiPatchDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiPatchDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.patchDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(axios, basePath));
        },
        /**
         * Replaces the calling org\'s zones and their records in bulk. The owning org is the caller\'s own validated claim, never the body, so a sync reaches nobody else\'s zones. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Push a set of zones and records in one call
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postDnsSync(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postDnsSync(options).then((request) => request(axios, basePath));
        },
        /**
         * Creates a zone for the calling org — authoritative, or backed by a DNS provider the org has connected. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Create a DNS zone
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postDnsZones(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postDnsZones(options).then((request) => request(axios, basePath));
        },
        /**
         * Creates a record in one zone. A provider-backed zone is written at the provider first, then mirrored locally so the resolver serves it. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Create a DNS record
         * @param {DnsApiPostDnsZonesByZoneRecordsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postDnsZonesByZoneRecords(requestParameters: DnsApiPostDnsZonesByZoneRecordsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postDnsZonesByZoneRecords(requestParameters.zone, options).then((request) => request(axios, basePath));
        },
        /**
         * Amends one record of one zone. Only the fields the body carries change; this is the same partial update PATCH performs, not a replacement of the whole record. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
         * @summary Amend a DNS record
         * @param {DnsApiPutDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        putDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiPutDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.putDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteDnsZonesByZone operation in DnsApi.
 * @export
 * @interface DnsApiDeleteDnsZonesByZoneRequest
 */
export interface DnsApiDeleteDnsZonesByZoneRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiDeleteDnsZonesByZone
     */
    readonly zone: string
}

/**
 * Request parameters for deleteDnsZonesByZoneRecordsByRecord operation in DnsApi.
 * @export
 * @interface DnsApiDeleteDnsZonesByZoneRecordsByRecordRequest
 */
export interface DnsApiDeleteDnsZonesByZoneRecordsByRecordRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiDeleteDnsZonesByZoneRecordsByRecord
     */
    readonly zone: string

    /**
     * 
     * @type {string}
     * @memberof DnsApiDeleteDnsZonesByZoneRecordsByRecord
     */
    readonly record: string
}

/**
 * Request parameters for getDnsZonesByZone operation in DnsApi.
 * @export
 * @interface DnsApiGetDnsZonesByZoneRequest
 */
export interface DnsApiGetDnsZonesByZoneRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiGetDnsZonesByZone
     */
    readonly zone: string
}

/**
 * Request parameters for getDnsZonesByZoneRecords operation in DnsApi.
 * @export
 * @interface DnsApiGetDnsZonesByZoneRecordsRequest
 */
export interface DnsApiGetDnsZonesByZoneRecordsRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiGetDnsZonesByZoneRecords
     */
    readonly zone: string
}

/**
 * Request parameters for getDnsZonesByZoneRecordsByRecord operation in DnsApi.
 * @export
 * @interface DnsApiGetDnsZonesByZoneRecordsByRecordRequest
 */
export interface DnsApiGetDnsZonesByZoneRecordsByRecordRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiGetDnsZonesByZoneRecordsByRecord
     */
    readonly zone: string

    /**
     * 
     * @type {string}
     * @memberof DnsApiGetDnsZonesByZoneRecordsByRecord
     */
    readonly record: string
}

/**
 * Request parameters for patchDnsZonesByZoneRecordsByRecord operation in DnsApi.
 * @export
 * @interface DnsApiPatchDnsZonesByZoneRecordsByRecordRequest
 */
export interface DnsApiPatchDnsZonesByZoneRecordsByRecordRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiPatchDnsZonesByZoneRecordsByRecord
     */
    readonly zone: string

    /**
     * 
     * @type {string}
     * @memberof DnsApiPatchDnsZonesByZoneRecordsByRecord
     */
    readonly record: string
}

/**
 * Request parameters for postDnsZonesByZoneRecords operation in DnsApi.
 * @export
 * @interface DnsApiPostDnsZonesByZoneRecordsRequest
 */
export interface DnsApiPostDnsZonesByZoneRecordsRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiPostDnsZonesByZoneRecords
     */
    readonly zone: string
}

/**
 * Request parameters for putDnsZonesByZoneRecordsByRecord operation in DnsApi.
 * @export
 * @interface DnsApiPutDnsZonesByZoneRecordsByRecordRequest
 */
export interface DnsApiPutDnsZonesByZoneRecordsByRecordRequest {
    /**
     * 
     * @type {string}
     * @memberof DnsApiPutDnsZonesByZoneRecordsByRecord
     */
    readonly zone: string

    /**
     * 
     * @type {string}
     * @memberof DnsApiPutDnsZonesByZoneRecordsByRecord
     */
    readonly record: string
}

/**
 * DnsApi - object-oriented interface
 * @export
 * @class DnsApi
 * @extends {BaseAPI}
 */
export class DnsApi extends BaseAPI {
    /**
     * Removes one of the calling org\'s zones, and the records in it, from the DNS control plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Delete a DNS zone
     * @param {DnsApiDeleteDnsZonesByZoneRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public deleteDnsZonesByZone(requestParameters: DnsApiDeleteDnsZonesByZoneRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).deleteDnsZonesByZone(requestParameters.zone, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes one record from one zone. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Delete a DNS record
     * @param {DnsApiDeleteDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public deleteDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiDeleteDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).deleteDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reports whether the DNS control plane is answering. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Check the DNS control plane
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public getDnsHealth(options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).getDnsHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists every DNS zone the calling org holds, authoritative and provider-backed alike. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary List your org\'s DNS zones
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public getDnsZones(options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).getDnsZones(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads one of the calling org\'s zones by name. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Read one DNS zone
     * @param {DnsApiGetDnsZonesByZoneRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public getDnsZonesByZone(requestParameters: DnsApiGetDnsZonesByZoneRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).getDnsZonesByZone(requestParameters.zone, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the records in one zone. A provider-backed zone is read from the provider, which is its source of truth. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary List a zone\'s DNS records
     * @param {DnsApiGetDnsZonesByZoneRecordsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public getDnsZonesByZoneRecords(requestParameters: DnsApiGetDnsZonesByZoneRecordsRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).getDnsZonesByZoneRecords(requestParameters.zone, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads one record of one zone by its id. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Read one DNS record
     * @param {DnsApiGetDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public getDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiGetDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).getDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Amends one record of one zone. Only the fields the body carries change; the rest keep the values they hold at the plane. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Amend a DNS record
     * @param {DnsApiPatchDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public patchDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiPatchDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).patchDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Replaces the calling org\'s zones and their records in bulk. The owning org is the caller\'s own validated claim, never the body, so a sync reaches nobody else\'s zones. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Push a set of zones and records in one call
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public postDnsSync(options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).postDnsSync(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Creates a zone for the calling org — authoritative, or backed by a DNS provider the org has connected. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Create a DNS zone
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public postDnsZones(options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).postDnsZones(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Creates a record in one zone. A provider-backed zone is written at the provider first, then mirrored locally so the resolver serves it. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Create a DNS record
     * @param {DnsApiPostDnsZonesByZoneRecordsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public postDnsZonesByZoneRecords(requestParameters: DnsApiPostDnsZonesByZoneRecordsRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).postDnsZonesByZoneRecords(requestParameters.zone, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Amends one record of one zone. Only the fields the body carries change; this is the same partial update PATCH performs, not a replacement of the whole record. The plane owns the authoritative zone and record store behind every name pointed at Hanzo; this head keeps none of it. The address and the query string ARE the plane\'s own, relayed verbatim, and the plane\'s answer comes back unchanged — its status code, its Content-Type, and its Location on a redirect this head never follows.  It travels under the CALLER\'S OWN identity and substitutes no service credential, which would collapse tenants: the caller\'s validated session bearer goes upstream as Authorization and the server-validated org as X-Org-Id, so a caller in one org reaches only that org\'s zones, exactly as if it had called the plane directly. The upstream host comes only from deployment config, never from the request, so no path can re-target another host.  Fails closed before a byte leaves cloud: no validated principal is 403; an API key is 401, because a pk-/sk- key is not a JWT the OIDC-gated plane can validate and there is no substitute credential to send in its place; a path that normalizes outside /v1/dns, or still carries a percent-escape or a `..` after one decode, is 400; an unconfigured plane is 503 and an unreachable one 502.
     * @summary Amend a DNS record
     * @param {DnsApiPutDnsZonesByZoneRecordsByRecordRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DnsApi
     */
    public putDnsZonesByZoneRecordsByRecord(requestParameters: DnsApiPutDnsZonesByZoneRecordsByRecordRequest, options?: RawAxiosRequestConfig) {
        return DnsApiFp(this.configuration).putDnsZonesByZoneRecordsByRecord(requestParameters.zone, requestParameters.record, options).then((request) => request(this.axios, this.basePath));
    }
}

