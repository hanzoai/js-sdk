/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: 8.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { AuthoredSkillList } from '../models';
// @ts-ignore
import type { SkillDeleted } from '../models';
// @ts-ignore
import type { SkillIn } from '../models';
// @ts-ignore
import type { SkillWritten } from '../models';
// @ts-ignore
import type { SourceToolList } from '../models';
/**
 * SkillsApi - axios parameter creator
 * @export
 */
export const SkillsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Removes one of the caller org\'s authored skills. Scoped to the caller\'s org, so an id belonging to another tenant is never reached. Removing what is not there is not an error — the caller\'s intent is \"gone\", and it is.
         * @summary Removes one of the caller org\'s authored skills.
         * @param {string} id ID is the skill to remove, from the path. It is the skill\&#39;s name.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1SkillsId: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('deleteV1SkillsId', 'id', id)
            const localVarPath = `/v1/skills/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag. A skill is discovery and activation metadata attached to an agent, never called directly, so every entry here is non-dispatchable. It is GET /v1/tools narrowed to one source, not a second store: a name a caller sees here is the same entry, with the same activation state, that discovery reports.
         * @summary Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag.
         * @param {string} [activated] Activated keeps only the tools activated for the caller\&#39;s org and project, and only when it is exactly the string \&quot;true\&quot;.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1Skills: async (activated?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/skills`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (activated !== undefined) {
                localVarQueryParameter['activated'] = activated;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the caller org\'s OWN skills with their SKILL.md bodies. GET /v1/skills is the registry view — the brand\'s catalogue plus this org\'s, with activation flags and no bodies; this is the EDITABLE set, so it carries the content that view omits and nothing the org did not write.
         * @summary Lists the caller org\'s OWN skills with their SKILL.md bodies.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1SkillsAuthored: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/skills/authored`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Serves a single agent skill\'s SKILL.md as text/markdown — the instructions a client follows once index.json has told it the skill exists, and byte for byte the document that index.json\'s sha256 for that skill was computed over.  The skill segment is a flat, service-prefixed id (`ai_models`): one path segment with no separators, so a request can never address anything outside the embedded catalogue. An id of any other shape, or a skill the serving brand does not carry, is `{\"error\":…}` at 404 — the same answer, so a probe learns nothing about which is which.  Brand resolution and caching are index.json\'s: the Host picks the catalogue, and the response is `Cache-Control: public, max-age=300`. Public — no bearer, no tenant scope.
         * @summary One skill\'s document, as markdown
         * @param {string} skill 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getWellKnownAgentSkillsBySkillSkillMd: async (skill: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'skill' is not null or undefined
            assertParamExists('getWellKnownAgentSkillsBySkillSkillMd', 'skill', skill)
            const localVarPath = `/.well-known/agent-skills/{skill}/SKILL.md`
                .replace(`{${"skill"}}`, encodeURIComponent(String(skill)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * The Agent Skills Discovery catalogue an AI client reads to learn what this deployment can do: every skill, with the sha256 of the SKILL.md that is actually served for it, so a client can verify the document it then fetches.  The catalogue is GENERATED from the per-service OpenAPI specs and embedded in the binary; this route serves those bytes verbatim and never re-derives them, which is what makes the digests hold. Which brand\'s catalogue you get is decided per request from the Host — api.hanzo.ai answers the Hanzo catalogue, api.lux.network the Lux one, api.zoo.ngo the Zoo one — never one brand\'s skills on another\'s surface; a Host whose brand has no embedded catalogue falls back to the deployment brand, then to hanzo.  Public by design: the discovery surface carries no secrets, so there is no bearer and no tenant scope. Answers `Cache-Control: public, max-age=300`, and a catalogue that is not embedded is `{\"error\":…}` at 404.
         * @summary The brand\'s master catalogue of agent skills
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getWellKnownAgentSkillsIndexJson: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/.well-known/agent-skills/index.json`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record. The id is derived from the name, so writing the same name again REVISES that skill rather than accumulating near-duplicates that would then collide in the registry. An org\'s skills are private to it by construction — they live in a different store from the brand\'s embedded catalogue and have no path into the public gallery — and a brand skill always wins a name collision against an org\'s.
         * @summary Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record.
         * @param {SkillIn} skillIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1Skills: async (skillIn: SkillIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'skillIn' is not null or undefined
            assertParamExists('postV1Skills', 'skillIn', skillIn)
            const localVarPath = `/v1/skills`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(skillIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * SkillsApi - functional programming interface
 * @export
 */
export const SkillsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = SkillsApiAxiosParamCreator(configuration)
    return {
        /**
         * Removes one of the caller org\'s authored skills. Scoped to the caller\'s org, so an id belonging to another tenant is never reached. Removing what is not there is not an error — the caller\'s intent is \"gone\", and it is.
         * @summary Removes one of the caller org\'s authored skills.
         * @param {string} id ID is the skill to remove, from the path. It is the skill\&#39;s name.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1SkillsId(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SkillDeleted>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1SkillsId(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SkillsApi.deleteV1SkillsId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag. A skill is discovery and activation metadata attached to an agent, never called directly, so every entry here is non-dispatchable. It is GET /v1/tools narrowed to one source, not a second store: a name a caller sees here is the same entry, with the same activation state, that discovery reports.
         * @summary Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag.
         * @param {string} [activated] Activated keeps only the tools activated for the caller\&#39;s org and project, and only when it is exactly the string \&quot;true\&quot;.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1Skills(activated?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SourceToolList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1Skills(activated, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SkillsApi.getV1Skills']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the caller org\'s OWN skills with their SKILL.md bodies. GET /v1/skills is the registry view — the brand\'s catalogue plus this org\'s, with activation flags and no bodies; this is the EDITABLE set, so it carries the content that view omits and nothing the org did not write.
         * @summary Lists the caller org\'s OWN skills with their SKILL.md bodies.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1SkillsAuthored(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<AuthoredSkillList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1SkillsAuthored(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SkillsApi.getV1SkillsAuthored']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Serves a single agent skill\'s SKILL.md as text/markdown — the instructions a client follows once index.json has told it the skill exists, and byte for byte the document that index.json\'s sha256 for that skill was computed over.  The skill segment is a flat, service-prefixed id (`ai_models`): one path segment with no separators, so a request can never address anything outside the embedded catalogue. An id of any other shape, or a skill the serving brand does not carry, is `{\"error\":…}` at 404 — the same answer, so a probe learns nothing about which is which.  Brand resolution and caching are index.json\'s: the Host picks the catalogue, and the response is `Cache-Control: public, max-age=300`. Public — no bearer, no tenant scope.
         * @summary One skill\'s document, as markdown
         * @param {string} skill 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getWellKnownAgentSkillsBySkillSkillMd(skill: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getWellKnownAgentSkillsBySkillSkillMd(skill, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SkillsApi.getWellKnownAgentSkillsBySkillSkillMd']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * The Agent Skills Discovery catalogue an AI client reads to learn what this deployment can do: every skill, with the sha256 of the SKILL.md that is actually served for it, so a client can verify the document it then fetches.  The catalogue is GENERATED from the per-service OpenAPI specs and embedded in the binary; this route serves those bytes verbatim and never re-derives them, which is what makes the digests hold. Which brand\'s catalogue you get is decided per request from the Host — api.hanzo.ai answers the Hanzo catalogue, api.lux.network the Lux one, api.zoo.ngo the Zoo one — never one brand\'s skills on another\'s surface; a Host whose brand has no embedded catalogue falls back to the deployment brand, then to hanzo.  Public by design: the discovery surface carries no secrets, so there is no bearer and no tenant scope. Answers `Cache-Control: public, max-age=300`, and a catalogue that is not embedded is `{\"error\":…}` at 404.
         * @summary The brand\'s master catalogue of agent skills
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getWellKnownAgentSkillsIndexJson(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getWellKnownAgentSkillsIndexJson(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SkillsApi.getWellKnownAgentSkillsIndexJson']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record. The id is derived from the name, so writing the same name again REVISES that skill rather than accumulating near-duplicates that would then collide in the registry. An org\'s skills are private to it by construction — they live in a different store from the brand\'s embedded catalogue and have no path into the public gallery — and a brand skill always wins a name collision against an org\'s.
         * @summary Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record.
         * @param {SkillIn} skillIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1Skills(skillIn: SkillIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SkillWritten>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1Skills(skillIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SkillsApi.postV1Skills']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * SkillsApi - factory interface
 * @export
 */
export const SkillsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = SkillsApiFp(configuration)
    return {
        /**
         * Removes one of the caller org\'s authored skills. Scoped to the caller\'s org, so an id belonging to another tenant is never reached. Removing what is not there is not an error — the caller\'s intent is \"gone\", and it is.
         * @summary Removes one of the caller org\'s authored skills.
         * @param {SkillsApiDeleteV1SkillsIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1SkillsId(requestParameters: SkillsApiDeleteV1SkillsIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<SkillDeleted> {
            return localVarFp.deleteV1SkillsId(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag. A skill is discovery and activation metadata attached to an agent, never called directly, so every entry here is non-dispatchable. It is GET /v1/tools narrowed to one source, not a second store: a name a caller sees here is the same entry, with the same activation state, that discovery reports.
         * @summary Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag.
         * @param {SkillsApiGetV1SkillsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1Skills(requestParameters: SkillsApiGetV1SkillsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<SourceToolList> {
            return localVarFp.getV1Skills(requestParameters.activated, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the caller org\'s OWN skills with their SKILL.md bodies. GET /v1/skills is the registry view — the brand\'s catalogue plus this org\'s, with activation flags and no bodies; this is the EDITABLE set, so it carries the content that view omits and nothing the org did not write.
         * @summary Lists the caller org\'s OWN skills with their SKILL.md bodies.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1SkillsAuthored(options?: RawAxiosRequestConfig): AxiosPromise<AuthoredSkillList> {
            return localVarFp.getV1SkillsAuthored(options).then((request) => request(axios, basePath));
        },
        /**
         * Serves a single agent skill\'s SKILL.md as text/markdown — the instructions a client follows once index.json has told it the skill exists, and byte for byte the document that index.json\'s sha256 for that skill was computed over.  The skill segment is a flat, service-prefixed id (`ai_models`): one path segment with no separators, so a request can never address anything outside the embedded catalogue. An id of any other shape, or a skill the serving brand does not carry, is `{\"error\":…}` at 404 — the same answer, so a probe learns nothing about which is which.  Brand resolution and caching are index.json\'s: the Host picks the catalogue, and the response is `Cache-Control: public, max-age=300`. Public — no bearer, no tenant scope.
         * @summary One skill\'s document, as markdown
         * @param {SkillsApiGetWellKnownAgentSkillsBySkillSkillMdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getWellKnownAgentSkillsBySkillSkillMd(requestParameters: SkillsApiGetWellKnownAgentSkillsBySkillSkillMdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getWellKnownAgentSkillsBySkillSkillMd(requestParameters.skill, options).then((request) => request(axios, basePath));
        },
        /**
         * The Agent Skills Discovery catalogue an AI client reads to learn what this deployment can do: every skill, with the sha256 of the SKILL.md that is actually served for it, so a client can verify the document it then fetches.  The catalogue is GENERATED from the per-service OpenAPI specs and embedded in the binary; this route serves those bytes verbatim and never re-derives them, which is what makes the digests hold. Which brand\'s catalogue you get is decided per request from the Host — api.hanzo.ai answers the Hanzo catalogue, api.lux.network the Lux one, api.zoo.ngo the Zoo one — never one brand\'s skills on another\'s surface; a Host whose brand has no embedded catalogue falls back to the deployment brand, then to hanzo.  Public by design: the discovery surface carries no secrets, so there is no bearer and no tenant scope. Answers `Cache-Control: public, max-age=300`, and a catalogue that is not embedded is `{\"error\":…}` at 404.
         * @summary The brand\'s master catalogue of agent skills
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getWellKnownAgentSkillsIndexJson(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getWellKnownAgentSkillsIndexJson(options).then((request) => request(axios, basePath));
        },
        /**
         * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record. The id is derived from the name, so writing the same name again REVISES that skill rather than accumulating near-duplicates that would then collide in the registry. An org\'s skills are private to it by construction — they live in a different store from the brand\'s embedded catalogue and have no path into the public gallery — and a brand skill always wins a name collision against an org\'s.
         * @summary Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record.
         * @param {SkillsApiPostV1SkillsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1Skills(requestParameters: SkillsApiPostV1SkillsRequest, options?: RawAxiosRequestConfig): AxiosPromise<SkillWritten> {
            return localVarFp.postV1Skills(requestParameters.skillIn, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteV1SkillsId operation in SkillsApi.
 * @export
 * @interface SkillsApiDeleteV1SkillsIdRequest
 */
export interface SkillsApiDeleteV1SkillsIdRequest {
    /**
     * ID is the skill to remove, from the path. It is the skill\&#39;s name.
     * @type {string}
     * @memberof SkillsApiDeleteV1SkillsId
     */
    readonly id: string
}

/**
 * Request parameters for getV1Skills operation in SkillsApi.
 * @export
 * @interface SkillsApiGetV1SkillsRequest
 */
export interface SkillsApiGetV1SkillsRequest {
    /**
     * Activated keeps only the tools activated for the caller\&#39;s org and project, and only when it is exactly the string \&quot;true\&quot;.
     * @type {string}
     * @memberof SkillsApiGetV1Skills
     */
    readonly activated?: string
}

/**
 * Request parameters for getWellKnownAgentSkillsBySkillSkillMd operation in SkillsApi.
 * @export
 * @interface SkillsApiGetWellKnownAgentSkillsBySkillSkillMdRequest
 */
export interface SkillsApiGetWellKnownAgentSkillsBySkillSkillMdRequest {
    /**
     * 
     * @type {string}
     * @memberof SkillsApiGetWellKnownAgentSkillsBySkillSkillMd
     */
    readonly skill: string
}

/**
 * Request parameters for postV1Skills operation in SkillsApi.
 * @export
 * @interface SkillsApiPostV1SkillsRequest
 */
export interface SkillsApiPostV1SkillsRequest {
    /**
     * 
     * @type {SkillIn}
     * @memberof SkillsApiPostV1Skills
     */
    readonly skillIn: SkillIn
}

/**
 * SkillsApi - object-oriented interface
 * @export
 * @class SkillsApi
 * @extends {BaseAPI}
 */
export class SkillsApi extends BaseAPI {
    /**
     * Removes one of the caller org\'s authored skills. Scoped to the caller\'s org, so an id belonging to another tenant is never reached. Removing what is not there is not an error — the caller\'s intent is \"gone\", and it is.
     * @summary Removes one of the caller org\'s authored skills.
     * @param {SkillsApiDeleteV1SkillsIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SkillsApi
     */
    public deleteV1SkillsId(requestParameters: SkillsApiDeleteV1SkillsIdRequest, options?: RawAxiosRequestConfig) {
        return SkillsApiFp(this.configuration).deleteV1SkillsId(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag. A skill is discovery and activation metadata attached to an agent, never called directly, so every entry here is non-dispatchable. It is GET /v1/tools narrowed to one source, not a second store: a name a caller sees here is the same entry, with the same activation state, that discovery reports.
     * @summary Lists the skills the caller\'s org can reach — the brand\'s embedded catalogue plus the org\'s own authored ones — with each one\'s activation flag.
     * @param {SkillsApiGetV1SkillsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SkillsApi
     */
    public getV1Skills(requestParameters: SkillsApiGetV1SkillsRequest = {}, options?: RawAxiosRequestConfig) {
        return SkillsApiFp(this.configuration).getV1Skills(requestParameters.activated, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the caller org\'s OWN skills with their SKILL.md bodies. GET /v1/skills is the registry view — the brand\'s catalogue plus this org\'s, with activation flags and no bodies; this is the EDITABLE set, so it carries the content that view omits and nothing the org did not write.
     * @summary Lists the caller org\'s OWN skills with their SKILL.md bodies.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SkillsApi
     */
    public getV1SkillsAuthored(options?: RawAxiosRequestConfig) {
        return SkillsApiFp(this.configuration).getV1SkillsAuthored(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Serves a single agent skill\'s SKILL.md as text/markdown — the instructions a client follows once index.json has told it the skill exists, and byte for byte the document that index.json\'s sha256 for that skill was computed over.  The skill segment is a flat, service-prefixed id (`ai_models`): one path segment with no separators, so a request can never address anything outside the embedded catalogue. An id of any other shape, or a skill the serving brand does not carry, is `{\"error\":…}` at 404 — the same answer, so a probe learns nothing about which is which.  Brand resolution and caching are index.json\'s: the Host picks the catalogue, and the response is `Cache-Control: public, max-age=300`. Public — no bearer, no tenant scope.
     * @summary One skill\'s document, as markdown
     * @param {SkillsApiGetWellKnownAgentSkillsBySkillSkillMdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SkillsApi
     */
    public getWellKnownAgentSkillsBySkillSkillMd(requestParameters: SkillsApiGetWellKnownAgentSkillsBySkillSkillMdRequest, options?: RawAxiosRequestConfig) {
        return SkillsApiFp(this.configuration).getWellKnownAgentSkillsBySkillSkillMd(requestParameters.skill, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * The Agent Skills Discovery catalogue an AI client reads to learn what this deployment can do: every skill, with the sha256 of the SKILL.md that is actually served for it, so a client can verify the document it then fetches.  The catalogue is GENERATED from the per-service OpenAPI specs and embedded in the binary; this route serves those bytes verbatim and never re-derives them, which is what makes the digests hold. Which brand\'s catalogue you get is decided per request from the Host — api.hanzo.ai answers the Hanzo catalogue, api.lux.network the Lux one, api.zoo.ngo the Zoo one — never one brand\'s skills on another\'s surface; a Host whose brand has no embedded catalogue falls back to the deployment brand, then to hanzo.  Public by design: the discovery surface carries no secrets, so there is no bearer and no tenant scope. Answers `Cache-Control: public, max-age=300`, and a catalogue that is not embedded is `{\"error\":…}` at 404.
     * @summary The brand\'s master catalogue of agent skills
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SkillsApi
     */
    public getWellKnownAgentSkillsIndexJson(options?: RawAxiosRequestConfig) {
        return SkillsApiFp(this.configuration).getWellKnownAgentSkillsIndexJson(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record. The id is derived from the name, so writing the same name again REVISES that skill rather than accumulating near-duplicates that would then collide in the registry. An org\'s skills are private to it by construction — they live in a different store from the brand\'s embedded catalogue and have no path into the public gallery — and a brand skill always wins a name collision against an org\'s.
     * @summary Adds or revises one of the caller org\'s own skills, and answers 201 with the stored record.
     * @param {SkillsApiPostV1SkillsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SkillsApi
     */
    public postV1Skills(requestParameters: SkillsApiPostV1SkillsRequest, options?: RawAxiosRequestConfig) {
        return SkillsApiFp(this.configuration).postV1Skills(requestParameters.skillIn, options).then((request) => request(this.axios, this.basePath));
    }
}

