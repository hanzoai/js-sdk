/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudGpuAlertList } from '../models';
// @ts-ignore
import type { CloudGpuList } from '../models';
/**
 * GpusApi - axios parameter creator
 * @export
 */
export const GpusApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts. It stays a real, tenant-gated route so the console\'s alerts fetch resolves (200 [], not a 404) — an honest \"no alerts\", the same discipline the rest of the surface follows.
         * @summary Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListGpuAlerts: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/gpus/alerts`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.  Live telemetry is absent on Visor rows because Visor\'s machine object carries none — an honest omission the console renders as \"—\", never a fabricated 0.
         * @summary Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListGpus: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/gpus`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * GpusApi - functional programming interface
 * @export
 */
export const GpusApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = GpusApiAxiosParamCreator(configuration)
    return {
        /**
         * Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts. It stays a real, tenant-gated route so the console\'s alerts fetch resolves (200 [], not a 404) — an honest \"no alerts\", the same discipline the rest of the surface follows.
         * @summary Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudListGpuAlerts(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudGpuAlertList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudListGpuAlerts(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GpusApi.cloudListGpuAlerts']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.  Live telemetry is absent on Visor rows because Visor\'s machine object carries none — an honest omission the console renders as \"—\", never a fabricated 0.
         * @summary Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudListGpus(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudGpuList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudListGpus(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['GpusApi.cloudListGpus']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * GpusApi - factory interface
 * @export
 */
export const GpusApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = GpusApiFp(configuration)
    return {
        /**
         * Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts. It stays a real, tenant-gated route so the console\'s alerts fetch resolves (200 [], not a 404) — an honest \"no alerts\", the same discipline the rest of the surface follows.
         * @summary Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListGpuAlerts(options?: RawAxiosRequestConfig): AxiosPromise<CloudGpuAlertList> {
            return localVarFp.cloudListGpuAlerts(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.  Live telemetry is absent on Visor rows because Visor\'s machine object carries none — an honest omission the console renders as \"—\", never a fabricated 0.
         * @summary Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListGpus(options?: RawAxiosRequestConfig): AxiosPromise<CloudGpuList> {
            return localVarFp.cloudListGpus(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * GpusApi - object-oriented interface
 * @export
 * @class GpusApi
 * @extends {BaseAPI}
 */
export class GpusApi extends BaseAPI {
    /**
     * Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts. It stays a real, tenant-gated route so the console\'s alerts fetch resolves (200 [], not a 404) — an honest \"no alerts\", the same discipline the rest of the surface follows.
     * @summary Is an HONEST empty surface: Visor exposes no GPU alert inventory, so this returns [] rather than fabricating alerts.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GpusApi
     */
    public cloudListGpuAlerts(options?: RawAxiosRequestConfig) {
        return GpusApiFp(this.configuration).cloudListGpuAlerts(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.  Live telemetry is absent on Visor rows because Visor\'s machine object carries none — an honest omission the console renders as \"—\", never a fabricated 0.
     * @summary Returns one row per physical accelerator the caller\'s org has, derived from its real GPU machines (the size slug says how many cards a node holds) and from the accelerators BYO workers report through nvidia-smi.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof GpusApi
     */
    public cloudListGpus(options?: RawAxiosRequestConfig) {
        return GpusApiFp(this.configuration).cloudListGpus(options).then((request) => request(this.axios, this.basePath));
    }
}

