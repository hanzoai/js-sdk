/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { ChainList } from '../models';
// @ts-ignore
import type { ChainStatus } from '../models';
/**
 * ChainsApi - axios parameter creator
 * @export
 */
export const ChainsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
         * @summary Reports the chains this deployment can reach.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getChains: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/chains`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
         * @summary Reports one chain and whether its upstream is answering.
         * @param {string} chain Chain is the registry id, as in /v1/chains/lux.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getChainsByChain: async (chain: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'chain' is not null or undefined
            assertParamExists('getChainsByChain', 'chain', chain)
            const localVarPath = `/v1/chains/{chain}`
                .replace(`{${"chain"}}`, encodeURIComponent(String(chain)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * ChainsApi - functional programming interface
 * @export
 */
export const ChainsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = ChainsApiAxiosParamCreator(configuration)
    return {
        /**
         * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
         * @summary Reports the chains this deployment can reach.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getChains(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ChainList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getChains(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ChainsApi.getChains']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
         * @summary Reports one chain and whether its upstream is answering.
         * @param {string} chain Chain is the registry id, as in /v1/chains/lux.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getChainsByChain(chain: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ChainStatus>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getChainsByChain(chain, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ChainsApi.getChainsByChain']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * ChainsApi - factory interface
 * @export
 */
export const ChainsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = ChainsApiFp(configuration)
    return {
        /**
         * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
         * @summary Reports the chains this deployment can reach.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getChains(options?: RawAxiosRequestConfig): AxiosPromise<ChainList> {
            return localVarFp.getChains(options).then((request) => request(axios, basePath));
        },
        /**
         * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
         * @summary Reports one chain and whether its upstream is answering.
         * @param {ChainsApiGetChainsByChainRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getChainsByChain(requestParameters: ChainsApiGetChainsByChainRequest, options?: RawAxiosRequestConfig): AxiosPromise<ChainStatus> {
            return localVarFp.getChainsByChain(requestParameters.chain, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for getChainsByChain operation in ChainsApi.
 * @export
 * @interface ChainsApiGetChainsByChainRequest
 */
export interface ChainsApiGetChainsByChainRequest {
    /**
     * Chain is the registry id, as in /v1/chains/lux.
     * @type {string}
     * @memberof ChainsApiGetChainsByChain
     */
    readonly chain: string
}

/**
 * ChainsApi - object-oriented interface
 * @export
 * @class ChainsApi
 * @extends {BaseAPI}
 */
export class ChainsApi extends BaseAPI {
    /**
     * Reports the chains this deployment can reach. The list is the declared registry, so it is exactly what /v1/rpc will accept — a chain that appears here is one this deployment actually has an upstream for.
     * @summary Reports the chains this deployment can reach.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ChainsApi
     */
    public getChains(options?: RawAxiosRequestConfig) {
        return ChainsApiFp(this.configuration).getChains(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reports one chain and whether its upstream is answering. An unreachable chain is still a 200 with live:false — the chain is configured, which is a different fact from the chain being up, and a 502 here would make a console page error rather than show the outage.
     * @summary Reports one chain and whether its upstream is answering.
     * @param {ChainsApiGetChainsByChainRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ChainsApi
     */
    public getChainsByChain(requestParameters: ChainsApiGetChainsByChainRequest, options?: RawAxiosRequestConfig) {
        return ChainsApiFp(this.configuration).getChainsByChain(requestParameters.chain, options).then((request) => request(this.axios, this.basePath));
    }
}

