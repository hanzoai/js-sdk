/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { ClearReferenceOut } from '../models';
// @ts-ignore
import type { ReferenceOut } from '../models';
// @ts-ignore
import type { ReferenceSetsOut } from '../models';
// @ts-ignore
import type { RefreshReferenceIn } from '../models';
// @ts-ignore
import type { RefreshReferenceOut } from '../models';
// @ts-ignore
import type { ResolveReferenceIn } from '../models';
// @ts-ignore
import type { ResolveReferenceOut } from '../models';
// @ts-ignore
import type { RiskAdoptIn } from '../models';
// @ts-ignore
import type { RiskAppetiteIn } from '../models';
// @ts-ignore
import type { RiskCatalog } from '../models';
// @ts-ignore
import type { RiskDataset } from '../models';
// @ts-ignore
import type { RiskDatasetDisposal } from '../models';
// @ts-ignore
import type { RiskDatasetList } from '../models';
// @ts-ignore
import type { RiskDatasetRows } from '../models';
// @ts-ignore
import type { RiskDatasetSpec } from '../models';
// @ts-ignore
import type { RiskDatasetVersions } from '../models';
// @ts-ignore
import type { RiskDisposeIn } from '../models';
// @ts-ignore
import type { RiskDisposeOut } from '../models';
// @ts-ignore
import type { RiskHoldIn } from '../models';
// @ts-ignore
import type { RiskHoldOut } from '../models';
// @ts-ignore
import type { RiskLabelCoverage } from '../models';
// @ts-ignore
import type { RiskLabelIn } from '../models';
// @ts-ignore
import type { RiskLabelOut } from '../models';
// @ts-ignore
import type { RiskLabelVocabulary } from '../models';
// @ts-ignore
import type { RiskLabelsOut } from '../models';
// @ts-ignore
import type { RiskLearnIn } from '../models';
// @ts-ignore
import type { RiskLearnOut } from '../models';
// @ts-ignore
import type { RiskLineage } from '../models';
// @ts-ignore
import type { RiskModelState } from '../models';
// @ts-ignore
import type { RiskPolicyOut } from '../models';
// @ts-ignore
import type { RiskPublishOut } from '../models';
// @ts-ignore
import type { RiskResolveIn } from '../models';
// @ts-ignore
import type { RiskResolveOut } from '../models';
// @ts-ignore
import type { RiskScoreIn } from '../models';
// @ts-ignore
import type { RiskScoreOut } from '../models';
// @ts-ignore
import type { RiskSearchIn } from '../models';
// @ts-ignore
import type { RiskSearchReport } from '../models';
// @ts-ignore
import type { RiskSearchRun } from '../models';
// @ts-ignore
import type { SetReferenceIn } from '../models';
// @ts-ignore
import type { SetReferenceOut } from '../models';
/**
 * RiskApi - axios parameter creator
 * @export
 */
export const RiskApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Reports whether the risk model plane can actually work right now.  It is a REAL probe, not status theatre: it reports that the per-organisation model plane was built, that the per-organisation stores can be written, and whether the event surface the feature plane is rolled up from is reachable. 200 only when the plane can work; otherwise 503 CARRYING THE REPORT — which part failed and the real error.  An unreachable event surface is REPORTED and is not a failure. Scoring reads in-memory aggregates and never the warehouse, so a warm that cannot run degrades how much history a model has seen and does not stop it deciding.  It also reports how many organisations\' models are resident, how many have been evicted to hold that bound, and how many of the resident ones are at their own aggregate bound. Eviction is lossless — learned state is written to that organisation\'s own store first and its aggregates rebuild from its own record — so a climbing count is a capacity signal, not a loss. A STRAINED model is different: it has started forgetting its own least-recently-active subjects, and each forgotten subject reads as inactive until it is active again. That is a control degrading, and it is reported here because it is otherwise silent.  It answers about the process, not about a tenant: it takes no organisation and names none.
         * @summary Reports whether the risk model plane can actually work right now.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1RiskHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Puts one of your organisation\'s OWN PUBLISHED VALUES in force, by name — which is what an instant rollback is, what promoting a challenger is, and what installing the shape a search found is.  IT TAKES AN ADDRESS AND NEVER STATE. The masses are read from your own store, so nothing about your model has to be held by whatever is making this call. That closes the sharpest edge the previous shape had: a body of counters is something a caller can COMPOSE, and a region filled until activity inside it reads as ordinary is a model that has been shaped rather than learned. The engine\'s mass invariant was the only thing standing between a composed body and the model; with an address there is no body to compose.  IT ADOPTS THE SHAPE, NOT ONLY THE MASSES. A value records the model space its masses were taken in, and a value whose space differs from the one in force REPLANTS your model into that space before restoring them. That is what makes POST /v1/risk/search actionable: a search answers with the shape that fits your own history best and publishes it fitted, and its address is what you name here. Before this, a winning shape was advice nobody could take — the adoption path refused every shape change, and a winner is a different shape by definition.  WHAT ADOPTING A SEARCHED SHAPE COSTS, SAID PLAINLY: the value a search fits has learned the window the search replayed and nothing older, so installing it trades history for fit. Your appetite is untouched — that is your policy record\'s, with its own versions — and so is the geometry, which stays your own.  An address your organisation has not published is NOT FOUND. That includes one another organisation published, and it is not a lookup that failed: the store is per organisation and the address is a name, never an authority.
         * @summary Put one of your organisation\'s own published model values in force
         * @param {RiskAdoptIn} riskAdoptIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskAdoptModel: async (riskAdoptIn: RiskAdoptIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskAdoptIn' is not null or undefined
            assertParamExists('riskAdoptModel', 'riskAdoptIn', riskAdoptIn)
            const localVarPath = `/v1/risk/state/model`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskAdoptIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes one of your organisation\'s overrides.  It removes an entry your organisation wrote, never a baseline member: the published set is not writable from here, so a removal can only ever restore the baseline\'s own answer.
         * @summary Removes one of your organisation\'s overrides.
         * @param {string} set 
         * @param {string} [key] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskClearReference: async (set: string, key?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'set' is not null or undefined
            assertParamExists('riskClearReference', 'set', set)
            const localVarPath = `/v1/risk/reference/{set}`
                .replace(`{${"set"}}`, encodeURIComponent(String(set)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (key !== undefined) {
                localVarQueryParameter['key'] = key;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
         * @summary Declare the next version of a dataset
         * @param {RiskDatasetSpec} riskDatasetSpec 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskCreateDataset: async (riskDatasetSpec: RiskDatasetSpec, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskDatasetSpec' is not null or undefined
            assertParamExists('riskCreateDataset', 'riskDatasetSpec', riskDatasetSpec)
            const localVarPath = `/v1/risk/datasets`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskDatasetSpec, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
         * @summary Describe every version of one dataset
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDataset: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('riskDataset', 'name', name)
            const localVarPath = `/v1/risk/datasets/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
         * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to trace. Zero takes the newest published one.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDatasetLineage: async (name: string, version?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('riskDatasetLineage', 'name', name)
            const localVarPath = `/v1/risk/datasets/{name}/lineage`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (version !== undefined) {
                localVarQueryParameter['version'] = version;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
         * @summary List this org\'s datasets
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDatasets: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/datasets`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
         * @summary Dispose of one dataset and every version of it
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDeleteDataset: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('riskDeleteDataset', 'name', name)
            const localVarPath = `/v1/risk/datasets/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Applies this tenant\'s retention, and only this tenant\'s.  It is bounded three ways, each a compliance property rather than a convenience. It refuses a boundary younger than the platform floor, because a label can be the input to an adverse action and five years is what the retention ledger holds such a record for. It never touches a record under litigation hold. And it disposes of whole records rather than redacting fields.  It removes the derived columnar copy BEFORE the record, and refuses the whole disposal if the warehouse cannot be reached. The other order would leave rows in the warehouse that nothing can identify any more, which is a disposal that did not happen and says it did.
         * @summary Dispose of this tenant\'s expired assertions, whole records only
         * @param {RiskDisposeIn} riskDisposeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDisposeLabels: async (riskDisposeIn: RiskDisposeIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskDisposeIn' is not null or undefined
            assertParamExists('riskDisposeLabels', 'riskDisposeIn', riskDisposeIn)
            const localVarPath = `/v1/risk/labels/dispose`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskDisposeIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
         * @summary Read a version\'s rows back, one page at a time
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to read. Zero takes the newest published one.
         * @param {string} [split] Split narrows to train, val or test. Empty reads every split.
         * @param {number} [offset] Offset is where the page starts, in the version\&#39;s own row order (by id, which is derived from the row and therefore stable forever).
         * @param {number} [limit] Limit is how many rows to return. Zero and anything above the plane\&#39;s bound take the bound.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskExportDataset: async (name: string, version?: number, split?: string, offset?: number, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('riskExportDataset', 'name', name)
            const localVarPath = `/v1/risk/datasets/{name}/export`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (version !== undefined) {
                localVarQueryParameter['version'] = version;
            }

            if (split !== undefined) {
                localVarQueryParameter['split'] = split;
            }

            if (offset !== undefined) {
                localVarQueryParameter['offset'] = offset;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Features is the feature catalogue in its two honest lenses.  The MODEL lens is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves, the supervisor\'s own words for the indicator, and the published standard those words come from — so a coverage claim is checkable rather than asserted. It is the same for every organisation.  The SURFACE lens is what THIS organisation\'s own event surface actually carries, measured over the window: how many of its buckets carry each dimension at all, and what the dimension reads where it is present. A dimension present in no bucket is BLIND, and saying so is the difference between no risk and no data.
         * @summary The feature catalogue: what the model reads, and what your surface carries
         * @param {number} [days] Days is how far back to measure the organisation\&#39;s own coverage, 1 to 400. Zero takes thirty.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskFeatures: async (days?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/features`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (days !== undefined) {
                localVarQueryParameter['days'] = days;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Places or releases a litigation hold on named records.  A hold is a fact about the RECORD, not about the world: it says retention may not dispose of this row, and it asserts nothing about what happened. So it is not a field on an assertion and it is not folded into the content digest — carried there it was silently a no-op on any record that already existed, since re-filing the same assertion with a hold flag produced the same digest, the insert was ignored, and the caller was answered `duplicate` while the hold it asked for was never placed. This op is the one way a hold moves, in either direction, and the move is written to the audit log.  Every named id is this tenant\'s or is nothing. The statement runs against the tenant\'s own file, which holds no other tenant\'s rows and has no column that could name one.
         * @summary Place or release a litigation hold on named records
         * @param {RiskHoldIn} riskHoldIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskHoldLabels: async (riskHoldIn: RiskHoldIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskHoldIn' is not null or undefined
            assertParamExists('riskHoldLabels', 'riskHoldIn', riskHoldIn)
            const localVarPath = `/v1/risk/labels/hold`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskHoldIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Records a batch of ground truth against the entities it judges.  Each assertion carries TWO times — when the judged event happened, and when the assertion became knowable — and both are required. The second is what keeps a chargeback that landed in June out of a model that had to decide in February.  It is idempotent on the CONTENT of an assertion, so a webhook that redelivers is safe. It never overwrites: a source that corrects itself later files a NEW assertion, which wins from the moment it became knowable and leaves every earlier observation instant seeing exactly what it saw.  The asserter is stamped from the validated credential and is not a body field.
         * @summary Assert ground truth about events
         * @param {RiskLabelIn} riskLabelIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabel: async (riskLabelIn: RiskLabelIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskLabelIn' is not null or undefined
            assertParamExists('riskLabel', 'riskLabelIn', riskLabelIn)
            const localVarPath = `/v1/risk/labels`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskLabelIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reports how much of a window has matured and how much of that is judged, per source.  It is the gate on training. A supervised fit over a window whose judged count is near zero produces a number, and the number is meaningless; this op is what lets that be stated before the fit rather than discovered after it.  It reads the RECORD plane and folds every assertion at that event\'s OWN as-of, so the counts obey exactly the leakage rule a materialisation would. It counts only what was ASSERTED: what share of the whole event STREAM carries a label is a question about the feature plane\'s denominator and is not answerable here.
         * @summary How much of the window has matured, and how much of that is judged
         * @param {string} [from] From and To bound the EVENT window, half-open, RFC 3339.  Unstated, the window is the 90 days ENDING where maturity begins — &#x60;to&#x60; is the horizon ago, not now. A default window running to now under a default horizon could not contain one matured event, so every count below it would be zero however much ground truth the tenant held.
         * @param {string} [to] 
         * @param {number} [horizon] Horizon is the maturity horizon in days the coverage is measured under. Unstated takes 120. It also moves the default window, which ends where maturity begins.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabelCoverage: async (from?: string, to?: string, horizon?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/labels/coverage`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (from !== undefined) {
                localVarQueryParameter['from'] = from;
            }

            if (to !== undefined) {
                localVarQueryParameter['to'] = to;
            }

            if (horizon !== undefined) {
                localVarQueryParameter['horizon'] = horizon;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Publishes the closed vocabularies and the precedence rule that resolves a conflict between two sources.  A precedence rule nobody can read is a rule nobody can audit or dispute, and the whole defensibility of a contested label rests on being able to say why one assertion beat another. The order returned here is derived from the same declaration the resolver reads — it is not a description of it.
         * @summary The closed vocabularies and the precedence rule that resolves a conflict
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabelVocabulary: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/labels/vocabulary`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads the assertions this tenant has recorded, newest event first.  It reads the RECORD — the tenant\'s own store — and not the columnar copy, so what it returns is what would be produced in an audit. Narrow it by entity, by asserter, or by event window.
         * @summary Read the assertions this tenant has recorded
         * @param {string} [kind] Kind and Subject narrow to one entity.
         * @param {string} [subject] 
         * @param {string} [source] Source narrows to one asserter — the read that answers \&quot;what has commerce told us\&quot;, separately from \&quot;what has an analyst told us\&quot;.
         * @param {string} [from] From and To bound the EVENT time, half-open, RFC 3339.
         * @param {string} [to] 
         * @param {number} [limit] Limit caps the page. Out of range takes the plane\&#39;s own bound.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabels: async (kind?: string, subject?: string, source?: string, from?: string, to?: string, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/labels`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (kind !== undefined) {
                localVarQueryParameter['kind'] = kind;
            }

            if (subject !== undefined) {
                localVarQueryParameter['subject'] = subject;
            }

            if (source !== undefined) {
                localVarQueryParameter['source'] = source;
            }

            if (from !== undefined) {
                localVarQueryParameter['from'] = from;
            }

            if (to !== undefined) {
                localVarQueryParameter['to'] = to;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Learn records a batch of events into the caller organisation\'s own aggregates and lets its model learn from them. It answers how many it learned from.  IT DOES NOT SCORE, AND THAT IS THE POINT. An observation is a value you record; learning is a transformation over observations; a verdict is a query against the result. This op is the first two. [ops.score] is the third, it is pure, and it is the ONE door to a verdict. They were one call, which meant you could not record without training and could not train without being answered — and the model ran twice over every event to produce a verdict the response carried and no caller read.  TO OBSERVE AND JUDGE, COMPOSE THE TWO, and mind the order. Score FIRST, then learn: the score is then the model\'s opinion of an event it has not yet learned from, which is the question worth asking. The other order answers for a model that has already absorbed the event it is judging.  This is the training path, and there is no job behind it: the model IS a set of mass counters over half-space trees, so learning is an increment and the model is current the instant the last event lands. Nothing from any other organisation is in it, and nothing from this organisation leaves it.  A RETRY IS INERT. The record deduplicates on the event id you send, and an event already in it moves nothing, costs nothing and is not counted — so a client that timed out can send the same batch again and its model holds what it holds. Without an id of your own there is nothing to converge on: two identical bodies are two events.
         * @summary Teach your organisation\'s own model from its own events
         * @param {RiskLearnIn} riskLearnIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLearn: async (riskLearnIn: RiskLearnIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskLearnIn' is not null or undefined
            assertParamExists('riskLearn', 'riskLearnIn', riskLearnIn)
            const localVarPath = `/v1/risk/learn`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskLearnIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
         * @summary Materialise the declared version into immutable rows
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskMaterializeDataset: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('riskMaterializeDataset', 'name', name)
            const localVarPath = `/v1/risk/datasets/{name}/materialize`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Policy reports the caller organisation\'s own decision-regime history: every distinct regime it has adopted, which version is in force, and what retention has taken.  WHY IT EXISTS. Every score cites the version it was decided under ([riskScoreOut.Policy]), and the threshold that score was measured against is derived from the appetite that version states. Restate the appetite and, without this record, every earlier decision becomes unreconstructible — the cut it was judged by no longer exists anywhere. An adverse decision that cannot be explained against the policy in force when it was taken cannot be defended.  It covers ONE organisation. The history is on that organisation\'s own shelf, so another\'s versions are not filtered out of the answer — they are not in the file the answer is read from.
         * @summary Your organisation\'s decision-regime history, and which version is in force
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskPolicy: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/policy`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Publishes your organisation\'s model as a NAMED VALUE, so a decision taken today can be reconstructed tomorrow and a change made today can be undone.  It answers with a NAME and not with the state. The masses stay on your organisation\'s own encrypted store and are referred to by an address computed from their own content: the shape, the geometry seed, the position in the window, the threshold, the masses themselves as IEEE-754 bits, and the fold watermark behind them. That is what makes the value nameable without making the caller its custodian.  IT IS IDEMPOTENT ON THE VALUE. A model that has not changed publishes to the name it already has and mints nothing, reporting minted=false — so publishing at every boundary that matters is free. Ten values are retained per organisation, bounded in BYTES rather than in rows, and the oldest is disposed of past that.  A model that has learned nothing is refused: planted is not learned, and a value that reproduces nothing is not a value.  It is POST and PUT on one address because they are one plane\'s two verbs over one kind of thing: POST mints a value from the model in force, PUT puts a value in force. They were /v1/risk/state/snapshot and /v1/risk/state/restore — two addresses named after the operation rather than after the thing, which is how a reader ends up asking what the difference between a snapshot and a value is.
         * @summary Publish your organisation\'s model as a named, immutable value
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskPublishModel: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/state/model`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reference describes one set and lists your org\'s overrides in it.  The set half is public data about a published list — its version, its publishers, their licences and how current each one is. The overrides half is yours alone: it is read from your organisation\'s own store, and no other organisation\'s entries can appear in it.
         * @summary Reference describes one set and lists your org\'s overrides in it.
         * @param {string} set 
         * @param {string} [after] After pages the override listing: the last key of the previous page.
         * @param {number} [limit] Limit caps the override listing: default 200, maximum 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskReference: async (set: string, after?: string, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'set' is not null or undefined
            assertParamExists('riskReference', 'set', set)
            const localVarPath = `/v1/risk/reference/{set}`
                .replace(`{${"set"}}`, encodeURIComponent(String(set)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            if (after !== undefined) {
                localVarQueryParameter['after'] = after;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists every set this plane publishes, with its version and how fresh it is.  Read the Stale and Refused lists first: they are the two ways this plane can be quietly wrong, and they are reported rather than inferred. A set in Refused answers nothing — it has never loaded, it is held by another component, or it names a source we hold no licence for.
         * @summary Lists every set this plane publishes, with its version and how fresh it is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskReferenceSets: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/reference`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Takes a new version of one set. SuperAdmin only.  It is platform work, not tenant work: it writes the shared baseline every organisation reads, so it is gated to the platform\'s own identity. Nothing here can write an organisation\'s overrides, and nothing an organisation sends can reach this route.  Idempotent. A version is the content digest of what was taken, so refreshing an unchanged publisher writes no rows and reports unchanged. Resumable: a run that died half-way is continued from where it stopped rather than restarted.  A set whose source needs a licence we do not hold is refused with the reason, rather than being quietly skipped.
         * @summary Takes a new version of one set.
         * @param {RefreshReferenceIn} refreshReferenceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskRefreshReference: async (refreshReferenceIn: RefreshReferenceIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'refreshReferenceIn' is not null or undefined
            assertParamExists('riskRefreshReference', 'refreshReferenceIn', refreshReferenceIn)
            const localVarPath = `/v1/risk/reference/refresh`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(refreshReferenceIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers, for each named event, which assertion was in force AS OF that event\'s own horizon — and what disagreed with it.  This is the join surface: the dataset materialiser calls it to attach ground truth to training rows, and the evaluator calls it to score a past decision against what was knowable when the decision had to be made. One mechanism for both, so a model can never be trained under one leakage rule and scored under another.  Three answers are distinct and all three are honest: a resolved label, an event that has not matured, and a matured event nobody has judged. The last is never reported as unproductive.
         * @summary Resolve the label in force for named events, as of each event\'s own horizon
         * @param {RiskResolveIn} riskResolveIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskResolveLabels: async (riskResolveIn: RiskResolveIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskResolveIn' is not null or undefined
            assertParamExists('riskResolveLabels', 'riskResolveIn', riskResolveIn)
            const localVarPath = `/v1/risk/labels/resolve`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskResolveIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Looks keys up against the reference plane.  Your organisation\'s own overrides are consulted FIRST and win outright; the shared baseline answers everything they do not cover. Every answer names the version that produced it, when that version was current and whether it is stale, so a decision can record exactly what it consulted.  Read Refusal before reading Hit. A set that has never loaded, one held by the component that screens against it, and one whose source needs a licence we do not hold all answer with a refusal — and a miss on a refusing set means nothing is known, not that the key is clean.
         * @summary Looks keys up against the reference plane.
         * @param {ResolveReferenceIn} resolveReferenceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskResolveReference: async (resolveReferenceIn: ResolveReferenceIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'resolveReferenceIn' is not null or undefined
            assertParamExists('riskResolveReference', 'resolveReferenceIn', resolveReferenceIn)
            const localVarPath = `/v1/risk/reference/resolve`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(resolveReferenceIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Score judges one event against the caller organisation\'s OWN model and learns nothing from it. It is how a candidate is tried against real behaviour before anything depends on the answer, and it is the model\'s analogue of testing a rule.  Because it records nothing, the aggregates it reads do not include the event: the numbers are the organisation\'s history as it stands. A model still warming declines with a reason rather than answering zero, because silence must never read as a clean result.
         * @summary Score one event against your organisation\'s own model
         * @param {RiskScoreIn} riskScoreIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskScore: async (riskScoreIn: RiskScoreIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskScoreIn' is not null or undefined
            assertParamExists('riskScore', 'riskScoreIn', riskScoreIn)
            const localVarPath = `/v1/risk/score`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskScoreIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Search runs an exhaustive search for the model shape that best fits the caller organisation\'s own history, and answers 202 with the run to read back.  Every candidate is replayed over that organisation\'s OWN feature surface in its own sandbox — its own aggregates, its own model, neither of them the live one — so a run cannot move a live threshold and cannot see another organisation\'s data. The result is the learning curve for each shape and the one that fit best, ranked on how closely it honoured the stated appetite, whether it warmed at all, whether it saturated, and how much of the coordinate space it left blind.  An empty history is REFUSED rather than reported as zero alerts, because \"no alerts\" is exactly what a quiet model looks like.
         * @summary Search exhaustively for the model shape that fits your own history
         * @param {RiskSearchIn} riskSearchIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSearch: async (riskSearchIn: RiskSearchIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskSearchIn' is not null or undefined
            assertParamExists('riskSearch', 'riskSearchIn', riskSearchIn)
            const localVarPath = `/v1/risk/search`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskSearchIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads back one search run: every shape tried over this organisation\'s own history, best first, and the one that fit.  A run another organisation started is simply not there — the same 404 an unknown id gives, so the read is not a probe oracle.
         * @summary Read back one exhaustive search
         * @param {string} id ID is the run, taken from the path. A run another organisation started is simply not there — the same answer an unknown id gives.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSearchResult: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('riskSearchResult', 'id', id)
            const localVarPath = `/v1/risk/search/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * States the decision regime the caller organisation\'s model decides under: how much of its own stream may be sent for examination, how much of the rest is sampled to measure what was missed, and whether the model may change an outcome at all.  The appetite is the decision a model is not permitted to make for itself: its output is a probability, so how likely it is to MISS something is a matter of policy that has to be stated, measured and reviewed rather than absorbed into a constant. The alert threshold is derived from it as a quantile of the scores actually observed, which is what keeps its meaning as the distribution drifts.  It is DURABLE BEFORE IT IS IN FORCE. The regime is recorded as a new version on the organisation\'s own shelf before anything in memory moves, so a policy that cannot be written down is refused rather than answered from state the next rollout would silently undo.  ARMING IS AN ADMIN ACT AND TUNING IS NOT. Setting `live` requires an admin of this organisation; stating the appetite and the sample is self-service for any member. Taking the model live decides whether it may change an OUTCOME at all — a payment frozen, a grant refused — for every customer this organisation has, and that is a decision an organisation takes rather than one of its members.  A RESTATEMENT OF THE REGIME IN FORCE MINTS NOTHING and answers the version already in force. Compare the version you receive with the version you had: unchanged means the numbers were the same, which is why there is no flag for it.  Learned state survives the change. The model\'s identity covers its SHAPE — the inventory and the geometry — and not its appetite, so restating policy unlearns nothing. It also does not REPORT the learned state: what the model is is read from the model.
         * @summary State the decision regime: the appetite, the sample, and whether the model is live
         * @param {RiskAppetiteIn} riskAppetiteIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSetPolicy: async (riskAppetiteIn: RiskAppetiteIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'riskAppetiteIn' is not null or undefined
            assertParamExists('riskSetPolicy', 'riskAppetiteIn', riskAppetiteIn)
            const localVarPath = `/v1/risk/policy`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(riskAppetiteIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Writes your organisation\'s own allow and deny entries over a set.  Idempotent on the key: writing the same entry twice is one entry, and writing it again replaces the verdict and the note. The whole batch is one transaction, so a batch that would cross the per-set bound writes nothing rather than half of itself — a half-applied deny list is worse than a refused one, because nobody can tell which half applied.  Your entries are held in your organisation\'s own store and are never visible to another organisation, and they never change what any other organisation sees. The shared baseline is not writable from here at all.
         * @summary Writes your organisation\'s own allow and deny entries over a set.
         * @param {string} set 
         * @param {SetReferenceIn} setReferenceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSetReference: async (set: string, setReferenceIn: SetReferenceIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'set' is not null or undefined
            assertParamExists('riskSetReference', 'set', set)
            // verify required parameter 'setReferenceIn' is not null or undefined
            assertParamExists('riskSetReference', 'setReferenceIn', setReferenceIn)
            const localVarPath = `/v1/risk/reference/{set}`
                .replace(`{${"set"}}`, encodeURIComponent(String(set)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(setReferenceIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * State reports the caller organisation\'s own model: what it has learned, whether it is live or still in shadow, the threshold in force, the appetite it stated beside the share it actually realised, every refusal by reason, every feature that read blind, and how much of the organisation\'s own event surface has been folded in.  It covers ONE organisation. A caller cannot learn another\'s volumes, alert rate or behaviour from it, because the state is read out of a model that holds only its own.
         * @summary Report your organisation\'s model: what it learned, and what it realised
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskState: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/risk/state`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * RiskApi - functional programming interface
 * @export
 */
export const RiskApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = RiskApiAxiosParamCreator(configuration)
    return {
        /**
         * Reports whether the risk model plane can actually work right now.  It is a REAL probe, not status theatre: it reports that the per-organisation model plane was built, that the per-organisation stores can be written, and whether the event surface the feature plane is rolled up from is reachable. 200 only when the plane can work; otherwise 503 CARRYING THE REPORT — which part failed and the real error.  An unreachable event surface is REPORTED and is not a failure. Scoring reads in-memory aggregates and never the warehouse, so a warm that cannot run degrades how much history a model has seen and does not stop it deciding.  It also reports how many organisations\' models are resident, how many have been evicted to hold that bound, and how many of the resident ones are at their own aggregate bound. Eviction is lossless — learned state is written to that organisation\'s own store first and its aggregates rebuild from its own record — so a climbing count is a capacity signal, not a loss. A STRAINED model is different: it has started forgetting its own least-recently-active subjects, and each forgotten subject reads as inactive until it is active again. That is a control degrading, and it is reported here because it is otherwise silent.  It answers about the process, not about a tenant: it takes no organisation and names none.
         * @summary Reports whether the risk model plane can actually work right now.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1RiskHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1RiskHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.getV1RiskHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Puts one of your organisation\'s OWN PUBLISHED VALUES in force, by name — which is what an instant rollback is, what promoting a challenger is, and what installing the shape a search found is.  IT TAKES AN ADDRESS AND NEVER STATE. The masses are read from your own store, so nothing about your model has to be held by whatever is making this call. That closes the sharpest edge the previous shape had: a body of counters is something a caller can COMPOSE, and a region filled until activity inside it reads as ordinary is a model that has been shaped rather than learned. The engine\'s mass invariant was the only thing standing between a composed body and the model; with an address there is no body to compose.  IT ADOPTS THE SHAPE, NOT ONLY THE MASSES. A value records the model space its masses were taken in, and a value whose space differs from the one in force REPLANTS your model into that space before restoring them. That is what makes POST /v1/risk/search actionable: a search answers with the shape that fits your own history best and publishes it fitted, and its address is what you name here. Before this, a winning shape was advice nobody could take — the adoption path refused every shape change, and a winner is a different shape by definition.  WHAT ADOPTING A SEARCHED SHAPE COSTS, SAID PLAINLY: the value a search fits has learned the window the search replayed and nothing older, so installing it trades history for fit. Your appetite is untouched — that is your policy record\'s, with its own versions — and so is the geometry, which stays your own.  An address your organisation has not published is NOT FOUND. That includes one another organisation published, and it is not a lookup that failed: the store is per organisation and the address is a name, never an authority.
         * @summary Put one of your organisation\'s own published model values in force
         * @param {RiskAdoptIn} riskAdoptIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskAdoptModel(riskAdoptIn: RiskAdoptIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskModelState>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskAdoptModel(riskAdoptIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskAdoptModel']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes one of your organisation\'s overrides.  It removes an entry your organisation wrote, never a baseline member: the published set is not writable from here, so a removal can only ever restore the baseline\'s own answer.
         * @summary Removes one of your organisation\'s overrides.
         * @param {string} set 
         * @param {string} [key] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskClearReference(set: string, key?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ClearReferenceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskClearReference(set, key, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskClearReference']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
         * @summary Declare the next version of a dataset
         * @param {RiskDatasetSpec} riskDatasetSpec 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskCreateDataset(riskDatasetSpec: RiskDatasetSpec, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskDataset>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskCreateDataset(riskDatasetSpec, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskCreateDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
         * @summary Describe every version of one dataset
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskDataset(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskDatasetVersions>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskDataset(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
         * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to trace. Zero takes the newest published one.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskDatasetLineage(name: string, version?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskLineage>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskDatasetLineage(name, version, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskDatasetLineage']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
         * @summary List this org\'s datasets
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskDatasets(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskDatasetList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskDatasets(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskDatasets']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
         * @summary Dispose of one dataset and every version of it
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskDeleteDataset(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskDatasetDisposal>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskDeleteDataset(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskDeleteDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Applies this tenant\'s retention, and only this tenant\'s.  It is bounded three ways, each a compliance property rather than a convenience. It refuses a boundary younger than the platform floor, because a label can be the input to an adverse action and five years is what the retention ledger holds such a record for. It never touches a record under litigation hold. And it disposes of whole records rather than redacting fields.  It removes the derived columnar copy BEFORE the record, and refuses the whole disposal if the warehouse cannot be reached. The other order would leave rows in the warehouse that nothing can identify any more, which is a disposal that did not happen and says it did.
         * @summary Dispose of this tenant\'s expired assertions, whole records only
         * @param {RiskDisposeIn} riskDisposeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskDisposeLabels(riskDisposeIn: RiskDisposeIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskDisposeOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskDisposeLabels(riskDisposeIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskDisposeLabels']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
         * @summary Read a version\'s rows back, one page at a time
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to read. Zero takes the newest published one.
         * @param {string} [split] Split narrows to train, val or test. Empty reads every split.
         * @param {number} [offset] Offset is where the page starts, in the version\&#39;s own row order (by id, which is derived from the row and therefore stable forever).
         * @param {number} [limit] Limit is how many rows to return. Zero and anything above the plane\&#39;s bound take the bound.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskExportDataset(name: string, version?: number, split?: string, offset?: number, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskDatasetRows>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskExportDataset(name, version, split, offset, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskExportDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Features is the feature catalogue in its two honest lenses.  The MODEL lens is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves, the supervisor\'s own words for the indicator, and the published standard those words come from — so a coverage claim is checkable rather than asserted. It is the same for every organisation.  The SURFACE lens is what THIS organisation\'s own event surface actually carries, measured over the window: how many of its buckets carry each dimension at all, and what the dimension reads where it is present. A dimension present in no bucket is BLIND, and saying so is the difference between no risk and no data.
         * @summary The feature catalogue: what the model reads, and what your surface carries
         * @param {number} [days] Days is how far back to measure the organisation\&#39;s own coverage, 1 to 400. Zero takes thirty.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskFeatures(days?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskCatalog>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskFeatures(days, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskFeatures']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Places or releases a litigation hold on named records.  A hold is a fact about the RECORD, not about the world: it says retention may not dispose of this row, and it asserts nothing about what happened. So it is not a field on an assertion and it is not folded into the content digest — carried there it was silently a no-op on any record that already existed, since re-filing the same assertion with a hold flag produced the same digest, the insert was ignored, and the caller was answered `duplicate` while the hold it asked for was never placed. This op is the one way a hold moves, in either direction, and the move is written to the audit log.  Every named id is this tenant\'s or is nothing. The statement runs against the tenant\'s own file, which holds no other tenant\'s rows and has no column that could name one.
         * @summary Place or release a litigation hold on named records
         * @param {RiskHoldIn} riskHoldIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskHoldLabels(riskHoldIn: RiskHoldIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskHoldOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskHoldLabels(riskHoldIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskHoldLabels']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Records a batch of ground truth against the entities it judges.  Each assertion carries TWO times — when the judged event happened, and when the assertion became knowable — and both are required. The second is what keeps a chargeback that landed in June out of a model that had to decide in February.  It is idempotent on the CONTENT of an assertion, so a webhook that redelivers is safe. It never overwrites: a source that corrects itself later files a NEW assertion, which wins from the moment it became knowable and leaves every earlier observation instant seeing exactly what it saw.  The asserter is stamped from the validated credential and is not a body field.
         * @summary Assert ground truth about events
         * @param {RiskLabelIn} riskLabelIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskLabel(riskLabelIn: RiskLabelIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskLabelOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskLabel(riskLabelIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskLabel']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reports how much of a window has matured and how much of that is judged, per source.  It is the gate on training. A supervised fit over a window whose judged count is near zero produces a number, and the number is meaningless; this op is what lets that be stated before the fit rather than discovered after it.  It reads the RECORD plane and folds every assertion at that event\'s OWN as-of, so the counts obey exactly the leakage rule a materialisation would. It counts only what was ASSERTED: what share of the whole event STREAM carries a label is a question about the feature plane\'s denominator and is not answerable here.
         * @summary How much of the window has matured, and how much of that is judged
         * @param {string} [from] From and To bound the EVENT window, half-open, RFC 3339.  Unstated, the window is the 90 days ENDING where maturity begins — &#x60;to&#x60; is the horizon ago, not now. A default window running to now under a default horizon could not contain one matured event, so every count below it would be zero however much ground truth the tenant held.
         * @param {string} [to] 
         * @param {number} [horizon] Horizon is the maturity horizon in days the coverage is measured under. Unstated takes 120. It also moves the default window, which ends where maturity begins.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskLabelCoverage(from?: string, to?: string, horizon?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskLabelCoverage>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskLabelCoverage(from, to, horizon, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskLabelCoverage']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Publishes the closed vocabularies and the precedence rule that resolves a conflict between two sources.  A precedence rule nobody can read is a rule nobody can audit or dispute, and the whole defensibility of a contested label rests on being able to say why one assertion beat another. The order returned here is derived from the same declaration the resolver reads — it is not a description of it.
         * @summary The closed vocabularies and the precedence rule that resolves a conflict
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskLabelVocabulary(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskLabelVocabulary>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskLabelVocabulary(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskLabelVocabulary']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads the assertions this tenant has recorded, newest event first.  It reads the RECORD — the tenant\'s own store — and not the columnar copy, so what it returns is what would be produced in an audit. Narrow it by entity, by asserter, or by event window.
         * @summary Read the assertions this tenant has recorded
         * @param {string} [kind] Kind and Subject narrow to one entity.
         * @param {string} [subject] 
         * @param {string} [source] Source narrows to one asserter — the read that answers \&quot;what has commerce told us\&quot;, separately from \&quot;what has an analyst told us\&quot;.
         * @param {string} [from] From and To bound the EVENT time, half-open, RFC 3339.
         * @param {string} [to] 
         * @param {number} [limit] Limit caps the page. Out of range takes the plane\&#39;s own bound.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskLabels(kind?: string, subject?: string, source?: string, from?: string, to?: string, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskLabelsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskLabels(kind, subject, source, from, to, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskLabels']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Learn records a batch of events into the caller organisation\'s own aggregates and lets its model learn from them. It answers how many it learned from.  IT DOES NOT SCORE, AND THAT IS THE POINT. An observation is a value you record; learning is a transformation over observations; a verdict is a query against the result. This op is the first two. [ops.score] is the third, it is pure, and it is the ONE door to a verdict. They were one call, which meant you could not record without training and could not train without being answered — and the model ran twice over every event to produce a verdict the response carried and no caller read.  TO OBSERVE AND JUDGE, COMPOSE THE TWO, and mind the order. Score FIRST, then learn: the score is then the model\'s opinion of an event it has not yet learned from, which is the question worth asking. The other order answers for a model that has already absorbed the event it is judging.  This is the training path, and there is no job behind it: the model IS a set of mass counters over half-space trees, so learning is an increment and the model is current the instant the last event lands. Nothing from any other organisation is in it, and nothing from this organisation leaves it.  A RETRY IS INERT. The record deduplicates on the event id you send, and an event already in it moves nothing, costs nothing and is not counted — so a client that timed out can send the same batch again and its model holds what it holds. Without an id of your own there is nothing to converge on: two identical bodies are two events.
         * @summary Teach your organisation\'s own model from its own events
         * @param {RiskLearnIn} riskLearnIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskLearn(riskLearnIn: RiskLearnIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskLearnOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskLearn(riskLearnIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskLearn']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
         * @summary Materialise the declared version into immutable rows
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskMaterializeDataset(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskDataset>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskMaterializeDataset(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskMaterializeDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Policy reports the caller organisation\'s own decision-regime history: every distinct regime it has adopted, which version is in force, and what retention has taken.  WHY IT EXISTS. Every score cites the version it was decided under ([riskScoreOut.Policy]), and the threshold that score was measured against is derived from the appetite that version states. Restate the appetite and, without this record, every earlier decision becomes unreconstructible — the cut it was judged by no longer exists anywhere. An adverse decision that cannot be explained against the policy in force when it was taken cannot be defended.  It covers ONE organisation. The history is on that organisation\'s own shelf, so another\'s versions are not filtered out of the answer — they are not in the file the answer is read from.
         * @summary Your organisation\'s decision-regime history, and which version is in force
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskPolicy(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskPolicyOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskPolicy(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskPolicy']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Publishes your organisation\'s model as a NAMED VALUE, so a decision taken today can be reconstructed tomorrow and a change made today can be undone.  It answers with a NAME and not with the state. The masses stay on your organisation\'s own encrypted store and are referred to by an address computed from their own content: the shape, the geometry seed, the position in the window, the threshold, the masses themselves as IEEE-754 bits, and the fold watermark behind them. That is what makes the value nameable without making the caller its custodian.  IT IS IDEMPOTENT ON THE VALUE. A model that has not changed publishes to the name it already has and mints nothing, reporting minted=false — so publishing at every boundary that matters is free. Ten values are retained per organisation, bounded in BYTES rather than in rows, and the oldest is disposed of past that.  A model that has learned nothing is refused: planted is not learned, and a value that reproduces nothing is not a value.  It is POST and PUT on one address because they are one plane\'s two verbs over one kind of thing: POST mints a value from the model in force, PUT puts a value in force. They were /v1/risk/state/snapshot and /v1/risk/state/restore — two addresses named after the operation rather than after the thing, which is how a reader ends up asking what the difference between a snapshot and a value is.
         * @summary Publish your organisation\'s model as a named, immutable value
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskPublishModel(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskPublishOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskPublishModel(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskPublishModel']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reference describes one set and lists your org\'s overrides in it.  The set half is public data about a published list — its version, its publishers, their licences and how current each one is. The overrides half is yours alone: it is read from your organisation\'s own store, and no other organisation\'s entries can appear in it.
         * @summary Reference describes one set and lists your org\'s overrides in it.
         * @param {string} set 
         * @param {string} [after] After pages the override listing: the last key of the previous page.
         * @param {number} [limit] Limit caps the override listing: default 200, maximum 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskReference(set: string, after?: string, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ReferenceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskReference(set, after, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskReference']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists every set this plane publishes, with its version and how fresh it is.  Read the Stale and Refused lists first: they are the two ways this plane can be quietly wrong, and they are reported rather than inferred. A set in Refused answers nothing — it has never loaded, it is held by another component, or it names a source we hold no licence for.
         * @summary Lists every set this plane publishes, with its version and how fresh it is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskReferenceSets(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ReferenceSetsOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskReferenceSets(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskReferenceSets']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Takes a new version of one set. SuperAdmin only.  It is platform work, not tenant work: it writes the shared baseline every organisation reads, so it is gated to the platform\'s own identity. Nothing here can write an organisation\'s overrides, and nothing an organisation sends can reach this route.  Idempotent. A version is the content digest of what was taken, so refreshing an unchanged publisher writes no rows and reports unchanged. Resumable: a run that died half-way is continued from where it stopped rather than restarted.  A set whose source needs a licence we do not hold is refused with the reason, rather than being quietly skipped.
         * @summary Takes a new version of one set.
         * @param {RefreshReferenceIn} refreshReferenceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskRefreshReference(refreshReferenceIn: RefreshReferenceIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RefreshReferenceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskRefreshReference(refreshReferenceIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskRefreshReference']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers, for each named event, which assertion was in force AS OF that event\'s own horizon — and what disagreed with it.  This is the join surface: the dataset materialiser calls it to attach ground truth to training rows, and the evaluator calls it to score a past decision against what was knowable when the decision had to be made. One mechanism for both, so a model can never be trained under one leakage rule and scored under another.  Three answers are distinct and all three are honest: a resolved label, an event that has not matured, and a matured event nobody has judged. The last is never reported as unproductive.
         * @summary Resolve the label in force for named events, as of each event\'s own horizon
         * @param {RiskResolveIn} riskResolveIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskResolveLabels(riskResolveIn: RiskResolveIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskResolveOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskResolveLabels(riskResolveIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskResolveLabels']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Looks keys up against the reference plane.  Your organisation\'s own overrides are consulted FIRST and win outright; the shared baseline answers everything they do not cover. Every answer names the version that produced it, when that version was current and whether it is stale, so a decision can record exactly what it consulted.  Read Refusal before reading Hit. A set that has never loaded, one held by the component that screens against it, and one whose source needs a licence we do not hold all answer with a refusal — and a miss on a refusing set means nothing is known, not that the key is clean.
         * @summary Looks keys up against the reference plane.
         * @param {ResolveReferenceIn} resolveReferenceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskResolveReference(resolveReferenceIn: ResolveReferenceIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ResolveReferenceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskResolveReference(resolveReferenceIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskResolveReference']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Score judges one event against the caller organisation\'s OWN model and learns nothing from it. It is how a candidate is tried against real behaviour before anything depends on the answer, and it is the model\'s analogue of testing a rule.  Because it records nothing, the aggregates it reads do not include the event: the numbers are the organisation\'s history as it stands. A model still warming declines with a reason rather than answering zero, because silence must never read as a clean result.
         * @summary Score one event against your organisation\'s own model
         * @param {RiskScoreIn} riskScoreIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskScore(riskScoreIn: RiskScoreIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskScoreOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskScore(riskScoreIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskScore']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Search runs an exhaustive search for the model shape that best fits the caller organisation\'s own history, and answers 202 with the run to read back.  Every candidate is replayed over that organisation\'s OWN feature surface in its own sandbox — its own aggregates, its own model, neither of them the live one — so a run cannot move a live threshold and cannot see another organisation\'s data. The result is the learning curve for each shape and the one that fit best, ranked on how closely it honoured the stated appetite, whether it warmed at all, whether it saturated, and how much of the coordinate space it left blind.  An empty history is REFUSED rather than reported as zero alerts, because \"no alerts\" is exactly what a quiet model looks like.
         * @summary Search exhaustively for the model shape that fits your own history
         * @param {RiskSearchIn} riskSearchIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskSearch(riskSearchIn: RiskSearchIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskSearchRun>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskSearch(riskSearchIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskSearch']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads back one search run: every shape tried over this organisation\'s own history, best first, and the one that fit.  A run another organisation started is simply not there — the same 404 an unknown id gives, so the read is not a probe oracle.
         * @summary Read back one exhaustive search
         * @param {string} id ID is the run, taken from the path. A run another organisation started is simply not there — the same answer an unknown id gives.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskSearchResult(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskSearchReport>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskSearchResult(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskSearchResult']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * States the decision regime the caller organisation\'s model decides under: how much of its own stream may be sent for examination, how much of the rest is sampled to measure what was missed, and whether the model may change an outcome at all.  The appetite is the decision a model is not permitted to make for itself: its output is a probability, so how likely it is to MISS something is a matter of policy that has to be stated, measured and reviewed rather than absorbed into a constant. The alert threshold is derived from it as a quantile of the scores actually observed, which is what keeps its meaning as the distribution drifts.  It is DURABLE BEFORE IT IS IN FORCE. The regime is recorded as a new version on the organisation\'s own shelf before anything in memory moves, so a policy that cannot be written down is refused rather than answered from state the next rollout would silently undo.  ARMING IS AN ADMIN ACT AND TUNING IS NOT. Setting `live` requires an admin of this organisation; stating the appetite and the sample is self-service for any member. Taking the model live decides whether it may change an OUTCOME at all — a payment frozen, a grant refused — for every customer this organisation has, and that is a decision an organisation takes rather than one of its members.  A RESTATEMENT OF THE REGIME IN FORCE MINTS NOTHING and answers the version already in force. Compare the version you receive with the version you had: unchanged means the numbers were the same, which is why there is no flag for it.  Learned state survives the change. The model\'s identity covers its SHAPE — the inventory and the geometry — and not its appetite, so restating policy unlearns nothing. It also does not REPORT the learned state: what the model is is read from the model.
         * @summary State the decision regime: the appetite, the sample, and whether the model is live
         * @param {RiskAppetiteIn} riskAppetiteIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskSetPolicy(riskAppetiteIn: RiskAppetiteIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskPolicyOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskSetPolicy(riskAppetiteIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskSetPolicy']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Writes your organisation\'s own allow and deny entries over a set.  Idempotent on the key: writing the same entry twice is one entry, and writing it again replaces the verdict and the note. The whole batch is one transaction, so a batch that would cross the per-set bound writes nothing rather than half of itself — a half-applied deny list is worse than a refused one, because nobody can tell which half applied.  Your entries are held in your organisation\'s own store and are never visible to another organisation, and they never change what any other organisation sees. The shared baseline is not writable from here at all.
         * @summary Writes your organisation\'s own allow and deny entries over a set.
         * @param {string} set 
         * @param {SetReferenceIn} setReferenceIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskSetReference(set: string, setReferenceIn: SetReferenceIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SetReferenceOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskSetReference(set, setReferenceIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskSetReference']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * State reports the caller organisation\'s own model: what it has learned, whether it is live or still in shadow, the threshold in force, the appetite it stated beside the share it actually realised, every refusal by reason, every feature that read blind, and how much of the organisation\'s own event surface has been folded in.  It covers ONE organisation. A caller cannot learn another\'s volumes, alert rate or behaviour from it, because the state is read out of a model that holds only its own.
         * @summary Report your organisation\'s model: what it learned, and what it realised
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async riskState(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<RiskModelState>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.riskState(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['RiskApi.riskState']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * RiskApi - factory interface
 * @export
 */
export const RiskApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = RiskApiFp(configuration)
    return {
        /**
         * Reports whether the risk model plane can actually work right now.  It is a REAL probe, not status theatre: it reports that the per-organisation model plane was built, that the per-organisation stores can be written, and whether the event surface the feature plane is rolled up from is reachable. 200 only when the plane can work; otherwise 503 CARRYING THE REPORT — which part failed and the real error.  An unreachable event surface is REPORTED and is not a failure. Scoring reads in-memory aggregates and never the warehouse, so a warm that cannot run degrades how much history a model has seen and does not stop it deciding.  It also reports how many organisations\' models are resident, how many have been evicted to hold that bound, and how many of the resident ones are at their own aggregate bound. Eviction is lossless — learned state is written to that organisation\'s own store first and its aggregates rebuild from its own record — so a climbing count is a capacity signal, not a loss. A STRAINED model is different: it has started forgetting its own least-recently-active subjects, and each forgotten subject reads as inactive until it is active again. That is a control degrading, and it is reported here because it is otherwise silent.  It answers about the process, not about a tenant: it takes no organisation and names none.
         * @summary Reports whether the risk model plane can actually work right now.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1RiskHealth(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getV1RiskHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Puts one of your organisation\'s OWN PUBLISHED VALUES in force, by name — which is what an instant rollback is, what promoting a challenger is, and what installing the shape a search found is.  IT TAKES AN ADDRESS AND NEVER STATE. The masses are read from your own store, so nothing about your model has to be held by whatever is making this call. That closes the sharpest edge the previous shape had: a body of counters is something a caller can COMPOSE, and a region filled until activity inside it reads as ordinary is a model that has been shaped rather than learned. The engine\'s mass invariant was the only thing standing between a composed body and the model; with an address there is no body to compose.  IT ADOPTS THE SHAPE, NOT ONLY THE MASSES. A value records the model space its masses were taken in, and a value whose space differs from the one in force REPLANTS your model into that space before restoring them. That is what makes POST /v1/risk/search actionable: a search answers with the shape that fits your own history best and publishes it fitted, and its address is what you name here. Before this, a winning shape was advice nobody could take — the adoption path refused every shape change, and a winner is a different shape by definition.  WHAT ADOPTING A SEARCHED SHAPE COSTS, SAID PLAINLY: the value a search fits has learned the window the search replayed and nothing older, so installing it trades history for fit. Your appetite is untouched — that is your policy record\'s, with its own versions — and so is the geometry, which stays your own.  An address your organisation has not published is NOT FOUND. That includes one another organisation published, and it is not a lookup that failed: the store is per organisation and the address is a name, never an authority.
         * @summary Put one of your organisation\'s own published model values in force
         * @param {RiskApiRiskAdoptModelRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskAdoptModel(requestParameters: RiskApiRiskAdoptModelRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskModelState> {
            return localVarFp.riskAdoptModel(requestParameters.riskAdoptIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Removes one of your organisation\'s overrides.  It removes an entry your organisation wrote, never a baseline member: the published set is not writable from here, so a removal can only ever restore the baseline\'s own answer.
         * @summary Removes one of your organisation\'s overrides.
         * @param {RiskApiRiskClearReferenceRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskClearReference(requestParameters: RiskApiRiskClearReferenceRequest, options?: RawAxiosRequestConfig): AxiosPromise<ClearReferenceOut> {
            return localVarFp.riskClearReference(requestParameters.set, requestParameters.key, options).then((request) => request(axios, basePath));
        },
        /**
         * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
         * @summary Declare the next version of a dataset
         * @param {RiskApiRiskCreateDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskCreateDataset(requestParameters: RiskApiRiskCreateDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskDataset> {
            return localVarFp.riskCreateDataset(requestParameters.riskDatasetSpec, options).then((request) => request(axios, basePath));
        },
        /**
         * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
         * @summary Describe every version of one dataset
         * @param {RiskApiRiskDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDataset(requestParameters: RiskApiRiskDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskDatasetVersions> {
            return localVarFp.riskDataset(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
         * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
         * @param {RiskApiRiskDatasetLineageRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDatasetLineage(requestParameters: RiskApiRiskDatasetLineageRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskLineage> {
            return localVarFp.riskDatasetLineage(requestParameters.name, requestParameters.version, options).then((request) => request(axios, basePath));
        },
        /**
         * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
         * @summary List this org\'s datasets
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDatasets(options?: RawAxiosRequestConfig): AxiosPromise<RiskDatasetList> {
            return localVarFp.riskDatasets(options).then((request) => request(axios, basePath));
        },
        /**
         * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
         * @summary Dispose of one dataset and every version of it
         * @param {RiskApiRiskDeleteDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDeleteDataset(requestParameters: RiskApiRiskDeleteDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskDatasetDisposal> {
            return localVarFp.riskDeleteDataset(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Applies this tenant\'s retention, and only this tenant\'s.  It is bounded three ways, each a compliance property rather than a convenience. It refuses a boundary younger than the platform floor, because a label can be the input to an adverse action and five years is what the retention ledger holds such a record for. It never touches a record under litigation hold. And it disposes of whole records rather than redacting fields.  It removes the derived columnar copy BEFORE the record, and refuses the whole disposal if the warehouse cannot be reached. The other order would leave rows in the warehouse that nothing can identify any more, which is a disposal that did not happen and says it did.
         * @summary Dispose of this tenant\'s expired assertions, whole records only
         * @param {RiskApiRiskDisposeLabelsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskDisposeLabels(requestParameters: RiskApiRiskDisposeLabelsRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskDisposeOut> {
            return localVarFp.riskDisposeLabels(requestParameters.riskDisposeIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
         * @summary Read a version\'s rows back, one page at a time
         * @param {RiskApiRiskExportDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskExportDataset(requestParameters: RiskApiRiskExportDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskDatasetRows> {
            return localVarFp.riskExportDataset(requestParameters.name, requestParameters.version, requestParameters.split, requestParameters.offset, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Features is the feature catalogue in its two honest lenses.  The MODEL lens is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves, the supervisor\'s own words for the indicator, and the published standard those words come from — so a coverage claim is checkable rather than asserted. It is the same for every organisation.  The SURFACE lens is what THIS organisation\'s own event surface actually carries, measured over the window: how many of its buckets carry each dimension at all, and what the dimension reads where it is present. A dimension present in no bucket is BLIND, and saying so is the difference between no risk and no data.
         * @summary The feature catalogue: what the model reads, and what your surface carries
         * @param {RiskApiRiskFeaturesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskFeatures(requestParameters: RiskApiRiskFeaturesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<RiskCatalog> {
            return localVarFp.riskFeatures(requestParameters.days, options).then((request) => request(axios, basePath));
        },
        /**
         * Places or releases a litigation hold on named records.  A hold is a fact about the RECORD, not about the world: it says retention may not dispose of this row, and it asserts nothing about what happened. So it is not a field on an assertion and it is not folded into the content digest — carried there it was silently a no-op on any record that already existed, since re-filing the same assertion with a hold flag produced the same digest, the insert was ignored, and the caller was answered `duplicate` while the hold it asked for was never placed. This op is the one way a hold moves, in either direction, and the move is written to the audit log.  Every named id is this tenant\'s or is nothing. The statement runs against the tenant\'s own file, which holds no other tenant\'s rows and has no column that could name one.
         * @summary Place or release a litigation hold on named records
         * @param {RiskApiRiskHoldLabelsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskHoldLabels(requestParameters: RiskApiRiskHoldLabelsRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskHoldOut> {
            return localVarFp.riskHoldLabels(requestParameters.riskHoldIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Records a batch of ground truth against the entities it judges.  Each assertion carries TWO times — when the judged event happened, and when the assertion became knowable — and both are required. The second is what keeps a chargeback that landed in June out of a model that had to decide in February.  It is idempotent on the CONTENT of an assertion, so a webhook that redelivers is safe. It never overwrites: a source that corrects itself later files a NEW assertion, which wins from the moment it became knowable and leaves every earlier observation instant seeing exactly what it saw.  The asserter is stamped from the validated credential and is not a body field.
         * @summary Assert ground truth about events
         * @param {RiskApiRiskLabelRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabel(requestParameters: RiskApiRiskLabelRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskLabelOut> {
            return localVarFp.riskLabel(requestParameters.riskLabelIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Reports how much of a window has matured and how much of that is judged, per source.  It is the gate on training. A supervised fit over a window whose judged count is near zero produces a number, and the number is meaningless; this op is what lets that be stated before the fit rather than discovered after it.  It reads the RECORD plane and folds every assertion at that event\'s OWN as-of, so the counts obey exactly the leakage rule a materialisation would. It counts only what was ASSERTED: what share of the whole event STREAM carries a label is a question about the feature plane\'s denominator and is not answerable here.
         * @summary How much of the window has matured, and how much of that is judged
         * @param {RiskApiRiskLabelCoverageRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabelCoverage(requestParameters: RiskApiRiskLabelCoverageRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<RiskLabelCoverage> {
            return localVarFp.riskLabelCoverage(requestParameters.from, requestParameters.to, requestParameters.horizon, options).then((request) => request(axios, basePath));
        },
        /**
         * Publishes the closed vocabularies and the precedence rule that resolves a conflict between two sources.  A precedence rule nobody can read is a rule nobody can audit or dispute, and the whole defensibility of a contested label rests on being able to say why one assertion beat another. The order returned here is derived from the same declaration the resolver reads — it is not a description of it.
         * @summary The closed vocabularies and the precedence rule that resolves a conflict
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabelVocabulary(options?: RawAxiosRequestConfig): AxiosPromise<RiskLabelVocabulary> {
            return localVarFp.riskLabelVocabulary(options).then((request) => request(axios, basePath));
        },
        /**
         * Reads the assertions this tenant has recorded, newest event first.  It reads the RECORD — the tenant\'s own store — and not the columnar copy, so what it returns is what would be produced in an audit. Narrow it by entity, by asserter, or by event window.
         * @summary Read the assertions this tenant has recorded
         * @param {RiskApiRiskLabelsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLabels(requestParameters: RiskApiRiskLabelsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<RiskLabelsOut> {
            return localVarFp.riskLabels(requestParameters.kind, requestParameters.subject, requestParameters.source, requestParameters.from, requestParameters.to, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Learn records a batch of events into the caller organisation\'s own aggregates and lets its model learn from them. It answers how many it learned from.  IT DOES NOT SCORE, AND THAT IS THE POINT. An observation is a value you record; learning is a transformation over observations; a verdict is a query against the result. This op is the first two. [ops.score] is the third, it is pure, and it is the ONE door to a verdict. They were one call, which meant you could not record without training and could not train without being answered — and the model ran twice over every event to produce a verdict the response carried and no caller read.  TO OBSERVE AND JUDGE, COMPOSE THE TWO, and mind the order. Score FIRST, then learn: the score is then the model\'s opinion of an event it has not yet learned from, which is the question worth asking. The other order answers for a model that has already absorbed the event it is judging.  This is the training path, and there is no job behind it: the model IS a set of mass counters over half-space trees, so learning is an increment and the model is current the instant the last event lands. Nothing from any other organisation is in it, and nothing from this organisation leaves it.  A RETRY IS INERT. The record deduplicates on the event id you send, and an event already in it moves nothing, costs nothing and is not counted — so a client that timed out can send the same batch again and its model holds what it holds. Without an id of your own there is nothing to converge on: two identical bodies are two events.
         * @summary Teach your organisation\'s own model from its own events
         * @param {RiskApiRiskLearnRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskLearn(requestParameters: RiskApiRiskLearnRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskLearnOut> {
            return localVarFp.riskLearn(requestParameters.riskLearnIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
         * @summary Materialise the declared version into immutable rows
         * @param {RiskApiRiskMaterializeDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskMaterializeDataset(requestParameters: RiskApiRiskMaterializeDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskDataset> {
            return localVarFp.riskMaterializeDataset(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Policy reports the caller organisation\'s own decision-regime history: every distinct regime it has adopted, which version is in force, and what retention has taken.  WHY IT EXISTS. Every score cites the version it was decided under ([riskScoreOut.Policy]), and the threshold that score was measured against is derived from the appetite that version states. Restate the appetite and, without this record, every earlier decision becomes unreconstructible — the cut it was judged by no longer exists anywhere. An adverse decision that cannot be explained against the policy in force when it was taken cannot be defended.  It covers ONE organisation. The history is on that organisation\'s own shelf, so another\'s versions are not filtered out of the answer — they are not in the file the answer is read from.
         * @summary Your organisation\'s decision-regime history, and which version is in force
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskPolicy(options?: RawAxiosRequestConfig): AxiosPromise<RiskPolicyOut> {
            return localVarFp.riskPolicy(options).then((request) => request(axios, basePath));
        },
        /**
         * Publishes your organisation\'s model as a NAMED VALUE, so a decision taken today can be reconstructed tomorrow and a change made today can be undone.  It answers with a NAME and not with the state. The masses stay on your organisation\'s own encrypted store and are referred to by an address computed from their own content: the shape, the geometry seed, the position in the window, the threshold, the masses themselves as IEEE-754 bits, and the fold watermark behind them. That is what makes the value nameable without making the caller its custodian.  IT IS IDEMPOTENT ON THE VALUE. A model that has not changed publishes to the name it already has and mints nothing, reporting minted=false — so publishing at every boundary that matters is free. Ten values are retained per organisation, bounded in BYTES rather than in rows, and the oldest is disposed of past that.  A model that has learned nothing is refused: planted is not learned, and a value that reproduces nothing is not a value.  It is POST and PUT on one address because they are one plane\'s two verbs over one kind of thing: POST mints a value from the model in force, PUT puts a value in force. They were /v1/risk/state/snapshot and /v1/risk/state/restore — two addresses named after the operation rather than after the thing, which is how a reader ends up asking what the difference between a snapshot and a value is.
         * @summary Publish your organisation\'s model as a named, immutable value
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskPublishModel(options?: RawAxiosRequestConfig): AxiosPromise<RiskPublishOut> {
            return localVarFp.riskPublishModel(options).then((request) => request(axios, basePath));
        },
        /**
         * Reference describes one set and lists your org\'s overrides in it.  The set half is public data about a published list — its version, its publishers, their licences and how current each one is. The overrides half is yours alone: it is read from your organisation\'s own store, and no other organisation\'s entries can appear in it.
         * @summary Reference describes one set and lists your org\'s overrides in it.
         * @param {RiskApiRiskReferenceRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskReference(requestParameters: RiskApiRiskReferenceRequest, options?: RawAxiosRequestConfig): AxiosPromise<ReferenceOut> {
            return localVarFp.riskReference(requestParameters.set, requestParameters.after, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists every set this plane publishes, with its version and how fresh it is.  Read the Stale and Refused lists first: they are the two ways this plane can be quietly wrong, and they are reported rather than inferred. A set in Refused answers nothing — it has never loaded, it is held by another component, or it names a source we hold no licence for.
         * @summary Lists every set this plane publishes, with its version and how fresh it is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskReferenceSets(options?: RawAxiosRequestConfig): AxiosPromise<ReferenceSetsOut> {
            return localVarFp.riskReferenceSets(options).then((request) => request(axios, basePath));
        },
        /**
         * Takes a new version of one set. SuperAdmin only.  It is platform work, not tenant work: it writes the shared baseline every organisation reads, so it is gated to the platform\'s own identity. Nothing here can write an organisation\'s overrides, and nothing an organisation sends can reach this route.  Idempotent. A version is the content digest of what was taken, so refreshing an unchanged publisher writes no rows and reports unchanged. Resumable: a run that died half-way is continued from where it stopped rather than restarted.  A set whose source needs a licence we do not hold is refused with the reason, rather than being quietly skipped.
         * @summary Takes a new version of one set.
         * @param {RiskApiRiskRefreshReferenceRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskRefreshReference(requestParameters: RiskApiRiskRefreshReferenceRequest, options?: RawAxiosRequestConfig): AxiosPromise<RefreshReferenceOut> {
            return localVarFp.riskRefreshReference(requestParameters.refreshReferenceIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers, for each named event, which assertion was in force AS OF that event\'s own horizon — and what disagreed with it.  This is the join surface: the dataset materialiser calls it to attach ground truth to training rows, and the evaluator calls it to score a past decision against what was knowable when the decision had to be made. One mechanism for both, so a model can never be trained under one leakage rule and scored under another.  Three answers are distinct and all three are honest: a resolved label, an event that has not matured, and a matured event nobody has judged. The last is never reported as unproductive.
         * @summary Resolve the label in force for named events, as of each event\'s own horizon
         * @param {RiskApiRiskResolveLabelsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskResolveLabels(requestParameters: RiskApiRiskResolveLabelsRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskResolveOut> {
            return localVarFp.riskResolveLabels(requestParameters.riskResolveIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Looks keys up against the reference plane.  Your organisation\'s own overrides are consulted FIRST and win outright; the shared baseline answers everything they do not cover. Every answer names the version that produced it, when that version was current and whether it is stale, so a decision can record exactly what it consulted.  Read Refusal before reading Hit. A set that has never loaded, one held by the component that screens against it, and one whose source needs a licence we do not hold all answer with a refusal — and a miss on a refusing set means nothing is known, not that the key is clean.
         * @summary Looks keys up against the reference plane.
         * @param {RiskApiRiskResolveReferenceRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskResolveReference(requestParameters: RiskApiRiskResolveReferenceRequest, options?: RawAxiosRequestConfig): AxiosPromise<ResolveReferenceOut> {
            return localVarFp.riskResolveReference(requestParameters.resolveReferenceIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Score judges one event against the caller organisation\'s OWN model and learns nothing from it. It is how a candidate is tried against real behaviour before anything depends on the answer, and it is the model\'s analogue of testing a rule.  Because it records nothing, the aggregates it reads do not include the event: the numbers are the organisation\'s history as it stands. A model still warming declines with a reason rather than answering zero, because silence must never read as a clean result.
         * @summary Score one event against your organisation\'s own model
         * @param {RiskApiRiskScoreRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskScore(requestParameters: RiskApiRiskScoreRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskScoreOut> {
            return localVarFp.riskScore(requestParameters.riskScoreIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Search runs an exhaustive search for the model shape that best fits the caller organisation\'s own history, and answers 202 with the run to read back.  Every candidate is replayed over that organisation\'s OWN feature surface in its own sandbox — its own aggregates, its own model, neither of them the live one — so a run cannot move a live threshold and cannot see another organisation\'s data. The result is the learning curve for each shape and the one that fit best, ranked on how closely it honoured the stated appetite, whether it warmed at all, whether it saturated, and how much of the coordinate space it left blind.  An empty history is REFUSED rather than reported as zero alerts, because \"no alerts\" is exactly what a quiet model looks like.
         * @summary Search exhaustively for the model shape that fits your own history
         * @param {RiskApiRiskSearchRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSearch(requestParameters: RiskApiRiskSearchRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskSearchRun> {
            return localVarFp.riskSearch(requestParameters.riskSearchIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads back one search run: every shape tried over this organisation\'s own history, best first, and the one that fit.  A run another organisation started is simply not there — the same 404 an unknown id gives, so the read is not a probe oracle.
         * @summary Read back one exhaustive search
         * @param {RiskApiRiskSearchResultRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSearchResult(requestParameters: RiskApiRiskSearchResultRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskSearchReport> {
            return localVarFp.riskSearchResult(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * States the decision regime the caller organisation\'s model decides under: how much of its own stream may be sent for examination, how much of the rest is sampled to measure what was missed, and whether the model may change an outcome at all.  The appetite is the decision a model is not permitted to make for itself: its output is a probability, so how likely it is to MISS something is a matter of policy that has to be stated, measured and reviewed rather than absorbed into a constant. The alert threshold is derived from it as a quantile of the scores actually observed, which is what keeps its meaning as the distribution drifts.  It is DURABLE BEFORE IT IS IN FORCE. The regime is recorded as a new version on the organisation\'s own shelf before anything in memory moves, so a policy that cannot be written down is refused rather than answered from state the next rollout would silently undo.  ARMING IS AN ADMIN ACT AND TUNING IS NOT. Setting `live` requires an admin of this organisation; stating the appetite and the sample is self-service for any member. Taking the model live decides whether it may change an OUTCOME at all — a payment frozen, a grant refused — for every customer this organisation has, and that is a decision an organisation takes rather than one of its members.  A RESTATEMENT OF THE REGIME IN FORCE MINTS NOTHING and answers the version already in force. Compare the version you receive with the version you had: unchanged means the numbers were the same, which is why there is no flag for it.  Learned state survives the change. The model\'s identity covers its SHAPE — the inventory and the geometry — and not its appetite, so restating policy unlearns nothing. It also does not REPORT the learned state: what the model is is read from the model.
         * @summary State the decision regime: the appetite, the sample, and whether the model is live
         * @param {RiskApiRiskSetPolicyRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSetPolicy(requestParameters: RiskApiRiskSetPolicyRequest, options?: RawAxiosRequestConfig): AxiosPromise<RiskPolicyOut> {
            return localVarFp.riskSetPolicy(requestParameters.riskAppetiteIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Writes your organisation\'s own allow and deny entries over a set.  Idempotent on the key: writing the same entry twice is one entry, and writing it again replaces the verdict and the note. The whole batch is one transaction, so a batch that would cross the per-set bound writes nothing rather than half of itself — a half-applied deny list is worse than a refused one, because nobody can tell which half applied.  Your entries are held in your organisation\'s own store and are never visible to another organisation, and they never change what any other organisation sees. The shared baseline is not writable from here at all.
         * @summary Writes your organisation\'s own allow and deny entries over a set.
         * @param {RiskApiRiskSetReferenceRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskSetReference(requestParameters: RiskApiRiskSetReferenceRequest, options?: RawAxiosRequestConfig): AxiosPromise<SetReferenceOut> {
            return localVarFp.riskSetReference(requestParameters.set, requestParameters.setReferenceIn, options).then((request) => request(axios, basePath));
        },
        /**
         * State reports the caller organisation\'s own model: what it has learned, whether it is live or still in shadow, the threshold in force, the appetite it stated beside the share it actually realised, every refusal by reason, every feature that read blind, and how much of the organisation\'s own event surface has been folded in.  It covers ONE organisation. A caller cannot learn another\'s volumes, alert rate or behaviour from it, because the state is read out of a model that holds only its own.
         * @summary Report your organisation\'s model: what it learned, and what it realised
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        riskState(options?: RawAxiosRequestConfig): AxiosPromise<RiskModelState> {
            return localVarFp.riskState(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for riskAdoptModel operation in RiskApi.
 * @export
 * @interface RiskApiRiskAdoptModelRequest
 */
export interface RiskApiRiskAdoptModelRequest {
    /**
     * 
     * @type {RiskAdoptIn}
     * @memberof RiskApiRiskAdoptModel
     */
    readonly riskAdoptIn: RiskAdoptIn
}

/**
 * Request parameters for riskClearReference operation in RiskApi.
 * @export
 * @interface RiskApiRiskClearReferenceRequest
 */
export interface RiskApiRiskClearReferenceRequest {
    /**
     * 
     * @type {string}
     * @memberof RiskApiRiskClearReference
     */
    readonly set: string

    /**
     * 
     * @type {string}
     * @memberof RiskApiRiskClearReference
     */
    readonly key?: string
}

/**
 * Request parameters for riskCreateDataset operation in RiskApi.
 * @export
 * @interface RiskApiRiskCreateDatasetRequest
 */
export interface RiskApiRiskCreateDatasetRequest {
    /**
     * 
     * @type {RiskDatasetSpec}
     * @memberof RiskApiRiskCreateDataset
     */
    readonly riskDatasetSpec: RiskDatasetSpec
}

/**
 * Request parameters for riskDataset operation in RiskApi.
 * @export
 * @interface RiskApiRiskDatasetRequest
 */
export interface RiskApiRiskDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof RiskApiRiskDataset
     */
    readonly name: string
}

/**
 * Request parameters for riskDatasetLineage operation in RiskApi.
 * @export
 * @interface RiskApiRiskDatasetLineageRequest
 */
export interface RiskApiRiskDatasetLineageRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof RiskApiRiskDatasetLineage
     */
    readonly name: string

    /**
     * Version is the version to trace. Zero takes the newest published one.
     * @type {number}
     * @memberof RiskApiRiskDatasetLineage
     */
    readonly version?: number
}

/**
 * Request parameters for riskDeleteDataset operation in RiskApi.
 * @export
 * @interface RiskApiRiskDeleteDatasetRequest
 */
export interface RiskApiRiskDeleteDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof RiskApiRiskDeleteDataset
     */
    readonly name: string
}

/**
 * Request parameters for riskDisposeLabels operation in RiskApi.
 * @export
 * @interface RiskApiRiskDisposeLabelsRequest
 */
export interface RiskApiRiskDisposeLabelsRequest {
    /**
     * 
     * @type {RiskDisposeIn}
     * @memberof RiskApiRiskDisposeLabels
     */
    readonly riskDisposeIn: RiskDisposeIn
}

/**
 * Request parameters for riskExportDataset operation in RiskApi.
 * @export
 * @interface RiskApiRiskExportDatasetRequest
 */
export interface RiskApiRiskExportDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof RiskApiRiskExportDataset
     */
    readonly name: string

    /**
     * Version is the version to read. Zero takes the newest published one.
     * @type {number}
     * @memberof RiskApiRiskExportDataset
     */
    readonly version?: number

    /**
     * Split narrows to train, val or test. Empty reads every split.
     * @type {string}
     * @memberof RiskApiRiskExportDataset
     */
    readonly split?: string

    /**
     * Offset is where the page starts, in the version\&#39;s own row order (by id, which is derived from the row and therefore stable forever).
     * @type {number}
     * @memberof RiskApiRiskExportDataset
     */
    readonly offset?: number

    /**
     * Limit is how many rows to return. Zero and anything above the plane\&#39;s bound take the bound.
     * @type {number}
     * @memberof RiskApiRiskExportDataset
     */
    readonly limit?: number
}

/**
 * Request parameters for riskFeatures operation in RiskApi.
 * @export
 * @interface RiskApiRiskFeaturesRequest
 */
export interface RiskApiRiskFeaturesRequest {
    /**
     * Days is how far back to measure the organisation\&#39;s own coverage, 1 to 400. Zero takes thirty.
     * @type {number}
     * @memberof RiskApiRiskFeatures
     */
    readonly days?: number
}

/**
 * Request parameters for riskHoldLabels operation in RiskApi.
 * @export
 * @interface RiskApiRiskHoldLabelsRequest
 */
export interface RiskApiRiskHoldLabelsRequest {
    /**
     * 
     * @type {RiskHoldIn}
     * @memberof RiskApiRiskHoldLabels
     */
    readonly riskHoldIn: RiskHoldIn
}

/**
 * Request parameters for riskLabel operation in RiskApi.
 * @export
 * @interface RiskApiRiskLabelRequest
 */
export interface RiskApiRiskLabelRequest {
    /**
     * 
     * @type {RiskLabelIn}
     * @memberof RiskApiRiskLabel
     */
    readonly riskLabelIn: RiskLabelIn
}

/**
 * Request parameters for riskLabelCoverage operation in RiskApi.
 * @export
 * @interface RiskApiRiskLabelCoverageRequest
 */
export interface RiskApiRiskLabelCoverageRequest {
    /**
     * From and To bound the EVENT window, half-open, RFC 3339.  Unstated, the window is the 90 days ENDING where maturity begins — &#x60;to&#x60; is the horizon ago, not now. A default window running to now under a default horizon could not contain one matured event, so every count below it would be zero however much ground truth the tenant held.
     * @type {string}
     * @memberof RiskApiRiskLabelCoverage
     */
    readonly from?: string

    /**
     * 
     * @type {string}
     * @memberof RiskApiRiskLabelCoverage
     */
    readonly to?: string

    /**
     * Horizon is the maturity horizon in days the coverage is measured under. Unstated takes 120. It also moves the default window, which ends where maturity begins.
     * @type {number}
     * @memberof RiskApiRiskLabelCoverage
     */
    readonly horizon?: number
}

/**
 * Request parameters for riskLabels operation in RiskApi.
 * @export
 * @interface RiskApiRiskLabelsRequest
 */
export interface RiskApiRiskLabelsRequest {
    /**
     * Kind and Subject narrow to one entity.
     * @type {string}
     * @memberof RiskApiRiskLabels
     */
    readonly kind?: string

    /**
     * 
     * @type {string}
     * @memberof RiskApiRiskLabels
     */
    readonly subject?: string

    /**
     * Source narrows to one asserter — the read that answers \&quot;what has commerce told us\&quot;, separately from \&quot;what has an analyst told us\&quot;.
     * @type {string}
     * @memberof RiskApiRiskLabels
     */
    readonly source?: string

    /**
     * From and To bound the EVENT time, half-open, RFC 3339.
     * @type {string}
     * @memberof RiskApiRiskLabels
     */
    readonly from?: string

    /**
     * 
     * @type {string}
     * @memberof RiskApiRiskLabels
     */
    readonly to?: string

    /**
     * Limit caps the page. Out of range takes the plane\&#39;s own bound.
     * @type {number}
     * @memberof RiskApiRiskLabels
     */
    readonly limit?: number
}

/**
 * Request parameters for riskLearn operation in RiskApi.
 * @export
 * @interface RiskApiRiskLearnRequest
 */
export interface RiskApiRiskLearnRequest {
    /**
     * 
     * @type {RiskLearnIn}
     * @memberof RiskApiRiskLearn
     */
    readonly riskLearnIn: RiskLearnIn
}

/**
 * Request parameters for riskMaterializeDataset operation in RiskApi.
 * @export
 * @interface RiskApiRiskMaterializeDatasetRequest
 */
export interface RiskApiRiskMaterializeDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof RiskApiRiskMaterializeDataset
     */
    readonly name: string
}

/**
 * Request parameters for riskReference operation in RiskApi.
 * @export
 * @interface RiskApiRiskReferenceRequest
 */
export interface RiskApiRiskReferenceRequest {
    /**
     * 
     * @type {string}
     * @memberof RiskApiRiskReference
     */
    readonly set: string

    /**
     * After pages the override listing: the last key of the previous page.
     * @type {string}
     * @memberof RiskApiRiskReference
     */
    readonly after?: string

    /**
     * Limit caps the override listing: default 200, maximum 1000.
     * @type {number}
     * @memberof RiskApiRiskReference
     */
    readonly limit?: number
}

/**
 * Request parameters for riskRefreshReference operation in RiskApi.
 * @export
 * @interface RiskApiRiskRefreshReferenceRequest
 */
export interface RiskApiRiskRefreshReferenceRequest {
    /**
     * 
     * @type {RefreshReferenceIn}
     * @memberof RiskApiRiskRefreshReference
     */
    readonly refreshReferenceIn: RefreshReferenceIn
}

/**
 * Request parameters for riskResolveLabels operation in RiskApi.
 * @export
 * @interface RiskApiRiskResolveLabelsRequest
 */
export interface RiskApiRiskResolveLabelsRequest {
    /**
     * 
     * @type {RiskResolveIn}
     * @memberof RiskApiRiskResolveLabels
     */
    readonly riskResolveIn: RiskResolveIn
}

/**
 * Request parameters for riskResolveReference operation in RiskApi.
 * @export
 * @interface RiskApiRiskResolveReferenceRequest
 */
export interface RiskApiRiskResolveReferenceRequest {
    /**
     * 
     * @type {ResolveReferenceIn}
     * @memberof RiskApiRiskResolveReference
     */
    readonly resolveReferenceIn: ResolveReferenceIn
}

/**
 * Request parameters for riskScore operation in RiskApi.
 * @export
 * @interface RiskApiRiskScoreRequest
 */
export interface RiskApiRiskScoreRequest {
    /**
     * 
     * @type {RiskScoreIn}
     * @memberof RiskApiRiskScore
     */
    readonly riskScoreIn: RiskScoreIn
}

/**
 * Request parameters for riskSearch operation in RiskApi.
 * @export
 * @interface RiskApiRiskSearchRequest
 */
export interface RiskApiRiskSearchRequest {
    /**
     * 
     * @type {RiskSearchIn}
     * @memberof RiskApiRiskSearch
     */
    readonly riskSearchIn: RiskSearchIn
}

/**
 * Request parameters for riskSearchResult operation in RiskApi.
 * @export
 * @interface RiskApiRiskSearchResultRequest
 */
export interface RiskApiRiskSearchResultRequest {
    /**
     * ID is the run, taken from the path. A run another organisation started is simply not there — the same answer an unknown id gives.
     * @type {string}
     * @memberof RiskApiRiskSearchResult
     */
    readonly id: string
}

/**
 * Request parameters for riskSetPolicy operation in RiskApi.
 * @export
 * @interface RiskApiRiskSetPolicyRequest
 */
export interface RiskApiRiskSetPolicyRequest {
    /**
     * 
     * @type {RiskAppetiteIn}
     * @memberof RiskApiRiskSetPolicy
     */
    readonly riskAppetiteIn: RiskAppetiteIn
}

/**
 * Request parameters for riskSetReference operation in RiskApi.
 * @export
 * @interface RiskApiRiskSetReferenceRequest
 */
export interface RiskApiRiskSetReferenceRequest {
    /**
     * 
     * @type {string}
     * @memberof RiskApiRiskSetReference
     */
    readonly set: string

    /**
     * 
     * @type {SetReferenceIn}
     * @memberof RiskApiRiskSetReference
     */
    readonly setReferenceIn: SetReferenceIn
}

/**
 * RiskApi - object-oriented interface
 * @export
 * @class RiskApi
 * @extends {BaseAPI}
 */
export class RiskApi extends BaseAPI {
    /**
     * Reports whether the risk model plane can actually work right now.  It is a REAL probe, not status theatre: it reports that the per-organisation model plane was built, that the per-organisation stores can be written, and whether the event surface the feature plane is rolled up from is reachable. 200 only when the plane can work; otherwise 503 CARRYING THE REPORT — which part failed and the real error.  An unreachable event surface is REPORTED and is not a failure. Scoring reads in-memory aggregates and never the warehouse, so a warm that cannot run degrades how much history a model has seen and does not stop it deciding.  It also reports how many organisations\' models are resident, how many have been evicted to hold that bound, and how many of the resident ones are at their own aggregate bound. Eviction is lossless — learned state is written to that organisation\'s own store first and its aggregates rebuild from its own record — so a climbing count is a capacity signal, not a loss. A STRAINED model is different: it has started forgetting its own least-recently-active subjects, and each forgotten subject reads as inactive until it is active again. That is a control degrading, and it is reported here because it is otherwise silent.  It answers about the process, not about a tenant: it takes no organisation and names none.
     * @summary Reports whether the risk model plane can actually work right now.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public getV1RiskHealth(options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).getV1RiskHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Puts one of your organisation\'s OWN PUBLISHED VALUES in force, by name — which is what an instant rollback is, what promoting a challenger is, and what installing the shape a search found is.  IT TAKES AN ADDRESS AND NEVER STATE. The masses are read from your own store, so nothing about your model has to be held by whatever is making this call. That closes the sharpest edge the previous shape had: a body of counters is something a caller can COMPOSE, and a region filled until activity inside it reads as ordinary is a model that has been shaped rather than learned. The engine\'s mass invariant was the only thing standing between a composed body and the model; with an address there is no body to compose.  IT ADOPTS THE SHAPE, NOT ONLY THE MASSES. A value records the model space its masses were taken in, and a value whose space differs from the one in force REPLANTS your model into that space before restoring them. That is what makes POST /v1/risk/search actionable: a search answers with the shape that fits your own history best and publishes it fitted, and its address is what you name here. Before this, a winning shape was advice nobody could take — the adoption path refused every shape change, and a winner is a different shape by definition.  WHAT ADOPTING A SEARCHED SHAPE COSTS, SAID PLAINLY: the value a search fits has learned the window the search replayed and nothing older, so installing it trades history for fit. Your appetite is untouched — that is your policy record\'s, with its own versions — and so is the geometry, which stays your own.  An address your organisation has not published is NOT FOUND. That includes one another organisation published, and it is not a lookup that failed: the store is per organisation and the address is a name, never an authority.
     * @summary Put one of your organisation\'s own published model values in force
     * @param {RiskApiRiskAdoptModelRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskAdoptModel(requestParameters: RiskApiRiskAdoptModelRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskAdoptModel(requestParameters.riskAdoptIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes one of your organisation\'s overrides.  It removes an entry your organisation wrote, never a baseline member: the published set is not writable from here, so a removal can only ever restore the baseline\'s own answer.
     * @summary Removes one of your organisation\'s overrides.
     * @param {RiskApiRiskClearReferenceRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskClearReference(requestParameters: RiskApiRiskClearReferenceRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskClearReference(requestParameters.set, requestParameters.key, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
     * @summary Declare the next version of a dataset
     * @param {RiskApiRiskCreateDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskCreateDataset(requestParameters: RiskApiRiskCreateDatasetRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskCreateDataset(requestParameters.riskDatasetSpec, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
     * @summary Describe every version of one dataset
     * @param {RiskApiRiskDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskDataset(requestParameters: RiskApiRiskDatasetRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskDataset(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
     * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
     * @param {RiskApiRiskDatasetLineageRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskDatasetLineage(requestParameters: RiskApiRiskDatasetLineageRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskDatasetLineage(requestParameters.name, requestParameters.version, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
     * @summary List this org\'s datasets
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskDatasets(options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskDatasets(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
     * @summary Dispose of one dataset and every version of it
     * @param {RiskApiRiskDeleteDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskDeleteDataset(requestParameters: RiskApiRiskDeleteDatasetRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskDeleteDataset(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Applies this tenant\'s retention, and only this tenant\'s.  It is bounded three ways, each a compliance property rather than a convenience. It refuses a boundary younger than the platform floor, because a label can be the input to an adverse action and five years is what the retention ledger holds such a record for. It never touches a record under litigation hold. And it disposes of whole records rather than redacting fields.  It removes the derived columnar copy BEFORE the record, and refuses the whole disposal if the warehouse cannot be reached. The other order would leave rows in the warehouse that nothing can identify any more, which is a disposal that did not happen and says it did.
     * @summary Dispose of this tenant\'s expired assertions, whole records only
     * @param {RiskApiRiskDisposeLabelsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskDisposeLabels(requestParameters: RiskApiRiskDisposeLabelsRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskDisposeLabels(requestParameters.riskDisposeIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
     * @summary Read a version\'s rows back, one page at a time
     * @param {RiskApiRiskExportDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskExportDataset(requestParameters: RiskApiRiskExportDatasetRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskExportDataset(requestParameters.name, requestParameters.version, requestParameters.split, requestParameters.offset, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Features is the feature catalogue in its two honest lenses.  The MODEL lens is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves, the supervisor\'s own words for the indicator, and the published standard those words come from — so a coverage claim is checkable rather than asserted. It is the same for every organisation.  The SURFACE lens is what THIS organisation\'s own event surface actually carries, measured over the window: how many of its buckets carry each dimension at all, and what the dimension reads where it is present. A dimension present in no bucket is BLIND, and saying so is the difference between no risk and no data.
     * @summary The feature catalogue: what the model reads, and what your surface carries
     * @param {RiskApiRiskFeaturesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskFeatures(requestParameters: RiskApiRiskFeaturesRequest = {}, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskFeatures(requestParameters.days, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Places or releases a litigation hold on named records.  A hold is a fact about the RECORD, not about the world: it says retention may not dispose of this row, and it asserts nothing about what happened. So it is not a field on an assertion and it is not folded into the content digest — carried there it was silently a no-op on any record that already existed, since re-filing the same assertion with a hold flag produced the same digest, the insert was ignored, and the caller was answered `duplicate` while the hold it asked for was never placed. This op is the one way a hold moves, in either direction, and the move is written to the audit log.  Every named id is this tenant\'s or is nothing. The statement runs against the tenant\'s own file, which holds no other tenant\'s rows and has no column that could name one.
     * @summary Place or release a litigation hold on named records
     * @param {RiskApiRiskHoldLabelsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskHoldLabels(requestParameters: RiskApiRiskHoldLabelsRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskHoldLabels(requestParameters.riskHoldIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Records a batch of ground truth against the entities it judges.  Each assertion carries TWO times — when the judged event happened, and when the assertion became knowable — and both are required. The second is what keeps a chargeback that landed in June out of a model that had to decide in February.  It is idempotent on the CONTENT of an assertion, so a webhook that redelivers is safe. It never overwrites: a source that corrects itself later files a NEW assertion, which wins from the moment it became knowable and leaves every earlier observation instant seeing exactly what it saw.  The asserter is stamped from the validated credential and is not a body field.
     * @summary Assert ground truth about events
     * @param {RiskApiRiskLabelRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskLabel(requestParameters: RiskApiRiskLabelRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskLabel(requestParameters.riskLabelIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reports how much of a window has matured and how much of that is judged, per source.  It is the gate on training. A supervised fit over a window whose judged count is near zero produces a number, and the number is meaningless; this op is what lets that be stated before the fit rather than discovered after it.  It reads the RECORD plane and folds every assertion at that event\'s OWN as-of, so the counts obey exactly the leakage rule a materialisation would. It counts only what was ASSERTED: what share of the whole event STREAM carries a label is a question about the feature plane\'s denominator and is not answerable here.
     * @summary How much of the window has matured, and how much of that is judged
     * @param {RiskApiRiskLabelCoverageRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskLabelCoverage(requestParameters: RiskApiRiskLabelCoverageRequest = {}, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskLabelCoverage(requestParameters.from, requestParameters.to, requestParameters.horizon, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Publishes the closed vocabularies and the precedence rule that resolves a conflict between two sources.  A precedence rule nobody can read is a rule nobody can audit or dispute, and the whole defensibility of a contested label rests on being able to say why one assertion beat another. The order returned here is derived from the same declaration the resolver reads — it is not a description of it.
     * @summary The closed vocabularies and the precedence rule that resolves a conflict
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskLabelVocabulary(options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskLabelVocabulary(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads the assertions this tenant has recorded, newest event first.  It reads the RECORD — the tenant\'s own store — and not the columnar copy, so what it returns is what would be produced in an audit. Narrow it by entity, by asserter, or by event window.
     * @summary Read the assertions this tenant has recorded
     * @param {RiskApiRiskLabelsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskLabels(requestParameters: RiskApiRiskLabelsRequest = {}, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskLabels(requestParameters.kind, requestParameters.subject, requestParameters.source, requestParameters.from, requestParameters.to, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Learn records a batch of events into the caller organisation\'s own aggregates and lets its model learn from them. It answers how many it learned from.  IT DOES NOT SCORE, AND THAT IS THE POINT. An observation is a value you record; learning is a transformation over observations; a verdict is a query against the result. This op is the first two. [ops.score] is the third, it is pure, and it is the ONE door to a verdict. They were one call, which meant you could not record without training and could not train without being answered — and the model ran twice over every event to produce a verdict the response carried and no caller read.  TO OBSERVE AND JUDGE, COMPOSE THE TWO, and mind the order. Score FIRST, then learn: the score is then the model\'s opinion of an event it has not yet learned from, which is the question worth asking. The other order answers for a model that has already absorbed the event it is judging.  This is the training path, and there is no job behind it: the model IS a set of mass counters over half-space trees, so learning is an increment and the model is current the instant the last event lands. Nothing from any other organisation is in it, and nothing from this organisation leaves it.  A RETRY IS INERT. The record deduplicates on the event id you send, and an event already in it moves nothing, costs nothing and is not counted — so a client that timed out can send the same batch again and its model holds what it holds. Without an id of your own there is nothing to converge on: two identical bodies are two events.
     * @summary Teach your organisation\'s own model from its own events
     * @param {RiskApiRiskLearnRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskLearn(requestParameters: RiskApiRiskLearnRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskLearn(requestParameters.riskLearnIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
     * @summary Materialise the declared version into immutable rows
     * @param {RiskApiRiskMaterializeDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskMaterializeDataset(requestParameters: RiskApiRiskMaterializeDatasetRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskMaterializeDataset(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Policy reports the caller organisation\'s own decision-regime history: every distinct regime it has adopted, which version is in force, and what retention has taken.  WHY IT EXISTS. Every score cites the version it was decided under ([riskScoreOut.Policy]), and the threshold that score was measured against is derived from the appetite that version states. Restate the appetite and, without this record, every earlier decision becomes unreconstructible — the cut it was judged by no longer exists anywhere. An adverse decision that cannot be explained against the policy in force when it was taken cannot be defended.  It covers ONE organisation. The history is on that organisation\'s own shelf, so another\'s versions are not filtered out of the answer — they are not in the file the answer is read from.
     * @summary Your organisation\'s decision-regime history, and which version is in force
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskPolicy(options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskPolicy(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Publishes your organisation\'s model as a NAMED VALUE, so a decision taken today can be reconstructed tomorrow and a change made today can be undone.  It answers with a NAME and not with the state. The masses stay on your organisation\'s own encrypted store and are referred to by an address computed from their own content: the shape, the geometry seed, the position in the window, the threshold, the masses themselves as IEEE-754 bits, and the fold watermark behind them. That is what makes the value nameable without making the caller its custodian.  IT IS IDEMPOTENT ON THE VALUE. A model that has not changed publishes to the name it already has and mints nothing, reporting minted=false — so publishing at every boundary that matters is free. Ten values are retained per organisation, bounded in BYTES rather than in rows, and the oldest is disposed of past that.  A model that has learned nothing is refused: planted is not learned, and a value that reproduces nothing is not a value.  It is POST and PUT on one address because they are one plane\'s two verbs over one kind of thing: POST mints a value from the model in force, PUT puts a value in force. They were /v1/risk/state/snapshot and /v1/risk/state/restore — two addresses named after the operation rather than after the thing, which is how a reader ends up asking what the difference between a snapshot and a value is.
     * @summary Publish your organisation\'s model as a named, immutable value
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskPublishModel(options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskPublishModel(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reference describes one set and lists your org\'s overrides in it.  The set half is public data about a published list — its version, its publishers, their licences and how current each one is. The overrides half is yours alone: it is read from your organisation\'s own store, and no other organisation\'s entries can appear in it.
     * @summary Reference describes one set and lists your org\'s overrides in it.
     * @param {RiskApiRiskReferenceRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskReference(requestParameters: RiskApiRiskReferenceRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskReference(requestParameters.set, requestParameters.after, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists every set this plane publishes, with its version and how fresh it is.  Read the Stale and Refused lists first: they are the two ways this plane can be quietly wrong, and they are reported rather than inferred. A set in Refused answers nothing — it has never loaded, it is held by another component, or it names a source we hold no licence for.
     * @summary Lists every set this plane publishes, with its version and how fresh it is.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskReferenceSets(options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskReferenceSets(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Takes a new version of one set. SuperAdmin only.  It is platform work, not tenant work: it writes the shared baseline every organisation reads, so it is gated to the platform\'s own identity. Nothing here can write an organisation\'s overrides, and nothing an organisation sends can reach this route.  Idempotent. A version is the content digest of what was taken, so refreshing an unchanged publisher writes no rows and reports unchanged. Resumable: a run that died half-way is continued from where it stopped rather than restarted.  A set whose source needs a licence we do not hold is refused with the reason, rather than being quietly skipped.
     * @summary Takes a new version of one set.
     * @param {RiskApiRiskRefreshReferenceRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskRefreshReference(requestParameters: RiskApiRiskRefreshReferenceRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskRefreshReference(requestParameters.refreshReferenceIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers, for each named event, which assertion was in force AS OF that event\'s own horizon — and what disagreed with it.  This is the join surface: the dataset materialiser calls it to attach ground truth to training rows, and the evaluator calls it to score a past decision against what was knowable when the decision had to be made. One mechanism for both, so a model can never be trained under one leakage rule and scored under another.  Three answers are distinct and all three are honest: a resolved label, an event that has not matured, and a matured event nobody has judged. The last is never reported as unproductive.
     * @summary Resolve the label in force for named events, as of each event\'s own horizon
     * @param {RiskApiRiskResolveLabelsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskResolveLabels(requestParameters: RiskApiRiskResolveLabelsRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskResolveLabels(requestParameters.riskResolveIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Looks keys up against the reference plane.  Your organisation\'s own overrides are consulted FIRST and win outright; the shared baseline answers everything they do not cover. Every answer names the version that produced it, when that version was current and whether it is stale, so a decision can record exactly what it consulted.  Read Refusal before reading Hit. A set that has never loaded, one held by the component that screens against it, and one whose source needs a licence we do not hold all answer with a refusal — and a miss on a refusing set means nothing is known, not that the key is clean.
     * @summary Looks keys up against the reference plane.
     * @param {RiskApiRiskResolveReferenceRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskResolveReference(requestParameters: RiskApiRiskResolveReferenceRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskResolveReference(requestParameters.resolveReferenceIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Score judges one event against the caller organisation\'s OWN model and learns nothing from it. It is how a candidate is tried against real behaviour before anything depends on the answer, and it is the model\'s analogue of testing a rule.  Because it records nothing, the aggregates it reads do not include the event: the numbers are the organisation\'s history as it stands. A model still warming declines with a reason rather than answering zero, because silence must never read as a clean result.
     * @summary Score one event against your organisation\'s own model
     * @param {RiskApiRiskScoreRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskScore(requestParameters: RiskApiRiskScoreRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskScore(requestParameters.riskScoreIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Search runs an exhaustive search for the model shape that best fits the caller organisation\'s own history, and answers 202 with the run to read back.  Every candidate is replayed over that organisation\'s OWN feature surface in its own sandbox — its own aggregates, its own model, neither of them the live one — so a run cannot move a live threshold and cannot see another organisation\'s data. The result is the learning curve for each shape and the one that fit best, ranked on how closely it honoured the stated appetite, whether it warmed at all, whether it saturated, and how much of the coordinate space it left blind.  An empty history is REFUSED rather than reported as zero alerts, because \"no alerts\" is exactly what a quiet model looks like.
     * @summary Search exhaustively for the model shape that fits your own history
     * @param {RiskApiRiskSearchRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskSearch(requestParameters: RiskApiRiskSearchRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskSearch(requestParameters.riskSearchIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads back one search run: every shape tried over this organisation\'s own history, best first, and the one that fit.  A run another organisation started is simply not there — the same 404 an unknown id gives, so the read is not a probe oracle.
     * @summary Read back one exhaustive search
     * @param {RiskApiRiskSearchResultRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskSearchResult(requestParameters: RiskApiRiskSearchResultRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskSearchResult(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * States the decision regime the caller organisation\'s model decides under: how much of its own stream may be sent for examination, how much of the rest is sampled to measure what was missed, and whether the model may change an outcome at all.  The appetite is the decision a model is not permitted to make for itself: its output is a probability, so how likely it is to MISS something is a matter of policy that has to be stated, measured and reviewed rather than absorbed into a constant. The alert threshold is derived from it as a quantile of the scores actually observed, which is what keeps its meaning as the distribution drifts.  It is DURABLE BEFORE IT IS IN FORCE. The regime is recorded as a new version on the organisation\'s own shelf before anything in memory moves, so a policy that cannot be written down is refused rather than answered from state the next rollout would silently undo.  ARMING IS AN ADMIN ACT AND TUNING IS NOT. Setting `live` requires an admin of this organisation; stating the appetite and the sample is self-service for any member. Taking the model live decides whether it may change an OUTCOME at all — a payment frozen, a grant refused — for every customer this organisation has, and that is a decision an organisation takes rather than one of its members.  A RESTATEMENT OF THE REGIME IN FORCE MINTS NOTHING and answers the version already in force. Compare the version you receive with the version you had: unchanged means the numbers were the same, which is why there is no flag for it.  Learned state survives the change. The model\'s identity covers its SHAPE — the inventory and the geometry — and not its appetite, so restating policy unlearns nothing. It also does not REPORT the learned state: what the model is is read from the model.
     * @summary State the decision regime: the appetite, the sample, and whether the model is live
     * @param {RiskApiRiskSetPolicyRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskSetPolicy(requestParameters: RiskApiRiskSetPolicyRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskSetPolicy(requestParameters.riskAppetiteIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Writes your organisation\'s own allow and deny entries over a set.  Idempotent on the key: writing the same entry twice is one entry, and writing it again replaces the verdict and the note. The whole batch is one transaction, so a batch that would cross the per-set bound writes nothing rather than half of itself — a half-applied deny list is worse than a refused one, because nobody can tell which half applied.  Your entries are held in your organisation\'s own store and are never visible to another organisation, and they never change what any other organisation sees. The shared baseline is not writable from here at all.
     * @summary Writes your organisation\'s own allow and deny entries over a set.
     * @param {RiskApiRiskSetReferenceRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskSetReference(requestParameters: RiskApiRiskSetReferenceRequest, options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskSetReference(requestParameters.set, requestParameters.setReferenceIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * State reports the caller organisation\'s own model: what it has learned, whether it is live or still in shadow, the threshold in force, the appetite it stated beside the share it actually realised, every refusal by reason, every feature that read blind, and how much of the organisation\'s own event surface has been folded in.  It covers ONE organisation. A caller cannot learn another\'s volumes, alert rate or behaviour from it, because the state is read out of a model that holds only its own.
     * @summary Report your organisation\'s model: what it learned, and what it realised
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof RiskApi
     */
    public riskState(options?: RawAxiosRequestConfig) {
        return RiskApiFp(this.configuration).riskState(options).then((request) => request(this.axios, this.basePath));
    }
}

