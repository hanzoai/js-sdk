/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { Blob } from '../models';
// @ts-ignore
import type { EndIn } from '../models';
// @ts-ignore
import type { ExecRequest } from '../models';
// @ts-ignore
import type { ExecResult } from '../models';
// @ts-ignore
import type { LeaseIn } from '../models';
// @ts-ignore
import type { Leased } from '../models';
// @ts-ignore
import type { PathIn } from '../models';
// @ts-ignore
import type { Ran } from '../models';
// @ts-ignore
import type { RunIn } from '../models';
// @ts-ignore
import type { Sandbox } from '../models';
// @ts-ignore
import type { SandboxList } from '../models';
// @ts-ignore
import type { StopIn } from '../models';
// @ts-ignore
import type { Stopped } from '../models';
// @ts-ignore
import type { TicketGrant } from '../models';
// @ts-ignore
import type { WriteIn } from '../models';
// @ts-ignore
import type { Wrote } from '../models';
/**
 * SandboxApi - axios parameter creator
 * @export
 */
export const SandboxApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Ends a sandbox and releases the compute behind it. Answers 204.  ENDING IS NOT STOPPING. This releases the resource: the pod goes and anything only inside it goes with it. To end what a sandbox is RUNNING while keeping the sandbox — the checkout, the logs, the half-written file — the verb is POST /v1/sandbox/stop.  `?purge=1` additionally removes the record, so the sandbox stops being listed at all rather than being listed as ended.
         * @summary Ends a sandbox and releases the compute behind it.
         * @param {string} id ID is the sandbox to end, from the path.
         * @param {string} [purge] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteSandboxById: async (id: string, purge?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('deleteSandboxById', 'id', id)
            const localVarPath = `/v1/sandbox/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (purge !== undefined) {
                localVarQueryParameter['purge'] = purge;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
         * @summary End a sandbox and release it
         * @param {EndIn} endIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        endSandbox: async (endIn: EndIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'endIn' is not null or undefined
            assertParamExists('endSandbox', 'endIn', endIn)
            const localVarPath = `/v1/sandbox/end`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(endIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the caller org\'s sandboxes, newest first.  `?project=` and `?status=` narrow it. Only the caller\'s org\'s: the store is keyed on the validated org, so another tenant\'s sandbox is not something this operation can return.
         * @summary Lists the caller org\'s sandboxes, newest first.
         * @param {string} [project] 
         * @param {string} [status] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandbox: async (project?: string, status?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/sandbox`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (project !== undefined) {
                localVarQueryParameter['project'] = project;
            }

            if (status !== undefined) {
                localVarQueryParameter['status'] = status;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.  An id the caller\'s org does not hold is the same 404 an unknown id gives — the store is keyed on the org, so a cross-tenant id simply is not there.
         * @summary Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.
         * @param {string} id ID is the sandbox to address, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxById: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getSandboxById', 'id', id)
            const localVarPath = `/v1/sandbox/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
         * @summary Read a file, or list a directory
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdFs: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getSandboxByIdFs', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/fs`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
         * @summary The screen, as a page
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdScreen: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getSandboxByIdScreen', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/screen`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
         * @summary The screen, as a socket
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdScreenWs: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getSandboxByIdScreenWs', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/screen/ws`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
         * @summary The terminal, as a page
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdTerminal: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getSandboxByIdTerminal', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/terminal`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
         * @summary The terminal, as a socket
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdTerminalWs: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('getSandboxByIdTerminalWs', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/terminal/ws`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
         * @summary Lease a sandbox — a real computer — or resume one you hold
         * @param {LeaseIn} leaseIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        leaseSandbox: async (leaseIn: LeaseIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'leaseIn' is not null or undefined
            assertParamExists('leaseSandbox', 'leaseIn', leaseIn)
            const localVarPath = `/v1/sandbox/lease`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(leaseIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Leases a sandbox — a real computer — for the caller\'s org.  The class decides what it is for and therefore its image, working directory and isolation. A dev or desktop sandbox is SINGLE-ATTACH per project, so asking twice for one project resumes the one that exists rather than paying for a second; an exec sandbox carries no project and is bounded per org instead, refused 429 past the ceiling because the caller\'s correct response is to wait.  Answers 201 with the sandbox as leased, which names the runtime it GOT — not the one that was asked for.
         * @summary Leases a sandbox — a real computer — for the caller\'s org.
         * @param {LeaseIn} leaseIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandbox: async (leaseIn: LeaseIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'leaseIn' is not null or undefined
            assertParamExists('postSandbox', 'leaseIn', leaseIn)
            const localVarPath = `/v1/sandbox`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(leaseIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.  Send `argv` — an argument vector cannot be word-split by accident — or `command` for a shell line, which is the only input here that ever reaches a shell. A non-zero exit is a SUCCESSFUL call carrying a failed command: the status is 200 and the exit code is in the answer, because \"the command failed\" and \"the call failed\" are different facts.
         * @summary Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.
         * @param {string} id ID is the sandbox to run in, from the path.
         * @param {ExecRequest} execRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdExec: async (id: string, execRequest: ExecRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postSandboxByIdExec', 'id', id)
            // verify required parameter 'execRequest' is not null or undefined
            assertParamExists('postSandboxByIdExec', 'execRequest', execRequest)
            const localVarPath = `/v1/sandbox/{id}/exec`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(execRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
         * @summary Write a file
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdFs: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postSandboxByIdFs', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/fs`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Mints a short-lived grant to open the screen of a desktop sandbox. Same properties as the terminal ticket, for the other door.
         * @summary Mints a short-lived grant to open the screen of a desktop sandbox.
         * @param {string} id ID is the sandbox to address, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdScreenTicket: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postSandboxByIdScreenTicket', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/screen/ticket`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Mints a short-lived grant to open a terminal on a sandbox.  The ticket travels in the query string of the URL it answers with, because a browser cannot set an Authorization header on a WebSocket handshake. It is single-purpose and short-lived for exactly that reason. A sandbox that is not running is 409 rather than a ticket that cannot be used.
         * @summary Mints a short-lived grant to open a terminal on a sandbox.
         * @param {string} id ID is the sandbox to address, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdTerminalTicket: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('postSandboxByIdTerminalTicket', 'id', id)
            const localVarPath = `/v1/sandbox/{id}/terminal/ticket`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
         * @summary Read a file from a sandbox you hold
         * @param {PathIn} pathIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        readSandboxFile: async (pathIn: PathIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'pathIn' is not null or undefined
            assertParamExists('readSandboxFile', 'pathIn', pathIn)
            const localVarPath = `/v1/sandbox/read`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(pathIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
         * @summary Run a command in a sandbox you hold and read its output
         * @param {RunIn} runIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        runInSandbox: async (runIn: RunIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'runIn' is not null or undefined
            assertParamExists('runInSandbox', 'runIn', runIn)
            const localVarPath = `/v1/sandbox/run`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(runIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
         * @summary Stop what a sandbox is running, and keep the sandbox
         * @param {StopIn} stopIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        stopRun: async (stopIn: StopIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'stopIn' is not null or undefined
            assertParamExists('stopRun', 'stopIn', stopIn)
            const localVarPath = `/v1/sandbox/stop`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(stopIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
         * @summary Write a file into a sandbox you hold
         * @param {WriteIn} writeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        writeSandboxFile: async (writeIn: WriteIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'writeIn' is not null or undefined
            assertParamExists('writeSandboxFile', 'writeIn', writeIn)
            const localVarPath = `/v1/sandbox/write`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(writeIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * SandboxApi - functional programming interface
 * @export
 */
export const SandboxApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = SandboxApiAxiosParamCreator(configuration)
    return {
        /**
         * Ends a sandbox and releases the compute behind it. Answers 204.  ENDING IS NOT STOPPING. This releases the resource: the pod goes and anything only inside it goes with it. To end what a sandbox is RUNNING while keeping the sandbox — the checkout, the logs, the half-written file — the verb is POST /v1/sandbox/stop.  `?purge=1` additionally removes the record, so the sandbox stops being listed at all rather than being listed as ended.
         * @summary Ends a sandbox and releases the compute behind it.
         * @param {string} id ID is the sandbox to end, from the path.
         * @param {string} [purge] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteSandboxById(id: string, purge?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteSandboxById(id, purge, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.deleteSandboxById']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
         * @summary End a sandbox and release it
         * @param {EndIn} endIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async endSandbox(endIn: EndIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.endSandbox(endIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.endSandbox']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the caller org\'s sandboxes, newest first.  `?project=` and `?status=` narrow it. Only the caller\'s org\'s: the store is keyed on the validated org, so another tenant\'s sandbox is not something this operation can return.
         * @summary Lists the caller org\'s sandboxes, newest first.
         * @param {string} [project] 
         * @param {string} [status] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSandbox(project?: string, status?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<SandboxList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSandbox(project, status, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.getSandbox']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.  An id the caller\'s org does not hold is the same 404 an unknown id gives — the store is keyed on the org, so a cross-tenant id simply is not there.
         * @summary Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.
         * @param {string} id ID is the sandbox to address, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSandboxById(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Sandbox>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSandboxById(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.getSandboxById']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
         * @summary Read a file, or list a directory
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSandboxByIdFs(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSandboxByIdFs(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.getSandboxByIdFs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
         * @summary The screen, as a page
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSandboxByIdScreen(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSandboxByIdScreen(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.getSandboxByIdScreen']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
         * @summary The screen, as a socket
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSandboxByIdScreenWs(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSandboxByIdScreenWs(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.getSandboxByIdScreenWs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
         * @summary The terminal, as a page
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSandboxByIdTerminal(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSandboxByIdTerminal(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.getSandboxByIdTerminal']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
         * @summary The terminal, as a socket
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getSandboxByIdTerminalWs(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getSandboxByIdTerminalWs(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.getSandboxByIdTerminalWs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
         * @summary Lease a sandbox — a real computer — or resume one you hold
         * @param {LeaseIn} leaseIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async leaseSandbox(leaseIn: LeaseIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Leased>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.leaseSandbox(leaseIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.leaseSandbox']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Leases a sandbox — a real computer — for the caller\'s org.  The class decides what it is for and therefore its image, working directory and isolation. A dev or desktop sandbox is SINGLE-ATTACH per project, so asking twice for one project resumes the one that exists rather than paying for a second; an exec sandbox carries no project and is bounded per org instead, refused 429 past the ceiling because the caller\'s correct response is to wait.  Answers 201 with the sandbox as leased, which names the runtime it GOT — not the one that was asked for.
         * @summary Leases a sandbox — a real computer — for the caller\'s org.
         * @param {LeaseIn} leaseIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postSandbox(leaseIn: LeaseIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Sandbox>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postSandbox(leaseIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.postSandbox']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.  Send `argv` — an argument vector cannot be word-split by accident — or `command` for a shell line, which is the only input here that ever reaches a shell. A non-zero exit is a SUCCESSFUL call carrying a failed command: the status is 200 and the exit code is in the answer, because \"the command failed\" and \"the call failed\" are different facts.
         * @summary Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.
         * @param {string} id ID is the sandbox to run in, from the path.
         * @param {ExecRequest} execRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postSandboxByIdExec(id: string, execRequest: ExecRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<ExecResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postSandboxByIdExec(id, execRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.postSandboxByIdExec']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
         * @summary Write a file
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postSandboxByIdFs(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postSandboxByIdFs(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.postSandboxByIdFs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Mints a short-lived grant to open the screen of a desktop sandbox. Same properties as the terminal ticket, for the other door.
         * @summary Mints a short-lived grant to open the screen of a desktop sandbox.
         * @param {string} id ID is the sandbox to address, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postSandboxByIdScreenTicket(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<TicketGrant>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postSandboxByIdScreenTicket(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.postSandboxByIdScreenTicket']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Mints a short-lived grant to open a terminal on a sandbox.  The ticket travels in the query string of the URL it answers with, because a browser cannot set an Authorization header on a WebSocket handshake. It is single-purpose and short-lived for exactly that reason. A sandbox that is not running is 409 rather than a ticket that cannot be used.
         * @summary Mints a short-lived grant to open a terminal on a sandbox.
         * @param {string} id ID is the sandbox to address, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postSandboxByIdTerminalTicket(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<TicketGrant>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postSandboxByIdTerminalTicket(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.postSandboxByIdTerminalTicket']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
         * @summary Read a file from a sandbox you hold
         * @param {PathIn} pathIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async readSandboxFile(pathIn: PathIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Blob>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.readSandboxFile(pathIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.readSandboxFile']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
         * @summary Run a command in a sandbox you hold and read its output
         * @param {RunIn} runIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async runInSandbox(runIn: RunIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Ran>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.runInSandbox(runIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.runInSandbox']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
         * @summary Stop what a sandbox is running, and keep the sandbox
         * @param {StopIn} stopIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async stopRun(stopIn: StopIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Stopped>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.stopRun(stopIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.stopRun']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
         * @summary Write a file into a sandbox you hold
         * @param {WriteIn} writeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async writeSandboxFile(writeIn: WriteIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Wrote>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.writeSandboxFile(writeIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['SandboxApi.writeSandboxFile']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * SandboxApi - factory interface
 * @export
 */
export const SandboxApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = SandboxApiFp(configuration)
    return {
        /**
         * Ends a sandbox and releases the compute behind it. Answers 204.  ENDING IS NOT STOPPING. This releases the resource: the pod goes and anything only inside it goes with it. To end what a sandbox is RUNNING while keeping the sandbox — the checkout, the logs, the half-written file — the verb is POST /v1/sandbox/stop.  `?purge=1` additionally removes the record, so the sandbox stops being listed at all rather than being listed as ended.
         * @summary Ends a sandbox and releases the compute behind it.
         * @param {SandboxApiDeleteSandboxByIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteSandboxById(requestParameters: SandboxApiDeleteSandboxByIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteSandboxById(requestParameters.id, requestParameters.purge, options).then((request) => request(axios, basePath));
        },
        /**
         * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
         * @summary End a sandbox and release it
         * @param {SandboxApiEndSandboxRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        endSandbox(requestParameters: SandboxApiEndSandboxRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.endSandbox(requestParameters.endIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the caller org\'s sandboxes, newest first.  `?project=` and `?status=` narrow it. Only the caller\'s org\'s: the store is keyed on the validated org, so another tenant\'s sandbox is not something this operation can return.
         * @summary Lists the caller org\'s sandboxes, newest first.
         * @param {SandboxApiGetSandboxRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandbox(requestParameters: SandboxApiGetSandboxRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<SandboxList> {
            return localVarFp.getSandbox(requestParameters.project, requestParameters.status, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.  An id the caller\'s org does not hold is the same 404 an unknown id gives — the store is keyed on the org, so a cross-tenant id simply is not there.
         * @summary Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.
         * @param {SandboxApiGetSandboxByIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxById(requestParameters: SandboxApiGetSandboxByIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<Sandbox> {
            return localVarFp.getSandboxById(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
         * @summary Read a file, or list a directory
         * @param {SandboxApiGetSandboxByIdFsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdFs(requestParameters: SandboxApiGetSandboxByIdFsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getSandboxByIdFs(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
         * @summary The screen, as a page
         * @param {SandboxApiGetSandboxByIdScreenRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdScreen(requestParameters: SandboxApiGetSandboxByIdScreenRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getSandboxByIdScreen(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
         * @summary The screen, as a socket
         * @param {SandboxApiGetSandboxByIdScreenWsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdScreenWs(requestParameters: SandboxApiGetSandboxByIdScreenWsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getSandboxByIdScreenWs(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
         * @summary The terminal, as a page
         * @param {SandboxApiGetSandboxByIdTerminalRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdTerminal(requestParameters: SandboxApiGetSandboxByIdTerminalRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getSandboxByIdTerminal(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
         * @summary The terminal, as a socket
         * @param {SandboxApiGetSandboxByIdTerminalWsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getSandboxByIdTerminalWs(requestParameters: SandboxApiGetSandboxByIdTerminalWsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getSandboxByIdTerminalWs(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
         * @summary Lease a sandbox — a real computer — or resume one you hold
         * @param {SandboxApiLeaseSandboxRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        leaseSandbox(requestParameters: SandboxApiLeaseSandboxRequest, options?: RawAxiosRequestConfig): AxiosPromise<Leased> {
            return localVarFp.leaseSandbox(requestParameters.leaseIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Leases a sandbox — a real computer — for the caller\'s org.  The class decides what it is for and therefore its image, working directory and isolation. A dev or desktop sandbox is SINGLE-ATTACH per project, so asking twice for one project resumes the one that exists rather than paying for a second; an exec sandbox carries no project and is bounded per org instead, refused 429 past the ceiling because the caller\'s correct response is to wait.  Answers 201 with the sandbox as leased, which names the runtime it GOT — not the one that was asked for.
         * @summary Leases a sandbox — a real computer — for the caller\'s org.
         * @param {SandboxApiPostSandboxRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandbox(requestParameters: SandboxApiPostSandboxRequest, options?: RawAxiosRequestConfig): AxiosPromise<Sandbox> {
            return localVarFp.postSandbox(requestParameters.leaseIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.  Send `argv` — an argument vector cannot be word-split by accident — or `command` for a shell line, which is the only input here that ever reaches a shell. A non-zero exit is a SUCCESSFUL call carrying a failed command: the status is 200 and the exit code is in the answer, because \"the command failed\" and \"the call failed\" are different facts.
         * @summary Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.
         * @param {SandboxApiPostSandboxByIdExecRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdExec(requestParameters: SandboxApiPostSandboxByIdExecRequest, options?: RawAxiosRequestConfig): AxiosPromise<ExecResult> {
            return localVarFp.postSandboxByIdExec(requestParameters.id, requestParameters.execRequest, options).then((request) => request(axios, basePath));
        },
        /**
         * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
         * @summary Write a file
         * @param {SandboxApiPostSandboxByIdFsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdFs(requestParameters: SandboxApiPostSandboxByIdFsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postSandboxByIdFs(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Mints a short-lived grant to open the screen of a desktop sandbox. Same properties as the terminal ticket, for the other door.
         * @summary Mints a short-lived grant to open the screen of a desktop sandbox.
         * @param {SandboxApiPostSandboxByIdScreenTicketRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdScreenTicket(requestParameters: SandboxApiPostSandboxByIdScreenTicketRequest, options?: RawAxiosRequestConfig): AxiosPromise<TicketGrant> {
            return localVarFp.postSandboxByIdScreenTicket(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Mints a short-lived grant to open a terminal on a sandbox.  The ticket travels in the query string of the URL it answers with, because a browser cannot set an Authorization header on a WebSocket handshake. It is single-purpose and short-lived for exactly that reason. A sandbox that is not running is 409 rather than a ticket that cannot be used.
         * @summary Mints a short-lived grant to open a terminal on a sandbox.
         * @param {SandboxApiPostSandboxByIdTerminalTicketRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postSandboxByIdTerminalTicket(requestParameters: SandboxApiPostSandboxByIdTerminalTicketRequest, options?: RawAxiosRequestConfig): AxiosPromise<TicketGrant> {
            return localVarFp.postSandboxByIdTerminalTicket(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
         * @summary Read a file from a sandbox you hold
         * @param {SandboxApiReadSandboxFileRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        readSandboxFile(requestParameters: SandboxApiReadSandboxFileRequest, options?: RawAxiosRequestConfig): AxiosPromise<Blob> {
            return localVarFp.readSandboxFile(requestParameters.pathIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
         * @summary Run a command in a sandbox you hold and read its output
         * @param {SandboxApiRunInSandboxRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        runInSandbox(requestParameters: SandboxApiRunInSandboxRequest, options?: RawAxiosRequestConfig): AxiosPromise<Ran> {
            return localVarFp.runInSandbox(requestParameters.runIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
         * @summary Stop what a sandbox is running, and keep the sandbox
         * @param {SandboxApiStopRunRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        stopRun(requestParameters: SandboxApiStopRunRequest, options?: RawAxiosRequestConfig): AxiosPromise<Stopped> {
            return localVarFp.stopRun(requestParameters.stopIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
         * @summary Write a file into a sandbox you hold
         * @param {SandboxApiWriteSandboxFileRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        writeSandboxFile(requestParameters: SandboxApiWriteSandboxFileRequest, options?: RawAxiosRequestConfig): AxiosPromise<Wrote> {
            return localVarFp.writeSandboxFile(requestParameters.writeIn, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteSandboxById operation in SandboxApi.
 * @export
 * @interface SandboxApiDeleteSandboxByIdRequest
 */
export interface SandboxApiDeleteSandboxByIdRequest {
    /**
     * ID is the sandbox to end, from the path.
     * @type {string}
     * @memberof SandboxApiDeleteSandboxById
     */
    readonly id: string

    /**
     * 
     * @type {string}
     * @memberof SandboxApiDeleteSandboxById
     */
    readonly purge?: string
}

/**
 * Request parameters for endSandbox operation in SandboxApi.
 * @export
 * @interface SandboxApiEndSandboxRequest
 */
export interface SandboxApiEndSandboxRequest {
    /**
     * 
     * @type {EndIn}
     * @memberof SandboxApiEndSandbox
     */
    readonly endIn: EndIn
}

/**
 * Request parameters for getSandbox operation in SandboxApi.
 * @export
 * @interface SandboxApiGetSandboxRequest
 */
export interface SandboxApiGetSandboxRequest {
    /**
     * 
     * @type {string}
     * @memberof SandboxApiGetSandbox
     */
    readonly project?: string

    /**
     * 
     * @type {string}
     * @memberof SandboxApiGetSandbox
     */
    readonly status?: string
}

/**
 * Request parameters for getSandboxById operation in SandboxApi.
 * @export
 * @interface SandboxApiGetSandboxByIdRequest
 */
export interface SandboxApiGetSandboxByIdRequest {
    /**
     * ID is the sandbox to address, from the path.
     * @type {string}
     * @memberof SandboxApiGetSandboxById
     */
    readonly id: string
}

/**
 * Request parameters for getSandboxByIdFs operation in SandboxApi.
 * @export
 * @interface SandboxApiGetSandboxByIdFsRequest
 */
export interface SandboxApiGetSandboxByIdFsRequest {
    /**
     * 
     * @type {string}
     * @memberof SandboxApiGetSandboxByIdFs
     */
    readonly id: string
}

/**
 * Request parameters for getSandboxByIdScreen operation in SandboxApi.
 * @export
 * @interface SandboxApiGetSandboxByIdScreenRequest
 */
export interface SandboxApiGetSandboxByIdScreenRequest {
    /**
     * 
     * @type {string}
     * @memberof SandboxApiGetSandboxByIdScreen
     */
    readonly id: string
}

/**
 * Request parameters for getSandboxByIdScreenWs operation in SandboxApi.
 * @export
 * @interface SandboxApiGetSandboxByIdScreenWsRequest
 */
export interface SandboxApiGetSandboxByIdScreenWsRequest {
    /**
     * 
     * @type {string}
     * @memberof SandboxApiGetSandboxByIdScreenWs
     */
    readonly id: string
}

/**
 * Request parameters for getSandboxByIdTerminal operation in SandboxApi.
 * @export
 * @interface SandboxApiGetSandboxByIdTerminalRequest
 */
export interface SandboxApiGetSandboxByIdTerminalRequest {
    /**
     * 
     * @type {string}
     * @memberof SandboxApiGetSandboxByIdTerminal
     */
    readonly id: string
}

/**
 * Request parameters for getSandboxByIdTerminalWs operation in SandboxApi.
 * @export
 * @interface SandboxApiGetSandboxByIdTerminalWsRequest
 */
export interface SandboxApiGetSandboxByIdTerminalWsRequest {
    /**
     * 
     * @type {string}
     * @memberof SandboxApiGetSandboxByIdTerminalWs
     */
    readonly id: string
}

/**
 * Request parameters for leaseSandbox operation in SandboxApi.
 * @export
 * @interface SandboxApiLeaseSandboxRequest
 */
export interface SandboxApiLeaseSandboxRequest {
    /**
     * 
     * @type {LeaseIn}
     * @memberof SandboxApiLeaseSandbox
     */
    readonly leaseIn: LeaseIn
}

/**
 * Request parameters for postSandbox operation in SandboxApi.
 * @export
 * @interface SandboxApiPostSandboxRequest
 */
export interface SandboxApiPostSandboxRequest {
    /**
     * 
     * @type {LeaseIn}
     * @memberof SandboxApiPostSandbox
     */
    readonly leaseIn: LeaseIn
}

/**
 * Request parameters for postSandboxByIdExec operation in SandboxApi.
 * @export
 * @interface SandboxApiPostSandboxByIdExecRequest
 */
export interface SandboxApiPostSandboxByIdExecRequest {
    /**
     * ID is the sandbox to run in, from the path.
     * @type {string}
     * @memberof SandboxApiPostSandboxByIdExec
     */
    readonly id: string

    /**
     * 
     * @type {ExecRequest}
     * @memberof SandboxApiPostSandboxByIdExec
     */
    readonly execRequest: ExecRequest
}

/**
 * Request parameters for postSandboxByIdFs operation in SandboxApi.
 * @export
 * @interface SandboxApiPostSandboxByIdFsRequest
 */
export interface SandboxApiPostSandboxByIdFsRequest {
    /**
     * 
     * @type {string}
     * @memberof SandboxApiPostSandboxByIdFs
     */
    readonly id: string
}

/**
 * Request parameters for postSandboxByIdScreenTicket operation in SandboxApi.
 * @export
 * @interface SandboxApiPostSandboxByIdScreenTicketRequest
 */
export interface SandboxApiPostSandboxByIdScreenTicketRequest {
    /**
     * ID is the sandbox to address, from the path.
     * @type {string}
     * @memberof SandboxApiPostSandboxByIdScreenTicket
     */
    readonly id: string
}

/**
 * Request parameters for postSandboxByIdTerminalTicket operation in SandboxApi.
 * @export
 * @interface SandboxApiPostSandboxByIdTerminalTicketRequest
 */
export interface SandboxApiPostSandboxByIdTerminalTicketRequest {
    /**
     * ID is the sandbox to address, from the path.
     * @type {string}
     * @memberof SandboxApiPostSandboxByIdTerminalTicket
     */
    readonly id: string
}

/**
 * Request parameters for readSandboxFile operation in SandboxApi.
 * @export
 * @interface SandboxApiReadSandboxFileRequest
 */
export interface SandboxApiReadSandboxFileRequest {
    /**
     * 
     * @type {PathIn}
     * @memberof SandboxApiReadSandboxFile
     */
    readonly pathIn: PathIn
}

/**
 * Request parameters for runInSandbox operation in SandboxApi.
 * @export
 * @interface SandboxApiRunInSandboxRequest
 */
export interface SandboxApiRunInSandboxRequest {
    /**
     * 
     * @type {RunIn}
     * @memberof SandboxApiRunInSandbox
     */
    readonly runIn: RunIn
}

/**
 * Request parameters for stopRun operation in SandboxApi.
 * @export
 * @interface SandboxApiStopRunRequest
 */
export interface SandboxApiStopRunRequest {
    /**
     * 
     * @type {StopIn}
     * @memberof SandboxApiStopRun
     */
    readonly stopIn: StopIn
}

/**
 * Request parameters for writeSandboxFile operation in SandboxApi.
 * @export
 * @interface SandboxApiWriteSandboxFileRequest
 */
export interface SandboxApiWriteSandboxFileRequest {
    /**
     * 
     * @type {WriteIn}
     * @memberof SandboxApiWriteSandboxFile
     */
    readonly writeIn: WriteIn
}

/**
 * SandboxApi - object-oriented interface
 * @export
 * @class SandboxApi
 * @extends {BaseAPI}
 */
export class SandboxApi extends BaseAPI {
    /**
     * Ends a sandbox and releases the compute behind it. Answers 204.  ENDING IS NOT STOPPING. This releases the resource: the pod goes and anything only inside it goes with it. To end what a sandbox is RUNNING while keeping the sandbox — the checkout, the logs, the half-written file — the verb is POST /v1/sandbox/stop.  `?purge=1` additionally removes the record, so the sandbox stops being listed at all rather than being listed as ended.
     * @summary Ends a sandbox and releases the compute behind it.
     * @param {SandboxApiDeleteSandboxByIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public deleteSandboxById(requestParameters: SandboxApiDeleteSandboxByIdRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).deleteSandboxById(requestParameters.id, requestParameters.purge, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Ends the caller\'s sandbox lease: the pod goes, and the volume goes only when the caller asked for that too.
     * @summary End a sandbox and release it
     * @param {SandboxApiEndSandboxRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public endSandbox(requestParameters: SandboxApiEndSandboxRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).endSandbox(requestParameters.endIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the caller org\'s sandboxes, newest first.  `?project=` and `?status=` narrow it. Only the caller\'s org\'s: the store is keyed on the validated org, so another tenant\'s sandbox is not something this operation can return.
     * @summary Lists the caller org\'s sandboxes, newest first.
     * @param {SandboxApiGetSandboxRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public getSandbox(requestParameters: SandboxApiGetSandboxRequest = {}, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).getSandbox(requestParameters.project, requestParameters.status, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.  An id the caller\'s org does not hold is the same 404 an unknown id gives — the store is keyed on the org, so a cross-tenant id simply is not there.
     * @summary Returns one sandbox: its class, project, image, the runtime it was given, its status and when its lease ends.
     * @param {SandboxApiGetSandboxByIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public getSandboxById(requestParameters: SandboxApiGetSandboxByIdRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).getSandboxById(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads one file from the sandbox\'s project directory as text, or lists the entries when the path names a directory. Paths resolve under the project root and a path that climbs out is refused rather than rewritten.
     * @summary Read a file, or list a directory
     * @param {SandboxApiGetSandboxByIdFsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public getSandboxByIdFs(requestParameters: SandboxApiGetSandboxByIdFsRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).getSandboxByIdFs(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox\'s display. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands\' hosts and nothing further.  It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
     * @summary The screen, as a page
     * @param {SandboxApiGetSandboxByIdScreenRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public getSandboxByIdScreen(requestParameters: SandboxApiGetSandboxByIdScreenRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).getSandboxByIdScreen(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox\'s display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller\'s client and the server inside the pod.  THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.  The window size is ignored. A browser pane is not the X server\'s geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
     * @summary The screen, as a socket
     * @param {SandboxApiGetSandboxByIdScreenWsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public getSandboxByIdScreenWs(requestParameters: SandboxApiGetSandboxByIdScreenWsRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).getSandboxByIdScreenWs(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.  `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.  When the terminal is up it posts `{source:\"hanzo-term\", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands\' hosts and nothing further.
     * @summary The terminal, as a page
     * @param {SandboxApiGetSandboxByIdTerminalRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public getSandboxByIdTerminal(requestParameters: SandboxApiGetSandboxByIdTerminalRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).getSandboxByIdTerminal(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.  THE WIRE. A text frame is stdin, unless it is the one control object `{\"resize\":{\"cols\":N,\"rows\":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.  `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.  The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
     * @summary The terminal, as a socket
     * @param {SandboxApiGetSandboxByIdTerminalWsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public getSandboxByIdTerminalWs(requestParameters: SandboxApiGetSandboxByIdTerminalWsRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).getSandboxByIdTerminalWs(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Leases the caller\'s sandbox, or returns the one it named if that lease is still running.  What comes back is a real computer: a pod under a runtime boundary with a toolchain already in it, its own filesystem, and a lease that ends it. Every other op here acts on the one this returns.
     * @summary Lease a sandbox — a real computer — or resume one you hold
     * @param {SandboxApiLeaseSandboxRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public leaseSandbox(requestParameters: SandboxApiLeaseSandboxRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).leaseSandbox(requestParameters.leaseIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Leases a sandbox — a real computer — for the caller\'s org.  The class decides what it is for and therefore its image, working directory and isolation. A dev or desktop sandbox is SINGLE-ATTACH per project, so asking twice for one project resumes the one that exists rather than paying for a second; an exec sandbox carries no project and is bounded per org instead, refused 429 past the ceiling because the caller\'s correct response is to wait.  Answers 201 with the sandbox as leased, which names the runtime it GOT — not the one that was asked for.
     * @summary Leases a sandbox — a real computer — for the caller\'s org.
     * @param {SandboxApiPostSandboxRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public postSandbox(requestParameters: SandboxApiPostSandboxRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).postSandbox(requestParameters.leaseIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.  Send `argv` — an argument vector cannot be word-split by accident — or `command` for a shell line, which is the only input here that ever reaches a shell. A non-zero exit is a SUCCESSFUL call carrying a failed command: the status is 200 and the exit code is in the answer, because \"the command failed\" and \"the call failed\" are different facts.
     * @summary Runs one command in a sandbox the caller holds and answers with its exit code, stdout and stderr.
     * @param {SandboxApiPostSandboxByIdExecRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public postSandboxByIdExec(requestParameters: SandboxApiPostSandboxByIdExecRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).postSandboxByIdExec(requestParameters.id, requestParameters.execRequest, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Writes the request body to one file in the sandbox\'s project directory, creating parent directories. Same confinement as the read above.
     * @summary Write a file
     * @param {SandboxApiPostSandboxByIdFsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public postSandboxByIdFs(requestParameters: SandboxApiPostSandboxByIdFsRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).postSandboxByIdFs(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Mints a short-lived grant to open the screen of a desktop sandbox. Same properties as the terminal ticket, for the other door.
     * @summary Mints a short-lived grant to open the screen of a desktop sandbox.
     * @param {SandboxApiPostSandboxByIdScreenTicketRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public postSandboxByIdScreenTicket(requestParameters: SandboxApiPostSandboxByIdScreenTicketRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).postSandboxByIdScreenTicket(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Mints a short-lived grant to open a terminal on a sandbox.  The ticket travels in the query string of the URL it answers with, because a browser cannot set an Authorization header on a WebSocket handshake. It is single-purpose and short-lived for exactly that reason. A sandbox that is not running is 409 rather than a ticket that cannot be used.
     * @summary Mints a short-lived grant to open a terminal on a sandbox.
     * @param {SandboxApiPostSandboxByIdTerminalTicketRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public postSandboxByIdTerminalTicket(requestParameters: SandboxApiPostSandboxByIdTerminalTicketRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).postSandboxByIdTerminalTicket(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads one path in the caller\'s sandbox: a file\'s bytes, or a directory\'s entries when the path names one.
     * @summary Read a file from a sandbox you hold
     * @param {SandboxApiReadSandboxFileRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public readSandboxFile(requestParameters: SandboxApiReadSandboxFileRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).readSandboxFile(requestParameters.pathIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Runs one command inside the caller\'s sandbox and answers its exit code, stdout and stderr. A non-zero exit is a successful call carrying a failed program, so it comes back as data and not as an error.  Name a `session` and the command NARRATES INTO IT: its output is appended to that session\'s live log as the program produces it, so anything watching the session — GET /v1/agents/sessions/stream, scoped to one run with ?root= — watches the work happen rather than waiting for the verdict. Without it the call is what it always was: silent until it returns, which for an agentic run is twenty-five minutes of blank screen.  The session is named; the TENANT is not. It is the org the caller already proved, so a session belonging to somebody else is absent from the org this call acts for and the append is refused there.
     * @summary Run a command in a sandbox you hold and read its output
     * @param {SandboxApiRunInSandboxRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public runInSandbox(requestParameters: SandboxApiRunInSandboxRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).runInSandbox(requestParameters.runIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Interrupts whatever the caller\'s sandbox is running and answers how many commands it ended. The sandbox stays leased — stop ends the WORK, end ends the RESOURCE — so whoever stopped a run can still read what it left behind.
     * @summary Stop what a sandbox is running, and keep the sandbox
     * @param {SandboxApiStopRunRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public stopRun(requestParameters: SandboxApiStopRunRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).stopRun(requestParameters.stopIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Writes bytes to one path in the caller\'s sandbox, creating parents, and answers the resolved path.
     * @summary Write a file into a sandbox you hold
     * @param {SandboxApiWriteSandboxFileRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof SandboxApi
     */
    public writeSandboxFile(requestParameters: SandboxApiWriteSandboxFileRequest, options?: RawAxiosRequestConfig) {
        return SandboxApiFp(this.configuration).writeSandboxFile(requestParameters.writeIn, options).then((request) => request(this.axios, this.basePath));
    }
}

