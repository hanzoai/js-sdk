/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudActivationReq } from '../models';
// @ts-ignore
import type { CloudActivationSet } from '../models';
// @ts-ignore
import type { CloudCurateReq } from '../models';
// @ts-ignore
import type { CloudMCPListing } from '../models';
// @ts-ignore
import type { CloudMcpCatalog } from '../models';
// @ts-ignore
import type { CloudMcpCatalogSync } from '../models';
// @ts-ignore
import type { CloudToolCall } from '../models';
// @ts-ignore
import type { CloudToolList } from '../models';
// @ts-ignore
import type { CloudToolResult } from '../models';
/**
 * ToolsApi - axios parameter creator
 * @export
 */
export const ToolsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated. This is the discovery surface: one flat set of names spanning connector actions, user functions, zap-service routes, agents, skills and the org\'s own external MCP servers, deduplicated by name so the highest-precedence source wins a collision. It lists; it does not call — dispatch is POST /v1/tools/call.
         * @summary ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated.
         * @param {string} [source] Source keeps only tools from one source — connector, function, zap-service, agent, skill or mcp. Empty keeps every source.
         * @param {string} [activated] Activated keeps only the tools activated for the caller\&#39;s org and project, and only when it is exactly the string \&quot;true\&quot;.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Tools: async (source?: string, activated?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/tools`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (source !== undefined) {
                localVarQueryParameter['source'] = source;
            }

            if (activated !== undefined) {
                localVarQueryParameter['activated'] = activated;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * GetActivation reports which tools are switched on for the caller\'s org and project. Activation is what makes a tool dispatchable and what makes it visible to an agent, so this is the set the MCP tool list is drawn from — every other tool in the registry is discoverable but refused at call time.
         * @summary GetActivation reports which tools are switched on for the caller\'s org and project.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ToolsActivation: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/tools/activation`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.  This is the SHELF an org picks from. A listing with a streamable-http endpoint can be enabled as-is — POST /v1/mcp/servers with its id — and its tools then join the org\'s tool plane and the fleet\'s MCP door. A listing that only ships a stdio package needs a process to run it, which is why the transports are on every entry rather than implied.  Hidden entries are absent: they are the ones we took off the shelf. A platform SuperAdmin sees them, because the same query answers \"what is on the shelf\" and \"what is in the catalog\" and two queries would drift apart.  It is PAGED — 50 by default, 200 at most. The public registry publishes tens of thousands of servers, so an unbounded answer is a twenty-megabyte response and a storefront that renders in a minute. total is the whole match, not the page.
         * @summary ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.
         * @param {string} [q] Q matches the name, title or description, case-insensitively.
         * @param {string} [featured] Featured keeps only the listings we put on the front of the shelf, and only when it is exactly the string \&quot;true\&quot;.
         * @param {string} [official] Official keeps only the vendors\&#39; OWN servers — not third-party copies of them — and only when it is exactly the string \&quot;true\&quot;.
         * @param {number} [limit] Limit bounds the page: default 50, maximum 200. A value that is not a positive integer reads as the default.
         * @param {number} [offset] Offset skips that many listings.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ToolsCatalog: async (q?: string, featured?: string, official?: string, limit?: number, offset?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/tools/catalog`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (q !== undefined) {
                localVarQueryParameter['q'] = q;
            }

            if (featured !== undefined) {
                localVarQueryParameter['featured'] = featured;
            }

            if (official !== undefined) {
                localVarQueryParameter['official'] = official;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }

            if (offset !== undefined) {
                localVarQueryParameter['offset'] = offset;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint. It is what a branding page renders, and what tells a caller whether the listing can be enabled here and now (a streamable-http remote) or needs somewhere to run first (a stdio package).  A HIDDEN listing is not served to an org — a shelf that renders what it does not list would be a way around the shelf — but is served to a SuperAdmin, who is the one deciding whether to put it back.
         * @summary GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint.
         * @param {string} id ID is the listing, from the path. It is the publisher\&#39;s reverse-DNS name with its one slash written as an underscore — \&quot;com.stripe_mcp\&quot;.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ToolsCatalogId: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudGetV1ToolsCatalogId', 'id', id)
            const localVarPath = `/v1/tools/catalog/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing. SuperAdmin only; every other caller is refused.  Curation is the half of a catalog row a sync cannot write, and this is the only thing that writes it. The upstream half is never editable here: a description that disagreed with the publisher\'s would be a fork of their listing, and the next sync would silently undo it.
         * @summary CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing.
         * @param {string} id ID is the listing to curate, from the path.
         * @param {CloudCurateReq} cloudCurateReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1ToolsCatalogId: async (id: string, cloudCurateReq: CloudCurateReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudPatchV1ToolsCatalogId', 'id', id)
            // verify required parameter 'cloudCurateReq' is not null or undefined
            assertParamExists('cloudPatchV1ToolsCatalogId', 'cloudCurateReq', cloudCurateReq)
            const localVarPath = `/v1/tools/catalog/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudCurateReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * CallTool runs one of the caller\'s activated tools and answers with its output.  This is the door onto the tool plane\'s DYNAMIC half — the half no build-time catalogue can hold, because it is per-tenant: an org\'s connected connector actions, its authored skills, its agents and functions, and the tools of every external MCP server it registered. A tool\'s existence, its price and its activation are all rows, not code, so they cannot be known until the caller is.  One policy, the registry\'s: resolve by precedence, refuse an unactivated tool 403, settle a priced one through the x402 seam or fail closed 402, then dispatch to the winning source bound to the caller\'s own (org, project). One metered unit, one audit record. A caller can only ever dispatch its own tools.  Discovery is GET /v1/tools — ?activated=true for the callable set.
         * @summary CallTool runs one of the caller\'s activated tools and answers with its output.
         * @param {CloudToolCall} cloudToolCall 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ToolsCall: async (cloudToolCall: CloudToolCall, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudToolCall' is not null or undefined
            assertParamExists('cloudPostV1ToolsCall', 'cloudToolCall', cloudToolCall)
            const localVarPath = `/v1/tools/call`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudToolCall, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed. SuperAdmin only; every other caller is refused.  It is IDEMPOTENT: a listing is keyed by the publisher\'s own reverse-DNS name, so a second pass over an unchanged registry rewrites the same rows and reports added=0, updated=0. It never deletes — a listing that vanishes upstream may be one an org has already enabled, and dropping its description would not drop its server. And it never touches CURATION: hidden, featured, an admin-set official and a logo survive every sync, because the write does not name those columns.
         * @summary SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ToolsCatalogSync: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/tools/catalog/sync`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set. It is the ONE write path that turns skills, plugins and connectors into callable tools — an unactivated tool is listed by discovery but refused 403 at dispatch. Activate is applied before Deactivate, so a name in both lists ends up off. More than 256 toggles in one request is refused 413.
         * @summary PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set.
         * @param {CloudActivationReq} cloudActivationReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1ToolsActivation: async (cloudActivationReq: CloudActivationReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudActivationReq' is not null or undefined
            assertParamExists('cloudPutV1ToolsActivation', 'cloudActivationReq', cloudActivationReq)
            const localVarPath = `/v1/tools/activation`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudActivationReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * ToolsApi - functional programming interface
 * @export
 */
export const ToolsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = ToolsApiAxiosParamCreator(configuration)
    return {
        /**
         * ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated. This is the discovery surface: one flat set of names spanning connector actions, user functions, zap-service routes, agents, skills and the org\'s own external MCP servers, deduplicated by name so the highest-precedence source wins a collision. It lists; it does not call — dispatch is POST /v1/tools/call.
         * @summary ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated.
         * @param {string} [source] Source keeps only tools from one source — connector, function, zap-service, agent, skill or mcp. Empty keeps every source.
         * @param {string} [activated] Activated keeps only the tools activated for the caller\&#39;s org and project, and only when it is exactly the string \&quot;true\&quot;.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Tools(source?: string, activated?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudToolList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Tools(source, activated, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudGetV1Tools']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * GetActivation reports which tools are switched on for the caller\'s org and project. Activation is what makes a tool dispatchable and what makes it visible to an agent, so this is the set the MCP tool list is drawn from — every other tool in the registry is discoverable but refused at call time.
         * @summary GetActivation reports which tools are switched on for the caller\'s org and project.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ToolsActivation(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudActivationSet>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ToolsActivation(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudGetV1ToolsActivation']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.  This is the SHELF an org picks from. A listing with a streamable-http endpoint can be enabled as-is — POST /v1/mcp/servers with its id — and its tools then join the org\'s tool plane and the fleet\'s MCP door. A listing that only ships a stdio package needs a process to run it, which is why the transports are on every entry rather than implied.  Hidden entries are absent: they are the ones we took off the shelf. A platform SuperAdmin sees them, because the same query answers \"what is on the shelf\" and \"what is in the catalog\" and two queries would drift apart.  It is PAGED — 50 by default, 200 at most. The public registry publishes tens of thousands of servers, so an unbounded answer is a twenty-megabyte response and a storefront that renders in a minute. total is the whole match, not the page.
         * @summary ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.
         * @param {string} [q] Q matches the name, title or description, case-insensitively.
         * @param {string} [featured] Featured keeps only the listings we put on the front of the shelf, and only when it is exactly the string \&quot;true\&quot;.
         * @param {string} [official] Official keeps only the vendors\&#39; OWN servers — not third-party copies of them — and only when it is exactly the string \&quot;true\&quot;.
         * @param {number} [limit] Limit bounds the page: default 50, maximum 200. A value that is not a positive integer reads as the default.
         * @param {number} [offset] Offset skips that many listings.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ToolsCatalog(q?: string, featured?: string, official?: string, limit?: number, offset?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMcpCatalog>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ToolsCatalog(q, featured, official, limit, offset, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudGetV1ToolsCatalog']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint. It is what a branding page renders, and what tells a caller whether the listing can be enabled here and now (a streamable-http remote) or needs somewhere to run first (a stdio package).  A HIDDEN listing is not served to an org — a shelf that renders what it does not list would be a way around the shelf — but is served to a SuperAdmin, who is the one deciding whether to put it back.
         * @summary GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint.
         * @param {string} id ID is the listing, from the path. It is the publisher\&#39;s reverse-DNS name with its one slash written as an underscore — \&quot;com.stripe_mcp\&quot;.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1ToolsCatalogId(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMCPListing>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1ToolsCatalogId(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudGetV1ToolsCatalogId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing. SuperAdmin only; every other caller is refused.  Curation is the half of a catalog row a sync cannot write, and this is the only thing that writes it. The upstream half is never editable here: a description that disagreed with the publisher\'s would be a fork of their listing, and the next sync would silently undo it.
         * @summary CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing.
         * @param {string} id ID is the listing to curate, from the path.
         * @param {CloudCurateReq} cloudCurateReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPatchV1ToolsCatalogId(id: string, cloudCurateReq: CloudCurateReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMCPListing>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPatchV1ToolsCatalogId(id, cloudCurateReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudPatchV1ToolsCatalogId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * CallTool runs one of the caller\'s activated tools and answers with its output.  This is the door onto the tool plane\'s DYNAMIC half — the half no build-time catalogue can hold, because it is per-tenant: an org\'s connected connector actions, its authored skills, its agents and functions, and the tools of every external MCP server it registered. A tool\'s existence, its price and its activation are all rows, not code, so they cannot be known until the caller is.  One policy, the registry\'s: resolve by precedence, refuse an unactivated tool 403, settle a priced one through the x402 seam or fail closed 402, then dispatch to the winning source bound to the caller\'s own (org, project). One metered unit, one audit record. A caller can only ever dispatch its own tools.  Discovery is GET /v1/tools — ?activated=true for the callable set.
         * @summary CallTool runs one of the caller\'s activated tools and answers with its output.
         * @param {CloudToolCall} cloudToolCall 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1ToolsCall(cloudToolCall: CloudToolCall, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudToolResult>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1ToolsCall(cloudToolCall, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudPostV1ToolsCall']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed. SuperAdmin only; every other caller is refused.  It is IDEMPOTENT: a listing is keyed by the publisher\'s own reverse-DNS name, so a second pass over an unchanged registry rewrites the same rows and reports added=0, updated=0. It never deletes — a listing that vanishes upstream may be one an org has already enabled, and dropping its description would not drop its server. And it never touches CURATION: hidden, featured, an admin-set official and a logo survive every sync, because the write does not name those columns.
         * @summary SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1ToolsCatalogSync(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMcpCatalogSync>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1ToolsCatalogSync(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudPostV1ToolsCatalogSync']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set. It is the ONE write path that turns skills, plugins and connectors into callable tools — an unactivated tool is listed by discovery but refused 403 at dispatch. Activate is applied before Deactivate, so a name in both lists ends up off. More than 256 toggles in one request is refused 413.
         * @summary PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set.
         * @param {CloudActivationReq} cloudActivationReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1ToolsActivation(cloudActivationReq: CloudActivationReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudActivationSet>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1ToolsActivation(cloudActivationReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['ToolsApi.cloudPutV1ToolsActivation']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * ToolsApi - factory interface
 * @export
 */
export const ToolsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = ToolsApiFp(configuration)
    return {
        /**
         * ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated. This is the discovery surface: one flat set of names spanning connector actions, user functions, zap-service routes, agents, skills and the org\'s own external MCP servers, deduplicated by name so the highest-precedence source wins a collision. It lists; it does not call — dispatch is POST /v1/tools/call.
         * @summary ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated.
         * @param {ToolsApiCloudGetV1ToolsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Tools(requestParameters: ToolsApiCloudGetV1ToolsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudToolList> {
            return localVarFp.cloudGetV1Tools(requestParameters.source, requestParameters.activated, options).then((request) => request(axios, basePath));
        },
        /**
         * GetActivation reports which tools are switched on for the caller\'s org and project. Activation is what makes a tool dispatchable and what makes it visible to an agent, so this is the set the MCP tool list is drawn from — every other tool in the registry is discoverable but refused at call time.
         * @summary GetActivation reports which tools are switched on for the caller\'s org and project.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ToolsActivation(options?: RawAxiosRequestConfig): AxiosPromise<CloudActivationSet> {
            return localVarFp.cloudGetV1ToolsActivation(options).then((request) => request(axios, basePath));
        },
        /**
         * ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.  This is the SHELF an org picks from. A listing with a streamable-http endpoint can be enabled as-is — POST /v1/mcp/servers with its id — and its tools then join the org\'s tool plane and the fleet\'s MCP door. A listing that only ships a stdio package needs a process to run it, which is why the transports are on every entry rather than implied.  Hidden entries are absent: they are the ones we took off the shelf. A platform SuperAdmin sees them, because the same query answers \"what is on the shelf\" and \"what is in the catalog\" and two queries would drift apart.  It is PAGED — 50 by default, 200 at most. The public registry publishes tens of thousands of servers, so an unbounded answer is a twenty-megabyte response and a storefront that renders in a minute. total is the whole match, not the page.
         * @summary ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.
         * @param {ToolsApiCloudGetV1ToolsCatalogRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ToolsCatalog(requestParameters: ToolsApiCloudGetV1ToolsCatalogRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudMcpCatalog> {
            return localVarFp.cloudGetV1ToolsCatalog(requestParameters.q, requestParameters.featured, requestParameters.official, requestParameters.limit, requestParameters.offset, options).then((request) => request(axios, basePath));
        },
        /**
         * GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint. It is what a branding page renders, and what tells a caller whether the listing can be enabled here and now (a streamable-http remote) or needs somewhere to run first (a stdio package).  A HIDDEN listing is not served to an org — a shelf that renders what it does not list would be a way around the shelf — but is served to a SuperAdmin, who is the one deciding whether to put it back.
         * @summary GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint.
         * @param {ToolsApiCloudGetV1ToolsCatalogIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1ToolsCatalogId(requestParameters: ToolsApiCloudGetV1ToolsCatalogIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudMCPListing> {
            return localVarFp.cloudGetV1ToolsCatalogId(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing. SuperAdmin only; every other caller is refused.  Curation is the half of a catalog row a sync cannot write, and this is the only thing that writes it. The upstream half is never editable here: a description that disagreed with the publisher\'s would be a fork of their listing, and the next sync would silently undo it.
         * @summary CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing.
         * @param {ToolsApiCloudPatchV1ToolsCatalogIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1ToolsCatalogId(requestParameters: ToolsApiCloudPatchV1ToolsCatalogIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudMCPListing> {
            return localVarFp.cloudPatchV1ToolsCatalogId(requestParameters.id, requestParameters.cloudCurateReq, options).then((request) => request(axios, basePath));
        },
        /**
         * CallTool runs one of the caller\'s activated tools and answers with its output.  This is the door onto the tool plane\'s DYNAMIC half — the half no build-time catalogue can hold, because it is per-tenant: an org\'s connected connector actions, its authored skills, its agents and functions, and the tools of every external MCP server it registered. A tool\'s existence, its price and its activation are all rows, not code, so they cannot be known until the caller is.  One policy, the registry\'s: resolve by precedence, refuse an unactivated tool 403, settle a priced one through the x402 seam or fail closed 402, then dispatch to the winning source bound to the caller\'s own (org, project). One metered unit, one audit record. A caller can only ever dispatch its own tools.  Discovery is GET /v1/tools — ?activated=true for the callable set.
         * @summary CallTool runs one of the caller\'s activated tools and answers with its output.
         * @param {ToolsApiCloudPostV1ToolsCallRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ToolsCall(requestParameters: ToolsApiCloudPostV1ToolsCallRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudToolResult> {
            return localVarFp.cloudPostV1ToolsCall(requestParameters.cloudToolCall, options).then((request) => request(axios, basePath));
        },
        /**
         * SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed. SuperAdmin only; every other caller is refused.  It is IDEMPOTENT: a listing is keyed by the publisher\'s own reverse-DNS name, so a second pass over an unchanged registry rewrites the same rows and reports added=0, updated=0. It never deletes — a listing that vanishes upstream may be one an org has already enabled, and dropping its description would not drop its server. And it never touches CURATION: hidden, featured, an admin-set official and a logo survive every sync, because the write does not name those columns.
         * @summary SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1ToolsCatalogSync(options?: RawAxiosRequestConfig): AxiosPromise<CloudMcpCatalogSync> {
            return localVarFp.cloudPostV1ToolsCatalogSync(options).then((request) => request(axios, basePath));
        },
        /**
         * PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set. It is the ONE write path that turns skills, plugins and connectors into callable tools — an unactivated tool is listed by discovery but refused 403 at dispatch. Activate is applied before Deactivate, so a name in both lists ends up off. More than 256 toggles in one request is refused 413.
         * @summary PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set.
         * @param {ToolsApiCloudPutV1ToolsActivationRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1ToolsActivation(requestParameters: ToolsApiCloudPutV1ToolsActivationRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudActivationSet> {
            return localVarFp.cloudPutV1ToolsActivation(requestParameters.cloudActivationReq, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudGetV1Tools operation in ToolsApi.
 * @export
 * @interface ToolsApiCloudGetV1ToolsRequest
 */
export interface ToolsApiCloudGetV1ToolsRequest {
    /**
     * Source keeps only tools from one source — connector, function, zap-service, agent, skill or mcp. Empty keeps every source.
     * @type {string}
     * @memberof ToolsApiCloudGetV1Tools
     */
    readonly source?: string

    /**
     * Activated keeps only the tools activated for the caller\&#39;s org and project, and only when it is exactly the string \&quot;true\&quot;.
     * @type {string}
     * @memberof ToolsApiCloudGetV1Tools
     */
    readonly activated?: string
}

/**
 * Request parameters for cloudGetV1ToolsCatalog operation in ToolsApi.
 * @export
 * @interface ToolsApiCloudGetV1ToolsCatalogRequest
 */
export interface ToolsApiCloudGetV1ToolsCatalogRequest {
    /**
     * Q matches the name, title or description, case-insensitively.
     * @type {string}
     * @memberof ToolsApiCloudGetV1ToolsCatalog
     */
    readonly q?: string

    /**
     * Featured keeps only the listings we put on the front of the shelf, and only when it is exactly the string \&quot;true\&quot;.
     * @type {string}
     * @memberof ToolsApiCloudGetV1ToolsCatalog
     */
    readonly featured?: string

    /**
     * Official keeps only the vendors\&#39; OWN servers — not third-party copies of them — and only when it is exactly the string \&quot;true\&quot;.
     * @type {string}
     * @memberof ToolsApiCloudGetV1ToolsCatalog
     */
    readonly official?: string

    /**
     * Limit bounds the page: default 50, maximum 200. A value that is not a positive integer reads as the default.
     * @type {number}
     * @memberof ToolsApiCloudGetV1ToolsCatalog
     */
    readonly limit?: number

    /**
     * Offset skips that many listings.
     * @type {number}
     * @memberof ToolsApiCloudGetV1ToolsCatalog
     */
    readonly offset?: number
}

/**
 * Request parameters for cloudGetV1ToolsCatalogId operation in ToolsApi.
 * @export
 * @interface ToolsApiCloudGetV1ToolsCatalogIdRequest
 */
export interface ToolsApiCloudGetV1ToolsCatalogIdRequest {
    /**
     * ID is the listing, from the path. It is the publisher\&#39;s reverse-DNS name with its one slash written as an underscore — \&quot;com.stripe_mcp\&quot;.
     * @type {string}
     * @memberof ToolsApiCloudGetV1ToolsCatalogId
     */
    readonly id: string
}

/**
 * Request parameters for cloudPatchV1ToolsCatalogId operation in ToolsApi.
 * @export
 * @interface ToolsApiCloudPatchV1ToolsCatalogIdRequest
 */
export interface ToolsApiCloudPatchV1ToolsCatalogIdRequest {
    /**
     * ID is the listing to curate, from the path.
     * @type {string}
     * @memberof ToolsApiCloudPatchV1ToolsCatalogId
     */
    readonly id: string

    /**
     * 
     * @type {CloudCurateReq}
     * @memberof ToolsApiCloudPatchV1ToolsCatalogId
     */
    readonly cloudCurateReq: CloudCurateReq
}

/**
 * Request parameters for cloudPostV1ToolsCall operation in ToolsApi.
 * @export
 * @interface ToolsApiCloudPostV1ToolsCallRequest
 */
export interface ToolsApiCloudPostV1ToolsCallRequest {
    /**
     * 
     * @type {CloudToolCall}
     * @memberof ToolsApiCloudPostV1ToolsCall
     */
    readonly cloudToolCall: CloudToolCall
}

/**
 * Request parameters for cloudPutV1ToolsActivation operation in ToolsApi.
 * @export
 * @interface ToolsApiCloudPutV1ToolsActivationRequest
 */
export interface ToolsApiCloudPutV1ToolsActivationRequest {
    /**
     * 
     * @type {CloudActivationReq}
     * @memberof ToolsApiCloudPutV1ToolsActivation
     */
    readonly cloudActivationReq: CloudActivationReq
}

/**
 * ToolsApi - object-oriented interface
 * @export
 * @class ToolsApi
 * @extends {BaseAPI}
 */
export class ToolsApi extends BaseAPI {
    /**
     * ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated. This is the discovery surface: one flat set of names spanning connector actions, user functions, zap-service routes, agents, skills and the org\'s own external MCP servers, deduplicated by name so the highest-precedence source wins a collision. It lists; it does not call — dispatch is POST /v1/tools/call.
     * @summary ListTools lists every tool the caller\'s org and project can reach, from every source, each flagged with whether it is activated.
     * @param {ToolsApiCloudGetV1ToolsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudGetV1Tools(requestParameters: ToolsApiCloudGetV1ToolsRequest = {}, options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudGetV1Tools(requestParameters.source, requestParameters.activated, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * GetActivation reports which tools are switched on for the caller\'s org and project. Activation is what makes a tool dispatchable and what makes it visible to an agent, so this is the set the MCP tool list is drawn from — every other tool in the registry is discoverable but refused at call time.
     * @summary GetActivation reports which tools are switched on for the caller\'s org and project.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudGetV1ToolsActivation(options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudGetV1ToolsActivation(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.  This is the SHELF an org picks from. A listing with a streamable-http endpoint can be enabled as-is — POST /v1/mcp/servers with its id — and its tools then join the org\'s tool plane and the fleet\'s MCP door. A listing that only ships a stdio package needs a process to run it, which is why the transports are on every entry rather than implied.  Hidden entries are absent: they are the ones we took off the shelf. A platform SuperAdmin sees them, because the same query answers \"what is on the shelf\" and \"what is in the catalog\" and two queries would drift apart.  It is PAGED — 50 by default, 200 at most. The public registry publishes tens of thousands of servers, so an unbounded answer is a twenty-megabyte response and a storefront that renders in a minute. total is the whole match, not the page.
     * @summary ListCatalog lists the MCP servers the public registries publish, as we hold them: our canonical copy of registry.modelcontextprotocol.io, plus what we decided about each entry.
     * @param {ToolsApiCloudGetV1ToolsCatalogRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudGetV1ToolsCatalog(requestParameters: ToolsApiCloudGetV1ToolsCatalogRequest = {}, options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudGetV1ToolsCatalog(requestParameters.q, requestParameters.featured, requestParameters.official, requestParameters.limit, requestParameters.offset, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint. It is what a branding page renders, and what tells a caller whether the listing can be enabled here and now (a streamable-http remote) or needs somewhere to run first (a stdio package).  A HIDDEN listing is not served to an org — a shelf that renders what it does not list would be a way around the shelf — but is served to a SuperAdmin, who is the one deciding whether to put it back.
     * @summary GetListing returns one catalog entry in full: the publisher\'s description, its repository and site, every package form with the runtime that launches it, and every hosted endpoint.
     * @param {ToolsApiCloudGetV1ToolsCatalogIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudGetV1ToolsCatalogId(requestParameters: ToolsApiCloudGetV1ToolsCatalogIdRequest, options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudGetV1ToolsCatalogId(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing. SuperAdmin only; every other caller is refused.  Curation is the half of a catalog row a sync cannot write, and this is the only thing that writes it. The upstream half is never editable here: a description that disagreed with the publisher\'s would be a fork of their listing, and the next sync would silently undo it.
     * @summary CurateListing sets what WE say about one catalog entry — hidden, featured, official, logo — and answers with the stored listing.
     * @param {ToolsApiCloudPatchV1ToolsCatalogIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudPatchV1ToolsCatalogId(requestParameters: ToolsApiCloudPatchV1ToolsCatalogIdRequest, options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudPatchV1ToolsCatalogId(requestParameters.id, requestParameters.cloudCurateReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * CallTool runs one of the caller\'s activated tools and answers with its output.  This is the door onto the tool plane\'s DYNAMIC half — the half no build-time catalogue can hold, because it is per-tenant: an org\'s connected connector actions, its authored skills, its agents and functions, and the tools of every external MCP server it registered. A tool\'s existence, its price and its activation are all rows, not code, so they cannot be known until the caller is.  One policy, the registry\'s: resolve by precedence, refuse an unactivated tool 403, settle a priced one through the x402 seam or fail closed 402, then dispatch to the winning source bound to the caller\'s own (org, project). One metered unit, one audit record. A caller can only ever dispatch its own tools.  Discovery is GET /v1/tools — ?activated=true for the callable set.
     * @summary CallTool runs one of the caller\'s activated tools and answers with its output.
     * @param {ToolsApiCloudPostV1ToolsCallRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudPostV1ToolsCall(requestParameters: ToolsApiCloudPostV1ToolsCallRequest, options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudPostV1ToolsCall(requestParameters.cloudToolCall, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed. SuperAdmin only; every other caller is refused.  It is IDEMPOTENT: a listing is keyed by the publisher\'s own reverse-DNS name, so a second pass over an unchanged registry rewrites the same rows and reports added=0, updated=0. It never deletes — a listing that vanishes upstream may be one an org has already enabled, and dropping its description would not drop its server. And it never touches CURATION: hidden, featured, an admin-set official and a logo survive every sync, because the write does not name those columns.
     * @summary SyncCatalog pulls the public MCP registry into our canonical copy and reports what changed.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudPostV1ToolsCatalogSync(options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudPostV1ToolsCatalogSync(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set. It is the ONE write path that turns skills, plugins and connectors into callable tools — an unactivated tool is listed by discovery but refused 403 at dispatch. Activate is applied before Deactivate, so a name in both lists ends up off. More than 256 toggles in one request is refused 413.
     * @summary PutActivation switches tools on and off for the caller\'s org and project, and answers with the resulting activated set.
     * @param {ToolsApiCloudPutV1ToolsActivationRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof ToolsApi
     */
    public cloudPutV1ToolsActivation(requestParameters: ToolsApiCloudPutV1ToolsActivationRequest, options?: RawAxiosRequestConfig) {
        return ToolsApiFp(this.configuration).cloudPutV1ToolsActivation(requestParameters.cloudActivationReq, options).then((request) => request(this.axios, this.basePath));
    }
}

