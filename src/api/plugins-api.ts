/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudAuthoredPluginList } from '../models';
// @ts-ignore
import type { CloudBuildOut } from '../models';
// @ts-ignore
import type { CloudBuildRequest } from '../models';
// @ts-ignore
import type { CloudPluginDeleted } from '../models';
// @ts-ignore
import type { CloudPluginMountList } from '../models';
/**
 * PluginsApi - axios parameter creator
 * @export
 */
export const PluginsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it. Scoped to the caller\'s org, so an id belonging to another tenant answers 404 and is not deleted.
         * @summary DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it.
         * @param {string} id ID is the plugin to remove, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1PluginsAuthoredId: async (id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudDeleteV1PluginsAuthoredId', 'id', id)
            const localVarPath = `/v1/plugins/authored/{id}`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on. A plugin here is MOUNTED CODE that extends the deployment\'s own surface — not a tool an agent calls — so this is an inventory and not a tool source. It is read off the same boot snapshot every traced request resolves its subsystem label against, so it cannot drift from what is serving. Enabled-only by default, because a caller asking what this deployment can do wants what is running; ?all=true adds the configured-but-off ones.
         * @summary ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on.
         * @param {string} [all] All includes the configured-but-disabled subsystems too, but only when it is exactly the string \&quot;true\&quot;. Otherwise only the running ones are reported.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Plugins: async (all?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/plugins`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (all !== undefined) {
                localVarQueryParameter['all'] = all;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored. That is a different set with a different lifecycle from GET /v1/plugins, which reports the subsystems this deployment mounted. The bundled CommonJS the runtime executes is never included, and neither is any credential — a plugin names the connectors provider it needs and reads the credential from ctx.auth at run time.
         * @summary ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1PluginsAuthored: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/plugins/authored`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Builds one plugin for the caller\'s org and answers 201 with the bundle\'s size, whether a model wrote the source, and the plugin as stored. Post `source` to build TypeScript as-is, or `spec` — an OpenAPI document or plain prose describing the endpoints — to have one generated; the generated source comes back in the answer, so a caller reads what will run before it runs. Exactly one of the two, and `name` must be one lowercase path segment; both or neither is 400.  COMPILING IS THE GATE. The source goes through the same pipeline the committed connectors do — esbuild to one CommonJS program, then compiled in the goja runtime that will actually execute it — and anything that fails is rejected and NEVER stored. So a plugin in the store is one this deployment has already loaded once, not one a model claimed was fine. A failed build answers 422 carrying the diagnostics a caller needs to fix it: the bundler\'s error, the source that failed, and whether the model wrote it — a body outside the declared success shape.  CREDENTIALS ARE NOT PART OF A PLUGIN. A plugin names the connectors `provider` it needs and reads that credential from `ctx.auth` at run time, under KMS custody. Source that contains something shaped like a key is REFUSED rather than silently scrubbed, so a caller who pasted one finds out instead of shipping it — register it as a connector instead.  Requires a validated principal; 403 without one. The plugin is stored under that principal\'s org and is what `/v1/plugins/authored` lists — never `/v1/plugins`, which is this deployment\'s mounted-subsystem inventory. Source over 512 KiB or a spec over 256 KiB is refused. Posting a `spec` to a deployment with no AI client configured is 503, and a generation that fails upstream is 502.
         * @summary Build a plugin for your org from TypeScript, or from an API spec a model writes it from
         * @param {CloudBuildRequest} [cloudBuildRequest] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1PluginsBuild: async (cloudBuildRequest?: CloudBuildRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/plugins/build`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudBuildRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * PluginsApi - functional programming interface
 * @export
 */
export const PluginsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = PluginsApiAxiosParamCreator(configuration)
    return {
        /**
         * DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it. Scoped to the caller\'s org, so an id belonging to another tenant answers 404 and is not deleted.
         * @summary DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it.
         * @param {string} id ID is the plugin to remove, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1PluginsAuthoredId(id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudPluginDeleted>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1PluginsAuthoredId(id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['PluginsApi.cloudDeleteV1PluginsAuthoredId']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on. A plugin here is MOUNTED CODE that extends the deployment\'s own surface — not a tool an agent calls — so this is an inventory and not a tool source. It is read off the same boot snapshot every traced request resolves its subsystem label against, so it cannot drift from what is serving. Enabled-only by default, because a caller asking what this deployment can do wants what is running; ?all=true adds the configured-but-off ones.
         * @summary ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on.
         * @param {string} [all] All includes the configured-but-disabled subsystems too, but only when it is exactly the string \&quot;true\&quot;. Otherwise only the running ones are reported.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Plugins(all?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudPluginMountList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Plugins(all, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['PluginsApi.cloudGetV1Plugins']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored. That is a different set with a different lifecycle from GET /v1/plugins, which reports the subsystems this deployment mounted. The bundled CommonJS the runtime executes is never included, and neither is any credential — a plugin names the connectors provider it needs and reads the credential from ctx.auth at run time.
         * @summary ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1PluginsAuthored(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudAuthoredPluginList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1PluginsAuthored(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['PluginsApi.cloudGetV1PluginsAuthored']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Builds one plugin for the caller\'s org and answers 201 with the bundle\'s size, whether a model wrote the source, and the plugin as stored. Post `source` to build TypeScript as-is, or `spec` — an OpenAPI document or plain prose describing the endpoints — to have one generated; the generated source comes back in the answer, so a caller reads what will run before it runs. Exactly one of the two, and `name` must be one lowercase path segment; both or neither is 400.  COMPILING IS THE GATE. The source goes through the same pipeline the committed connectors do — esbuild to one CommonJS program, then compiled in the goja runtime that will actually execute it — and anything that fails is rejected and NEVER stored. So a plugin in the store is one this deployment has already loaded once, not one a model claimed was fine. A failed build answers 422 carrying the diagnostics a caller needs to fix it: the bundler\'s error, the source that failed, and whether the model wrote it — a body outside the declared success shape.  CREDENTIALS ARE NOT PART OF A PLUGIN. A plugin names the connectors `provider` it needs and reads that credential from `ctx.auth` at run time, under KMS custody. Source that contains something shaped like a key is REFUSED rather than silently scrubbed, so a caller who pasted one finds out instead of shipping it — register it as a connector instead.  Requires a validated principal; 403 without one. The plugin is stored under that principal\'s org and is what `/v1/plugins/authored` lists — never `/v1/plugins`, which is this deployment\'s mounted-subsystem inventory. Source over 512 KiB or a spec over 256 KiB is refused. Posting a `spec` to a deployment with no AI client configured is 503, and a generation that fails upstream is 502.
         * @summary Build a plugin for your org from TypeScript, or from an API spec a model writes it from
         * @param {CloudBuildRequest} [cloudBuildRequest] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1PluginsBuild(cloudBuildRequest?: CloudBuildRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudBuildOut>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1PluginsBuild(cloudBuildRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['PluginsApi.cloudPostV1PluginsBuild']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * PluginsApi - factory interface
 * @export
 */
export const PluginsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = PluginsApiFp(configuration)
    return {
        /**
         * DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it. Scoped to the caller\'s org, so an id belonging to another tenant answers 404 and is not deleted.
         * @summary DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it.
         * @param {PluginsApiCloudDeleteV1PluginsAuthoredIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1PluginsAuthoredId(requestParameters: PluginsApiCloudDeleteV1PluginsAuthoredIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudPluginDeleted> {
            return localVarFp.cloudDeleteV1PluginsAuthoredId(requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on. A plugin here is MOUNTED CODE that extends the deployment\'s own surface — not a tool an agent calls — so this is an inventory and not a tool source. It is read off the same boot snapshot every traced request resolves its subsystem label against, so it cannot drift from what is serving. Enabled-only by default, because a caller asking what this deployment can do wants what is running; ?all=true adds the configured-but-off ones.
         * @summary ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on.
         * @param {PluginsApiCloudGetV1PluginsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Plugins(requestParameters: PluginsApiCloudGetV1PluginsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudPluginMountList> {
            return localVarFp.cloudGetV1Plugins(requestParameters.all, options).then((request) => request(axios, basePath));
        },
        /**
         * ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored. That is a different set with a different lifecycle from GET /v1/plugins, which reports the subsystems this deployment mounted. The bundled CommonJS the runtime executes is never included, and neither is any credential — a plugin names the connectors provider it needs and reads the credential from ctx.auth at run time.
         * @summary ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1PluginsAuthored(options?: RawAxiosRequestConfig): AxiosPromise<CloudAuthoredPluginList> {
            return localVarFp.cloudGetV1PluginsAuthored(options).then((request) => request(axios, basePath));
        },
        /**
         * Builds one plugin for the caller\'s org and answers 201 with the bundle\'s size, whether a model wrote the source, and the plugin as stored. Post `source` to build TypeScript as-is, or `spec` — an OpenAPI document or plain prose describing the endpoints — to have one generated; the generated source comes back in the answer, so a caller reads what will run before it runs. Exactly one of the two, and `name` must be one lowercase path segment; both or neither is 400.  COMPILING IS THE GATE. The source goes through the same pipeline the committed connectors do — esbuild to one CommonJS program, then compiled in the goja runtime that will actually execute it — and anything that fails is rejected and NEVER stored. So a plugin in the store is one this deployment has already loaded once, not one a model claimed was fine. A failed build answers 422 carrying the diagnostics a caller needs to fix it: the bundler\'s error, the source that failed, and whether the model wrote it — a body outside the declared success shape.  CREDENTIALS ARE NOT PART OF A PLUGIN. A plugin names the connectors `provider` it needs and reads that credential from `ctx.auth` at run time, under KMS custody. Source that contains something shaped like a key is REFUSED rather than silently scrubbed, so a caller who pasted one finds out instead of shipping it — register it as a connector instead.  Requires a validated principal; 403 without one. The plugin is stored under that principal\'s org and is what `/v1/plugins/authored` lists — never `/v1/plugins`, which is this deployment\'s mounted-subsystem inventory. Source over 512 KiB or a spec over 256 KiB is refused. Posting a `spec` to a deployment with no AI client configured is 503, and a generation that fails upstream is 502.
         * @summary Build a plugin for your org from TypeScript, or from an API spec a model writes it from
         * @param {PluginsApiCloudPostV1PluginsBuildRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1PluginsBuild(requestParameters: PluginsApiCloudPostV1PluginsBuildRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudBuildOut> {
            return localVarFp.cloudPostV1PluginsBuild(requestParameters.cloudBuildRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudDeleteV1PluginsAuthoredId operation in PluginsApi.
 * @export
 * @interface PluginsApiCloudDeleteV1PluginsAuthoredIdRequest
 */
export interface PluginsApiCloudDeleteV1PluginsAuthoredIdRequest {
    /**
     * ID is the plugin to remove, from the path.
     * @type {string}
     * @memberof PluginsApiCloudDeleteV1PluginsAuthoredId
     */
    readonly id: string
}

/**
 * Request parameters for cloudGetV1Plugins operation in PluginsApi.
 * @export
 * @interface PluginsApiCloudGetV1PluginsRequest
 */
export interface PluginsApiCloudGetV1PluginsRequest {
    /**
     * All includes the configured-but-disabled subsystems too, but only when it is exactly the string \&quot;true\&quot;. Otherwise only the running ones are reported.
     * @type {string}
     * @memberof PluginsApiCloudGetV1Plugins
     */
    readonly all?: string
}

/**
 * Request parameters for cloudPostV1PluginsBuild operation in PluginsApi.
 * @export
 * @interface PluginsApiCloudPostV1PluginsBuildRequest
 */
export interface PluginsApiCloudPostV1PluginsBuildRequest {
    /**
     * 
     * @type {CloudBuildRequest}
     * @memberof PluginsApiCloudPostV1PluginsBuild
     */
    readonly cloudBuildRequest?: CloudBuildRequest
}

/**
 * PluginsApi - object-oriented interface
 * @export
 * @class PluginsApi
 * @extends {BaseAPI}
 */
export class PluginsApi extends BaseAPI {
    /**
     * DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it. Scoped to the caller\'s org, so an id belonging to another tenant answers 404 and is not deleted.
     * @summary DeleteAuthoredPlugin removes one of the caller org\'s built plugins, so the runtime can no longer load it.
     * @param {PluginsApiCloudDeleteV1PluginsAuthoredIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof PluginsApi
     */
    public cloudDeleteV1PluginsAuthoredId(requestParameters: PluginsApiCloudDeleteV1PluginsAuthoredIdRequest, options?: RawAxiosRequestConfig) {
        return PluginsApiFp(this.configuration).cloudDeleteV1PluginsAuthoredId(requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on. A plugin here is MOUNTED CODE that extends the deployment\'s own surface — not a tool an agent calls — so this is an inventory and not a tool source. It is read off the same boot snapshot every traced request resolves its subsystem label against, so it cannot drift from what is serving. Enabled-only by default, because a caller asking what this deployment can do wants what is running; ?all=true adds the configured-but-off ones.
     * @summary ListPlugins reports what this deployment actually mounted: every subsystem the composition root declared and whether it is switched on.
     * @param {PluginsApiCloudGetV1PluginsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof PluginsApi
     */
    public cloudGetV1Plugins(requestParameters: PluginsApiCloudGetV1PluginsRequest = {}, options?: RawAxiosRequestConfig) {
        return PluginsApiFp(this.configuration).cloudGetV1Plugins(requestParameters.all, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored. That is a different set with a different lifecycle from GET /v1/plugins, which reports the subsystems this deployment mounted. The bundled CommonJS the runtime executes is never included, and neither is any credential — a plugin names the connectors provider it needs and reads the credential from ctx.auth at run time.
     * @summary ListAuthoredPlugins lists the plugins the caller\'s org BUILT, newest first, each with the TypeScript as authored.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof PluginsApi
     */
    public cloudGetV1PluginsAuthored(options?: RawAxiosRequestConfig) {
        return PluginsApiFp(this.configuration).cloudGetV1PluginsAuthored(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Builds one plugin for the caller\'s org and answers 201 with the bundle\'s size, whether a model wrote the source, and the plugin as stored. Post `source` to build TypeScript as-is, or `spec` — an OpenAPI document or plain prose describing the endpoints — to have one generated; the generated source comes back in the answer, so a caller reads what will run before it runs. Exactly one of the two, and `name` must be one lowercase path segment; both or neither is 400.  COMPILING IS THE GATE. The source goes through the same pipeline the committed connectors do — esbuild to one CommonJS program, then compiled in the goja runtime that will actually execute it — and anything that fails is rejected and NEVER stored. So a plugin in the store is one this deployment has already loaded once, not one a model claimed was fine. A failed build answers 422 carrying the diagnostics a caller needs to fix it: the bundler\'s error, the source that failed, and whether the model wrote it — a body outside the declared success shape.  CREDENTIALS ARE NOT PART OF A PLUGIN. A plugin names the connectors `provider` it needs and reads that credential from `ctx.auth` at run time, under KMS custody. Source that contains something shaped like a key is REFUSED rather than silently scrubbed, so a caller who pasted one finds out instead of shipping it — register it as a connector instead.  Requires a validated principal; 403 without one. The plugin is stored under that principal\'s org and is what `/v1/plugins/authored` lists — never `/v1/plugins`, which is this deployment\'s mounted-subsystem inventory. Source over 512 KiB or a spec over 256 KiB is refused. Posting a `spec` to a deployment with no AI client configured is 503, and a generation that fails upstream is 502.
     * @summary Build a plugin for your org from TypeScript, or from an API spec a model writes it from
     * @param {PluginsApiCloudPostV1PluginsBuildRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof PluginsApi
     */
    public cloudPostV1PluginsBuild(requestParameters: PluginsApiCloudPostV1PluginsBuildRequest = {}, options?: RawAxiosRequestConfig) {
        return PluginsApiFp(this.configuration).cloudPostV1PluginsBuild(requestParameters.cloudBuildRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

