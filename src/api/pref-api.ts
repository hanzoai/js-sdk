/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { PrefsView } from '../models';
/**
 * PrefApi - axios parameter creator
 * @export
 */
export const PrefApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface. There is no path to another user\'s preferences: not for an org admin, not for a platform SuperAdmin, because the subject is built from the validated credential and is the mandatory predicate on the read. A caller who has never saved anything gets an empty document at 200, never a 404, so the user menu always renders.
         * @summary Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getPref: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/pref`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Merges a JSON object key-wise into the signed-in caller\'s OWN preference document and answers with the whole document after the merge, so a surface saves `theme` without having to send back the `density` another surface owns. The merge is SHALLOW and the key space is open: an unnamed key is left untouched, a named key is replaced whole, and a key sent with a `null` value is DELETED. The subject is the `<owner>/<name>` identity built from the validated credential and is the mandatory predicate on the write, so there is no path to another user\'s preferences — not for an org admin, not for a platform SuperAdmin. Fails closed: no validated principal is 403; an empty body or a literal `null` is 400; and a patch or a resulting document over 16 KiB or 128 keys is 413.
         * @summary Save the preference keys your surface owns, leaving every other key alone
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchPref: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/pref`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * PrefApi - functional programming interface
 * @export
 */
export const PrefApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = PrefApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface. There is no path to another user\'s preferences: not for an org admin, not for a platform SuperAdmin, because the subject is built from the validated credential and is the mandatory predicate on the read. A caller who has never saved anything gets an empty document at 200, never a 404, so the user menu always renders.
         * @summary Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getPref(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<PrefsView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getPref(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['PrefApi.getPref']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Merges a JSON object key-wise into the signed-in caller\'s OWN preference document and answers with the whole document after the merge, so a surface saves `theme` without having to send back the `density` another surface owns. The merge is SHALLOW and the key space is open: an unnamed key is left untouched, a named key is replaced whole, and a key sent with a `null` value is DELETED. The subject is the `<owner>/<name>` identity built from the validated credential and is the mandatory predicate on the write, so there is no path to another user\'s preferences — not for an org admin, not for a platform SuperAdmin. Fails closed: no validated principal is 403; an empty body or a literal `null` is 400; and a patch or a resulting document over 16 KiB or 128 keys is 413.
         * @summary Save the preference keys your surface owns, leaving every other key alone
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchPref(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchPref(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['PrefApi.patchPref']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * PrefApi - factory interface
 * @export
 */
export const PrefApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = PrefApiFp(configuration)
    return {
        /**
         * Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface. There is no path to another user\'s preferences: not for an org admin, not for a platform SuperAdmin, because the subject is built from the validated credential and is the mandatory predicate on the read. A caller who has never saved anything gets an empty document at 200, never a 404, so the user menu always renders.
         * @summary Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getPref(options?: RawAxiosRequestConfig): AxiosPromise<PrefsView> {
            return localVarFp.getPref(options).then((request) => request(axios, basePath));
        },
        /**
         * Merges a JSON object key-wise into the signed-in caller\'s OWN preference document and answers with the whole document after the merge, so a surface saves `theme` without having to send back the `density` another surface owns. The merge is SHALLOW and the key space is open: an unnamed key is left untouched, a named key is replaced whole, and a key sent with a `null` value is DELETED. The subject is the `<owner>/<name>` identity built from the validated credential and is the mandatory predicate on the write, so there is no path to another user\'s preferences — not for an org admin, not for a platform SuperAdmin. Fails closed: no validated principal is 403; an empty body or a literal `null` is 400; and a patch or a resulting document over 16 KiB or 128 keys is 413.
         * @summary Save the preference keys your surface owns, leaving every other key alone
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchPref(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.patchPref(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * PrefApi - object-oriented interface
 * @export
 * @class PrefApi
 * @extends {BaseAPI}
 */
export class PrefApi extends BaseAPI {
    /**
     * Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface. There is no path to another user\'s preferences: not for an org admin, not for a platform SuperAdmin, because the subject is built from the validated credential and is the mandatory predicate on the read. A caller who has never saved anything gets an empty document at 200, never a 404, so the user menu always renders.
     * @summary Returns the signed-in caller\'s OWN preference document — the theme, density and pinned nav that follow them across every Hanzo surface.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof PrefApi
     */
    public getPref(options?: RawAxiosRequestConfig) {
        return PrefApiFp(this.configuration).getPref(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Merges a JSON object key-wise into the signed-in caller\'s OWN preference document and answers with the whole document after the merge, so a surface saves `theme` without having to send back the `density` another surface owns. The merge is SHALLOW and the key space is open: an unnamed key is left untouched, a named key is replaced whole, and a key sent with a `null` value is DELETED. The subject is the `<owner>/<name>` identity built from the validated credential and is the mandatory predicate on the write, so there is no path to another user\'s preferences — not for an org admin, not for a platform SuperAdmin. Fails closed: no validated principal is 403; an empty body or a literal `null` is 400; and a patch or a resulting document over 16 KiB or 128 keys is 413.
     * @summary Save the preference keys your surface owns, leaving every other key alone
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof PrefApi
     */
    public patchPref(options?: RawAxiosRequestConfig) {
        return PrefApiFp(this.configuration).patchPref(options).then((request) => request(this.axios, this.basePath));
    }
}

