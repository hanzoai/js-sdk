/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudApiKeyList } from '../models';
// @ts-ignore
import type { CloudKeyTypeIn } from '../models';
// @ts-ignore
import type { CloudMintedKey } from '../models';
// @ts-ignore
import type { CloudOnboardReq } from '../models';
// @ts-ignore
import type { CloudOnboardResp } from '../models';
// @ts-ignore
import type { CloudRevokedKey } from '../models';
/**
 * IamApi - axios parameter creator
 * @export
 */
export const IamApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1Iam: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IamByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudDeleteV1IamByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/iam/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * RevokeKey revokes the caller\'s own API key of the requested class. The class is the same field mint takes — `?type=publishable`, defaulting to secret — so revoking the key that ships in a browser bundle does not sign its holder out of their own API: the other key keeps working.  Revoking is how a key is replaced when it does not need replacing; minting the same class again rotates it in one step. IAM drops the credential immediately, but the gateway caches keys for a few minutes, so a request that beat the cache expiry may still be served.  For callers written against the older shape, the class is also accepted in a JSON request body, read only when `?type=` is absent.
         * @summary RevokeKey revokes the caller\'s own API key of the requested class.
         * @param {string} [type] Type is the key class to act on: \&quot;secret\&quot; (sk-, session-equivalent, belongs on a server) or \&quot;publishable\&quot; (pk-, org-identifying, safe in a browser bundle). Omitted means secret, which is what every existing caller means.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IamKeys: async (type?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam/keys`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (type !== undefined) {
                localVarQueryParameter['type'] = type;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Iam: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IamByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudGetV1IamByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/iam/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago. No secret material comes back: a secret key is represented by its prefix, and only a publishable key (public by construction) carries its full value.  A transient IAM read failure reports an empty set rather than a 5xx, so the page shows the honest empty state and never a fabricated key.
         * @summary GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IamKeys: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam/keys`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1Iam: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'OPTIONS', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1IamByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudOptionsV1IamByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/iam/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'OPTIONS', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1Iam: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1IamByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudPatchV1IamByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/iam/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Iam: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IamByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudPostV1IamByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/iam/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE. A real IAM failure surfaces as 502, never a fabricated key.  Rotating is what creating means here: a user holds one key per type, so the endpoint is idempotent by (caller, type) and the superseded credential stops working. Two live secrets for one user would make \"revoke my key\" a lie.
         * @summary MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE.
         * @param {CloudKeyTypeIn} cloudKeyTypeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IamKeys: async (cloudKeyTypeIn: CloudKeyTypeIn, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudKeyTypeIn' is not null or undefined
            assertParamExists('cloudPostV1IamKeys', 'cloudKeyTypeIn', cloudKeyTypeIn)
            const localVarPath = `/v1/iam/keys`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudKeyTypeIn, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no owner): create + MOVE the user in as admin, so their next JWT     carries the new owner and the cloud scopes everything to it.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
         * @summary Onboard creates the caller\'s organization.
         * @param {CloudOnboardReq} cloudOnboardReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IamOnboard: async (cloudOnboardReq: CloudOnboardReq, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudOnboardReq' is not null or undefined
            assertParamExists('cloudPostV1IamOnboard', 'cloudOnboardReq', cloudOnboardReq)
            const localVarPath = `/v1/iam/onboard`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudOnboardReq, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1Iam: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/iam`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1IamByWildcard1: async (wildcard1: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'wildcard1' is not null or undefined
            assertParamExists('cloudPutV1IamByWildcard1', 'wildcard1', wildcard1)
            const localVarPath = `/v1/iam/{wildcard1}`
                .replace(`{${"wildcard1"}}`, encodeURIComponent(String(wildcard1)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * IamApi - functional programming interface
 * @export
 */
export const IamApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = IamApiAxiosParamCreator(configuration)
    return {
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1Iam(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1Iam(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudDeleteV1Iam']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1IamByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1IamByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudDeleteV1IamByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * RevokeKey revokes the caller\'s own API key of the requested class. The class is the same field mint takes — `?type=publishable`, defaulting to secret — so revoking the key that ships in a browser bundle does not sign its holder out of their own API: the other key keeps working.  Revoking is how a key is replaced when it does not need replacing; minting the same class again rotates it in one step. IAM drops the credential immediately, but the gateway caches keys for a few minutes, so a request that beat the cache expiry may still be served.  For callers written against the older shape, the class is also accepted in a JSON request body, read only when `?type=` is absent.
         * @summary RevokeKey revokes the caller\'s own API key of the requested class.
         * @param {string} [type] Type is the key class to act on: \&quot;secret\&quot; (sk-, session-equivalent, belongs on a server) or \&quot;publishable\&quot; (pk-, org-identifying, safe in a browser bundle). Omitted means secret, which is what every existing caller means.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1IamKeys(type?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudRevokedKey>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1IamKeys(type, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudDeleteV1IamKeys']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1Iam(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1Iam(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudGetV1Iam']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IamByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IamByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudGetV1IamByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago. No secret material comes back: a secret key is represented by its prefix, and only a publishable key (public by construction) carries its full value.  A transient IAM read failure reports an empty set rather than a 5xx, so the page shows the honest empty state and never a fabricated key.
         * @summary GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IamKeys(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudApiKeyList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IamKeys(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudGetV1IamKeys']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudOptionsV1Iam(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudOptionsV1Iam(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudOptionsV1Iam']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudOptionsV1IamByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudOptionsV1IamByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudOptionsV1IamByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPatchV1Iam(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPatchV1Iam(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPatchV1Iam']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPatchV1IamByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPatchV1IamByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPatchV1IamByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1Iam(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1Iam(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPostV1Iam']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1IamByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1IamByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPostV1IamByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE. A real IAM failure surfaces as 502, never a fabricated key.  Rotating is what creating means here: a user holds one key per type, so the endpoint is idempotent by (caller, type) and the superseded credential stops working. Two live secrets for one user would make \"revoke my key\" a lie.
         * @summary MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE.
         * @param {CloudKeyTypeIn} cloudKeyTypeIn 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1IamKeys(cloudKeyTypeIn: CloudKeyTypeIn, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMintedKey>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1IamKeys(cloudKeyTypeIn, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPostV1IamKeys']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no owner): create + MOVE the user in as admin, so their next JWT     carries the new owner and the cloud scopes everything to it.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
         * @summary Onboard creates the caller\'s organization.
         * @param {CloudOnboardReq} cloudOnboardReq 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1IamOnboard(cloudOnboardReq: CloudOnboardReq, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudOnboardResp>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1IamOnboard(cloudOnboardReq, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPostV1IamOnboard']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1Iam(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1Iam(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPutV1Iam']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {string} wildcard1 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1IamByWildcard1(wildcard1: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1IamByWildcard1(wildcard1, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IamApi.cloudPutV1IamByWildcard1']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * IamApi - factory interface
 * @export
 */
export const IamApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = IamApiFp(configuration)
    return {
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1Iam(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudDeleteV1Iam(options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {IamApiCloudDeleteV1IamByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IamByWildcard1(requestParameters: IamApiCloudDeleteV1IamByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudDeleteV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * RevokeKey revokes the caller\'s own API key of the requested class. The class is the same field mint takes — `?type=publishable`, defaulting to secret — so revoking the key that ships in a browser bundle does not sign its holder out of their own API: the other key keeps working.  Revoking is how a key is replaced when it does not need replacing; minting the same class again rotates it in one step. IAM drops the credential immediately, but the gateway caches keys for a few minutes, so a request that beat the cache expiry may still be served.  For callers written against the older shape, the class is also accepted in a JSON request body, read only when `?type=` is absent.
         * @summary RevokeKey revokes the caller\'s own API key of the requested class.
         * @param {IamApiCloudDeleteV1IamKeysRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IamKeys(requestParameters: IamApiCloudDeleteV1IamKeysRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudRevokedKey> {
            return localVarFp.cloudDeleteV1IamKeys(requestParameters.type, options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1Iam(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1Iam(options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {IamApiCloudGetV1IamByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IamByWildcard1(requestParameters: IamApiCloudGetV1IamByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago. No secret material comes back: a secret key is represented by its prefix, and only a publishable key (public by construction) carries its full value.  A transient IAM read failure reports an empty set rather than a 5xx, so the page shows the honest empty state and never a fabricated key.
         * @summary GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IamKeys(options?: RawAxiosRequestConfig): AxiosPromise<CloudApiKeyList> {
            return localVarFp.cloudGetV1IamKeys(options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1Iam(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudOptionsV1Iam(options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {IamApiCloudOptionsV1IamByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudOptionsV1IamByWildcard1(requestParameters: IamApiCloudOptionsV1IamByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudOptionsV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1Iam(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPatchV1Iam(options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {IamApiCloudPatchV1IamByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1IamByWildcard1(requestParameters: IamApiCloudPatchV1IamByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPatchV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Iam(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1Iam(options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {IamApiCloudPostV1IamByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IamByWildcard1(requestParameters: IamApiCloudPostV1IamByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
        /**
         * MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE. A real IAM failure surfaces as 502, never a fabricated key.  Rotating is what creating means here: a user holds one key per type, so the endpoint is idempotent by (caller, type) and the superseded credential stops working. Two live secrets for one user would make \"revoke my key\" a lie.
         * @summary MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE.
         * @param {IamApiCloudPostV1IamKeysRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IamKeys(requestParameters: IamApiCloudPostV1IamKeysRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudMintedKey> {
            return localVarFp.cloudPostV1IamKeys(requestParameters.cloudKeyTypeIn, options).then((request) => request(axios, basePath));
        },
        /**
         * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no owner): create + MOVE the user in as admin, so their next JWT     carries the new owner and the cloud scopes everything to it.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
         * @summary Onboard creates the caller\'s organization.
         * @param {IamApiCloudPostV1IamOnboardRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IamOnboard(requestParameters: IamApiCloudPostV1IamOnboardRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudOnboardResp> {
            return localVarFp.cloudPostV1IamOnboard(requestParameters.cloudOnboardReq, options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1Iam(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPutV1Iam(options).then((request) => request(axios, basePath));
        },
        /**
         * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
         * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
         * @param {IamApiCloudPutV1IamByWildcard1Request} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1IamByWildcard1(requestParameters: IamApiCloudPutV1IamByWildcard1Request, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPutV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudDeleteV1IamByWildcard1 operation in IamApi.
 * @export
 * @interface IamApiCloudDeleteV1IamByWildcard1Request
 */
export interface IamApiCloudDeleteV1IamByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof IamApiCloudDeleteV1IamByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudDeleteV1IamKeys operation in IamApi.
 * @export
 * @interface IamApiCloudDeleteV1IamKeysRequest
 */
export interface IamApiCloudDeleteV1IamKeysRequest {
    /**
     * Type is the key class to act on: \&quot;secret\&quot; (sk-, session-equivalent, belongs on a server) or \&quot;publishable\&quot; (pk-, org-identifying, safe in a browser bundle). Omitted means secret, which is what every existing caller means.
     * @type {string}
     * @memberof IamApiCloudDeleteV1IamKeys
     */
    readonly type?: string
}

/**
 * Request parameters for cloudGetV1IamByWildcard1 operation in IamApi.
 * @export
 * @interface IamApiCloudGetV1IamByWildcard1Request
 */
export interface IamApiCloudGetV1IamByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof IamApiCloudGetV1IamByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudOptionsV1IamByWildcard1 operation in IamApi.
 * @export
 * @interface IamApiCloudOptionsV1IamByWildcard1Request
 */
export interface IamApiCloudOptionsV1IamByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof IamApiCloudOptionsV1IamByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudPatchV1IamByWildcard1 operation in IamApi.
 * @export
 * @interface IamApiCloudPatchV1IamByWildcard1Request
 */
export interface IamApiCloudPatchV1IamByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof IamApiCloudPatchV1IamByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudPostV1IamByWildcard1 operation in IamApi.
 * @export
 * @interface IamApiCloudPostV1IamByWildcard1Request
 */
export interface IamApiCloudPostV1IamByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof IamApiCloudPostV1IamByWildcard1
     */
    readonly wildcard1: string
}

/**
 * Request parameters for cloudPostV1IamKeys operation in IamApi.
 * @export
 * @interface IamApiCloudPostV1IamKeysRequest
 */
export interface IamApiCloudPostV1IamKeysRequest {
    /**
     * 
     * @type {CloudKeyTypeIn}
     * @memberof IamApiCloudPostV1IamKeys
     */
    readonly cloudKeyTypeIn: CloudKeyTypeIn
}

/**
 * Request parameters for cloudPostV1IamOnboard operation in IamApi.
 * @export
 * @interface IamApiCloudPostV1IamOnboardRequest
 */
export interface IamApiCloudPostV1IamOnboardRequest {
    /**
     * 
     * @type {CloudOnboardReq}
     * @memberof IamApiCloudPostV1IamOnboard
     */
    readonly cloudOnboardReq: CloudOnboardReq
}

/**
 * Request parameters for cloudPutV1IamByWildcard1 operation in IamApi.
 * @export
 * @interface IamApiCloudPutV1IamByWildcard1Request
 */
export interface IamApiCloudPutV1IamByWildcard1Request {
    /**
     * 
     * @type {string}
     * @memberof IamApiCloudPutV1IamByWildcard1
     */
    readonly wildcard1: string
}

/**
 * IamApi - object-oriented interface
 * @export
 * @class IamApi
 * @extends {BaseAPI}
 */
export class IamApi extends BaseAPI {
    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudDeleteV1Iam(options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudDeleteV1Iam(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {IamApiCloudDeleteV1IamByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudDeleteV1IamByWildcard1(requestParameters: IamApiCloudDeleteV1IamByWildcard1Request, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudDeleteV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * RevokeKey revokes the caller\'s own API key of the requested class. The class is the same field mint takes — `?type=publishable`, defaulting to secret — so revoking the key that ships in a browser bundle does not sign its holder out of their own API: the other key keeps working.  Revoking is how a key is replaced when it does not need replacing; minting the same class again rotates it in one step. IAM drops the credential immediately, but the gateway caches keys for a few minutes, so a request that beat the cache expiry may still be served.  For callers written against the older shape, the class is also accepted in a JSON request body, read only when `?type=` is absent.
     * @summary RevokeKey revokes the caller\'s own API key of the requested class.
     * @param {IamApiCloudDeleteV1IamKeysRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudDeleteV1IamKeys(requestParameters: IamApiCloudDeleteV1IamKeysRequest = {}, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudDeleteV1IamKeys(requestParameters.type, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudGetV1Iam(options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudGetV1Iam(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {IamApiCloudGetV1IamByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudGetV1IamByWildcard1(requestParameters: IamApiCloudGetV1IamByWildcard1Request, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudGetV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago. No secret material comes back: a secret key is represented by its prefix, and only a publishable key (public by construction) carries its full value.  A transient IAM read failure reports an empty set rather than a 5xx, so the page shows the honest empty state and never a fabricated key.
     * @summary GetKey returns the caller\'s own API keys — every type they hold, read AUTHORITATIVELY from IAM rather than from the session claim, which lags a key minted moments ago.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudGetV1IamKeys(options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudGetV1IamKeys(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudOptionsV1Iam(options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudOptionsV1Iam(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {IamApiCloudOptionsV1IamByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudOptionsV1IamByWildcard1(requestParameters: IamApiCloudOptionsV1IamByWildcard1Request, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudOptionsV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPatchV1Iam(options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPatchV1Iam(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {IamApiCloudPatchV1IamByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPatchV1IamByWildcard1(requestParameters: IamApiCloudPatchV1IamByWildcard1Request, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPatchV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPostV1Iam(options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPostV1Iam(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {IamApiCloudPostV1IamByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPostV1IamByWildcard1(requestParameters: IamApiCloudPostV1IamByWildcard1Request, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPostV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE. A real IAM failure surfaces as 502, never a fabricated key.  Rotating is what creating means here: a user holds one key per type, so the endpoint is idempotent by (caller, type) and the superseded credential stops working. Two live secrets for one user would make \"revoke my key\" a lie.
     * @summary MintKey creates — or rotates — the caller\'s API key of the requested type and returns it ONCE.
     * @param {IamApiCloudPostV1IamKeysRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPostV1IamKeys(requestParameters: IamApiCloudPostV1IamKeysRequest, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPostV1IamKeys(requestParameters.cloudKeyTypeIn, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Onboard creates the caller\'s organization. Two flows, keyed on whether the caller already has a home org (mirrors app/onboard/route.ts):    - FIRST-RUN (no owner): create + MOVE the user in as admin, so their next JWT     carries the new owner and the cloud scopes everything to it.   - ADDITIONAL (owner set): create the org but do NOT move the user — a move     changes their IAM owner (stripping a SuperAdmin\'s status + orphaning their     current org). They reach the new org via the OrgSwitcher, which re-scopes     X-Org-Id without touching IAM membership. A personal-org request from someone     who already has an org is meaningless → 409.
     * @summary Onboard creates the caller\'s organization.
     * @param {IamApiCloudPostV1IamOnboardRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPostV1IamOnboard(requestParameters: IamApiCloudPostV1IamOnboardRequest, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPostV1IamOnboard(requestParameters.cloudOnboardReq, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPutV1Iam(options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPutV1Iam(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Relays github.com/hanzoai/iam verbatim. The remainder of the path and the method together select the operation INSIDE IAM — OIDC discovery and JWKS, the oauth authorize/token/userinfo/logout/introspect/revoke/device endpoints, credential and wallet sign-in, the front door a hosted login page self-configures from, the typed CRUD over users, organizations, applications, providers, roles, projects, workspaces, permissions, certs, keys, invitations and audit logs, SCIM 2.0, service accounts, memberships, TOTP enrollment, and the Casdoor verb aliases (get-users, add-organization, …) the live consoles still call. cloud does not interpret the remainder or rewrite the reply: the status, the bytes and the Content-Type are the nested app\'s own.  WHO MAY CALL IT is structural inside IAM — decided by which group a route is registered on, never by an allow-list that can drift. The protocol half is public by construction, because a caller holding no token has to be able to reach it: discovery, JWKS, authorize, token, userinfo, logout, introspect, revoke, credential sign-in, the CAIP-122 wallet flow, and the Docker registry token endpoint. Everything else is behind IAM\'s one Guard and fails closed at 401 — in IAM\'s OWN {\"status\":401,\"error\":\"authentication required\"} envelope, which is NOT cloud\'s error shape, so a client that only parses cloud errors will not recognise a refusal from here.  WHAT IT IS SCOPED TO is the verified bearer\'s own organization: a request parameter can never widen a read past the caller\'s authority, because the owner queried is the owner authorized. The single cross-tenant scope is membership of the reserved `admin` organization, and only such a principal may write a platform-owned record — the gate that stops a tenant from overwriting a signing cert and minting its own tokens. That predicate is read from the token SUBJECT, never from its `owner` or `organization` claims, which name the APPLICATION\'s org and diverge from the user\'s for a shared app. An org admin manages only what its own org owns; an ordinary user gets self-service on its own record and nothing more.  TWO RULES a reader otherwise gets wrong. The oauth token, introspect and revoke endpoints take application/x-www-form-urlencoded bodies by RFC 6749/7662/7009, not JSON — a generated client that posts JSON to everything under this prefix turns a working token exchange into a 400. And the prefix is not exclusively IAM\'s: a more specific route beats this wildcard, so /v1/iam/keys and /v1/iam/onboard are served by the account app rather than by the relay, even though the nested app registers those addresses too.  If IAM cannot open its encrypted store or finish mounting, every address here answers a JSON 503 instead of falling through to the console\'s single-page app. An identity path must say it is down rather than return HTML a client will try to parse.
     * @summary The whole Hanzo identity surface: OIDC and OAuth2, sign-in, and the user, organization, application, role and credential records every Hanzo service authenticates against.
     * @param {IamApiCloudPutV1IamByWildcard1Request} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IamApi
     */
    public cloudPutV1IamByWildcard1(requestParameters: IamApiCloudPutV1IamByWildcard1Request, options?: RawAxiosRequestConfig) {
        return IamApiFp(this.configuration).cloudPutV1IamByWildcard1(requestParameters.wildcard1, options).then((request) => request(this.axios, this.basePath));
    }
}

