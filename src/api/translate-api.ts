/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudMemoryEntry } from '../models';
// @ts-ignore
import type { CloudMemoryPage } from '../models';
// @ts-ignore
import type { CloudReviewRequest } from '../models';
/**
 * TranslateApi - axios parameter creator
 * @export
 */
export const TranslateApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder. It is the review lane\'s read: what a human reviewer works through.  The org is ALWAYS the validated principal\'s org, never a request field, so one tenant can never read another\'s memory — the entries hold customer source text.
         * @summary List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder.
         * @param {string} [target] Target narrows to one target language tag (BCP-47, e.g. \&quot;es\&quot; or \&quot;pt-BR\&quot;).
         * @param {string} [state] State narrows to one position on the review ladder: machine, suggested, approved or published.
         * @param {number} [limit] Limit caps the rows returned. Non-positive or unparseable means the server default (200); the ceiling is 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1TranslateMemory: async (target?: string, state?: string, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/translate/memory`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (target !== undefined) {
                localVarQueryParameter['target'] = target;
            }

            if (state !== undefined) {
                localVarQueryParameter['state'] = state;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one translation per input string, in input order, each carrying where it sits on the review ladder and whether it came from your memory rather than an engine — plus a usage block of REAL counts (strings, cached, translated, and the source characters that actually reached an engine). Send `text` for one string or `batch` for many, never both. When you name no `source`, the detected one is reported back.  THE TRANSLATION MEMORY IS CONSULTED FIRST AND IT IS NORMATIVE, NOT A CACHE. Every string keys on (source text, target, glossary version, tier); a hit is returned VERBATIM and never re-translated, which is what makes a locale rebuild idempotent under a non-deterministic model and the bill proportional to what actually changed. Misses go to the engine and are written back at state `machine`. Editing a glossary term changes the key, so a stale rendering can never be served.  IT CANNOT TRAMPLE REVIEWED WORK. A write from this route may create an entry or refresh one still at `machine`, and nothing else — a string a human moved to approved or published through the memory review lane survives every rebuild, and comes back here unchanged. The memory is the caller\'s OWN org\'s, a separate store per org: the source text you send is customer content and lands nowhere else. Read it back or review it at /v1/translate/memory.  `tier` picks the engine and defaults to quality — the model plane, which carries context, terminology and tone, and which bills its own tokens, so nothing is charged twice here. `bulk` is the high-volume engine and is metered HERE, on the source characters that reached it: a fully-cached rebuild reports zero characters and costs zero. BULK NEVER FALLS BACK TO QUALITY — on a deployment that does not serve it the answer is 503 for that tier, so a caller is never quietly served, or charged, at a tier it did not ask for. A bulk request beyond its balance is refused with the nested {\"error\":{\"code\",\"message\"}} body at 402/503.  `target` IS CHECKED FOR SHAPE, NOT FOR SUPPORT: anything BCP-47-shaped is accepted (`es`, `pt-BR`), anything else is 400. There is no unsupported-language error — a well-formed tag no engine can actually render is passed straight through, and whatever comes back is what gets stored and returned. `format` (text, html, markdown) tells the engine what markup to preserve; `glossary` fixes terms verbatim.  Requires a validated principal — 401 without one, and the org is always that principal\'s. At most 512 strings per call and 32768 characters per string; an engine that fails or answers a reply that does not cover every input is 502, and nothing is stored.
         * @summary Translate a string or a batch into one target language
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Translate: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/translate`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Review records a human decision on one translation-memory entry, and returns the entry as stored. A human write always wins over the stored value, and once it lands at approved or published no machine write can move it again — which is what makes a locale rebuild safe to run against reviewed work.  The org is ALWAYS the validated principal\'s org, never a request field, so a review can only ever land in the caller\'s own memory.
         * @summary Review records a human decision on one translation-memory entry, and returns the entry as stored.
         * @param {CloudReviewRequest} cloudReviewRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1TranslateMemory: async (cloudReviewRequest: CloudReviewRequest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudReviewRequest' is not null or undefined
            assertParamExists('cloudPutV1TranslateMemory', 'cloudReviewRequest', cloudReviewRequest)
            const localVarPath = `/v1/translate/memory`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudReviewRequest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * TranslateApi - functional programming interface
 * @export
 */
export const TranslateApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = TranslateApiAxiosParamCreator(configuration)
    return {
        /**
         * List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder. It is the review lane\'s read: what a human reviewer works through.  The org is ALWAYS the validated principal\'s org, never a request field, so one tenant can never read another\'s memory — the entries hold customer source text.
         * @summary List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder.
         * @param {string} [target] Target narrows to one target language tag (BCP-47, e.g. \&quot;es\&quot; or \&quot;pt-BR\&quot;).
         * @param {string} [state] State narrows to one position on the review ladder: machine, suggested, approved or published.
         * @param {number} [limit] Limit caps the rows returned. Non-positive or unparseable means the server default (200); the ceiling is 1000.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1TranslateMemory(target?: string, state?: string, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMemoryPage>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1TranslateMemory(target, state, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TranslateApi.cloudGetV1TranslateMemory']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one translation per input string, in input order, each carrying where it sits on the review ladder and whether it came from your memory rather than an engine — plus a usage block of REAL counts (strings, cached, translated, and the source characters that actually reached an engine). Send `text` for one string or `batch` for many, never both. When you name no `source`, the detected one is reported back.  THE TRANSLATION MEMORY IS CONSULTED FIRST AND IT IS NORMATIVE, NOT A CACHE. Every string keys on (source text, target, glossary version, tier); a hit is returned VERBATIM and never re-translated, which is what makes a locale rebuild idempotent under a non-deterministic model and the bill proportional to what actually changed. Misses go to the engine and are written back at state `machine`. Editing a glossary term changes the key, so a stale rendering can never be served.  IT CANNOT TRAMPLE REVIEWED WORK. A write from this route may create an entry or refresh one still at `machine`, and nothing else — a string a human moved to approved or published through the memory review lane survives every rebuild, and comes back here unchanged. The memory is the caller\'s OWN org\'s, a separate store per org: the source text you send is customer content and lands nowhere else. Read it back or review it at /v1/translate/memory.  `tier` picks the engine and defaults to quality — the model plane, which carries context, terminology and tone, and which bills its own tokens, so nothing is charged twice here. `bulk` is the high-volume engine and is metered HERE, on the source characters that reached it: a fully-cached rebuild reports zero characters and costs zero. BULK NEVER FALLS BACK TO QUALITY — on a deployment that does not serve it the answer is 503 for that tier, so a caller is never quietly served, or charged, at a tier it did not ask for. A bulk request beyond its balance is refused with the nested {\"error\":{\"code\",\"message\"}} body at 402/503.  `target` IS CHECKED FOR SHAPE, NOT FOR SUPPORT: anything BCP-47-shaped is accepted (`es`, `pt-BR`), anything else is 400. There is no unsupported-language error — a well-formed tag no engine can actually render is passed straight through, and whatever comes back is what gets stored and returned. `format` (text, html, markdown) tells the engine what markup to preserve; `glossary` fixes terms verbatim.  Requires a validated principal — 401 without one, and the org is always that principal\'s. At most 512 strings per call and 32768 characters per string; an engine that fails or answers a reply that does not cover every input is 502, and nothing is stored.
         * @summary Translate a string or a batch into one target language
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1Translate(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1Translate(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TranslateApi.cloudPostV1Translate']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Review records a human decision on one translation-memory entry, and returns the entry as stored. A human write always wins over the stored value, and once it lands at approved or published no machine write can move it again — which is what makes a locale rebuild safe to run against reviewed work.  The org is ALWAYS the validated principal\'s org, never a request field, so a review can only ever land in the caller\'s own memory.
         * @summary Review records a human decision on one translation-memory entry, and returns the entry as stored.
         * @param {CloudReviewRequest} cloudReviewRequest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1TranslateMemory(cloudReviewRequest: CloudReviewRequest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMemoryEntry>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1TranslateMemory(cloudReviewRequest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TranslateApi.cloudPutV1TranslateMemory']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * TranslateApi - factory interface
 * @export
 */
export const TranslateApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = TranslateApiFp(configuration)
    return {
        /**
         * List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder. It is the review lane\'s read: what a human reviewer works through.  The org is ALWAYS the validated principal\'s org, never a request field, so one tenant can never read another\'s memory — the entries hold customer source text.
         * @summary List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder.
         * @param {TranslateApiCloudGetV1TranslateMemoryRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1TranslateMemory(requestParameters: TranslateApiCloudGetV1TranslateMemoryRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudMemoryPage> {
            return localVarFp.cloudGetV1TranslateMemory(requestParameters.target, requestParameters.state, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one translation per input string, in input order, each carrying where it sits on the review ladder and whether it came from your memory rather than an engine — plus a usage block of REAL counts (strings, cached, translated, and the source characters that actually reached an engine). Send `text` for one string or `batch` for many, never both. When you name no `source`, the detected one is reported back.  THE TRANSLATION MEMORY IS CONSULTED FIRST AND IT IS NORMATIVE, NOT A CACHE. Every string keys on (source text, target, glossary version, tier); a hit is returned VERBATIM and never re-translated, which is what makes a locale rebuild idempotent under a non-deterministic model and the bill proportional to what actually changed. Misses go to the engine and are written back at state `machine`. Editing a glossary term changes the key, so a stale rendering can never be served.  IT CANNOT TRAMPLE REVIEWED WORK. A write from this route may create an entry or refresh one still at `machine`, and nothing else — a string a human moved to approved or published through the memory review lane survives every rebuild, and comes back here unchanged. The memory is the caller\'s OWN org\'s, a separate store per org: the source text you send is customer content and lands nowhere else. Read it back or review it at /v1/translate/memory.  `tier` picks the engine and defaults to quality — the model plane, which carries context, terminology and tone, and which bills its own tokens, so nothing is charged twice here. `bulk` is the high-volume engine and is metered HERE, on the source characters that reached it: a fully-cached rebuild reports zero characters and costs zero. BULK NEVER FALLS BACK TO QUALITY — on a deployment that does not serve it the answer is 503 for that tier, so a caller is never quietly served, or charged, at a tier it did not ask for. A bulk request beyond its balance is refused with the nested {\"error\":{\"code\",\"message\"}} body at 402/503.  `target` IS CHECKED FOR SHAPE, NOT FOR SUPPORT: anything BCP-47-shaped is accepted (`es`, `pt-BR`), anything else is 400. There is no unsupported-language error — a well-formed tag no engine can actually render is passed straight through, and whatever comes back is what gets stored and returned. `format` (text, html, markdown) tells the engine what markup to preserve; `glossary` fixes terms verbatim.  Requires a validated principal — 401 without one, and the org is always that principal\'s. At most 512 strings per call and 32768 characters per string; an engine that fails or answers a reply that does not cover every input is 502, and nothing is stored.
         * @summary Translate a string or a batch into one target language
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1Translate(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1Translate(options).then((request) => request(axios, basePath));
        },
        /**
         * Review records a human decision on one translation-memory entry, and returns the entry as stored. A human write always wins over the stored value, and once it lands at approved or published no machine write can move it again — which is what makes a locale rebuild safe to run against reviewed work.  The org is ALWAYS the validated principal\'s org, never a request field, so a review can only ever land in the caller\'s own memory.
         * @summary Review records a human decision on one translation-memory entry, and returns the entry as stored.
         * @param {TranslateApiCloudPutV1TranslateMemoryRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1TranslateMemory(requestParameters: TranslateApiCloudPutV1TranslateMemoryRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudMemoryEntry> {
            return localVarFp.cloudPutV1TranslateMemory(requestParameters.cloudReviewRequest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudGetV1TranslateMemory operation in TranslateApi.
 * @export
 * @interface TranslateApiCloudGetV1TranslateMemoryRequest
 */
export interface TranslateApiCloudGetV1TranslateMemoryRequest {
    /**
     * Target narrows to one target language tag (BCP-47, e.g. \&quot;es\&quot; or \&quot;pt-BR\&quot;).
     * @type {string}
     * @memberof TranslateApiCloudGetV1TranslateMemory
     */
    readonly target?: string

    /**
     * State narrows to one position on the review ladder: machine, suggested, approved or published.
     * @type {string}
     * @memberof TranslateApiCloudGetV1TranslateMemory
     */
    readonly state?: string

    /**
     * Limit caps the rows returned. Non-positive or unparseable means the server default (200); the ceiling is 1000.
     * @type {number}
     * @memberof TranslateApiCloudGetV1TranslateMemory
     */
    readonly limit?: number
}

/**
 * Request parameters for cloudPutV1TranslateMemory operation in TranslateApi.
 * @export
 * @interface TranslateApiCloudPutV1TranslateMemoryRequest
 */
export interface TranslateApiCloudPutV1TranslateMemoryRequest {
    /**
     * 
     * @type {CloudReviewRequest}
     * @memberof TranslateApiCloudPutV1TranslateMemory
     */
    readonly cloudReviewRequest: CloudReviewRequest
}

/**
 * TranslateApi - object-oriented interface
 * @export
 * @class TranslateApi
 * @extends {BaseAPI}
 */
export class TranslateApi extends BaseAPI {
    /**
     * List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder. It is the review lane\'s read: what a human reviewer works through.  The org is ALWAYS the validated principal\'s org, never a request field, so one tenant can never read another\'s memory — the entries hold customer source text.
     * @summary List returns the org\'s own translation-memory entries, newest first, optionally narrowed to one target language and/or one position on the review ladder.
     * @param {TranslateApiCloudGetV1TranslateMemoryRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TranslateApi
     */
    public cloudGetV1TranslateMemory(requestParameters: TranslateApiCloudGetV1TranslateMemoryRequest = {}, options?: RawAxiosRequestConfig) {
        return TranslateApiFp(this.configuration).cloudGetV1TranslateMemory(requestParameters.target, requestParameters.state, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one translation per input string, in input order, each carrying where it sits on the review ladder and whether it came from your memory rather than an engine — plus a usage block of REAL counts (strings, cached, translated, and the source characters that actually reached an engine). Send `text` for one string or `batch` for many, never both. When you name no `source`, the detected one is reported back.  THE TRANSLATION MEMORY IS CONSULTED FIRST AND IT IS NORMATIVE, NOT A CACHE. Every string keys on (source text, target, glossary version, tier); a hit is returned VERBATIM and never re-translated, which is what makes a locale rebuild idempotent under a non-deterministic model and the bill proportional to what actually changed. Misses go to the engine and are written back at state `machine`. Editing a glossary term changes the key, so a stale rendering can never be served.  IT CANNOT TRAMPLE REVIEWED WORK. A write from this route may create an entry or refresh one still at `machine`, and nothing else — a string a human moved to approved or published through the memory review lane survives every rebuild, and comes back here unchanged. The memory is the caller\'s OWN org\'s, a separate store per org: the source text you send is customer content and lands nowhere else. Read it back or review it at /v1/translate/memory.  `tier` picks the engine and defaults to quality — the model plane, which carries context, terminology and tone, and which bills its own tokens, so nothing is charged twice here. `bulk` is the high-volume engine and is metered HERE, on the source characters that reached it: a fully-cached rebuild reports zero characters and costs zero. BULK NEVER FALLS BACK TO QUALITY — on a deployment that does not serve it the answer is 503 for that tier, so a caller is never quietly served, or charged, at a tier it did not ask for. A bulk request beyond its balance is refused with the nested {\"error\":{\"code\",\"message\"}} body at 402/503.  `target` IS CHECKED FOR SHAPE, NOT FOR SUPPORT: anything BCP-47-shaped is accepted (`es`, `pt-BR`), anything else is 400. There is no unsupported-language error — a well-formed tag no engine can actually render is passed straight through, and whatever comes back is what gets stored and returned. `format` (text, html, markdown) tells the engine what markup to preserve; `glossary` fixes terms verbatim.  Requires a validated principal — 401 without one, and the org is always that principal\'s. At most 512 strings per call and 32768 characters per string; an engine that fails or answers a reply that does not cover every input is 502, and nothing is stored.
     * @summary Translate a string or a batch into one target language
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TranslateApi
     */
    public cloudPostV1Translate(options?: RawAxiosRequestConfig) {
        return TranslateApiFp(this.configuration).cloudPostV1Translate(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Review records a human decision on one translation-memory entry, and returns the entry as stored. A human write always wins over the stored value, and once it lands at approved or published no machine write can move it again — which is what makes a locale rebuild safe to run against reviewed work.  The org is ALWAYS the validated principal\'s org, never a request field, so a review can only ever land in the caller\'s own memory.
     * @summary Review records a human decision on one translation-memory entry, and returns the entry as stored.
     * @param {TranslateApiCloudPutV1TranslateMemoryRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TranslateApi
     */
    public cloudPutV1TranslateMemory(requestParameters: TranslateApiCloudPutV1TranslateMemoryRequest, options?: RawAxiosRequestConfig) {
        return TranslateApiFp(this.configuration).cloudPutV1TranslateMemory(requestParameters.cloudReviewRequest, options).then((request) => request(this.axios, this.basePath));
    }
}

