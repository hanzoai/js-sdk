/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { Appearance } from '../models';
/**
 * AppearanceApi - axios parameter creator
 * @export
 */
export const AppearanceApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
         * @summary Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAppearance: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/appearance`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
         * @summary Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
         * @param {Appearance} appearance 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAppearance: async (appearance: Appearance, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'appearance' is not null or undefined
            assertParamExists('postAppearance', 'appearance', appearance)
            const localVarPath = `/v1/appearance`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(appearance, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * AppearanceApi - functional programming interface
 * @export
 */
export const AppearanceApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = AppearanceApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
         * @summary Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getAppearance(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Appearance>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getAppearance(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AppearanceApi.getAppearance']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
         * @summary Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
         * @param {Appearance} appearance 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postAppearance(appearance: Appearance, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Appearance>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postAppearance(appearance, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AppearanceApi.postAppearance']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * AppearanceApi - factory interface
 * @export
 */
export const AppearanceApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = AppearanceApiFp(configuration)
    return {
        /**
         * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
         * @summary Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getAppearance(options?: RawAxiosRequestConfig): AxiosPromise<Appearance> {
            return localVarFp.getAppearance(options).then((request) => request(axios, basePath));
        },
        /**
         * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
         * @summary Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
         * @param {AppearanceApiPostAppearanceRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postAppearance(requestParameters: AppearanceApiPostAppearanceRequest, options?: RawAxiosRequestConfig): AxiosPromise<Appearance> {
            return localVarFp.postAppearance(requestParameters.appearance, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for postAppearance operation in AppearanceApi.
 * @export
 * @interface AppearanceApiPostAppearanceRequest
 */
export interface AppearanceApiPostAppearanceRequest {
    /**
     * 
     * @type {Appearance}
     * @memberof AppearanceApiPostAppearance
     */
    readonly appearance: Appearance
}

/**
 * AppearanceApi - object-oriented interface
 * @export
 * @class AppearanceApi
 * @extends {BaseAPI}
 */
export class AppearanceApi extends BaseAPI {
    /**
     * Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface. An unset preference is an empty object.  A transient IAM read failure reports the empty preference rather than a 5xx, so a surface applies its published default and never error-toasts on load — the same fail-soft the key read uses.
     * @summary Returns the signed-in caller\'s own appearance preference — text size, density and accent — read from their IAM account so it is the same on every device and every Hanzo surface.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AppearanceApi
     */
    public getAppearance(options?: RawAxiosRequestConfig) {
        return AppearanceApiFp(this.configuration).getAppearance(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row. The accent is validated as a real colour token before it is stored; an unset or invalid axis is dropped rather than stored.
     * @summary Stores the caller\'s appearance preference on their IAM account, preserving every other field of the row.
     * @param {AppearanceApiPostAppearanceRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AppearanceApi
     */
    public postAppearance(requestParameters: AppearanceApiPostAppearanceRequest, options?: RawAxiosRequestConfig) {
        return AppearanceApiFp(this.configuration).postAppearance(requestParameters.appearance, options).then((request) => request(this.axios, this.basePath));
    }
}

