/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudHealthReport } from '../models';
// @ts-ignore
import type { CloudOverview } from '../models';
// @ts-ignore
import type { CloudTimeseries } from '../models';
// @ts-ignore
import type { CloudTop } from '../models';
// @ts-ignore
import type { DnsError } from '../models';
// @ts-ignore
import type { DnsQueryAnalytics } from '../models';
/**
 * AnalyticsApi - axios parameter creator
 * @export
 */
export const AnalyticsApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Reports the analytics subsystem\'s own liveness in BOTH directions: `plane` is the event plane it WRITES (the bus and the JetStream stream every accepted event is published to, both named in the report), and `datastore` is the warehouse it READS, with each read lens\'s table reported as it is provisioned (the LLM usage ledger and the product-event table).  EITHER ONE DOWN IS A 503, and the report says WHICH — they are probed independently and never collapse into a single bit. This endpoint used to report the read half only, and answered 200/ok while every POST /v1/event failed on a stream that could not bind: a total ingest outage behind a green probe. A readiness gate wired here now gates on the write path too.  `plane.ready` IS A REAL PROBE and walks the ingest path itself — the same connection and the same stream a publish uses — so it cannot answer ready while a publish would 503. `plane.reason` carries the plane\'s own error text when it is false.  `datastore` IS NOT PROBED WITH A QUERY. It is the state of the process\'s own shared client — established, and not since closed — so a warehouse accepting connections and failing reads still reports true. Degraded CARRIES the report (status, the failing half, reason) as its body rather than an error envelope, so a gate reads the cause off the same object it got at 200.  A MISSING LENS TABLE IS NOT A FAILURE and never moves the status: a lens reported available:false answers honest-empty rather than erroring, so a fresh deployment whose collector has not emitted yet is legitimately 200 with the product-event lens unavailable. The lens block is reported whenever the warehouse is REACHABLE — including on a report degraded by the plane, where the tables genuinely were probed — and is absent only when the warehouse is not, having nothing to say about tables it could not reach.  Unauthenticated on purpose — liveness has to be probe-able — and it reads NO tenant data: table existence and stream presence only, never a row and never an event.
         * @summary Whether the event plane can take a write and the warehouse can answer a read
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/analytics/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Overview returns the caller org\'s analytics KPIs for one time window. Three lenses over one warehouse: llm is the live per-org LLM usage ledger (requests, tokens, spend, models, providers, errors) and is always real; web (pageviews, visitors, sessions) and commerce (orders, revenue, AOV) read the product-event table and report available=false rather than fabricating zeros when it holds nothing yet.  The org is the validated principal\'s — never a parameter — so a caller can only ever read its own tenant. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Overview returns the caller org\'s analytics KPIs for one time window.
         * @param {string} [range] Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
         * @param {string} [start] Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
         * @param {string} [end] End is the exclusive upper bound of a custom window, RFC3339. Requires start.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsOverview: async (range?: string, start?: string, end?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/analytics/overview`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }

            if (start !== undefined) {
                localVarQueryParameter['start'] = start;
            }

            if (end !== undefined) {
                localVarQueryParameter['end'] = end;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series. One point per hour or per day — the bucket the window implies, 24h giving hours and 7d/30d giving days — carrying requests, total tokens and spend in cents. Empty buckets are filled with zeros so a client charts a continuous line.  The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series.
         * @param {string} [range] Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
         * @param {string} [start] Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
         * @param {string} [end] End is the exclusive upper bound of a custom window, RFC3339. Requires start.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsTimeseries: async (range?: string, start?: string, end?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/analytics/timeseries`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }

            if (start !== undefined) {
                localVarQueryParameter['start'] = start;
            }

            if (end !== undefined) {
                localVarQueryParameter['end'] = end;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Top returns the caller org\'s ranked lenses for one window, five of them at once. models ranks LLM models by spend and is always real; products ranks commerce orders by revenue; topPages ranks requested paths, topReferrers the external referrer domains (\"(direct)\" for a missing or same-origin one) and topSources the utm_source campaigns (\"(none)\" when absent), each by pageviews. Every lens carries each row\'s share of the in-window total, so a top-N honestly shows the long tail.  The four event lenses report available=false rather than fabricating zeros when the product-event table holds nothing yet. The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Top returns the caller org\'s ranked lenses for one window, five of them at once.
         * @param {string} [range] Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
         * @param {string} [start] Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
         * @param {string} [end] End is the exclusive upper bound of a custom window, RFC3339. Requires start.
         * @param {number} [limit] Limit bounds every ranked lens in the response. Default 10, maximum 100; a value at or below zero, or one that is not a number, takes the default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsTop: async (range?: string, start?: string, end?: string, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/analytics/top`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }

            if (start !== undefined) {
                localVarQueryParameter['start'] = start;
            }

            if (end !== undefined) {
                localVarQueryParameter['end'] = end;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * 
         * @summary Get query analytics
         * @param {string} zone 
         * @param {string} [from] 
         * @param {string} [to] 
         * @param {DnsGetZoneAnalyticsGranularityEnum} [granularity] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        dnsGetZoneAnalytics: async (zone: string, from?: string, to?: string, granularity?: DnsGetZoneAnalyticsGranularityEnum, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'zone' is not null or undefined
            assertParamExists('dnsGetZoneAnalytics', 'zone', zone)
            const localVarPath = `/v1/dns/zones/{zone}/analytics`
                .replace(`{${"zone"}}`, encodeURIComponent(String(zone)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (from !== undefined) {
                localVarQueryParameter['from'] = (from as any instanceof Date) ?
                    (from as any).toISOString() :
                    from;
            }

            if (to !== undefined) {
                localVarQueryParameter['to'] = (to as any instanceof Date) ?
                    (to as any).toISOString() :
                    to;
            }

            if (granularity !== undefined) {
                localVarQueryParameter['granularity'] = granularity;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * AnalyticsApi - functional programming interface
 * @export
 */
export const AnalyticsApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = AnalyticsApiAxiosParamCreator(configuration)
    return {
        /**
         * Reports the analytics subsystem\'s own liveness in BOTH directions: `plane` is the event plane it WRITES (the bus and the JetStream stream every accepted event is published to, both named in the report), and `datastore` is the warehouse it READS, with each read lens\'s table reported as it is provisioned (the LLM usage ledger and the product-event table).  EITHER ONE DOWN IS A 503, and the report says WHICH — they are probed independently and never collapse into a single bit. This endpoint used to report the read half only, and answered 200/ok while every POST /v1/event failed on a stream that could not bind: a total ingest outage behind a green probe. A readiness gate wired here now gates on the write path too.  `plane.ready` IS A REAL PROBE and walks the ingest path itself — the same connection and the same stream a publish uses — so it cannot answer ready while a publish would 503. `plane.reason` carries the plane\'s own error text when it is false.  `datastore` IS NOT PROBED WITH A QUERY. It is the state of the process\'s own shared client — established, and not since closed — so a warehouse accepting connections and failing reads still reports true. Degraded CARRIES the report (status, the failing half, reason) as its body rather than an error envelope, so a gate reads the cause off the same object it got at 200.  A MISSING LENS TABLE IS NOT A FAILURE and never moves the status: a lens reported available:false answers honest-empty rather than erroring, so a fresh deployment whose collector has not emitted yet is legitimately 200 with the product-event lens unavailable. The lens block is reported whenever the warehouse is REACHABLE — including on a report degraded by the plane, where the tables genuinely were probed — and is absent only when the warehouse is not, having nothing to say about tables it could not reach.  Unauthenticated on purpose — liveness has to be probe-able — and it reads NO tenant data: table existence and stream presence only, never a row and never an event.
         * @summary Whether the event plane can take a write and the warehouse can answer a read
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1AnalyticsHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudHealthReport>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1AnalyticsHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AnalyticsApi.cloudGetV1AnalyticsHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Overview returns the caller org\'s analytics KPIs for one time window. Three lenses over one warehouse: llm is the live per-org LLM usage ledger (requests, tokens, spend, models, providers, errors) and is always real; web (pageviews, visitors, sessions) and commerce (orders, revenue, AOV) read the product-event table and report available=false rather than fabricating zeros when it holds nothing yet.  The org is the validated principal\'s — never a parameter — so a caller can only ever read its own tenant. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Overview returns the caller org\'s analytics KPIs for one time window.
         * @param {string} [range] Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
         * @param {string} [start] Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
         * @param {string} [end] End is the exclusive upper bound of a custom window, RFC3339. Requires start.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1AnalyticsOverview(range?: string, start?: string, end?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudOverview>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1AnalyticsOverview(range, start, end, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AnalyticsApi.cloudGetV1AnalyticsOverview']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series. One point per hour or per day — the bucket the window implies, 24h giving hours and 7d/30d giving days — carrying requests, total tokens and spend in cents. Empty buckets are filled with zeros so a client charts a continuous line.  The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series.
         * @param {string} [range] Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
         * @param {string} [start] Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
         * @param {string} [end] End is the exclusive upper bound of a custom window, RFC3339. Requires start.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1AnalyticsTimeseries(range?: string, start?: string, end?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudTimeseries>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1AnalyticsTimeseries(range, start, end, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AnalyticsApi.cloudGetV1AnalyticsTimeseries']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Top returns the caller org\'s ranked lenses for one window, five of them at once. models ranks LLM models by spend and is always real; products ranks commerce orders by revenue; topPages ranks requested paths, topReferrers the external referrer domains (\"(direct)\" for a missing or same-origin one) and topSources the utm_source campaigns (\"(none)\" when absent), each by pageviews. Every lens carries each row\'s share of the in-window total, so a top-N honestly shows the long tail.  The four event lenses report available=false rather than fabricating zeros when the product-event table holds nothing yet. The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Top returns the caller org\'s ranked lenses for one window, five of them at once.
         * @param {string} [range] Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
         * @param {string} [start] Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
         * @param {string} [end] End is the exclusive upper bound of a custom window, RFC3339. Requires start.
         * @param {number} [limit] Limit bounds every ranked lens in the response. Default 10, maximum 100; a value at or below zero, or one that is not a number, takes the default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1AnalyticsTop(range?: string, start?: string, end?: string, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudTop>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1AnalyticsTop(range, start, end, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AnalyticsApi.cloudGetV1AnalyticsTop']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * 
         * @summary Get query analytics
         * @param {string} zone 
         * @param {string} [from] 
         * @param {string} [to] 
         * @param {DnsGetZoneAnalyticsGranularityEnum} [granularity] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async dnsGetZoneAnalytics(zone: string, from?: string, to?: string, granularity?: DnsGetZoneAnalyticsGranularityEnum, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<DnsQueryAnalytics>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.dnsGetZoneAnalytics(zone, from, to, granularity, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['AnalyticsApi.dnsGetZoneAnalytics']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * AnalyticsApi - factory interface
 * @export
 */
export const AnalyticsApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = AnalyticsApiFp(configuration)
    return {
        /**
         * Reports the analytics subsystem\'s own liveness in BOTH directions: `plane` is the event plane it WRITES (the bus and the JetStream stream every accepted event is published to, both named in the report), and `datastore` is the warehouse it READS, with each read lens\'s table reported as it is provisioned (the LLM usage ledger and the product-event table).  EITHER ONE DOWN IS A 503, and the report says WHICH — they are probed independently and never collapse into a single bit. This endpoint used to report the read half only, and answered 200/ok while every POST /v1/event failed on a stream that could not bind: a total ingest outage behind a green probe. A readiness gate wired here now gates on the write path too.  `plane.ready` IS A REAL PROBE and walks the ingest path itself — the same connection and the same stream a publish uses — so it cannot answer ready while a publish would 503. `plane.reason` carries the plane\'s own error text when it is false.  `datastore` IS NOT PROBED WITH A QUERY. It is the state of the process\'s own shared client — established, and not since closed — so a warehouse accepting connections and failing reads still reports true. Degraded CARRIES the report (status, the failing half, reason) as its body rather than an error envelope, so a gate reads the cause off the same object it got at 200.  A MISSING LENS TABLE IS NOT A FAILURE and never moves the status: a lens reported available:false answers honest-empty rather than erroring, so a fresh deployment whose collector has not emitted yet is legitimately 200 with the product-event lens unavailable. The lens block is reported whenever the warehouse is REACHABLE — including on a report degraded by the plane, where the tables genuinely were probed — and is absent only when the warehouse is not, having nothing to say about tables it could not reach.  Unauthenticated on purpose — liveness has to be probe-able — and it reads NO tenant data: table existence and stream presence only, never a row and never an event.
         * @summary Whether the event plane can take a write and the warehouse can answer a read
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsHealth(options?: RawAxiosRequestConfig): AxiosPromise<CloudHealthReport> {
            return localVarFp.cloudGetV1AnalyticsHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Overview returns the caller org\'s analytics KPIs for one time window. Three lenses over one warehouse: llm is the live per-org LLM usage ledger (requests, tokens, spend, models, providers, errors) and is always real; web (pageviews, visitors, sessions) and commerce (orders, revenue, AOV) read the product-event table and report available=false rather than fabricating zeros when it holds nothing yet.  The org is the validated principal\'s — never a parameter — so a caller can only ever read its own tenant. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Overview returns the caller org\'s analytics KPIs for one time window.
         * @param {AnalyticsApiCloudGetV1AnalyticsOverviewRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsOverview(requestParameters: AnalyticsApiCloudGetV1AnalyticsOverviewRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudOverview> {
            return localVarFp.cloudGetV1AnalyticsOverview(requestParameters.range, requestParameters.start, requestParameters.end, options).then((request) => request(axios, basePath));
        },
        /**
         * Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series. One point per hour or per day — the bucket the window implies, 24h giving hours and 7d/30d giving days — carrying requests, total tokens and spend in cents. Empty buckets are filled with zeros so a client charts a continuous line.  The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series.
         * @param {AnalyticsApiCloudGetV1AnalyticsTimeseriesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsTimeseries(requestParameters: AnalyticsApiCloudGetV1AnalyticsTimeseriesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudTimeseries> {
            return localVarFp.cloudGetV1AnalyticsTimeseries(requestParameters.range, requestParameters.start, requestParameters.end, options).then((request) => request(axios, basePath));
        },
        /**
         * Top returns the caller org\'s ranked lenses for one window, five of them at once. models ranks LLM models by spend and is always real; products ranks commerce orders by revenue; topPages ranks requested paths, topReferrers the external referrer domains (\"(direct)\" for a missing or same-origin one) and topSources the utm_source campaigns (\"(none)\" when absent), each by pageviews. Every lens carries each row\'s share of the in-window total, so a top-N honestly shows the long tail.  The four event lenses report available=false rather than fabricating zeros when the product-event table holds nothing yet. The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
         * @summary Top returns the caller org\'s ranked lenses for one window, five of them at once.
         * @param {AnalyticsApiCloudGetV1AnalyticsTopRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1AnalyticsTop(requestParameters: AnalyticsApiCloudGetV1AnalyticsTopRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudTop> {
            return localVarFp.cloudGetV1AnalyticsTop(requestParameters.range, requestParameters.start, requestParameters.end, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * 
         * @summary Get query analytics
         * @param {AnalyticsApiDnsGetZoneAnalyticsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        dnsGetZoneAnalytics(requestParameters: AnalyticsApiDnsGetZoneAnalyticsRequest, options?: RawAxiosRequestConfig): AxiosPromise<DnsQueryAnalytics> {
            return localVarFp.dnsGetZoneAnalytics(requestParameters.zone, requestParameters.from, requestParameters.to, requestParameters.granularity, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudGetV1AnalyticsOverview operation in AnalyticsApi.
 * @export
 * @interface AnalyticsApiCloudGetV1AnalyticsOverviewRequest
 */
export interface AnalyticsApiCloudGetV1AnalyticsOverviewRequest {
    /**
     * Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsOverview
     */
    readonly range?: string

    /**
     * Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsOverview
     */
    readonly start?: string

    /**
     * End is the exclusive upper bound of a custom window, RFC3339. Requires start.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsOverview
     */
    readonly end?: string
}

/**
 * Request parameters for cloudGetV1AnalyticsTimeseries operation in AnalyticsApi.
 * @export
 * @interface AnalyticsApiCloudGetV1AnalyticsTimeseriesRequest
 */
export interface AnalyticsApiCloudGetV1AnalyticsTimeseriesRequest {
    /**
     * Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsTimeseries
     */
    readonly range?: string

    /**
     * Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsTimeseries
     */
    readonly start?: string

    /**
     * End is the exclusive upper bound of a custom window, RFC3339. Requires start.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsTimeseries
     */
    readonly end?: string
}

/**
 * Request parameters for cloudGetV1AnalyticsTop operation in AnalyticsApi.
 * @export
 * @interface AnalyticsApiCloudGetV1AnalyticsTopRequest
 */
export interface AnalyticsApiCloudGetV1AnalyticsTopRequest {
    /**
     * Range is a relative window: 24h, 7d or 30d. Default 24h. Ignored when both start and end are given. An unknown value is a 400.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsTop
     */
    readonly range?: string

    /**
     * Start is the inclusive lower bound of a custom window, RFC3339. Requires end.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsTop
     */
    readonly start?: string

    /**
     * End is the exclusive upper bound of a custom window, RFC3339. Requires start.
     * @type {string}
     * @memberof AnalyticsApiCloudGetV1AnalyticsTop
     */
    readonly end?: string

    /**
     * Limit bounds every ranked lens in the response. Default 10, maximum 100; a value at or below zero, or one that is not a number, takes the default.
     * @type {number}
     * @memberof AnalyticsApiCloudGetV1AnalyticsTop
     */
    readonly limit?: number
}

/**
 * Request parameters for dnsGetZoneAnalytics operation in AnalyticsApi.
 * @export
 * @interface AnalyticsApiDnsGetZoneAnalyticsRequest
 */
export interface AnalyticsApiDnsGetZoneAnalyticsRequest {
    /**
     * 
     * @type {string}
     * @memberof AnalyticsApiDnsGetZoneAnalytics
     */
    readonly zone: string

    /**
     * 
     * @type {string}
     * @memberof AnalyticsApiDnsGetZoneAnalytics
     */
    readonly from?: string

    /**
     * 
     * @type {string}
     * @memberof AnalyticsApiDnsGetZoneAnalytics
     */
    readonly to?: string

    /**
     * 
     * @type {'hour' | 'day' | 'week'}
     * @memberof AnalyticsApiDnsGetZoneAnalytics
     */
    readonly granularity?: DnsGetZoneAnalyticsGranularityEnum
}

/**
 * AnalyticsApi - object-oriented interface
 * @export
 * @class AnalyticsApi
 * @extends {BaseAPI}
 */
export class AnalyticsApi extends BaseAPI {
    /**
     * Reports the analytics subsystem\'s own liveness in BOTH directions: `plane` is the event plane it WRITES (the bus and the JetStream stream every accepted event is published to, both named in the report), and `datastore` is the warehouse it READS, with each read lens\'s table reported as it is provisioned (the LLM usage ledger and the product-event table).  EITHER ONE DOWN IS A 503, and the report says WHICH — they are probed independently and never collapse into a single bit. This endpoint used to report the read half only, and answered 200/ok while every POST /v1/event failed on a stream that could not bind: a total ingest outage behind a green probe. A readiness gate wired here now gates on the write path too.  `plane.ready` IS A REAL PROBE and walks the ingest path itself — the same connection and the same stream a publish uses — so it cannot answer ready while a publish would 503. `plane.reason` carries the plane\'s own error text when it is false.  `datastore` IS NOT PROBED WITH A QUERY. It is the state of the process\'s own shared client — established, and not since closed — so a warehouse accepting connections and failing reads still reports true. Degraded CARRIES the report (status, the failing half, reason) as its body rather than an error envelope, so a gate reads the cause off the same object it got at 200.  A MISSING LENS TABLE IS NOT A FAILURE and never moves the status: a lens reported available:false answers honest-empty rather than erroring, so a fresh deployment whose collector has not emitted yet is legitimately 200 with the product-event lens unavailable. The lens block is reported whenever the warehouse is REACHABLE — including on a report degraded by the plane, where the tables genuinely were probed — and is absent only when the warehouse is not, having nothing to say about tables it could not reach.  Unauthenticated on purpose — liveness has to be probe-able — and it reads NO tenant data: table existence and stream presence only, never a row and never an event.
     * @summary Whether the event plane can take a write and the warehouse can answer a read
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApi
     */
    public cloudGetV1AnalyticsHealth(options?: RawAxiosRequestConfig) {
        return AnalyticsApiFp(this.configuration).cloudGetV1AnalyticsHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Overview returns the caller org\'s analytics KPIs for one time window. Three lenses over one warehouse: llm is the live per-org LLM usage ledger (requests, tokens, spend, models, providers, errors) and is always real; web (pageviews, visitors, sessions) and commerce (orders, revenue, AOV) read the product-event table and report available=false rather than fabricating zeros when it holds nothing yet.  The org is the validated principal\'s — never a parameter — so a caller can only ever read its own tenant. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
     * @summary Overview returns the caller org\'s analytics KPIs for one time window.
     * @param {AnalyticsApiCloudGetV1AnalyticsOverviewRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApi
     */
    public cloudGetV1AnalyticsOverview(requestParameters: AnalyticsApiCloudGetV1AnalyticsOverviewRequest = {}, options?: RawAxiosRequestConfig) {
        return AnalyticsApiFp(this.configuration).cloudGetV1AnalyticsOverview(requestParameters.range, requestParameters.start, requestParameters.end, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series. One point per hour or per day — the bucket the window implies, 24h giving hours and 7d/30d giving days — carrying requests, total tokens and spend in cents. Empty buckets are filled with zeros so a client charts a continuous line.  The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
     * @summary Timeseries returns the caller org\'s LLM usage over time as an evenly-spaced series.
     * @param {AnalyticsApiCloudGetV1AnalyticsTimeseriesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApi
     */
    public cloudGetV1AnalyticsTimeseries(requestParameters: AnalyticsApiCloudGetV1AnalyticsTimeseriesRequest = {}, options?: RawAxiosRequestConfig) {
        return AnalyticsApiFp(this.configuration).cloudGetV1AnalyticsTimeseries(requestParameters.range, requestParameters.start, requestParameters.end, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Top returns the caller org\'s ranked lenses for one window, five of them at once. models ranks LLM models by spend and is always real; products ranks commerce orders by revenue; topPages ranks requested paths, topReferrers the external referrer domains (\"(direct)\" for a missing or same-origin one) and topSources the utm_source campaigns (\"(none)\" when absent), each by pageviews. Every lens carries each row\'s share of the in-window total, so a top-N honestly shows the long tail.  The four event lenses report available=false rather than fabricating zeros when the product-event table holds nothing yet. The org is the validated principal\'s — never a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
     * @summary Top returns the caller org\'s ranked lenses for one window, five of them at once.
     * @param {AnalyticsApiCloudGetV1AnalyticsTopRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApi
     */
    public cloudGetV1AnalyticsTop(requestParameters: AnalyticsApiCloudGetV1AnalyticsTopRequest = {}, options?: RawAxiosRequestConfig) {
        return AnalyticsApiFp(this.configuration).cloudGetV1AnalyticsTop(requestParameters.range, requestParameters.start, requestParameters.end, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * 
     * @summary Get query analytics
     * @param {AnalyticsApiDnsGetZoneAnalyticsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof AnalyticsApi
     */
    public dnsGetZoneAnalytics(requestParameters: AnalyticsApiDnsGetZoneAnalyticsRequest, options?: RawAxiosRequestConfig) {
        return AnalyticsApiFp(this.configuration).dnsGetZoneAnalytics(requestParameters.zone, requestParameters.from, requestParameters.to, requestParameters.granularity, options).then((request) => request(this.axios, this.basePath));
    }
}

/**
 * @export
 */
export const DnsGetZoneAnalyticsGranularityEnum = {
    Hour: 'hour',
    Day: 'day',
    Week: 'week'
} as const;
export type DnsGetZoneAnalyticsGranularityEnum = typeof DnsGetZoneAnalyticsGranularityEnum[keyof typeof DnsGetZoneAnalyticsGranularityEnum];
