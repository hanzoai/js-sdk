/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudFleetBoard } from '../models';
// @ts-ignore
import type { CloudJobCancel } from '../models';
// @ts-ignore
import type { CloudJobCanceled } from '../models';
// @ts-ignore
import type { CloudJobList } from '../models';
// @ts-ignore
import type { CloudSampleAccepted } from '../models';
// @ts-ignore
import type { CloudSampleIngest } from '../models';
// @ts-ignore
import type { CloudSampleList } from '../models';
// @ts-ignore
import type { CloudWorkerList } from '../models';
/**
 * FleetApi - axios parameter creator
 * @export
 */
export const FleetApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Cancels a queued or running render in the caller\'s org. The engine cancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in another tenant\'s shard is 404, exactly like one that never existed. An already-finished job is 409.
         * @summary Cancels a queued or running render in the caller\'s org.
         * @param {string} id ID is the job (activity) id, from the URL path.
         * @param {CloudJobCancel} cloudJobCancel 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudCancelFleetJob: async (id: string, cloudJobCancel: CloudJobCancel, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudCancelFleetJob', 'id', id)
            // verify required parameter 'cloudJobCancel' is not null or undefined
            assertParamExists('cloudCancelFleetJob', 'cloudJobCancel', cloudJobCancel)
            const localVarPath = `/v1/fleet/jobs/{id}/cancel`
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudJobCancel, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.  A unit with a live snapshot of its own keeps it; the rest are overlaid from the utilization series, and only when the sample agrees about the SOURCE — two planes could mint the same unit id, and a board must never show one machine\'s load on another\'s row. BYO GPU units also carry their gpu-jobs queue depth. Every source is folded in independently: a broken one costs its own rows and nothing else.
         * @summary Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleet: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/fleet`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.  A job whose worker died — STARTED with an elapsed lease and not yet reclaimed — reads \"stalled\", not \"running\". Fail-soft: an unavailable tasks engine yields an empty queue rather than an error.
         * @summary Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.
         * @param {string} [gpu] GPU selects one node\&#39;s lane: jobs TARGETED at it (gpu:&lt;node&gt;) or CLAIMED by it. The literal \&quot;shared\&quot; selects the any-GPU lane — no target, no claimant. Matched case-insensitively.
         * @param {string} [status] Status selects one lifecycle state: queued, running, stalled, completed, failed or canceled.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleetJobs: async (gpu?: string, status?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/fleet/jobs`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (gpu !== undefined) {
                localVarQueryParameter['gpu'] = gpu;
            }

            if (status !== undefined) {
                localVarQueryParameter['status'] = status;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the caller org\'s utilization series, oldest first.  A rejected narrower is a 400 carrying its own reason (the vocabulary is ours and safe to echo); a warehouse failure is logged and answered 503 \"unavailable\", because a chart that silently reads \"no load\" when the truth is \"we cannot tell\" is worse than one that says so. An ABSENT warehouse is different again: it returns an empty series, which renders honestly as \"no samples yet\".
         * @summary Returns the caller org\'s utilization series, oldest first.
         * @param {string} [unit] Unit selects one compute unit\&#39;s series by its source-local id.
         * @param {string} [source] Source selects one plane: \&quot;agent\&quot;, \&quot;byo\&quot; or \&quot;visor\&quot;.
         * @param {string} [range] Range is the lookback window (e.g. \&quot;1h\&quot;, \&quot;24h\&quot;, \&quot;7d\&quot;); empty takes the warehouse default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleetSamples: async (unit?: string, source?: string, range?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/fleet/samples`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (unit !== undefined) {
                localVarQueryParameter['unit'] = unit;
            }

            if (source !== undefined) {
                localVarQueryParameter['source'] = source;
            }

            if (range !== undefined) {
                localVarQueryParameter['range'] = range;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself. The Machines and GPUs pages fold the same data into their normalized shapes; this is the canonical raw list a fleet view (or the CLI\'s `status`) reads.
         * @summary Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleetWorkers: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/fleet/workers`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays. The org is the validated principal and source/kind are fixed server-side, so a worker names only its own metrics — never another tenant or another source. Answers 202: the warehouse write is DETACHED (its own bounded context, never in the response path), so a slow or absent warehouse cannot stall a heartbeat.
         * @summary Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays.
         * @param {CloudSampleIngest} cloudSampleIngest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudRecordFleetSample: async (cloudSampleIngest: CloudSampleIngest, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'cloudSampleIngest' is not null or undefined
            assertParamExists('cloudRecordFleetSample', 'cloudSampleIngest', cloudSampleIngest)
            const localVarPath = `/v1/fleet/samples`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(cloudSampleIngest, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * FleetApi - functional programming interface
 * @export
 */
export const FleetApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = FleetApiAxiosParamCreator(configuration)
    return {
        /**
         * Cancels a queued or running render in the caller\'s org. The engine cancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in another tenant\'s shard is 404, exactly like one that never existed. An already-finished job is 409.
         * @summary Cancels a queued or running render in the caller\'s org.
         * @param {string} id ID is the job (activity) id, from the URL path.
         * @param {CloudJobCancel} cloudJobCancel 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudCancelFleetJob(id: string, cloudJobCancel: CloudJobCancel, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudJobCanceled>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudCancelFleetJob(id, cloudJobCancel, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['FleetApi.cloudCancelFleetJob']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.  A unit with a live snapshot of its own keeps it; the rest are overlaid from the utilization series, and only when the sample agrees about the SOURCE — two planes could mint the same unit id, and a board must never show one machine\'s load on another\'s row. BYO GPU units also carry their gpu-jobs queue depth. Every source is folded in independently: a broken one costs its own rows and nothing else.
         * @summary Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudListFleet(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudFleetBoard>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudListFleet(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['FleetApi.cloudListFleet']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.  A job whose worker died — STARTED with an elapsed lease and not yet reclaimed — reads \"stalled\", not \"running\". Fail-soft: an unavailable tasks engine yields an empty queue rather than an error.
         * @summary Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.
         * @param {string} [gpu] GPU selects one node\&#39;s lane: jobs TARGETED at it (gpu:&lt;node&gt;) or CLAIMED by it. The literal \&quot;shared\&quot; selects the any-GPU lane — no target, no claimant. Matched case-insensitively.
         * @param {string} [status] Status selects one lifecycle state: queued, running, stalled, completed, failed or canceled.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudListFleetJobs(gpu?: string, status?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudJobList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudListFleetJobs(gpu, status, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['FleetApi.cloudListFleetJobs']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the caller org\'s utilization series, oldest first.  A rejected narrower is a 400 carrying its own reason (the vocabulary is ours and safe to echo); a warehouse failure is logged and answered 503 \"unavailable\", because a chart that silently reads \"no load\" when the truth is \"we cannot tell\" is worse than one that says so. An ABSENT warehouse is different again: it returns an empty series, which renders honestly as \"no samples yet\".
         * @summary Returns the caller org\'s utilization series, oldest first.
         * @param {string} [unit] Unit selects one compute unit\&#39;s series by its source-local id.
         * @param {string} [source] Source selects one plane: \&quot;agent\&quot;, \&quot;byo\&quot; or \&quot;visor\&quot;.
         * @param {string} [range] Range is the lookback window (e.g. \&quot;1h\&quot;, \&quot;24h\&quot;, \&quot;7d\&quot;); empty takes the warehouse default.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudListFleetSamples(unit?: string, source?: string, range?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudSampleList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudListFleetSamples(unit, source, range, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['FleetApi.cloudListFleetSamples']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself. The Machines and GPUs pages fold the same data into their normalized shapes; this is the canonical raw list a fleet view (or the CLI\'s `status`) reads.
         * @summary Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudListFleetWorkers(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudWorkerList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudListFleetWorkers(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['FleetApi.cloudListFleetWorkers']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays. The org is the validated principal and source/kind are fixed server-side, so a worker names only its own metrics — never another tenant or another source. Answers 202: the warehouse write is DETACHED (its own bounded context, never in the response path), so a slow or absent warehouse cannot stall a heartbeat.
         * @summary Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays.
         * @param {CloudSampleIngest} cloudSampleIngest 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudRecordFleetSample(cloudSampleIngest: CloudSampleIngest, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudSampleAccepted>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudRecordFleetSample(cloudSampleIngest, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['FleetApi.cloudRecordFleetSample']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * FleetApi - factory interface
 * @export
 */
export const FleetApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = FleetApiFp(configuration)
    return {
        /**
         * Cancels a queued or running render in the caller\'s org. The engine cancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in another tenant\'s shard is 404, exactly like one that never existed. An already-finished job is 409.
         * @summary Cancels a queued or running render in the caller\'s org.
         * @param {FleetApiCloudCancelFleetJobRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudCancelFleetJob(requestParameters: FleetApiCloudCancelFleetJobRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudJobCanceled> {
            return localVarFp.cloudCancelFleetJob(requestParameters.id, requestParameters.cloudJobCancel, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.  A unit with a live snapshot of its own keeps it; the rest are overlaid from the utilization series, and only when the sample agrees about the SOURCE — two planes could mint the same unit id, and a board must never show one machine\'s load on another\'s row. BYO GPU units also carry their gpu-jobs queue depth. Every source is folded in independently: a broken one costs its own rows and nothing else.
         * @summary Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleet(options?: RawAxiosRequestConfig): AxiosPromise<CloudFleetBoard> {
            return localVarFp.cloudListFleet(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.  A job whose worker died — STARTED with an elapsed lease and not yet reclaimed — reads \"stalled\", not \"running\". Fail-soft: an unavailable tasks engine yields an empty queue rather than an error.
         * @summary Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.
         * @param {FleetApiCloudListFleetJobsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleetJobs(requestParameters: FleetApiCloudListFleetJobsRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudJobList> {
            return localVarFp.cloudListFleetJobs(requestParameters.gpu, requestParameters.status, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the caller org\'s utilization series, oldest first.  A rejected narrower is a 400 carrying its own reason (the vocabulary is ours and safe to echo); a warehouse failure is logged and answered 503 \"unavailable\", because a chart that silently reads \"no load\" when the truth is \"we cannot tell\" is worse than one that says so. An ABSENT warehouse is different again: it returns an empty series, which renders honestly as \"no samples yet\".
         * @summary Returns the caller org\'s utilization series, oldest first.
         * @param {FleetApiCloudListFleetSamplesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleetSamples(requestParameters: FleetApiCloudListFleetSamplesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<CloudSampleList> {
            return localVarFp.cloudListFleetSamples(requestParameters.unit, requestParameters.source, requestParameters.range, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself. The Machines and GPUs pages fold the same data into their normalized shapes; this is the canonical raw list a fleet view (or the CLI\'s `status`) reads.
         * @summary Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudListFleetWorkers(options?: RawAxiosRequestConfig): AxiosPromise<CloudWorkerList> {
            return localVarFp.cloudListFleetWorkers(options).then((request) => request(axios, basePath));
        },
        /**
         * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays. The org is the validated principal and source/kind are fixed server-side, so a worker names only its own metrics — never another tenant or another source. Answers 202: the warehouse write is DETACHED (its own bounded context, never in the response path), so a slow or absent warehouse cannot stall a heartbeat.
         * @summary Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays.
         * @param {FleetApiCloudRecordFleetSampleRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudRecordFleetSample(requestParameters: FleetApiCloudRecordFleetSampleRequest, options?: RawAxiosRequestConfig): AxiosPromise<CloudSampleAccepted> {
            return localVarFp.cloudRecordFleetSample(requestParameters.cloudSampleIngest, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudCancelFleetJob operation in FleetApi.
 * @export
 * @interface FleetApiCloudCancelFleetJobRequest
 */
export interface FleetApiCloudCancelFleetJobRequest {
    /**
     * ID is the job (activity) id, from the URL path.
     * @type {string}
     * @memberof FleetApiCloudCancelFleetJob
     */
    readonly id: string

    /**
     * 
     * @type {CloudJobCancel}
     * @memberof FleetApiCloudCancelFleetJob
     */
    readonly cloudJobCancel: CloudJobCancel
}

/**
 * Request parameters for cloudListFleetJobs operation in FleetApi.
 * @export
 * @interface FleetApiCloudListFleetJobsRequest
 */
export interface FleetApiCloudListFleetJobsRequest {
    /**
     * GPU selects one node\&#39;s lane: jobs TARGETED at it (gpu:&lt;node&gt;) or CLAIMED by it. The literal \&quot;shared\&quot; selects the any-GPU lane — no target, no claimant. Matched case-insensitively.
     * @type {string}
     * @memberof FleetApiCloudListFleetJobs
     */
    readonly gpu?: string

    /**
     * Status selects one lifecycle state: queued, running, stalled, completed, failed or canceled.
     * @type {string}
     * @memberof FleetApiCloudListFleetJobs
     */
    readonly status?: string
}

/**
 * Request parameters for cloudListFleetSamples operation in FleetApi.
 * @export
 * @interface FleetApiCloudListFleetSamplesRequest
 */
export interface FleetApiCloudListFleetSamplesRequest {
    /**
     * Unit selects one compute unit\&#39;s series by its source-local id.
     * @type {string}
     * @memberof FleetApiCloudListFleetSamples
     */
    readonly unit?: string

    /**
     * Source selects one plane: \&quot;agent\&quot;, \&quot;byo\&quot; or \&quot;visor\&quot;.
     * @type {string}
     * @memberof FleetApiCloudListFleetSamples
     */
    readonly source?: string

    /**
     * Range is the lookback window (e.g. \&quot;1h\&quot;, \&quot;24h\&quot;, \&quot;7d\&quot;); empty takes the warehouse default.
     * @type {string}
     * @memberof FleetApiCloudListFleetSamples
     */
    readonly range?: string
}

/**
 * Request parameters for cloudRecordFleetSample operation in FleetApi.
 * @export
 * @interface FleetApiCloudRecordFleetSampleRequest
 */
export interface FleetApiCloudRecordFleetSampleRequest {
    /**
     * 
     * @type {CloudSampleIngest}
     * @memberof FleetApiCloudRecordFleetSample
     */
    readonly cloudSampleIngest: CloudSampleIngest
}

/**
 * FleetApi - object-oriented interface
 * @export
 * @class FleetApi
 * @extends {BaseAPI}
 */
export class FleetApi extends BaseAPI {
    /**
     * Cancels a queued or running render in the caller\'s org. The engine cancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in another tenant\'s shard is 404, exactly like one that never existed. An already-finished job is 409.
     * @summary Cancels a queued or running render in the caller\'s org.
     * @param {FleetApiCloudCancelFleetJobRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof FleetApi
     */
    public cloudCancelFleetJob(requestParameters: FleetApiCloudCancelFleetJobRequest, options?: RawAxiosRequestConfig) {
        return FleetApiFp(this.configuration).cloudCancelFleetJob(requestParameters.id, requestParameters.cloudJobCancel, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.  A unit with a live snapshot of its own keeps it; the rest are overlaid from the utilization series, and only when the sample agrees about the SOURCE — two planes could mint the same unit id, and a board must never show one machine\'s load on another\'s row. BYO GPU units also carry their gpu-jobs queue depth. Every source is folded in independently: a broken one costs its own rows and nothing else.
     * @summary Returns every compute unit the caller\'s org has, from every source, each carrying its latest utilization: agent run-targets, the BYO machines that dialed in, attached BYO clusters and Visor-provisioned machines.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof FleetApi
     */
    public cloudListFleet(options?: RawAxiosRequestConfig) {
        return FleetApiFp(this.configuration).cloudListFleet(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.  A job whose worker died — STARTED with an elapsed lease and not yet reclaimed — reads \"stalled\", not \"running\". Fail-soft: an unavailable tasks engine yields an empty queue rather than an error.
     * @summary Returns the caller org\'s gpu-jobs render queue, each row tagged with the GPU it targets (empty = the shared any-GPU lane) and the node claiming it, optionally narrowed to one GPU\'s queue and/or one status.
     * @param {FleetApiCloudListFleetJobsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof FleetApi
     */
    public cloudListFleetJobs(requestParameters: FleetApiCloudListFleetJobsRequest = {}, options?: RawAxiosRequestConfig) {
        return FleetApiFp(this.configuration).cloudListFleetJobs(requestParameters.gpu, requestParameters.status, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the caller org\'s utilization series, oldest first.  A rejected narrower is a 400 carrying its own reason (the vocabulary is ours and safe to echo); a warehouse failure is logged and answered 503 \"unavailable\", because a chart that silently reads \"no load\" when the truth is \"we cannot tell\" is worse than one that says so. An ABSENT warehouse is different again: it returns an empty series, which renders honestly as \"no samples yet\".
     * @summary Returns the caller org\'s utilization series, oldest first.
     * @param {FleetApiCloudListFleetSamplesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof FleetApi
     */
    public cloudListFleetSamples(requestParameters: FleetApiCloudListFleetSamplesRequest = {}, options?: RawAxiosRequestConfig) {
        return FleetApiFp(this.configuration).cloudListFleetSamples(requestParameters.unit, requestParameters.source, requestParameters.range, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself. The Machines and GPUs pages fold the same data into their normalized shapes; this is the canonical raw list a fleet view (or the CLI\'s `status`) reads.
     * @summary Returns the caller org\'s BYO machines — the ones that dialed in via `hanzo link` — with everything each host reported about itself.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof FleetApi
     */
    public cloudListFleetWorkers(options?: RawAxiosRequestConfig) {
        return FleetApiFp(this.configuration).cloudListFleetWorkers(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays. The org is the validated principal and source/kind are fixed server-side, so a worker names only its own metrics — never another tenant or another source. Answers 202: the warehouse write is DETACHED (its own bounded context, never in the response path), so a slow or absent warehouse cannot stall a heartbeat.
     * @summary Records a BYO worker\'s live GPU utilization into the SAME series the fleet board overlays.
     * @param {FleetApiCloudRecordFleetSampleRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof FleetApi
     */
    public cloudRecordFleetSample(requestParameters: FleetApiCloudRecordFleetSampleRequest, options?: RawAxiosRequestConfig) {
        return FleetApiFp(this.configuration).cloudRecordFleetSample(requestParameters.cloudSampleIngest, options).then((request) => request(this.axios, this.basePath));
    }
}

