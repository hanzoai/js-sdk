/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { EdgeState } from '../models';
/**
 * EdgeApi - axios parameter creator
 * @export
 */
export const EdgeApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * health reports whether a publish reaches readers, rather than whether it was accepted. Those are different questions and only the second one was ever visible.  It asks the edge and nothing else. There is no live call to the provider here: Configured is a local fact, it is the fact that was missing, and a health check that spends a third-party API call is one an operator learns not to run.
         * @summary health reports whether a publish reaches readers, rather than whether it was accepted.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getEdge: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/edge`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * EdgeApi - functional programming interface
 * @export
 */
export const EdgeApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = EdgeApiAxiosParamCreator(configuration)
    return {
        /**
         * health reports whether a publish reaches readers, rather than whether it was accepted. Those are different questions and only the second one was ever visible.  It asks the edge and nothing else. There is no live call to the provider here: Configured is a local fact, it is the fact that was missing, and a health check that spends a third-party API call is one an operator learns not to run.
         * @summary health reports whether a publish reaches readers, rather than whether it was accepted.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getEdge(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<EdgeState>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getEdge(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['EdgeApi.getEdge']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * EdgeApi - factory interface
 * @export
 */
export const EdgeApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = EdgeApiFp(configuration)
    return {
        /**
         * health reports whether a publish reaches readers, rather than whether it was accepted. Those are different questions and only the second one was ever visible.  It asks the edge and nothing else. There is no live call to the provider here: Configured is a local fact, it is the fact that was missing, and a health check that spends a third-party API call is one an operator learns not to run.
         * @summary health reports whether a publish reaches readers, rather than whether it was accepted.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getEdge(options?: RawAxiosRequestConfig): AxiosPromise<EdgeState> {
            return localVarFp.getEdge(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * EdgeApi - object-oriented interface
 * @export
 * @class EdgeApi
 * @extends {BaseAPI}
 */
export class EdgeApi extends BaseAPI {
    /**
     * health reports whether a publish reaches readers, rather than whether it was accepted. Those are different questions and only the second one was ever visible.  It asks the edge and nothing else. There is no live call to the provider here: Configured is a local fact, it is the fact that was missing, and a health check that spends a third-party API call is one an operator learns not to run.
     * @summary health reports whether a publish reaches readers, rather than whether it was accepted.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof EdgeApi
     */
    public getEdge(options?: RawAxiosRequestConfig) {
        return EdgeApiFp(this.configuration).getEdge(options).then((request) => request(this.axios, this.basePath));
    }
}

