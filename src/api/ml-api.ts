/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: 8.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { MlDataset } from '../models';
// @ts-ignore
import type { MlDatasetList } from '../models';
// @ts-ignore
import type { MlDatasetRows } from '../models';
// @ts-ignore
import type { MlDatasetSpec } from '../models';
// @ts-ignore
import type { MlDatasetVersions } from '../models';
// @ts-ignore
import type { MlDisposal } from '../models';
// @ts-ignore
import type { MlLineage } from '../models';
// @ts-ignore
import type { MlResource } from '../models';
// @ts-ignore
import type { MlResourceList } from '../models';
/**
 * MlApi - axios parameter creator
 * @export
 */
export const MlApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Deletes a deployed inference model. kserve owns the teardown: the InferenceService goes away and the serving deployment behind it follows, so the model stops answering predict calls. Answers 204, or 404 for a name the caller\'s org does not own.
         * @summary Deletes a deployed inference model.
         * @param {string} name Name is the resource to act on, taken from the path. Lower-cased and trimmed to the DNS-1123 label a CustomResource\&#39;s metadata.name must be.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1MlModelsName: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('deleteV1MlModelsName', 'name', name)
            const localVarPath = `/v1/ml/models/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reports whether the model-serving plane is genuinely usable: that the Kubernetes API answers, and that the InferenceService CRD is actually served by this cluster. It is a REAL probe, not status theatre — it makes a live call rather than reporting a flag set at boot.  200 only when everything checks out. Otherwise 503 CARRYING THE REPORT — which component failed, and the real error — and that body is the reason this is not a typed op: a typed op reaches a non-2xx by returning an error, and the envelope that produces would drop exactly the detail the probe exists to deliver.  It answers about the cluster, not about a tenant, so it takes no org and reveals no tenant data. A cluster with no kserve CRD reports degraded honestly rather than failing later at the first deploy.
         * @summary Whether model serving can actually work right now
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1MlHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/ml/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Lists the inference models deployed in the caller\'s org. Each entry carries the model\'s name, when Kubernetes admitted it, and kserve\'s live status — the spec is on the single-model read. An org that has deployed nothing gets an empty list.
         * @summary Lists the inference models deployed in the caller\'s org.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1MlModels: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/ml/models`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one deployed inference model. Its spec comes with it, and kserve\'s live status, which is where readiness and the serving address appear. A name the caller\'s org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s models.
         * @summary Returns one deployed inference model.
         * @param {string} name Name is the resource to act on, taken from the path. Lower-cased and trimmed to the DNS-1123 label a CustomResource\&#39;s metadata.name must be.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1MlModelsName: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('getV1MlModelsName', 'name', name)
            const localVarPath = `/v1/ml/models/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
         * @summary Declare the next version of a dataset
         * @param {MlDatasetSpec} mlDatasetSpec 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlCreateDataset: async (mlDatasetSpec: MlDatasetSpec, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'mlDatasetSpec' is not null or undefined
            assertParamExists('mlCreateDataset', 'mlDatasetSpec', mlDatasetSpec)
            const localVarPath = `/v1/ml/datasets`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(mlDatasetSpec, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
         * @summary Describe every version of one dataset
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDataset: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('mlDataset', 'name', name)
            const localVarPath = `/v1/ml/datasets/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
         * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to trace. Zero takes the newest published one.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDatasetLineage: async (name: string, version?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('mlDatasetLineage', 'name', name)
            const localVarPath = `/v1/ml/datasets/{name}/lineage`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (version !== undefined) {
                localVarQueryParameter['version'] = version;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
         * @summary List this org\'s datasets
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDatasets: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/ml/datasets`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
         * @summary Dispose of one dataset and every version of it
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDeleteDataset: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('mlDeleteDataset', 'name', name)
            const localVarPath = `/v1/ml/datasets/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
         * @summary Read a version\'s rows back, one page at a time
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to read. Zero takes the newest published one.
         * @param {string} [split] Split narrows to train, val or test. Empty reads every split.
         * @param {number} [offset] Offset is where the page starts, in the version\&#39;s own row order (by id, which is derived from the row and therefore stable forever).
         * @param {number} [limit] Limit is how many rows to return. Zero and anything above the plane\&#39;s bound take the bound.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlExportDataset: async (name: string, version?: number, split?: string, offset?: number, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('mlExportDataset', 'name', name)
            const localVarPath = `/v1/ml/datasets/{name}/export`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (version !== undefined) {
                localVarQueryParameter['version'] = version;
            }

            if (split !== undefined) {
                localVarQueryParameter['split'] = split;
            }

            if (offset !== undefined) {
                localVarQueryParameter['offset'] = offset;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
         * @summary Materialise the declared version into immutable rows
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlMaterializeDataset: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('mlMaterializeDataset', 'name', name)
            const localVarPath = `/v1/ml/datasets/{name}/materialize`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Applies a JSON merge patch to one of the caller org\'s deployed models and answers the updated resource — the way to change a model\'s image, replica count or resource requests without tearing the deployment down.  The body is relayed to Kubernetes VERBATIM. That is deliberate and it is why this route is not a typed op: re-encoding a merge patch changes what it means, because an integer that round-trips through a generic decoder comes back a float. Merge-patch semantics apply as written — a null removes a field, and a list is replaced whole rather than merged.  Scoped to the caller\'s own tenant namespace, resolved from the validated org and project; a name the caller\'s tenant does not hold is a 404, never another tenant\'s resource. An empty body is refused, and a patch Kubernetes rejects comes back 422 with its reason rather than being silently dropped.
         * @summary Change a deployed model in place
         * @param {string} name 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1MlModelsByName: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('patchV1MlModelsByName', 'name', name)
            const localVarPath = `/v1/ml/models/{name}`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Deploys a model into the caller\'s own tenant namespace and answers the created resource, 201. The spec is the kserve InferenceService spec, relayed as given, so anything kserve serves is deployable here without this layer knowing what it is.  THE BALANCE GATE RUNS FIRST, before a namespace or a resource exists, so an unfunded org cannot start GPU compute and then be billed for it. It fails CLOSED: a commerce that cannot be reached refuses rather than admits. The refusal carries the fleet\'s nested error body — the 402 shape a funded-balance client already parses — which is precisely why this route is not a typed op. On success the submission fee is debited from the caller org\'s own ledger, asynchronously and best-effort; ongoing GPU-hour cost is metered elsewhere.  The tenant namespace is derived from the VALIDATED org and project — never from a field — and the mapping is injective in both, so two tenants can never land in one namespace. An unvalidated caller is refused before any of that. The name must be a DNS-1123 label; a name already taken in the tenant\'s namespace is a 409.
         * @summary Deploy an inference model
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1MlModels: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/ml/models`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Sends the request body to the named model\'s predictor and answers the predictor\'s reply — its status code, its body bytes and its Content-Type, all unchanged. This is the inference call itself, not a description of one.  VERBATIM IS THE CONTRACT, and it is why this route is not a typed op: a model-side error has to surface as the model\'s own error, not as this layer\'s paraphrase of it. The body shape is the kserve v2 inference protocol\'s, which means the runtime decides it, not this API. The v2 model name defaults to the resource name — kserve\'s single-model convention — and a multi-model runtime selects one with the `model` query parameter.  A model that exists but has no serving address yet answers 503 \'not ready\' rather than a confusing connection error: deployed is not the same as serving. Scoped to the caller\'s own tenant namespace from the validated org and project, so a name another tenant owns is simply a 404. The predictor\'s response body is read up to a fixed ceiling.
         * @summary Run inference against one of your deployed models
         * @param {string} name 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1MlModelsByNamePredict: async (name: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'name' is not null or undefined
            assertParamExists('postV1MlModelsByNamePredict', 'name', name)
            const localVarPath = `/v1/ml/models/{name}/predict`
                .replace(`{${"name"}}`, encodeURIComponent(String(name)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * MlApi - functional programming interface
 * @export
 */
export const MlApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = MlApiAxiosParamCreator(configuration)
    return {
        /**
         * Deletes a deployed inference model. kserve owns the teardown: the InferenceService goes away and the serving deployment behind it follows, so the model stops answering predict calls. Answers 204, or 404 for a name the caller\'s org does not own.
         * @summary Deletes a deployed inference model.
         * @param {string} name Name is the resource to act on, taken from the path. Lower-cased and trimmed to the DNS-1123 label a CustomResource\&#39;s metadata.name must be.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteV1MlModelsName(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteV1MlModelsName(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.deleteV1MlModelsName']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reports whether the model-serving plane is genuinely usable: that the Kubernetes API answers, and that the InferenceService CRD is actually served by this cluster. It is a REAL probe, not status theatre — it makes a live call rather than reporting a flag set at boot.  200 only when everything checks out. Otherwise 503 CARRYING THE REPORT — which component failed, and the real error — and that body is the reason this is not a typed op: a typed op reaches a non-2xx by returning an error, and the envelope that produces would drop exactly the detail the probe exists to deliver.  It answers about the cluster, not about a tenant, so it takes no org and reveals no tenant data. A cluster with no kserve CRD reports degraded honestly rather than failing later at the first deploy.
         * @summary Whether model serving can actually work right now
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1MlHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1MlHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.getV1MlHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Lists the inference models deployed in the caller\'s org. Each entry carries the model\'s name, when Kubernetes admitted it, and kserve\'s live status — the spec is on the single-model read. An org that has deployed nothing gets an empty list.
         * @summary Lists the inference models deployed in the caller\'s org.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1MlModels(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlResourceList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1MlModels(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.getV1MlModels']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one deployed inference model. Its spec comes with it, and kserve\'s live status, which is where readiness and the serving address appear. A name the caller\'s org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s models.
         * @summary Returns one deployed inference model.
         * @param {string} name Name is the resource to act on, taken from the path. Lower-cased and trimmed to the DNS-1123 label a CustomResource\&#39;s metadata.name must be.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getV1MlModelsName(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlResource>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getV1MlModelsName(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.getV1MlModelsName']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
         * @summary Declare the next version of a dataset
         * @param {MlDatasetSpec} mlDatasetSpec 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlCreateDataset(mlDatasetSpec: MlDatasetSpec, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlDataset>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlCreateDataset(mlDatasetSpec, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.mlCreateDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
         * @summary Describe every version of one dataset
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlDataset(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlDatasetVersions>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlDataset(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.mlDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
         * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to trace. Zero takes the newest published one.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlDatasetLineage(name: string, version?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlLineage>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlDatasetLineage(name, version, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.mlDatasetLineage']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
         * @summary List this org\'s datasets
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlDatasets(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlDatasetList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlDatasets(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.mlDatasets']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
         * @summary Dispose of one dataset and every version of it
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlDeleteDataset(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlDisposal>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlDeleteDataset(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.mlDeleteDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
         * @summary Read a version\'s rows back, one page at a time
         * @param {string} name Name is the dataset, from the path.
         * @param {number} [version] Version is the version to read. Zero takes the newest published one.
         * @param {string} [split] Split narrows to train, val or test. Empty reads every split.
         * @param {number} [offset] Offset is where the page starts, in the version\&#39;s own row order (by id, which is derived from the row and therefore stable forever).
         * @param {number} [limit] Limit is how many rows to return. Zero and anything above the plane\&#39;s bound take the bound.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlExportDataset(name: string, version?: number, split?: string, offset?: number, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlDatasetRows>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlExportDataset(name, version, split, offset, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.mlExportDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
         * @summary Materialise the declared version into immutable rows
         * @param {string} name Name is the dataset, from the path.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async mlMaterializeDataset(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<MlDataset>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.mlMaterializeDataset(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.mlMaterializeDataset']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Applies a JSON merge patch to one of the caller org\'s deployed models and answers the updated resource — the way to change a model\'s image, replica count or resource requests without tearing the deployment down.  The body is relayed to Kubernetes VERBATIM. That is deliberate and it is why this route is not a typed op: re-encoding a merge patch changes what it means, because an integer that round-trips through a generic decoder comes back a float. Merge-patch semantics apply as written — a null removes a field, and a list is replaced whole rather than merged.  Scoped to the caller\'s own tenant namespace, resolved from the validated org and project; a name the caller\'s tenant does not hold is a 404, never another tenant\'s resource. An empty body is refused, and a patch Kubernetes rejects comes back 422 with its reason rather than being silently dropped.
         * @summary Change a deployed model in place
         * @param {string} name 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchV1MlModelsByName(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchV1MlModelsByName(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.patchV1MlModelsByName']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Deploys a model into the caller\'s own tenant namespace and answers the created resource, 201. The spec is the kserve InferenceService spec, relayed as given, so anything kserve serves is deployable here without this layer knowing what it is.  THE BALANCE GATE RUNS FIRST, before a namespace or a resource exists, so an unfunded org cannot start GPU compute and then be billed for it. It fails CLOSED: a commerce that cannot be reached refuses rather than admits. The refusal carries the fleet\'s nested error body — the 402 shape a funded-balance client already parses — which is precisely why this route is not a typed op. On success the submission fee is debited from the caller org\'s own ledger, asynchronously and best-effort; ongoing GPU-hour cost is metered elsewhere.  The tenant namespace is derived from the VALIDATED org and project — never from a field — and the mapping is injective in both, so two tenants can never land in one namespace. An unvalidated caller is refused before any of that. The name must be a DNS-1123 label; a name already taken in the tenant\'s namespace is a 409.
         * @summary Deploy an inference model
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1MlModels(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1MlModels(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.postV1MlModels']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Sends the request body to the named model\'s predictor and answers the predictor\'s reply — its status code, its body bytes and its Content-Type, all unchanged. This is the inference call itself, not a description of one.  VERBATIM IS THE CONTRACT, and it is why this route is not a typed op: a model-side error has to surface as the model\'s own error, not as this layer\'s paraphrase of it. The body shape is the kserve v2 inference protocol\'s, which means the runtime decides it, not this API. The v2 model name defaults to the resource name — kserve\'s single-model convention — and a multi-model runtime selects one with the `model` query parameter.  A model that exists but has no serving address yet answers 503 \'not ready\' rather than a confusing connection error: deployed is not the same as serving. Scoped to the caller\'s own tenant namespace from the validated org and project, so a name another tenant owns is simply a 404. The predictor\'s response body is read up to a fixed ceiling.
         * @summary Run inference against one of your deployed models
         * @param {string} name 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postV1MlModelsByNamePredict(name: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postV1MlModelsByNamePredict(name, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MlApi.postV1MlModelsByNamePredict']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * MlApi - factory interface
 * @export
 */
export const MlApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = MlApiFp(configuration)
    return {
        /**
         * Deletes a deployed inference model. kserve owns the teardown: the InferenceService goes away and the serving deployment behind it follows, so the model stops answering predict calls. Answers 204, or 404 for a name the caller\'s org does not own.
         * @summary Deletes a deployed inference model.
         * @param {MlApiDeleteV1MlModelsNameRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteV1MlModelsName(requestParameters: MlApiDeleteV1MlModelsNameRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteV1MlModelsName(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Reports whether the model-serving plane is genuinely usable: that the Kubernetes API answers, and that the InferenceService CRD is actually served by this cluster. It is a REAL probe, not status theatre — it makes a live call rather than reporting a flag set at boot.  200 only when everything checks out. Otherwise 503 CARRYING THE REPORT — which component failed, and the real error — and that body is the reason this is not a typed op: a typed op reaches a non-2xx by returning an error, and the envelope that produces would drop exactly the detail the probe exists to deliver.  It answers about the cluster, not about a tenant, so it takes no org and reveals no tenant data. A cluster with no kserve CRD reports degraded honestly rather than failing later at the first deploy.
         * @summary Whether model serving can actually work right now
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1MlHealth(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.getV1MlHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Lists the inference models deployed in the caller\'s org. Each entry carries the model\'s name, when Kubernetes admitted it, and kserve\'s live status — the spec is on the single-model read. An org that has deployed nothing gets an empty list.
         * @summary Lists the inference models deployed in the caller\'s org.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1MlModels(options?: RawAxiosRequestConfig): AxiosPromise<MlResourceList> {
            return localVarFp.getV1MlModels(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one deployed inference model. Its spec comes with it, and kserve\'s live status, which is where readiness and the serving address appear. A name the caller\'s org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s models.
         * @summary Returns one deployed inference model.
         * @param {MlApiGetV1MlModelsNameRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getV1MlModelsName(requestParameters: MlApiGetV1MlModelsNameRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlResource> {
            return localVarFp.getV1MlModelsName(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
         * @summary Declare the next version of a dataset
         * @param {MlApiMlCreateDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlCreateDataset(requestParameters: MlApiMlCreateDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlDataset> {
            return localVarFp.mlCreateDataset(requestParameters.mlDatasetSpec, options).then((request) => request(axios, basePath));
        },
        /**
         * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
         * @summary Describe every version of one dataset
         * @param {MlApiMlDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDataset(requestParameters: MlApiMlDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlDatasetVersions> {
            return localVarFp.mlDataset(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
         * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
         * @param {MlApiMlDatasetLineageRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDatasetLineage(requestParameters: MlApiMlDatasetLineageRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlLineage> {
            return localVarFp.mlDatasetLineage(requestParameters.name, requestParameters.version, options).then((request) => request(axios, basePath));
        },
        /**
         * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
         * @summary List this org\'s datasets
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDatasets(options?: RawAxiosRequestConfig): AxiosPromise<MlDatasetList> {
            return localVarFp.mlDatasets(options).then((request) => request(axios, basePath));
        },
        /**
         * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
         * @summary Dispose of one dataset and every version of it
         * @param {MlApiMlDeleteDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlDeleteDataset(requestParameters: MlApiMlDeleteDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlDisposal> {
            return localVarFp.mlDeleteDataset(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
         * @summary Read a version\'s rows back, one page at a time
         * @param {MlApiMlExportDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlExportDataset(requestParameters: MlApiMlExportDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlDatasetRows> {
            return localVarFp.mlExportDataset(requestParameters.name, requestParameters.version, requestParameters.split, requestParameters.offset, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
         * @summary Materialise the declared version into immutable rows
         * @param {MlApiMlMaterializeDatasetRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        mlMaterializeDataset(requestParameters: MlApiMlMaterializeDatasetRequest, options?: RawAxiosRequestConfig): AxiosPromise<MlDataset> {
            return localVarFp.mlMaterializeDataset(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Applies a JSON merge patch to one of the caller org\'s deployed models and answers the updated resource — the way to change a model\'s image, replica count or resource requests without tearing the deployment down.  The body is relayed to Kubernetes VERBATIM. That is deliberate and it is why this route is not a typed op: re-encoding a merge patch changes what it means, because an integer that round-trips through a generic decoder comes back a float. Merge-patch semantics apply as written — a null removes a field, and a list is replaced whole rather than merged.  Scoped to the caller\'s own tenant namespace, resolved from the validated org and project; a name the caller\'s tenant does not hold is a 404, never another tenant\'s resource. An empty body is refused, and a patch Kubernetes rejects comes back 422 with its reason rather than being silently dropped.
         * @summary Change a deployed model in place
         * @param {MlApiPatchV1MlModelsByNameRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchV1MlModelsByName(requestParameters: MlApiPatchV1MlModelsByNameRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.patchV1MlModelsByName(requestParameters.name, options).then((request) => request(axios, basePath));
        },
        /**
         * Deploys a model into the caller\'s own tenant namespace and answers the created resource, 201. The spec is the kserve InferenceService spec, relayed as given, so anything kserve serves is deployable here without this layer knowing what it is.  THE BALANCE GATE RUNS FIRST, before a namespace or a resource exists, so an unfunded org cannot start GPU compute and then be billed for it. It fails CLOSED: a commerce that cannot be reached refuses rather than admits. The refusal carries the fleet\'s nested error body — the 402 shape a funded-balance client already parses — which is precisely why this route is not a typed op. On success the submission fee is debited from the caller org\'s own ledger, asynchronously and best-effort; ongoing GPU-hour cost is metered elsewhere.  The tenant namespace is derived from the VALIDATED org and project — never from a field — and the mapping is injective in both, so two tenants can never land in one namespace. An unvalidated caller is refused before any of that. The name must be a DNS-1123 label; a name already taken in the tenant\'s namespace is a 409.
         * @summary Deploy an inference model
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1MlModels(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1MlModels(options).then((request) => request(axios, basePath));
        },
        /**
         * Sends the request body to the named model\'s predictor and answers the predictor\'s reply — its status code, its body bytes and its Content-Type, all unchanged. This is the inference call itself, not a description of one.  VERBATIM IS THE CONTRACT, and it is why this route is not a typed op: a model-side error has to surface as the model\'s own error, not as this layer\'s paraphrase of it. The body shape is the kserve v2 inference protocol\'s, which means the runtime decides it, not this API. The v2 model name defaults to the resource name — kserve\'s single-model convention — and a multi-model runtime selects one with the `model` query parameter.  A model that exists but has no serving address yet answers 503 \'not ready\' rather than a confusing connection error: deployed is not the same as serving. Scoped to the caller\'s own tenant namespace from the validated org and project, so a name another tenant owns is simply a 404. The predictor\'s response body is read up to a fixed ceiling.
         * @summary Run inference against one of your deployed models
         * @param {MlApiPostV1MlModelsByNamePredictRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postV1MlModelsByNamePredict(requestParameters: MlApiPostV1MlModelsByNamePredictRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postV1MlModelsByNamePredict(requestParameters.name, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteV1MlModelsName operation in MlApi.
 * @export
 * @interface MlApiDeleteV1MlModelsNameRequest
 */
export interface MlApiDeleteV1MlModelsNameRequest {
    /**
     * Name is the resource to act on, taken from the path. Lower-cased and trimmed to the DNS-1123 label a CustomResource\&#39;s metadata.name must be.
     * @type {string}
     * @memberof MlApiDeleteV1MlModelsName
     */
    readonly name: string
}

/**
 * Request parameters for getV1MlModelsName operation in MlApi.
 * @export
 * @interface MlApiGetV1MlModelsNameRequest
 */
export interface MlApiGetV1MlModelsNameRequest {
    /**
     * Name is the resource to act on, taken from the path. Lower-cased and trimmed to the DNS-1123 label a CustomResource\&#39;s metadata.name must be.
     * @type {string}
     * @memberof MlApiGetV1MlModelsName
     */
    readonly name: string
}

/**
 * Request parameters for mlCreateDataset operation in MlApi.
 * @export
 * @interface MlApiMlCreateDatasetRequest
 */
export interface MlApiMlCreateDatasetRequest {
    /**
     * 
     * @type {MlDatasetSpec}
     * @memberof MlApiMlCreateDataset
     */
    readonly mlDatasetSpec: MlDatasetSpec
}

/**
 * Request parameters for mlDataset operation in MlApi.
 * @export
 * @interface MlApiMlDatasetRequest
 */
export interface MlApiMlDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof MlApiMlDataset
     */
    readonly name: string
}

/**
 * Request parameters for mlDatasetLineage operation in MlApi.
 * @export
 * @interface MlApiMlDatasetLineageRequest
 */
export interface MlApiMlDatasetLineageRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof MlApiMlDatasetLineage
     */
    readonly name: string

    /**
     * Version is the version to trace. Zero takes the newest published one.
     * @type {number}
     * @memberof MlApiMlDatasetLineage
     */
    readonly version?: number
}

/**
 * Request parameters for mlDeleteDataset operation in MlApi.
 * @export
 * @interface MlApiMlDeleteDatasetRequest
 */
export interface MlApiMlDeleteDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof MlApiMlDeleteDataset
     */
    readonly name: string
}

/**
 * Request parameters for mlExportDataset operation in MlApi.
 * @export
 * @interface MlApiMlExportDatasetRequest
 */
export interface MlApiMlExportDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof MlApiMlExportDataset
     */
    readonly name: string

    /**
     * Version is the version to read. Zero takes the newest published one.
     * @type {number}
     * @memberof MlApiMlExportDataset
     */
    readonly version?: number

    /**
     * Split narrows to train, val or test. Empty reads every split.
     * @type {string}
     * @memberof MlApiMlExportDataset
     */
    readonly split?: string

    /**
     * Offset is where the page starts, in the version\&#39;s own row order (by id, which is derived from the row and therefore stable forever).
     * @type {number}
     * @memberof MlApiMlExportDataset
     */
    readonly offset?: number

    /**
     * Limit is how many rows to return. Zero and anything above the plane\&#39;s bound take the bound.
     * @type {number}
     * @memberof MlApiMlExportDataset
     */
    readonly limit?: number
}

/**
 * Request parameters for mlMaterializeDataset operation in MlApi.
 * @export
 * @interface MlApiMlMaterializeDatasetRequest
 */
export interface MlApiMlMaterializeDatasetRequest {
    /**
     * Name is the dataset, from the path.
     * @type {string}
     * @memberof MlApiMlMaterializeDataset
     */
    readonly name: string
}

/**
 * Request parameters for patchV1MlModelsByName operation in MlApi.
 * @export
 * @interface MlApiPatchV1MlModelsByNameRequest
 */
export interface MlApiPatchV1MlModelsByNameRequest {
    /**
     * 
     * @type {string}
     * @memberof MlApiPatchV1MlModelsByName
     */
    readonly name: string
}

/**
 * Request parameters for postV1MlModelsByNamePredict operation in MlApi.
 * @export
 * @interface MlApiPostV1MlModelsByNamePredictRequest
 */
export interface MlApiPostV1MlModelsByNamePredictRequest {
    /**
     * 
     * @type {string}
     * @memberof MlApiPostV1MlModelsByNamePredict
     */
    readonly name: string
}

/**
 * MlApi - object-oriented interface
 * @export
 * @class MlApi
 * @extends {BaseAPI}
 */
export class MlApi extends BaseAPI {
    /**
     * Deletes a deployed inference model. kserve owns the teardown: the InferenceService goes away and the serving deployment behind it follows, so the model stops answering predict calls. Answers 204, or 404 for a name the caller\'s org does not own.
     * @summary Deletes a deployed inference model.
     * @param {MlApiDeleteV1MlModelsNameRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public deleteV1MlModelsName(requestParameters: MlApiDeleteV1MlModelsNameRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).deleteV1MlModelsName(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reports whether the model-serving plane is genuinely usable: that the Kubernetes API answers, and that the InferenceService CRD is actually served by this cluster. It is a REAL probe, not status theatre — it makes a live call rather than reporting a flag set at boot.  200 only when everything checks out. Otherwise 503 CARRYING THE REPORT — which component failed, and the real error — and that body is the reason this is not a typed op: a typed op reaches a non-2xx by returning an error, and the envelope that produces would drop exactly the detail the probe exists to deliver.  It answers about the cluster, not about a tenant, so it takes no org and reveals no tenant data. A cluster with no kserve CRD reports degraded honestly rather than failing later at the first deploy.
     * @summary Whether model serving can actually work right now
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public getV1MlHealth(options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).getV1MlHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Lists the inference models deployed in the caller\'s org. Each entry carries the model\'s name, when Kubernetes admitted it, and kserve\'s live status — the spec is on the single-model read. An org that has deployed nothing gets an empty list.
     * @summary Lists the inference models deployed in the caller\'s org.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public getV1MlModels(options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).getV1MlModels(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one deployed inference model. Its spec comes with it, and kserve\'s live status, which is where readiness and the serving address appear. A name the caller\'s org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s models.
     * @summary Returns one deployed inference model.
     * @param {MlApiGetV1MlModelsNameRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public getV1MlModelsName(requestParameters: MlApiGetV1MlModelsNameRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).getV1MlModelsName(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Declares the next version of a dataset from a bound query over this org\'s own feature surface.  It mints a VERSION and writes no rows: a version is declared, then materialised once, then never rewritten. Version numbers are monotone and never reused, so \"version 3 of signups\" means one thing forever — which is the whole reason a model can cite one.  The window is bounded by the source\'s retention, the horizon by a year, the rows by the plane\'s cap, and the number of datasets and versions per org by their own limits. Every refusal names which bound it hit.
     * @summary Declare the next version of a dataset
     * @param {MlApiMlCreateDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public mlCreateDataset(requestParameters: MlApiMlCreateDatasetRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).mlCreateDataset(requestParameters.mlDatasetSpec, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Dataset describes every version of one dataset, newest first — the whole history, because the point of a version is that the older ones are still there and a model fitted last quarter cites one of them.  A name this org does not own answers 404, exactly as an unknown name does, so a probe learns nothing about another tenant\'s datasets.
     * @summary Describe every version of one dataset
     * @param {MlApiMlDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public mlDataset(requestParameters: MlApiMlDatasetRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).mlDataset(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Shows where a version\'s rows came from and whether that can still be demonstrated.  The answer is MEASURED, not recalled: the plane asks the source the same bounded question again and compares it to the fingerprint taken when the version was built. Anything but exact agreement is reported as drift — the source is fed by a rollup that runs behind the events, so \"it holds more now\" is the ordinary case and it means re-running the spec would not reproduce this version. An admitted gap is actionable; an unfalsifiable claim is not.  IT IS A PRICED, BOUNDED READ, because it is the same statement a materialisation is charged for: an exact distinct-count over up to 400 days of this org\'s feature surface. It takes the org\'s ONE source-scan slot, so a tenant looping it spends one scan and not a thousand; it counts against the plane\'s ceiling, so the fleet\'s warehouse is bounded too; and it runs under this plane\'s own deadline rather than the caller\'s patience.
     * @summary Show where a version\'s rows came from, and whether that can still be demonstrated
     * @param {MlApiMlDatasetLineageRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public mlDatasetLineage(requestParameters: MlApiMlDatasetLineageRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).mlDatasetLineage(requestParameters.name, requestParameters.version, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Datasets lists this org\'s datasets, each with its newest version. An org that has declared none gets an empty list; a store that cannot be reached gets a refusal, never an empty list, because the two read identically and only one of them is true.
     * @summary List this org\'s datasets
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public mlDatasets(options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).mlDatasets(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Disposes of one dataset and every version of it: the rows are dropped and the register is marked with what went.  This is the ONLY expiry in this plane. Neither table carries a TTL, deliberately: a table TTL is a fleet-wide clock no tenant can hold longer or shorten, which is the opposite of a retention decision belonging to the tenant whose records they are. The drop is a partition drop on (org, dataset), so the tenant is the first component of the thing being dropped and a disposal cannot be spelled across one.  The BYTES are what goes. The register keeps one `disposed` row per version — the name, the number, the spec, the digest and who disposed of it when — for two reasons: a retention obligation is answered by a record of the deletion, not by silence; and version numbers must stay monotone, so that after `orders` is disposed of and declared again the next version is 4 and not 1. A number that could be reused would make every citation of `orders v3` ambiguous forever.  It is not reversible and there is no soft state in between. A version a model cited has no rows once this returns, and every read of it says so.
     * @summary Dispose of one dataset and every version of it
     * @param {MlApiMlDeleteDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public mlDeleteDataset(requestParameters: MlApiMlDeleteDatasetRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).mlDeleteDataset(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Reads a published version\'s rows back, one bounded page at a time, in the version\'s own stable row order.  Only a published version can be exported. Rows written by an attempt that never completed are inert — no register row names them — and they are disposed of with the dataset.
     * @summary Read a version\'s rows back, one page at a time
     * @param {MlApiMlExportDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public mlExportDataset(requestParameters: MlApiMlExportDatasetRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).mlExportDataset(requestParameters.name, requestParameters.version, requestParameters.split, requestParameters.offset, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Builds the declared version into immutable rows and answers 202 as soon as the attempt is on record.  It never holds the request open for the work: a materialisation is a bounded warehouse scan, and letting an HTTP client\'s timeout be a data plane\'s timeout is how one tenant\'s retry loop becomes everyone\'s outage. ONE materialisation runs per org at a time; a second is refused rather than queued, because a queue admits the same work later and the honest answer to \"again\" while one is running is that one is running.  Only a DECLARED version is admitted. A published version is immutable, and a version whose earlier attempt did not complete is never re-attempted — that would union two runs\' rows under one number and make the digest a lie. In both cases the answer is to declare a new version, which is what a second run over a moving source honestly is.
     * @summary Materialise the declared version into immutable rows
     * @param {MlApiMlMaterializeDatasetRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public mlMaterializeDataset(requestParameters: MlApiMlMaterializeDatasetRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).mlMaterializeDataset(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Applies a JSON merge patch to one of the caller org\'s deployed models and answers the updated resource — the way to change a model\'s image, replica count or resource requests without tearing the deployment down.  The body is relayed to Kubernetes VERBATIM. That is deliberate and it is why this route is not a typed op: re-encoding a merge patch changes what it means, because an integer that round-trips through a generic decoder comes back a float. Merge-patch semantics apply as written — a null removes a field, and a list is replaced whole rather than merged.  Scoped to the caller\'s own tenant namespace, resolved from the validated org and project; a name the caller\'s tenant does not hold is a 404, never another tenant\'s resource. An empty body is refused, and a patch Kubernetes rejects comes back 422 with its reason rather than being silently dropped.
     * @summary Change a deployed model in place
     * @param {MlApiPatchV1MlModelsByNameRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public patchV1MlModelsByName(requestParameters: MlApiPatchV1MlModelsByNameRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).patchV1MlModelsByName(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Deploys a model into the caller\'s own tenant namespace and answers the created resource, 201. The spec is the kserve InferenceService spec, relayed as given, so anything kserve serves is deployable here without this layer knowing what it is.  THE BALANCE GATE RUNS FIRST, before a namespace or a resource exists, so an unfunded org cannot start GPU compute and then be billed for it. It fails CLOSED: a commerce that cannot be reached refuses rather than admits. The refusal carries the fleet\'s nested error body — the 402 shape a funded-balance client already parses — which is precisely why this route is not a typed op. On success the submission fee is debited from the caller org\'s own ledger, asynchronously and best-effort; ongoing GPU-hour cost is metered elsewhere.  The tenant namespace is derived from the VALIDATED org and project — never from a field — and the mapping is injective in both, so two tenants can never land in one namespace. An unvalidated caller is refused before any of that. The name must be a DNS-1123 label; a name already taken in the tenant\'s namespace is a 409.
     * @summary Deploy an inference model
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public postV1MlModels(options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).postV1MlModels(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Sends the request body to the named model\'s predictor and answers the predictor\'s reply — its status code, its body bytes and its Content-Type, all unchanged. This is the inference call itself, not a description of one.  VERBATIM IS THE CONTRACT, and it is why this route is not a typed op: a model-side error has to surface as the model\'s own error, not as this layer\'s paraphrase of it. The body shape is the kserve v2 inference protocol\'s, which means the runtime decides it, not this API. The v2 model name defaults to the resource name — kserve\'s single-model convention — and a multi-model runtime selects one with the `model` query parameter.  A model that exists but has no serving address yet answers 503 \'not ready\' rather than a confusing connection error: deployed is not the same as serving. Scoped to the caller\'s own tenant namespace from the validated org and project, so a name another tenant owns is simply a 404. The predictor\'s response body is read up to a fixed ceiling.
     * @summary Run inference against one of your deployed models
     * @param {MlApiPostV1MlModelsByNamePredictRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MlApi
     */
    public postV1MlModelsByNamePredict(requestParameters: MlApiPostV1MlModelsByNamePredictRequest, options?: RawAxiosRequestConfig) {
        return MlApiFp(this.configuration).postV1MlModelsByNamePredict(requestParameters.name, options).then((request) => request(this.axios, this.basePath));
    }
}

