/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * Composed from each subsystem\'s own projection of its router, in the fleet\'s mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { Balances } from '../models';
/**
 * TokensApi - axios parameter creator
 * @export
 */
export const TokensApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Reads an address\'s native balance on a chain.  ERC-20 positions are NOT enumerated here: eth_getBalance answers the native one, but \"every token this address holds\" is an indexer question — there is no RPC call that answers it, and walking a token list would return a number that silently omits whatever the list missed. explorer owns the indexer relationship; this returns the balance the chain itself can prove.
         * @summary Reads an address\'s native balance on a chain.
         * @param {string} chain Chain is the registry id.
         * @param {string} address Address is the account, 0x-prefixed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTokensByChainByAddress: async (chain: string, address: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'chain' is not null or undefined
            assertParamExists('getTokensByChainByAddress', 'chain', chain)
            // verify required parameter 'address' is not null or undefined
            assertParamExists('getTokensByChainByAddress', 'address', address)
            const localVarPath = `/v1/tokens/{chain}/{address}`
                .replace(`{${"chain"}}`, encodeURIComponent(String(chain)))
                .replace(`{${"address"}}`, encodeURIComponent(String(address)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * TokensApi - functional programming interface
 * @export
 */
export const TokensApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = TokensApiAxiosParamCreator(configuration)
    return {
        /**
         * Reads an address\'s native balance on a chain.  ERC-20 positions are NOT enumerated here: eth_getBalance answers the native one, but \"every token this address holds\" is an indexer question — there is no RPC call that answers it, and walking a token list would return a number that silently omits whatever the list missed. explorer owns the indexer relationship; this returns the balance the chain itself can prove.
         * @summary Reads an address\'s native balance on a chain.
         * @param {string} chain Chain is the registry id.
         * @param {string} address Address is the account, 0x-prefixed.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getTokensByChainByAddress(chain: string, address: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Balances>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getTokensByChainByAddress(chain, address, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TokensApi.getTokensByChainByAddress']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * TokensApi - factory interface
 * @export
 */
export const TokensApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = TokensApiFp(configuration)
    return {
        /**
         * Reads an address\'s native balance on a chain.  ERC-20 positions are NOT enumerated here: eth_getBalance answers the native one, but \"every token this address holds\" is an indexer question — there is no RPC call that answers it, and walking a token list would return a number that silently omits whatever the list missed. explorer owns the indexer relationship; this returns the balance the chain itself can prove.
         * @summary Reads an address\'s native balance on a chain.
         * @param {TokensApiGetTokensByChainByAddressRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTokensByChainByAddress(requestParameters: TokensApiGetTokensByChainByAddressRequest, options?: RawAxiosRequestConfig): AxiosPromise<Balances> {
            return localVarFp.getTokensByChainByAddress(requestParameters.chain, requestParameters.address, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for getTokensByChainByAddress operation in TokensApi.
 * @export
 * @interface TokensApiGetTokensByChainByAddressRequest
 */
export interface TokensApiGetTokensByChainByAddressRequest {
    /**
     * Chain is the registry id.
     * @type {string}
     * @memberof TokensApiGetTokensByChainByAddress
     */
    readonly chain: string

    /**
     * Address is the account, 0x-prefixed.
     * @type {string}
     * @memberof TokensApiGetTokensByChainByAddress
     */
    readonly address: string
}

/**
 * TokensApi - object-oriented interface
 * @export
 * @class TokensApi
 * @extends {BaseAPI}
 */
export class TokensApi extends BaseAPI {
    /**
     * Reads an address\'s native balance on a chain.  ERC-20 positions are NOT enumerated here: eth_getBalance answers the native one, but \"every token this address holds\" is an indexer question — there is no RPC call that answers it, and walking a token list would return a number that silently omits whatever the list missed. explorer owns the indexer relationship; this returns the balance the chain itself can prove.
     * @summary Reads an address\'s native balance on a chain.
     * @param {TokensApiGetTokensByChainByAddressRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TokensApi
     */
    public getTokensByChainByAddress(requestParameters: TokensApiGetTokensByChainByAddressRequest, options?: RawAxiosRequestConfig) {
        return TokensApiFp(this.configuration).getTokensByChainByAddress(requestParameters.chain, requestParameters.address, options).then((request) => request(this.axios, this.basePath));
    }
}

