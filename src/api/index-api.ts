/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
/**
 * IndexApi - axios parameter creator
 * @export
 */
export const IndexApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Drops one index in the caller\'s org together with all of its documents. This is the only way to retire an index; without it a mistaken uid would be permanent. It is idempotent — dropping an index that is not there still succeeds. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete an index and everything in it
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IndexIndexesByUid: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudDeleteV1IndexIndexesByUid', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes one document from an index. It is IDEMPOTENT: deleting a key that is not there succeeds rather than 404, so a retry after a lost response is safe. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete one document by its primary key
         * @param {string} uid 
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IndexIndexesByUidDocumentsById: async (uid: string, id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudDeleteV1IndexIndexesByUidDocumentsById', 'uid', uid)
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudDeleteV1IndexIndexesByUidDocumentsById', 'id', id)
            const localVarPath = `/v1/index/indexes/{uid}/documents/{id}`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)))
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers Meilisearch\'s `{\"status\":\"available\"}` when the index store is readable. It FAILS CLOSED — an unreadable store answers 503 and `unavailable` — so a replica whose volume has gone bad stops taking traffic instead of answering every search with nothing found. It touches no tenant data and needs no credential.
         * @summary Report whether the search plane can serve
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexHealth: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/index/health`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers every index in the caller\'s org with its primary key and timestamps. It is the only way to enumerate what an org holds — without it an index whose uid a caller has forgotten is unreachable. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary List the indexes your org holds
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexes: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/index/indexes`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers a single index\'s uid, primary key and timestamps. An index the caller\'s org does not hold is 404 `index_not_found` — which is the same answer another org\'s index gives, since the org is a bound predicate on the read. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read one index\'s definition
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUid: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudGetV1IndexIndexesByUid', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the documents in one index with a total count. `limit` defaults to 20 and is capped at 1000, `offset` pages, and the response echoes both back so a pager knows what it actually got. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Page through the documents in an index
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUidDocuments: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudGetV1IndexIndexesByUidDocuments', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}/documents`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the stored document whose primary key matches, exactly as it was written. A missing document is 404 `document_not_found` and a missing index is 404 `index_not_found` — two different codes, because a client that branches on them treats the cases differently. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read one document by its primary key
         * @param {string} uid 
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUidDocumentsById: async (uid: string, id: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudGetV1IndexIndexesByUidDocumentsById', 'uid', uid)
            // verify required parameter 'id' is not null or undefined
            assertParamExists('cloudGetV1IndexIndexesByUidDocumentsById', 'id', id)
            const localVarPath = `/v1/index/indexes/{uid}/documents/{id}`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)))
                .replace(`{${"id"}}`, encodeURIComponent(String(id)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the attributes an index allows filtering on. This dialect implements the filterable-attributes setting and no other, so that is the whole of what comes back. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read an index\'s filterable attributes
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUidSettings: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudGetV1IndexIndexesByUidSettings', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}/settings`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers a document count per index for the caller\'s org, plus their sum. `isIndexing` is always false, which is the honest answer here rather than a stub: writes are applied before their response, so there is never a backlog in progress to report. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Count the documents in each of your indexes
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexStats: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/index/stats`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers `succeeded` for the task id given. It ALWAYS answers succeeded, and that is honest rather than a stub: writes on this surface are applied before their response returns, so by the time any task id exists to ask about, its work is done. It exists so a Meilisearch client\'s waitForTask resolves at once instead of polling forever for a queue that was never there. It requires a validated principal but reads no tenant data.
         * @summary Check a write task, which has already finished
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexTasksByUid: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudGetV1IndexTasksByUid', 'uid', uid)
            const localVarPath = `/v1/index/tasks/{uid}`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the version shape a Meilisearch client expects. It names THIS implementation rather than a Meilisearch release — the commit field reads `hanzo-cloud` — so a client that logs it records which server actually answered instead of implying a Meilisearch build. Needs no credential.
         * @summary Identify the search implementation answering
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexVersion: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/index/version`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Replaces an index\'s filterable attributes with the list in `filterableAttributes`; omitting the field leaves them as they are. The index is CREATED ON DEMAND rather than 404\'d, because a client that configures an index it has just asked for should not have to create it first — this is the one read-shaped path on the surface that writes. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Set which attributes an index can be filtered on
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1IndexIndexesByUidSettings: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudPatchV1IndexIndexesByUidSettings', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}/settings`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Creates an index named by `uid` in the caller\'s org. `primaryKey` names the document field that identifies a document and defaults to `id`. Creating an index that already exists is not an error — it settles on the existing one, primary key included — so a client that creates before every write is safe to run repeatedly. A missing or over-long uid is 400 `invalid_index_uid`. A new index starts with `user` filterable, which is what lets a multi-user app narrow searches to one end user without configuring anything. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Create an index
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexes: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/index/indexes`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the PUT on the same path are the SAME operation: both are a whole document upsert, which is what a Meilisearch client\'s addDocuments and updateDocuments both reduce to here. A body that is neither an array nor an object is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Add or replace documents in an index
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexesByUidDocuments: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudPostV1IndexIndexesByUidDocuments', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}/documents`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Removes every document named by an array of primary keys. Keys may be sent as strings or numbers — a number keeps its exact decimal form, so an integer key round-trips as `42` and never as scientific notation. Keys that are absent from the index are skipped rather than failing the batch, so this is idempotent. A body that is not an array is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete many documents by primary key in one call
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexesByUidDocumentsDeleteBatch: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudPostV1IndexIndexesByUidDocumentsDeleteBatch', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}/documents/delete-batch`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers the documents in one index matching `q`, ranked by how many of the query\'s terms they match, with prefix matching so a partial word still finds its document. `limit` defaults to 20 and is capped at 1000, `offset` pages; a negative value falls back to the default rather than erroring.  `filter` takes a Meilisearch filter expression, or an array of them, and the `user = \"…\"` and `user IN […]` forms are honoured — that is how an app with many end users narrows results to one of them WITHIN the org. `estimatedTotalHits` is exact for the page returned, not an estimate, because every hit is materialised. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Search an index, forgiving typos
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexesByUidSearch: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudPostV1IndexIndexesByUidSearch', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}/search`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the POST on the same path are the SAME operation, served by one handler. Both exist because the Meilisearch dialect has both verbs; there is no partial-update semantics on this one — a document is replaced whole either way. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Add or update documents in an index
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1IndexIndexesByUidDocuments: async (uid: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'uid' is not null or undefined
            assertParamExists('cloudPutV1IndexIndexesByUidDocuments', 'uid', uid)
            const localVarPath = `/v1/index/indexes/{uid}/documents`
                .replace(`{${"uid"}}`, encodeURIComponent(String(uid)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PUT', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * IndexApi - functional programming interface
 * @export
 */
export const IndexApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = IndexApiAxiosParamCreator(configuration)
    return {
        /**
         * Drops one index in the caller\'s org together with all of its documents. This is the only way to retire an index; without it a mistaken uid would be permanent. It is idempotent — dropping an index that is not there still succeeds. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete an index and everything in it
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1IndexIndexesByUid(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1IndexIndexesByUid(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudDeleteV1IndexIndexesByUid']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes one document from an index. It is IDEMPOTENT: deleting a key that is not there succeeds rather than 404, so a retry after a lost response is safe. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete one document by its primary key
         * @param {string} uid 
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudDeleteV1IndexIndexesByUidDocumentsById(uid: string, id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudDeleteV1IndexIndexesByUidDocumentsById(uid, id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudDeleteV1IndexIndexesByUidDocumentsById']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers Meilisearch\'s `{\"status\":\"available\"}` when the index store is readable. It FAILS CLOSED — an unreadable store answers 503 and `unavailable` — so a replica whose volume has gone bad stops taking traffic instead of answering every search with nothing found. It touches no tenant data and needs no credential.
         * @summary Report whether the search plane can serve
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexHealth(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexHealth(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexHealth']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers every index in the caller\'s org with its primary key and timestamps. It is the only way to enumerate what an org holds — without it an index whose uid a caller has forgotten is unreachable. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary List the indexes your org holds
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexIndexes(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexIndexes(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexIndexes']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers a single index\'s uid, primary key and timestamps. An index the caller\'s org does not hold is 404 `index_not_found` — which is the same answer another org\'s index gives, since the org is a bound predicate on the read. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read one index\'s definition
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexIndexesByUid(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexIndexesByUid(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexIndexesByUid']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the documents in one index with a total count. `limit` defaults to 20 and is capped at 1000, `offset` pages, and the response echoes both back so a pager knows what it actually got. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Page through the documents in an index
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexIndexesByUidDocuments(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexIndexesByUidDocuments(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexIndexesByUidDocuments']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the stored document whose primary key matches, exactly as it was written. A missing document is 404 `document_not_found` and a missing index is 404 `index_not_found` — two different codes, because a client that branches on them treats the cases differently. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read one document by its primary key
         * @param {string} uid 
         * @param {string} id 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexIndexesByUidDocumentsById(uid: string, id: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexIndexesByUidDocumentsById(uid, id, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexIndexesByUidDocumentsById']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the attributes an index allows filtering on. This dialect implements the filterable-attributes setting and no other, so that is the whole of what comes back. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read an index\'s filterable attributes
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexIndexesByUidSettings(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexIndexesByUidSettings(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexIndexesByUidSettings']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers a document count per index for the caller\'s org, plus their sum. `isIndexing` is always false, which is the honest answer here rather than a stub: writes are applied before their response, so there is never a backlog in progress to report. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Count the documents in each of your indexes
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexStats(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexStats(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexStats']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers `succeeded` for the task id given. It ALWAYS answers succeeded, and that is honest rather than a stub: writes on this surface are applied before their response returns, so by the time any task id exists to ask about, its work is done. It exists so a Meilisearch client\'s waitForTask resolves at once instead of polling forever for a queue that was never there. It requires a validated principal but reads no tenant data.
         * @summary Check a write task, which has already finished
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexTasksByUid(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexTasksByUid(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexTasksByUid']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the version shape a Meilisearch client expects. It names THIS implementation rather than a Meilisearch release — the commit field reads `hanzo-cloud` — so a client that logs it records which server actually answered instead of implying a Meilisearch build. Needs no credential.
         * @summary Identify the search implementation answering
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1IndexVersion(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1IndexVersion(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudGetV1IndexVersion']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Replaces an index\'s filterable attributes with the list in `filterableAttributes`; omitting the field leaves them as they are. The index is CREATED ON DEMAND rather than 404\'d, because a client that configures an index it has just asked for should not have to create it first — this is the one read-shaped path on the surface that writes. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Set which attributes an index can be filtered on
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPatchV1IndexIndexesByUidSettings(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPatchV1IndexIndexesByUidSettings(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudPatchV1IndexIndexesByUidSettings']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Creates an index named by `uid` in the caller\'s org. `primaryKey` names the document field that identifies a document and defaults to `id`. Creating an index that already exists is not an error — it settles on the existing one, primary key included — so a client that creates before every write is safe to run repeatedly. A missing or over-long uid is 400 `invalid_index_uid`. A new index starts with `user` filterable, which is what lets a multi-user app narrow searches to one end user without configuring anything. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Create an index
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1IndexIndexes(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1IndexIndexes(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudPostV1IndexIndexes']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the PUT on the same path are the SAME operation: both are a whole document upsert, which is what a Meilisearch client\'s addDocuments and updateDocuments both reduce to here. A body that is neither an array nor an object is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Add or replace documents in an index
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1IndexIndexesByUidDocuments(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1IndexIndexesByUidDocuments(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudPostV1IndexIndexesByUidDocuments']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Removes every document named by an array of primary keys. Keys may be sent as strings or numbers — a number keeps its exact decimal form, so an integer key round-trips as `42` and never as scientific notation. Keys that are absent from the index are skipped rather than failing the batch, so this is idempotent. A body that is not an array is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete many documents by primary key in one call
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1IndexIndexesByUidDocumentsDeleteBatch(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1IndexIndexesByUidDocumentsDeleteBatch(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudPostV1IndexIndexesByUidDocumentsDeleteBatch']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers the documents in one index matching `q`, ranked by how many of the query\'s terms they match, with prefix matching so a partial word still finds its document. `limit` defaults to 20 and is capped at 1000, `offset` pages; a negative value falls back to the default rather than erroring.  `filter` takes a Meilisearch filter expression, or an array of them, and the `user = \"…\"` and `user IN […]` forms are honoured — that is how an app with many end users narrows results to one of them WITHIN the org. `estimatedTotalHits` is exact for the page returned, not an estimate, because every hit is materialised. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Search an index, forgiving typos
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPostV1IndexIndexesByUidSearch(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPostV1IndexIndexesByUidSearch(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudPostV1IndexIndexesByUidSearch']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the POST on the same path are the SAME operation, served by one handler. Both exist because the Meilisearch dialect has both verbs; there is no partial-update semantics on this one — a document is replaced whole either way. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Add or update documents in an index
         * @param {string} uid 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudPutV1IndexIndexesByUidDocuments(uid: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudPutV1IndexIndexesByUidDocuments(uid, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['IndexApi.cloudPutV1IndexIndexesByUidDocuments']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * IndexApi - factory interface
 * @export
 */
export const IndexApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = IndexApiFp(configuration)
    return {
        /**
         * Drops one index in the caller\'s org together with all of its documents. This is the only way to retire an index; without it a mistaken uid would be permanent. It is idempotent — dropping an index that is not there still succeeds. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete an index and everything in it
         * @param {IndexApiCloudDeleteV1IndexIndexesByUidRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IndexIndexesByUid(requestParameters: IndexApiCloudDeleteV1IndexIndexesByUidRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudDeleteV1IndexIndexesByUid(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Removes one document from an index. It is IDEMPOTENT: deleting a key that is not there succeeds rather than 404, so a retry after a lost response is safe. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete one document by its primary key
         * @param {IndexApiCloudDeleteV1IndexIndexesByUidDocumentsByIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudDeleteV1IndexIndexesByUidDocumentsById(requestParameters: IndexApiCloudDeleteV1IndexIndexesByUidDocumentsByIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudDeleteV1IndexIndexesByUidDocumentsById(requestParameters.uid, requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers Meilisearch\'s `{\"status\":\"available\"}` when the index store is readable. It FAILS CLOSED — an unreadable store answers 503 and `unavailable` — so a replica whose volume has gone bad stops taking traffic instead of answering every search with nothing found. It touches no tenant data and needs no credential.
         * @summary Report whether the search plane can serve
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexHealth(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexHealth(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers every index in the caller\'s org with its primary key and timestamps. It is the only way to enumerate what an org holds — without it an index whose uid a caller has forgotten is unreachable. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary List the indexes your org holds
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexes(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexIndexes(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers a single index\'s uid, primary key and timestamps. An index the caller\'s org does not hold is 404 `index_not_found` — which is the same answer another org\'s index gives, since the org is a bound predicate on the read. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read one index\'s definition
         * @param {IndexApiCloudGetV1IndexIndexesByUidRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUid(requestParameters: IndexApiCloudGetV1IndexIndexesByUidRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexIndexesByUid(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the documents in one index with a total count. `limit` defaults to 20 and is capped at 1000, `offset` pages, and the response echoes both back so a pager knows what it actually got. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Page through the documents in an index
         * @param {IndexApiCloudGetV1IndexIndexesByUidDocumentsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUidDocuments(requestParameters: IndexApiCloudGetV1IndexIndexesByUidDocumentsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexIndexesByUidDocuments(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the stored document whose primary key matches, exactly as it was written. A missing document is 404 `document_not_found` and a missing index is 404 `index_not_found` — two different codes, because a client that branches on them treats the cases differently. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read one document by its primary key
         * @param {IndexApiCloudGetV1IndexIndexesByUidDocumentsByIdRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUidDocumentsById(requestParameters: IndexApiCloudGetV1IndexIndexesByUidDocumentsByIdRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexIndexesByUidDocumentsById(requestParameters.uid, requestParameters.id, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the attributes an index allows filtering on. This dialect implements the filterable-attributes setting and no other, so that is the whole of what comes back. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Read an index\'s filterable attributes
         * @param {IndexApiCloudGetV1IndexIndexesByUidSettingsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexIndexesByUidSettings(requestParameters: IndexApiCloudGetV1IndexIndexesByUidSettingsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexIndexesByUidSettings(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers a document count per index for the caller\'s org, plus their sum. `isIndexing` is always false, which is the honest answer here rather than a stub: writes are applied before their response, so there is never a backlog in progress to report. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Count the documents in each of your indexes
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexStats(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexStats(options).then((request) => request(axios, basePath));
        },
        /**
         * Answers `succeeded` for the task id given. It ALWAYS answers succeeded, and that is honest rather than a stub: writes on this surface are applied before their response returns, so by the time any task id exists to ask about, its work is done. It exists so a Meilisearch client\'s waitForTask resolves at once instead of polling forever for a queue that was never there. It requires a validated principal but reads no tenant data.
         * @summary Check a write task, which has already finished
         * @param {IndexApiCloudGetV1IndexTasksByUidRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexTasksByUid(requestParameters: IndexApiCloudGetV1IndexTasksByUidRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexTasksByUid(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the version shape a Meilisearch client expects. It names THIS implementation rather than a Meilisearch release — the commit field reads `hanzo-cloud` — so a client that logs it records which server actually answered instead of implying a Meilisearch build. Needs no credential.
         * @summary Identify the search implementation answering
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1IndexVersion(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudGetV1IndexVersion(options).then((request) => request(axios, basePath));
        },
        /**
         * Replaces an index\'s filterable attributes with the list in `filterableAttributes`; omitting the field leaves them as they are. The index is CREATED ON DEMAND rather than 404\'d, because a client that configures an index it has just asked for should not have to create it first — this is the one read-shaped path on the surface that writes. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Set which attributes an index can be filtered on
         * @param {IndexApiCloudPatchV1IndexIndexesByUidSettingsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPatchV1IndexIndexesByUidSettings(requestParameters: IndexApiCloudPatchV1IndexIndexesByUidSettingsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPatchV1IndexIndexesByUidSettings(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Creates an index named by `uid` in the caller\'s org. `primaryKey` names the document field that identifies a document and defaults to `id`. Creating an index that already exists is not an error — it settles on the existing one, primary key included — so a client that creates before every write is safe to run repeatedly. A missing or over-long uid is 400 `invalid_index_uid`. A new index starts with `user` filterable, which is what lets a multi-user app narrow searches to one end user without configuring anything. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Create an index
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexes(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1IndexIndexes(options).then((request) => request(axios, basePath));
        },
        /**
         * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the PUT on the same path are the SAME operation: both are a whole document upsert, which is what a Meilisearch client\'s addDocuments and updateDocuments both reduce to here. A body that is neither an array nor an object is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Add or replace documents in an index
         * @param {IndexApiCloudPostV1IndexIndexesByUidDocumentsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexesByUidDocuments(requestParameters: IndexApiCloudPostV1IndexIndexesByUidDocumentsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1IndexIndexesByUidDocuments(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Removes every document named by an array of primary keys. Keys may be sent as strings or numbers — a number keeps its exact decimal form, so an integer key round-trips as `42` and never as scientific notation. Keys that are absent from the index are skipped rather than failing the batch, so this is idempotent. A body that is not an array is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Delete many documents by primary key in one call
         * @param {IndexApiCloudPostV1IndexIndexesByUidDocumentsDeleteBatchRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexesByUidDocumentsDeleteBatch(requestParameters: IndexApiCloudPostV1IndexIndexesByUidDocumentsDeleteBatchRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1IndexIndexesByUidDocumentsDeleteBatch(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers the documents in one index matching `q`, ranked by how many of the query\'s terms they match, with prefix matching so a partial word still finds its document. `limit` defaults to 20 and is capped at 1000, `offset` pages; a negative value falls back to the default rather than erroring.  `filter` takes a Meilisearch filter expression, or an array of them, and the `user = \"…\"` and `user IN […]` forms are honoured — that is how an app with many end users narrows results to one of them WITHIN the org. `estimatedTotalHits` is exact for the page returned, not an estimate, because every hit is materialised. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
         * @summary Search an index, forgiving typos
         * @param {IndexApiCloudPostV1IndexIndexesByUidSearchRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPostV1IndexIndexesByUidSearch(requestParameters: IndexApiCloudPostV1IndexIndexesByUidSearchRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPostV1IndexIndexesByUidSearch(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
        /**
         * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the POST on the same path are the SAME operation, served by one handler. Both exist because the Meilisearch dialect has both verbs; there is no partial-update semantics on this one — a document is replaced whole either way. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
         * @summary Add or update documents in an index
         * @param {IndexApiCloudPutV1IndexIndexesByUidDocumentsRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudPutV1IndexIndexesByUidDocuments(requestParameters: IndexApiCloudPutV1IndexIndexesByUidDocumentsRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.cloudPutV1IndexIndexesByUidDocuments(requestParameters.uid, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for cloudDeleteV1IndexIndexesByUid operation in IndexApi.
 * @export
 * @interface IndexApiCloudDeleteV1IndexIndexesByUidRequest
 */
export interface IndexApiCloudDeleteV1IndexIndexesByUidRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudDeleteV1IndexIndexesByUid
     */
    readonly uid: string
}

/**
 * Request parameters for cloudDeleteV1IndexIndexesByUidDocumentsById operation in IndexApi.
 * @export
 * @interface IndexApiCloudDeleteV1IndexIndexesByUidDocumentsByIdRequest
 */
export interface IndexApiCloudDeleteV1IndexIndexesByUidDocumentsByIdRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudDeleteV1IndexIndexesByUidDocumentsById
     */
    readonly uid: string

    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudDeleteV1IndexIndexesByUidDocumentsById
     */
    readonly id: string
}

/**
 * Request parameters for cloudGetV1IndexIndexesByUid operation in IndexApi.
 * @export
 * @interface IndexApiCloudGetV1IndexIndexesByUidRequest
 */
export interface IndexApiCloudGetV1IndexIndexesByUidRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudGetV1IndexIndexesByUid
     */
    readonly uid: string
}

/**
 * Request parameters for cloudGetV1IndexIndexesByUidDocuments operation in IndexApi.
 * @export
 * @interface IndexApiCloudGetV1IndexIndexesByUidDocumentsRequest
 */
export interface IndexApiCloudGetV1IndexIndexesByUidDocumentsRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudGetV1IndexIndexesByUidDocuments
     */
    readonly uid: string
}

/**
 * Request parameters for cloudGetV1IndexIndexesByUidDocumentsById operation in IndexApi.
 * @export
 * @interface IndexApiCloudGetV1IndexIndexesByUidDocumentsByIdRequest
 */
export interface IndexApiCloudGetV1IndexIndexesByUidDocumentsByIdRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudGetV1IndexIndexesByUidDocumentsById
     */
    readonly uid: string

    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudGetV1IndexIndexesByUidDocumentsById
     */
    readonly id: string
}

/**
 * Request parameters for cloudGetV1IndexIndexesByUidSettings operation in IndexApi.
 * @export
 * @interface IndexApiCloudGetV1IndexIndexesByUidSettingsRequest
 */
export interface IndexApiCloudGetV1IndexIndexesByUidSettingsRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudGetV1IndexIndexesByUidSettings
     */
    readonly uid: string
}

/**
 * Request parameters for cloudGetV1IndexTasksByUid operation in IndexApi.
 * @export
 * @interface IndexApiCloudGetV1IndexTasksByUidRequest
 */
export interface IndexApiCloudGetV1IndexTasksByUidRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudGetV1IndexTasksByUid
     */
    readonly uid: string
}

/**
 * Request parameters for cloudPatchV1IndexIndexesByUidSettings operation in IndexApi.
 * @export
 * @interface IndexApiCloudPatchV1IndexIndexesByUidSettingsRequest
 */
export interface IndexApiCloudPatchV1IndexIndexesByUidSettingsRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudPatchV1IndexIndexesByUidSettings
     */
    readonly uid: string
}

/**
 * Request parameters for cloudPostV1IndexIndexesByUidDocuments operation in IndexApi.
 * @export
 * @interface IndexApiCloudPostV1IndexIndexesByUidDocumentsRequest
 */
export interface IndexApiCloudPostV1IndexIndexesByUidDocumentsRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudPostV1IndexIndexesByUidDocuments
     */
    readonly uid: string
}

/**
 * Request parameters for cloudPostV1IndexIndexesByUidDocumentsDeleteBatch operation in IndexApi.
 * @export
 * @interface IndexApiCloudPostV1IndexIndexesByUidDocumentsDeleteBatchRequest
 */
export interface IndexApiCloudPostV1IndexIndexesByUidDocumentsDeleteBatchRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudPostV1IndexIndexesByUidDocumentsDeleteBatch
     */
    readonly uid: string
}

/**
 * Request parameters for cloudPostV1IndexIndexesByUidSearch operation in IndexApi.
 * @export
 * @interface IndexApiCloudPostV1IndexIndexesByUidSearchRequest
 */
export interface IndexApiCloudPostV1IndexIndexesByUidSearchRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudPostV1IndexIndexesByUidSearch
     */
    readonly uid: string
}

/**
 * Request parameters for cloudPutV1IndexIndexesByUidDocuments operation in IndexApi.
 * @export
 * @interface IndexApiCloudPutV1IndexIndexesByUidDocumentsRequest
 */
export interface IndexApiCloudPutV1IndexIndexesByUidDocumentsRequest {
    /**
     * 
     * @type {string}
     * @memberof IndexApiCloudPutV1IndexIndexesByUidDocuments
     */
    readonly uid: string
}

/**
 * IndexApi - object-oriented interface
 * @export
 * @class IndexApi
 * @extends {BaseAPI}
 */
export class IndexApi extends BaseAPI {
    /**
     * Drops one index in the caller\'s org together with all of its documents. This is the only way to retire an index; without it a mistaken uid would be permanent. It is idempotent — dropping an index that is not there still succeeds. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
     * @summary Delete an index and everything in it
     * @param {IndexApiCloudDeleteV1IndexIndexesByUidRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudDeleteV1IndexIndexesByUid(requestParameters: IndexApiCloudDeleteV1IndexIndexesByUidRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudDeleteV1IndexIndexesByUid(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes one document from an index. It is IDEMPOTENT: deleting a key that is not there succeeds rather than 404, so a retry after a lost response is safe. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
     * @summary Delete one document by its primary key
     * @param {IndexApiCloudDeleteV1IndexIndexesByUidDocumentsByIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudDeleteV1IndexIndexesByUidDocumentsById(requestParameters: IndexApiCloudDeleteV1IndexIndexesByUidDocumentsByIdRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudDeleteV1IndexIndexesByUidDocumentsById(requestParameters.uid, requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers Meilisearch\'s `{\"status\":\"available\"}` when the index store is readable. It FAILS CLOSED — an unreadable store answers 503 and `unavailable` — so a replica whose volume has gone bad stops taking traffic instead of answering every search with nothing found. It touches no tenant data and needs no credential.
     * @summary Report whether the search plane can serve
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexHealth(options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexHealth(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers every index in the caller\'s org with its primary key and timestamps. It is the only way to enumerate what an org holds — without it an index whose uid a caller has forgotten is unreachable. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
     * @summary List the indexes your org holds
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexIndexes(options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexIndexes(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers a single index\'s uid, primary key and timestamps. An index the caller\'s org does not hold is 404 `index_not_found` — which is the same answer another org\'s index gives, since the org is a bound predicate on the read. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
     * @summary Read one index\'s definition
     * @param {IndexApiCloudGetV1IndexIndexesByUidRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexIndexesByUid(requestParameters: IndexApiCloudGetV1IndexIndexesByUidRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexIndexesByUid(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the documents in one index with a total count. `limit` defaults to 20 and is capped at 1000, `offset` pages, and the response echoes both back so a pager knows what it actually got. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
     * @summary Page through the documents in an index
     * @param {IndexApiCloudGetV1IndexIndexesByUidDocumentsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexIndexesByUidDocuments(requestParameters: IndexApiCloudGetV1IndexIndexesByUidDocumentsRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexIndexesByUidDocuments(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the stored document whose primary key matches, exactly as it was written. A missing document is 404 `document_not_found` and a missing index is 404 `index_not_found` — two different codes, because a client that branches on them treats the cases differently. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
     * @summary Read one document by its primary key
     * @param {IndexApiCloudGetV1IndexIndexesByUidDocumentsByIdRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexIndexesByUidDocumentsById(requestParameters: IndexApiCloudGetV1IndexIndexesByUidDocumentsByIdRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexIndexesByUidDocumentsById(requestParameters.uid, requestParameters.id, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the attributes an index allows filtering on. This dialect implements the filterable-attributes setting and no other, so that is the whole of what comes back. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
     * @summary Read an index\'s filterable attributes
     * @param {IndexApiCloudGetV1IndexIndexesByUidSettingsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexIndexesByUidSettings(requestParameters: IndexApiCloudGetV1IndexIndexesByUidSettingsRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexIndexesByUidSettings(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers a document count per index for the caller\'s org, plus their sum. `isIndexing` is always false, which is the honest answer here rather than a stub: writes are applied before their response, so there is never a backlog in progress to report. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
     * @summary Count the documents in each of your indexes
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexStats(options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexStats(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers `succeeded` for the task id given. It ALWAYS answers succeeded, and that is honest rather than a stub: writes on this surface are applied before their response returns, so by the time any task id exists to ask about, its work is done. It exists so a Meilisearch client\'s waitForTask resolves at once instead of polling forever for a queue that was never there. It requires a validated principal but reads no tenant data.
     * @summary Check a write task, which has already finished
     * @param {IndexApiCloudGetV1IndexTasksByUidRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexTasksByUid(requestParameters: IndexApiCloudGetV1IndexTasksByUidRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexTasksByUid(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the version shape a Meilisearch client expects. It names THIS implementation rather than a Meilisearch release — the commit field reads `hanzo-cloud` — so a client that logs it records which server actually answered instead of implying a Meilisearch build. Needs no credential.
     * @summary Identify the search implementation answering
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudGetV1IndexVersion(options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudGetV1IndexVersion(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Replaces an index\'s filterable attributes with the list in `filterableAttributes`; omitting the field leaves them as they are. The index is CREATED ON DEMAND rather than 404\'d, because a client that configures an index it has just asked for should not have to create it first — this is the one read-shaped path on the surface that writes. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
     * @summary Set which attributes an index can be filtered on
     * @param {IndexApiCloudPatchV1IndexIndexesByUidSettingsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudPatchV1IndexIndexesByUidSettings(requestParameters: IndexApiCloudPatchV1IndexIndexesByUidSettingsRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudPatchV1IndexIndexesByUidSettings(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Creates an index named by `uid` in the caller\'s org. `primaryKey` names the document field that identifies a document and defaults to `id`. Creating an index that already exists is not an error — it settles on the existing one, primary key included — so a client that creates before every write is safe to run repeatedly. A missing or over-long uid is 400 `invalid_index_uid`. A new index starts with `user` filterable, which is what lets a multi-user app narrow searches to one end user without configuring anything. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
     * @summary Create an index
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudPostV1IndexIndexes(options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudPostV1IndexIndexes(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the PUT on the same path are the SAME operation: both are a whole document upsert, which is what a Meilisearch client\'s addDocuments and updateDocuments both reduce to here. A body that is neither an array nor an object is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
     * @summary Add or replace documents in an index
     * @param {IndexApiCloudPostV1IndexIndexesByUidDocumentsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudPostV1IndexIndexesByUidDocuments(requestParameters: IndexApiCloudPostV1IndexIndexesByUidDocumentsRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudPostV1IndexIndexesByUidDocuments(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Removes every document named by an array of primary keys. Keys may be sent as strings or numbers — a number keeps its exact decimal form, so an integer key round-trips as `42` and never as scientific notation. Keys that are absent from the index are skipped rather than failing the batch, so this is idempotent. A body that is not an array is 400. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
     * @summary Delete many documents by primary key in one call
     * @param {IndexApiCloudPostV1IndexIndexesByUidDocumentsDeleteBatchRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudPostV1IndexIndexesByUidDocumentsDeleteBatch(requestParameters: IndexApiCloudPostV1IndexIndexesByUidDocumentsDeleteBatchRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudPostV1IndexIndexesByUidDocumentsDeleteBatch(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers the documents in one index matching `q`, ranked by how many of the query\'s terms they match, with prefix matching so a partial word still finds its document. `limit` defaults to 20 and is capped at 1000, `offset` pages; a negative value falls back to the default rather than erroring.  `filter` takes a Meilisearch filter expression, or an array of them, and the `user = \"…\"` and `user IN […]` forms are honoured — that is how an app with many end users narrows results to one of them WITHIN the org. `estimatedTotalHits` is exact for the page returned, not an estimate, because every hit is materialised. An index the caller\'s org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.
     * @summary Search an index, forgiving typos
     * @param {IndexApiCloudPostV1IndexIndexesByUidSearchRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudPostV1IndexIndexesByUidSearch(requestParameters: IndexApiCloudPostV1IndexIndexesByUidSearchRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudPostV1IndexIndexesByUidSearch(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Upserts documents into one index, keyed by the index\'s primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400\'d. The index is created on demand, so a first write needs no create call.  This and the POST on the same path are the SAME operation, served by one handler. Both exist because the Meilisearch dialect has both verbs; there is no partial-update semantics on this one — a document is replaced whole either way. The tenant is the org minted from the VALIDATED bearer\'s owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named \"messages\" and neither can see the other\'s documents. Without a validated principal the answer is 403 carrying Meilisearch\'s `invalid_api_key` body. Errors use Meilisearch\'s {message, code, type, link} shape rather than cloud\'s, because that `code` is a wire contract a Meilisearch client branches on.  The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
     * @summary Add or update documents in an index
     * @param {IndexApiCloudPutV1IndexIndexesByUidDocumentsRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof IndexApi
     */
    public cloudPutV1IndexIndexesByUidDocuments(requestParameters: IndexApiCloudPutV1IndexIndexesByUidDocumentsRequest, options?: RawAxiosRequestConfig) {
        return IndexApiFp(this.configuration).cloudPutV1IndexIndexesByUidDocuments(requestParameters.uid, options).then((request) => request(this.axios, this.basePath));
    }
}

