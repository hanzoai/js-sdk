/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud\'s own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { CloudMeshServiceList } from '../models';
/**
 * MeshApi - axios parameter creator
 * @export
 */
export const MeshApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and edge-node reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
         * @summary Returns the Zero Trust edge services the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1MeshServices: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/mesh/services`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearerAuth required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * MeshApi - functional programming interface
 * @export
 */
export const MeshApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = MeshApiAxiosParamCreator(configuration)
    return {
        /**
         * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and edge-node reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
         * @summary Returns the Zero Trust edge services the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async cloudGetV1MeshServices(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<CloudMeshServiceList>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.cloudGetV1MeshServices(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['MeshApi.cloudGetV1MeshServices']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * MeshApi - factory interface
 * @export
 */
export const MeshApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = MeshApiFp(configuration)
    return {
        /**
         * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and edge-node reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
         * @summary Returns the Zero Trust edge services the caller\'s org owns.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        cloudGetV1MeshServices(options?: RawAxiosRequestConfig): AxiosPromise<CloudMeshServiceList> {
            return localVarFp.cloudGetV1MeshServices(options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * MeshApi - object-oriented interface
 * @export
 * @class MeshApi
 * @extends {BaseAPI}
 */
export class MeshApi extends BaseAPI {
    /**
     * Returns the Zero Trust edge services the caller\'s org owns.  One row per real ZT edge service tagged with the org\'s \"org-<org>\" role attribute: mtls is \"required\" when the service mandates end-to-end encryption and \"enabled\" otherwise (the fabric always mutually authenticates every link), and status is \"active\" because a listed service is a configured, dialable entry. A service tagged for another org, or tagged for none, is invisible here.  Unlike the network and edge-node reads this does NOT degrade: an unconfigured deployment answers 503 and an unreachable controller surfaces the upstream\'s status, so a mesh page never renders \"no services\" for a fabric it simply could not read.
     * @summary Returns the Zero Trust edge services the caller\'s org owns.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof MeshApi
     */
    public cloudGetV1MeshServices(options?: RawAxiosRequestConfig) {
        return MeshApiFp(this.configuration).cloudGetV1MeshServices(options).then((request) => request(this.axios, this.basePath));
    }
}

