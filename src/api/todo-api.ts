/* tslint:disable */
/* eslint-disable */
/**
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator\'s admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { IssueEdit } from '../models';
// @ts-ignore
import type { IssueHit } from '../models';
// @ts-ignore
import type { IssueHits } from '../models';
// @ts-ignore
import type { IssueView } from '../models';
// @ts-ignore
import type { NewIssue } from '../models';
// @ts-ignore
import type { TodoProject } from '../models';
/**
 * TodoApi - axios parameter creator
 * @export
 */
export const TodoApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {string} key 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteTodoProjectsByKey: async (key: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('deleteTodoProjectsByKey', 'key', key)
            const localVarPath = `/v1/todo/projects/{key}`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'DELETE', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
         * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
         * @param {string} [key] Key is the project whose issues to list, from the path. EMPTY means every project in the org — the global board. It is a filter like the rest of this struct rather than an address, which is what lets one op answer both \&quot;this board\&quot; and \&quot;all the work\&quot; without a second surface disagreeing with the first about what a column is.
         * @param {string} [status] Status keeps only issues in that board column: backlog, todo, in_progress, done or canceled. An unknown value is refused with 400.
         * @param {string} [kind] Kind keeps only work items of that shape: issue, pr or epic. An unknown value is refused with 400.
         * @param {string} [repo] Repo keeps only issues bound to that git repository.
         * @param {string} [label] Label keeps only issues carrying that label, compared case-insensitively.  This is how a board narrows to something SMALLER than a repository — the one mechanism for it. An estate whose apps are directories inside one repository (hanzoai/cloud carries ~140 of them) has no repository per app to address, so the app is a label: &#x60;label&#x3D;app/meet&#x60; is the meet board. Nothing is provisioned to make one exist; a board is the query.
         * @param {string} [source] Source keeps only issues opened from that surface: team, git, crm, helpdesk, cms or agent. An unknown value is refused with 400.
         * @param {boolean} [scheduled] Scheduled keeps only issues that carry a date — a start, a due date or both. This is the timeline\&#39;s slice of the board: pass scheduled&#x3D;true to get exactly the rows a gantt has somewhere to draw, instead of fetching every issue and discarding the undated ones client-side.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoBoard: async (key?: string, status?: string, kind?: string, repo?: string, label?: string, source?: string, scheduled?: boolean, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/todo/board`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (key !== undefined) {
                localVarQueryParameter['key'] = key;
            }

            if (status !== undefined) {
                localVarQueryParameter['status'] = status;
            }

            if (kind !== undefined) {
                localVarQueryParameter['kind'] = kind;
            }

            if (repo !== undefined) {
                localVarQueryParameter['repo'] = repo;
            }

            if (label !== undefined) {
                localVarQueryParameter['label'] = label;
            }

            if (source !== undefined) {
                localVarQueryParameter['source'] = source;
            }

            if (scheduled !== undefined) {
                localVarQueryParameter['scheduled'] = scheduled;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers across every project in the org.  The org comes from the validated principal and never from the request: a caller able to name the org could read another tenant\'s backlog, and a search is exactly the shape that would quietly return it.
         * @summary Answers across every project in the org.
         * @param {string} [q] Q matches an issue\&#39;s title or description. A word from the issue, which is what someone remembers — not its number, which is what they are looking up.
         * @param {string} [project] Project narrows to one team key; \&quot;\&quot; searches every project in the org, which is the point of this op.
         * @param {string} [status] Status keeps one board column: backlog, todo, in_progress, done, canceled.
         * @param {string} [kind] Kind keeps one shape: issue, pr, epic.
         * @param {string} [repo] Repo keeps issues bound to one git repository.
         * @param {string} [source] Source keeps one origin: team, git, crm, helpdesk, cms, agent. \&quot;git\&quot; is how you ask for the mirrored GitHub issues specifically.
         * @param {string} [assignee] Assignee keeps issues held by one person. Pass \&quot;me\&quot; for yourself.
         * @param {number} [limit] Limit caps the answer; 0 means the default, and anything above the ceiling is clamped rather than refused — a search that errors on being too broad teaches people to guess.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoIssues: async (q?: string, project?: string, status?: string, kind?: string, repo?: string, source?: string, assignee?: string, limit?: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/todo/issues`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (q !== undefined) {
                localVarQueryParameter['q'] = q;
            }

            if (project !== undefined) {
                localVarQueryParameter['project'] = project;
            }

            if (status !== undefined) {
                localVarQueryParameter['status'] = status;
            }

            if (kind !== undefined) {
                localVarQueryParameter['kind'] = kind;
            }

            if (repo !== undefined) {
                localVarQueryParameter['repo'] = repo;
            }

            if (source !== undefined) {
                localVarQueryParameter['source'] = source;
            }

            if (assignee !== undefined) {
                localVarQueryParameter['assignee'] = assignee;
            }

            if (limit !== undefined) {
                localVarQueryParameter['limit'] = limit;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns the boards of your org — the places your work actually is. The key addresses the board\'s issues.  A BOARD IS A PLACE WORK IS, not an object somebody provisioned. So the list is assembled from the work itself: the repositories your org has filed issues on, plus the boards the index holds. A repository with nothing on it is not in the list and is still perfectly addressable — GET /projects/<name> reads it and a create files into it — so nothing is lost by leaving it out.  Measured, which is why: reading the forge\'s whole repository inventory put 745 boards here, of which all but a handful were vendored forks and mirrors (.github, .profile, DOMPurify, BoatAttack) that will never carry this org\'s work. A list that long is not a list — the estate\'s real roadmap was in it somewhere and no one could see it.  The forge half is the FORGE\'s answer for your own account, so two people in one org can legitimately see different boards.
         * @summary Returns the boards of your org — the places your work actually is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjects: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/todo/projects`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns one board of your org by its key — the repository name. 404 when your org has no repository under that key, or when your own forge account cannot see it.
         * @summary Returns one board of your org by its key — the repository name.
         * @param {string} key Key is the project\&#39;s org-unique handle: 2-8 uppercase alphanumerics starting with a letter (\&quot;ENG\&quot;, \&quot;OPS2\&quot;). Matched case-insensitively.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjectsByKey: async (key: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('getTodoProjectsByKey', 'key', key)
            const localVarPath = `/v1/todo/projects/{key}`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
         * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
         * @param {string} key Key is the project whose issues to list, from the path. EMPTY means every project in the org — the global board. It is a filter like the rest of this struct rather than an address, which is what lets one op answer both \&quot;this board\&quot; and \&quot;all the work\&quot; without a second surface disagreeing with the first about what a column is.
         * @param {string} [status] Status keeps only issues in that board column: backlog, todo, in_progress, done or canceled. An unknown value is refused with 400.
         * @param {string} [kind] Kind keeps only work items of that shape: issue, pr or epic. An unknown value is refused with 400.
         * @param {string} [repo] Repo keeps only issues bound to that git repository.
         * @param {string} [label] Label keeps only issues carrying that label, compared case-insensitively.  This is how a board narrows to something SMALLER than a repository — the one mechanism for it. An estate whose apps are directories inside one repository (hanzoai/cloud carries ~140 of them) has no repository per app to address, so the app is a label: &#x60;label&#x3D;app/meet&#x60; is the meet board. Nothing is provisioned to make one exist; a board is the query.
         * @param {string} [source] Source keeps only issues opened from that surface: team, git, crm, helpdesk, cms or agent. An unknown value is refused with 400.
         * @param {boolean} [scheduled] Scheduled keeps only issues that carry a date — a start, a due date or both. This is the timeline\&#39;s slice of the board: pass scheduled&#x3D;true to get exactly the rows a gantt has somewhere to draw, instead of fetching every issue and discarding the undated ones client-side.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjectsByKeyIssues: async (key: string, status?: string, kind?: string, repo?: string, label?: string, source?: string, scheduled?: boolean, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('getTodoProjectsByKeyIssues', 'key', key)
            const localVarPath = `/v1/todo/projects/{key}/issues`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)

            if (status !== undefined) {
                localVarQueryParameter['status'] = status;
            }

            if (kind !== undefined) {
                localVarQueryParameter['kind'] = kind;
            }

            if (repo !== undefined) {
                localVarQueryParameter['repo'] = repo;
            }

            if (label !== undefined) {
                localVarQueryParameter['label'] = label;
            }

            if (source !== undefined) {
                localVarQueryParameter['source'] = source;
            }

            if (scheduled !== undefined) {
                localVarQueryParameter['scheduled'] = scheduled;
            }


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Returns ONE work item in full — its description included.  The list reads answer a board, and a board is a summary: the description is where the actual content of a work item lives — what an issue asks for, what an epic\'s acceptance criteria are — and no read on this surface returned it. The address is the one PATCH already accepts, so an item you can move is now an item you can read.  It reads the forge directly rather than filtering the org fan-out, then falls back to the index for a board the forge has never heard of — the same order, and the same reason, as GetProject: a row is one kind of thing however it came to exist, so a caller does not have to know which store it is in to fetch it.
         * @summary Returns ONE work item in full — its description included.
         * @param {string} key Key is the board — the repository name, or an index board\&#39;s key.
         * @param {number} num Num is the issue\&#39;s number on that board.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjectsByKeyIssuesByNum: async (key: string, num: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('getTodoProjectsByKeyIssuesByNum', 'key', key)
            // verify required parameter 'num' is not null or undefined
            assertParamExists('getTodoProjectsByKeyIssuesByNum', 'num', num)
            const localVarPath = `/v1/todo/projects/{key}/issues/{num}`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)))
                .replace(`{${"num"}}`, encodeURIComponent(String(num)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {string} key 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchTodoProjectsByKey: async (key: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('patchTodoProjectsByKey', 'key', key)
            const localVarPath = `/v1/todo/projects/{key}`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it. Absent fields are left alone.  MOVING A CARD IS A RELABEL. The column lives in the forge\'s label set, so the move replaces that set rather than writing a status column here that a forge-side change could contradict. Moving to `done` also CLOSES the issue on the forge, because a done card and an open issue are a contradiction.
         * @summary Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it.
         * @param {string} key Key is the board — the repository name, from the path.
         * @param {number} num Num is the issue number on that repository, from the path.
         * @param {IssueEdit} issueEdit 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchTodoProjectsByKeyIssuesByNum: async (key: string, num: number, issueEdit: IssueEdit, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('patchTodoProjectsByKeyIssuesByNum', 'key', key)
            // verify required parameter 'num' is not null or undefined
            assertParamExists('patchTodoProjectsByKeyIssuesByNum', 'num', num)
            // verify required parameter 'issueEdit' is not null or undefined
            assertParamExists('patchTodoProjectsByKeyIssuesByNum', 'issueEdit', issueEdit)
            const localVarPath = `/v1/todo/projects/{key}/issues/{num}`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)))
                .replace(`{${"num"}}`, encodeURIComponent(String(num)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'PATCH', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(issueEdit, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postTodoProjects: async (options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            const localVarPath = `/v1/todo/projects`;
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.  The column and priority are written as LABELS, which is what makes the card and the forge issue the same object: someone relabelling in the forge web UI has moved your card.
         * @summary Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.
         * @param {string} key Key is the board — the repository name, from the path.
         * @param {NewIssue} newIssue 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postTodoProjectsByKeyIssues: async (key: string, newIssue: NewIssue, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('postTodoProjectsByKeyIssues', 'key', key)
            // verify required parameter 'newIssue' is not null or undefined
            assertParamExists('postTodoProjectsByKeyIssues', 'newIssue', newIssue)
            const localVarPath = `/v1/todo/projects/{key}/issues`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            localVarHeaderParameter['Content-Type'] = 'application/json';

            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = serializeDataIfNeeded(newIssue, localVarRequestOptions, configuration)

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Takes an issue: it becomes yours and it moves to in_progress.  The holder is the CALLER, never an argument. \"Assign this to someone else\" is a different act with different authority, and it already exists as a PATCH; conflating them would let anyone hand work to anyone by naming them.  Claiming something already held by someone else is refused rather than silently taken — two agents on one issue is the failure this prevents, and a claim that quietly wins a race is worse than one that says no.
         * @summary Takes an issue: it becomes yours and it moves to in_progress.
         * @param {string} key 
         * @param {number} num 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postTodoProjectsByKeyIssuesByNumClaim: async (key: string, num: number, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'key' is not null or undefined
            assertParamExists('postTodoProjectsByKeyIssuesByNumClaim', 'key', key)
            // verify required parameter 'num' is not null or undefined
            assertParamExists('postTodoProjectsByKeyIssuesByNumClaim', 'num', num)
            const localVarPath = `/v1/todo/projects/{key}/issues/{num}/claim`
                .replace(`{${"key"}}`, encodeURIComponent(String(key)))
                .replace(`{${"num"}}`, encodeURIComponent(String(num)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication bearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * TodoApi - functional programming interface
 * @export
 */
export const TodoApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = TodoApiAxiosParamCreator(configuration)
    return {
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {string} key 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async deleteTodoProjectsByKey(key: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.deleteTodoProjectsByKey(key, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.deleteTodoProjectsByKey']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
         * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
         * @param {string} [key] Key is the project whose issues to list, from the path. EMPTY means every project in the org — the global board. It is a filter like the rest of this struct rather than an address, which is what lets one op answer both \&quot;this board\&quot; and \&quot;all the work\&quot; without a second surface disagreeing with the first about what a column is.
         * @param {string} [status] Status keeps only issues in that board column: backlog, todo, in_progress, done or canceled. An unknown value is refused with 400.
         * @param {string} [kind] Kind keeps only work items of that shape: issue, pr or epic. An unknown value is refused with 400.
         * @param {string} [repo] Repo keeps only issues bound to that git repository.
         * @param {string} [label] Label keeps only issues carrying that label, compared case-insensitively.  This is how a board narrows to something SMALLER than a repository — the one mechanism for it. An estate whose apps are directories inside one repository (hanzoai/cloud carries ~140 of them) has no repository per app to address, so the app is a label: &#x60;label&#x3D;app/meet&#x60; is the meet board. Nothing is provisioned to make one exist; a board is the query.
         * @param {string} [source] Source keeps only issues opened from that surface: team, git, crm, helpdesk, cms or agent. An unknown value is refused with 400.
         * @param {boolean} [scheduled] Scheduled keeps only issues that carry a date — a start, a due date or both. This is the timeline\&#39;s slice of the board: pass scheduled&#x3D;true to get exactly the rows a gantt has somewhere to draw, instead of fetching every issue and discarding the undated ones client-side.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getTodoBoard(key?: string, status?: string, kind?: string, repo?: string, label?: string, source?: string, scheduled?: boolean, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Array<IssueView>>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getTodoBoard(key, status, kind, repo, label, source, scheduled, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.getTodoBoard']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers across every project in the org.  The org comes from the validated principal and never from the request: a caller able to name the org could read another tenant\'s backlog, and a search is exactly the shape that would quietly return it.
         * @summary Answers across every project in the org.
         * @param {string} [q] Q matches an issue\&#39;s title or description. A word from the issue, which is what someone remembers — not its number, which is what they are looking up.
         * @param {string} [project] Project narrows to one team key; \&quot;\&quot; searches every project in the org, which is the point of this op.
         * @param {string} [status] Status keeps one board column: backlog, todo, in_progress, done, canceled.
         * @param {string} [kind] Kind keeps one shape: issue, pr, epic.
         * @param {string} [repo] Repo keeps issues bound to one git repository.
         * @param {string} [source] Source keeps one origin: team, git, crm, helpdesk, cms, agent. \&quot;git\&quot; is how you ask for the mirrored GitHub issues specifically.
         * @param {string} [assignee] Assignee keeps issues held by one person. Pass \&quot;me\&quot; for yourself.
         * @param {number} [limit] Limit caps the answer; 0 means the default, and anything above the ceiling is clamped rather than refused — a search that errors on being too broad teaches people to guess.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getTodoIssues(q?: string, project?: string, status?: string, kind?: string, repo?: string, source?: string, assignee?: string, limit?: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<IssueHits>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getTodoIssues(q, project, status, kind, repo, source, assignee, limit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.getTodoIssues']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns the boards of your org — the places your work actually is. The key addresses the board\'s issues.  A BOARD IS A PLACE WORK IS, not an object somebody provisioned. So the list is assembled from the work itself: the repositories your org has filed issues on, plus the boards the index holds. A repository with nothing on it is not in the list and is still perfectly addressable — GET /projects/<name> reads it and a create files into it — so nothing is lost by leaving it out.  Measured, which is why: reading the forge\'s whole repository inventory put 745 boards here, of which all but a handful were vendored forks and mirrors (.github, .profile, DOMPurify, BoatAttack) that will never carry this org\'s work. A list that long is not a list — the estate\'s real roadmap was in it somewhere and no one could see it.  The forge half is the FORGE\'s answer for your own account, so two people in one org can legitimately see different boards.
         * @summary Returns the boards of your org — the places your work actually is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getTodoProjects(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Array<TodoProject>>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getTodoProjects(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.getTodoProjects']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns one board of your org by its key — the repository name. 404 when your org has no repository under that key, or when your own forge account cannot see it.
         * @summary Returns one board of your org by its key — the repository name.
         * @param {string} key Key is the project\&#39;s org-unique handle: 2-8 uppercase alphanumerics starting with a letter (\&quot;ENG\&quot;, \&quot;OPS2\&quot;). Matched case-insensitively.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getTodoProjectsByKey(key: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<TodoProject>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getTodoProjectsByKey(key, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.getTodoProjectsByKey']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
         * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
         * @param {string} key Key is the project whose issues to list, from the path. EMPTY means every project in the org — the global board. It is a filter like the rest of this struct rather than an address, which is what lets one op answer both \&quot;this board\&quot; and \&quot;all the work\&quot; without a second surface disagreeing with the first about what a column is.
         * @param {string} [status] Status keeps only issues in that board column: backlog, todo, in_progress, done or canceled. An unknown value is refused with 400.
         * @param {string} [kind] Kind keeps only work items of that shape: issue, pr or epic. An unknown value is refused with 400.
         * @param {string} [repo] Repo keeps only issues bound to that git repository.
         * @param {string} [label] Label keeps only issues carrying that label, compared case-insensitively.  This is how a board narrows to something SMALLER than a repository — the one mechanism for it. An estate whose apps are directories inside one repository (hanzoai/cloud carries ~140 of them) has no repository per app to address, so the app is a label: &#x60;label&#x3D;app/meet&#x60; is the meet board. Nothing is provisioned to make one exist; a board is the query.
         * @param {string} [source] Source keeps only issues opened from that surface: team, git, crm, helpdesk, cms or agent. An unknown value is refused with 400.
         * @param {boolean} [scheduled] Scheduled keeps only issues that carry a date — a start, a due date or both. This is the timeline\&#39;s slice of the board: pass scheduled&#x3D;true to get exactly the rows a gantt has somewhere to draw, instead of fetching every issue and discarding the undated ones client-side.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getTodoProjectsByKeyIssues(key: string, status?: string, kind?: string, repo?: string, label?: string, source?: string, scheduled?: boolean, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<Array<IssueView>>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getTodoProjectsByKeyIssues(key, status, kind, repo, label, source, scheduled, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.getTodoProjectsByKeyIssues']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Returns ONE work item in full — its description included.  The list reads answer a board, and a board is a summary: the description is where the actual content of a work item lives — what an issue asks for, what an epic\'s acceptance criteria are — and no read on this surface returned it. The address is the one PATCH already accepts, so an item you can move is now an item you can read.  It reads the forge directly rather than filtering the org fan-out, then falls back to the index for a board the forge has never heard of — the same order, and the same reason, as GetProject: a row is one kind of thing however it came to exist, so a caller does not have to know which store it is in to fetch it.
         * @summary Returns ONE work item in full — its description included.
         * @param {string} key Key is the board — the repository name, or an index board\&#39;s key.
         * @param {number} num Num is the issue\&#39;s number on that board.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async getTodoProjectsByKeyIssuesByNum(key: string, num: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<IssueView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.getTodoProjectsByKeyIssuesByNum(key, num, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.getTodoProjectsByKeyIssuesByNum']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {string} key 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchTodoProjectsByKey(key: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchTodoProjectsByKey(key, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.patchTodoProjectsByKey']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it. Absent fields are left alone.  MOVING A CARD IS A RELABEL. The column lives in the forge\'s label set, so the move replaces that set rather than writing a status column here that a forge-side change could contradict. Moving to `done` also CLOSES the issue on the forge, because a done card and an open issue are a contradiction.
         * @summary Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it.
         * @param {string} key Key is the board — the repository name, from the path.
         * @param {number} num Num is the issue number on that repository, from the path.
         * @param {IssueEdit} issueEdit 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async patchTodoProjectsByKeyIssuesByNum(key: string, num: number, issueEdit: IssueEdit, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<IssueView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.patchTodoProjectsByKeyIssuesByNum(key, num, issueEdit, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.patchTodoProjectsByKeyIssuesByNum']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postTodoProjects(options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<void>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postTodoProjects(options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.postTodoProjects']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.  The column and priority are written as LABELS, which is what makes the card and the forge issue the same object: someone relabelling in the forge web UI has moved your card.
         * @summary Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.
         * @param {string} key Key is the board — the repository name, from the path.
         * @param {NewIssue} newIssue 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postTodoProjectsByKeyIssues(key: string, newIssue: NewIssue, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<IssueView>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postTodoProjectsByKeyIssues(key, newIssue, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.postTodoProjectsByKeyIssues']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Takes an issue: it becomes yours and it moves to in_progress.  The holder is the CALLER, never an argument. \"Assign this to someone else\" is a different act with different authority, and it already exists as a PATCH; conflating them would let anyone hand work to anyone by naming them.  Claiming something already held by someone else is refused rather than silently taken — two agents on one issue is the failure this prevents, and a claim that quietly wins a race is worse than one that says no.
         * @summary Takes an issue: it becomes yours and it moves to in_progress.
         * @param {string} key 
         * @param {number} num 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async postTodoProjectsByKeyIssuesByNumClaim(key: string, num: number, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<IssueHit>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.postTodoProjectsByKeyIssuesByNumClaim(key, num, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['TodoApi.postTodoProjectsByKeyIssuesByNumClaim']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * TodoApi - factory interface
 * @export
 */
export const TodoApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = TodoApiFp(configuration)
    return {
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {TodoApiDeleteTodoProjectsByKeyRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        deleteTodoProjectsByKey(requestParameters: TodoApiDeleteTodoProjectsByKeyRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.deleteTodoProjectsByKey(requestParameters.key, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
         * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
         * @param {TodoApiGetTodoBoardRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoBoard(requestParameters: TodoApiGetTodoBoardRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<Array<IssueView>> {
            return localVarFp.getTodoBoard(requestParameters.key, requestParameters.status, requestParameters.kind, requestParameters.repo, requestParameters.label, requestParameters.source, requestParameters.scheduled, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers across every project in the org.  The org comes from the validated principal and never from the request: a caller able to name the org could read another tenant\'s backlog, and a search is exactly the shape that would quietly return it.
         * @summary Answers across every project in the org.
         * @param {TodoApiGetTodoIssuesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoIssues(requestParameters: TodoApiGetTodoIssuesRequest = {}, options?: RawAxiosRequestConfig): AxiosPromise<IssueHits> {
            return localVarFp.getTodoIssues(requestParameters.q, requestParameters.project, requestParameters.status, requestParameters.kind, requestParameters.repo, requestParameters.source, requestParameters.assignee, requestParameters.limit, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns the boards of your org — the places your work actually is. The key addresses the board\'s issues.  A BOARD IS A PLACE WORK IS, not an object somebody provisioned. So the list is assembled from the work itself: the repositories your org has filed issues on, plus the boards the index holds. A repository with nothing on it is not in the list and is still perfectly addressable — GET /projects/<name> reads it and a create files into it — so nothing is lost by leaving it out.  Measured, which is why: reading the forge\'s whole repository inventory put 745 boards here, of which all but a handful were vendored forks and mirrors (.github, .profile, DOMPurify, BoatAttack) that will never carry this org\'s work. A list that long is not a list — the estate\'s real roadmap was in it somewhere and no one could see it.  The forge half is the FORGE\'s answer for your own account, so two people in one org can legitimately see different boards.
         * @summary Returns the boards of your org — the places your work actually is.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjects(options?: RawAxiosRequestConfig): AxiosPromise<Array<TodoProject>> {
            return localVarFp.getTodoProjects(options).then((request) => request(axios, basePath));
        },
        /**
         * Returns one board of your org by its key — the repository name. 404 when your org has no repository under that key, or when your own forge account cannot see it.
         * @summary Returns one board of your org by its key — the repository name.
         * @param {TodoApiGetTodoProjectsByKeyRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjectsByKey(requestParameters: TodoApiGetTodoProjectsByKeyRequest, options?: RawAxiosRequestConfig): AxiosPromise<TodoProject> {
            return localVarFp.getTodoProjectsByKey(requestParameters.key, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
         * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
         * @param {TodoApiGetTodoProjectsByKeyIssuesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjectsByKeyIssues(requestParameters: TodoApiGetTodoProjectsByKeyIssuesRequest, options?: RawAxiosRequestConfig): AxiosPromise<Array<IssueView>> {
            return localVarFp.getTodoProjectsByKeyIssues(requestParameters.key, requestParameters.status, requestParameters.kind, requestParameters.repo, requestParameters.label, requestParameters.source, requestParameters.scheduled, options).then((request) => request(axios, basePath));
        },
        /**
         * Returns ONE work item in full — its description included.  The list reads answer a board, and a board is a summary: the description is where the actual content of a work item lives — what an issue asks for, what an epic\'s acceptance criteria are — and no read on this surface returned it. The address is the one PATCH already accepts, so an item you can move is now an item you can read.  It reads the forge directly rather than filtering the org fan-out, then falls back to the index for a board the forge has never heard of — the same order, and the same reason, as GetProject: a row is one kind of thing however it came to exist, so a caller does not have to know which store it is in to fetch it.
         * @summary Returns ONE work item in full — its description included.
         * @param {TodoApiGetTodoProjectsByKeyIssuesByNumRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        getTodoProjectsByKeyIssuesByNum(requestParameters: TodoApiGetTodoProjectsByKeyIssuesByNumRequest, options?: RawAxiosRequestConfig): AxiosPromise<IssueView> {
            return localVarFp.getTodoProjectsByKeyIssuesByNum(requestParameters.key, requestParameters.num, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {TodoApiPatchTodoProjectsByKeyRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchTodoProjectsByKey(requestParameters: TodoApiPatchTodoProjectsByKeyRequest, options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.patchTodoProjectsByKey(requestParameters.key, options).then((request) => request(axios, basePath));
        },
        /**
         * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it. Absent fields are left alone.  MOVING A CARD IS A RELABEL. The column lives in the forge\'s label set, so the move replaces that set rather than writing a status column here that a forge-side change could contradict. Moving to `done` also CLOSES the issue on the forge, because a done card and an open issue are a contradiction.
         * @summary Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it.
         * @param {TodoApiPatchTodoProjectsByKeyIssuesByNumRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        patchTodoProjectsByKeyIssuesByNum(requestParameters: TodoApiPatchTodoProjectsByKeyIssuesByNumRequest, options?: RawAxiosRequestConfig): AxiosPromise<IssueView> {
            return localVarFp.patchTodoProjectsByKeyIssuesByNum(requestParameters.key, requestParameters.num, requestParameters.issueEdit, options).then((request) => request(axios, basePath));
        },
        /**
         * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
         * @summary Refused — a board is a repository on the forge
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postTodoProjects(options?: RawAxiosRequestConfig): AxiosPromise<void> {
            return localVarFp.postTodoProjects(options).then((request) => request(axios, basePath));
        },
        /**
         * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.  The column and priority are written as LABELS, which is what makes the card and the forge issue the same object: someone relabelling in the forge web UI has moved your card.
         * @summary Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.
         * @param {TodoApiPostTodoProjectsByKeyIssuesRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postTodoProjectsByKeyIssues(requestParameters: TodoApiPostTodoProjectsByKeyIssuesRequest, options?: RawAxiosRequestConfig): AxiosPromise<IssueView> {
            return localVarFp.postTodoProjectsByKeyIssues(requestParameters.key, requestParameters.newIssue, options).then((request) => request(axios, basePath));
        },
        /**
         * Takes an issue: it becomes yours and it moves to in_progress.  The holder is the CALLER, never an argument. \"Assign this to someone else\" is a different act with different authority, and it already exists as a PATCH; conflating them would let anyone hand work to anyone by naming them.  Claiming something already held by someone else is refused rather than silently taken — two agents on one issue is the failure this prevents, and a claim that quietly wins a race is worse than one that says no.
         * @summary Takes an issue: it becomes yours and it moves to in_progress.
         * @param {TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest} requestParameters Request parameters.
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        postTodoProjectsByKeyIssuesByNumClaim(requestParameters: TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest, options?: RawAxiosRequestConfig): AxiosPromise<IssueHit> {
            return localVarFp.postTodoProjectsByKeyIssuesByNumClaim(requestParameters.key, requestParameters.num, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * Request parameters for deleteTodoProjectsByKey operation in TodoApi.
 * @export
 * @interface TodoApiDeleteTodoProjectsByKeyRequest
 */
export interface TodoApiDeleteTodoProjectsByKeyRequest {
    /**
     * 
     * @type {string}
     * @memberof TodoApiDeleteTodoProjectsByKey
     */
    readonly key: string
}

/**
 * Request parameters for getTodoBoard operation in TodoApi.
 * @export
 * @interface TodoApiGetTodoBoardRequest
 */
export interface TodoApiGetTodoBoardRequest {
    /**
     * Key is the project whose issues to list, from the path. EMPTY means every project in the org — the global board. It is a filter like the rest of this struct rather than an address, which is what lets one op answer both \&quot;this board\&quot; and \&quot;all the work\&quot; without a second surface disagreeing with the first about what a column is.
     * @type {string}
     * @memberof TodoApiGetTodoBoard
     */
    readonly key?: string

    /**
     * Status keeps only issues in that board column: backlog, todo, in_progress, done or canceled. An unknown value is refused with 400.
     * @type {string}
     * @memberof TodoApiGetTodoBoard
     */
    readonly status?: string

    /**
     * Kind keeps only work items of that shape: issue, pr or epic. An unknown value is refused with 400.
     * @type {string}
     * @memberof TodoApiGetTodoBoard
     */
    readonly kind?: string

    /**
     * Repo keeps only issues bound to that git repository.
     * @type {string}
     * @memberof TodoApiGetTodoBoard
     */
    readonly repo?: string

    /**
     * Label keeps only issues carrying that label, compared case-insensitively.  This is how a board narrows to something SMALLER than a repository — the one mechanism for it. An estate whose apps are directories inside one repository (hanzoai/cloud carries ~140 of them) has no repository per app to address, so the app is a label: &#x60;label&#x3D;app/meet&#x60; is the meet board. Nothing is provisioned to make one exist; a board is the query.
     * @type {string}
     * @memberof TodoApiGetTodoBoard
     */
    readonly label?: string

    /**
     * Source keeps only issues opened from that surface: team, git, crm, helpdesk, cms or agent. An unknown value is refused with 400.
     * @type {string}
     * @memberof TodoApiGetTodoBoard
     */
    readonly source?: string

    /**
     * Scheduled keeps only issues that carry a date — a start, a due date or both. This is the timeline\&#39;s slice of the board: pass scheduled&#x3D;true to get exactly the rows a gantt has somewhere to draw, instead of fetching every issue and discarding the undated ones client-side.
     * @type {boolean}
     * @memberof TodoApiGetTodoBoard
     */
    readonly scheduled?: boolean
}

/**
 * Request parameters for getTodoIssues operation in TodoApi.
 * @export
 * @interface TodoApiGetTodoIssuesRequest
 */
export interface TodoApiGetTodoIssuesRequest {
    /**
     * Q matches an issue\&#39;s title or description. A word from the issue, which is what someone remembers — not its number, which is what they are looking up.
     * @type {string}
     * @memberof TodoApiGetTodoIssues
     */
    readonly q?: string

    /**
     * Project narrows to one team key; \&quot;\&quot; searches every project in the org, which is the point of this op.
     * @type {string}
     * @memberof TodoApiGetTodoIssues
     */
    readonly project?: string

    /**
     * Status keeps one board column: backlog, todo, in_progress, done, canceled.
     * @type {string}
     * @memberof TodoApiGetTodoIssues
     */
    readonly status?: string

    /**
     * Kind keeps one shape: issue, pr, epic.
     * @type {string}
     * @memberof TodoApiGetTodoIssues
     */
    readonly kind?: string

    /**
     * Repo keeps issues bound to one git repository.
     * @type {string}
     * @memberof TodoApiGetTodoIssues
     */
    readonly repo?: string

    /**
     * Source keeps one origin: team, git, crm, helpdesk, cms, agent. \&quot;git\&quot; is how you ask for the mirrored GitHub issues specifically.
     * @type {string}
     * @memberof TodoApiGetTodoIssues
     */
    readonly source?: string

    /**
     * Assignee keeps issues held by one person. Pass \&quot;me\&quot; for yourself.
     * @type {string}
     * @memberof TodoApiGetTodoIssues
     */
    readonly assignee?: string

    /**
     * Limit caps the answer; 0 means the default, and anything above the ceiling is clamped rather than refused — a search that errors on being too broad teaches people to guess.
     * @type {number}
     * @memberof TodoApiGetTodoIssues
     */
    readonly limit?: number
}

/**
 * Request parameters for getTodoProjectsByKey operation in TodoApi.
 * @export
 * @interface TodoApiGetTodoProjectsByKeyRequest
 */
export interface TodoApiGetTodoProjectsByKeyRequest {
    /**
     * Key is the project\&#39;s org-unique handle: 2-8 uppercase alphanumerics starting with a letter (\&quot;ENG\&quot;, \&quot;OPS2\&quot;). Matched case-insensitively.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKey
     */
    readonly key: string
}

/**
 * Request parameters for getTodoProjectsByKeyIssues operation in TodoApi.
 * @export
 * @interface TodoApiGetTodoProjectsByKeyIssuesRequest
 */
export interface TodoApiGetTodoProjectsByKeyIssuesRequest {
    /**
     * Key is the project whose issues to list, from the path. EMPTY means every project in the org — the global board. It is a filter like the rest of this struct rather than an address, which is what lets one op answer both \&quot;this board\&quot; and \&quot;all the work\&quot; without a second surface disagreeing with the first about what a column is.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKeyIssues
     */
    readonly key: string

    /**
     * Status keeps only issues in that board column: backlog, todo, in_progress, done or canceled. An unknown value is refused with 400.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKeyIssues
     */
    readonly status?: string

    /**
     * Kind keeps only work items of that shape: issue, pr or epic. An unknown value is refused with 400.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKeyIssues
     */
    readonly kind?: string

    /**
     * Repo keeps only issues bound to that git repository.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKeyIssues
     */
    readonly repo?: string

    /**
     * Label keeps only issues carrying that label, compared case-insensitively.  This is how a board narrows to something SMALLER than a repository — the one mechanism for it. An estate whose apps are directories inside one repository (hanzoai/cloud carries ~140 of them) has no repository per app to address, so the app is a label: &#x60;label&#x3D;app/meet&#x60; is the meet board. Nothing is provisioned to make one exist; a board is the query.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKeyIssues
     */
    readonly label?: string

    /**
     * Source keeps only issues opened from that surface: team, git, crm, helpdesk, cms or agent. An unknown value is refused with 400.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKeyIssues
     */
    readonly source?: string

    /**
     * Scheduled keeps only issues that carry a date — a start, a due date or both. This is the timeline\&#39;s slice of the board: pass scheduled&#x3D;true to get exactly the rows a gantt has somewhere to draw, instead of fetching every issue and discarding the undated ones client-side.
     * @type {boolean}
     * @memberof TodoApiGetTodoProjectsByKeyIssues
     */
    readonly scheduled?: boolean
}

/**
 * Request parameters for getTodoProjectsByKeyIssuesByNum operation in TodoApi.
 * @export
 * @interface TodoApiGetTodoProjectsByKeyIssuesByNumRequest
 */
export interface TodoApiGetTodoProjectsByKeyIssuesByNumRequest {
    /**
     * Key is the board — the repository name, or an index board\&#39;s key.
     * @type {string}
     * @memberof TodoApiGetTodoProjectsByKeyIssuesByNum
     */
    readonly key: string

    /**
     * Num is the issue\&#39;s number on that board.
     * @type {number}
     * @memberof TodoApiGetTodoProjectsByKeyIssuesByNum
     */
    readonly num: number
}

/**
 * Request parameters for patchTodoProjectsByKey operation in TodoApi.
 * @export
 * @interface TodoApiPatchTodoProjectsByKeyRequest
 */
export interface TodoApiPatchTodoProjectsByKeyRequest {
    /**
     * 
     * @type {string}
     * @memberof TodoApiPatchTodoProjectsByKey
     */
    readonly key: string
}

/**
 * Request parameters for patchTodoProjectsByKeyIssuesByNum operation in TodoApi.
 * @export
 * @interface TodoApiPatchTodoProjectsByKeyIssuesByNumRequest
 */
export interface TodoApiPatchTodoProjectsByKeyIssuesByNumRequest {
    /**
     * Key is the board — the repository name, from the path.
     * @type {string}
     * @memberof TodoApiPatchTodoProjectsByKeyIssuesByNum
     */
    readonly key: string

    /**
     * Num is the issue number on that repository, from the path.
     * @type {number}
     * @memberof TodoApiPatchTodoProjectsByKeyIssuesByNum
     */
    readonly num: number

    /**
     * 
     * @type {IssueEdit}
     * @memberof TodoApiPatchTodoProjectsByKeyIssuesByNum
     */
    readonly issueEdit: IssueEdit
}

/**
 * Request parameters for postTodoProjectsByKeyIssues operation in TodoApi.
 * @export
 * @interface TodoApiPostTodoProjectsByKeyIssuesRequest
 */
export interface TodoApiPostTodoProjectsByKeyIssuesRequest {
    /**
     * Key is the board — the repository name, from the path.
     * @type {string}
     * @memberof TodoApiPostTodoProjectsByKeyIssues
     */
    readonly key: string

    /**
     * 
     * @type {NewIssue}
     * @memberof TodoApiPostTodoProjectsByKeyIssues
     */
    readonly newIssue: NewIssue
}

/**
 * Request parameters for postTodoProjectsByKeyIssuesByNumClaim operation in TodoApi.
 * @export
 * @interface TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest
 */
export interface TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest {
    /**
     * 
     * @type {string}
     * @memberof TodoApiPostTodoProjectsByKeyIssuesByNumClaim
     */
    readonly key: string

    /**
     * 
     * @type {number}
     * @memberof TodoApiPostTodoProjectsByKeyIssuesByNumClaim
     */
    readonly num: number
}

/**
 * TodoApi - object-oriented interface
 * @export
 * @class TodoApi
 * @extends {BaseAPI}
 */
export class TodoApi extends BaseAPI {
    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * @summary Refused — a board is a repository on the forge
     * @param {TodoApiDeleteTodoProjectsByKeyRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public deleteTodoProjectsByKey(requestParameters: TodoApiDeleteTodoProjectsByKeyRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).deleteTodoProjectsByKey(requestParameters.key, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
     * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
     * @param {TodoApiGetTodoBoardRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public getTodoBoard(requestParameters: TodoApiGetTodoBoardRequest = {}, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).getTodoBoard(requestParameters.key, requestParameters.status, requestParameters.kind, requestParameters.repo, requestParameters.label, requestParameters.source, requestParameters.scheduled, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers across every project in the org.  The org comes from the validated principal and never from the request: a caller able to name the org could read another tenant\'s backlog, and a search is exactly the shape that would quietly return it.
     * @summary Answers across every project in the org.
     * @param {TodoApiGetTodoIssuesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public getTodoIssues(requestParameters: TodoApiGetTodoIssuesRequest = {}, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).getTodoIssues(requestParameters.q, requestParameters.project, requestParameters.status, requestParameters.kind, requestParameters.repo, requestParameters.source, requestParameters.assignee, requestParameters.limit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns the boards of your org — the places your work actually is. The key addresses the board\'s issues.  A BOARD IS A PLACE WORK IS, not an object somebody provisioned. So the list is assembled from the work itself: the repositories your org has filed issues on, plus the boards the index holds. A repository with nothing on it is not in the list and is still perfectly addressable — GET /projects/<name> reads it and a create files into it — so nothing is lost by leaving it out.  Measured, which is why: reading the forge\'s whole repository inventory put 745 boards here, of which all but a handful were vendored forks and mirrors (.github, .profile, DOMPurify, BoatAttack) that will never carry this org\'s work. A list that long is not a list — the estate\'s real roadmap was in it somewhere and no one could see it.  The forge half is the FORGE\'s answer for your own account, so two people in one org can legitimately see different boards.
     * @summary Returns the boards of your org — the places your work actually is.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public getTodoProjects(options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).getTodoProjects(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns one board of your org by its key — the repository name. 404 when your org has no repository under that key, or when your own forge account cannot see it.
     * @summary Returns one board of your org by its key — the repository name.
     * @param {TodoApiGetTodoProjectsByKeyRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public getTodoProjectsByKey(requestParameters: TodoApiGetTodoProjectsByKeyRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).getTodoProjectsByKey(requestParameters.key, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.  WHICH board is a filter, not an address. Bound to a repository (the key from the path) it is that project\'s board; left unbound it is the org\'s whole board; narrowed by label it is a board smaller than any repository — which is the only way an app that lives as a directory inside a shared repository can have one. Every combination is the same rows through the same projection, so no two boards can disagree about what a column means.  The column is a LABEL on the forge, so the board and the forge web UI are the same object seen twice: relabelling in either moves the card in both. A closed issue reads as done whatever its labels say.
     * @summary Returns a board\'s issues — work items with their column, priority, assignee, labels and schedule.
     * @param {TodoApiGetTodoProjectsByKeyIssuesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public getTodoProjectsByKeyIssues(requestParameters: TodoApiGetTodoProjectsByKeyIssuesRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).getTodoProjectsByKeyIssues(requestParameters.key, requestParameters.status, requestParameters.kind, requestParameters.repo, requestParameters.label, requestParameters.source, requestParameters.scheduled, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Returns ONE work item in full — its description included.  The list reads answer a board, and a board is a summary: the description is where the actual content of a work item lives — what an issue asks for, what an epic\'s acceptance criteria are — and no read on this surface returned it. The address is the one PATCH already accepts, so an item you can move is now an item you can read.  It reads the forge directly rather than filtering the org fan-out, then falls back to the index for a board the forge has never heard of — the same order, and the same reason, as GetProject: a row is one kind of thing however it came to exist, so a caller does not have to know which store it is in to fetch it.
     * @summary Returns ONE work item in full — its description included.
     * @param {TodoApiGetTodoProjectsByKeyIssuesByNumRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public getTodoProjectsByKeyIssuesByNum(requestParameters: TodoApiGetTodoProjectsByKeyIssuesByNumRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).getTodoProjectsByKeyIssuesByNum(requestParameters.key, requestParameters.num, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * @summary Refused — a board is a repository on the forge
     * @param {TodoApiPatchTodoProjectsByKeyRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public patchTodoProjectsByKey(requestParameters: TodoApiPatchTodoProjectsByKeyRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).patchTodoProjectsByKey(requestParameters.key, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it. Absent fields are left alone.  MOVING A CARD IS A RELABEL. The column lives in the forge\'s label set, so the move replaces that set rather than writing a status column here that a forge-side change could contradict. Moving to `done` also CLOSES the issue on the forge, because a done card and an open issue are a contradiction.
     * @summary Edits a work item — rename it, rewrite it, move it to another column, or re-prioritise it.
     * @param {TodoApiPatchTodoProjectsByKeyIssuesByNumRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public patchTodoProjectsByKeyIssuesByNum(requestParameters: TodoApiPatchTodoProjectsByKeyIssuesByNumRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).patchTodoProjectsByKeyIssuesByNum(requestParameters.key, requestParameters.num, requestParameters.issueEdit, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Answers 405. A todo board IS a repository on this deployment\'s forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.  Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: \"not this service\'s job\" and \"no such thing\" are different facts, and the body names the forge so a caller knows where the job IS done.  What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
     * @summary Refused — a board is a repository on the forge
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public postTodoProjects(options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).postTodoProjects(options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.  The column and priority are written as LABELS, which is what makes the card and the forge issue the same object: someone relabelling in the forge web UI has moved your card.
     * @summary Opens a work item on the board — an issue on that repository on the deployment\'s forge, filed as YOU.
     * @param {TodoApiPostTodoProjectsByKeyIssuesRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public postTodoProjectsByKeyIssues(requestParameters: TodoApiPostTodoProjectsByKeyIssuesRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).postTodoProjectsByKeyIssues(requestParameters.key, requestParameters.newIssue, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Takes an issue: it becomes yours and it moves to in_progress.  The holder is the CALLER, never an argument. \"Assign this to someone else\" is a different act with different authority, and it already exists as a PATCH; conflating them would let anyone hand work to anyone by naming them.  Claiming something already held by someone else is refused rather than silently taken — two agents on one issue is the failure this prevents, and a claim that quietly wins a race is worse than one that says no.
     * @summary Takes an issue: it becomes yours and it moves to in_progress.
     * @param {TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest} requestParameters Request parameters.
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TodoApi
     */
    public postTodoProjectsByKeyIssuesByNumClaim(requestParameters: TodoApiPostTodoProjectsByKeyIssuesByNumClaimRequest, options?: RawAxiosRequestConfig) {
        return TodoApiFp(this.configuration).postTodoProjectsByKeyIssuesByNumClaim(requestParameters.key, requestParameters.num, options).then((request) => request(this.axios, this.basePath));
    }
}

